---
type: source
source-type: pdf
title: "Linux高性能服务器编程"
path: books/Linux高性能服务器编程.pdf
size: 33126 KB
category: book
ingested: 2026-06-08
tool: liteparse
liteparse-version: 2.0.5
source-md5: 1cc1016092121f1b48d5750147a95e75
ocr-applied: true
---

# Linux高性能服务器编程

> Ingested from `/private/tmp/lfs_pdfs/books/Linux高性能服务器编程.pdf` via `lit parse` on 2026-06-08.
> Source file: 32.35 MB.

## Page 1

T Linux 服 务 器 编程 领域 的 经 典 著作，资深Linux 软件开发 工程师撰写
        从 网 络 协议 、服 务 器 编程 核心要素、  理 制、
                                  原
            机                          工 具 框 架 等 多 角度 全 面 编    =|
   科技 写 高 性 能Linux 服 务 器 应用的 方法、 巧和思想  ，包 含 两个   综合 性 案例 ，极 具  iy
  华章                                技
        实战 意义

                                           附 赠 项     目     源 代码










                                           游 双     #

        High Performance    Linux Server Programming

        L i
        性              能                          编程
        局                   服务




   *  机                     工
                            械 业 出 版 社
  cr  China                 Machine Press

## Page 2

    一

  a    51CT0.c0m 联合 策划
            成就梦想
              技术


        面向    服务 器 的   应  用开发   一直 是    Linux          软件        开发 领域  的重点， 而 Linux高性    能         服务 器应用 的
       开发 则 是 重点 中 的 难点    ， 软 件 开 发 工程             师   们 不 仅   需要 深入 钻研 很    多    理论 技术    ，      还 需要 在 实际
       的 生产 环境 中 积    经验。    因此，     不仅            这 方面 的 从    业   大 员   奇 缺 ，而 且 这 方面    的   图  书  资料  也 极为
          ，                                               。        从                                     的
       少见 本 书 算是 弥补 了 这      个 领域 市 场 的 空白                    作 者 将 自己 多 年 来     事 Linux 服    务 器   端 编程   经
       验 进行    了总结  和 梳理  ，从 原理 到 实践               ，从 底层 的    网   络 协议   到   上 层 的 服务 器    性  能  优化 与 监测 ，
       系统 地 讲解   了  开发 高  性能           应 服务 器 端    用 的  方法     、技 巧 和   编程 思想 。
        全 书    一共 分 为  三  大 部 人 ，      部    第 三    分 对        服务 器 编程 的      核心基础 一      一TCP/IP    协 议 进 行了
          的    解读和阐述  ;第                    性                                        了
i,     深刻 二 部 分 对                                           高 能服务 器 编程 的 核心 要 素 进 行   深入 且 全 面 的 剖析   ，    原
       理、 技 术 与 方法  并 重;第    三  部 分 从 侧重 实战               的        角度 讲解 了高 性 能  服务   器 的     优化 与 监测。
              下
       主要 包含 以     知识 点   :
                          协议族、TCP协          、IP                  协议 的 深刻
 1        1    TCPHP                   议                             解读;
              TCP/IP          ;     访问Internet                     Web    务
          2 经 典            实 战 案例    上 的                      服 器     ;
 y        3 Linux 网络 编程      API          和 高 级           的 全 面剖析   |;
          4 Linux   服 务 器 程序       规范、  高 性 能 服务 器 程序 框架      的              深刻  解读  ;
  ，       5     MO 复  用、信 号  、    定 时 器 、    高      性   能 WO   框架 库 的 详细 讲解   ，
         6 多 进程 编程         、 多 线程 编程        、      进 程    池和   线程    池 的  深入 剖析
        ( 7 ) 服务 器 的 调制、调 试 与 测试 ，以 及 各 种 主流 性 能 检测 工具 的 详细 使用方法 。

        书     个            均衡   服务                 程序         实际项 源                    代码。              CT
       本 附 赠 一     负载                   器              的      完整                        目的



 q
 )                                                                                                  |

      f.
                                                                                     ISBN        978-7-111-42519-9






  客服 热线: 010 88378991 88361066 | 数字 阅读:www.hzmedia.com.cn 1 >
        88379649 689952 |re
        华章 网 wwwhzbook.com 定价 69.00 一
i 26294 站: www.china-pub.com : 元

## Page 3

   High aerformance Linux Server FP
   L
[| 性 能      编                     程
                                 服务
高      本



it MF










个  工
   机 械         业 出 版 社
   China Machine Press

## Page 4

   书    版  编目
 图     在         〈CIP)  数据

 Linux 高性能服务 器       编程    游 双 著   一 北京 :  机  出版社
                          .               械工业 ，2013.5

 ISBN 978-7-111-42519-9

                TIL  Linux   操作 系统   IV. TP316.89

 中 国版本 图 书馆 CIP           2013 第       号
                                       101235

                     SE
 版权所 有            HALA
                      Be
                                   书法 ”北京
 本       律顾问                     市展达律师事务所
                                                                                            务
     本 书 是 Linux         服务 器 编程 领域 的经典 著  作  ，由 次 Linux 软件开发 工程 师        撰写 ，从 网 络 协议    、 服
           要素、原
                      理机制、   工   具     框 架等多 角度 全 面 阅释了 编写 商 性 能 Linux       服务 器 应 用 的 方法 、技
 器 编程                                                                                       巧
 和思想。    不 仅 理论 全     面 、这   ，抓    住 了重点   和  难点 ，还 包含 两个综 台 性 案例 ，极 具 实 战总区。
     全 书 共 17 章 ，分 为3个 部分     :     第  一 部 分对 Linux 服务 器    编程 的 核心 基础   一一          协议 进行了 深
                                   协议
                       TCP/IP       族      协议 ，以 及 一 个    经典的   TCPVIP   通信 案例 ;  第 二 部 分 对 高
                                       售    Linux         网     络 编程API、 商
                                                                          级  IO 国
 性 能服务 器 编程     的       要 素 进 行了   全 面     的 剖析  ，包                          数       ，Linux 服
                                             用   、情
 务 器   程序 规范、   高  性  能服务 器 程序 框架 、LO 复     号、    定     时     、高 性 能 IO 框架
                                                           器                 库Libevent、  多 进程
                                                 理、技 术与 方法
 编程    、  多 线程 编程   、 进 程 地  和 线程    等 内   容， 原           并重 ，  第 三 部    分从 侧重 实战 的角       讲解
了 高    性 能服务 器 的 优化 与 监测     ，   包 售   务 器  的 调制  、调 试 和  ，    以 及 各 种 实用 系统 监测   工具 的  使用  等
Ae
     本 书 另外    免费  赠送   一  个负载均衡服务 器 程序的完整实际 项 目的源 代码         !










 机 械       出版社                         22              编码
     工业 北 京 市             区    百 万     庄  天 街  号       邮政  100037
 责任 编辑          孙 诲 亮
 北京 市        盛  彩色   印刷    有   限 公司印 刷
 2013  年 6   月  第 1  版第 1 次 印刷
 186mm 240mm         * 22.5 Ells
 标准 书号      :  ISBN 978-7-111-42519-9
                     元
 定     fff:    69.00

 凡 购 本 书    ，如 有 献     页 便 页  、脱 页 ，由 本 社 发 行 部调换
             COLO)  88378991  88361066               A: COLO) 883979604
    544k:    (O10)  68326294  88379649    68995259   # hzjsj@hzbook.com

## Page 5

 cw.




 为   什么
             要 写 这 本     书

     长篇大论，恨
目前国内 计算 机 书籍 的 一 个明显 弊病 就是内 容 宽泛 而 空洞 。很 多 书籍 不
     读者
 得       括所有 最 新     的 技术，     连    一 个   最基本     的 技术 细节 也 无 法 解释 清楚。      有 些 书 籍 给     展
 现 的 是 网 络       上随处    可 见 的 知识    ， 基 本 没有 自己 的 观点     ，甚     至  连 一 点 自己 的 总 结 都 没有 。  反
 观 大 师们 的      经  典 书 籍   ，整 本 书    只 专注 于  一     个 问题 ，而 且 对 每 个技术 细节 的 描述 都是精           细
               关键 我 们 在 阅读 这 些              典  时         乎 是    用 心  与   一位    高       交流 这
 琢 。最            的是，                    经 书籍 ，似           在      编程 手 ，                   绝
 对 是    一 种享受    。
         我们 把问题 缩小 到 计算 机 网 络 编程 领域           。       关 于 计算 机 网 络 编程 的    相关 书籍 ，   不得 不 提
 的 是 已    故  网   络 教 育    巨匠                  先生
                 W     Richard Stevens                的《TCPAP      MH)                   WR
                 本 )。作 为一名                    网 络 程序  员  ， 即    使 没有阅读 过 这 几 本    书 ，也 应 该听
 《UNIX    网  络 编程           》( 两
   过             容 实在是太 庞大                            没有醒     的读者 根本         可 能 把 它们 全 部 读
 说
             它们 。但 这 几 本 书 中 的 内                  了   ，        不
 完 。 而 且 对 于 英文    不    太 好 的 朋友 来      说   ，选 择 阅  读 其 翻译 版 本又有 失 原 汁原味 。
     以上两点原因，                            了这本               》。        笔者
                          基于 笔 者 编写 《Linux 高 性 能服务 器 编程                本 书 是     多 年    来 学
 习 网 络 编程       之 总   结 ， 是 在 充分 理解     大   师 的 作品 并 融 人 自己 的 理解    和  见解 后 写成    的  。本 书讨论
 的 主题    和  定位 很 明确     。 简 单   来   说 就 是 :  如 何通过    各 种 手段    编写高  性 能 的 服务 器 程序  。
                                              如       提供
           络 技术 是 在 不 断 向     前 发 展         比             的      机 制 就 是    在内
     网                                的   ，     Linux     epoll        核      2.6   版本之 后 才
     是，编 思想却可以 享用一 辈子。我们 在 不 断 学 习 并 使用新 技术，不
 正式 引入         的 。但      程                                                              断运应
           的     ，                            我           看 得     ，想
                                                          得更多。     者相信，
                                                                               笔          没
 新 环境                                                     有谁
             同时     书 中 提 到 的 网 络 编程 思想 能 让               们   更    远
 会 认为          Richard Stevens  先生
     W                                的 网   络     书籍过时 了     。

     对
 读者     象

     本书之前，       需要                                       ，      一定的                     和
阅读 读 者 了 解 基本 的计算 机 网 络 知识 并具有 Linux 系统 编程
 C++      编程   基础 ，   否 则 阅读 起 来 会    有 些 困   难    。本 书 读者 对 象主要 包括    ;

## Page 6

    IV



    CY   Linux   网     络应用 程序   开发 人员
   L) Linux 系统 程序 开发人员
   加 C/CH 程序开发人员
   中 对 网 络 编程技术 感 兴趣，或希望 参与 网络 程序开发 的人员
    口 开设 相关 课程 的     大     专 院 校    师  生

    本 书 特 色

    本 书 的 特点 :       不 求 内 容 宽泛       ，但  求专      而       ，深 人 地 剖析 服务 器 编程                     的    要      素                 不
                                                     精                                                          ，          求内容精
    准 ， 但 求 融入笔者自己 的 理解和              观点 ，    谓            ”       器           。
                     可 “ 另 眼 看服务
                                                                   编程
    如 何 提高 服务 器 程序 性 能 是 本 书     要 着 重 讨论                     。 第                                                             等
                                                           的       6、8、9、11、12、15、16                                   章中都
    了                论  。                            :        ，                        程序                          一
    用 相当 的 篇 幅讨      这     一    主题        其 论述 方法    是    首先     探 讨 提 高 服务 器                       性 能            的 般 原 则    ，
                   ”    以 牺牲
    比如 使用“ 池         空间换取效率，    使               用  零 找 贝    函数          以 避免 内 核    和                   用 户 空间  的    切换 等 ，   其
    次 ，介 绍  一   些 高     效 的 编程  模式 及 其 应      ， 比         使用有    状态            分                                ，     用       池
                     用                          如 限                机 来           析 用 户                      数据             使 进程
    或 线程池 来处理 用 户 请     求 ，     最后，      探讨 如 何 通过 调整           系统     参数 来 从服务 器 程序 外 部 提高                                  其整
    体 性 能 。
        说 不 练 假 把     式     。 如 果 没有实例    ，或      者 只 是 给 出几           个“Hello World”，                  那       么  本 书  就    真没
    有 出 版的 必要 了      笔      者要做 的  是让 读者 能 真正             把理论       和 实践 完美    地    结合 起                     。 在       本书之
                   。                                                                                    来                    写作
    前 ，笔    者 阅读 了 不 少 开 源 社区 的 优秀服务 器 软件                 的    源 代   码 ，自 己 也 写 过 相当 多 的小 型                            服务     器
    程序 。这 些 软件 中 那些最精彩 的 部     分          ，在 书          中 都 有  充分 的 体现         。比   如  第        15      章 给 出       的   两    个实
例 一用 进程池 实 现 的 简单 CGI 服务器和 用 线程 池 实 现 的 简单Web 服务
    一                                                                                                       器， 就 充分     展现    了
    如何 利用 各 种 提高 服务器 性 能 的 手段 来 高效                地  解决    实际        问题 ，
        外，    为  了 帮助 读者 进一步 把 书 中 的 知识 融 汇 到 实际                   项     目 中 ，    笔    者        特意 编写 了还               一    个 较
    此
    为完整的负载均衡 服务 器 程序    springsnail。          该 程序         能   从 所有          服务       器    中    选取 负荷 最                小      一 的
                     中，                         使         用了 进程
    台 来处理 新 到 的 客户 连接。在 这 个 程序                                   池、            有 限 状 态     、    高           效             数 据结构
                                                                                           机                                 来提
    其性能 ，同
    高            时，    细 致    地封装  了 每个  函数 和 模块           ，   使之更 符合 实际 工程 项                           目    。      由 于 篇 幅 的 限
    制 ，笔者未 将 该程序 的 源代码 列 在 书     中    ，                  读 者 可 从 华章 网 站 es上 下 载 它    。
    何
    如     阅   读本 书

    本 书 分 为      三 篇    ，
                篇 〈
    第 一          第 1     4 章 )  介绍 TCP/IP                        协议 族 及 各 种 重要 的 网 络 协     。    只 有很               好       地 理解
                     一                                                                议                                       了
        通信的过程，才 能编写     出     高质量 的网络应用 程序 。毕
    底层 TCP/IP                                                                        竟，坚 实                  的          基础 设施 造就
              的上层建筑。
    稳固
    第 二篇 (第5         一    15  章 ) 细致 地      了 服务 器 编程            的   各 主要 方面           ，        其     中 对       每 个 重要      的 概

    CO     华章 网站 www.hzbook.com。          一 编辑
                     一                      注

## Page 7

    V


念 、模 型 以及函数 等 都 以 实例代码 的 形式 加 以 阐述。这 一 篇又可 细 分为如 下 四 个 部 分 ;
         第 一       部分 〈  第  5       一7  )章  介绍  Linux 操作 系统 为 网 络     编程  提供 的       众多  API。     些
                                                                                                这   API
         就 像是基本 的 音符 ，我                   们 通过 组 织     来谱写 优美 的 旋律       。
         第 二    部分  〈 第  8      )  章  探讨 高     性能服务 器 程序   的  一    般 框架  。在 这    一 部 分  中   ，    我们 将服务
         器 程序解构       为 IO     单元          、  辑 单元  和 存储 单元   三 个 部   件  ，并 重点 介绍 了      VO Moc,      B
         辑 单 元   的  几 种  高  效            实现 模 。 此
                                            式   外，       我们 还 探讨    了提高 服务 器 性 能        的 其他 建议  。
         第 三部分 (第        9    一     12     章)  深入 剖析服务 器      程序    的 IO 单元 。     我们 将 探讨    IO       需
                                                                                                     单元
         要 处 理 的    VO  事件 、信和号 事件          和 定时 事件        ， 并 介绍    一 款  优 秀    的 开源 IO        框架库 一 一
         Libevent.
                      8  13        ~ 15     章)     深入 剖析 服务 器 程序    的 逻辑 单元 。    这 一      部 分 我 们 要讨论 多
         线程 、多 进程 编程                ，以  及 高 性 能 逻辑    处理 模 型      一   进程 池和 线程       ，    并 给 出      的实
                                                               一                     池               相应
         PARES.
       第 三篇  〈 第   16 一     17  章 )     探讨    如 何 从 系统 的 角度   优化    和 监测 服务 器 性 能        。本 篇   的 内 容 涉
    及 服务   程 序 的     调制 、       试  调 和 测试     ，以  及 诸多 常用 系统 监测    工具    的 使   用 。

         和
    勘误
              支持

       由 于 作者 的 水 平         有      限 ，加 之 编写时间 人           书 中 难免 会    出   现 一 些 错误 或    者   不   准确 的 地
    方 恳请
           读者
    ，        批评指正。                 书 中 的 全 部 源 文件 都    可以  从       华章 网站 下 载   。 如 果 您    有  更 多 的 宝贵 意
见 或 建议 ，也 欢迎 发 送 邮件 至 邮箱 pmjhq87@gmailcom， 期 待能 够 得 到 您 的 真挚反馈 。

    致谢

       首先 要 感谢伟大 的 网 络 教育导师                 W     + Richard Stevens 先生 ，           他 的 书籍 帮助 了 无 数的 网 络
    程序开发人    员 ，              也 给 笔者 指明 了 学 习   的 道路 。
    感谢 机 械     工   业 出 版           社 华章 公司 的    编辑  杨 福 川   老师    和 孙 海 亮 老师 ，    是 他们 在 这 两     年多 的 时
    间 中 始终 文持着 我 的 写作 ，因 为有了 他              们 的 鼓励      、     助和      ，
                                                          帮
                                                                  引导     笔 者 才能 顺利 完成 全 部 书稿        。
           好友史正政            ， 他 对         编程 充满 热爱 ，  也 无 私地 为 本 书 提供 了     原材料    。
   感谢
         以 此 书 献                   给 我最亲爱 的家人 和 朋友 ，  以 及 那些 为 计算 机 网 络        教育  做 出    巨大 贡献   的 大 师
                                                               的    IT 工程
    们，还 有 正在 为自己 的 未 来 努力拼搏 、                    充 满 朝气     和 活力      师们         !



                                                                                     于 北京

## Page 8

    加    -~中


    3  AIX  系统     E
  UNIX                构建高可 用
   Dit     i       3  LinuXx 服 务 器




      UNIX
 AIX            系统:     管理 、    维 护 与 高       可用 集群    建设
                                                   元
      作者:     文平 ISBN:   978-7-111-35951-7 定价: 78.00
          资深       UNIX  系 统专家        数 据 库专家
                             、               、
         构                 工 经 验      结晶，  系
                                            统、    全
  系统       师 近  20 年 一 线     作                    面、实    用
                                      部 管 维护 性 优化
  紧 扣 实际 生产 环境  ，从系统         、    工 程 署、     理     、     能  、
高 可 用 环境 建设等多 角度 详细 探讨 AIX 系 统 的 管理和 运 维 的 方法与最 佳 实践

                构建         可            Linux
                         高   用            服务器
      作者: 余 洪   春  ISBN:   978-7-111-36055-1  tft: 79.007
    实际生产  环境，      从Linux
                           虚拟化、       集
    基于                                群、      服 务 器 故障 诊断 与 排除 、
                                              服务器的最佳
      系统 安全 性 等 多人 角度 阐述 构建 高可 用 Linux            实践
  资深Linuxy/Unix  系 统管      理 专家   架构 师 多 年 一 线    工作 经 验结品  ，
                                        和 ChinaUnix
          51CTO                   等知名社区联          荐

## Page 9

 推荐 阅读



    、  原本    Domain  Languages       |
     领域                           之
     特定        代码
 设计     .        语言        x      ¥,-
 :

 1     ~-
 中  ~- 一


 |  © 有



                                                 本 )
                                          设计 原本 ( 精装

 如 果 说  月  ?              所                                              么
     《人 神话 是 近 40 年 来 有 软件          开  工程 师 和 项 目   经 理们 必 读 的   一  本  书 ， 那 本 书 将 会
 是 未来数 十 年内从 事 软 件行业 的 程序   、    项 目 经理和 架构 师 必 读 的    一本     。  是     月     $    、
                            员                                 书 它        神话 作者
家、    软 件 工 程 教父        、 美 国    两 院 院 士 、图 灵  奖 和 IEEE计 算 机  先驱 奖 得主  Brooks 在计算机
 著名 计算 机 科学
     与设计、  建    筑和 组织机 构  的     架构 与设计 等 领域    毕生  经  验的 结晶   ， 是计算 机 图 书 领域   的 又 一
 软 硬 件架构
    级著作。
 史诗


                            领域     特定 语言

    本 书是DsL 领 域的丰碑 之  ，由   世界 级 软件 开发大师 和 软件   开发“ 教 公            Fowler
                      作                                                历时多年
 写作 而 成。全 面详尽 地 讲解了各 种 DSL 及 其 构造 方式 ，揭 示 了    与 编程 语言无       关 的 通用 原则 和 模式， 释
 了 如和 通过 DSL 有 效 提高 开发人员 的 生产 力以 及 增进 与 领域专家的有 效 沟通 ，能            为 开发人员 选 择和 使 用
 DsL 提人殿有 的 决策 依据 和 指导 方法  。



                                   之
                            代码

    本 书是  大全 姊妹 ，           为      行业的财富”
    《代码 的 篇 被 普 “软件 ， 资 深 软件 开发 专家 30 余 年 工作 经 验
 的 结晶 ，是微软 公司 软件 工程师必 读     。它 从   开改                                  、  管理、
                            书         软件 技术、 方法、 项 目 管                   理 团队
 人 际 沟 通等多 角度 总 结 出90 作  具有 代表 性 的 问题 ( 太 数 问题都    可 能 会      给公司 或 软件 项 目 带来    毁灭
 性 灾难 )  ，  并 给出了问题 的 解决 方案 和 最 佳 实践 ，值 得 所有 软件 工程师和 项 目 管 理 者   研读        。

## Page 10

 | aan    |









 ae  &
 4

    言
    前


                   篇     «TCP
                   第 一

 1章            协议族/2
第

                   以及主要协议    /2
                   结构
 1.1           协议族体系
               数据
     1.1.1          链路层/12
                    层13
     1.1.2     网络
               传输
     1.1.3          层14
     1.1.4     应 用 层 15
 1.2   封装  /16
           用/7
 1.3   分
       测试
 1.4         网络 /18
 1.5   ARP协议工作 原理     /9
     1.5.1     以 太 网 ARP请 求 / 应 答 报 文 详解19
     15.2      ARP 高速 缓存 的 查看   和修改 /10
     1.5.3     使 用tcpdump 观察ARP  通 信 过程/110
 1.6  DNS     工作 原理 /12
     1.6.1     DNS 坦 询 和 应 答报 文 详     解/12
       1.6.2   Linux下 访问  DNS 服务 /14
                 用 tcpdump
       1.6.3   使     观察     通信过     程 /115
                   协议族的关系    /   16
 1.7   socket 和TCP/IP

## Page 11

    Vil


2章 ”JP 协议 详 解 /17
        第

                             了 服务
    2.1    了               的特点/17
                          头部结构/18
    2.2    IPv4
                     JPv4
           2.2.1              头部结构/18
              2.2.2   使 tcpdump
                           用      观察IPv4 头 部结构/20
    2.3
    2.4               路由/22
                                     /23
           2.4.1     卫 模 块 工作流程
              2.4.2    路 由     机制124
            2.4.3      路 由 表 更   新 /125
                          转
                       /125
    2.5    了 P  发
                     定 / 26
    2.6    重
                     ICMP
            2.6.1               重定向报文126
            2.6.2     主机 重 定向 实例 /27
    2.7              头部结构/27
            2.7.1    IPv6     固 定头部 结构  128
                              扩展头部/129
            2.7.2    JIPv6

    3                协议详解/30
        第

    3.1              服 务 的 特点   /30
    3.2                 头部结构 / 32
            3.2.1    TCP 固      定头部结构   /32
                              头部选项 /33
            3.2.2    TCP
                              tcpdump 头 信息 /35
            3.2.3     使 用         观察TCP 部
      3.3   ITCP 连 接   的 建立和 关闭 /37
            3.3.1     使用tcpdump 观 察TCP 连 接 的 建立和 关闭 /137
                                  /39
            3.3.2    ” 半 关 闭 状态
            3.3.3     连接 超时 139
      3.4            状态转移 /40
            3.4.1    TCP 状 态     转移 总 图 /41
            3.4.2      TIME     WAIT 状态 /43
                         报文段 /44
      3.5   复位
            3.5.1     访问 不       存在 的   /144
            3.5.2     异常 终止 连接145

## Page 12

  Vill


         3.5.3   处理   半 打开 连接 /45
  3.6        交 互 数据    流/46
  3.7                 i /48
                    /50
                 带 外 数据
  3.9        超时重传/51
  3.10   拥塞 控制 /53
         3.10.1    拥塞  控制 概述 /153
         3.10.2    启动   和 拥塞   避免 /154
         3.10.3     ”快速 重 传 和 快速 恢复 /155

 4章                通信案例:     Internet   的 服务器
  第                    访问      上 Web     /   57

         实例
  4.1        总图    /157
  4.2         部 署 代理服 务  /58
         4.2.1   HTTP 代理 服务 器 的工作 原理/158
代理服务器                  159
         4.2.2   部 署         squid
         使 tcpdump     /     60
  4.3    用                 抓取传输数据包
                       器/62
  4.4    访问    DNS 服务
  4.5    本 地 名 称 查询 /63
  4.6        通信    /64
         4.6.1   HTTP
         4.6.2   HTTP  应答/166
                 总结/ 68
  4.7    实例


                  第 二    篇“深入           性
                                      解析 高 能 服务 器 编程

  5               网 络  编程 基础          70
       第

         socket
  5.1            地址API770
         5.1.1    主机 字 节 序 和 网 络 字 节  序
                         地址/771
         §.1.2    通用 socket
                          地址/72
         5.13     专用 socket
         5.1.4         /     73
  5.2    创建 socket/  74
                   / 75
  5.3.   fitsocket
  5.4      监听 socket/76

## Page 13

 Ix


 5.5      ”接受    连接 178
 5.6    发 起 连接      /80
 5.7      ”关闭    连接 /80
                  读写/81
 5.8                 数据
                        数据读写181
        5.8.1                      TCP
        5.8.2     UDP 数据 读       写  185
        5.8.3     通用 数据       读  写  函数    186
        带 外 标记    /87
 5.10   地址 信息           数 /87
 5.11             选项    /87
        $.11.1                          选项/89
                  SO_REUSEADDR
        5.11.2                        和        项
                  SO_RCVBUF
                                        SO_SNDBUF  /189
                                        和SO    _SNDLOWAT选项/93
        5.11.3    SO_RCVLOWAT
                                        选项/93
        5.11.4    SO_LINGER
 5.12   网 络 信息        API/ 94
        3.12.1    gethostbyname4egethostbyaddr    / 94
        5.12.2    getservbyname4vgetservbyport / 95
                                  / 96
        5.12.3    getaddrinfo
        5.12.4                    /
                  getnameinfo 98

 6章                        /  100
第

        pipers
 6.1        2x / 100
 6.2                          AX / 101
 6.3              Alwritev            / 103
 6.4                    / 106
 6.5                              phi     / 107
 6.6              RL /  108
 6.7              / 110
 6.8        a     / 112

                        SRE       MIE     / 114
        志
 7.1    日
        7.1.1                         Linux
                             8/114
        7.1.2                / 115
                            syslogd
 7.2    用 户 信息 7116

## Page 14

  X


                                  和    EGID/ 116
        7.2.1                       UID、EUID、GID
        7.2.2        接 用 户 /1117
                      间关系/118
  7.3   ”进程
                         组  1118
        7.3.1     进程
        7.3.2               会
                         1118
        7.3.3     ”用 ps  命令 查看     进程  关系1 119
        系统
  7.4                   资源限制/119
  7.5   改变 工作目录 和 根目录 / 120
  7.6       ”服务   器 程序   后台 化   /121

  8                                  框架
      第 性 能 服务 器 程序               123

        服务
  8.1                 器模型/123
                  CI/S
        8.1.1            型  1123
                            型/124
        8.1.2     P2P
        ”服务
  8.2                 器编程框架 / 125
  8.3              / 126
  8.4   ”两 种 高 效      的  事件处理 模 式 / 127
        8.4.1     Reactor 模式    /128
                              式 1128
        8.4.2              ”Proactor
        8.4.3     ”模拟Proactor    模
  8.5 ”两 种        高效  的  并 发    模式 /130
        8.5.1      FR             /     131
            ”领导                        者/追随
        8.5.2                       者模式1134
  8.6   有限 状态 机 /136
  8.7   ”提高服务         器 性 能   的 其他 建议/144
        8.7.1      池  1144
        8.7.2      数据 复制      1145
           8.7.3   上 下 文 切    换和

                     9意 ”LIUO
  第                    复用/146

        select
  9.1                   系统调用/146
                          API / 146
           9.1.1   select
9.1.2                             / 148
9.1.3              处 理   带 外  数据   1148

## Page 15

XI


                           系统调用/150
9.2   poll
9.3  epoll     系 列系统 调用           /  151
                                      事件
      9.3.1    ”内 核                表/151
      9.3.2                         / 152
                                    / 153
      9.3.3         LT#ETHX
                    EPOLLONESHOT        157
      9.3.4                             ¥   # /
94                   Hi            LER / 161
                                                 connect/
9.5  LIO 复       用 的  高 级   应 用     一:   非 阻  塞      162
                                        室    程序1/    165
96    IO       复 用 的  高       用 级 应 二:                聊天
      9.6.1         BP/     165
                    MAB
      9.6.2              /  167
97    IO       复 用 的  高  级  应 用     三:   同时 处    理 TCP 和UDP 服务 / 171
      超级
9.8  服务xinetd / 175
                    ”xinetd
      9.8.1                      配置文件/1175
                                 工作流程/1176
      9.8.2         xinetd

      =   4/178

          Linux
                         概述   /178
10.1                信号
                                   号1178
          10.1.1    发送信
                    ”信号
          10.1.2                   处理方式1 179
                    Linux
          10.1.3          信号        /1179
                                      调用/181
          10.1.4                      中 断 系统
10.2  ”信号 函数          /181
                              #39 A     / 181
          10.2.1    signal  A
                    sigaction
          10.2.2                       Fl  /  181
                    集/182
10.3      信号
                                  集函数/182
          10.3.1    信号
                    ”进程
          10.3.2                    信号掩码1183
          10.3.3     被 挂 起   的      信号 /1183
10.4      统一 事件         源/ 184
10.5      网络 编程相关 信和                /1188
                                   188
          10.5.1    SIGHUP/
                              /      189
          10.5.2    SIGPIPE
          10.5.3                     190
                    SIGURG/

## Page 16

 XI


                      / 193
 11.1                    SNDTIMEO     /     193
                         信和 /195
 11.2    SIGALRM
         11.2.1      基于 升序链表 的 定时 器     /195
         11.2.2      处理 非 活动    连接 /200
 11.3    IO  复 用 系统 调用 的 超时 参数     /205
 11.4    高 性 能 定时 器     /   206
                    时间
         11.4.1         轮 /1206
         11.4.2     时间 堆 1211

 第 12 章 ”高 性能        IO     库 Libevent/218

 12.1    IO 框架库 概述       /218
                        源码分析 / 220
 12.2    ”Libevent
         12.2.1     ”一   个 实例 /220
         12.2.2      源 代 码  组织     /222
         12.2.3          #444     / 224
         12.2.4          FAAS) PH to KS ALS / 226
                    往事
         12.2.5                  件多路分发器中 注册 事件 /1230
                    eventop#
         12.2.6          494k     / 233
         12.2.7     event      44% / 235
         12.2.8     ”事件 循环    1236
13       ”多              /239
 第 章         进程 编程

 13.1       ”fork 系 统 调 用  /239
 13.2    exec 系    列 系 统调用     /240
 13.3    ”处  理 价 尸   进程 /240
 13.4    管道1241
                 量 1243
 13.5    信号
                            量原语1243
         13.5.1     信号
                    semget
         13.5.2                系统调用1244
                    scmop
         13.5.3               系统调用/1245
                    semctl
         13.5.4               系统调用/1247
         13.5.5      特     刍 值 IPCPRIVATE / 249
                    内存 /251
 13.6    ”共享

## Page 17

XIII


                    shmget    /
       13.6.1                        251
       13.6.2       shmat4eshmdt #44349 A / 252
                             系统调用/125S3
       13.6.3       shmctl
       13.6.4       共享内存     的POSIX 方   法  1254
       13.6.5           / 254
13.7   ”消息 队列         / 263
                    msgget
       13.7.1                  系统调用/1263
                               系统调用/1264
       13.7.2       msgsnd
                                系统调用1264
       13.7.3       msgrcv
                    msgctl
       13.7.4                 系统调用 /265
                    / 266
13.8   IPCfir>
13.9   ”在 进程间           传递 文件描述 符 /267

第 14 章 ”多 线程 编程 /269

                      线程概述/269
14.1   Linux
       14.1.1          线程 模型1269
                          线程库/1270
       14.1.2       Linux
14.2   创建 线程和结束 线程1271
14.3   ”线程 属性/1273
       POSIX
14.4                信号量 /275
14.5       / 276
       14.5.1       互 斥 镇  基础API/1276
       14.5.2       互 斥     属性1277
       14.5.3       死 锁 举 例 /1278
14.6   ”条件 变量 /279
14.7   ”线程 同步 机 制 包装 类     /   280
       ”多 线程
14.8                环境/282
       14.8.1       可 重 入 函数  1282
                        和  进程 1283
       14.8.2       线程
                           和信号1284
       14.8.3       线程

第 1S 章                  地和线程地/1287
       ”进程

15.1   ”进程     和 线程 池 概述      /287
       处理
15.2                 多客户/288
15.3     同 步     /  半     步 进程池 实 现 /289

## Page 18

XIV



           池实现的               简单CGI 服务 器 /298
15.4   用 进程
15.5   半 同     步 / 半 反应 堆 线   程 池 实 现 1301
15.6  ”用 线程 池 实    现的 简单        Web 服 务器  /304
       15.6.1     http_ conn  类 /304
       15.6.2                 /318






            器调制 、调                试 和   /1324
 第16 章  ”服务                           测试

16.1    最 大 文件        符 数     / 324
                 内核参数 /325
16.2   ”调整
        16.2.1  /proc/sys/      目录  下 的 部 分 文件 1325
                                      下的
        16.2.2                  目 录     部 分 文件 1325
       gdb
16.3  调试          1326
        16.3.1    用 gdb 调试多       进程 程序   1326
        16.3.2    用 gdb 调 试     多 线程 程序   1328
16.4    压力 测试1329

第 17 章 ”系统 监测 工具1333

                 / 333
17.1   tcpdump
17.2   lsof/ 334
17.3   ne/ 336
17.4   strace/    338
17.5   netstat/ 341
                / 342
17.6   vmstat
17.7   ifstat/    344
17.8   mpstat/    344

参考   文献 /346

## Page 19

“





        第  ”用
TCP/IP 协议  详解 “









#1      TCP/IP 族
            协议

第  2 章  协议    详解

第  3 章  TCP 协议 详解
            的                   Web 服务
第  4 章  TCP/IP 通信 案例; 访问 Internet 上  器

## Page 20

    第             族
        1 TCP/IP 协议


        现在 Internet                )    使用 的 主流 协议 族是     协议    ，        它 是 一个分      多 协议 的
    通信 体系。本 章 简要讨论                 TCP/IP  协议 族 各 层 包 含的主要 协议   ，以        及 它们 之间 是 如 何 协作 完
               的。
    成网络 通信
        TCP/AP   协议 族 包含            众多  协议 ， 我们  无法一 一 讨论。 本 书 将 在 后 续 章 节 详 细讨  论    IP  协议
    和 TCP                          协议 ，因 为 它们 对 编写网 络 应 用 程序具有 最 直接 的 影响 。本 章 则 简单介绍 其中几 个 相
    关 协     ，ICMP 协议           、ARP 协议
        议                          和       DNS 协议 ，  学 习 它们 对 于 理解 网     络通信 很 有 帮助   。读 者 如
    果 想 要 系统 地 学 习 网 络 协议          ，
                                   那    么        评 论 请                       )            文档
                                           RFC   (Request For Comments，      求          无疑是首
    选 资料   。

    1.1     TCP/IP                协议 族       结 构 以 及 主要 协议

   TCP/IP 协议 族 是 一 个 四 层 协议 系统 ，自 底而上 分 别是数据 链 路 层、网 络 屋、传 输层和
    应 用    层 。每 一层完 成不同 的 功能 ，             且 通过 若干 协议 来 实现 ，    上 层 协议 使 用 下      协 议 提 供 的服
        图    1-1 所 示   。
    务 ， 如

        应 用层 =|ping| |telnet| |OSPF| |DNS| 用 户空间
                 nn nn                       pen                                socket
                 传输                                         _UDP
                         层                                     |

                 网 络 层                         P|
                       路           ARP         |
 数据 链 导

                                                                         物理 传输媒介
                                   图 1-1  TCP/IP    协议 族体系 结构及 主要 协议
                            链路 层
    1.1.1                  ”数据
   链路层                         了             网      处     据               媒介 《比如
   数据                                                                                  以太网、令
实现 网 卡 接口 的                                    络 驱 动 程序 ，以 理 数       在 物理
    牌 环 等    ) 上 的 传输        。 不 同  的   物理 网 络  具   有 不 同     的电气 特性 ，网络驱 动  程序隐藏 了 这 些 细节 ，
为 上 层 协议 提供 一 个 统一 的 接口 。
   数据 链 路 屋 两 个常用 的 协议是 ARP 协议 (Address Resolve Protocol， 地 址解析 协议) 和

## Page 21

                                                                         章          协议
                                                                      第 1    TCPIIP     族  oe   3

    RARP   协议 〈Reverse   Address Resolve Protocol，     道 地   址 解析      )。           了     址
                                                               协议 它                 们 实现 IP 地 和 机
    器 物理 地  址   〈 通 常    是             地
        MAC 址，以 太 、令 牌 环和 802.11 无线网 络 都 使用MAC 址)
                                   网                                                          地 之
间 的 相互 转换 。
   网 络 层 使用 地 址 寻 址 一 台 机 器，而 数据 链 路 层 使用物理 地 址 寻 址一台 机 器，因 此 网
    络层必 须 先 将目标 机 器 的     IP   地 址 转化 成    其      物理 地     ， 才 能 使 用数据 链 路 层 提 供 的 服务     ，     就
                                                      址                                         这
        的用途。RARP 协议仅用 于 网络上 的 某 些 无 盘工作站 因 为 缺乏 设备，
    是 ARP 协议                                                                     存储             无
                                                               。                                盘
                站无法记住自 己
    工作       的                    址，    但 它们
                          下        可以利用               网    卡 上 的 物理 地 址 来  向     网 络 管理    者〈  务器
                                                                                                服
                          A    SAY    IP      FF     RARP 服务 的 网 络    管理   者 通常    存有 该 网    络上 所
    或 网 络                    管理 软件) ALIA
    有 机 器 的 物理地址 到     IP  地  址   的映射    。
                           很重要，所以 我 们 将 在 后 面 章节 专门          讨论    它  。
        由 于 ARP   协议

                    络层
    1.1.2           ”网

        网 络 层 实现     数据    包 的 选  路    和  转发  ，WAN   (Wide Area Network，    广 域 网)           使用众多
                                                                                               通常
    分 级 的路由 器 来 连接 分 散     的   主机    或    LAN (Local Area Network，     局 域 网 )，  因 此，    通 信   两台
                                                                                                的
    主机  一  般 不 是 直接 相连     ，   而 是    通过     多 个 中 间 节 点   〈   由 )       的。          是
             的                                                              路 器 连接 网 络 层 的 任务 就 选
      这 些 中 间 节     ，  以 确定    两台  主 机 之 间 的 通信 路          。
                                                             同
             点                                        径      时，   网 络    屋 对 上 层    协议 隐藏 了 网 络 拓
     的细节，使 得 在 传输层和 网 络应用 程序 看 来 ，通 信 的 双方是直接 相连 的 。
扑 连接
        网络层 最 核心 的 协议是         IP 协议     〈Internet Protocol，   因 特 网 协议 )。IP         协议 根据 数据 包 的
    目的 IP  地 址 来     决定如   何 投递    。    如 果 数 据 包 不    能 直接 发  送  给 目标   主机，     么            就为它
                                   它                                             那  IP         协议
    寻找   一 个 合适 的 下 一跳 (next          hop)    路 由 器， 并 将 数据 包 交付 给    该  路 由 器 来 转发     。  多 次 重复
    这 一  过  ，数 据     包 最           终 到 达 目标 主机，或  者由 于 发   送 失 败 而 被     。 可 见 ，IP           使用逐跳
            程                                                                                  协议
    Chop by hop)  的 方式 确定 通信 路               。我们 将 在 第 2 章详 细讨 论    IP 协议
                                         径                                 。
        网 络 层 另 外    一 个 重要 的 协议         是    ICMP 协议 (internet Control Message  Protocol， 因 特 网
             是     协 议 的 重要
    制报文协 议 )。它                     补充，    主 要 用 于 检测 网         络    连                    。ICMP 协议
                                                               接                           使用的报文格
    式 如 图  1-2   所 示 。

         0                                        15 16                              31





                               fl 1-2                       ICMP
                                                            报文格式

        图 1-2 中，8 位 类 型 字段用于 区 分 报 文 类 。                   将                     大
                                                     它
                                                  型            ICMP 报 文 分 为 两    类  :  一  类 是差错
  文，这 类 报 文主要 用 来 回应网络 错误，比 如 目标
                                                           不可到达   〈    类 型 值 为   3 和 重  定向  〈 类
    报                                                                                         型
    值    为5 ， 一 类 是 查询 报       文 ，这 类 报 文用来       查询   网     络 信息 ， 比如 ping      程序 就 是 使用 ICMP
    报 文  查看 目标是否 可 到 达 《类 型 值     为8  的   。       有 的  ICMP     报 文 还 使 用 8  位     代码 字段 来  进一步
                                                                                         对    重主机
    细 分 不 同 的 条 件 。比 如 重 定向 报 文 使用代码值             0  表示 对 网 络 重 定    向  ， 代 码 值    !         表示

## Page 22

    4     第 一篇 TCP/IP  协议 详解

              问 。ICMP
    定            报文使用  16 位 校 验 和 字段 对 整个 报文 〈 包 插头部和 内 容 部分) 进行      循环   宛 余 校
    验     Redundancy Check，CRC)7， 以 检验 报 文   在 传输过 程 中 是 否 损坏。          不 同 ICMP
                                                                     的         报
    文 类 型具有 不 同 的   正文 内 容。我 们 将 在 第 2 章 详 细讨 论 主机 重 定向 报  ，  他              报文格
                                                 文                        其 ICMP
                    的标准文档   RFC 792.
    式 请 参考 ICMP 协议
         需要 指出 的  是 ，ICMP 协议 并 非 严 格 意义上 的 网 络 层 协议 ，  因 为 它 使 用处于 同一 层     的 IP
    协议 提供  的 服务〈  一 般     说，     使用
                      来 上 层 协议 下层 协议 提供        的 服务 )。

    1.1.3    fei

   传输 层 为两台 主机上 的 应用 程序提供 端 到端〈end to end) 的 通信 。 与 网 络 屋 使 用的 逐 跳
    通信 方式 不同 ，传 输      只 关心  通信 的 起 始  目的      ，而 不 在乎数据   包 的 中 转  过   程 。图 1-3
    展示 了 传输 层 和 网   络层 的  这种 区 别 。

                                                                    器
        客户 端                                                     服务




                  器
                  -由




    驱动 程序         |  | 驱动 程序                 程序 | ，    驱动 程序





                                                                    ”传输
                                  图 1-3                        屋和网络层的区别
   图 1-3 中，垂 直 的 实 线 箭头 TCP/IP 协议 族 各 层 之间 的 实体 通信 〔 数 据 包 确 实 是
        的)，而 水 平 的 虚线 箭头 表示 通信 线路，该图中 还 附带 描述了 不 同 物理
着 这 些线路传递
                       见，              层数 据 链 路 〈   动 程序   )    封装了 物理 网 络的电气 细节    ;  网 络 层封  装
    网 络 的 连接 方法  。    可
    了 网 络 连接 的 细节               层 则 为  用      应   程序 封装 了    一  条 端 到 端的逻辑 通信 链 路 ，    它 负责 数据 的
    收发   、链 路  的 超时 重 连 等  。
              层协    主  要 有三
                                个 ，TCP 协议       、UDP        协议
         传输                                                     和 SCTP  协议
                 议                                                       。
                                                            传 输  制  )     AMAR
         TCP  i (Transmission Control Protocol，                控 协议     Set ay               fol
   的和         基于     流 〈stream)                  的 服务 ，TCP 协议
    连接                                                                  使用超时重传、数 据确认 等 方式 来确保 数据
         正确       至               此                           ARS
    包 被       地 发 送    目的       因 TCP                SE AY SEAS.  (AG TCP 协议 通信 的 双方 必须 先   建  立
                                                                                     的状态 、读 写 缓冲
    TCP  连接   ，并 在 内   核中  为 该    连接         维持   一 些 必 要 的  数据 结构     ，比 如 连接                区，

## Page 23

                                                                                          第 1 章    TCPIIP 协议             族 vs  5

以 及 请 多 定时 器 等。当 通信 结束 时，双方 必须 关 闭 连接以 释放 这 些 内 核 数 。TCP 是其
        据 服务
    于 流    的。 基于 流   的  数据 没有     边界        〈        度)      ，                  断                              流          。
        长 限制 它 源源 不 地 从 通信 的一 端 人 另一 端 发
    送 可以 逐个 字 节 地 向 数据 流                  中 写    人 数据     ，接 收 端   也 可以 逐个 字 节 地 将 它们 读 出                            。
   UDP 协议 (User Datagram Protocol， 用 户 数 据 报 协议) 则 与 TCP 协议 完全 相反， 为
        它
    应 用 层 提 供 不 可      、    无 连接    和 基于 数据 报 的 服务              。“           不 可 靠    ”   意 味 着UDP                无法          数据
                                                                                                                              协议
                       靠                                                                                                 保证
        端。如 果 数 据在中 途 丢失，或 者 目的 端通过 数据校验 发现数据 错
从 发 送 端正确地 传送到 目的
 ILA FE,             MY UDP   协议 只 是 简单 地 通知 应 用 程序                            发 送     失      。 此，           用
                                                                                            败   因 使              UDP      协议 的 应
    用 程序 通常 要 自己处      理 数 据 确          、          超 时 重 传  等                   协议
                                        认                              。UDP                   是无   连接      的     ， 即     通信 双方 不
    保持   一 个   长久 的  联系     ，因 此 应 用 程序 每 次 发 送 数据                 都  要 明确 指定 接收 端的 地                         址〈IP       地 址 等 信
      )。   基 于 数据
                    报的服务，    是     相对 基于 流 的         服务     而       言  的    。每    个       UDP   数据 报 都         有  一   个 长    度，接
    收
                必须 以 该长 度 为 最 小  单位 将 其所 有 内 容                  次 性 读       ，    否 则          将被截断
                                                     一                 出                  数据               ，
       SCTP                            Control Transmission Protocol，                    流 控制 传输 协议  )     是一         种 相对 较 新 的
            层协议 ，  它 是为     了 在 因特网 上传输 电话 信号                   而 设计        的        。    本   不                          ，
    传输                                                                                           讨论  SCTP 协议                感 兴趣
        书
        其标准 文档 RFC 2960.
的读者 可 参考
     们将在第 3 章详细讨论 TCP 协议 ，并 附带 介绍 UDP 协议。
   我

    1.1.4          用层
                    应

       应 用层 负责 处 理应    用     程序       的     逻辑       。数 据 链 路      层 、             网 络屋   和    传输层          处    理 网     络 通 信 细
      ，这 部分    必须 既 稳 定 又     高      ，  因 此它们 都          在    内 核 空    间    中 实现            ，   图          所示。
                       效                                                                                         如 1-1 而 应 用 屋 则
    在 用 户 室间 实     现 ， 因 为 它 负     责 处  理 众多 逻辑          ，    比 如  文件 传输        、         名 称 查询 和 网 络 管理                 。    果
                                                                                                                          等    如
    应 用 层 也 在 内 核中实现 ，则 会 使内核                        变 得 非常 庞大     。                然，                                是
                                                                     当 也 有 少数 服务 器 程序                                    在 内 核 中
           的， 这 样 代码 就 无 须    在    用  户          空间和 内 核 空间 来 回        切    换    〈     主 要 是 数据         的  复制        )，      大地提
    实现                                                                                                                         极
    高 了 工作 效率 。不 过 这 种 代码 实现 起                       来 较复杂，        够             ，                         。                  用户
                                                     不
                                                                      灵活 且 不 便于 移植                                       本 书 只讨论
    室间 的   网  络 编程 。
       应 用 层 协 议 很     多，   图   1-1  仅  列举           了 其中 的 几 个  :
       ping                       是 应 用 程序 ，而 不 是 协议 ，前 面 说 过 它 利  用            报 文 检测
                                                                     ICMP                     网络连接，                   是       网络
                                                                                                                              调试
              的必备工具。
    环境
                       登录                        协议   ， 它 使 我 们 能 在 本 地 完成 远程 任务                     ，                      本 书后
       telnet  协议 是    一                        种远程                                                                        续章节将会
    多 次 使 用   telnet 客户     端登  录 到 其 他              服务 上 。
       OSPF (Open Shortest Path First，                 开 放 最 短路 径 优先)           协议 是          一 种 动态       路 由   更 新 协议     ，  用
    于 路 由  器 之 间 的   通信    ，以     告知对 方          各 自    的 路 由 信息 。
                                                     名服务 )     协议 提供        机     器 域名 到      IP     地     址 的 转换        ，    们将
       DNS (Domain Name Service，                 域                                                                             我
    在 后 面简要 介绍                 协议
                    DNS         。
               用层协议  〈      或 程序 )  可 能            过 传输 层 直 接 使      用      络网    层 提供 的 服务                ， 如           ping 程序
       应                                                                                                   比                   和
    OSPF 协议     。应 用 层 协议      〈   或 程序          )     通常  既可以 使   用            服务          ， 又 可以
                                                                           TCP                          使用     UDP         服务 ，比
    如 DNS 协议 。我 们 可以通过 /etc/services                 文件 查看         所 有 知名 的 应 用 屋 协                        ，以    及           都能使
                                                                                                        议                     它们

## Page 24

 6    TCP/IP 协议 详解


 用 哪些 传输层 服务 。

 1.2  封装

     封装Cencapsulation) 实
上 上 层 协议是如何 使用下 层 协议 提供 的 服务的呢? 其 实这 是通过
的。应      用 程序数据 在 发 送 到 物理  网 络 上 之  ， 将 沿 着 协议 栈 上 往 下 依次 传递 。 层
 现                                                              每
                                前        从                     协议
     将在上 层 数 据的基础上 加 上 自己   的 头 部信息  〈《有 时 还 包括 尾部信息 )，以 该屋的功能，
                                         实现
                   图    1-4
 这 个 过 程 就 称为 封装，如      所示。





                   TCP/UDP

                   -一TCP    报文自UDP 数据报->

                                      太网
                                       以



                    一一一   网                              以 太 网
     -一                以 大     -一
                       一    1500字节 一 一|
                                图 1.4 封装

       过 TCP 封装 后 的 数据  称 为TCP  报 文   段(TCP message  segment)， 或 者 简称TCP 段 。
     经
     文提到 ，TCP  协议 为通信 双方 维持     个 连接  ，并 且 在 内 核 中   存储 相关 数据  。    这 部 分 数据
 前                             一                                           中
             和 TCP  内 核 缓冲
                       区       〈    发 送 缓冲 区 或 接收 缓冲 区) 数据
 的 TCP   头 部信息                                          一起    构成   了       报
                                                         TCP               文
                   框所示。
 PY, Ql  1-5      中 的虚线


                                 缓冲
 应 用                              区
 程序                         send/write               FP tia

 re     !                   TCP                      TCP  、



 ，

                   图 1-$        TCP 报 文 段封装 过 程

## Page 25

    章 TCP/IP 协议族 «+ 7
    第 1


        当 发送端 应用 程序 使用               send〈          者                  向一个                 写人       时，
                                                    或    write)        函数     TCP 连接
                                                                                                     数据      内 核 中 的
    TCP    模块首先 把 这 些 数 据 复制到 与 该连接 对 应                                内 核发
                                                                       的 TCP     送       缓冲 区 中，      然后TCP        调
                                                   包括    TCP      头    部 信息
    用 IP   模块 提供   的 服务        ，传 递  的                                     和  TCP    发 送   缓冲 区 中 的 数据 ， 即       TCP
                                            参数
    报 文 。关
    段            于 TCP 报 文 段        头部    的 细节      ，    我 们 将 在 第     3     章讨论   。
           过 UDP   封装
    经          后               的  数据     称  为 UDP 数据              报    (UDP datagram).           UDP 对 应 用 程序 数据 的 封
    #45        AMM.                  UDP      无 须    为 应           用 层 数 据 保 存副本      ，因 为 它 提 供   的    服务 是    不可 靠
       个UDP 数据 报 被 成 功 发 送 之 ，UDP 内 核 缓冲区 中 的 该 数据 报 就 被委 了 如
    的 。当 一                                               后                                                 。       果
    应 用    程序检测 到 该  数据         报  未 能被    接收           端正    确    接收  ，并 打算 重    发  这 个 数据     ， 则 应 用           需要
                                                                                               报                  程序
           从用户    空间 将 该     数据    报 拷贝 到                内 核发
    重新                                        UDP                      送缓冲区中。
           过                        称为            报
               经 IP 封装 后   的 数据         IP 数据 CIP datagram).               IP                 数据 报 也 包括 头 部信息 和 数据 部
    分 ，其 中 数据 部 分            就 是 一  个 TCP   FR        CBR.    UDP             报或者          文。
                                                                       数据     ICMP 报            我 们 将   在 第 2  音
    细讨 论   IP   数据 报 的  头      部信息   。
        经 过 数据 链 路 层           封装   的 数据 称为              帧    〈frame)。  传    输媒 介   不 同  ，  的 类   型  也  不同  。    如，
                                                                                                               比
    以 太网上 传输 的     是 以       太 网    帧              〈ethemet frame)，    而 令 牌 环 网   络上传输的 则 是 令 牌 环    帧       〈token
ring frame)。 以以 太 网 帆 为例，其封装格式 如 图 1-6 所 示 。
           以 太 网     使用        字 节 的 目的物理
               6                                                地址和    字 节 的  源 物        址 来      通信     。
                                                                  6                理  地     表示                的 双方 关
    于 类 型  (type)  字段          ， 我 们  将   在 后  面 讨                。4 字
                                                              论        节 CRC 字段       对  帧的 其  他 部   分 提供   循环
    余 校 验  。
               单元              〔〈Max Transmit Unit，MTU)，                   即 帧  最   多 能 携带 多  少   上 层 协议 数    据  〈 比
   帧的最大 传输
如 IP 数据 报 )， 通 常 受到 网 络 类 型的 限制 。图 1-6 所 示 的以 太 网 是 的 MTU 是 1500 节。
        字 正
             如此，过 长  的 IP      数据 报 可 能 需要 被 分                片    〈fragment)     传输。
    因为



           6 字 节                    6         字节                           46 1500字
                                                              2        字 节  一            节           4 字 节
                                              图     1-6       以 太 网 帧      封装

         帧 是 最 终 在 物理 网络上 传送                   字 节 序列         。        至
                                              的                        此，   封装    过 程 完  成 。

    1.3    3H

                                                  栈   自 底 向      上     依次 传递     。  各 层 协议 依次 处 理 帧    中 本 层 负
         当 帧 到 达 目 的   主机      时，   将 沿 着 协议
    责  的 头 部 数据    ，以 获取 所 需 的信息              ，    并 最 终将     处  理     后 的   交 给 目标      应  用 程序  。这 个 过 程 称 为
       用   〈demultiplexing)。   分 用 是 依靠           头 部信息 中 的 类 型 字段 实现                 的  。 标 准 文档  RFC 1700 定义
分
了所 有 标识 上层 协议 的 类 型 字段 以 及 每 个上层 协议 对 应 的 数值 。 图 1-7 了
        显示 以 太 网 帧 的 分
用 过 程 。

## Page 26

   8 第 一篇 TCPIIP 协议 详解



   应 用
   根据 头 部 的


      IP
     头部的
      根据
     值复用
protocol
   +    |
   '    ARP
 以太网帧头部的
  根据


                                                                     HEA
                                                图      1-7 以 太 网 帧 的 分 用 过 程

       PAY     IP     PMX.            协议
                                 ARP           和RARP 协议 都 使用                 传输  数据   ，   所以 帧    的      头      部 需要 提供 某 个
   字段   〈 具 体 情 况 取决 于                 的    类 型 )      来 区分它们        。  以以 太 网   帧为       例 ，它 使 用       2   字 节 的 类 型   字段
                                   图  1-6)。    如 果 主     机 接收 到 的以 太 网        帧  类    型 字段 的 值       为   0x800，    则 由    数
   来 标识 上 层 协议        〈    见
                            报 〈       图                                                          ，                      的值为
   据 部 分                 数据       见 1-4)，           以 太 网 驱动 程序 就 将           交付 给                              模块 若 类 型 字段
                                        部分为 ARP 请 求或     应 答 报         文 ，以  太 网 驱动 程序 就 将       帧       交付 给   ARP
   0x806， 则           的 数据
   佑 类    字段
       型              的值为   0x835，     则 帧 的 数据 部分                为  RARP    请 求 或    应 答 报  文   ，以           太 网 驱动 程序 就 将
   帧 交付   给    RARP            模块。
                      为ICMP        协议          、TCP      和                       用          ，     以                   报的头部采
       同样 ，因                                        协议     UDP         协议 都 使     IP  协议 所           IP                  数据
                                                来区分它们。
   用 16   位 的协议 (protocol)             字段
       TCP     报 文 段      和 UDP 数据 报 则 通过 其 头                        部 中      位 的           号                             来
                                                                                          端口 〈port number)               字段
                                          如 DNS 协议
                                                         对应的           端口 号
   区分 上 层 应 用              程序     。比                                         是              S3，HTTP 协议 CHyper-Text Transfer
                 超 文本传送                  协议 )  对 应 端口
   Protocol，                                         的     号是       80。  所 有 知名 应 用 层协议 使                用  的   端口 号 都  可 在
                       中    找到     。
   /ete/services 文件
                                通 上 述分 用 步骤    ，最 终    将   封装   前   的  原始 数据 送 至 目标          服务  〔〈         1-7    的
                 过                     后                                                                        中     ARP 服
   务    、RARP   服务      、ICMP         服务 或       者   应 用 程序      )。      样，           目标                 ，      装
                                                                    这 在 顶层                     服务 看    来         封 和 分 用 似乎
              发生过。
   没有

                           网络
   1.4                     测试

                                                                         了图   1-8     所 示 的 测试
                为了深 人 理解 网 络 通信 和 网 络 编程                 ，       我 们 准备                          网络，                   其 包括
                                                                                                                 中       两台
       A 和B，              以
   主机                     及 个         连接 到 因特网                                                                           测试
                          一                            的路由器。             后 文如    没有   特别 声明 ，所 有                      硬件指的都
              该网络。我 们 将 使 用 机 器 名              来 标识 测试 机 器          。
   是
                      网络主要 于 分
       该 测试                            析               协议           协议            协议                        协议
                           用                   ARP            、IP         、ICMP             、TCP             和 DNS  协议  。 我
   们 通过   抓         取 该 网 络 上  的以 太 网            ，   查 看 其    中  的以 太 网       头     部 、IP   数据 报头           部  、TCP 报 文 段 头
                       网络通信           的 细节           这
   部信息     ， 以  获取                                       样    ，以   理论结合 实践          ， 我 们                    就 清楚 TCP/IP 通信
                                                。                                                                    和具体  是

## Page 27

    族 «+ 9
    第 1 章 TCP/IP 协议


       的了。作 者 编写的 多 个 客户 端、服 务 器 程序 都 是 使 用 该网络 来 调试 和 测试 的 。
如何 进行


                  A         TIP   地   址:                                       IP 地         址 ，192.168.1.109
                                MAC 地 址:    00:16:d3:5c:bg:e3                  MAC 地         址: 08:00:27:53:10:67      B
                                  AK:       Ubuntu 9.10                                 i:                  16
                                                                                                        Fedora          :
                        网 卡 接口    : eth                                                              加        接口  :  papl
                                                                                                                           太网
                                                                                                                            以
                                    wi                      fk:         192.168.1.

  去 因特网

                                                                                  网络
                                                           图 1-8                  测试
                                                               络    PP 地
        对 于 路 由     器   ，我 们      仅 列       出    了其  LAN    网                  址〔192.168.1.1)，                    而 忽略 了 ISP         Cinternet
                                            提供 给 它 分 配                         WAN          网         IP                   地 因 为 全 书  的 讨论 都 不
    Service  Provider，           因 特 网服务             商)                        的       络                        址，
        它。
    涉及

    1.5      ARP     协议 工作 原理

                                                                                                              过本               书 仅讨论
        ARP                                           协议 能 实现任意 网 络 层 地 址 到任意 物理 地 址      的  转换        ，不                      从     IP  地 址 到
  太网地址(MAC 地 址 ) 的 转换。其 工作 原理是 : 主机 向 自己 所 在 的 网 络 广播 一个ARP 请
以
    求 ，该      请   求 包 含 目标 机 器      的  网        络  地     址 。此 网     络    上 的 其他 机 器 都 将                  收      到 这 个 请        求   ，但   只有 被 请
    求 的目标 机 器 会回应 一个                               应
                                    ARP         符，         其 中 包含 自己 的 物理 地 址                        。
    1.5.1         以 太 网 ARP                 求            答报文详解
                                  请 /应

                          求            报文的格式如图                 1-9             所 示 。
        以 太 网 ARP 请

    型             |       型 |         度     |     长度                                    |                |         地    | 。      址
    类                   类         址 长              址                太 网 地 址                   地址              太 网     址      地
    节                   FH        OLS  OI
                  2字                                                    6  字 节              4 字 节               6字 节              47
                                            图            ”以太
                                       1-9                     网 ARP 请            求/    应 答 报 文
                        以太网    ARP     请
        图 1-9     所示                            求          报 文 各 字段                            具体 介绍 如 下 。
              硬件 类 型 字段定义 物理 地 址 的 类                           ，  它 的 值            为  1 表示             MAC        址。
                                                               型                                                   地
              协议 类 型 字段 表示 要                             的协议        地 址 类      ，  它 的 值         为               表
                                                                           型                    0x800，              示  IP    地   址 。
        La)   硬件 地 址 长度字段和 协议 地 址 长度字段                                     ，顾               义，                         是字节。
                                                                               名    思 其 单位 对                                     MAC 地 址     来
             说     ，其 长 度 为       SIP (v4)               地 址 来           ，其
                            6;                                      说              长  度     为 4。
                                                类          ARP      求   请  〈     值      为         1)、ARP            AVS 〈值 为 2)、RARP         请
              操作 字段指出       4     种操作                型                                                                                       求
             〈《                                         应符 值
              值 为 3     和 RARP                  〈       为4)。
                  后 4   个 字段 指定通信 双方 的以 太 网 地址和                                IP         地 。            发    送        端填充
             最                                                                                  址                         除目 的 端    以    太 网 地
               外的其他     3   个 字段                ，以 构建                                      HEN                A AAR
             址                                             ARP                                                               AY    A     IP Bb

## Page 28

    10           第 一  篇        协议 详解

                址 是 自己      ，  把    自己       的以 太 网  地   址 填 进            ，然 后            两个     址                          端
                                  就    去                                          交换 目的     端 地     和 两 个 发 送                地
                址 ，以 构建       ARP     应 答 并 返回    之  〈              然     前       述，       需要     为
                                                         当 ， 如
                                                                          所               操 作 字 段     设置 2)。
                图 1-9 可
            由              知，ARP     请 求        /应 答 报 文 的          长 度   为28 字 节      。  如 果 再   加  上以 太 网 帧 头       部   和 尾部
    的   18  49 CULPA        1-6)，     则      个 携带    ARP     请 求      /   应 答 报 文 的以 太 网 帧 长     度    为    46      节。       过有
                                         一                                                                                 字 不
    的 实现 要 求以太 网 帧数据 部                       分 长  度  至 少   为      46              图        此
                                                                      字 节  〈 见         1-4)，     时 ARP     请求  /   应   答 报 文 将
            一些填充字节，            满足
    增加                        以          这个要求。    在 这 种                   下，              携带                             报文的以太
                                                                     情况           一    个                           ARP 请 求 /应答
    网 帧 长度为 64 字 节            。

    1.5.2        ARP       高 速 缓存的 查看和 修改

            通常      ，ARP       一个高速缓存，                                  经常访问               最                               的机器
                           维护 其 中 包含                                              〈    比 如   网 关 地 址 )                  或 近 访问
                                                                          的        请
    的   IP    地 址 到 物理 地 址 的     映射          。  这 样  就避免       了    重复     ARP            求， 提   高了  发  送  数据     包 的 速度   。
                              使用 arp     命令 来 查看和 修改                  ARP    高 速 缓存        。 比
            Linux    下 可以                                                                  如     ，emest-laptop        在 某  一 时
                意                                 变化   )             ARP  缓存                      用
    刻 〈 注            ，ARP  高 速 缓存 是 动态            的          的            内 容             如 下 〈 使    amp-a tr):
   Kongming20 192.168.1.109 at 08:00:27:53:10:67 [ether] on ethd
    ?(192.168.1.1) at 14:e6:e4:93:5b:78 [ether] on etho
                中，第   一    项 描述 的        是    另 一     台 测 试    机 器                         意，      址                       地址都
        其                                                                              注 其                                、MAC
       图              的一致)，                               是               。       两        除                           个
    与       1-8 描述 第                     二 项 描述 的            路   由      器     下 面 条 命令 则 分 别     和 添加 一                    ARP
            项，
    缓存

        $     sudo   arp   -d 192.168.1.109                                       #    HR Kongming20 stew     ARP          项
        缓存
   $ sudo arp -s 192.168.1.109 08:00:27:53:10:67 # ii Kongming20 对 应的ARP

    1.5.3        {FR tcpdump 观察                   ARP 通信 过 程

        为了清楚地                 了 解    ARP      的   运作 过               ，    我    们                                从 emest-laptop
                                                               程                                                 上执行telnet命令登录
    Kongming20        的       服务                经    )，并
                        echo     〈       已      开启             用     tcpdump    〈  详 见 第      17 章 ) 抓         取 这 个 过程中   两 台
        试机器 之间 交 换 的以太 网                     帧  。具 体 的操作         过 程      如    下 ;
        $     sudo   arp   -d 192.168.1.109                                       # 清除 ARP     缓存中 Kongming20         对 应  的 项
        $     sudo   tcpdump    -i  ethO      -ent   ‘(dst     192.168.1.109         and     sre      192.168.1.108)or
            (dst      192.168.1.108  and      sre    192.168.1.109)'                                                 ernest-
                                                                                       laptop    上 执行
                                                                                                                     #4
        $     telnet       192.168.1.109     echo                                          telnet
        Trying       192.168.1.109...
        Connected        to   192.168.1.109,
        Escape       character       is  '*)".
        “]                                                                         #     输入 Ctrl+]

                                  回
        telnet>       quit(      车)
        Connection         closed.
        在 执行        telnet         命令 之  ，    应 先 清除 ARP         缓存       中    与 Kongming20     对 应 的   ，  否则 ARP            不
                               前                                                           项                          通信
被 执行 ，我 们 也 就 无 法抓取 到 期 望的 以太网 帧 。当 执行 telnet 命令 并 在两台 通信 主机 之 间 建 立

## Page 29

    族 ee 11
    第 1 章 TcPnP 协议


              连接 (telnet 输出 “Connected         to          192.168.1.109"),   $A Ctrl+]                以 调 出 telnet 程序
    TCP              后                                                                                         的     提
                                                                                                               命令
                                                   符后输入    quit，   退
                                                                      出 telnet 客户    端 程序 〈 因
                                                                                              为  ARP 通信
    ARTS,               telnet                     命令提示                                                    在     TCP 连
    接 建立 之 前 就 已经完成 ，故 我                       们 不 关心后         续 内   容    )。tcpdump  抓 取 到 的 众多  数据包    中 ，    只 有 最 靠
        的两个和                        ARP 通信
                                有       关系 ，   现 在 将它们
    前                                                          列出            包 前 面 的 编号 是 笔者     入      ，
                                                                                                         后
                                                                                                 加 的       同   );
              Ll.   O0:16:d3:5c:b9:e3     >     ff:ff:ff:ff:ff:ff,           ethertype ARP   (0x0806),    length   42:
Request who-has 192.168.1.109 tell 192.168.1.108, length 28
              2.    08:00:27:53:10:67     >                                  ethertype ARP   (0x0806),   length    60:
    Reply         192.168.1.109     is-at           08:00:27:53:10:67,    length 46
              由 tcpdump                        抓 取 的 数据 包 本 质 上 是以 太 网 帧  ，我 们   通过 该  命令    的众多 选项 来 控制 帧         的 过
                                                              端IP    地 址  和 源    端 卫 地址) 和   显示    【比如
    HE  Che   SAY dst #         指定通信 的 目的                                                                用           选
                              sro                                                                          -e       项开
    启   以     太 网    帧    头 部 信息 的    显示  )。
              第                  个 数据
                  一                包中 ，ARP 通信 的 源 端             的  物理  地  址  是 00:16:d3:5c:b9:e3 (ernest-laptop)，    目
    的 端的物理 地 址 是    ALDOR
                                A,                                 RA           SRE, FAR RES     LAN.           KLAN 上
    的   所有        机 器 都 会     收    到 并 处        样 的           。
                                                              数
                                  理 这                      帧       值  0x806  是以 太 网 帧   头 部 的 类    型 字段 的      值 ， 它 表
    示 分 用 的                 是 ARP  。            以                                节      际上是      节
                     目标         模块 该 太 网 帧 的 长 度                            为42 字    实                 46 字 ，tcpdump 未
    以太 网 帆 尾部 4 字 节 的 CRC 字段 )， 其 中 数据 部 分 长 度为28 字
    统计                                                                                       节     。“Request”    表 示 这
                            求    ,“who-has  192.168.1.109 tell        192.168.1.108”   则 表示 是    ernest-laptop    要 查询
    是 一个          ARP     请
                                       址。
    Kongming20 的 IP 地
                                   包中 ，ARP  通信 的 源            端的   物理 地 址    是08:00:27:53:10:67 (Kongming20),       Al
              第   二              个 数据
                                是    00:16:d3:S$c:b9:e3       (ernest-laptop)。“Reply”   表
                                                                                             示这 是 一
    的 端的 物理地                  址                                                                         个          应 答
                                                                                                          ARP        ，
    “192.168.1.109                                            则 表示 目标 机 器                    报告
                            is-at                                                Kongming20      其 物理            地址。 以
                                                                                                                     该
                                        节 (实际
    太 网           的  长    度 为           字       上是            字
                            60                  64             节   )， 可 见 它 使    用   了 填充 字 节 来 满足   最  小 帧    长 度 。
              为 了 便于 理解 ，我 们将上 述 讨论用 图                         1-10   来       说明 。
                                                                          详细

                         emest-laptop       telnet客  户     程序                    $               Kongming20




                    AR      |                                                |







    以 太 网 帆                   的 内 容

    以 太 网 帧                 2 的 内 容
                                                           1-10    ARP 通信 过 程

## Page 30

 12 第 一篇 TCP/IP 协议 详解



     一，         我 们 将    两  次     传输    的 以 太 网 帧 按照图       1-6 所 描述 的 以 太 网        封装 格式 绘制 在图   的 下
      第
 半 部 分。
          二 ，ARP          请 求 和   应 答 是   从以 太 网 驱动 程序 发 出         的  ，  而 并 非 像 图  中 描述 的 那样 从ARP
      第
 模块               直接 发 送 到以 太     网上， 所 以 我 们 将 它们 用 虚线 表示         ，    这 主要 是 为    了体现   携带ARP    数据
 的 以 太 网帧和 其 他           以  太      网     比  如      数据 报 的 以 太 网 帧    )   的 区 别   。
              三，路 由    器 也 将 接收 到 以 太 网            帧 1，因 为 该 帧     是      个 广播    不 过     ，         路
      第                                                            一        。          很 显然       由器并
 没有 回应 其 中                 请
                  的    ARP          求 ， 正 如 前  文 讨论 的  那样     。

 1.6   DNS             工作 原理

我 们 通常 使用机 器 的域名 来 访问 这人台 机 器， 直接
                                                               而 不         使用其  IP  址
                                                                                地 ，比     如     访问 因特网
 上 的 各 种 网站           。 那 么   如 何  将 机 器 的 域名转换 成     IJP      地 址  呢 ?  这 就 需要 使用域名   查询 服务  。     名
                                                                                                    域
                                      如        NIS (Network Information Service，     网 络信息 服务   )、DNS
 查询 服务 有 很 多 种 实现 方式                  ， 比
 和 本 地 静态 文件             等 。本 节 主 要讨论          DNS.
 1.6.1 DNS                 查询 和 应 答 报 文 详解
DNS 是 一 套 分 布式 的 域名服务 系统 。每个DNS 服务 器上 都存放 着 大 量的 机 器名和 JIP 地
            并的映射 ，     是   动态    更 新     。  众 多    络             程序
 址                且              的               网         客户 端     都使用  DNS    协议 来 向 DNS    器
                                                                                                   服务
                                                                                                   查询
 目标 主机 的    IP         址
                  地        。DNS 查询    和     应 答 报 文    的 格式 如 图     1-11 所 示 。

 0                                                 15    16                             31










                                                               和应符报广
                                   图 1-11       DNS               查询

       位 标识                       一对           查询
 16               字段用于 标记             DNS          和应符         ，以 此 区 分 一个         DNS 应 符 是 哪个 DNS 查

 名     “标识 ”和 “标志 ”在 《现代 汉语                    词典〈 第 版             在                           机业界也是如
                                                     5 )》 中 表示 同一     但 是 本 书       中             【计算
     此 ) ，  它 们 为 两    个概念        ，代 表 不 同 的 含义    ，读 者在阅读 时 应    严格区 分。

## Page 31

    第1  族 fs 13
     章  TCP/IP 协议


  的回应。
询
        用于协商具体 的 通信 方式和 反馈 通信 状态 。DNS 报 文头部 的 16 位 标志 字
   16 位 标志 字段
  的细节如图 1-12 所 示 。
段

              ee          |               an     |   te     |      wm    [im        |  me   | noe
                1  位      4 位           1  位         1  位     1     位                      位
                                                                            1 位        3 位     4
                                        图    1-12   DNS     报 文头        部 的  标志    字段

   图 1-12 中 各 标志 的 含义 分 别 是 :
        /应答标志。0 表示 这 是 一 个 查询 报 文 ，!1 表示 这 是 一 个 应 答 报 文 。
     opcode， 定 义 查询和应 答 的 类 。0 表示 标准 查询，!1 表示
                                                     型                                 反向查询    (由  IP    地 址 获得
              主机      域名)，2        表示 请 求  服务    器  状态     。
       口      AA，     授 权 应 答 标 志，仅 由 应 答 报 文 使               用    。1    表示   域名 服务 器 是 授权 服务     器  。
                        断标志，    仅 当     报 文 使            用              服务
       口      TC，     截                 DNS                 UDP              时使用。      因 为  UDP 数据 报  有  长 度  限
        节，并 被截断 。
     制 ，所以 过 长的DNS 报 文 将 被 截断 。1 表示 DNS 报 文 超过 $12 字
       ORD,                                                     表示 执行
                                    |                    递归查询            ，即 如 果 目    标 DNS  服务 器 无 法  解析  某 个 主
                名，    则 它 将 向 其  他DNS 服务
              机                                      器继续查询，              如 此 递归    ，   直 到 获得 结果 并 把 该    结果  返
                给客户端。0 表示 执行
              回                                  代查询        ， 即 如       果 目 标    DNS  服务 器 无 法  解析 某  个  主机 名 ，
              则 它 将 自己      的其他                                                        端      参
        知道 DNS 服务 器 的 IP 地 址 返回 给 客户 ，以 供 客 户 端 考 。
   口 RA， 人区 许 递归 标 志 。 仅 由 应 答 报 文 使用 ，!1 表示 DNS 服务 器 支持递归 查询 。
     zero，这 3 位 未 ，必 须 都 为
                          用                      设置      0。
       UL     reode,  4 位 返回
                                                           码，表 示 应 答 的 状态 。常 用
                                                                          值有 0   (无 错误 )    和     不   存在  )。
   接 下 来的4 个 字段 则分 别 指出 DNS 报 文 的 最后 4 个 字段 的 资源记录 数目 。 查询
        对 报 文
    而  言 ，    它 一  般 包含 1                                     个 查询 问题 ，而 应 答 资源 记录 数 、授 权 资源记 录 数 和 额外资源 记录 数 则
   0。           答 报       答 资源                              1，                             资源
    为 应               文 的 应      记 录 数 则 至 少                        而 授权 资源 记录 数 和 额外                记录 数 可 为 0
    非  0。
    或
                问题的格式如     1-13         所 示     。
       查询

         0                                               15   16                                        31





                                       图     1-13    DNS             查询 问题 的格式
              1-13 中  ，    查 询 名以 一 定  的 格式          封装了 要 查询            的 主机 域名       。16 位 查询 类 型 表示   如 何 热
           行 查询 操作    ， 常 见 的 类 型 有 如 下 几 种      :
       口 类    型 A，值是 1，表            示 获 取 目       标 主 机 的 IP            址。
                                                                         地

## Page 32

    14  %* 第 一 篇 TCPIIP       协议 详解

        口 类型   CNAME， 值        是 5，  表 示 获 得 目标 主机 的 别名 。
           型   PTR，   值
            是              12，   表 示 反 向 查询
        加 类                        。
                    类通常    为   表示 获 取 因 特 网地  址     CIP 地
   16              位 查询                           址)。
        应
                      字段、授 权 字 段和额外 信息 字段 都 使 用资源   记录   (Resource Record, RR) at. YE
源 记 录 格 式    如 图 1-14 所 示     。

    0                              15         16        31









                                                资源数据    度可变)

                                                图 1-14 资源 记录 格式

         图 1-14 中            ，32   位 域名是该记录 中 与 资源对 应 的         名字，                 其    格式          和 查询 问题 中 的 查询 名 字
    段 相        同 。16   位 类 型     和  16 位 类 字段 的 含义   也 与             的      DNS 查询 问题     对 应 字段相同   。
         32       位 生存时 间 表示 该查询记录 结果 可 被本                                  地 客户 端 程序    缓存多 长 时间    ，   位是秒。
                                                                                               单
              16  位 资源 数据 长度字段和资源 数据              字段 的 内 容 取决    于 类 型                     字段  。   对 类型  A   言，       源
                                                                                                                    而 资
           是          的             址，                  4〔〈              单位
    数据         32     位    IPv4 地     而 资源 数据 长 度 则 为      以 字 节 为                       )。
                    此，我    们 简要
         至                          地介绍
        了 DNS 协议 。我 们 将 在 后 面 给 出 一个DNS 通信 的 具体 例子 。
                                    请参考其      RFC 文档 (DNS 协议 存在 诸多 RFC UR,                                           文档
    DNS 协议 的                         更多 细节                                                                 REC        介
                                 如            介绍
    绍 其        个 侧面        ，                      的是域名         的 实现
                            比                                                                                        协议
           一                          1035                 和     规范      ，RFC 1886             则 描述        DNS        对
    IPv6   的 扩展 支持)。

    1.6.2  Linux                下 访问 DNS 服务
                  们要访问          DNS 服务   ，    就 必须 先知  道DNS 服务 器     卫              的       址      FA
         我                                                                                  地  。Linux 4    /ete/resolv.
                        来存放 DNS 服务 器 的 IP 地
                                                   址。 机
                                                       器             ，ernest-laptop 上    该 文件  的 内 容
    conf 文件                                                                                    如下;
         #         Generated    by Network Manager
         nameserver         219,.239.26.42
         nameserver          124.207.160.106
               中           个                    首选      器地    址和                   备选          器地址。          件    中   注
         其 的 两                     地 址 分 别 是      DNS            服务  DNS                   服务 文                       的
    释 语句 “Generated by              Network Manager” 告 诉 我     ，这 两  DNS            个          器地址是由
                                                           们                                                  服务 网 络 管理
           写人的。
    程序
         Linux       下      个 常用 的 访问    DNS    服务
                      一                             器的  客户 端 程序   是host，                   比  如 下 面 的      命令是     向 首选
                                                                                       址:
    DNS              219.239.26.42     查询     机 器 www.baidu.com 的                    IP 地

## Page 33

  第 1 章 TCP/IP 协议族 se 15


       $              host   -t A    www.baidu.com
       wew.baidu.com                        is  an alias     for   www.a.shifen.com.
       www.a.shifen.com has                        address        119.75.217.56
       wew.a.shifen.com                       has  address        119.75.218.77
       host 命令
                                           的输出     我们，               机 器 名 www.baidu.com     是   www.a.shifen.com.     的别名    ，     并
                                         告诉                                                                                         且
                                         趾        址     。host      命令 使用      协议
  该    器 名 对应 两 个机                                地                                     和                          服务
                                                                          DNS                DNS     器             通信，其 -t      选项 告诉
  DNS  协议 使 用 哪 种查询 类                                  。 我 们      这 里 使用      是         型                            域名     其       址
                                                   型                      的 A 类 ， 即 通过 机 器 的                            获得       IP 地
                                                                              于         host     命令 的    详细     使   用 方法    ，  请 参考 其
  《但 实际上 返回 的 资源 记录 中 还 包含 机 器 的别名                                            )。 关
                        册
  man  于

  1.6.3                   {FR tcpdump 观察 DNS                           通信 过 程

 为 了 看 清楚 DNS 的过程，
      通信 下 面 我 们 将 从 ernest-laptop 上 运行 host 命令 以 查询 主机
      址，并 使用tcpdump 抓 取 这 过 程
  www.baidu.com                    对 应    的      地                                               中    LAN 上 传输 的
                                                                                            一                         以       太 网 帧 。
       的操作过程如下:
  具体

       $              sudo   tcpdump      -i    echO     -nt   -s  500 port    domain
       §              host   -t A    www.baidu.com

                          次执行                              抓
       这                一          tcpdump             包时，我 们         使  用“port                  过 滤 数    据     包 ，     表 示 只 抓 取 使 用
                                          的数据包         ， 即     DNS 查询
                                                                          和应答报文                  。tcpdump 的 输出
  domain                        )  服务                                                                             如下:
                  1.  IP     192.168.1.108.34319       >     219.239.26.42.53:        57428+          A?  www.baidu.com.    31
       2.             IP     219.239.26.42.53          > 192.168.1.108.34319:     57428            3/4/4  CNAME www.a.shifen.com.,
  A 119.75.218.77,                    A  119.75.217.56            226
       这 两个数据 包 开 始 的“IP”                              出，                                              是
      指 它们 后 面 的 内 容 描述 的 BAR. tcpdump Li “IP
址 端口
  地              .           号”的      形式 来 描述 通信 的 某 一                端 ; 以                 数据   传输     的 方向 ,“>”     前 面      是源 端 ，
  后 面  是                  目的    。     见，第                                 机                      是                                  其
                                   可          一    个 数据 包 是 测试                器 ernest-laptop    CIP     地 址    192.168.1.108       向
                                   器  CIP     地 址  是    219.239.26.42     发 送       的DNS 查询 报    文    〈 目       标     端口S3  是   DNS 服
  首选   DNS                服务
  务 使用的                      端口 ，  这  一  点 我 们 在 前 面    介         绍 过)，   第    二 个 数据    包       是 服务 器 反馈      的 DNS    应 答 报 文 。
                                   包中，数
       第                               个数据     值   S7428     是    DNS  查询
                        一                                                     报文的    标识          值，    因 此 该 值    也 出   现在  DNS 应
  报 文                 中  。“+”   表 示 局 用 递归 查询 标志                          表 示 使用    A    类 型的查询 方式 .“www.baidu.com”
    是  DNS 查询                    问题中 的 查询名             括 号 中 的 数值
  则                                                                           31   是 DNS 查询
                                                       。                                         报文的长度〈               以 字 节 为   单位)。
      包中 ,“3/4/4” 表 示 该 报 文中包含 个 应 答 资源记录 个 授权 资源
       第                二              个数据                                    3                  、4                           记录和   4
  个 额外                       记录
                      信息             。“CNAME www.a.shifen.com.，A 119.75.218.77，A 119.75.217.56”                                     3 则 表示
  个应 答 资源记录 的 内 容。                                     中 CNAME 表示 紧 随         其     后 的记录 是 机 器 的                 别名  ，A      紧 随 其 后
                                              其
                                 址。该 应 答 报 文 的 长 度为                 226   字 节    。
  的 记录 是 IP 地

                                                                                             者     项 )。     如 果 使     用  -X 选
  注意 ”我们抓包 的 时 候 没有开启                                  tcpdump 的 -X 选               项 〈 或        -X                               项，我
                          将能看到        DNS     报 文 的     一         个    ，  也 就       能         明白 上 面 31  字 节 的 查询
                      们                                                                                               报文和      226 FH
                      的应答 报 文 的具体 含义。限 于 篇                             ，这 里 不 再 讨论                 ，读 者 不 妨自己 分 析     。

## Page 34

 16 +  第 一   篇 TCP/IP     协议 详解

 1.7   socket #1                                 协议
     TCP/IP                              族的      关系
     文提到，    数 据 链    路   层 、网 络     层、  传输层    协议 是 在 内 核    中  实现     的 。  因 此 操作 系统 需要
     前
     一组系统调用，    使 得       应用 程序 能    够 访问 这 些 协议 提供 的 服务          。实 现 这 组 系统 调用   的  API
 实现
                                         编程接口)         主要
 Application Programming Interface，  应 用 程序                      有 套     socket 和 XTI。XTI
                                                                  两
基本不     使 ，    本 书 仅讨论                   FA        显示
 现在                                                                  协议
                                                                     关系
     用                         socket.     1-1      了 socket  与  TCP/IP     族的  。
     的这一组                      提供
     由 socket 定义          API        如下两点功能:          一 是   将 应 用 程序 数据 从 用 户 缓冲 区    中 复
    到TCP/UDP 内      送缓冲区，                送数据                               的
 制      核发 以 交付 内 核                      来 发           〈 比 如 图     1-5   所 示 send MRO, wy
 者 是从内核TCP/UDP     接收 缓冲       区          中 复制 数据 到 用 户 缓冲    ，以 读 取 数    据     是 应 用   可
                                                       区             ;二                程序
 以通过 它们 来 修改 内 核     中    各 层 协 议 的  某 些      头 部 信息 或 其 他  数据 结构  ，   从 而 精细 地 控制    底层通
 信 的 行为 。比     如 可以 通过                       函数
                          setsockopt     来   设置 IP    数据报   在 网  络上 的 存活时 间    。我 们 将 在 第
    章 详细
 5     讨论这一组      API。
     一提的是，socket          是 一  套 通用 网 络 编程         接口  ，它 不 但 可以   访问 内 核    中         协议
     值得
                                                   栈    、UNIX 本 地 域 协议 栈    等)，
 栈 ，而 且 可以 访问 其 他 网 络 协 议栈         〈 上比如 X.25  协议

## Page 35

 a 2%  协
     卫 议 详解


                                                                  也族的核心协议，
       IP     协议是  TCPAP     协议                                       是     socket  网  络 编程   的基础     之     一  。本 章 从 两          个 方
                     地探讨    协议
 面 较为 深信     IP                      ，
       LIP     头 部 信息       。IP   头 部 信息                 出        个   现在 每 IP    报中                            的                  址、
                                                                                      数据 ， 用 于 指定 IP  通信
                                                                                                                源         端 耻      地
           目的     中               指导 IP              分 片     和
     重组 ，以 及 指定 部 分 通信 行为 。
     报的路由和 转发 。IP 数据 报 的 路由和 转发发生 在 除 目标
       口正                                                                                             机器之           外 的   所     有 主机
   数据
 和 路 由 器 上 它们 决定数据 报 是 否 应 该 转发 以及如 何 转发 。
              于                          即将                           ，                 了             本
       由     32    位 表示 的                      地 址                  全 部 使 用 完 因 此 人 们 开发 出     新 版       的      IP  BI,          ROW
 IPv6  协议 ，而 原来 的 版 本 则 称为                                         IPv4 协议。本 意 前 面部 分 的 讨论 都    是    基于             协议
                                                                                                         IPv4                  的，  只
 在 最 后     一     节 简 要讨论    IPv6                 。
                                     协议
       在 开始 讨论    前      ，我       们  先 简单 介绍                  下 IP 服务
                                                             一        。

                         的特点
 2.1                                 IP 服务

                                         族的动力 ，                        它 为上 层 协议 提供 无 状态    、 无 连接    、  不 可 靠            的  服务  。
IP 协议是TCP/IP 协议
     指 IP 通信 双方不 同步 传输 数据 状态信息 ，因 此 所 有 数据
       无 状态 (stateless)                      是                                         的                            IP            报的
                                送 、传输和 接收 都是相互    独立     、 没有上 下 文 关 系     的     。  这 种 服务    最  大 的  缺点      是 无         处法   理 乱 序
 发
 和 重复      的    IP    数据    。比 发 送 端 发 送出 的
                         报           如                                第     下           报可能比第            个     IP   数     据    报 后 到
 达 接 收     ，    而 同一     个  IP    数据 报 也 可 能 经 过 不 同                  的     路 径 多 次 到 达 接       收 。   在 这 两 种                  下，
           端                                                                            端                                       情况 接
                                                                                                                                   收
 收 端的 IP            模块 无 法检测      到 乱    序    和 重复             ，  因 为 这 些 IP     数据 报 之间 没    有   任何  上     下 文 关 系            。   接
        IP     模块只     收 到        完整的                    数据
 端的        要                 了                                 报          是IP           IP     模块 将      先     执行 重组             )，就
 其数据部分     (TCP                          UDP             数据
                                                               报或者        ICMP   报
 将                                                                                  文)     上 交 给 上 层 协议        。         那 么 从 上 层 协
                                                                                    的     协议    比
 汉 来       ，    这 些 数据 就 可 能 是乱 序                        、     复      。    面向             连接
          看                                              的   重    的                     ，     如   TCP 协议        ，           则 能 够 自己
 理乱序的、重 复                的  报 文          段 ，它递交 给              上 层 协 议 的 内 容        绝对     是 有 序  的 、正 确 的     。
 处
       虽然 中 数据 报头 部 提供 了                 一               个 标识字段       〈      见 后 文)       用以  唯一标识 一     个               数 据   ，   它
                                                                                                                             报     但
                                                 的  ，而     不         是 用 来 指示 接收 顺序 的     。
 是被    用     来    处理 IP  分 片   和  重组
       无    状态     服务 的 优点 也 很 明显                          简单            、
                                                               高
                                                     :                效。       我 们  无须 为 保持 通信 的 状态 而 分 配                      一 些 内
       源，  也 无 须 每 次        传输数据 时 都携带状态信息。在                          网             络     协 议 中，无状态 是 很             常见
的，                                                                                                                                 比
 核资                                                                                                                                如
       协议
 UDP       和 HTTP        协议 都 是 无        状态                          协议 。以 HTTP 协议 为     例  ，一  个 浏览  器  的              连续 两 次 网 页 请
                                                                                 服务
 求 之间       有 任何 关联         ，它      们     将被Web                             器独立地处理。
                                             指           IP     通信 双方 都 不    长      久 地 维持 对 方 的 任何 信息              。     这
       无 连接 (connectionless)             是                                                                                     样，  上
 层 协议 每 次 发    送    数据 的 时               ，都          必须 明确 指定 对 方     的 IP                址。
                                         候                                          地

## Page 36

 18  w%*     第 一篇 TCP/IP 协议 详解

不 可 是指耳 协议 不 能 保证 数据 报 准确地 到 达 接 收 端 ，它 只 是 承诺 尽最大 努力
 (best effort)。 很 多 种  情况 都       导致 了  报发送失败            如，  个      现
 能       数据     。比 某 中 转 路 由 器 发                                            中 数据
 报 在 网络上 存活 的 时 间 太长    〈   根 据     数据 报头 部 字段  TTL      HU,    SUG),
               一个 ICMP 错误   消息 〈   超 时错误) 给
 之 ，并 返回        发    送    端。                           又 比如 ，接 收 端  发现收到 的 IP 数据
 报 不 正确 〈 通 过 校验机制 )，它     将丢弃
                             也      之，    并     一个    错误)
         返回                                           ICMP 错误 消息  CIP     头 部 参数
         TCA,      IP                     一旦    到         报发送失败，             上层协
         模块
         检测                                           IP           数据       就 通知
    发送失败，    而 不    会 试图
                             重传。因
 议       此，
         使    用     服务
         IP      的上层协议                                    〈  比 如 TCP 协议)   需要 自己
 实现 数据 确认、超 时 重 传     等 机       制以      的     。
                                   达到 可 靠 传输    目的

 2.2     |IPv4  头 部 结构

 2.2.1     IPv4 头 部 结构

         的 头 部 结构
     IPv4        如图    2-1    所 示    其 长 度通常为   20 节，     含有     选项
         。        字 除非                                    可 变 长     的    部 分  。

     0        15     16                                                    31










 |                                选项                  ，  最 多    40字 节
                          图       2-1                 IPv4     头 部结构

      版本号   (version) 指定          议 的 版                   。    对 IPv4
 4 位                                                      来说，               其 值  是 4。   他 IPv4
                                                           本                    其             协议 的
 扩展 版本 如              和      )，                           号                                也
      〈     SIP 协议       PIP 协议       则                  具有 不 同 的 版 本    〈  它们 的 头 部 结构    和 图 2-1
   同)。
 不
                                                 该        LAA B+         bit
 4 位      头 部 长度〈header length)       标识                                    字      字
                                                      IP        32              4   节)。       为4位最
                                                                                                 因
      15， 所
 大 能 表示      以  IP 头 部 最  长是 60   字 节  。
      服务 (Type        Of Service,             TOS)    包括 一     3  位 的       权 字 段  (现在
 8    位  类型                                               个      优先                     已经被忽略)，
      的TOS 字段
             和  1  位  保留字段  〈   必 须         0)。4 置        位
                                                          的    TOS 字段
 4位                                                                          分别  表示 :   最 小 延 ，  最
                                                                                              时  大
 叶    量 ，最 高可车性    和  最 小 费用。               多其 中 最    有     一   个 能 置为 1，  应 用 程序 应   该 根据 实际 需要 来
      它。比 如像ssh Al telnet 这 样 的   登录                    程序 需要 的 是 最 小  延 时  的 服务  ，而 文件    传输 程序fp
 设置
    需要
 则 最 大 吞吐 量     的  服务 。

## Page 37

    2章 详解 se*
    第 IP 协议  19


                位 总     长    度    〈total  是 指               整个        数据
         16                               length)                 IP      报的长度                      ，以       字 节 为 单位                          ，      此             报
                                                                                                                                                              因 IP 数据
                                          节。                但 由    于 MTU  的     限制           ，      长        度超过     MTU 的                                         数据
    的  最  大 长 度         为 65      535   2 -1 字                                                                                                                   报都将被
    分 片 传输         ，所         实际 传输                   数据
                          以                  的   中        报     〈 或 分   片) 的 长 度 都              远   远 没有           达到 最 大                           值 。 接 下 来    的  3
    个 字段 则         述      了 如何 实现分 片             。
                                                                                                                                                      数据 其 初始 值 系统 随机
          16                 位 标识 〈identification)    唯一    地 标识 主机 发 送 的 每                 一             个        报。                                    由
    生成 ， 每发送                      个 数据
                          一                 报，其 值        就 加    1。 该 值在数据报 分                 片      时     被 复制 到 每 个 分 片                                 中  ，   因 此 同
    一  个 数据 报的所有 分 片 都具有 相同 的 标识 值                                  。
                                                                                                                                                 分片               如 果
         3     位 标志 字段 的 第                一  位 保留     。 第 二 位     (Don’t   Fragment,                DF)                                             表示 “禁止          设
        了这个位            ，IP   模块 将           不          对 数据 报 进行 分 片   。在 这 种 情况
                                                                                             下，                  如
                                                                                                                 果                 IP                   数据 报 长
    置                                                                                                                                                  度    超过    MTU
        话      ，IP 模块 将                 该数据 报 并         返回 一      个 ICMP   差错 报              文 。        第 三          (More Fragment,位                             MF)
    的
                   多分        片
    表示 “更                     "。除       了 数据 报        的 最  后    一  个 分 片 外 ，                        都其他 分 片  要  把 它      置                   1。
                                                                                                                                                                   指数
          13    位    分 片 偏移 〈fragmentation offset)                    是 分 片  相  对 原始                下        数据 报 开始                              处 〈  仅           据部
    分 )  的 偏       。      实 际     的   偏   移   值  是 该 值 左    移      位     乘 8 后
                   移                                        3                    得          到       的。                                   由 于 这 个 原因 ，     除 了 最 后   一
                   片外，每
    个  IP 分                       个IP     分 片的           部分的       长  度 必 须 是 8     的 整数                倍                               〈 这 样 才能 保证 后 面     的  IP   分
                                                 数据
    片拥有        一   个 合适           的   移值)。
                          时间      (Time To Live,          TTL)     是 数据 报到 达 目                 的    地 之 前 允许
         8     位 生存                                                                                                                           经过  的   路 由    器 跳  数 。
                                  【常见 64)。 数 据 报 在 转发 过                                                   中  每 经     一                       个 路 由      该 值 就       路
    TTL       值 被 发 送             设置             的值是                                           程                                    过               ，       被
              减 1。   当  TTL       值 减       为 0  时 ，路 由 器 将丢弃 数据                              ，并
    由 器                                                                                      向      源 端 发                                    一个               ICMP 差错
                                                                                报                                                       送                         报文。
TTL 值 可以 防止数据 报 人 路 由 循环 。
    位 协议 〈protocol) 用 来 区分上 层 协议 ， 们在第 章讨
         8                                                              我               1                    论过      。/etc/protocols                            文件 定义
                                                            的数值 。其中，ICMP                                                               是 1，TCP
                                                                                                                         是                     6，UDP
    了 所 有 上 层 协议 对 应              的       protocol 字段                                                                                                  dt   17. /ete/
protocols 文件是RFC 1700 的 一 个 子 集 。
        以检验
   16 位 头 部 校 验 和 Cheader checksum) 由 发 送 端 填充，接 收端对 其 使用CRC 算法
                                            检验 在                      过 程  中 是 否 损坏            。
    IP   数据 报头部                   ，仅                  头部)     传输
                                          址和目的
         32     位 的 源端IP 地                              端IP     地 址 用 来  标识 数据 报 的 发 送端和 接收                                                    端    。 一   般 情 况 下   ，
    这 两个地 址 在 整个数据 报 的 传递过 程中保持 不                                         变 ，而 不 论 它 中间 经 过 多 少                                                            个中 转路由 器。关
    于 这 一          点，我 们      将 在 第 4       章 进       一 步 讨论 。
                                                                      可 变 长
         IPv4      最    后                   个 选项字段    Coption) 是             的  可       选                    。                           这 部 分最多 包含       40        字
                             一                                                               信息                                                                    节，
    因为    IP       头 部 最 长是       60           节【〈                                                                                           部分)。
                                                     字 其 中 还 包含 前 面 讨论              的 20     字 节 的 固定 可 用 的                                                    IP   选
    项 包括       :
         口 记录 路              由              route)，      告   诉              数据 报 途 经的 所 有 路 由                器 都 将自己 的                                        址 人  IP
                                  部分，       这 样 我 们        就 可以 跟踪 数据 报 的               传递路             径      。
                头 部 的 选项
                                                                                                                                                      间          IP 地
         口 时       蕉                                        路             将                                                                      时        与         址
                   间                                  告 诉 每 个     由     器 都  数据 报 被 转发 的时 间                                                  【 或
                 Xf) HLA          头 部 的 选项
                                  IP      部分，               这 样 就 可以 测量 途经                      路由           之 间 数 据 报                         传输     的   时 间  。
                   源路由选择                    Cloose          routing
         CD 松散                                        source            )，指 定       一 个 路 由                    IP  地     址 列表                              ，数 据 报 发送过

## Page 38

20      第 一      篇  TCP/IP         协议 详解

                            程中必须 经 过 其 中所有 的 路 由   器   。
    加    格 源 路 由 选择            (strict     source routing)，     和                       源路由选择                    ，                     报只能经
                                                                          松散                   类     似                               不 过 数据
       过 被指定 的路由 器                 。
    关于   IP 头 部     选项   字段 更 详细 的 信息                   ， 请 参考            IP                  的     文档
                                                                                   协议
                                                                                                    标准                RFC 791。 不 过 这 些
          很少被使用，        使 用松散 源 路 由 选择                                        和 严格 源 路 由   选择 选项 的 例子              大        仅概 有 traceroute
项 字段
程序 。     外，                 路由                   的        品    用                                                      和
       此 作 为 记录                    IP 选项
                                    替代                                       ，traceroute   程序 使     UDP 报 文                      ICMP 报 文 实
现 了 更 可  靠 的记录 路 由 功能 ，                   详 情 请         文档
                                    参考                                          RFC 1393.

              用
2.2.2   使 tcpdump 观察 IPv4                               头 部 结构
    为了深入 理解                          头 部 中     每个
                                                 字段
                     IPv4                               的含义               ，        我 们  从      机器
                                                                                                    测试    ernest-laptop         上 执行 telnet
                     用  tcpdump            抓    取 这 个 过   程   中 telnet 客户                     端 程    序    和                         服务 器 程序
命令登录 本 机，并                                                                                     telnet                                   之间交
         包。   具 体 的     操作         过     程 如 下  ，
换 的 数据

                                                                                               上    的  数据
    $  sudo  tepdump       -ntx     -i    lo                          #     抓      取 本 地 回路
    §  telnet     127.0.0.1                                           #     开启 一个 终端 执行        kelnet
    Trying   127.0.0.1...
    Connected     to  127.0.0.1.
    Escape   character       is     '*]'.,
    Ubuntu   9.10
    ernest-laptop     login:       ernest                             RAMP LHEOE
    Password:                                                         #     输入 密码 并 加
                                                        包，      其 内 容 如 代码
    此 时 观察       ttpdump 输出 的 第             一    个数据                                               清单2-1     所 示  。
一                                             代码 清单2-1  Fi tcpdump                  抓 取 数 据 包
                 -一 -一
    IP 127.0.0.1.41621             > 127.0.0.1.23:        Flags           (S],         seq     3499745539,        win      32792,
    options   [mss    16396,sackOK,TS              val  40781017          ecr          0,nop,wscale          6],   Length      0
           Ox0000:    4510     003c     a5Sda    4000   4006    96cf               7FOO    0001
           Ox0010:    Tt00     0001     a295     0017   dO99    e103               0000    0000
                               8018     fe30     0000   0204    400c               0402    O80a
           Ox0030:    O26e     4449     0000     0000   0103    0306
一                                                                     人人人人人人人人人一
    该数据 包 描述 的是一个                  I        据        。  由 于 我                   们  是 使     用  telnet     本机的，                   以
                                                报                                                                            登录 所    IP  数据
                                   端 IP    地 址 都是    “127.0.0.1”。telnet                        服务 器 程序 使 用                      号
报 的 源    端IP  地 址    和目的                                                                                                 的     端口       是23
(参见   /ete/services 文件 )，          而 telnet 客户              端             使用临时                       号                          器     。
                                                              程序 端口                            41621              与服务
                                                                                                                                通信        关
              号，我     们         将 在 第  3                                “Flags”. “seq”、“win” 和 “options”                       描 述   的 都  是
于 临时 端口
    头
TCP    部       信息 ， 这 也 将 在 第      3       章讨    论 。“length”              指 出           该  IP  数据 报 所 携带 的            应用       程序      数据 的
  度。
长
    这 次抓包 我 们 开启了                  tcpdump       的      选项            使        ，   之输出        数据  包 的      二 进 制         。     此         包共
                                                -x                                                                    码                  数据
                                                     部，
Le 60 字 节，其 中 前                    字 节 是         头       后                          字 节 是  头
                             20                                 40                         TCP            部， 不                       应用程序数据
                                                                                                                                         包含
                                                                                       表   2-1       所
(length 值 为 0)。         现 在 我 们     分 析    IP 头 部 的 每    个    字 节                  ， 如              示。

## Page 39

                                                                                                                        第 2 章     IP    协 议 详解 *%*  21

                                                                                                           详解
                                                              表  2-1      1IPv4                    头 部 各 个 字段
               十 六 进 制 数                                                                                                      头
                                                                                                                        IP      部    信息
            区
            0x5                                    Po                                       头 部 长            度 为    5   个 32  %        字节)
                                                                                                                               20
            0x10                                                                            TOS            选项 中     最小 延 时 服务   被       开启
            0x003¢                                                        ee                        报总长度
                                                                                            数据                            ，60 字 节
            OxaSda                                                                                  报
                                                                                            数据
                                                                                                        标识
            0x4                                                                                     了禁止分片标志
            0x000                                                                           设置
                                                   ET
            0x06                                                                     ee
                                                                                            协议 字段             为 6，             上层        是
                                                                                                                        表示
                                                                                                                                  协议      TCP 协议

            Ox7f000001                                                                      32 {Mie IP 地                  址   127.0.0.1
            0x7f000001                                                                      32  {i  Fi              IP        址
                                                                                                                        地 127.0.0.1

                表  2-1 可
            由                          见     ，telnet 服务 选择 使 用 具有 最 小 延                     时   的   服务        ，     并 且 默 认       使 用 的     传输    屋 协议 是
    TCP     协议 〈 回 顾 第                    1   章讨 论 的 分        用  )。                           这 些 都 符合 我 们 通常    的  理解        。  个         报没有被分
        这 IP 数据
片 ，因 为 它 没有的 任何 应 用 程序数据。接 下 来 我 们 将抓取 并讨论 被 分 片的IP 报，
        数据

                                     片
    2.3         |IP                  分

                文曾提到，当                        IP 数据 报 的 长度               超过 策        的MTU        时 ，    它 将 被分 片 传              输       。分 片 可 能   发 生 在
            前
    发 送            也 可 能               发  生   在    中 转路   由     器 上  ，    而 且 可 能 在             传输 过 程        中     被     多 次 分 片       ，但  只  有 在 最 终 的
                   机器上，                                   内                            重新
           目标 这 些 分 片 才 会 被 核                                 中      的    IP 模块             组 装  。
            IP  头   部 中        的 如 下 三 个 字段 给                 IP     的 分 片    和      重组 提供 了 足够                     的   信息               报        、
                                                                                                                                               数据
                                                                                                                                 :             标识   标 志和
    片 偏 。             个                                                   具有                               部，                                         具有
     移 一 下 数据 报 的 每 个分 片 都 自己 的 IP 头 它们 具有 相同 的 标识 值 ， 但
    不 同 的 片 偏      。并 且 除 了 最 后                           一     个 分 片         ，    其 他分 片 都        将设置MF                      标志    。    外，         个分片的
                    移                                                       外                                                                        此 每
    IP 头 部      的          度       总 长 字 段    将    被 设置 为 该 分 片             的   长  度 。
                                                              节    〔 可
                                       MTU 是 1500 字                             以 通过 ifconfig      命令 或者                  netstat     命令 查看  )，    因 此 它
            的       据          报 的 数据 部 分 最                   是                                 部                             节
    携带                                                        多      1480     字 节      CIP 头 占用 20 字 )。                              用     IP     数据 报 封
                                                   的 ICMP              报
    装                      1481 个 长 度为       字 节                     文               8 字 节 的                  头
       一                                                                                                 ICMP             部，    所以 其数据         部 分 长 度 为
    1473    字 节 )，     则 该数据 报 在 使                            用以     太 网  是 传    输     时 必须 被 分               ，         图
                                                                                                          片         如 2-2         所 示  。
                                                                                                                              片，    第  一  个 分 片 长度     为
            图 2-2 中，                          为长 度 1501 字 节 的                据 报 被     拆    分 成 两           个       分
                节，第                    个           分 片 的 长 度为             字
    1500    字      二                      下                   21              节。       个                                                   部        节)，
                                                                                        每 IP                              AGH     IP    头          20 字
                                                                                        第  二个   IP      分 片           的   IP   头 部 则     没有 设置 该   标志 ，
    且 第 一 个耳      分 片                          的 耳 头 部 设置     了       MF       标志 ，而
                                                                       始        数据 报 中
    因为 它 已 经是最 后                          一        个 分 片  。    原         下                 的ICMP             头 部 内 容 被 完整                地      到
                                                          了                                                                                复制 了 第      一

       后       此 列 中 的                  空格表示 我     们 并 不 关心 相应 字段 的             十 进 制  值。

## Page 40

22 “%* 第 一篇   TCPIIP   协议 详解

个  中 分 片中。第   二    个 IP 分 片不    ICMP  部      ， 为      重组
                     头
                     信息 因                        IP 模块  该       ICMP 报 文 的时 候
    份   ICMP     头 部信息    ，重 复  传送这 个信息 没有 任何 益处     。1473 字 节  的 ICMP 报 文 数据
只 击 要  一
                         复制到第一个 IP 分 片   中   ，使 其 总 长 度为1500 字 节，从 而   满足 MTU
的 1472 字 节 被     下 模块
的 要 求  ，而 多 出 的  最 后 1 字 节 则 被 复制  第  个      片中。
                     到
                                    二    IP  分
                     数 据报     字 )
                     ICMPHR C1481                4


                 ORB                         eet



                                          8 字 节               1472 字 节                                       20 字 节    1      字 节
                                               分 【1500
                     IP               片            字节)                                                         IP 分片   字节》
                                                                                                                 〈21
                                                                                        报被分片
                                          图 2-2     携带        ICMP           报 文的 IP 数据

              指出 的     ，ICMP              报 文    的     头   部长     度取决 于 报 文             的 类           ， 其 变化 范围 很           。 图      以
                            是                                                           型                        大                 2-2
                                                                                        使用的               ICMP     回  显    和 应 答 报 文 的
8 季节 为例 ，因 为 后 面的 例子 用 到 了                ping                    程序     ，  而     ping    程序
  部长度是                8 字 节
头                           。
          了看清楚       分                    具体
     为                                                      考 ernest-laptop 机                                Kongming20，         每 次 传
                         IP      片的       过程，                     虑从                    来ping             器
                          的数据 〈 这               是 ICMP                    报 文 的数据
                                                                              部分)   以     强制 引起     IP     分
                                                                                                             片，  并
                                                                                                                 用    tcpdump        抓
枯 1473                      节                                                                                                        取
这 过 程 双方 交换                           数据 。具 体操
  一           中                的          包                    作过程如下:
                                                                                  只抓取               看
     §     sudo  tcpdump     -ntv         -i     eth0     icmp              #           ICMP        文
                                                                                        指定 每次发送           1473            字 节
     $     ping         Kongming20        -s  1473                          #     用 -s    选项                     的         数据
     下 面 我 们 考察 tcpdump 输出 的 一个                                IP               数据 报 的 两个 分     片  ， 其 内   容 如 下
     l.   IP(tos         0x0,  ttl        64,    id    61197,  offset     0,      flaqs   [+],     proto   ICMP  (1),  length   1500
                     192.168.1.108        >   192.168.1.110:      ICMP    echo      request,       id     41737, seq  1, length   1480
     2.   IP(tos 0x0,     ttl             64, id    61197,  offset    1480,       flags   [none],     proto  ICMP    (1), length  21
             192.168.1.108                  > 192.168.1.110:       icmp
                  IP Sh                   61197，                  说 明 它们 是 同一           个             数 据   报 的 分     片   。第  一   个分 片
     XDA
的片偶 移 值 为 0，         而 第 二 个 则 是 1480。                         很 显然     ，   第 二 个分 片    的     片 偏 移 值            实际   上 也 是   第    个 分 一
片     的 ICMP               报 文的 长     。第     个 分 片 设 置了            MF       标志以 表示 还 有 后 续分                      ，     以       tcpdump
                                 度        一                                                                      片   所              fi
                                第         个分 片 则     没有     设置 任何 标志              ， 所
出 “fags [+]”。        而           二                                                      以 tcpdump 输出 “flags      Cnone]”.           ix
个 两     个 分 片 的 长 度分 别                为    1500     字 节 和   21     节，                  图                的一致。
                                                                  字 这与                                2-2 描述
          后     ，IP  层 传 递 给数据 链 路 层      的                 数据 可 能 是 一               个完整         IP       数据
     最                                                                                        的           报，     也 可 能 是 一      个 IP 分
                     为           分
                     组                    本书 如 无特殊
                                                                                    声明，           分       数据
片 ，它          统称                                                                                                 报和 IP 分
          们                               (packet)。                                         不 区      IP          组。

2.4       IP

     IP   协议     的     个 核心 任务 是 数据 报 的 路                         由     ， 即    决定 发送数据 报到 目标                     机   器 的  路         。为
                     一                                                                                           径                了理

## Page 41

第 2 章 议 解
    协
    详    ** 23


              们先简要 分 析 IP 模块的 基本 工作 流程 。
解      由     ，我
2.4.1  IP 模块 工作 流程

   IP  模块 基本 工作 流程如 图 2-3 所 示 。



                                  /                                  一    人

                                      aah                              送                   的  |
                 发 给 本 机                                                                        |

              i                   ¥
              |    理IP                                                                   部
                                      计算 下       一          数据 报       处
                                                                   源 站                          |
              |                          IP输出                      a                            |
              :                                                                                 i
              IPB                                            网络        动程序          |           |
              7    | ”来自

                                                               基本工作流程
                                      图  2-3     IP                模块
    我 们 从 右 往 左 来分析               2-3。  当    IP  模块 接收 到 来 自     数据 链 路 屋           的   JP 数据 报时     ，   它 首先
对 该 数据 报          头    部 做     校
           的              CRC      验，确 认 无       误  之 后 就  分   析 其 头 部    的  具体 信息       。
    如 果 该        IP 数据 报   的 头 部 设置    了   源 站 选    路    选项〈   松 散 源   路  由  选择 或        严格   源 路 由   选择 )， 则
IP 模块调用 数据 报 转 发子模 块 来处理 该 数据                            。  如 果 该      据 报 头
                                                        报      IP                 的     部中    目标  JP  地 址 是 本
    址，或 者 是 广播
    地址 ， 即 该数据 报 是 发送给 本 机 的 ， 则 IP 模块
机 的 茶 个 IP 地                                                                                      就根据数据报
头 部 中 的 协议 字段 来 决定将它 派 发 给 哪个上层                            应用     用)。     果                  现
                                                                       如        IP     模块 发       这 个  数据 报 不
是 发 送给本 机         的    ，则 也 调用 数据 报 转 发         子模    块 来  处理 该 数据 报     。
                                                是否人   允许   转发，    如 果 不 允许      , IP     模块 就     将数据 报  丢弃 。
    数据 报 转 发子模 块 将首先 检测 系统
   果人允许 ，数 据      报 转发     子   模块     将对 该   数据 报 执行 一 些 操作        ，   然 后   将       它 交 给 IP     数据 报答 出 子 模
如
块 。我 们 将 在 后      面 讨论           数据 报 转发的 具体 过 程     。
                                                                        机器)，以 及 经 过      哪个     网 卡 来 发  送 ，就
    IP   数据 报应 该 发 送 至 哪个下 一 跳 路由【〈 或者目标
                                         下一跳路由”            子 模块 。LP       AR
JE IP BRA,        2-3          中  “计算
    是路            。               数          目标             分类
结构 表      由          这 个 表 按 照        据 报 的         IP     地址 ，同       一  类 型                 数据 报 将 被 发 往 相同
的 下     中    由 器     〈  或者
                  目标
    一                          机器)。      我    们  将 在 后     面讨论 IP  路 由 过 程   。
                                                           数据 其 中 除了 需要 转发                      的 IP       数据
    IP   输出 队列 中 存放 的 是所有 等 待 发 送                    的 IP      报，                                         报外，
                                             文      、TCP   报 文 段  和UDP 数据
                                                                                  报)     的 IP 数据
还 包括     封装  了 本 机 上 层 数                 报                                                          报。
    几 2-3    中 的 虚线 箭头 显示了 路 由 表 更 新 的 过                     程 。这 一    过   程 是  指   通过     路 由 协议 或者    route
命令 调整路由 表         使 之更适应 最 新的网络 拓扑 结构 ，称
                  ，                                          为       也    路 由 策略    。    我    们 将 在 后   面 简单讨

## Page 42

24 第 一篇 TCP/IP 协议 详解


 论它  。

                  由      制
2.4.2                    路
                        机

                                  制，需 要         先 了 解       路    由 表
     要 研究           IP  路 由 机                                          的  内容。         我 可以           用            命令
                                                                                        们            使   route        或netstat 命令
                  路由表。 在      机器                                                        ，                         清单
查看                        测试 ernest-laptop                  上 执行       route 命令 输 出 内 容                  如代码         2-2  所 示  。

                                                  代码 清单                   ”路
                                                                2-2        由表实例
     Kernel       IP    routing    table
     Destination              Gateway                  Genmask                    Flags        Metric          Ref     Use Iface
     default                  192.168.1.1              0.0.0.0                    UG           0               0       0   etho
     192.168.1.0              *                        255.255.255.0              U            1               0       0   etho

             路由表      包 含 两       ，    每 项 都包含              个字段         ， 如
     该                            项                    8                   表      2-2    所 示  。

                                                       表 2-2           路 由 表 内 容
             字    &                                                               a        &
       Destination                                        目标
                                                网络或主机
       Gateway                                       *     表示目标        和 本 机 在 同一 个 网             ，         不需要
                                                                                               络             路由
       Genmask                         网 络      掩 码
                                           由项标志，常               见 标志
                                       路                         有如下 5     种                         route     命令 的man Fi):
                                       口   U，该 路       由 项 是 活动 的      ，
       Flags 8                         口 H，       该  路 由 项 的 目标 是 一          台 主 机    ，
                                       OG,                      AEX:
                                       口 D，       该路由 项 是 由 重 定   向           生成 的    ，
                                       口   M，     该  路 由    项 被    重  定向 修改 过
       Metric                          Pe     FE,                               ee         ey
       Ref                             路 项             用 的 次       数  (Linux 未
                                           由                                      使用)
       Use                                           ae EA
       Iface                           该 路 由 项 对 应 的 输出               网 卡 接 口
|
     代码 清单            2-2  所 示 的     路 由   表 中      ，  第        项 的        地址是                                     默认     。
                                                             一         目标                default，       即 所 谓 的       路 由     项 该
                              志，    说 明 路 由 的 下                    跳  目标    是  网  关    ， 其 地 址       是                〔〈   这 是 测试
项 包含 一 个“G” 标                                                一                                          192.168.1.1             网
络 中 路 由           器 的 本 地  IP     地 址  )。  另 外       一        个 路 由 项  的  目标 地 址           是 192.168.1.0，         它 指 的 是 本 地 局 域
                                                                                         转，  可以 直接
                                                                                                              发送到  目标
网。该路              由 项   的网 关 地 址    为  *，       说 明 数 据 报 不 需要 路 由 中                                                  机器。
                  路由                                                                                                  址，       匹配
     那么 表 是 如 何按照                               地 址 分 类 的 呢 ?             或 者  说 给 定数 据 报 的 目标                    中 地 它 将
                                                                    制  ， 分
路 由 表 中 的 哪           一  项 呢  ?    这 就 是      IP  的             路 由 机       为  3  个      步骤  :
     1 查找 路 由 表 中 和 数据 报                        的目标        IP     地 址  完全 匹配           的 主机    IP    地        。 如 果 找 到 ，     使用该
                                                                                                          址                     就
路 由          ，    没 找到
          项             则转步骤      2。
                      路由表中        和 数据
             查找                            报的     目标                    具有   相同
     2                                                      IP       地 址               网路        ID  的 网  络    卫   址            清
                                                                                                                       地 〈 比 如 代码
                                                                     找到，就 使    用  该      路   由    ，  没 找到
单 2-2     所       的 路 由                中 的 第 二 项 )。  如 果                                          项           则转步骤

## Page 43

 2章 IP 协议 详解 f+ 25
 第


      3         选择默认     路 由          项，     这 通常 意味 着 数据 报 的 下 一 跳               路     由   是 网 关  。
      因此，对 于                       机器                            言，                              为
                        测试                   ernest-laptop 而     所 有 发            送 到  IP   地 址     192.168.1.*         的 机 器   的 IP
 数据 报 都 可以 直接 发                     送 到      目标 机 器 〈        匹  配 路 由 表 第 二       项    )， 而  所    有 访问 因特网           的  请 求 都   将 通
 过 网 关 来        转发      〈 匹  配    默认 路 由 项          )。
                     由表    更      新
 2.4.3          ”路
      路 由 表 必 须 能 够 更                        ，以     反映 网 络 连 接    的    变化     ，     样                          、        效         数
                                        新                                      这         IP  模块 才能 准确                        高 地 转发
       报                 可以                  路由 。                             例子                                          ):
 据     。route     命令
     修改 表 我 们 看如 下 几 个 〈 在 机 器 ermest-laptop 上执行
      $ sudo         route   add    -host       192.168.1.109     dev     ethn0
      > sudo         route   del    -net      192.168.1.0       netmask       255.255.255.0
      $ sudo         route   del    default
      $ sudo         route   add    default         gw 192.168.1.109          dev etho
      第         1 行  表示 添加 主机           192.168.1.109〈           器                                                。         之
                                                             机     Kongming20)              对 应 的   路 由        项     这 样 设 置    后 ，
 所 有 从      emest-laptop         发 送 到                Kongming20 的       据 报 将 通过 网
                                                                 耻                           卡    eth0   直接 发    送               机器
                                                                                                                               至 目标
                     网卡。第 2  行    表示 删除 网               络        对应的路由项。                         样，            器
 的 接收                                                        192.168.1.0 这                               除 了 机           Kongming20
          试     机    器  ernest-laptop          将 无    法 访问 该 局 域 网    上       的任何 其 他 机 器                能 访问
 外，测                                                                                                〈          到         Kongming20
      由 于 执行了上           一               条 命令)。第    3   行    表示 删除    默认     路 由  项 ，   这 样 做 的     后    果 是 无    法     访问 因特网    。
 第  4 行 表 示 重 新          设置        默认    路 由          项 ，    不 过 这 次 其 网 关 是 机 器        Kongming20《〈       而 不    是         能 直接 访问
            的路由器)        !       经  过   上 述 修改            后  的 路 由     表 如 下 ;
 因特网

      Kernel         IP   routing     table
      Destination           Gateway                          Genmask              Flags      Metric        Ref       Use   Iface
      Kongming20            *                                255.255.255.255      UH         0             0         0
      default                         Kongming20             0.0.0.0              UG         0             0         0     ethd
      这 个 新 的 路 由 表 中，第                      一      个 路 由    项   是    主机 路 由   ，  所 以 它 被 设置             了    “H”       志。       们设
                                                                              项                                                 标 我
 计 这 样一个 路 由             的                                讨论
                         表 目的 是 为 后 文                          ICMP 重 定向 提供 环境              。
      通过 route           命令 或 其              他工具        手工 修改    路    由 表    ，          是 静态 的 路 由  更 新 方式     。对 于 大           型 的
                                  通过                   BGP (Border Gateway Protocol，     边
 路 由            ，它   们 通常                                                                    际   网  关 协
            器                                                                                              议         )、RIP (Routing
 Information Protocol，             路 由 信息 协议                   )、OSPF  等协议    来 发    现   路   径   ， 并 更 新   自己     的     路 由 表 。   这
 种 更 新 方式 是 动态                 的  、 自 动       的     。这 部分 内 容 超        出了 本 书     的讨论 范围 ，              感            兴趣 的 读者 可 阅读 参
 考 资料       1  。

 2.5        |IP   转发

  文提到，不 是 发送给 本 机 的 数 据 报将由 数据 报 转发子模块 来 处 理。路 由 器 都能执
前
 行 数据 报 的 转发 操作，                    而 主机        一     般 只 发    送和 接收 数据           报 ，        这 是 因为 主机        上 /proc/sys/net/ipv4/
                                                             0。我 们 可 以   通过       修改 它 来 使        能 主机 的 数据 报 转发                 功能
 ip_forward     内 核      数       默  认 被      设置           为
                                             以  root         身份 执行    ):
 (在 测试机 器           Kongming20 上

## Page 44

    |
    26 第 一篇 TCPI/IP 协议 详解


       # echo   1       /proc/sys/net/ipv4/ip
                  >                                      forward
                于允许 IP  数据
       对                              报转发的系统         或路由器)，         数 据 报 转发子   模 块将对 期 望 转发    的数
                     如下操作;
    据 报执行
                                                果 TTL               OO,
       1   )  检查 数据 报头 部      的 TTL    值   。    如
   2 查看 数据 报头 部 的 严格 路 由 选择 选项 。如 果 该选项 被 设置 ， 则 检测 数据 报 的 目标 IP
    地 址 是 否是本 机 的 某 个     IP      址                  ，                       路               端
                               地 。     如      果 不  是 则 发 送     一 个  ICMP  源 站 选    失败 报 文 给 发 送 。
                                                                    报文    ，以 告诉 它 一 个 更  合理 的 下 一 跳
       3      如 果 有 必要    ， 则 给 源 端 发      送  一个   ICMP        重 定向
    路 由 器   。
                          减 1。
       4 将 TTL 值
           处      IP    头 部   。
       5  理                 选项
       6      如 果 有 必要    ，则 执行  IP    分 片    操 作 。

    26     BED

       图 2-3      了    由 ，                                                               讨论
        显示 ICMP 重 定向 报 文 也 能 用 于 更 新 路 表 因 此 本 节 我们 简要 ICMP 重
定 问

                            定向  报 文
    2.6.1     ICMP 重

       ICMP     重 定向 报 文格式     如  图    2-4 所 示 。

       0                                             15   16                                 31









                                                        重 定向
                                  2-4         ICMP                  报文格式
         们在            节讨 论过             报 文 头 部的       个 固定           :     位 类            位 代码
         我      1.1            ICMP                3        字段          8           型 、8          和  16 位 校 验
                      定向 报 文 的 类 型 值 是 $，     代 码 字 段有  4   个 可 选       值    ，用   来 区分      不 同 的 重  定向 类 型 。
    Al.  ICMP 重
                                         值为 1。
    本 书 仅 讨论 主机 重 定向         ，其代码
         ICMP   重 定向 报 文 的     数据 部 分      含义 很    明确 ， 它 给 接收 方 提      供      了 如 下 两        个信息 :
              重        定向的IP   BE CHIPS       2-4  中 的 原始   IP 数据
                                                                        报)     的 源 端
                                                                                               耳地
         口 引起                                                                                  址。
         口 应 该 使       用的 路 由 器 的 IP     址。
                                          地
                                                                                                        路由器来转
         接收 主机 根据 这 两          个             信息 就 可以 断定 引起 重 定向的    了 P                         数据 报应 该 使用 哪个
    发   ，并    来 更       新 路 由 表 (通常
           且以 此    是更新路由                                表 缓   ，而 不      是直接 更 改 路 由 表           )。
                                                                                      送    ICMP 重 定 向 报 文   ，
         /proc/sys/net/ipv4/conf/all/send_redirects 内 核 参 数 指定 是 否 允 许 发

## Page 45

    议        详解 ee 27
    第 2 章 |IP 协


而 /proc/sys/net/ipv4/conf/all/accept_redirects 内 核 参 数 则 指定 是 许 ICMP 重 定向 报
    文。     般 来    ，主 机 只 能 接收 ICMP      重        报文，           送        报文。
        一     说                         定向 而 路 由 器 只 能 发       ICMP     重 定向

                             重定向实例
    26.2                       ”主机
            中，我     把     器             的        网    关 设  置 成了机 器        节 中
        2.4.3   节     们    机     emest-laptop                  Kongming20，2.5          我们又
        报 转发 功能 ，因 此 机 器 ernest-laptop 将通过 Kongming20 来 访问
使 能了 Kongming20 的 数据
      比 ernest-laptop 上 执行 ping 命令
    因特网     ，   如在                      如下

        $  ping www.baidu.com
        PING    www.a.shifen.com   119.75.217.56     56(84)  bytes  of data.
        From    Kongming20 (192.168.1.109): icmp seq-l    Redirect      nexthop: 192.168.1.1
        64 bytes   from    119.75.217.56: icmp   seq=1 ttl=54 time=6.78   ms

        一 一一        ping              statistics    一 一一
        1 packets     transmitted,  1 received,  0%   packet loss,  time Oms
        rtt     min/avg/max/mdev =  6.789/6.789/6.789/0.000   ms
            的     输出 来
        从 ping 命令     看               ，Kongming20 给 ernest-laptop 发    送了 一 个 ICMP 重 定向 报 文 ，
          它请通过                                   ，             是                       路
告诉 192.168.1.1 来 访问 目标 机 器 因 为 这 对 ernest-laptop 来 说 更 合理 的 由 方
        报文后，它 将 更 新 其路由 表 缓冲〈 使
    式 。 当 主机 ernest-laptop 收 到 这 样      的 ICMP   重 定向                                   用命
                        )，并     使用  新 的路 由 方式 来      发  送后 续 数 据     报 。 上 面 讨论 的 重 定 向过 程 可
    令 route     -Cn 查看
      图 2-5     来 总 结 。
    用




                 |                      TICMP        定向        |
                 |        GD                         重     报    文      +     |

                     4 后 续          [P数 据 报      1         1        2 数据 报

                                                 |         |

                                        图                          主机
                                        2-$                     重定向过程
                                                                              {

    2.7   |IPv6  头 部 结构

        做
   IPv6 协议是 网 络 层技术 发展的 必然 趋势。它 不 仅 解决了 IPv4 地 址 不 够用的 问题 ，还
                                    比 增 了 多 播 流  的    功能  为     上        的
    了很 大 的 改进        。     如， 加         和            ，      网   络    多 媒体 内 容 质量 提供   精细 的 控
    制 ， 引 和人 自动 配置     功能    ，使 得 局   域 网    管理      更 方便  增加     了 专门 的网    络 安全功能
                                                     ，                             等。本 节 简 要
    地讨论   IPv6 头 部 结构  ，    它 的 更 多 细节 请 参考 其 标准                   文档RFC 2460。

## Page 46

    28 。%*   第 一篇 TCP/IP 协议 详解

                         头部 结构
    2.7.1    IPvV6 固定

   IPv6 头 部 由 40 字节 的 固定 头 部和可 变 长的扩展 头 部 组 。
        成 图 2-6 所 示 是 IPv6 的 固定 头
部 结构 ，

       0         15     16    31

             本号
            版










                 图                                        国定
                 2-6                               IPv6       头部       结构
                      版本号      指定     协议
   4 位 〈version) IP 的版本。对 IPv6 说， 是
        来 其 值 6.
      8    位 通信 类 型     class)     指示 数据 流通 信 类 型 或 优先        ，    和     IPv4     的
                                                              级      中     TOS KW.
                                                          的       字段 ， 用 于 某 些 对 连接 的 服务       质量 有 特殊 要
      20        位 流标签    (flow label)    是 IPv6                 新 增加
      的通信，       比 如 音频 或    视频     等    实时 数据     传输  。
    求
      16        位      傈 长度  〈payload length)   指 的    是IPv6       扩展 头   部和 应 用 程序数据 长 度       之和     包
                                                              ，                                        不
                    头部长度。
    括固定
                                                   紧跟     IPv6 固定 头 部 后    的 包头 类     ，    如 扩展
      8    位下     个     包头   〈next header)  指出                头《〈                                      如
                 一                                            型                                        果
                 如    TCP,                         UDP
    有 的 话  )     或 某 个上层 协议头    〈 比                         或     ICMP)。  它 类 似 于 IPv4 头 部 中 的 协议 字段   ，
                 的    含义           。
    且 相 同           的取 值有         相同
            位 跳 数 限制                            中
      8                   Chop limit)    和 IPv4    的   TTL    含义    相同   。
                 用                 节)                   址，     得        个。                           以有人
      IPv6       128     位      〈16 字    来 表示   了                          P 地 使 IP 地 址 的 总 量 达 到 了 2° 所
    说      ,“IPv6  使 得 地   球 上 的 每  粒    沙 子 都 有   个 IP 地
                                                一           址  ”。
      32        位 表示 的 IPv4     地 址 一    般 用 点  分  十   进 制  来  表示       ，而 IPv6 地 址 则 用 十  六 进制     字符 串
    表示，比 如 “FE80:0000:0000:0000:1234:5678:0000:0012”。         可 见     地
                                                              ，IPv6     址                   用“:”       分
                  成8组，每  组     2字节。               但 这 种 表示 方法 过    于 麻烦   ，通 常  可   以 使    用所 谓 的 零 压缩 法
                 包含
           将其         ，      是      的、                      。    如，                   用         法
    来
                    简写   也 就    省 略 连续 全 零的               组 比 上 面 的 例子 使     零 压 缩                可 表示 为
    “FE80::1234:5678:0000:0012”。     不 过 零 压 缩          法 对 一  个   IPv6   地 址 只 能 使用   一    次 ，比如上 面 的 倒
      中，           字 节组  “5678” 后 面 的 全 零 组 就      不 能 再 省     略 ，和   理 则 我 们 就 无 法计算 每 个“:…:”     之 间 省
    子
    略 了 多 少个全 零 组     。

## Page 47

                                                                       第 2 章            IP 协 议 详     解 *%*   29

                                     头部
    2.7.2                       |Pv6 扩展

       可 变 长    的扩展               头 部 使得IPv6 能 支持       更    多 的 选项  ，并 且       很便于 将 来 的  扩展 需要     。    它 的 长
                                                                               报可以       包含 多 个 扩展 头    ，     每
度 可以是0， 表 示 数 据 报 没 使用任何 扩展 头 。 个 数据
                                                            部 一                                      部       个扩
    展 头 部 的 类 型          由前    一 个 头 部  〈 固 定 头 部 或 扩展 头        部  ) 中 的 下          个 报头 字段 指定   目 。    前 可 使
                                                                           一                                  以
                       头部如表    2-3 所 示 。
    用 的 扩展

                                             表 2-3              扩展 头 部

                     扩展 头 部                                            x&
    Hop-by-Hop                            逐 跳 选项
                                                     头部，    它 包含 每 个 路  由 器 都 必须 检查 和 处 理 的 特殊   数 选项
                                                            定由
    Destination options                   目的 选项 头           ，指     最            目 的节点
                                                        部                       处理的选项
    Routin                                路 由 头  部，     指 定数 据 报 要 经 过  哪些  中 转 路 由 器，功 能 类 似 于 IPv4
                                       路 由 选择    选项     和 记录 路 由 选项
    Fragment                              分片头 部 ，处 理 分片和             的
                                                                          重组
                                                                          细节
    Authentication                        认证 头       ，提 供          认证、      性               保护
                                                 部          数据 数 据 完 整 检查 和 反 重
                                          加
                                            头        提 供 加 密 服 务
    Encapsulating Security Payload  密         部，
    No next   header                      没有后 续 扩 展 头 部

               协议并不是    IPv4 协议 的                简单 扩展      ，   而 是 完全 独立 的 协议     。   用 以 太    网     帆 封   装 的
    注意     IPv6
               报和     数据
               数据                                               型 1     提 到     ，IPv4                   报 的 太 网
           IPv6      [IPv4                报具有不          同    的类      值。第    章              数据           以
                                                                   以    网     装    类 型 值是               (BW RFC
           帧 封闭类 型 值是    0x800，                 而 IPv6      据 报    的 太
           2464).

## Page 48

    第 3 章 TCP 协议 详解



              协议
         TCP     是     协议 族 中 另                  一    个     重要 的 协议             。和        协    议 比               更         应用
                                                                                           相                          ，TCP 协议
                                                                                                                    靠近
    层 ，因 此 在 应 用 程序 中具有 更强       的 可 操作                   。           一    些 重 要 的   socket      都和
                                                          性                                选项     TCP 协议 相关          。
             章从如下     来讨论                             ;
         本 四 方面                  TCP 协议
                      部信息 。TCP     头 部 信息 出 现在 每                           个      TCP 报 文 段     中，用 于 指定 通信 的 源 端          端口
         TCP 头
   号，                            理    TCP 连接                      控 制  ，    两   个 方向 的 数据 流     。
             导、目 的 端 端口          管
         口 TCP   状态 转移 过         。TCP     连接 的 任意                      一 端 都    是 一 个  状态     。   在 TCP          从         到断
                                                                                                                           连接
                              程                                                            机                        建立
                           过程中，      连 接 两 端的    状态       机 将 经             历   不 同 的    状态     变迁。     解
             开 的 整个                                                                        理              TCP         状态 转移 对
             于 调试     网络应用 程序    将    有很 大 的 帮助           。
                       流。         通 过 分 析 TCP 数据
                 数据                                       流，          我 们 就      可以 从 网 络 应 用     程序    外 部 来    了  解  应  用 层
             协议和 通信 双方               交换 的应 用     程序               。   数据 这 一     部 分 将讨论 两    种   类    型 的TCP        流 ，
                                                                                                                         数据 交
             互 数 据流和 成 块  数据     。TCP                             中 数据 流 有 一    种 特殊 的 数据    ，    称 为 紧急 数据      ，        们也将
                                    流                                                                                       我
             简单 讨论 之 。
                 流的          控制    。为    了 保证 可 靠 传输                  和 提高       网  络 通信 质量     ，     内 核 需 要 对        TCR 数据
         OO  TCP 数据
             流 进行 控制  。这  一  部 分    讨论       TCP          数据 流 控制                的两 个    方面    ，   超 时 重 传和    拥塞 控制   。
                 讨论               协议 之 ，         我 们 先                简单   介绍 一    下                    服务
         不 过 在 详细     TCP                    前                                          TCP    的        特点     ，以 及 它  和  UDP
             的区别。
    服务

                                   的特点
    3.1      TCP 服务

         传输层协 议    主 要      有 两个     TCP                  和                                                         的     是
                                             协议           UDP         协议         。TCP  协议 相对 于     UDP 协议
                                                                                                                    特点     ，
    面向连接、字 节 流 和 可 靠传输               。
             用TCP    协议 通信 的 双方 必须 先 建立连接 ，然后才能 开始 数据                                                 的 读     。双 方
         使                                                                                                写         都    必须 为
    该连接 分 配 必要 的 内 核资            ，以     管理                  连接 的  状态       和 连接   上 数据 的 传输               连接
                                 源                                                         。TCP                  是全双工的，
    即 双 方的数据 读 写 可以 通过           个 连接 进行                       。 完 成 数据 交换
                                 一                                               之后，              通 信 双 方 都    必须 断 开 连接 以
    释放 系统 资源     。
                 的这种连接是          一 对     一            所以 基于 广播 ，
         TCP 协议                                  的                               和多播   〈      目  标 是 多 个  主机 地 址    )  的 应 用
         不能使用    TCP 服务   。          而 无 连接 协议               UDP  则 非常           适合 于        广播 和 多 播 。
    程序
             们在  1.1 节 中 简单         介绍 过 字 节 流                服务 和 数据 报服务            的 区 别    。          这 种 区 别 对 应 到 实际  编程
         我
    中，则 体现 为通信 双方是否 必 须 执行相同 次 数 的                                              、 写 操作   当
                                                                             读           〈     然，             这 只 是 表现 形式 )。当
发送 用 程序 连续 执行 多 次 写 操作时 ，TCP 模块先将 这 些 数 据 放 人 TCP 发 送 区中。
        缓冲
    TCP                                                              模块 真正开始 发送数据 时，发 送 缓冲区 中 这 些 等 待 发送的 数据可 能 被 封装成 一个或 多 个

## Page 49

                                              第                 3     章    TCP 协 议 详解 fe       31

           报 文 段 发 。因 此          模块
    TCP        出      ，TCP           发   送出  的              数TCP 报 文 段 的 个 和 应 用 程序执行的    写操作   次
    数 之 间 没有固定 的 数量 关系     。
        当 接 收     收 到 一 个或多 个      TCP 报 文 段   后   ，TCP 模块将它们 携带 的             应 用 程序     数据   按照
                                       放人 TCP 接收 缓冲    区中 ，并 通知 应 用 程序 读 取 数                 据。 接
    TCP    报 文 段的 序号〈《见 后 文) 依次
    收      应 用    程序可以 一 次 性将    TCP 接收   缓冲区 中 的 数据 全 部 读  出     ，    也 可以 分 多 次 读 取     ，  这 取决
    于 用 户 指定 的 应 用 程序 读 缓    冲   区 的 大  小 。  此，             的
                                                               因 应 用 程序 执行 读 操作 次 数 和   TCP 模块 接收
    到 的 TCP 报 文 段 个 数     之间 也   没有国定 的   数量  关系   。
              上   述，          的      操作   数                                    之
        综
    所          发 送 端 执行            写     次 和 接收 端 执行    的 读 操作 次 数                   间 没 有任何 数量 关
    系 ，这 就是字 节 流 的 概念     :  应 用     程序对 数据 的 发送和 接收 是 没有 边界 限制                的     。UDP 则 不 然 。
    发 送 端     应 用 程序 每 执行一次 写 操作     ，UDP   模块 就 将 其 封装         一    个UDP      报并发送之。
                                              成                                              数据 接
        报执行读操作 〈 通
收 必须 及 时 针对 每 一个UDP 数据 过 recvfrom
                                     并 如 果 用 户 没     指定                    应 用   程序       区 来 读 取
    《这 经 常     发生 在 较 慢 的   服务器上 )。      且，      有                     的       缓冲
                                将被截断，
    UDP 数据     ，             则 UDP 数据
          图 3-1   AIPA 3-2  了                 和                        上       。     图        略
        显示 TCP 字 节 流 服务 UDP 数据 报 服务 的 述 区 别 两 中 省 了
           层以下的通信         细节。
    传输

               发           送端

               |                              应 用 层


                                              传输层




                                 图 3-1    TCP 字 节 流 服务

           发送                                                              KOR

                                          用层
                                           应


                                              fee



                                               报服务
                                 3-2    UDP     数据

             是可靠的。
             传输 首
      TCP        先               ，TCP 协议 采用 发   送 机 制       ， 即            发送 发送的 每个TCP 报
    文 段都   必须 得 到 接收 方 的 应       ，才       这个                成功。               次
                                 符  认为 TCP 报 文 段 传输 其 ，TCP 协议 采用 超

## Page 50

32 .se  TCP/IP 协议 详解


重传机制，  发 送 端 在 发  送出一    个TCP 报 文 段之      后 启动 定时    器，如 果 在定时  时间 内 未 收 到 应
时
                     后， 因
答 ，它 将 重 发该报 文 。           为  报 文 段 最      终是      数据
    段     最                  TCP               以 IP    报发送的，   而 IP    数据 报到
    Seg AY      SL, PTL  TCP 协议 还 会       对接收 到 的 TCP 报 文段重    、  理，
ATEN                                               排                  整 再 交付
     应用屋。
给
          则和 IP 协议 一  样  ，提 供 不    可 靠 服务   。它们 都 需要 上 层 协议 来  处理 数 据确   认 和
    UDP   协议
超时 重 传  。

3.2    TCP  头 部 结构

    TCP  头 部 信息 出 现在 每个TCP 报 文 段    中， 用 于 指定 通信 的 源 端     端口 ，目 的 端 端口  ， 管
    等，本 节 详      细 介 绍 TCP 的 头 部 结构    ，  包 括 固定 头 部  结构和 头 部 选项。
理 TCP 连接

    头部    结构
3.2.1            TCP 国定

    如图    3-3     所
    TCP  头 部结构          示，其 中 的 诸多 字段 为     管理 TCP     连接 和 控制 数据 流 提 供 了 足够
的 信息   。

    0                        15     16             31






4位    U                  IAA  R


                             选项       ，最 多40字节

                             图 3-3    TCP 头 部 结构

    16  位 端口 〈port number):  告知主机 该 报 文 段 是     来    自   哪里 源 )        及       传 给 哪个
      号                                                     〈  端口 以                 上
          或应用程序      目 的  ) 的 。进
层 协议                         行           通信
      〈                端口      TCP         时，   客 户    端 通常 使  用系统 自动    选择  的     临时
    号，  而 服务  器 则 使 用    知名 服务 端口   号 。1.3 节 中 提 到 过 ，   所 有 知名 服务 使   用 的   端口   号 都
端口
jE MAZE /etc/services 文件
                       中  。
                                    次 TCP   通信 (从 TCP     连接   建立到 断   开 )   过 程中   革
   32   位 序号〈sequence number): 一
一            个传输 方向上 的 字 节 流 的 每个字 节 的 编号 。假 设 主机A     和 主机 B 进行 TCP 通信     ，A      发
  给 B   的 第 一个TCP  报 文 段   中，序 号 值 被 系统    初始 化 为    某 个  随机   值ISN (Initial Sequence
送
      始序号
Number， 初     值)。      那 么 在 该 传输   方向   上 (从 A  到 B)，    后 续  的TCP 报 文 段    中      值
                                                                                   序号
将 被系统 设置     成 ISN 加 上 该 报  文 段所 携带   数据 的 第    一个字 节 在       整个字 节 流 中 的 偏    移。   例

## Page 51

                                                                                第 3 章      TCP 协议 详解        <>    33

              个TCP 报 文 段     传送  的 数据 是 字 节 流 中 的             第  1025                 字
     如，某                                                                  ~    2048       节  ， 那  么   报  该  文 段 的  序
     号 值 就是    ISN+1025。     外       个 传输
                                 一       方向                B  到  A)     的 TCP      报 文 段 的            也序号值     具有 相同
     的含义     。
          32   位 确认 号          〈acknowledgement number):      用 作对 另      一     方发      送 来 的TCP 报 文 段      的   响应 。
     其 值是收 到 的     TCP  报 文 段            值加                主机                                                      送
                                 的序号        1。       假 设         A    和 主机      B 进行 TCP WN,     BRA         A     发
       的  TCP 报 文 段 不   仅携带 自己         的 序号  ，  而 且 包 含对 B            发 送 来      TCP   报 文 段    的           号。     反
     出                                                                          的              确认                 之，
               的 TCP    报 文 段也同时
                                       携带 自己             的 序号
     B 发 送出                                                和对   A 发 送 来 的 报           文 段  的 确认 号   。
                                                   该  TCP     头  部      有 多 少   个   32bit 字 4     节)。         为
          4  位 头 部 长度   (header length):     标识                                                字 因              4  位
           大能表示      PLL     头 部 最 长         是     字
     最           1S,      TCP                   60 节。
          6  位 标志 位 包含    如 下 几 项    ;
                            示紧 急     指 针           pointer)      是    否 有 效
          口  URG 标志，表                                                     。
                                                                                    的 TCP 报 文    段  为确认     报 文   段。
          QUACK 标志，表 示 确 认 号 是否有 效 。我                  们    称 携带   ACK 标志
  ，       CY PSH 标志，提 示 接 收 端应用 程序应                      该 立即 从  TCP 接收             缓冲 区 中 读 走 数据     ，       为 接收 后
             续 数 据 腾 出 空间〈 如 果 应 用 程序不 将 接收 到 的 数据 读                                走 ，它们 就     会 一 直 停 留     在  TCP
             接收 缓冲区 中     )。
          DRST 标志，表 示 要 求对 方 重新 建立连接。我                           们    称 携     带 RST         的                 为
                                                                                      标志       TCP 报 文 段          复位
             报 文 段 。
                                                                                       的                   报 文 段 为同步
          CISYN 标志，表 示 请 求建立一个 连接 。                        我们 称 携       带  SYN          标志 TCP                     报
             MEE.
          LY FIN 标志，表 示 通 知 对 方本端 要 关闭 连接了                            我   们称 携带       FIN      的                段
                                                                 。                         标志     TCP     报 文      为
                   报文段。
             结束
                     大小 Cwindow size):       是 TCP 流量 控制              的 一  个 手段       。这 里 说 的     窗口     ， 指 的  是 接
          16 位 窗口
                                                              对方本端的             TCP    接收      缓冲       区 还 能 容纳 多 少
     收 通告 窗口                Window，RWND)。                                  它 告诉
     字 节 的 数据 ，这 样对 方 就可以 控制 发送数据 的 速度                        。
          16   位 校验和 (TCP checksum):         由 发   送   端 填充      ，        接 收 端 对    TCP 报 文 段 执行 CRC              以
                                                                                                                  算法
     检验   TCP  报 文 段 在 传输过 程 中 是 否 损坏 。                意，                       仅              部，
注 这 个 校                                                                   验 不       包括 TCP 头                 也 包括 数据
       分。                   传输
     部 这 也 是       TCP 可 靠       的     一 个 重要 保障      。
          16  位 紧急 指针   Cargent pointer):       是  一  个 正 的 偏 移           量。   它 和 序号 字段 的 值    相    加           最 后
                                            此，     确
     一 个 紧急 数据的下        一 字 节 的 序号       。因           切地说，        这 个 字段            是             紧急 指针 相对 当前 序号 的 偏
     移 ，不 妨 称 之 为       紧急 偏     。TCP    的 紧急 指针 是         发 送   端    向 接收 端 发      送  紧急 数据 的 方法           。     们将
                            移                                                                                      我
       后  面  讨论  TCP    紧急 数据   。
     在

     3.2.2     TCP 头 部 选项

          TCP  头 部   的  最后一 个选项 字段         (options)             是 可 变 长 的 可    选 信 息  。   这 部 分 最 多 包含            字
                                                                                                                  40
          (AA  TCP   头 部 最 长是    60      节【〈                              论                的     部分)。             型的
                                                         字 其 中 还 包含 前 面讨     的  20                          字 节 固定 典
               选项 3-4 所 示              。
     TCP  头 部      结构如图

## Page 52

    34 «f+ S— TCP/IP 协议 详解





                                               的 一 般 结 构
                           图 3-4   TCP     头 部选项

                           明选 项    的 类 。      有
      选项 的 第 一个字段    kind 说      型      的    TCP   选项   没有  后 面 两个字段 ，仅 包含 1
    字 节 的 kind 字段 。第 二 个 字段 length 〔 如 果 有 的 话) 指定 该 选项     总 长 ， 长度包括
                           的                                    度 该     kind
    TRAM length 字段 占据的 2  字 节   。第 三 个 字段 info (如 果 有 的 话)  是 选项  的具体 信息。常 见
      有 种，          如
    的 TCP 选项 7      图  3-5 所 示 。









            cee      =N*842      左边            语       右边 沿         ™    左边           布边沿
            est      [ee     [sm                                                      or
                                           3-8     7 种 TCP 选项

      kind=0     是选项 表 结       选  项。
      kind=1     是 空操作     (nop)  选项 ， 没   有   特殊 含义     ，  一     般 用 于 将 TCP      选项 的 总 长 度 填充 为    4
                 倍。
    字 节 的 整数
      kind=2     是 最大 报   文 段    长 度 选 项 。TCP     连接 初始 化 时         ，通 信 双 方 使     用该  选项  来   协商 最 大 报
        节【〈 减
MERI (Max Segment Size, MSS). TCP 模块通常将MSS 设置为(MTU-40) 字
掉 的 这 字 节 包括 字 节 的 头
          40         20     TCP            部和    20  字 节 的          头 这 样           携带     报 文 段的
                                                                IP     部 )。           TCP            IP
                                                      部和 IP     头   部 都不 包含     选项  字段  ，  并 且 这 也 是  一
    数据 报 的 长度就 不 会 超过    MTU                       TCP 头
                             本机发生   分
    般 情 况 )，从 而避免          IP     片     。 对以     太 网 而     言    ，MSS  值是    1460   1500-40   字 节 。
      kind=3  是 窗口扩大 因子 选项     。TCP                 连接 初始 化   时 ，  通 信 双 方 使            用该选项 来 协商 接收 通告
                           头    部    中，     接 收      告 窗 口      大 小 是用  位 表示
    窗口 的 扩大 因子 .在 TCP 的                                             16              的，  故 最 大 为 65  535
      节，                                                            这     数        了
    字 但 实际         上 TCP          模块 允许 的 接收 通告     窗口 大小 远 不 止                     个 (为
                                                                                      提高 TCP     通信 的 香
                     因子     解决   了这 个 问题   。     设   TCP 头 部 中 的             接收 通告 窗口 大小   是 W，     窗 口
    叶 量 )。    口  扩大                        假                                                          扩
      因子             )        么                                     是              X
    大     〈 移 位 数     是 M，      那 TCP 报 文 段    的                    实际 接收 通告 窗口 大小    乘     或 者 说     左
    移 1M 位  。    意   ，M   的 取 值 范 围 是0   一 14。      我 们 可 以          通过 修改 /proc/sys/netipv4/tcp_window
    scaling 内 核  变量  来 启用 或 关闭 窗口 扩大 因子 选项             。

## Page 53

                                                                                              第 3 章   TCP 协议       详解 *%r+                         35

        一梓，窗 口 扩 大 因子 选项只 能出 现在 同步 报 文 段中， 否则
        将被忽略。但
        Al MSS 选项                                                                                                                                   同
    步报文 段本身 不 执行 窗口扩大 操作，即 同步报 文 段头部                                                的 接收 通告 窗口            大小 就 是                           该 TCP  报 文
    段   的实际 接收       通告 窗口 大                小   。 当 连接   建立    好   之      后  ，每     个  数据   传输 方向     的 窗口         扩大                      因子 就    固定
  变了 。 关 于 窗口 扩大 因子 选项 的 细节，可 参考 标准 文档RFC 1323.
不
        选择
        确认 (Selective Acknowledgment. SACK) 选项 。TCP 通信 如 果 革
           kind=4 是 性                                                                                                                      时，
    个 TCP 报 文 段                      失 ，则 TCP    模块 会 重 传 最后被确认 的 TCP                       报 文 段    后         所                续 的  有 报 文   ，    这 样
               已经正确传输的                                                        ，               了                                   能
    原先                            TCP      报 文 段 也 可 能 重复 发                     从 而 降低                                             TCP 性 。SACK 技术 正 是
    为 改善这 种 情况 而 产生                    的     ，它使     TCP  模块     只 重新 发             送   失   的 TCP 报   文    段   ，    不 用 发 送所 有
    被确认 的 TCP          报 文 。选 择 性 确认 选项 用 在 连接 初始 化                                         ，                  持SACK GEAR.表 示 是 否 支                 我 时
                                                         sack    内 核
    们 可 以 通过 修改            /proc/sys/net/ipv4/tcp                       变    量 来 启用 或 关闭         选择   性 确认 选项                              。
           kind=5    是 SACK       实际工作 的 选项 。该 选项                       的    参数 告诉        发 送                             方 本 端 已 经  收 到 并   缓存 的 不 连
        的数据块，从                       而 让 发 送 端  可 以 据 此 检查 并 重 发             丢失   的 数据        块 。 每 个 块 边沿                            Cedge of block)
    续
    参数 包含 一个           4   字 节 的           序号   。 其 中   块   左边 沿 表示 不 连续 块                的 第 一   个   数据 的 序号      ，    而 块                    右边   沿
    则 表示 不 连续 块 的最后                    一    个    数据 的 序号 的 下       一    个    序号     。   这 样   一 对   参数    CRA                               AR
    沿 )  之 间 的 数据 是                      没有收 到  的 。  因 为 一   个 块 信息 占用            8    字 节  ， 所 以   TCP   头 部      选项                      中 实际   上 最
    多 可 以 包含       4 个 这 样的不 连续 数据块                      〈                       考 虑 选项类型和 长度占用 的   2 字 节                        )。
           kind=8  是 时间 惟 选项 。该选项 提供 了 较为 准确 的计算 通信 双方 之 间 的 回路 时间                                                                             (Round
    Trip Time,        RTT)    的 方法 ，从 而              为  TCP   流量 控制提供 重要 信息。我 们 可以 通过 修改                                                   /proc/sys/
    net/ipv4/tcp_timestamps         内 核 变                         来启用 或关闭 时 间和           项。 -
    3.2.3      {FFA                                                   观察 TCP 头
                                  tcpdump                                  部信息
                                      中，我
                                      们利用            tepdump 抓
        在    2.3         节                                  取  了     个 数据 包 并 分             析 了 其 中 的   IP    头 部信息                          ，    本 节
                                                                   一                                                                                分
    析 其  中   与 TCP     协议 相关 的 部                 分 〈     后 面 的  分析 中    ，    我  们 将    所  有 tcpdump   抓 取 到 的      数据                         包 都 称 为
                              为 TCP    报 文 段             既是 数据 包 的     主要 内 容         ，也 是 我 们                                          主要讨论 的对 象 )。为
    TCP  报 文 段    ，  因                                                                                                                             了方
    便 阅    读         ，先 将该    TCP                        报 文 段的内 容 复制 于 代码 清单 3-1     中  。
                                              代码     清单  3-1   用 tcpdump              抓取 数 据 包
          IP 127.0.0.1.41621                 >  127.0.0.1.23:           Flags       [S]，seq       3499745539,      win                         32792,
    options    [mss    16396,sackOK,TS             val      40781017    ecr   0,nop,wscale       6],                          length 0
               0x0000:          4510   003c       aSda  4000   4006     96cf    7£00     0001
               0x0010:          7£00   0001       a295  0017   d099     e103    0000     0000
               0x0020:          a002   8018       £e30  0000   0204     400c    0402     080a
               0x0030:          026e   44d9       0000  0000   0103     0306

                                                        该TCP     报 文段
        tepdump      输出 Flags[S]j，                表 示                     包含        SYN                                    标志，因 此它 是 一 个 同步 报 文 段   。
    果    TCP 报 文       段      包     含其 他 标志        ， 则 tcpdump 也 会 将 该标志 的 首 字 母                      显示           在     “Flags”                后 的 方
    如
          中。
    括号
                              值。           因 为 该  同步 报 文 段     是 从     127.0.0.1.41621      (客户       端 中 地址
         seq   是 序号                                                                                                和                         端口 号)  到

## Page 54

  36       第 一    篇    TCPJIIP 协议 详解

           a     IP            地址和 端口
                               号)                     这 个 传输
  127.0.0.1.23     CARS                               方向    上 的 第 一    个TCP 报 文  段 ，   所以 这 个 序
  号  值 也 就    是       此 次 通信 过 程中 该 传输 方向 的 ISN 值     。 并 且  ，因 为 这 是       整个通信 过 程   中的 第 一 个
  TCP 报 文     ，所    以  它      没有针对 对 方 发 送 来的TCP 报 文 段    的     确认
           段                                             值                  尚未 收 到任何 对 方 发    送
  KAY  TCP     报 文    段)。
      win     是 接收 通告 窗口的大     小    。 因 为 这 是 一 个    同步    报 文    所 以   win  值 反映 的    是 实际 的 接
  收 通 告         窗口  大小 。
      options     是 TCP 选项 ，其 具体 内 容 列 在 方             中                端        端)         的最大
                                                      括号     。mss 是 发 送                  (客户 通告
     文段长度。    通
  报        过            ifconfig  命令 查看 回路      接口的      为      字
                                                     MTU     16436     节，   因 此 可 以    预想 到 TCR
  报 文 段的    MSS 为 16396 16436-40      节                  支持                       用        。
                               字     。sackOK                 表示 发 送 端     并 同意 使     SACK 选项
  TS val 是 发 送 端的 时 间          是     时 间    回显 应      。       因 这 是 一次      通信
                                                  答      为      TCP         的第一个    TCP       报
     段，所         以  它 针 对 对 方  的时 间 锥 的应答   为 0〈 尚未   收 到 对 方     的 时间 惟 )。    紧 接着    的     一个
  文                                                                         nop               是
  室 操作     选项     。wscale  指出 发 送 端 使 用       的 窗口 扩大 因子    为6。
 接 下 来 我 们 分 析 tcpdump 输出 的 字 节 码 中 TCP 头 部 对 应 的信息，它 从 第 21 字 节 开始，
表
  如 3-1     所 示    。

                               表 3-1                   TCP 头部
        十     六  进 制数                                    TCP              部
                                                                              头
       Da295                                                                 信息
       0x0017                                         目的    端口号
           |                                         re
                           «|S
       0xa                 10|                       Tcp 头 部 长 度 为 10 个 32 位 40  节)
                                                                                  字
       0x002                                             SYN
       Ox8018                                        接收 通告         大小
                                                                   窗口
       Oxfe30              se
       0x0000              RE                            URG 标志，     所 以    紧急指针 值无    意义
       0x0204              |                           大        度      的      和
                                                     最 报 文     长    选 项 kind 值 length fH
       0x400c                  大报文段
                                                     最 长 度
       0x0402              SRE SACK
       0x080a              itt                           286 kina 4207 length {EK
                               ot                     a
       0x00000000          0                         回 显 应 答 时 间
       0x01                                          空  操作   选项
       0x0303                                        窗口    扩大 因子 选项  的 kind 值和 length 值
       0x06                6 窗口                          扩大 因子    为 6

     后       ”此 列   中 的空格 表示 我 们并 不 关心相应 字段 的 十 进 制值 。

## Page 55

                                                                       WIR                TCP   协议  详解            37

      表 3-1 中 可 ，TCP 报 文 段头部的二进 制码和 tcpdump 输出的TCP 报 文 段描述
        从                 见                                                                                       信息
        中，我 们 将 省 略 大 部分 TCP 头 部信息，仅 显示 序号、 认
    完全 对 应。在 后 面的 tcpdump 输出                                                                                       确
    号 、窗    口   大小以 及 标志位 等 与 主题相关 的 字段                。

                              的建立和关闭
    3.3     TCP 连接

        本节 我 们讨论 建立和 关闭                TCP            的过程。
                                                        连接

                   用                                    的
    3.3.1     使                                                      建立和关闭
                                                         tcpdump 观察 TCP 连接

        从 emmest-laptop 上 执行 telnet 命令 登录Kongming20 的 80 端口 ， 然 后 抓取这 一 过 程
    首先
    中 客户端和 服务 器 交换                  的 TCP  报 文    。  具          作过程如下:
                                                                    体操

        $   sudo tcpdump      -i     ethO  -nt  ‘(sre   192.168.1.109 and           dst 192.168.1.108      or   (sre
    192.168.1.108     and dst      192.168.1.109)'
        $   telnet  192.168.1.109      80
        Trying           192.168.1.109...
        Connected     to        192.168.1.109.
        Escape        character    is ‘'*]'.
                                        ecrl+]
        ^]           #       输入            并回车

        telnet>    quit
        Connection     closed.

                                                                               (telnet后
        当 执行     telnet   命令 并 在两台 通 信 主 机 之 间 建立           TCP       连接                        Hi  “Connected    to
                              入 Ctrl+]     以 调 出        程序
    192.168.1.109”)，          输                telnet      的命令     提示          然     符， 后 在 telnet         命令 提示 符 后
    输入 quit     以退出      telnet    客户 端 程序 ，从 而 结束      TCP ETE.                          CM  ETE BET
                     内   容 如 代码       清单    3-2 所 示  。
    tepdump 输出 的

                                                        和                     关闭 TCP 连接
                                      代码 清单 3-2      ”建立                            的过程
    1. IP 192.168.1.108.60871 > 192.168.1.109.80: Flags [S], seq 535734930, win
       5840, length 0
        2.   IP  192.168.1.109.80      >    192.168.1.108.60871:    Flags [S.],          seq  2159701207,      ack
             535734931, win        5792,   length 0
        3.   IP  192.168.1.108.60871         >  192.168.1.109.80:  Flags  [.],     ack    1,  win   92,    length  0
        4.   IP  192.168.1.108.60871         >   192.168.1.109.80: Flags  [F.],     sea      1, ack    1,  win 92,
             length     0
        S.   IP  192.168.1.109.80      >    192.168.1.108.60871:   Flags  [.],     ack    2,  win   91,    length  0
        6.   IP  192.168.1.109.80      >      192.168.1.108.60871: Flags  [F.],     seq      1, ack    2,  win 91,
             length     0
        7.   IP  192.168.1.108.60871         >  192.168.1.109.80:  Flags  [.],     ack    2,  win   92,    length  0

        因为 整个过程 并 没有发 生 应 用 层 数据                        的  交换     ，所以    TCP   报     文 段 的      数据  部      分 的 长   度
                                                                      的  整个    过程，                  我 们 将 tcpdump 输出
(length) 总 是 0。 为 了 更 清楚 地 表示 建立和 关闭 TCP 连接
的 内 容 绘 制 成图 3-6 所 示 的 时 序 图 。

## Page 56

38 第 一篇 TCP/IP 协议 详解


ermest-laptop       Kongming20
192.168.1.108.60871 >
报 文段1        S¢q 535734930 (Syn)

报 文段3        ack     535734931



报 文



HEEL?


                                                  TCP 连接 的          建立         和 关闭 时序 图
     ALS    TCPHR                SYN 标志             ，    因 此 它 是                 一 个    同步 报        段
                                                                                                    文 ， 即        ernest-laptop 〈 客
   端)                            器)                         请求。                    时， 同步                    段       个            为
户 向      Kongming20         (服务 发 连接            起                              同 该           报 文                        包含 一 ISN 值
                                                                                     段，  表
535734930 的 序号。第      2        个 TCP 报 文段           也       是 同步 报 文                   示                Kongming20      与
                                                                                                                    同意      ermest-
                                 自己          ISN      5         2159701207               的 序号       并 对 第        1            个 同步
laptop   建立 连接    。同 时 它 发        送      的                                                          ，                          报文段
                                 第              1     个     同步 报 文                   的  序号     值加   1。      前 文     说 过  ，序 号  值 是
进行 确认。确 认 值是            S35734931，     即
用 来     标识TCP  数据 流 中 的 每        一   字 节    的 。     但 同步 报 文 段                     比 较  特殊 ， 即 使        它   并      没有     携带任何 应 用
程序数据 ，      它 也 要 占用
                            一个   序号    值。   第   3        个  TCP 报 文 段是                                      对
                                                                                                                第             个 同步
                                                                                        ernest-laptop             2             报文段
            此           连接
的确认。至          ，TCP             就建立起来 。                     立                        的这                                         。
                                              了     建       TCP     连接                     3  个 步           被   称 为 TCP  三 次握手
         第3 个 TCP       报 文    段
     从                          开始 ,tcpdump 输出 的 序号 值                              和 确认 值 都 是 相对 初始                              值
                                                                                                                    ISN       的偏移，
当然 ，我    们  可以 开启 tcpdump 的      -S         选项 来                打印 序号 的 选择              绝对    值 。
     后 个       TCP  报 文 段        是关闭 连接 的 过                 。
                                                            程   第     4 个 TCP            报 文 段 包          含 FIN 标志    ，    因 此 它 是
   个 结束
一        报文段     ， 即    ernest-laptop
   个序     值   。Kongming20 用 TCP                   报 文 段  5      确认       来     该 结束    报 文     。    紧 接着        Kongming20     Rik
一
            报文          段 6，emest-laptop      用                     段                      确 。                ，
自己   的 结束                        则       TCP                             报 文 7 给 予 认                实 际       上        仅 用 于 确认 目的
的 确认 报 文段      5     是  可 以  省 略  的  ，因 为    结束                 段      报 文 6   也 携带    了   该  确认    信息      。   确 认 报 文   段 $  是 否
出 现在 连接 断 开 的 过 程              中，取 决  于TCP      的 延迟            确认             特性 。          迟确认    将 在 后        面讨论  。
     在 连接 的 关闭 过             程 中，因 为    emest-laptop            先 发            送        报      段                            端程序主动退
                                                                                                                   结束 文 〈telnet 客户
         称ernest-laptop      执行 主动 关闭         ，    而
出 )， 故                                                   称              Kongming20 执行 被      动            。
                                                                                                         关闭
     一   般 而 言    ，TCP  连接 是 由 客户 端     发       起  ，     并 通过       三          次 握手    建立    〈  特   殊    情  况   是所 谓  同  时 打开
的    。TCP 连接 的 关闭 过 程            相 对 复杂 一     些 。                     可 能 是 客户 端   执行        主动 关闭        ， 比   如 前 面的    例子    th
可 能 是 服务 器 执行 主动 关闭 ，比           如     服务 器 程序 被                             中 断 而 强制 关闭 连接                 ，   还 可 能 是   同时    关闭

## Page 57

                              第 3                        章   TCP 协议 详解 w%* 39

    《和 同时 打开 一 样 ， 非 常   少见 )，

    3.3.2  半 关 闭 状态

        连接
     TCP   是全双工的，           所以 它 允许 两 个 方向 的 数据 传输 被独立      关闭。 换言之 ，通     的一
    可以 发送结束 报 文 段       给 对 方 ， 告 诉 它 本 端 已 经 完成了数据 的 发  送  ，但 允许 继续 接收 来  自对
    方的数据 ，直 到 对 方 也 发     送 结束 报 文 段  以关闭 连接  。TCP 连接 的 这 种  状态  称 为 半 关 闭 (half
        图        3-7 Pras.
    close) 状态 ，如
                                                            器
                    客户                                   服务
               客户   热         nN
               行 半 关闭


               KARAS
                     数据                                     write
                  返回>0
                  read
                          的确认
                     数据
               返回   0 一                                  mae
        read






                                                图 3.7  半 关 闭 状态

                           图3-7        中，服 务器和 客户 端应用 程序 判断 对 方是            否  已经关闭 连接 的 方法    是 :
            请 注意 ，在
                                                报文段)。  当
            系统 调用 返回                   收 到 结束
    read                   0《〈                  然     ，Linux         还 提供 其 他 检测 连接 是 和  理被对 方 关 闭
    的 方法 ，这 将            在      后 续 章 节讨 论  。
                         络 编程
                                        接口
            socket  网                   通过  shutdown        提供 了 对 半 关 闭 的 支持 ，我 们 将 在 后 续 章 节 讨 论
        一下，虽 然 我们 介绍 了 半 关 闭 状态 ，但 是 使 用 半 关 闭 的 应 用 程序 很少见 。
它 。这 里强调

    3.3.3      连接 超时

            前 面 我 们讨论 的 是 很 快 建立 连接 的 情况 。如 果 客 户 端 访问                  一   个    它 很远    的 服务 器， 或
    者 由 于 网 络              ，导 致服务 器 对           于 客户 发      送出 的 同步  报  文 段 没有 应 ，           此 时客户
                                                                           等                  端程序将
                           什么样的行为呢?                SEARS     AY         说， 是     连
    产生                                                          TCP  来 它 必然 先进 行 重     〈     可 能 热
    行 多 次      7)，  如 果 重 连 仍 然 无        效  ，则  通知 应 用 程序    连接 超时   。
                         连接超时，                  服务                   环境 ，                       执行
            为 了 观察 我 们 横 拟                      一     个 繁忙 的     器       在 ernest-laptop 上     下面的
           :
    操作

            $  sudo       iptables     -F
            §  sudo       iptables     -I INPUT -p tep --syn -i      -3 DROP

## Page 58

40         第 一     篇   TCPIIP         协议 详解

                       用  于 过 滤 数 据       包   ，这 里 我们 利用 它 来        丢弃所有 接收 到 的 连接 请                求 〈 委          所 有
    iptable 命令
        报文段，       这 样    客  户 端 就 无 法 得 到 任何 确认 报 文             段)。
同步
                                                                                      用    tcpdump 抓
                                                                                                        取这           个
    接 下 来从         Kongming20        上 执行 telnet      命令   登录  到  ernest-laptop，      并                              过
程 中 双方 交换 的                      报 文     具体操
                      TCP                          作如下;
    $    sudo                -n  -i  ethO     port 23
    $      date;   telnet    192.168.1.108;        date     #  #  telnet 命 他 前  后 都 执行 date  #4,                    i]
    Mon      Jun   11  21:23:35     CST    2012
    Trying 192.168.1.108...
    telnet:        connect    to  address      192.168.1.108:    Connection     timed  out
    Mon      Jun   11  21:24:38     CST    2012
                               的     输出 来
                                              看  ，Kongming20  建立     TCP 连      接 的 超时
    从 两次 date 命令                                                                              时间是                    本
                                                                                                    63       s。      次
           的 输出
tcpdump               如代码清单          3-3    所 示 。
                                            代码 清单3-3     TCP     超时 重 连

    Ll.   21:23:35.612136           IP       192.168.1.109.39385  >  192.168.1.108.telnet:                Flags  [S$],
          seq        1355982096,  length      0
    2.    21:23:36.613146           IP       192.168.1.109.39385  >  192.168.1.108.telnet:                Flags  [S),
          seq        1355982096,    length    0
    3.    21:23:38.617279           IP       192.168.1.109.39385  >  192.168.1.108.telnet:                Flags  [S),
          seq        1355982096,    length    0
    4.    21:23:42.625140           IP       192.168.1.109.39385  >  192.168.1.108.     telnet:           Flags  [5S]，
          seq        1355982096,    length    0
    9.    21:23:50.641344           IP       192.168.1.109.39385  >  192.168.1.108.telnet:                Flags
          seq        1355982096,    length    0
    6.    21:24:06.673331           IP       192.168.1.109.39385  >  192.168.1.108.telnet:                Flags  [3]，
          seq        1355982096,    length    0

                                                                                                                     超
    这 次         抓包 我   们        保留了 tcpdump    (AME                  AL   -t    选 项 )，     以 便  推              Linux 的理
时 重 连 策略        。
    我 们     一    共 抓   取 到   6   个 TCP   报 文       ，它们 都 是 同步        报文         并 且 具 有 相同          的 序号         值 ， 这
   明后面       5   个 同步     报  文 段 都 是     超时    重 连 报 文   观 察 这       些 TCP      报 文 段 被 发 送     的   时 间 间 隔        ， 它
说
   分                                                                       ，                    都                偏差
们
         别  为   1s、2s、4s、8s          和   16s    (由  于 定时 器 精度 的 问题       这 些 时 间 间 隔 有              一   定           )，
                最后一个      TCP    报 文 段 的        超时  时间 是
可 以 推断                                                  32    s  63 s-16 s-8 s-4 s-2 s-1 s)。        因 此           ，TCP
        一共执行了                            ，                                          变量
模块                    5   次 重 连 操作          这 是 由 /proc/sys/net/ipv4/tcp_syn_retries     内  核         所 定义 的         。
每次重 连 的 超时            时 间 都       增加 一    倍 。 在  5  次 重 连 均 失    败 的 情况    下    ，TCP                   模块 放弃 连接 并 通知 应
用 程序     ，
                       中，    我 们 可 以     修改 连接 超时 时 间    ，     具 体 方法 将 在 本 书后 续 章 节 中                    介绍 进行    。
    在 应 用 程序

3.4      TCP                   状态 转移

    TCP     连接 的任意           一  端在任一时 刻 都处于 某种状态 ，当 前 状态 可                        以 通过     netstat           命令
                                         的是     TCP           连接 从 建立 到 关闭 的    整个过 程    中 通信 两 端         状态       的 变
17 章) 查看 。本 节 我              们 要 讨论

## Page 59

    协议详解 fe 41
    第 3 章 TCP


    化。图 3-8     是 完整 的状态 转移     ，  它 描绘
                        图                  了所有的    TCP     状态以 及 可 能 的 状态 转换   。

                                    被动打开 ;
                                        程序
                        SR                         NR,
                                                      ONC
                        ye                 So
                Cu      ay                收SYN  “y    Pity
发SYN，ACK
  同时 打开                                    &               主动     打开
                        .&,                Ae                         ，
                        es                 =
                    R     忆                wy     FIN!     CLOSE  WAIT  )
                    Ee     52
                                                                        RB
                                          同时 关闭    |   |     as          |
            :                                      |   |          ack
                                                   |
            !       =                              !
            |_          主动 关闭
                        图                  状态 转移 过 程

     图 3-8      中 的 粗 虚 线 表示 典型的服务 器 端连接 的状态 转移            ;      粗 实 线 表 示 典 型的客户 端    连接
的状态 转移 。CLOSED 是 一 个 假想的 起 始点，并 不 是 一个实际 的 状态 。

    3.4.1   TCP 状态 转移 总 图

     我 们 先 讨 论 服        务 器   的典型  状态  转移 过     程 ，此 时 我 们 说 的 连接 状态 都   是指 该    连接 的 服务 器
    端的状态    。
               器通过      系统 调用 见
                服务  1isten        (       第5$章)   进入  LISTEN  状态      ， 被 动 等 待客户     连接 ，
因此 执行 的是所 谓 的 被 动打开 。 服 务 器 旦监听 到 某 个 连接 请求〈 收 到 报文段)，
                                           一                                          同步 就
    将 该 连接 放 和 内核 等待队列 中，并 向               端发送带                             。
                                                  客户  SYN   标志 的 确认 报 文     段      此 时 该连接
    于SYN_RCVD 状态           。如 果    服 务 器 成 功    地 接收 到 客户 端 发 送   回   的 确认 报 文  ，则 该    连接
    处
        到  ESTABLISHED     ESTABLISHED 状态 是                连接 双方 能 够 进行     双向  数据传输     的
    转移
    。
    状态
     当 客 户             主动关闭 连接时        过      或                             结束      段
        通 close shutdown 系统 调用 向 服务 器 发 送 报 文 )，
    服务 器 通过 返回 确认 报 文 段 使连接 进入             WAIT            状态。                  明确       待
                                          CLOSE_ 这 个 状态 的 含义 很                      ，    等

## Page 60

    42    :5   第 一  篇   TCP/IP      协议 详解

                                               常，    服 务 器 检测
    服务 顺应 用 程序关闭 连接。通                                                        到客户端    关闭 连接       后   ，    也 会 立即 给 客户 端      发   送
       个         报  文 段  来         关闭 连接    。这 将 使       连接                  到    报
    一                                                            转移                    LAST_ACK 状态    ，以  等 待 客户             端对 结束
    文 段 的最后      一  次 确认    。    一               旦 确认完成    ，连 接           就  彻底   关闭 了 。
         下 面 讨    论 客户 端的 典型            状态       转移 过         程      ，    此 时 我 们 说 的 连接 状态 都 是 指 该           连接       的     客户 端的

                端通过     connect 系统 调用 〈 见
         客户                                              第    5      章    )  主动 与 服务 器 建立 连接              。connect         系统 调用 首
               器发        一 个同步 报              段                连接                             。        后
    先 给 服务              送               文 ， 使                        转移 到    SYN_SENT 状态 此                    ，connect       系统 调用
    可 能因为       如 下 两   个    原因 失败 返回         :
          口  如   果  connect            的                             存在                                                      处
                            连接 目标                    端口    不                  被 任何 进程 监听           )，     或 者 该 端口               于 仍 被
                                     的连接           所 占用     〈     见后 文       )，    则 服务 器 将 给      客户    端 发  送    个            报文
             TIME_WAIT 状态                                                                                          一            复位
             段    ，connect         调用 失败    。
             如 果 目  标 端 口 存          在 ，但    connect     在    超时 时 间 内 未 收 到 服务 器                  的  确认 报             ， 则 connect
             调用 失败      。
                                                         返回
                                                                     初始       的CLOSED 状态      如 果 客 户 端 成              功    收 到 服务
          connect   调用 失败 将使连接立即                         到                                       。
                                                                             成功返回， 连 接 转移
    器 的 同步 报     文      和 确认     ，  则  connect       调用                                    至    ESTABLISHED 状态           。
         当 客 户端执行                主动 关闭     时  ，    它 将 向 服务                  器 发  送  个 结束 报 文          ，  同时             进入      _
                                                                              一                        段      连接               FIN
    WAIT_1   状态。若 此 时 客户 端                    收  到 服务 器 专门 用                 于 确认 目的 的 确认 报 文             段   比    如   图 3-6   中 的
                                                                                                处于FIN_WAIT       2   状态
    TCP   报 文     段53)， 则 连接 转移 至              FIN_WAIT_2                 状态    。  当 客户 端                                  时   ， 服
             F CLOSE_WAIT 状态            ，     这    一                    对 状态 是 可 能 发生 半 关闭 的     状态    。      时
    Fs trek                                                                                               此 如 果 服 务 器            也
    关闭 连接 〈 发送结 束 报 文                  段    )， 则 客户      端    将           给 予   确认 并 进    和人              状态    。    关
                                                                                              TIME_WAIT                  于    TIME_
            状态
    WAIT         的含义 ，      我 们 将 在 下          一   节讨 论     。
                                     端从                                   状态 直接 进    人                 状态
          图 3-8   还 给 出 了 客户            FIN_WAIT_1                                        TIME_WAIT           的  一         条 线路 CR
       过 FIN_WAIT_2 状态 )，                       前 提 是 处    于FIN_WAIT
    经                                                                         1    状态 的 服务 器 直接 收 到 带            确认         信息 的 结
东 文段 而 不是先 收 到 确认 报 文 ，再收到 结束 报 文 段)。这 种 对应于图
                                                      段                                       情况                 3-6        中 的 服务
    ah AS RIE  TCP  报 文     段    $。
          前 面 说     ，       于                                                 需要              服务       结束        文
                 过      处        FIN_WAIT_2          状态 的 客户 端                等 待             器 发 送           报 朋            才 能 转
       至 TIME_WAIT 状态              ， 否 则 它 将         一     直 停 留          在  这 个 状态  。    如 果 不 是 为    了  在 半 关闭         状态   下 继续
    移
    接收 数据 ，连 接 长时 间 地 停留在                                        FIN_WAIT_2      并无           。           在 FIN
                                                                                              状态
                                                                                          益处     连 接 停留                   WAIT 2 状
    态 的 情况 可 能 发生在:                   客户 端   执行 半 关 闭                后    ，  未 等  服务 器 关闭 连接 就 强行 退出             了   。      此 时 客户
    端连接 由 内 核 来 接管               ， 可     称 之 为       孤儿    连接        〔        进程             类 似  )。Linux 为了            防止 孤儿 连接 长
    时 间 存 留     在   内 核     ，
        定
        中 义了两个内 核 变量 ，/proc/sys/netipv4/tcp_max_orphans 和 /proc/sys/
    neVipv4/tcp_fin_timeout。         前 者     指定内 核能                  接管      的    孤儿 连接 数目     ，                后 者 指定 孤儿 连接 在 内 核
    中 生存 的时 间     。
             此，我
                    们       简单
          至                      地 讨论 了 服务 器 和 客户 端 程序                        的     典型    TCP     状态 转移 路     线 。对 应 于       图 3-6
    所 示   的  TCP    连接 的 建立 与          断 开 过       程 ，    客 户        端    和 服务 器 的   状态       转移 如 图  3-9 所 示   。

## Page 61

    第3
     章  详解 43


                                ermest-laptop    Kongming20
                                (客户    端)                                       器)
                                                                               (服务
                       SYR      SENT        #2     (SYN)                   被 动       打开 LISTEN
                                                                           SYN RCVD

                     ESTABLISHED

ESTABLISHED

CLOSE_WAIT
                       FIN_      WAIT 2?
    ss
                       TIME_WAIT

                                                                                   CLOSED

        13-9 TCP 连接 的 建立和 断 开 过 程中客户 端和服务 器 的 状态 变化

         图       还         了 其他 非典
            3-8            绘        型的    TCP     状态 转移 路   线 ，        比 如 同时 关闭 与同时 打开           ，本 书 不 予
    讨论。

    3.4.2          TIME_WAIT 状态
            图    3-9  来
                        看，客     户     端连接 在 收 到 服务 器 的 结束 报 文段             CTCP    报 文  段       6 之
         从                                                                                        后，并   没有
                                    s，  而 是 转移
    直接      进入   CLOSED 状态          到    TIME_WAIT 状态                  。  在 这 个 状态     ，客 户 端     连 接 要 等待
   段 长      为2MSL (Maximum Segment Li
    一                               他，    报 文 段    最                  大 生存 时 间 )   的 时间  ，才 能 完全 关闭      。
    MSL 是 TCP        报 文段在网络 中 的 最大生存时间，标 准 文档                             的 建议
                                                                        RFC 1122       值是    2 min.
         TIME_WAIT         状态存在 的原因 有 两 点     :
                                    。
         CY 可    等   地 终止 TCP 连接
            保证                  TCP         报 文 段    足够 的时间 被 识别 并 丢弃     。
         QO          让迟来的           有
         第       个 原因 很 好 理解        。       假   设图 3-9 中 用 于           确认 服务 器 结束 报 文   段6
            一                                                                          的   TCP   报 文段  7 丢
    失 ， 那 么 服务 器 将 重 发结束 报 文                       。 因 此 客户 端            需要 停留 在某个 状态 以  处 理 重复 收 到 的
    报 文 段 《 即       问  服 务 器 发      送   确认 报 文 段     )。 否则，    客 户 端 将以 复位 报 文 段 来 回应 服务            ，    服
                                                                                                    器    务
    器 则 认为 这 是       一 个        错误 ，因 为 它 期 望 的是    一   个像   TCFP 报 文段    7     那样 的  确认 报 文段    。
                                上，一
         在 Linux 系统                 个     TCP    端口不能 被同时 打开 多 次                      次及以上)。当 一  个  TCP 连接
        于                  状态
    处 TIME_WAIT                 时，我         们    将 无 法 立即 使  用 该连接     占用 着 的 端口 来         建立一个   新 连接 。 反
        来思考 ，       如 果 不 存     在            WAIT                   状态 ，则 应 用 程序 能 够 立即建立 一个和 刚 关 闭 的 连接 相
    过
    似 的 连接          (这 里 说 相似       ，   是 指 它们 具有 相同
                         的          的    了     地    址                    和 端口 号 )。   这 个 新   的 、 和 原来   相似
    的 连接        被称 为   原来的      连接 的    化   身    〈incarnation)。 新 的 化  身 可 能 接收 到 属于 原来    的      连接   的、

            ”请           读者 根据 语 境 判 断 连 接 的 状态 是  指 客户 端状态 还   是服务 器  状态 ，后 同 。

## Page 62

    44   ++     第 一 篇     TCP/IP     协议     详解

                                                        的报文段)，                这 显然 是 不       应  该 发  生的 。        这 就是TIME
    带 应 用 程序数据          的   TCP      报 文       段(迟到
    WAIT 状态 存在 的 第 二 个 原因                   。
                      TCP     报 文 段 的           最    大 生存 时 间     是       MSL，   所以 坚持 2MSL           时 间 的     TIME  WAIT
         能够         网络                      传输 方向         被                   、                         段            消失
    状态
                确保                   上 两 个         上    尚未 接收 到               的          到 的 TCP 报 文     都 已 经           被
    中 转 路 由 器 丢弃 )。因 此，                     一  个 连接 的 新 的 化    身          可以 在    2MSL    时 间 之 后    安全      地 建立   ， 而 绝对
                到属于 原来连接                    的  应用 程序    数据     ，    这 就     是                 状态
    不 会 接收                                                                    TIME_WAIT           要    持续  2MSL       时间 的
    原因   。
         有 时 候 我      希望     避免 TIME_WAIT
                     们                                  状态，    因 为 当              程序         后，
                                                                                     退出 我 们 希望 能 够 立即 重启
    它 。但 由 于 处在         TIME_WAIT              状态 的 连接 还 占用 着 端口                  ，  程    序  将 无 法   启动(直到     2MSL     超时
                                                        机器 emnest-laptop      上以 客户 端            方式运行     〈     用 于 创建
    时间 结 )。考 虑          一    个 例子           :  在 测试                                               ne                     网
                        第    17      章) 命令 ，登 录 本 机                          Web 服务  ，    且 明确 指定 客户 端 使        用    12345
    络 连 接 的 工具 ，见                                         的                                                              端
                器通信。    然 后          从         输和人    Ctrl+C              终止 客户 端 程      ，接 着   又     重启                 完
    口 与服务                                       终端                                   序           立即     nc           程序 ，以
    全 相同 的 方式再次 连接 本 机 的                       Web 服务   。    具              作如下:
                                                          体操
         $  ne  -p  12345 192.168.1.108           80
                                                                                     端程序
                                                                           中 断 客户
                                                                                    端程序，      重 新    建立连接
         $  ne  -p  12345 192.168.1.108           80                      # 重启 客户
         ne:   bind failed:  Address            already    in             # 输出
                                                                     use            连接失 ，因为       12345      OM
         $  netstat  -nat                                                 # 用 netstat        查看注 接 状态
         Proto     Recv-Q0        Send-0  Local Address                       Foreign     Address        State
         tcp             0           0    192.168.1.108:12345            192.168.1.108:80                   TIME_WAIT
         这 里 我 们 使             用 netstat     命令 查看 连接 的 状态    。           其 输出 显示，        客 户    端 程序 被 中 断后        ，连 接 进
                                                  仍被           占用    ，所以 客户 端      重启     失败  。
    人 TIME_WAIT 状态                                      ，12345 端口
                   端程序来说，                我 们 通常    不    用 担心    上    面 描述 的 重启 问题             。 因 为 客户     端    一般 使 用 系统
         对 客户
    自动分      配  的 临时     端口  号   来 建立 连接           ，        而 由 于           性，      端口           般         上         用
                                                          随机 临 时                             号 一     和 程序  一    次 使    的 端
                                            状态的那个连接使用的端口号)                               不
    口号 〈 还 处于TIME_WAIT                                                                       同， 所以 客户 端 程序     一     般 可 以
    立即 重启       。上 面    的例子 仅仅              是 为   了   说 明 问     题    ，  我 们 强制 客户 端 使 用          12345    端口    ，这 才 导 致 立
    即 重启 客户 端        程序    失败 ，
         但    如                             果 是 服务 器 主动关闭 连接 后 异常    终止   ， 则 因为  它 总     是 使 用 同一 个      知名    服务   端口号 ，
    所以 连接的                 WAIT          状态 将 导致 它      不    能    立即        重启  。   过，            socket     选项SO
        不 我们 可以 通过
        _
REUSEADDR 来 强制进程 立即使 用 处于TIME_WAIT 状态 的 连接 占用 的 端口 ，这 将 在 第 章讨
        5 论。

                             复位
    3.5                     报文段
         在 某些特殊 条 件                      ，TCP  连接 的 一 端 会 向    另              端 发 送       携带 RST               标志
                                下                                           一                     的报文段            ， 即  复位
报 文 ，以 通知 对 方关闭 连接 或 重新 建立连接。本 节讨 论 产 生 复位 报 文 段的 3 种 情况 。
        不存在的端口
    3.5.1                                   访问

        到，当 客户 端 程序访问 一 个 不 存在 的 端口 时，目 标 主机 将 给它 发送一 个 复位
   3.4.1 小 节 提

## Page 63

    3章 TCP 协议 详解 f+ 45
    第


    报 文 。考 虑 从 Kongming20                上 执行               命令 登录                         上      个    不 存在
             段                                     telnet              emest-laptop         一               的     54321 端
       ，并                                                                                                体操
    口        用    tcpdump   抓  取 该 过    程中 两    台  主 机 交换      的 TCP   报 文        。具                 作过程如下:
         $   sudo    tcpdump   -nt -i     ech0 port  54321       仅     抓 取 发 送    至和 来  自 54321 ROM
   $ telnet 192.168.1.108 54321
   Trying 192.168.1.108...
   telnet: connect to address 192.168.1.108: Connection refused
      Fe FF AY ii ie anit Rea
        TT, Aik
   SMO                                                                                    topdump
                                                                                                        抓取到的          TCP
         telnet                                                                                                         报

         1.       IP 192.168.1.109.42001      >  192.168.1.108.54321:           Flags     ($],   seq    21621375, win
                  14600,    length 0
         2.       IP        192.168.1.108.54321  >   192.168.1.109.42001:           Flags           [(R.], seq 0, ack
                  21621376, win    0,  length   0
                 此可见               针对                          的 连接
         由                                     Kongming20                请求     〈 同  步  报 文      段  ) 回应了   一      个 复位 报
    文         段 (tcpdump    输出     标志
                               R     )。   因 为    复位  报 文 段       的  接收 通告 窗口        大   小    为     0，所以 可以 预见        :  收
    到        报文段的              应                   者重新           ，         回应
 复位 一 端 该 关闭 连接 或 连接 而 不 能 这 个 复位 报 文 段 ，
                     上，当 客户        端  程序  向服务 器 的 某         个      发起    连接     ， 而 该 端口
         实际                                                                                      仍被处于          TIME_ WAIT
         的连接所占用时，客 户                      端  程序 也 将 收 到 复位 报 文         段 。
    状态

    3.5.2        ”异常 终止        连接
         前 面 讨 论      的     连接 终止方式 都是正常 的终止 方式                  :       数据 交换 完成 之              后  ， 一  方 给另  一  方  发  送
         报文段          。TCP 提供 了 异常 终止 一            个 连接 的 方法           ， 即 给 对 方    发   送 一   个 复位 报 文       。          发
    结束                                                                                                       段          一
   复位 发 送 端 有 排队 等 待 发 的 数据 都 将 被 弃 。
    送了            报文段，             所                    送
                 用
         应        程序     可  以 使用   socket 选项     SO_LINGER       来 发     送 复 位 报     文        ，以    异常 终止    一 个  连接    。
       们将在第          5                章讨论SO_LINGER 选项
    我                                               。
                                      连接
    3.5.3        ”处               理 半 打开
                  下面的情况 : 服务
         考虑                             器                                                   关闭 或者异常 终止了连接 ，而 对 方 没有接收 到 结
                                                     时，客       户 端    (或 服务       器 )  还 维持 着 原来           的连接    ，     而
    束 报 文段〈 比 如 发 生 了 网            络    故    障 )，此                                                                      服
    Ft (ME Pi)        即使 重启 ，也               已 经     没有 该    连接 的 任何 信息 了           。  我 们 将 这 种         状态 称 为   半   打 开
状态 ，处 于 这 种 状态 的 连接 称为 半 打 开 连 。如 果 客 户端〈 或 服务
                                                         接                                器)        往 处  于    半 打 开 状 态 的
           写人数据 ，则 对 方将回应                 一  个 复位 报     文段     。
    连接
         举例 来         ，    我 们 在 Kongming20        上 使  用        命令 模拟          个 服务 器 程序           ，        使 之 监听 12345
                       说                                nc                 一                                            端
    口  ，然后从          ernest-laptop 运行 telnet      命令登录 到 该                 上，   接 着 拔 掉   ernest-laptop     的 网   线  ，  并
    在 Kongming20             上 中 断 服 务 器 程序 。        然，        时                            的               端          维持
                                                                        显 此 emest-laptop 上 运行    telnet                客户
                                                                                                             程序
    着  一   个 半 打 开 连 接    。        然 后 接  上 ernest-laptop    的 网    线 ，并 从 客户 端            程序 往 半 打 开 连     接 写      1  字
                      时，运
                                       行           程序抓取 整个过 程 中                 telnet 客户
    节 的 数据“a"。       同                                                                           端和                    服务
                                                                                                      nec            器交换的
    TCP    报 文                          。具 体操
                     段                 作过程如下:

## Page 64

46 «>  TCP/IP 协议 详解


    $    ne    -1 12345                                               # 在 Kongming20
    $    sudo    tcpdump     -nt     -i ethO       port     12345
    §    telnet     192.168.1.109     12345                           ##ernest-laptop              Papa
    Trying        192.168.1.109...
    Connected         to    192.168.1.109.
    Escape        character     is    '*)'.                           # Late   ernest-laptop HAH,        #HARSE
    a                                                                                         a
    Connection         closed     by    foreign          host.
    telnet     的 输出 显示 ，连 接 被服务 器 关闭了                       。tcpdump           取 的                   内
抓
                                                                                 到     TCP     报 文 段    容 如 下 ;
         IP    192.168.1.108.55100              >       192,168.1.109.12345:   Flags           seq 3093809365,
         length       0
    2.   IP    192.168.1.109.12345              >       192.168.1.108.55100:   Flags   [S.],    seq  1495337791,
         ack      3093809366,        length     0
3. IP 192.168.1.108.55100 > 192.168.1.109.12345: Flags [.], ack 1, length 0
    4.   IP    192.168.1.108.55100              >       192.168.1.109.12345:   Flags   [P.],    seq  1:4, ack  1,
         length       3
    5.   IP                                     >       192.168.1.108.55100:   Flags   [R]),   seq 1495337792,
         length       0
                                                                                       的     次 握手
    该 输出内 容 ，                                                                                        的过程。          第 4
                          中    前   3 个      TCP    报 文 段    是 正常        建立  TCP 连接      3                         个
TCP    报 文 段由客户 端 发送给 服务                                 ，它 携带了  3               程序        ，             是
                                                   器        字 节 的 应 用                  数据 这     3   字 节 依次        :  字
Ae “a”.     LAR “We”        “in”.                                       ARLE                           BELL Kongming20
对 客户 站         送    的  数据 回应 了     一    个 复位 报 文            段 $。

3.6      TCP 交互 数据 流

    前 面讨 论了                                           连接
                       TCP     及其                     状态 ，从 本节 开 始 我    们讨论 通过 TCP 连接 交换 的               应      用 程序 数
据   。TCP    报 文       段 所        携带 的应  用 程序              数据 按照 长度    分为 两  种          数据和成块数据。                    数
                                                                               :交互                       交 互         据
                                                                                       性要求高，         比
仅 包 含 很 少         的   字 节 。    使    用交互 数据 的 应 用               程序 (或 协议     )  对 实时                    如  telnet.  ssh
等。成 块数据 的              长 度 则 通常          为      TCP      报 文段 允许 的  最   大 数据 长      度  。 使 用 成 块     数据   的 应 用     程序
(或 协议) 对 传输效率 要 求                        ，        比
如              下          高                     如       fp。 本 节 我 们讨论 交互       数据   流  。
                                                                           登录到
                                                                                      本机
    考虑                                                                                  ，     在
    情况 + 在 emest-laptop 上 执行 telnet 命令 然 后 shell 命令 提示 符
                             时用    tepdump      抓       取 这   过程                   端和      服务
后 执行 ls 命令            ，       同                             一    中          telnet 客户  telnet       器交换   的      TCP 报
                               段。 体操
文        具  作过程如下:
    $        tcpdump  -nt   -i   lo  port     23
    5    telnet     127.0.0.1
    Trying     127.0.0.1...
    Connected      to 127.0.0.1.
    Escape     character         is  '*]'.
    Ubuntu     9.10
    ernest-laptop         login:     ernest         (B#)
    Password:      (H#)                                     #           输入     并轿车

    ernest@ernest-laptop:~5     ls

## Page 65

                                                                                                                       第 3    童   TCP 协议         详解          47

                                                                                     很多    TCP    报 文               。下 面 我 们 仅 列 出 我 们               感       的、
         上 述 过 程 将 引起 客户                  端                                   和 服务 器 交换                        段                                             兴趣
    执行   ls   命令 产生 的       tcpdump       输出         ，如 代码                           清单    3-4 所 示       。
                                                   代码  清单                 TCP 交互 数据 流

         1.   IP        127.0.0.1.58130       >                      127.0.0.1.23:         Flags      [P.),         seq  1408334912:1408334813,
              ack         1415955507,  win       613,     length 1
         2.   IP        127.0.0.1.23   >                          127.0.0.1.58130:         Flags      [P.],         seq  1:2,     ack    1,  win   512,
              lengt
         3.   IP                              >                      127.0.0.1.23:         Flags      [.],     ack      2,    win  613,     length    0
         4.   IP        127.0.0.1.58130       >                      127.0.0.1.23:         Flags      [P.],         seq  1:2,     ack    2,  win   613,
              length      1
         5.   IP        127.0.0.1.23   >                          127.0.0.1.58130:         Flags      [P.],         seq  2:3,     ack    2,  win   512,
              length      1
         6.   IP        127.0.0.1.58130       >                      127.0.0.1.23:         Flags      [.],     ack      3,    win  613,     length    0
         7.   IP        127.0.0.1.58130       >                      127.0.0.1.23:         Flags      [P.],         seq  2:4,     ack    3,  win   613,
              length      2
         8.   IP        127.0.0.1.23   >                          127.0.0.1.58130:         Flags      [P.],         seq  3:176,     ack     4,   win  512,
              length      173
         9.   IP        127.0.0.1.58130       >                      127.0.0.1.23:         Flags      [.],     ack      176,    win    630,     length    0
         10.       IP     127.0.0.1.23    >     127.0.0.1.58130:                           Flags         [P.],      seq    176:228,        ack   4,   win  512,
                   length  52
         ll.       IP     127.0.0.1.58130        >                       127.0.0.1.23:     Flags         [.],       ack  228,     win    630,    length    0

                       报 文段 由      客户 端 发送给 服务                            它 携带                                                                AB
         TCP                1                                   器                ，               1    ae.                                               Be “1”.
                                    器对       TCP   报 文      段  1 的 确认，同 时 回 显 字母 “1"。TCP 报 文段                                                       3      是 客户
    TCP  报 文           段2         是 服务
    对 TCP     报 文段 2        的    确认。      第4       一 6      个 TCP 报 文 段是针对 字母“s”                                             的    上 述 过 程        。TCP       IRE
    7       传送的     2   字 节 数据分 别         是 :    客户            键 和人的 回 车符                      和 流 结束               符            本 例 中 是 0x00).             TCP
    报 文段                     携带服务 器 返回    的 客户 查询 的 目录 的 内 容                                                  命令
              8                                                                                (ls                   的输出)，         包 括 该        目录       下 文件 的
    文件 名 及 其 显示 控制 参数                       。TCP     HOCK       9  是                           端对                          段                              段
                                                                客户                                        TCP 报 文 8          的 确认           。TCP      报 文    10
携带 也 是 服务 器 返回 给 客户端的 数据 ，包 括 一个回 车 、 个 换行
         的                                                                                                  符       一             符、客 户 端登录               用 户 的
    PS1  环境 变量 〈 第          级            命令 提示 符 )。TCP 报 文                                 段   11     是             端对                     段
                            一                                                                            客户              TCP      报 文       10   的 确认     。
         在 上 述 过 程          中    ， 客 户    端   针 对 服务 器 返回                 的数据                  所 发                   送的确认 报 文段           CTCP 报 文 段         6、9
                                                                                                                                                           CTCP
    和 11      都        不   携带 任何应     用   程序数据              〈 长    度      为 0)， 而 服务 器 每 次 发 送 的                                      确认 报 文段                 报
    文    段 2、5、8           和 10    都 包含 它          需 要 发 送 的    应         用                程序    数据         。       服 务  器     的 这 种 处 理 方式 称 为              延迟
    确认 ， 即 它 不马上 确认 上 次 收 到 的 数据 ，而 是 在 一 段 延迟时间 后 查看 本 端 是否有 数据 需要 发
    送 ， 如 果 有           ，  则 和 确认 信息        一     起 发        出 。 因 为  服务 器 对 客户 请 求                                     处理     得 很       快 ，所    以 它 发      送确认
    报 文 段     的        时 候 总 是 有 数据     一   起 发              。延   迟确认可 以                                  发送                                    。          由 用户
                                                       送                                       减少                   TCP    报 文 段 的 数量                       而 于
    的   输入 速度             明显 慢 于 客户 端              程序的       处 理 速度   ，                    所以 客户                    端的确认       报 文  段  总           是 不 携带任何 应 用
    程序 数据           。   前 文 兽    到 ，    在     TCP    连接 的             和 断 开 过 程 建立                          中  ，           也 可 能 发生    延    迟确 认 。
                                                                                                                                                             就未
         上 例 是             在本 地 回路 运行 的                    结果，               在 局 域    网    中 也 能得 到 基本 相同 的结果                               ，但 在            广域网
                                                                                                                    且，                       携 带 交互 数据 的 微小 TCP
    必 如 此 了        。    广 域 网    上 的 交互 数据 流 可 能 经 受 很                                     大的 延迟          ， 并                                                 报
    文段                 一般很多                                           个                                            )，               都                 拥塞发
         数量 〈               一      个 按键 输入                  就  导致 一 TCP 报 文 段                                          这 些 因素            可 能 导致             生 。

## Page 66

48           第 一    篇       TCP/IP  协议 详解

解决 该 问题            一    个       简单 有 效 的 方法 是          使用        算法
                   的                                       Nagle       。
             算法
    Nagle          要求一个              TCP   连接       的  通信 双方      在 任意 时 刻 都        最多 只   能 发  送一 个 未      被   确认 的
                   该    TCP 报 文 段 的 确认 到 达 之 前 不                         能 发 送 其 他      报 文     。另         方面  ，
TCP 报 文 ，在                                                                                                         发
               段                                                                  TCP      段       一               送
方 在 等 待确认 的 同时 收集                      本 端 需要   发    送 的    微量   数据   ， 并 在 确认 到 来      时以 一    个     TCP 报 文  段 将 它
们 全 部 发        出    。这 样 就   极    大 地 减少    了   网    络 上 的     微小  TCP 报 文 段 的 数量       。  该 算法 的       另 一 个   优点 在
于 其 自   适     应     性，  确 认 到 达 得 越        快  ， 数 据 也 就 发      送   得 越  快。
                   块                     流
3.7     TCP 成                           数据

    下 面考虑 用 FTP     协议 传输 一个大 文件                           ，    在    ermest-laptop  上 启动 一 个 vsftpd          服务 器 程序
        的、     安 全 版    的    ftp         器      )，                                      器      后
(升级                服务
                                             程序        并 执行  ftp    命令 登录 该 服务      上     ， 然 在 ftp            命令 提示
符 后输入        get    命令 ，从 服务 器 下 载                  个  几 百兆     的  大 文件   。  同
                                                一                             时用 tepdump     抓   取 这    一  个   过 程 中
              端和  服务
ftp 客户             器                   交换  的           报 文      。                  体操
                   vsftpd                   TCP            段     具             作过程如下:
    $     sudo tepdump         -nt   -i  ethO port     20                 #    vsftpd              MOS     20
    $     ftp  127.0.0.1
    Connected      to     127.0.0.1.
    220      (vsFTPd         2.3.0
    Name     (127.0.0.l:ernest):           ernest      (H#)               (HAA
    331   Please            specify  the password.
                                                                                        并回车
    Password:      (B#)                                                   4#  输入 密码
    230   Login     successful.
    Remote         system      type  is  UNIX.
    Using      binary mode     to       transfer     files.
    ftp>     get     bigfile                                                  获取
                                                                          #         大文件  bigtile
    代码 清单          3-5     是    该 过 程 的 部    分tcpdump 输出         。
                                           代码       清单 3-5     TCP  成 块       流
                                                                          数据

1. IP 127.0.0.1.20 > 127.0.0.1.39651: Flags [(.), seq 205783041:205799425, ack 1,
         win       513,      length  16384
2. IP 127.0.0.1.20 > 127.0.0.1.39651: Flags [.], seq 205799425:205815809, ack 1,
         win       513,      length  16384
3. IP 127.0.0.1.20 > 127.0.0.1.39651: Flags [.], seq 205815809:205832193, ack 1,
         win       513,      length  16384
    4.   IP    127.0.0.1.20          >  127.0.0.1.39651:         Flags  [P.], seq   205832193:205848577,       ack
         1,    win 513,     length       16384
5. IP 127.0.0.1.20 > 127.0.0.1.39651: Flags [.], seq 205848577:205864961, ack 1,
         win       513,      length  16384
6. IP 127.0.0.1.20 > 127.0.0.1.39651: Flags [.], seq 205864961:205881345, ack 1,
         win       513,      lenath  16384
7. IP 127.0.0.1.20 > 127.0.0.1.39651: Flags [.], seq 205881345:205897729, ack 1,
         win       513,     length   163864
    8.   IP    127.0.0.1.20          >  127.0.0.1.39651:         Flags  [P.], seq   205897729:205914113,       ack
         1,    win 513,     length       16384
    9.   IP    127.0.0.1.20          >  127.0.0.1.39651:       Flags          seq   205914113:205930497,     ack  1,
         win       513,     length   16384

## Page 67

章 TCP 协议 详解 f+ 49
第 3


   10,     IP   127.0.0.1.20      >              127.0.0.1.39651:   Flags               [.],           sea   205930497:205946881,             ack
           1,   win 513,      length 16384
   lL.     IP   127.0.0.1.20      >              127.0.0.1.39651:   Flags               [.],           seq   205946881:2059632765,            ack
           1,   win 513,      length 16384
   12.     IP   127.0.0.1.20      >              127.0.0.1.39651:   Flags               [P.],          seq                205963265:205979649,   ack
           1,   win 513,      length 16384
   13.     IP   127.0.0.1.20      >              127.0.0.1.39651:   Flags               [.],           seq   205979649:205996033,             ack
           1,   win 513,      length 16384
   14.     IP   127.0.0.1.20      >              127.0.0.1.39651:   Flags               [.],           seq   205996033:206012417,             ack
           1,   win 513,      length 16384
   15.     IP   127.0.0.1.20      >              127.0.0.1.39651:   Flags               [.],           seq   206012417:206028801,             ack
           1,   win 513,      length 16384
   16.     IP   127.0.0.1.20      >              127.0.0.1.39651:   Flags               [P.],          seq                206028801:206045185,   ack
           1,   win 513,      length 16384
   17.     IP   127.0.0.1.39651      >              127.0.0.1.20:   Flags               [.],           ack   205815809,         win     30084,
           length    0
   18.     IP   127.0.0.1.39651      >              127.0.0.1.20:   Flags                              ack   206045185,         win     27317,
           length    0

   注意 ，客 户 端 发 送 的 最 后 两个                        TCP     报 文        段 17 和 18，                       它们分 别 是 对 TCP 报 文                    2     和 16
                                                     此    可                                   见，当 传输大 量  大   块    数据    的   时     候 ，发 送 方       会 连
的 确认    〈  从 序号值和 确认值 来判断 )。由
              BT TCP    报 文     段 ， 接 收 方     可以    一     次 确认 所 有 这 些 报                                  文      。那 么 发       送   方 在    收 到 上 一   次
BRIE
      后，能 连续
确认                      发送多少个        TCP      报 文 段     呢  ?      这 是 由        接收 通告                         窗口      〈               还 需要 考虑 拥塞 窗口 ，
                            的           报文段                                             还 说明 客 户 端 能     接收      084X64       字 节                  本
见 后 文   )   的 大小 决定                                 17                                                       30                       〈          例中窗
口扩大 因子          为   6)， 即     1 925 376  字 节 的 数据。而在                      TCP                     报 文   段    18      中 ，    接 收 通   告  口        大小 为 窗
1 748 288   字 节，即 客户 端 能 接收                   的   数据 量变           小 了 。   这 表明 客户                                   端的TCP   接收 缓冲      区       有 更 多
的 数据 未 被应用 程序 读 取 而 停留 在 其                              ，  这 些            都                         自                  段
                                                      中                                         数据 来  TCP 报 文            3  ~    16 中 的   一   部 分  。
AR TCP 报 文段            18           后 ，它 至少【〈 因 为 接收 通告 窗口可 能 扩大 )                                               还 能 连续 发              送    的    未 被
      的报文 数量是               1748 288/16 384           个   ， 即             个                                  会              送         )。          中，
       确认                                                         106 〈 但                           一  般 不           连续 发       这 么   多        其
16 384 是 成      块 数据 的 长度         〈 见 TCP         报 文    段 1     一  16 的 length                        值     )，    很 显然         小   于 但     接近   MSS
      的 16  396 字 节     。
规定
   另外       一   个   值得 注意的地 方                 是   ， 服 务    器     每 发      送    4                  个 TCP  报 文 段 就            传送    一      个 PSH    标志
(tcpdump    输出 标志 P)        给 客户              ，以    通知 客户 端的              应        用              程序   尽快读             取 数  。    不 过 这 对           器
                                         端                                                                       据                                服务
来说显然 不是必需                 ，    因 为 它 知道 客户              TCP           接收                          缓冲 区 中     还有                       空闲 空间 〈 接 收 通告
                         的                                                                                                                        窗口
大 小 不为0)。
   下 面 我 们 修改           系统 的      TCP 接收 缓冲             区  和 TCP        发 送                         缓冲 区  的  大 〈 如 何 修改
                                                                                                                 小                         将在第 16 BE
      )，使 之 都               字
介绍                                节，然后重启 vsftpd 服务                                 ，并                再 次 执行      上           述 操作。  此
                    为   4096                                                   器                                                      次      tepdump
的 部分 输出 如代码             清单3-6       所 示 。
                  代码 清单             Be              接收和 发送组                                           大小 后的                 成 块数据
                          3-6            TCP                                       区                         TCP                流
1. IP 127.0.0.1.20 > 127.0.0.1.45227: Flags [.], seq 5195777:5197313, ack 1,
  win 3072, length 1536
2. IP 127.0.0.1.20 > 127.0.0.1.45227: Flags [.]), seq 5197313:5198849, ack 1,

## Page 68

 50 第 一篇 TCP/IP 协议 详解


         win 3072,        length      1536
     3.  IP      127.0.0.1.45227         > 127.0.0.1.20:       Flags       [.],   ack   5198849,    win   3072, length  0
     4.  IP      127.0.0.1.20       >    127,0.0.1.45227:      Flags       [P.},      seq  5198849:5200385,     ack 1,
         win 3072,        length      1536
     >.  IP      127.0.0.1.45227         > 127.0.0.1.20:       Flags       [.],   ack   5200385,    win   3072, length  0
 eee
                    报文段〈  未 在代码 清单              3-6     中 列      出)    得   知   在  这 次 通信 过     程    中，  客 户 端    和      器
     从 同步                                                                                                              服务
                      都   为 0，   因 而 客户                    和 服务 器 每         次 通告  的  窗口 大小 都     是  3072   节
 的 窗口 扩大 因子                                                                                                    字 〈   没 超过
 4096 字 节     ，预    料 之中  )。     因 为      每个 成      块    数据 的 长 度          为 1536       节，
                                                                                     字 所以服务 器 在                 收到  上 一 个
 TCP 报 文 段的确认 之 前 最 多                    还 能 再 发    送     1  个 TCP     报 文     ，      这 正 是         报 文   段            描述
                                                                               段          TCP               1  ~ 3      的
 THE

 3.8

     的概念 ，用 于 迅速 通告对 方 本 端 发
有 些 传输层协议具有 带外 (Out Of Band, OOB) 数据
 生 的 重要 事件         。 此，                                                    带            )           的         级
                     因 带 外 数据 比 普通 数据 (也 称 为                               内 数据           有 更    高 优先         ，    它应 该 总
        被发送，         而 不 论 发 送            缓冲 区  中 是    否    有 排队 等 待 发     送             的 普通 数据 。          外数据
 是立即                                                                                                          的传输可      以
 使用  一  条 独立 的 传输层 连                     接，也 可 以 映射 到              传输  普通 数据 的 连接             中。实 际 应     用 中，     带 外 数据
                                                               非活牙程序。
 的 使 用 很 少见 ，        已 知 的    仅     有 telnet.   ftp 等 远程
     UDP                      没有 实现 带 外 数据 传输   ，TCP     也     没有 真正       的 带 外 数据       。    过                    其头部中的
                                                                                                                 不 TCP 利用
 紧急 指针 标志 和 紧急 指针 两个字段 ，                          给 应 用            程序      了   一    种      紧急 方式 。TCP          的 紧急 方式 利用
 传输 普通 数据 的连接 来 传输紧急 数据 。                           这 种 紧急 数据 的 含义                    和 带 外 数据 类        似 ，   因 此 后 文 也 将
 TCP 紧急 数据 称为带外 数据                       。
        们先来        介绍  TCP     发      送    带 外 数据   的     过    。  假 设一         个  进程 已    经 往  某个   TCP              的发送组
 我                                                  程                                                                  连接
 冲 区 中 写人了     w     字 节 的 普通 数据          ，     并 等 待 其发           。     在     被发送前，                      又
                                                                    送                                  数据 该 进程 向 这 个 连接 写
                                                时，     发    送       的TCP     报 文 段      的 头  部 将 被  设置    URG 标志      ， 并
 人 了 3 字 节 的 带 外 数据“abc”。                 此         待
 且 紧 急 指 针 被 设置        为 指向 最 后           一  个 带 外 数据 的 下         一    字 节   〈    进   一   步 减 去当前   TCP    报 文 段    的  序号
                                                图   3-10     所 示 。
 值得 到 其 头部中 的 紧急 偏 移                      值 )， 如

发送                                                                缓冲   区
                                                                      TCP
              em

          第 1 字 节                               第 N 字    节                            紧急 指针
                                                   TCP     发 送 缓冲区 中 的 紧急 数据

          3-10 可图
       由               见，    发 送      端    一 次 发 送   的     多 字 节 的 带 外 数据 中               只有 最 后 一         字 节 被当 作 带 外 数
                     其他   数据              字   a 和     被 当成
 据 《〈字        c)，              而                                   了     普通    数据。        如         模块        TCP       报
          母                           〈      母      b)                                    果    TCP        以多个
                          示     TCP       发       送 缓冲
                                                    区    中
                                                    的             容，     则 每    个   TCP     报 文 段 都  将设置      URG      标志
 文 段 来 发 送       图 3-10 所                           内                                                                   ，

## Page 69

                                                                                     第     3    章     TCP     协议   详解            51

并 且 它 们 的 紧急 指针 指向 同一 个位置 (数据 流                                  中     带 外 数据 的下      一        个 位 置 )，但           只有 一       个   TCP
报 文 段 真正携带 带 外 数据    。
     现在 考虑     TCP     接收 带 外 数据的 过     程         。TCP           接收 端只 有 在 接收 到 紧急 指针 标志 时 才 检                                      查
紧急 指针，然 后根据 紧急 指针所 指 的 位置确 定 带 外 数据 的 位                                              置 ，   并 将它 读 和      人    一             个 特殊 的 缓存
                      只有 字
中。   这 个 缓存     1      节，                      称 为 带 外 缓存 。   如 果 上 层 应 用 程序 没有 及 时 将 带 外 数据 从 带 外 组
 中读出，          则 后 续 的   带 外 数据      〈 如 果 有 的 话 )               将 覆盖
存                                                                          它。
              讨论 的 带 外 数据    的                TCP 接收 过 程是        模块 接收 带 外 数据 的默认 方式 。如 果 我                                      们  给
     前 面
                                                                                                                                    在
TCP    连接 设置了 SO_OOBINLINE 选项                           ， 则   带 外 数据 将          和  普通 数据 一 样被                               TCP 模块 存放
TCP   接收 缓冲区中。此 时 应 用 程序 需要 像读取 普 通 数据                                       一     样 来 读 取 带 外 数据                  ， 那么        这 种 情况
下 如何 区 分 带 外 数据和 普通 数据呢? 显然 ，紧                                   急     指针 可以 用 来 指出 带 外 数据 的 位                           置    ，socket
编程接口 也 提供 了 系统 调用 来识别 带 外 数据                      〈 见第                 $    章 )。
           此，      了                 送                                                 。                 如
     至 我 们讨论 TCP 模块 发 和 接收 带 外 数据                                            的  过    程     至     于 内    核 何                    应 用 程序
              的到来     ，以 及 应 用 程序如何 发                   送 和 接收 带 外 数据           ，    将  在  后 续 章节讨论             。
带 外 数据

3.9        TCP     超时 重 传

               一    3.8  节
     在 3.6 节     中，我 们 讲述了           TCP          在 正常 网络情况下 的 数据                                       。    从 本 节 开     始    ，     我
                                                                                                        流                           们
       异常网络状总下 《开始
讨论                                出现超时或入                         )，TCP      如 何 控制数据 传输以 保证 其 承诺                              的 可   车
服务。
                                                                                                            此，TCP                  模块
     TCP      服务 必须 能 够 重 传超时 时间 内 未 收 到 确认 的                                         TCP 报 文    段   。 为                          为每个
TCP    报 文 段 都维护     个     重 传 定 时器 ，该定时 器 在                           TCP   报 文段第                      次 被 发 送 时   启动     。  如     超
               一                                                                            一                                       果
时 时 间 内 未收 到 接收 方    的    应          ，TCP 模块
                                 答                将重传              TCP      报 文段          并 重        定时     器 。 至 于 下 次 重 传         的
      时间如何选择                         多                                 是                              。
超时 ，以 及 最 多 执行 少 次 重                                          传                      ，就 TCP 的 重 传 策略                   我 们 通过 实例 来 研究
                             重传策略。
Linux      下 TCP              的 超时
               局
     fE ernest-laptop 上     动 iperf    服务         序           ，然 后从         Kongming20 上 执行 telnet                             命令登录 该
           程序。接 下        ，从 telnet     客户 端 发     一           些    数据 〈 此 处是“1234”)                      给 服务 ，
服务                                                                                                                                  然
               来                                  送                                                                 器             后断开
服务         的 网 线并 再次从 客户 端 发送一些数据 给服务                                       〈            此 处 是“12”)。      时，                       抓
                                                                                                                          同 用 tepdump
取 这        过 程中客户 端 和服务 器 交换              TCP     报 文                                          。具 体操
      一                              的                             段                          作过程如下:
     $     sudo  tcpdump -n  -i ethO   port           5001
           iperf  -s                                               #    #  ernest-Laptop Lift
     $     telnet   192.168.1.108 5001                             #    在  Kongming20      上 执行
     Trying     192.168.1.108...
     Connected     to    192.168,.1,108.
     Escape     character   is
     1234
     connection     closed   by  foreign host
     iperf                       状况               选项               表示 将         其    作为    服务        器                      运行 。iperf
              是  一  个 测量 网 络         的 工具              ，-s                                                                       默认监听
                                                                           当于        个 discard       服务
5001  端口      ，  并      该 端口    上 接收 到 的    所有 数据                  ，    相       一                        器。        上 述 操 作 过 程

## Page 70

 52        第 一  篇  TCP/IP         协议 详解

    分          如                     清单
 的 部           输出
        tcpdump     代码                 3-7   所 示 。
                                           清单                     重伟
                                       代码                   超时
 eee
1.        18:44:57.580341        IP  192.168.1.109.38234    >    192.168.1.108.5001: Flags    (S],  seq
          2381272950,        length  0
2.        16:44:57.580477        IP  192.168.1.108.5001   >     192.168.1.109.38234: Flags    [S.], seq
          466032301,  ack   2381272951,     length 0
3.        18:44:57.580498        IP  192.168.1.109.38234    >    192.168.1.108.5001: Flags    [.],  ack
          1, length  0
4.        18:44:59.866019        IP  192.168.1.109.38234    >    192.168.1.108.5001: Flags    [P.), seq
               ack  1,       length  6
5S.       18:44:59.866165        IP  192.168.1.108.5001   >     192.168.1.109.38234: Flags    [.},  ack
          7, length  0
6.        18:45:25.028933        IP  192.168.1.109.38234    >    192.168.1.108.5001: Flags    [P.], seq
          7:11, ack  1,  length       4
7.        18:45:25.230034        IP  192.168.1.109.38234    >    192.168.1.108.5001: Flags    [P.], seq
          7:11, ack  1,  length       4
8.        18:45:25.639407        IP  192.168.1.109.38234    >    192.168.1.108.5001: Flags    [P.), seq
          Till, ack  1,               4
9.        18:45:26.455942        IP   192.168.1.109.38234   >    192.168.1.108.5001: Flags    [P.], seq
          Till, ack  1,  Length       4
      10.  18:45:28,.092425   IP       192.168.1.109.38234     >  192.168.1.108.S5001:  Flags  [P.],
           seq 7:11,   ack 1,     length   4
      11.  18:45:31.362473    IP       192.168.1.109.38234     >  192.168.1.108.5001:   Flags  [P.},
           seq 7:11,   ack 1,     length   4
  12. 18:45:33.100888 ARP, Request who-has 192.168.1.108 tell 192,.168.1.109,
     length 238
  13. 16:45:34.098156 ARP, Request who-has 192.168.1.108 tell 192.168.1.109,
     length 28
  14. 18:45:35.100887 ARP, Request who-has 192.168.1.108 tell 192.168.1.109,
     length 28
  15. 18:45:37.902034 ARP, Request who-has 192.168.1.108 tell 192.168.1.109,
     length 28
  16. 18:45:38.903126 ARP, Request who-has 192.168.1.108 tell 192.168.1.109,
     length 28
i7.        18:45:39,901421    ARP,     Request   who-has  192.168.1.108  tell  192.168.1.109,
           length 28
18.        18:45:44.440049    ARP,     Request   who-has  192.168.1.108  tell  192.168.1.109,
           length 28
19,        18:45:45.438840    ARP,     Request   who-has  192.168.1.108  tell  192.168.1.109,
           length 28
20.        18:45:46,439932    ARP,     Request   who-has  192.168.1.108  tell  192.168.1.109,
           length 28
21.        18:45:50.976710    ARP,     Request   who-has  192.168.1.108  tell  192.166.1.109,
           length 28
22.        18:45:51.974134    ARP,     Request   who-has  192.168.1.108  tell  192.168.1.109,
           length 28
23.        18:45:52.973939    ARP,     Request   who-has  192.168.1.108  tell  192.168.1.109,
           length 28
 一 vv

               段                           建立      的                文                      端发
           报 文      是             次 握手                                                            送  数据
    TCP        1     一   3    三                  连接 过          程 ，TCP 报 4 一    5                   是 客户

## Page 71

                                                                                第 3 章       TCP 协议           详解 +f               53

              应 用 程序数据 长 度                6，      、                   个         ，       同)
                                          为   包 括 回     车    换 行 两 字符           后 及 服务 器 确认 的                            过 程      。
  TCP     报 文段    6 是 客户 端   第     一 次 发    送数据    “12”      的 过    程  。  因 为    服务     器 的    网     线 被 断        开，         所 以 客户
      无法收到     报 文          段       的 确认
              TCP 6                           报文段。           后，                                         6  了
                                                  此 客 户                端对 TCP           报 文             段 执行 5 次 重 传           ，
          是  TCP  报 文 段  7      11，  这 可以 从 每     个    TCP         报 文 段    的    序号 得     。    此
  它们                       一                                                              知             后，    数 据  包 12  ~       23
      是       模块
  部           的                输出 内
          ARP                     容  ， 即  Kongming20 查询            ernest-laptop 的 MAC 地
                                                                                                           址。
             们保留了     tcpdump 输出
          我                               的时间     ，以     便       推 理                                的 超时
                                                                     TCP        重传                    策略 。     察
                                                                                                           观              TCP 报 文 段
  6   一   11 补 发 送的时 间 间 隔 ，它们 分 别                为     0.2s、0.4s、0.8                     和
                                                                                s、1.6s 3.2              s。     由 此 可 见         ，TCP
  一   共 执行     5  次 重 传 ， 每    次   重 传    超时  时 间 都 增加 一           倍  因 此       ，和 TCP      超时 重 连 的 策略 相似                       )。
  在   5   次 重 传 均   失 败 的 情况        下，底 层     的 了 了 和 ARP 开始 接管             ，    直 到 telnet    客户 端 放弃 连接 为                    止。
          Linux   有 两 个重要         内          核参数与    超时
                              的                 TCP              重传相关       :                                                     和
                                                  在底层            IP 接管 之
  /proc/sys/neVipv4/tcp_retries2。                      前 者 指定                   前TCP                                             MR
  认 值        是 3。          后 者 指定 连接 放弃 前   TCP     最 多 可以 执行         的   重 传 次         ，    默 认 值           是15                般对应
                                                                                数                                                (一
                                        中                      ，TCP 超时
                                                                 重传发生了    5 次    ，  连 接 坚持
  13  一   30 min)。  在 我 们     的 实例                                                                   的时间是         15 min         (可
      用   date        命令
  以                  来测量  )。
          虽然 超时 会 导致      TCP      报 文 段 重      传   ， 但   TCP    报 文     的  重 传 可以 发             在 超时 之生           前 ， 即       快速
           传，这 将在   下 一      节 中 讨论  。
  重

  3.10        拥塞 控制

  3.10.1      拥塞 控制概述

        TCP 模块    还有      个 重要 的 任务           ，        就 是 提高    网 络 利用 率       ，降 低             ，   包 率 并        网络资源对
              一                                                                                                                保证
  每 条 数据 流 的 公平        性  。这 就      是 所 谓 的   拥塞    控制     。
        TCP     拥塞  控制的   标准 文档           是 RFC 5$681，     其 中 详细 介绍 了          拥塞        控制     的       四 个 部分      ，       慢 启动
              塞避兔                             avoidance)、        快 速 重    传 〈fast retransmit)              和 快速          恢复 (fast
  (slow start)、   拥
                                                                       如        算法                       算法
  recovery)。     拥赛控制 算法在           Linux     下 有 多 种 实现           ，   比  reno 、vegas                         和          cubic 算法
  等 它
        。        们或 者 部分 或 者 全 部 实现         了 上 述 四 个 部                                                            control
                                                                 分                            。/proc/sys/neUipv4/tcp_congestion 文
  件 指示 机 器 当前 所 使用的 拥塞 控制                     算法  。
        拥塞 控制 的最终 受 控 变 量 是                  发 送 端    向  网  络一     次连续 写        人《〈 收 到 其 中 第            一    个         数据 的 确认 之
              量，     们    称        为SWND (Send Window，             发 送窗口        8)。       过，                                    报
  前) 的 数据     我                                                                                                 不 发 送 端 最 终 以 TCP
  文 段 来 发 送 数据     ，      以                       端                    的                                     。    些             文
              所                            SWND 限定 了 发 送   能     连续 发 送         TCP 报 文                    量              这 TCP 报
                            据部分)          称       为 SMSS (Sender Maximum                        Segment Size，  发  送    者 最    大 段
  段 的最大 长度    〈     仅 指数
  大 小 )，其 值一般 等于          Mss.
                                                                 果 SWND 太
        发 送 端需要 合理 地 选择             SWND 的 大           小 。如                     小，   会 引起 明显                 的 网 络 延     迟  ，   反

SO 这 里 所 说 的 窗口 实际上 是 指 窗口的大 小 ，这 里 只 是 保留了行业 的 习惯 说 法 。

## Page 72

    54 «+    协议 详解


    之 ，  果  大，                   网     。 文 到，
    如 SWND 太 则 容易 导致               络 拥塞 前
                                   提          接 收 方 可 通过 其 接收 通告   窗口
           发送端的    SWND。         但 这 显然
    CRWND)  来 控制                   不够    ，所以 发 送 端 引入了 一 个 称 为拥塞   窗口
(Congestion Window, CWND) 的 状态 变量 。实 际的SWND 值是RWND 和 CWND 中 的 较小
        见，它 是 一 个 闭环 反馈 控制 )。
    者。 图 3-11 显示 了 拥 控 制 的  输入 和 输出  〈 可
                               络  状况 ， 比
                                   如RTT， 是
                                   网     否委  等



                                   RWND
                                 图 3-11    拥塞 控制 的   输入和 输出

    3.10.2             慢 启动和 拥塞 避免

                                   设置     TIW     Cinitial Window)，     其
         TCP    连接 建立好 之     ，CWND 将 被     成初始值                       大     一
                    后                                                 小       为 2      4
    个 SMSS。       但 新            内
        的 Linux 核提 高 了 该 初始 值 ，以 减 小传输 滞后。此 时 发 送 端最多 能 发送TW
 节的 数据。此 后 发 送 每 收 到 接收 端的 个 确认 ，其CWND 就 按照式〈3-1)》 增加
    字                              一                                  ，
                              CWND+=min (N,       SMSS)                              3-1
         JEN        EU HU PY ZT     AR                      这样 一  来      ，CWND 将 按照 指数 形
    式 扩大，这 就是所谓     慢 启动     。慢 启动 算法
                    的              的    理由    是    ，TCP    模块  刚 开始 发   送数 据时     并 不 知道
                    要用     种 试探的 方式平滑 地 增加     CWND            的
    网 络     的   实际 情况 ，需     一                   大小。
         但 是    如     果 不 施加 其他 手段 ，慢 启动必然 使得    CWND     快       〈《    见慢       其实不人慢 )
                                                             很
                                                            膨胀                      可 启动
                    此          TCP 拥塞 控制 定义
    并 最 终 导 致网络 拥塞 。因              了另                 一个          状态重要 的 变量 慢
                                   中                                  :       启     动 门限
                                                           该值时    ，TCP       拥塞 控制 将 进入拥
    (slow start  threshold size，ssthresh)。      当 CWND 的 大小 超过
    塞 避 免 阶段         。
                                   线性    方式增加，                    从 而 减缓
         拥塞 避免          算法  使得CWND 按照            其    扩大 。RFC         5681     中 提 到 了 如
    下 两   种         方式:
                     实现
                    式           3-1 计算 新    的CWND，        而 不论    RTT  该 时间 内 发 送 端收   到
         QC)    每个    RTT 时间 内 按照
                多 少 个      。
                          确认
         OD     每收到 一 个对 新 数据 的确认 报文  ，    就 按照式     3-2   来 更新  CWND.
                    CWND+=SMSS*SMSSICWND                                             3-2
         Pel    3-12     地   述了 慢 启动 和     拥塞 避免 发生的 时 机 和 区 别。 该     图 中，我 们以    SMSS 为
   来显示 CWND《【 实际上 它 是 以 字 节 为 单位的 )，以 次 数 为 单位 来显示 RIT， 这 只 是
    单位                                                                                为了
                    们    假设 当前
                                   的             大
    方便 讨论 问题        。    此 外  我                  小     当
                    ，              ssthresh     是  16SMSS     〈       然，实 际 的 ssthresh 显
    然 远      不  止 这 人么大 )。

## Page 73

 BIB TCP 协议 详解 55


                  CWND
                       SMSS)

              20
              18
              16         ssthresh
              14
              12
              10
               8
               6
               4
               2

                   0       1    2        3        4             5           6  7
                           RTT 【单位    :
                             图 3-12    tJ     Ee fe,
     拥
以 上 我 们讨论了 发送 在未检测 到 拥塞时所 采用 的 积极 避免 拥塞 的 方法 。接 下 来 介绍
 塞 发 生时 《可 能发生 在 慢 启 动阶段 或 者拥塞 避免 阶段)     拥塞 控制 的 行为 。不 过 我                                们 先 要 搞 清
 楚 发 送    是如 何 判断 拥塞 已 经 发 生             发 送 端 判断 拥塞 发     生        的  依据   有 如 下 两  个 :
  口     传输 超时  ，  或 者 说  TCP       重 传 定时 器 溢出 。
  品 接收 到 重复        的   确认 报 文 段  。
  拥塞 控制对 这 两 种 情况有 不 同 的处理 方式     。     对 第     一     种情况 仍然合用 慢 启动和拥塞 避免                          。
                                                                                                将在后面
 对 第 二 种 情况 则 使 用                                           快速 重 传和快速恢复 〈 如 果 是 真的 发生拥 塞的 话 )，这 种 情况
 讨论。     注意， 第 二 种 情况如 果 发 生 在 重 传 定时 器 溢出 之               后    ，   则 也 被      拥塞 控制  当成     第一种  情况

  如 果 发 送    端 检测 到 拥塞 发    生    是由  于 传输    超时 ， 即    上 述      第    一种     情况 ，  那 么 它 将 执行 重   传 并
      如下调整，
 做
                                       ssthresh=max(FlightSize/2，2*SMSS)                         3-3
                           CWMD<=SMSS
      中  FlightSize  是   已经     发 送但未 收 到 确认 的 字 节              。             这 样 调整
  其                                                                                         之后 ，CWMD
                                                           数                                   将小于
 SMSS，    那么     也 必然  小 于 新 的    慢 启 动 门限值   ssthresh                 (因为根据式 (3-3)，   它 一   定 不 小 于
      )，故          而拥塞
 SMSS 的 2 倍              控制再        次进   人 慢 启 动阶段 。
                              重传和快速恢复
 3.10.3                           ”快速
                   下，  发 送 端 都    可 能 接收 到 重复 的 确认 报 文              ，比如        TCP 报 文 段 丢        ，或
  在    很 多 情况                                                       段                              者  失
   端收到乱序       TCP     报 文段    并 重 排 之 等 。拥     控 制   算法   需要               当 收 到 重复 的      确认 报 文 段
 接收
 时，网    络是    否 真 的 发  生 了 拥塞    ，或 者说TCP     报 文 段   是否           真 的  朱   。  具 体 做 法   是 :
 果 连 续 收 到 3     个 重复  的 确认 报 文     ，     就 认为 是拥塞   发 生了  。        然   后它启    用 快速 重 传 和       快速恢复

## Page 74

    56  «+ 第 一 篇 TCPIIP  协议 详解

        来处理拥塞 ，     过
    算法               程如下:
         1 当 收 到 第  3    A     CR,   3-3 计算     然 后                    重传于
                        ssthresh，                                       立即
    失 的 报 文  段 ，并 按照    式3-4 设置 CWND。
                        CWND=ssthresh+3*SMSS                           3-4
               次收到  1    个 重复
         2       每      的   确认 时，
                        设 置 CWND=CWND+SMSS。     此 时 发 送 端 可以 发    送      新
    的 TCP  报 文 段〈 如 果 新  的CWND 人 的话)。
                        允许
                        置    CWND=ssthresh (ssthresh 是 新 的 人 启 动 门限 值 ，由 第
        3 当 收 到 新 数据 的   确认时，   设
            得到)。
    一   步     计算
   快速 重 传和快速恢复 完成 之 后 ，拥 塞 控制将恢复 到 拥塞避免 阶段 ，这 一 点 由 第 3 步操作
可 得 知 。

## Page 75

  第 4 章 TCP/P  :
      通信 案例     访问
      的      器
  Internet 上   Web 服务 |


         第 竟
     在                讨论 TCP/IP 协议 的                              和 部 分 协议 ，以             及 它们
           1      中，我 们 简单 地      了        族各层            功能                                之间
  是 如 何 协作 完成网络 通信     的 。    第      中，                   地         了        和               。
      在 2 章 和 第 3 章 我 们 详细
      探讨 IP 协议 TCP 协议
章，我们 分 析 个 完整的TCP/P 通信 的 实例 一访问 Internet 上
  本                                                                 的    Web 服务
                  一        一                                                      器  ， 通    过该
  实例 把 这 些 知 识 串 联 起     来 。选 择 使用    Web 服务 器    展开   讨论 的 理由 是  ;
                  的    Web 服务器随处 都     可以  获得     ，我 们  通过 浏览     器访问 任何 一 个      网    站 都是  在
         Internet 上
         与 Web 服务 器 通信     。
                                            器      程序 ， 因 此 先 学 习 其 工作   原理 是有    好 处 的  。
     口 本 书后 续 章 节 将 编写简单的    Web                   服务
                                                                  的内容相   当 广      ，          涵
     Web                 客户 端 和 服务 器 之间 使用  协议    通信      协议
                      HTTP        。HTTP                               泛                     了网
                      此，    学
  络应用 层 协 议 需 要 考虑 的 诸多 方面 。因      习                       HTTP 协议 对 应 用 层 协议 设计 将 大 有 神 益 。

  41     实例 总 图

     我 们 按照    如  下 方法 来 部 署  通信 实例 ;在     Kongming20   上 运行                客户
                                                              weet     端    程序    ，  在 ernest-
                                                                      取     Internet 上 的    主机
  laptop 上 运行 squid    代理服务器 程序 。客 户 端通过 代理 服务 器           的  中   转 ，获
  www.baidu.com   的 首页 文档 index.html,     4078 4-1 所 示 。

  Kongming20          答  ernest-laptop                  请求/应答         www. baidu.com
                      应




                  由器
                  路





     接口               接口 |  | 接口      接口               | |    SOR  接口


                                                       的Web 服务
                  疼 4-1 通过 代理 服务 器 访问 Internet 上                  器

## Page 76

    58 %—& TCP/IP 协议 详解


           FFA          可
                 4-1    见 ，wget     客户 端 程序       和 代理 服务 器 之间     ，以         及          服务   与                    器之
        代理 器 Web 服务
        是一种应用 层 协议 ，它 默认 使 用 的 传输层 协 议 是
间 都 是 使用HTTP 协议 通信的 。HTTP 协议
    TCP              我们 将 在 后 文中 简单 讨论          HTTP      。
                                                         协议
           为 了 将 ernest-laptop     设置    为  Kongming20 的 HTTP     代理 服务           ，            我 们 需要
                                                                                  器             在          Kongming20
上 设置环境变量 http_proxy:
   9 export http_proxy="ernest-laptop:3128" # 在 Kongming20 上 执行
                中    ，3128 是 squid  服务 器    默认  使   用 的      号                                                     监听
           其                                      端口     〈     可以 通过          lsof                       命令 查看 服务 器程序
                        第17 章 )。    设 置   好 环境 变量 之     ，Kongming20           访问 任何                    上
    的 端口 ，见
                号                                 后                                          Internet        的  Web 服
Fy eet, HTTP 请 求 都将首先 发 送至ernest-laptop 的 3128 端口 。
                         器    接收    到         客户 端的      请 求                 ，
        squid 代理 服务                     weet      HTTP     之                  将 简单         地                  这个请求， 后
                                                                           后                                     修改 然
    把 它 发 送给最 终 的 目标 Web                      器。      服务                      是                        器，          预见
                                    服务 既 然 代理     器 访问 的                           Internet   上 的 机           可以
                        数据 报 都 将    过 路 由 器
   IP                                           的 中 转， 这 一    点 也     在图
                      经                           体现                          4-1      中  了  。

   ”部署
    42                  代理服务 器

                                                  )，                          所以 先         简单      介绍    一 下  HTTP ft
           由 于 通信 实例 中 使用了 HTTP 代理 服务器            〈squid 程序
             服务 器    的  工作 原理 ，以    及 如 何 部   署 squid  代理服务 器     ，
    理

    4.2.1      HTTP 代理 服务 器 的                   工作  原理
        在               通信
             HTTP        链上     ，   客 户   端 和 目标 服务 器 之 间 通 常 存在              某些   中   转代理 服务          器   ，    它们 提供
        个HTTP 请 求可 能被多 个 代理 服务 器 转发 ，后 面的服务 器 称为 前
对 目标 资源 的 中 转 访问 。 一
    面 服务 器       的   上  游 服务  。    代 理 服 务 器         按照其 使 用  方式  和 作用     ，  分 为 正        向  代理 服务            、   向代
                         器                                                                                   器      反
                         器。
    理 服 务    器 和 透明 代理 服务
                                        代理 客户 端 自己 设置代理 服务 器     地 址      客 的 每 次 请 求 都                        直接 发 到
              正 疝       要求                        的     。                     户                    将             送
    该 代理 服务             器 ，并 由 代理服务 器 来 请 求       目标     资源   。比   如  处    于  防火墙        内   的 局 域 网         机 器 要 访问
                      或 者 要 访问 一 些 被屏    项 掉  的 国外 网站    ，就       需要  使 用  正 向代理 服务 器         。
        反问 代理 则 被设置      在          服务    器   端 ，因 而 客户 端 无    须   进  行    任何     设置      。            反 向代理 是 指 用 代理
    服务 器 来 接收 Internet     上 的 连接 请             求 ，然后  将 请 求     转 发给 内 部 网       络    上 的   服务 器  ，        并 将 从 内 部
    服务器上 得 到 的结果 返回 给                       端。      下，                                          一          真实
                                        客户        这 种 情况 代 理服务                器 对  外 就 表现 为        个             的 服务
         。各  大 网 站 通常 分 区 域           设 置 了 多 个代理服务     ，所以 在 不 同             的    地   方 ping                  个域名可能得
                                                  器                                                                同一
                                                                                       址。                    图 4-2 显示
    到 不 同 的    UP det,     AA          IP   地 址 实际 上 是 代理 服务 器    的           地                                 了正向代理
           器和反向          代理 服务 器 在  HTTP      通信 链 上 的 逻辑 位 置     。
    服务
          图  4-2     中 ，   正 向代理 服务 器 和 客户 端 主机     处于     同一 个 逻辑 网 络            中     。    该 还 辑 网    络    可以 是   一
    个 本 地    LAN，        也 可以 是   一 个    更大 的 网     。  反向 代理 服务 器    和        真正    的 Web                      器也位于同一
                                                  络                                                                服务
    个 多      网 络         中，这 通常 由 提供 网站 的 公司 来 配置          和  管理   。

## Page 77

第4章 TCP/IP 通信 案例 ;访问 Internet 上 的 Web 服务器 59


    1 HOA eg



    向代理
      反





                                                    通信 上 的 代理 服务 器
                                    图 4-2   HTTP      链
          透明 代理 只 能 设置 在 网          关上 。    用 户     访问 Internet     的 数据   报 必然 都 经    过网     关，如 果 在 网
    关上    设置 代理 ，则 该 代理 对 用 户 来说显然 是 透明             的              。透 明代   理 可以 看 作 正 向 代理 的 一    种   特
    殊 情 况   。
          代理 服务 器 通常还提供 缓存 目标资源 的 功能(可                               选 )，这 样 用 户        下 次访问 同一 资源 时 速
    度 将 很     。优     秀的
              开源
              快        软件           squid.  varnish   都是提供 了 缓存 能 力 的 代理 服务 器 软件                   ，其 中
            支持
    squid     所有代理方式     ，    而 varnish     仅   能    用 作 反 向 代理     。

    4.2.2     ”部署 squid              代理 服务 器

                    我们在             上 部    署                          代理 服务 。这 个 过 程 很 简单 ，只 需 修改 squid
          现在     ernest-laptop            squid                     器          IR
                     文件    在 其 中                    加     入      如  下 两 行 代码    〈 需
    务 器     的配置                                                                要     root 权限   ，且 应 该 加
             的位置     ， 详 可 参考
    在 合适                情          其他类似条        目的 设置
          acl localnet sre  192.168.1.0/24
          http_access allow localnet
          这 两 行 代码 的 含义     是      ，人 允许 网     络 192.168.1.0     上 的    所        有 机 器 通过 该代理 服务 器 来 访问
              器。          中  ,“192.168.1.0/24”  是 CIDR (Classless Inter-Domain Routing，          无 类 域间
    Web   服务              其
        由)      的               方法                                                   址  ”后
    路 风格 IP 地 址 表示                      :“/”    前  的  部 分           网 络  的        IP 地      的 部分 则 指定 子
                                          言       上 述 表示
    网 掩 码     中    “1” 的 位  。对  IPv4 而              等价于                  “192.168.1.0/255.255.255.0”(IP
              数                                 ，                                                      地
    址   /7子  网掩码 )。
                                                                                        应用中   ，squid 提供
          我 们     通过   上 面 的 两 行代码 简单 地 配置          了 squid     的 访问 控制    。但            实际            更
        、     的     如                       验证
    多 更安全                                   等。
              配置 ，比     用 户
                                          如下命令 ，    以 重 squid        IRF th:
          接 下 来在emmest-laptop                    上 执行

          §  sudo  service  squid3 restart
          *Restarting     Squid HTTP  Proxy   3.0    squid3                     [OK]
          service  是             个脚本
              一      程序               C/usr/sbin/service),           /ete/init.d/ 目录    下 的众多    服务 器 程序
              和                     mysqld 等    )    的       启动      停    止  〈stop)     和 重启 Crestart) 等
    《比如     httpd. vsftpd.  sshd                    (start)、                                           动
                                      在  ，Linux     程序
    作 提 供 了     个 统一 的 管理     。    现                          员  已 经越来 越   加              使 用 service 脚本
              一                                                                                     来管理服
                 了。
             务 器 程序

## Page 78

 “* 篇
 60 第 一 TCPAP 协议 详解


                 用                                      输     包
 4.3       ”使                tcpdump    抓   取       传    数据

           执行                  前   ，                应删除                                    中
      在            wget    命令 我                      们 首先                                      路  由
                                                        emest-laptop 的 ARP      高 速 缓存           器 对 应 的 项     ，
     便     察                    中                       何    时起   。     后       用                抓          取整个通信
 以
      观               通信 过 程                                    协议
                  TCPAP          ARP                            作用    然 ， 使                            tcpdump 命令
                            。    的操作                            清单
 过 程 中 传输 的 数据              包    完 整             过 程 如 代码     4-1     所 示 。
                                代码               清单      用               取网页
4-1                                                     使     wget 抓
 Oe                                                     een
 $         sudo  arp     -d 192.168.1.1
 $          sudo tcpdump    -s 2000     -i ethO     -ntX "(src 192.168.1.108 or {dst   192.168.1.108 or    (arp)'
 9         wget  --header="Connection:              close"    http://www.baidu.com/index.html
  ~~2012-07-03 00:51:12-- http://www.baidu.com/index.html
  Resolving ernest-laptop... 192.168.1.108
      Connecting      to                                                     connected.
      Proxy      request    sent,   awaiting        response...   200 OK
      Length:    8024      (7.8K)
      Saving     to:     “index.html”
      100%                                              --.-K/s                 in   0.001ls
  2012-07-03 00:51:12 8.76 MB/s) - “index.html” saved 8024/8024

                      的                                 送                                        ，
                          命令
      weet                输出   显示     ，HTTP         请 求 确实 是    先 被    至代理服务 器 的         3128 端口     并 且 代理
                  返回
 服务 器 正确 地            了 文件      index.html          的 内 容    。
                  的完整                                   清单
      这 次 通信                  trpdump     输出 内 容 如 代码     4-2           所 示  。
                                代码 清单4-2            ”访问 Internet    上 的 Web         器
                                                                             服务
      lL.   IP   192.168.1.109.40988          >     192.168.1.108.3128:  Flags  [S],   seq   227192137,
           length     0
      2.   IP    192,168.1.108.3128        >     192.168.1.109.40988:    Flags   [S.],   seq   1084588508,   ack
     227192138, length 0
 3. IP 192.168.1.109.40988 > 192,168.1.108.3128: Flags [(.], ack 1, length 0
      4.   IP    192.168.1.109.40988          >     192.168.1.108.3128:  Flags   [P.],   seq   1:137, ack   l,
           length     136
           IP    192.168.1.108.3128        >     192.168.1.109.40988:    Flags  [.],   ack   137, length    0
 6. ARP, Request who-has 192.168.1.1 tell 192.168.1.108, length 28
      7.   ARP,  Reply          192.168.1.1   is-at     14:e6:e4:93:5b:78,    length   46
      8.   IP    192.168.1.108.46149          >     219.239.26.42.53:   59410+  A?   www.baidu.com.     31
 9. IP 219.239.26.42.53 > 192.168.1.108.46149: 59410 3/4/0 CNAME www.a.shifen.
           com.,  A          119.75.218.77,   A     119.75.217.56    162
      10.  IP    192.168.1.108.34538             >   119.75.218.77.80:   Flags  [S],   seq  1084002207,
              length     0
      ll.  IP    119.75.218.77.80         >   192.168.1.108.34538:       Flags         seq   4261071806,    ack
              1064002208,      length     0
      12.  IP    192.168.1.108.34538             >   119.75.218.77.80:   Flags  [.],   ack  1, length 0
      13.  IP    192.168.1.108.34538             >   119.75.218.77.80:   Flags  [P.],  seq   1:226,   ack  1,
              length     225
      14.  IP    119.75.218.77.80         >   192.168.1.108.34538:       Flags  [.],   ack  226,  length   0
      1S.  IP    119.75.218.77.80         >   192,168.1.108.34538:       Flags  [P.],  seq   1:380,   ack  226,
              length   379

## Page 79

， 问    器
第 4 章 TCP/IP 通信 案例 访 Internet 上 的 Web 服务 61


     16.   IP  192.168.1.108.34538         >      119.75.218.77.80:  Flags  [.]，ack      380,  length  0
     17.   IP  119.75.218.77.80  >   192.168.1.108.34538:            Flags  [.],   seq   380:1820,   ack
           226, length 1440
     18.   IP  192.168.1.108.34538         >      119.75.218.77.80:  Flags  [.],   ack   1820,   length   0
     19.   IP  119.75.218.77.80  >   192.168.1.108.34538:            Flags  [.],   seq   1820:3260,     ack
           226, length 1440
     20.   IP  192.168.1.108.34538         >      119.75.218.77.80;  Flags  [.],   ack   3260,   length   0
     21.   IP  119.75.218.77.80  >   192.168.1.108.34538:            Flags  [P.],    seq  3260:4700,    ack
           226, length 1440
     22.   IP  192.168.1.108.34538         >      119.75.218.77.80:  Flags  [.],   ack   4700,   length   0
     23.   IP  192.168.1.108.3128    >          192.168.1.109.40988:  Flags           seq  1:1449,   ack
           137, length 1448
     24.   IP  192.168.1.108.3128    >          192.168.1.109.40988:  Flags    [P.],   seq    1449:2166,    ack
           137, length 717
     25.   IP  192.168.1.108.3128    >          192.168.1.109.40988:  Flags    (.],   seq  2166:3614,    ack
           137, length 1448
     26.   IP  119.75.218.77.60  >   192.168.1.108.34538:            Flags  [.],   seq   4700:6140,     ack
           226, length 1440
     27.   IP  192,168.1.108.34538         >      119.75.218.77.80:  Flags  [.],   ack   6140,   length   0
     28.   IP  119.75.218.77.80  >   192.168.1.108.34538:            Flags  [.],   seq   6140:7580,     ack
           226, length 1440
     29,   IP  192.168.1.108.34538         >      119.75.218.77.80:  Flags  (.],   ack   7580,   length   0
     30.   IP  119.75.218.77.80  >   192.168.1.108.34538:            Flags  [FP.],    seq  7580:8404,    ack
           226, length 824
     31.   IP  192.168.1.108.34538         >      119.75.218.77.80:  Flags  [F.],    seq  226,  ack  8405,
           length 0
     32.   IP  192.168.1.109.40988         >     192.168.1.108.3128:  Flags    [.],   ack  1449,     length  0
     33.   IP  192.168.1.108.3128    >          192.168.1.109.40988:  Flags    [.],   seq  3614:6510,    ack
           137, length 2896
     34.   IP  192.168.1.109.40988         >     192.168.1.108.3128:  Flags    [.],   ack  2166,     length  0
     35.   IP  192.168.1.108.3128    >          192.168.1.109.40988:  Flags    [.),   seq  6510:7958,    ack
           137, length 1448
     36.   IP  192.168.1.108.3128    >          192.168.1.109.40988:  Flags    [FP.],    seq  7958:8523,
           ack  137, length    565
     37.   IP  192.168.1.109.40988         >     192.168.1.108.3128:  Flags    [.],   ack  3614,     length  0
     38.   IP  192.168.1.109.40988         >     192.168.1.108.3128:  Flags    [.],   ack  5062,     length  0
     39.   IP  192.168.1.109.40988         >     192.168.1.108.3128:  Flags    [.],   ack  6510,     Length  0
     40.   IP  192.168.1.109.40988         >     192.168.1.108.3128:  Flags    [.],   ack  7958,     length  0
     41.   IP  119.75.218.77.80  >   192.168.1.108.34538:            Flags  [.],  ack    227,  length  0
     42.   IP  192.168.1.109.40988         >     192.168,1,108.3128:  Flags    [F.],   seq   137, ack   8524,
           length 0
     43.   IP  192,168.1.108.3128    >          192.168.1.109.40988:  Flags    [.],   ack  138,   length    0

                                      包                               ，                                     端
     我 们   一   共 抓 取 了 43          个 数据       与 前 面 章节 的 讨论 不             这 些 数 据 包 不        是     对 客户
                                              。                       同                         一             和
                  的       ，                        和        器               端                  ，              服
    服务 器 之 间 交 换     内  容     而 是 两 对 客户          端 服务                客户 和 代理 服务               器         以 及 代理
    器和                  器)                              。     以                        把
，   务            目标 Web 服务            之间 通 信 的 全 部 内 容      所     ，tcpdump   的 输出                这 两 组 通信 的 内 容
     在一起。                  的
                                     方便，                这                         其
    交织          但 为 了讨论问题                         我 将       个 数据
                                                   们        43            包 按照     逻辑 关系 分 为         如 下 4 个
    分
    :
    部
               服务    访问                    器      域名                                          址  ，
     口 代理         器       DNS  服务               以 查询    www.baidu.com       对 应 的     IP 地     包 括 数 据 包

## Page 80

    62 «+ 第 一篇 TCP/IP 协议 详解


  8、9。
口 代理 服务 器 查询 路 由 器 MAC 地 址的ARP 请 求和应 ，包 括 包
        答 数据
    中  wget 客户                   和 代理 服务器       〈192.168.1.108) 之    的      ，
                                                                  间 HTTP 通信 包
       括 数据  包 1~5. 23~25,.  32~40. 42 和 43。
                             器〈119.75.218.77)   之 间 的 HTTP 通信   ，    包 括 名
       代理服务器    和 Web 服务                                                数据 10~22.
       26~31 和 41 。
    下 面 我们 将 依次 讨论  前 3      个 部    ，第 4 个 部   分与   第 个 部 分     内        ，   再
                                 分                  3        的      容 基本 相似 不


                              器
    4.4 访问    DNS            服务

       包    8、9 表示 代理 服务       器  向                服务
        数据                                  DNS    器 219.239.26.42, TE         DNS 服
    器的      址，      )            域名                        址，       。              回
    务   IP 地 见    1.6.2 节 查询      www.baidu.com    对 应 的 IP 地   并 竺 到 了 回复         该
    复 包括 一 个 主机别名                和 两   个        地
                                 IP                址〈119.75.218.77 和 119.75.217.56).
    代理 器        完整               如
      服务
               执行 DNS 查询 的     过  程 图 4-3 所 示 。

        读 取   /   lveonf， 获        uid代         理

                               219.239.26.42


      UDP 模块 将 目标            号S3和源
                             端口
       口号46149 加 入 UDP       数据 报头 部


       址                     服务  oe             219,239.26.42   ane
       中 模块 将 地 和 DNS        器 的
       IPS                                      192.168.1.1        .


                                             程序   地 址   加      入 以 太 网帧头部
           ARP广 ，   询
           播        查
                  的MAC地址
       1     192.169.1.1

                             图 4-3     DNS 查询

                                                           地 〈 见     1.6.2 节       然
       squid 程序通过 读 取 /ete/resolv.conf 文件获得  DNS 服务 器 的 IP 址        )，             后
       权传递给    内 核 中的UDP 模块 。UDP 模块
                                             将DNS 查询
                                                           封装    报文成UDP 数据
                                                                    报，             同
    将 控制                                                                           时
    把 源 端口号和 目标 端口 号 加入      数据 报头           ，
           UDP                   部           然   后         调用UDP 模块 也 ARS. IP 模块 则 将

## Page 81

% 4H TCP/IP 通信 案例， 问    器
    访 Internet     上    的 Web 服务 63


           数据 成 数据 ， 并 把 源                                      端 地址 192.168.1.108              和      DNS      服务 器 的         IP 地
UDP                       报封装              报                                                                                      址
   和人      IP           数据 报头        。接 下     ，PP     模块查询 路 由 表 以 决定如何 发 送该                           IP 数据
加                            部             来                                                                     报。       根 据 路 由 策
                            址                            仅 能    匹配 路 由 表 中 的             默认 路   由   项  ，因 此 该      耳      数 据 报 先 被
Be,                IP 地
发    送至路           由 器               地 址为        然后通过 路 由 器 来 转发 。因
                          CIP                  192.168.1.1)，                                           为  ermest-laptop       的 ARP
               中     与路                    缓存项 我 手动 将 其 删除了                                     所
                                                                                                以                                需要
缓存                                                                                                                              发起一
                  没有        由        器 对 应 的    〈      们                                  )，        ernest-laptop
               以查询路由                   器
                             的             IP 地
                                                          址，    而 这 正 是 数据
                                                                           包          6 描述
                                                                                                的内容。路
个 ARP 广播                                                                                                        由      器 则 通过 ARP 应
                             的                                                                                            终      ，以
答 告            诉ernest-laptop        自己     MAC 地 址    是        14:e6:e4:93:S$b:78，       如 数据 包          7     所 示。 最            太
网 驱动 程序               将                报 封装 成以太 网 是 发送给 路 由 上 器。此后，                             代理 服务            再 次        发送数 据 到
                                                           为               的                                   高 速 缓存中 已经记录 了 路 由 器
Internet              时 将 不 再 需要       ARP           查询 ，因     ernest-laptop     ARP                                              的
               址                       映射
IP   地 和 MAC 地 址 的                                  关系。
           需要 指出 的                   ，虽     然   据     报 是     先 发 送 到    路 由             ， 再 由 它 转发
                                是                                                器                     给目标 主机，但        其          头 是
     的     目标 IP     地 址却是最终 的 目标主机                                    服务
                                                                                      器) 的 中    地
部                                                                                               址，而 不 是 中 转路由          的        TIP
          址    〔〈192.168.1.1)。       这 说      头 部 的 源             端      地 址   和      目的
地                                          明    ，IP                IP                     端     地址 在 转发 过 程               中  是 始终 不
          的                 是源路由                                部      源端物                和         端物理            在 转发过          中
变              种 例外 选择 )。 但                头                      的            理      地 址 目的                    地 址    程
则 是        一         直 在 变化 的   。

45             ”本 地 名 称 查询
                         ，                 来访问 Internet
                                                                上的某台主机时，和需要                         用                            服务
           一    般 来     说   通 过域名                                                                      使 DNS                 来获取该主机
的    IP        地 址 。但    如  果 我 们 通过 主机 名 来 访问 本 地 局 域 网                         上      的 机 器   ，则  可 通过 本 地 的                 文件 来
               该机器的    IP 地
获得                                   址。
                                                                             在 /etc/hosts       配置 文件           中      。当 需要 查询 某 个
           Linux 将 目标        主机 名 及 其 对 应 的                             地 址 存储
           名对     的                    时，     序将首先 检查              个 文件                                             文件
主机应            了            地址 程                                这              。Kongming20 上 /etc/hosts                    的内 容   如
下 【笔者手动修改 过                          ):
           127.0.0.1                   localhost
           192.168.1.109               Kongming20
           192.168.1.108
           其 中 第          项 指出 本 地 回路
               一                           地址     127.0.0.1        的 名   称 是 localhost，                        第 二项和 第 三 项 则 分 别 描述
                                                                                地 应 的 主机 。
了 Kongming20 和 ernest-laptop 的 IP          址及对                                         名
           代码     清单     4-1     中     ，wget    命令 输出     “Resolving ernest-laptop...           192.168.1.108”，           即 它   成功地
           了    主机    名  emest-laptop      对 应     的     IP 地
                                           址，原 因 如
解析                                                                         下            当  weet 访问 某个 Web 服务
                                                                                 :                                             器时，它
               变量           http_proxy。        如 果 该       环境   变量 被 设置               ， 并 且 我们      没有阻止                     使 用 代理
先 读 取 环境                                                                                                          weet            服
务    ，     则    weet    将 通过         http_proxy 指定    的    代理 服务 器 来                  访问 Web    服务 。但 http_proxy           环境
                                           此                          将 首先
中 包含 主机 名 emest-laptop，                    因     weet                  读取 /etc/hosts      配置 文件           ， 试     图通过 它 来      解析 主
      名                                    址。                                   所示，
机          ernest-laptop     对 应       的 IP     地 其 结果 正如              wget 的 输出 解 析 成                          功 。
                                           中未                                        找到 目标
                                                                       机器名对应的  IP 地
           如 果 程 序       在  /etcrhosts     文件                                                   址，     它 将 求助       于    DNS  ARG.

## Page 82

64   + ”第     一    篇       协议 详解

          户 可 以     通过 修改     /etc/host.conf 文件 来自定义 系统解析     主机 名 的       和    〈(
     用                                                                       方法
                                                                             顺序    一 般 是 先
           本地文件     /etc/hosts， 再   访问DNS 服务 )，Kongming20 上 的 该     文件  内 容 如 下
             访问

     order
     multi   on
     其 中 第          行  表 示 优 先      使用        来解析主机名                        后      AR
               一                        文件                    〈hosts)，  失 败   再 使用   DNS
务 《bind)。      第    二  行 表 示    如 果 /etchhosts 文件中 一 个 主机 名 对 应 多 个 IP  地  ，  那么       的结果
                                        址                                               解析
                       址                       。/etc/host.conf
就 包含 多 个 耳 地                                       文件通常仅包含这两行， 但 它 支 持更多   选项  ，具      使用请
     其      手册                                                                           体
参考   man  。
     标准 文档         RFC 1123 指出       ，    网络上 的 主机 都 应 该实现   一 个 简单 的 本 地 名 称 查询 服务  。

4.6     HTTP 通信

     为 了 方便 讨论 ，我          们    将    wget  客户  端和 代理 服务 器 之间 的 通信 过 程 画       成图4-4  所 示 的
TCP 时 序 图     。

         192.168,1.109.40988                                 192.168.1.108.3128
                                        seq    227192137 (SYN)

                       er                                                  ，
                                        ees                                .
                                                                           5
                                                                           2
                                                                           2




7

                                                             35
                                                             5

5                      -nu
38
x

42

                                                             43

                                    weet  squid AR 28— TCP 通信

## Page 83

    第 4 章 TCP/IP 通信 案例 Internet 的 器 ee
        上 Web 服务     65


       应该注意的是 ，TCP 连接 从 建立 到 关闭 的 过 程 ，客 户 端 仅给服务器 发送了 一
         首先                                                                中                             个
                                                                    字 见代码 清单     4-2     中      TCP  报 文 段
    HTTP HK CBN TCP     报 文 段 4)，         该 请 求 的 长          度  为136       节
        器则用6 个 TCP 报 文段(23、24、25、33、35 和 36 给 客户
    4 的 length   值 )。代 理 服务                                                                             端返
    回 了 总 长 度为       字                                                     确认
        算得 考
        8$22 节 〈 这 可以 从 对 方 的最后 一 个确认 报 文 有 42 的 值计 到， 虚
    同步   报 文 段   和结束     报 文        段               各 占用
                                         一个           序号 ) 的    HTTP       应
                                                                           答。   客 户 端
                                                                           使用
                                                                           了     7     个        TCP  报 文 段
    (32、34、37、38、39、40 和 42                        来确认 这        字节
                                                         8522       的  HTTP 应 答 数 据     。
                                                                 求和        8522 字 节 的 HTTP 应
         下 面 我 们 简单            分析  一  下 这 136 字 节 的      HTTP   请          答                      的 部 分 主要
    WA COPA                    的      选项
                 tcpdump          -X     来查看   )。

                               请
    4.6.1     HTTP              求
                                      如下:
         HTTP    请 求    的 部   分   内 容
         GET                                                HTTP/1.0
         User-Agent:     Wget/1.12     (linux-gnu)
         Host:   www.baidu.com
         Connection:        close
         第     1 行 是    请 求   行  。  其 中“GET” 是 请 求 方法           ，表 示 客 户     端   以只 读 的 方式 来申请    资源 。   常
    的            请 求 方法         有   种 ， 如
    见                                                      所
         HTTP                    9      表     4-1         示。
                                              表    4-1   HTTP   请 求  方法
                 请 求    方法                                                 =     x
                 GET                          GARI              ASRAR SS BR
                 HEAD                                        类似， 不 过 仅 要 求    服务 器 返回 头 部信息，而 不 需要   传输 任
                                              和 GET 方法
                                         何 实际 内 容
                 posT                         客户          服务    交 数 据 的 方法     。这 种 方法 会 影响服务   器:服务器 可 能
                                         根据      收 到 的   数据 动态  创建   新 的     资产，也 可 能 更 新原有 的 资源
                 PUT                          上 传 某 个 资源
                                                        某个资源
                 DELETE                       删除
                                                 求                         的     容，             查看
                 TRACE                        要 目 标 服务 器 返回 原始         HTTP 请 求 内 它 可 用 来       中 间 服 务 器
                                         《比如 代理 服务 器 )       对   HTTP  请 求 的    影响
                 OPTIONS                      查看 服务 器 对 某 个     特定  URL                          ELURL 设置
                                         为    *，     从 而 获得 服务 器 支持的       所有 请 求方法
                 CONNECT                      用     某些代理
                                                             服务
                                                 于              器 ，它 们 能 把 请 求 的连接 转化 为 一个安全        道
                 PATCH                        对 某 个 资源 做 部 分 修改

        中 ，HEAD、GET、OPTIONS 和 TRACE 被 视 为安全 的 方法 ，因 为它们
         这 些 方法                                                                                        只是从
服务 器 获得 资源或信息，而 不 对 服务 器 进行任何修改 。
        而 POST、PUT、DELETE 和 PATCH
则 影响服务器上 的 资源 。
         另     一 方面 ，GET、HEAD、OPTIONS、TRACE、PUT 和 DELETE                   等     请 求    方法被           认为 是
    等        〈idempotent)，        即 多 次          的、              和         具有
        连续 重 复 的 请 求 只 发 送 一 次 该 请 求 完全 相同 的 效

## Page 84

    66  “*    第 一   篇 TCPIIP    协议 详解

        则不同，连 续 多 次 发 送 同 样一个 请求可 能 进一步 影响服务 器上 的 资源 。
Fe. tii POST 方法
                一提的是     ，Linux  上 提供 了 几 个 命令 HEAD.               GET 和 POST。                   其含义    基本 与     HTTP
        值得
        中  的 同名 请求方法 相同          。   它们 适合 用 来  测试    Web 服务
    协议                                                                         器。
                       其 “http” 是
                                               是 目标 资源的URL。                    中                    所       的
                                                                                                        谓
                                       应    用 层 协议   。其 他          常见 的 scheme 还
    表示 获取 目标 资源 需要 使 用 的    有                                                                      fp、rtsp Al file 等。
    “www.baidu.com”   指 定 资源所 在        的    目标 主机     。“index.html”     指 定 资 源 文件               的 名       称 ，这 里 指的
    是 服务 大 根目录                    点的根目录， 而 不 是     服务 器 的 文件系统 根 目录                              中 的 索引 文件    。
                                 端        程序)      使 用             的
        “HTTP/1.0”    表 示 客 户                      的 HTTP          版本号是           1.0。           目 前       的 主流  HTTP
       本是
    版      1.1。
                                                                                 个                         请 求 可以 包含
        HTTP      请求内 容 中 的 第    2~4   行 都     是HTTP 请 求    的  头 部 字段          。一     HTTP                         多
       头部字段 。       个 头 部字段 用 一行 表示，包              段 名             、冒
    个                                                                         号、  室 格 和 字段
                    一                                              称                                的值      。HTTP  请
求 中 的 头 部 字段 可按任意 顺序 排列 。
   “User-Agent: Wget1.12 (linux-gnu)” 表 示 客户 端 使用的 程序是weet.
        示目标 主机
        “Host: www.baidu.com” 表                    名是    www.baidu.com。HTTP                  协议 规定                 请
                                                                                                            HTTP    求
    中必须 包含 的 头      就是
                    部字段 目标 主机 名           。
                                                   时传人的            见代码 清单                                      用以 告诉
        “Connection:     close”  是 我们 执行    weet    命令                                                            服务
    器 处    理 完 这  个 HTTP    请 求之后就关闭 连接。           旧                                                 端和
                                                   在 的     HTTP     协议 中         ，Web            客户         Web   服务
       之间的 一个       TCP 连接
    器               只能为一个                   HTTP     请 求 服务 。当 处 理 完 客 户          的              个            HTTP 请
                                                                                            一                   求之后，
    Web 服务 器就《主动) 将              TCP     连接 关闭 了    。 上     同一 客户 如 果 要 再                  发     送 一个  HTTP     请求 的
    话 ，必 须 与 服务 器 建立             个 新  的 TCP 连接 也 就 是               ，同         个 客户 的 多 个 连续             的     HTTP 请
                    一                              。           说        一                                          求
    不 能 共用 同一       个     连接 ，这 称         短 连接     。                     长 连接
                    TCP                为           与之                      相反 ，是 指 多 个 请 求 可以 使             用   同一 个
    TCP 连接      。 长 连接 在 编程 上 稍微       复杂   一      些 ，  但 性能上  却 有 很          大提高 :        它 极 大 地      减少    了 网络 上
                                                                                                        求和应答       中
    为 建立          连接 导致 的 负荷     ，    同时 对 每   次    请   求而 言 缩     了    处 时                          请
           TCP                                                                 理  间                                的
                                                                                                                   如立
    “Connection”                                   头 部 字段 就是专门用于 告诉对 方一个 请 求 完成 之后该    如   何 处     理 连  接  的      ，比
              关闭连接〈              该头部字段
    即                               的值 为“close”)     或者保持 一 段 时间以 等 待                      后 续 请求       〈  该  头 部 字段
    的 值 为“keep-alive”)。     当 用           问        个 网 页 时  ，  读 者          不 妨 使 用netstat              来        浏览器
                                                                                                                   命令
                                               一                                                            查看
                                                                            了多 长 时 间     。
    和 Web     服务 器 之 间 的 连接 是 否       是长    连接 ，以 及 该    连接    维持
            在 所 有 头 部 字段 之       ，HTTP     请 求 必须 包含 一 个 空                    ，以  标识 头 部 字段的 结束             。       求
                                 后                                     行                                           请
                                                                              符) ; 而 室 行 则 必须 只 包含               一 个
    行 和 每   个   头      部 字段都必须以      <CR><LF>  结束     回 车    符     和 换行
    <CR><LF>，       不    能有 其他 字符    ， 甚 至 是   空白     字符 。
     空行之后 ，HTTP 请求可以 包含 可 选的 消息 体。如 果 消 息 体非 空 ， 则 HTTP 请 求的 头
   在
    部 字段 中 必须 包含 描述 该 消息 体 长 度            的    字段“Content-Length”。                              我 们 的 实例 只 是 获取 目标 服
    务 上 的 资源        ，    所以 没有       消息
        器                            体。
                           答
    4.6.2             HTTP 应

                    答 部分             容
            HTTP 应 的     内         如下:

## Page 85

    第4    器
     章    TCP/IP 通信 案例 Internet 上 的 Web 服务 fe 67


         HTTP/1.0 200  OK
         Server:  BWS/1.0
         Content-Length: 8024
         Content-Type:  text/html;charset
         Set-Cookie:                                                    expires=Wed,
             04     -Jul-42    00:10:47  GMT; path=/; domain=.baidu.com
         Via: 1.0 localhost (squid/3.0 STABLE18)
             行     。“HTTP/1.0”     是 服务
             器使用的                                                    HTTP 协议
                                                                        的版本号             通常， 服务
         第 一 行 是 状态                                                                          器需
    要 使用和 客户 端相同     协议
                        的 HTTP     版本    。“200 OK”    是 状态   码和        状态 信息。   常见      的    码和
                                                                                             状态
                       如表 4-2 所 示   。
             状态 信息 及 其 含义

                                 R4-2 HTTP 状态 码 和 状态 信息 及 其     含义
         状态   类 型                                                         含义
                                                   服务器 收 到      了 客户   端的 请 求行  和 头  部 信息 ，   告
                                                                                              诉
    ex  *信息      100     Continue                       SE         BR       AEP              ER
                                                Expect: 100-continue            字段 告诉服务器 自己 还 有
                                                         要发送
                                                数据
             |                                             功
    2xx  成 功      200oK                            请 求 成

                                                   AS           VEG ETE Fb     ye),     (AA BAY
                                                                来获得它
                                                GET 方法
    3xx  重 定向                                      表示 被     申请  的 资源   没有更     ，和 之 前      的相同
                                                                            新           获得
                                                   通知 客户           源能 在   他 地 方 找到      与 302 不
                                                                                     。        同
             307 Temporary     Redirect         的     是 ，客 户 端  可  以 使 用 和 原始 请 求相   同  的 请 求方法
                                                来 访问 目标 资源



         ，                                                                     Fe Pay a


                                                      4                        OA

    ES

    第    2~7 HTTP 应 答 的头 部 字段 。其 表示 方法                与HTTP     请   求中 的 头 部 字段      相同 。
             示目
    “Server: BWS/1.0”     表 标Web 服务 器 程序            的名 字 是        BWS (Baidu Web Server).
             示    目     标     文  档         的 长 度    为8024  字 节  。   这 个 值 和 wsget        输出 的 文档
    “Content-Length:     8024” 表
    度一致。
    长
    “Content-Type: text/html;charset =gbk” #78 A ty 3¢ #4 ff)        类 型 。 其
                                                                MIME              中     “text” 是
    主 文档类 型 ,“html”  是 子      类型                                档                      是text类型中的
             文档                                 表 示 目                             标 文 index.html
html 文档 。“charset” 是 text 文档 类 型的一 个 参数 ，用 于 指定 文档的 字符 编码 。
   “Set-Cookie: BAIDUID=ASB6C72D68CF639CE8896FD79A03FBD8:FG=1: expires=Wed,

## Page 86

  68     第 一    篇  TCP/IP    协议 详解

      GMT; path=/; domain=.baidu.com” 表 示 服 务 器 传送一个Cookie 给 客户
  04 -Jul-42 00:10:47                                                                                    端。
     中        指
                            定 的
                               名    字                               指
                                                                      定                   的 生存
  其 ,“BAIDUID”          Cookie                   ,“expires”              Cookie             时间 ,“domain”  和
      生效
                           域名 和 路             径   下面 我 们 简单 分 析              一    下 Cookie 的 作用 。
  “path”       指 定 Cookie  的                    。
     第    2     章 中 曾 提 到   ，HTTP 协议 是   一   种 无    状态     的    协议 ， 即 每个         HTTP        请 求之间 没 有任何 上
     文关 。    如 果 服      务 器 处 理 后    续 HTTP  请     求 时需要
                                                                用   到
                                                                      前  面   的 HTTP             请 求的相关信息 ，客
  下                                                                                                       户
                              样就      导致 HTTP      请求   必须 传输         更  多 的   数据 。
  端 必须 重 传   这   些 信 息    。            这
                               之后                       协议          的这 种 无   状态 特性     就 显得 不 适应  了       因
     在 交互    式     应 用      程序    兴起
      Web                                    ，HTTP                                          ，             为
      程序通常要                   。      ，                    额外                                ，           见的解
  交互 承上启下 因 此 我 们 要 使 用                                         的    手段 来 保持 HTTP                   连接 状态 常
  决 方法 就    是Cookie。Cookie     是 服务 器 发       送   给 客户          端的特殊 信息                   HTTP 应 答 的 头 部 字段
                                                                                            过    HTTP     请
  “Set-Cookie”)，   客 户 端 每    次   向  服务 器 发       送请    求 的时 候      都需要带上 这 些信息            〈 通            求
     头部   字段 “Cookie”)。       这 样     服务 器 就  可   以    区 分 不 同      的 客户       。了 基于             器的自动登录 就 是
  的
                 实现
  用 Cookie       的。
                                                          表         HTTP 应 答 在 返回          程中     经 过 的 有 代
       “Wia:  1.0 localhost (squid/3.0 STABLE18)”              示                   过        历      所
                                                           上指的           是“192.168.1.108"。       这 个 头 部 字段
  理服务 器 的 地    址   和 名 称    。  这 里 的    localhost 实际                                                      的
            有点类似于          议 的 记录 路 由 功能     。
  功能
      所有    头 部 字段
     在                     之后     ，HTTP  应 答 必 须 包 含       一        个 空  行 ，以     标识 头 部 字段 的 结束      。状 态 行
      头      都                                     ，                              一个                       不
  和 每 个     部 字段         必须以  <CR><LF>           而 空 行 则 必须 只 包                    <CR><LF>，             能有其
  他 字符    ，其       至 是 空白 字符  。
                                                                    然，   我 们 并 不 关心
      行     之后   是    被  请 求   文档 index.html     的 内    容  〔〈   当                          它)，其 长 度 是   8024
        节。
  字

  47

           此，我 们     成   功         地 访问 了 Internet 上
                                                 的      Web     服务
     至                                                                器， 通 过 该 实例          ，我 们 分析    了 TCP/
                               的                        和             传输 层 的 TCP     和  UDP、    网
  IP  协议 族 各 层 的 部 分 协议        :    应  用 层  HTTP           DNS、                             络层的          IP.
      链路层的                     它们 之 间 是         何   协作 来 完成           网  络 通信     的。我   们  的分 析 方 法 是    使 用
  数据     ARP，              以 及              如
      抓 然 观察 协议 的 头 部 内 容以 推断 其                                           原理 在 后            章节 中           我
  tcpdump    包，     后          各层                                     工作     。              ，            们还将
多 次 使    用   这 种 方法 来    分析 问 题     。

## Page 87

CRA SET tes PE  95 As Sit FE
    一










第  5 章   Linux    网 络 编程基础API
      章 高
第 6          级IO BK
第    章                  服务
   7     Linux      器 程序规范
   8章        性        框架
第 高 能 服务 器 程序

第  9 章    LO 复    用
      章     信号
第 10

Rie       LHS
             性能       框架
第  12  章  高     LUO    库 Libevent
第  13  章        多 进程编程
   14             线程
第     章”多     编程
HASH          池和    线程池

## Page 88

第 S 章 Linux 网 络 编程基础
    API


             的一章。                    它 探讨 Linux      网 络  编程 基础             与 内 核中                    协议
     本 章 是 承前启后                                               API                TCP/IP      族之间
的 关系 ，并 为后续 章 节 提供 编程基础 。我                      们   将 从如  下  3 个 方面 讨论      Linux      络
                                                                                 网
                    址 API.   socket  最 开始 的 含义        是   个    地 址 和 端口
     Q)  socket 地                                      一     卫  对                 〈ip，port)。  它  唯一    地
         表示 了 使用       TCP        的一端。               称其为      地址。
                            通信 本 书 socket
     L} socket 基 fii API.    socket 的 主要 API 都 定义 在 sys/socketh 头 文件 中                     ，      包括 创 建
             名                    监
             命                       听                     接
         socket、     socket、           socket、     受 连    接、    起      、     写     据、                  、
                                                                     发
                                                              连接 读
                                                                                数              获 取 地 址信息
                                                          。
         MM                            socket           选项
               络信息 API. Linux     提供 了     一  套 网 络信息   API，      以 实现 主机    名 和   IP  地 址    之 间 的 转换 ，
         网
          及服务 名称    和 端口          号  之 间 的     转换。 这
                                                     些  API    都 定义
         以                                                           在 netdb.h     头 文件     ，          我
                                                                                           中     们将讨
         论 其 中 几 个 主要 的 函数         。

             址
5.1      socket          地  API

                         址  API，   先 要 理解      主机  字 节  序 和  网 络 字 节 序 。
     要 学 习 socket 地

             字
                      节
             序
                            和      络
5.1.1     主机                 网           字 节 序
     现代         CPU 的 累加 器   次 都     能 装载                 节      里                  机，
                                                     4               字 这
                            一                                         考虑 32  位 下 同 )， 即     一        个 整
           么这 4   字 节 在 内 存 中 排列 的 顺序 将            影响它 被 累加 器 装载 成 的         整数      的 值 。这 就    是 字 节 序
数 。 那
问题 。字 节 序 分 为大端 字 节 序                (big endian)  和小     端 字 节 序  〈little endian)。    大 端 字 节 序     是 指
                                                              位字节                                     存储
一 个 整数的 高位字     23              ~ 31 bit)  存储 在 内 存 的      低地   址  处 ，低            《〈0 ~  7 bit)       在
内 存 的 高 地 址     。     小  端  字 节 序 则 是     指 整 数 的  高 位            字 节 存储 在 内 存 的 高 地 址   ，而          字节则
             处                                                                   处                    低位
存储 在 内 存 的 低地         址 处   。代 码 清单    5-1    可 用 于 检查 机 器 的 字 节 序   。
                                                             机器字节序
                                         代码 清单5-1               判断

     #include     <stdio.h>
                     ()
     yoid     byteorder
     {


            shert value;
            char  union_bytes[     sizeof( short   )    ];
}        test;
test.value      =
if       (  ( test.union_bytes[    0 ]     ==      1 &&   (                  1 ] == 2     )

## Page 89

    5章 Linux 网 络 编程基础 71
    第



    {
        printf(  “big endian\n" );
    ise if (  (  test.union_bytes[ 0 ] == 2 && |  test.union_bytes[ 1 ]  )

        printf{  “little endian\n" );


           {
                printf{
           }
       }

   现代 PC 大 多 采用小 端字节 序 ，因 此 小 端 字 节 序 又 被称为主机 字 节 序 。
                化的数据           32 bit      整 型数  和 16 bit 短 整 型 数 ) 在    两     台 使用不  同    字 节 序 的  主机
       当 格式
                  时， 接 收 端     必   然   错误 地 解释    之。  解   决       问题 的 方法是  :  发 送 端  总    是 把要  发 送 的
    之 间 直 接 传递
    数据 转化 成 大端字 节 序数 据             后   再 发   送 ，而 接收 端 知道     对方       传送过  来  的       数据 总 是 采用 大 端 字
         序，所  以 接收 以 根据 自身 采用 的 字 节 序 决定                  是   否对 接收 到 的 数据 进行         转换     〈     端机转
    节                                                                                                小
                  )。     因 此 大 端 字 节 序 也 称 为       网    络 字 节 序 ， 它 给    所  有 接收 数据 的        主机   提供 了
    换，大         不 转换
    一 个 正确 解释 收 到 的 格式 化 数据 的 保证           。
       需要 指出 的       是  ， 即    使是  同一 台 机 器     上 的 两 个   进程        一个由        C  语言 编写      ， 另 一 个 由
JAVA 编写) 通信，也 要 考虑 字 节 序 的 问题 (JAVA 虚拟 机 采用 大 端 字 节 序 )。
       提供
       Linux         了如下 4     个 函数 来 完成    主机    字 节 序 和 网 络 字 节 序     之间 的 转换    ;
   #include <netinet/in.h>
   unsigned long int htonl( unsigned long int hostlong );
       unsigned   short int    htons(   unsigned  short   int hostshort  };
   unsigned long int ntohl( unsigned long int netlong );
       unsigned   short int    ntohs(   unsigned  short   int netshort
              的    很明确，           如                                                   型              主
       它们
                含义             比       htonl 表示 “host to  network long”，   即 将    长 整     32 bit)    的
    机 字 节 序数 据 转化 为网络 字 节 序 数据 。这                 个 函数                                             地 短
                                         4                中，  长 整 型 函数 通常         用 来 转换   了      址，
    整 型       用 来 转换     端口号   〈 当 然 不  此。                                                ，      使
                                           限于 任 何 格式 化 的 数据 通过 网 络 传输 时                   都 应 该      用
      些 数     来 转换 字 节 序 )。
    这

    5.1.2    通用 socket         地址

                        接口中      表示 socket     地 址 的 是结构
       socket 网        络 编程                                   体sockaddr,   Hee    LiF:
       #include   <bits/socket.h>
       struct   sockaddr

                        t sa_family;
              sa_family
              char
       }
      |
                  成员是地址族类型                 family
       sa_family                                   t)     的 变量 。地 址 族 类        型  通常    与  协议  族 类 型对

## Page 90

72    +  第 二    篇       深入    解析 高性   能 服务 器  编程

                                                  称    domain，     见 后 文      )     和 对 应  的    地 址 族 如 表 5-1    示。
Me                      (protocol family， 也                                                                       所

                        表 5-1                     协议族和 地址 族的关系
               协议
              PF_UNIX                                                                        UNI REDO

                                                                                                  BIL

      Ax PF_* 和         *都 定义在                                            ，                者              ，          以
                    AF     bits/socketh                 头 文件          中   且 后 者 与 前          有 完全 相同 的     值     所
二  者 通   混用     。
                成    员   用 于 存放            地 址        。但
      sa_data           socket                    值       是，   不 同        的     协议  族 的   地  址 值      具有 不 同 的   含义  和
        度 ， 如
长           表   5-2 所 示  。
                                         表 5-2                            族及其地址值
                                                                             ”协议
                   协议 族                                                       地 址 值 售     和 长 度
                   PF_UNIX                            的路径和名，                  度可          节         后立)
                                              文件 长
                                                                                 达到           108 字 【 见
                                                                 和    32 bit IPv4 地
                                                                                       址，      共 6 字
                   PF    INET                    16 bit         端口                                 节
                                                                 号            流标
                                              16 bit    端口            ，32 bit      识 ，128   bit IPv6    址
                   PF    INET6                                                                        地 ，32 bit 范围 ID,
                    ~        共                    26    节

        表  5-2 可
                       见 ，14      字 节
      由                 的     sa_data         根本 无 法 完全 容纳 多 数 协议 族 的                          地      址  。    因
                                                                                                        值     此 ，Linux
定义 了 下 面 这 个 新 的 通用                                   结构
                        socket     地 址                体:
      #include     <bits/socket.h>
      struct     sockaddr     storage
      {
           sa_   family七 sa_family;
           unsigned     long     int  _ ssalign;
           char                                                           )];
      }
      这 个 结构 体 不 仅 提供 了     足够    大 的             空间                 用 于 存放 地 址   ， 而 且 是 内     存对 齐      的〈    是
                                                                              值                           这     ss_
        员的 作用          )。
align 成

                                    址
5.1.3                    ”专用 socket 地

                                                                                             与获取      地
                    址和                                                                                    端口 号
      上 面 这 两个通用 socket     地 址 结构 体显然 很不好 用，比                                如               设置                   就
                                    以 Linux 为   各 个 协议 族 提供 了                 专门
                                                                              的    socket      地 址      结构
需要 执行 烦琐 的 位操作。所    体。
                                                                             结构
      UNIX             本 地 域 协议 族 使 用 如  下 专用 socket 地 址                     体:
      #include     <sys/un.h>
      struct     sockaddr_un
      {
           sa_family_t     sin_family;            /*    地 址    族: AF_UNIX     */
           char     sun  Path[108]:               /*    广 路径名       */
      he

## Page 91

    5章 Linux 网 络 编程基础 API| «fe 73
    7    第


                                 族有    sockaddr
                                           in                      和 sockaddr
                                                                          in6                 两
    个                                                                                 专用 socket     地 址 结构
          TCP/IP                 协议                                                                         体，     它 们 分 别 用
于 IPv4 和 IPv6:

                                       in
          struct                     sockaddr
          |
                                       t                            地址
                   sa_family               sin_family;        /*            族:，   AP_LINET     */
                                       sin_port;              /*         口      ，    用
                                                                              号   要 网 络 字 节 序 表示        */
                                                                                      体，    见 下    面 */
                   Struct               in_addr  sin_addr;    /*    IPv4      地 址 结构

          struct        in_addr
          {
                                                                                  址，   要
                   u_int32_t           s_addr;                /*    IPv4     地                 用网络字 节 序 表示 */

          struct
          {
                   safamily            七     sin6 family;     /*                  AF_INET6     */
                   u_int16_t           sin6     port;         /*    MOS,              RRA               */
                   u_int32_t           sin6é     flowinfo;    /*    流 信 息 ，应 设置               为    0 */
                   struct      in6 addr     sin6                    IPw6     Hh   ht   ag ok,        */
                                                                                      尚处于 实验
                   u_int32_t           sin6é_scope_id;        /*    scope         ID，                阶段 */
          hj
          struct       in6     addr
          {
                   unsigned     char       sa_addr[16);       /* IPv6         Mit,                         */
          he
   两个                                                             含义                  ，
          这
                            专用 socket                地 址 结构 体 各 字段 的          都 很 明确     我 们 只 在 右边 稍       加  注释 。
              有专用           socket         (LAR     sockaddr   storage)              类 型的                           需要变量 在 实际 使 用时 都 转化 为
          所
    通用 socket               址  型                                 即可)，                  所                                      编程接口 用   参数
                                  地
                                       类 sockaddr〈     强 制转换 因 为 有 socket                                      使    的     地 址
                 类型都是     sockaddr。
    的

    5.1.4          IP                     地址 转换 函数

          通常       ，    人 们 习惯 用 可 读       性                好 的 字符 串来    表示 中 地       址     ，    比如 用 点 分 十    进 制               字 符 串 表示
              址        ，以   及 用 十 六 进 制     字符 串 表 示                    地
    IPv4  地                                                   IPv6            址 。    但 编程 中 我 们 需要 先 把 它们 转化 为                         整数
                 进制数)                                                   ，                                   转化
    (二 方 能 使                               用 。 而 记录 日   志    时 则 相反         我 们 要 把 整数 表示 的 IP             地 址          为         可 读 的 字
                   面        个  函数 可 用 于 用 点     分 十 进 制       字符 串 表 示 的              地
    符 。下
          串            3                                                                    Pv4                       址和用网 络 字 节 序整数 表示 的
    IPv4               地 址 之 间 的 转换

          #include          <arpa/inet.h>
          in_addr_t       inet_addr(       const        char*      strptr     );
          int        inet_aton(        const     char*  cp,       struct in_addr*           inp     );
          char*                            struct   in_addr   in      );
          inet_addr          函数将       用 点 分     十  进 制     字符 串 表示 的 IPv4            地 址 转化 为    用     网  络 字 节 序                整数 表示 的
                           址。它
    IPv4 地                  失败 时       返回 INADDR_NONE.
                            函数 完成             和                        同样
          inet_aton                        inet_addr          的        功能    ，   但 是 将 转化 结果 存储 于 参数        inp         指     的     地 址
      中。
    结构 它 成                   功 时 返回          1，  失 败 则 返回 0。

## Page 92

    74   “se*  第 二    篇                深入 解析 高  性    能服务      器 编程

                      函数 网 络 字 节 序                                表示 的       IPv4 地 址 转化 为 用 点 分 十 进 制         字符 串 表 示
         inet_ntoa        将用                                  整数                                                  的
    IPv4 地    址。但 需要 注意                  的   是  ，    该 函数 内 部 用     一        个 静态 变量 存储 转化         结果， 数 的    返回 值   指向
             存，         此                                         。                             不  重
    该 静态 内            因 inet_ntoa              是 不 可         重     人 的 代 码 清单     5-2 揭示 了 其       可 人 性 。

                                               代码 清单              ”不
                                                             5-2     可重入的       inet_ntoa 函数
         char*   szValuel       =    inet_ntoa(              “1.2.3.4”     );
         char*   szValue2       =                            “10.194.71.60”     );
         printf(        “address     1:    %s\n”",            szValuel     );
         printf(        “address     2:    %s\n”          ,   szValue2     );

             这段代码 ，             得 到 的 结果
         运行                                               是;
         addressl:                10.194.71.60
         address2:                10.194.71.60
             Ch SE        AY               AE                 AT     Te    个 函数 同样
                                                                  3             的功能，                  并 且 它们 同时 适用 于 IPv4
                        址;
    地址和 IPv6 地

         #include     <arpa/inet.h>
         int   inet_pton(       int        af,   const        char*     src, void*   dst );
                                                                                                         t    cnt );
         const   char*               inet_ntop(  int         af,    const  void*  src, char*  dst, socklen
         inet_pton                         FF B Ze aN        AY   IP     址
                                                                  地                 用 点 分 十 进 制 字符 串 表 示 的   IPv4 地 址 或
                                                                                                    址，           并 把 转换
    用 十 六 进 制 字符 串 表 示 的                   IPv6      地 址      )     转换 成   用 网络   字 节 序  整数   表示 的 IP  地                结
    果 存 储 于    dst                              Hb,           af             HHI,                           者  AFINET6。
              成 返回                   失 败 则 返回                  0    并      errnoe。
    inet_pton  功时                    1，                           设置
         inet_ntop        数 进行相反 的 转换 ，前                          三     个  参数 的 含义 与     inet pton    的 参数 相同    最 后  个 ， 一
参数 cnt 指定 目标 存储 单元 的 大 小。下 面 的 两个宏 能 帮助 我 们 指定 这 个 大小 〈 别用于
        分 IPv4
All IPv6):

         #finclude    <netinet/in.h>
         #define     INET           ADDRSTRLEN   16
         #define               “INET6_ADDRSTRLEN     46
                      成 返回 目标 存储                                  的     地  址 失 败 则 返回    NULL   #12    errno.
         inet_ntop        功时                                 单元              ，

    5.2      创建       socket

                                                    是         所 有 东西
         UNIX/Linux       的  一      个 上哲学            :            都是文件          。socket    也 不  例外 ，  它 就是   可 读  、  写、
                                                                                                                      可
    可 控制     、                    可 关闭 的 文件 描述   。    下 面 的 socket          系统 调用 可        一个
                                                符                               创建              socket:

         #include                <sys/types.h>
         finclude     <sys/socket.h>
         int   socket(    int       domain,          int     type,     int  protocol   );


                 提 供  ermo           表示 各 种 错误。如 非特
                                以                             殊情况，本         书 将 不 一 一    指出 各 函 数 可 能 反馈 的 errmo值

## Page 93

                                                                                  第 5 章    Linux 网 络  编程基础    API   oe       75

      domain    参数 告诉系统 使 用 哪个 底层 协议           。                               对 TCP/IP        族而言，                       应该设置为
                                               族                                                                       协议 该参数
 PF_INET (Protocol Family of mmternet，         用 于 IPv4)     ak                PF_INET6            于 IPv6)             对 于 UNIX
 本 地 域协议 族 而           ，    该 参数 应    该        _                                                                所
                    言                   设置     为 PF UNIX。    关于 socket                         系统 调用 支持 的 有 协议 族            ，
 请读者 自己 参考其            手册
                       man           。
                                     服
      type      参数 指定服务 类            。    务             有                                            〔〈        )
                                     型             类 型 主要    SOCK_STREAM 服务                          流服务 和                 SOCK
                                               族而言，                            值取     SOCK_STREAM 表示            传输      层   使 用
 UGRAM                 报) 服务 。 对 TCP/IP 协议                                       其
                                                                                协议
 TCP  协议     ，  取  SOCK_DGRAM        表示      传输  层 使用                  UDP       。
      值得 指出 的       是     ，自  Linux     内 核 版本   2.6.17 起    ，type              参数 可以          接受  上 述 服务 类 型 与         下   面 两
                   相     与  的 值  :    SOCK_NONBLOCK 和 SOCK_CLOEXEC。                                          它 们 分 别 表示 将 新 创
 个 重要 的标志
 的       socket 设 为 非 阻 塞            ，以 及 用 fork 调用 创建    子      进 程                  时    在 子 进程  中  关闭   该  socket。       在
 建                               的                                                                                          内
 版本       2.6.17   之 前    的 Linux                                        中，文件 描述 符 的 这 两个属性 都需要  使 用 额外  的    系统    调用  〔〈    如
 核                                                                                                                            比
 fentl)   来设置    。
      protocol                                   参数 是 在 前 两 个参数 构成 的 协议 集合  下， 再      选择      一个 具体 的 协议      。       不 过 这 个 值
都是                 的     两个                  完全     了                          )。              所        下，              应
 通常
                                   唯一 《前
                                     参数 已 经      决定 它 的 值                         几 乎 在          有 情况         我 们 都         该 把
                                 使用默认协议。
 它 设 置     为                        0，表示
      socket    系统 调用 成    功     时 返回   一 个   socket 文件 描述                     符， 失 败 则返回          -1 并 设置    errno。

 5.3      命名       socket

      创建          时，     我们 给 它         指定   了 地 址     族 ，   是           但   并 未 指定 使 用 该            地     址 族 中 的           具体哪个
                                                                                                     器        程序    中，      我
 socket   地 址 。将  一    个 socket 与    socket                  给 地 址 绑 定 称 为   socket 命名         。 在 服务                        们通
   socket， 因 为只有 命名后 客户端才能 知道 该如 何 连接 它。客 户 端 则 通常不需要 命名
 socket，   而 是 采用 匿名 方式              ， 即 使用   操作 系统 自动 分 配 的                      socket  址。         名
                                                                                               地 命     socket            的 系统 调
                其定义如下:
 用 是 bind，

      #finclude    <sys/types.h>
      finclude     <sys/socket.h>
      int  bind(   int    sockfd, const    struct    sockaddr*                 my_addr,        socklen_t addrlen    );
                                                                                                 描述                 ，addrlen 参数
      bind   将  my_addr     所 指的 socket    地 址 分 配给 未        命名                的 sockfd     文件       符                        指
 该    socket    地 址 的    长度  。
 出
                                   失败
                                           返回 -1 并 设置                           其 中 两 种 常见           ermo     是 EACCES 和
      bind 成功时 返回                    则                                                             的
 EADDRINUSE，       它 们 的 含义 分 别 是         ，
                       被     定 的   地 址    是受 保 护     的  地                  ，仅         用                                     用户将
                                               址                               超级 户 能 够 访问           。     比  如              普通
                                                                                                                            。
          socket   绑 定 到 知名 服务 端口 〈 端 口号为                            0~-1023)    上 时    ，bind    将 返回               EACCES 错误
                                     乡  定的地 址 正在 使 用                           比 将               绑
          EADDRINUSE，                          中                 。                如    socket        定   到 一 个 处          TIME_于
                       的        socket 地
          WAIT   状态                  址  。

## Page 94

76  *¢    第 二       篇    深入   解析  高 性 能 服务 器    编程

5.4       监听 socket

                              后，  还 不 能    马上   接受 客户 连接        ，   我们 需要
                                                                             使    用   如      下 系统 调用 来 创建
                          之                                                                            一个
      socket    被 命名
监听    队列 以      存放 竺      处 理 的   客户 连接    :
      finclude      <sys/socket.h>
      int listen(         int sockfd,   int      backlog    );
      sockfd    2 3 48        WS UT AY      socket.    backlog 参数 提示 内 核监听 队 列的最 大 长     。              监
Ur BA  i) AY    BE On FR      et   backlog,      MR             Sc  HW BP Ee, AP
                          错误 信息      。 在 内 核
ECONNREFUSED                          版本                2.2   之 前   的Linux fF,  backlog   参数 是 指所 有 处 于 半
                                                                             核
连接     状态    (SYN_RCVD)           和完全 连接              状态          CESTABLISHED) 的 socket  的上限 。但 自 内    版
          后，              表示                  完全 连接 状态 的        的 上
本 2.2 之         它 只           处 于                           socket           限， 处 于 半 连 接 状态的socket 的 上 限
MEH /proc/sys/net/ipv4/tep_max_syn_backlog                  内 核参数            。backlog    参数
                                                                         定义        的典型          值是$。
                成   功 时 返回        失败
      listen                  0，            则返回    -1  并设置    errno。
                                                                                            对   listen 系统
      下 面 我们 编写              一个服务 器 程序     ， 如     代码 清单       5-3   所 示 ，    以 研究backlog  参数
调用 的 实际 影响            。
                                      代码 清单 5-3               backlog 参数

      #include       <sys/socket.h>
      #include       <netinet/in.h>
      #include       <arpa/inet.h>
      #include       <signal.h>
      #include       <unistd.h>
      #include       <stdlib.h>
      #include       <assert.h>
      #finclude      <stdio.h>
      flinclude      <string.h>

      static        bool stop  =  false;
                                  的处理函数，    时 结束
      /*  SIGTERM     信号              主程序中 的 循环                 */
      static        void  handle  term(    int  sig    )
      {
                stop  =   true;
      }

      int      main(  int argc,      char* argv[]      )
      {
                signal(   SIGTERM,   handle    term     );

                if{  arge <=   3  )
    {
    printf( "usage: %s ip address port_number backlog\n",
    basename( argv[Q] ) );
    return 1;
    }
                const    char* ip    = argv[1]:                              .

## Page 95

5章 Linux
第  网络编程基础API 77



      int  port =            atoi(     argv(2]     )
      int  backlog             =  atoi({     argv[3]  );

      int  sock =            socket(      PF_INET,   SOCK_STREAM,       0 );
      assert(       sock         >=  0 );

      /*                创建 一个IPv4   socket     */
      struct        sockaddr_in           address;
      bzero(        Saddress,          sizeof(     address     ) );
      address.sin_family                  =  AF_INET;
      inet_pton(             AF_INET,       ip,     4address.sin_addr     );
      address.sin_port                 =  htons(    port     );

      int  ret      =  bind(         sock,   {    struct     sockaddr*  )&address, sizeof(     address ) );
      assert(       ret        !=   -1 );

      ret  = listen(              sock,     backlog   );
      assert(       ret        !=   -l );

                                              信号将它中断             */
      /*    GREER.                          SIGTERM
      while    {    !        stop   )
      {
           sleep(            1   );
      }

      /*    #fl     socket,       BEX */
      close(        sock       );
      return        0;
}

YRS         BY (4A testlisten)                          3    个     :               号和     值。             们在
                                              接收
                                                               参数     下                        端口 backlog 我
                        该服务器程序，               并
                                              在      ernest-laptop     上 多 次 执行 telnet 命令              来 连接
Kongming20  上 运行                                                                                       该服务器
      时，每           使 用 telnet       命令 建立 一个连接，就 执行一次 netstat                               命令 来查看 服务器上 连接
程序 。同
                               作过程如下:
的 状态。                             具体操
                                                                                                传递 型什 5
$   .Acestlisten          192.168.1.109         12345     5    # #9 12345M0,     #backlog
                                                          执行
$   telnet  192.168.1.109                  12345     多          之
$   netstat    -nt     |     grep    12345     # 过 次    执行 之

代码 清单               是                  命令某次 输出 的 内 容 ，它 显示了 这                    时 刻                   监听队列
             $-4        netstat                                             一        listen            的内容。
                                          代码 清单 5-4                     监听队列的内容
                                                                         listen

Proto  Recv-Q       Send-Q           Local   Address                    Foreign  Address       Statetep
tcp         0             O      192.168.1.109:12345                      192.168.1.108:2240     SYN_RECV
tep         0             0      192.168.1.109:12345                      192.168.1.108:2228
tcp         0             O      192.168.1.109:12345                     192,.168.1.108:2230     SYN_RECV
tcp         0             O      192.168.1.109:12345                      192.168.1.108:2238     SYN_RECV
tcp         0             O      192.168.1.109:12345                     192.168.1.1086:2236     SYN_RECV

                                          状态。
后  ”等价    于 图 8-8 中                 的SYN_RCVD

## Page 96

    78 “*:* 第 二篇 深入 解析 高 性能服务 器 编程


           tcp          0        0          192.168.1.109:12345                 192.168.1.108:2217        ESTABLISHED
           tep          0        0          192.168.1.109:12345                 192.168.1.108:2226        ESTABLISHED
           tcp          0        0          192.168.1.109:12345                 192.168.1.108:2224        ESTABLISHED
           tcp          0        O          192.166.1.109:12345                 192.168.1.108:2212        ESTABLISHED
           tcp          0        O          192.168.1.109:12345                                           ESTABLISHED
           tcp          0        O          192.168.1.109:12345                 192.168.1.108:2222        ESTABLISHED

   见，               在 监听 队列
                                     中，     处
                                            于    ESTABLISHED 状态
           可                                                                    的  连接只有    6 个 Chacklog 值 加   1)，   其
    他 的            都处于                      。                                    程序 第                         ，    发
           连接               SYN_RCVD 状态     我 们 改变服务 器                           的 3 个 参数 并 重新 运行           之       能
     的规律 ， 即 完整连接 最多有 (backlog+1) 个。在 不 同 的 系统
    现 同样                                                                            上，    运 行    结        果 会 有   些 差
别 ，不 过 监听 队列 中 完整 连接 的上限 通常比backlog 略大。
        值

    5.5      ”接受        连接

                                 调用从        监听 队列 中 接受
                                  系统
           下 面     的                 listen        一个连接
           #include     <sys/types.h>
                        <sys/socket.h>
           int    accept(    int        sockfd, struct sockaddr          *addr, socklen_t     *addrlen );
        是执行过 listen 系统 调用 的 监听 socket®. addr 参数 用 来获取 被 接受 连接 的 远
           sockfd                      参数
                      址，
    Sj socket     地         该      socket    地 址 的 长 度由addrlen       参数 指出          。accept  成功时 返回     一 个   的 新  连接
    socket，     该 socket            唯一地      标识 了 被 接受 的 这 个 连接 ，服 务 器 可 通过 读 写 该                  socket 来     与被 接受
    连接 对 应 的 客户 端通信                                      失败
                                        。accept     时    返回 -1     并     设置     ermo。
                                            BAD     Ab     ESTABLISHED                                 状态
           现在 考虑如 下 情况                      WRU                                     的    连接    对应                的 客户
                                        :                                                                          端出
                            如掉       线 )，     或者提前     退出            服务  ， 那 么  器 对 这 个 连接 执行  的   accept         HAE
    现 网络 异         常 〈               比
    功?          我 们 编写  一             个 简单 的 服务 器 程序 来 测试     之      如 代码 清单 ，      5-5  ATA.
                                                                     一个异常的连接
                                            代码 清单5-5                              ”接受

                        <sys/socket.h>
           #include     <netinet/in.h>
           finclude     <arpa/inet.h>
           #include     <assert.h>
           #inelude     <stdio.h>
           #include     <unistd.h>                                                  .
           #include     <stdlib.h>
           #finelude    <errno.h>
           #include     <string.h>

           int    main(     int arge,       char*  argv[] )
           {
                   if(  arge    <=    2     )
                   [

        调用、处 于 LISTEN 状 态的socket 称 为 监听socket，而所有 处于ESTABLISHED
           马     我 们把 执行       过listen                                                                            状态的
                                 则称为连接socket。
                                       socket

## Page 97

第 5 章 Linux 网 络 编程基础API *** 79


    printf( "usage: ts ip_address port_number\n", basename( argv[0] ) );
    return 1;
 }
 const char* ip = argv(1];
 int port = atoi( argv[{2] );

 struct      sockaddr_in      address;
 bzero(      éaddress,   sizeof{      address ) );
                              =    AF_INET;
 inet_pton(      AF_INET,       ip,   saddress.sin_addr      );
                 port      =  htons(  port   );
 address.sin

 int   sock     =    socket(  PF_INET,       SOCK_STREAM,  0 );
             sock    >=  0   );

 int   ret   =    bind(  sock,      ( struct    sockaddr*  )&address, sizeof( address ) );
 assert(     ret    !=   -1  );

 ret   =  listen(      sock,    5   );
 assert(     ret    !=   -1  );
 /*    PR                                                      完成 */
 Sleepl      20   );
 struct      sockaddr_in      client;
 socklen_t            client_addrlength  =   sizeof(  client   );
 int   connfd     = accept(  sock,    (  struct sockaddr*      )éclient, &client_addrlength  };
 if (    connfd    <   0 )
 {
          printf(      “errno is:     td\n",  errno    );
 ]
 élse
 {
                TERR                  RP a     Ie              +/
          char    remote(INET_ADDRSTRLEN           ];
          printf£(    "connected      with ip:     ts and port:  #d\n",  inet_ntop( AF_INET,
    éclient.sin_addr, remote, INET_ADORSTRLEN , ntohs( client.sinport ) );
    close({ connfd );
 }

 close(      sock   };
 return      0;
}


                              该
                                    服务器程序             为               在
我 在                    运行
       们   Kongming20 上                        (名        testaccept)， 并    ernest-laptop 上 执行
           连接     服务          。       作         过程如下:
telnet 命令 来   该     器 程序                          具体操

$      ./testaccept 192.168.1.109   54321    # Bf    54321   端口
$     telnet  192.168.1.109 54321

       动         端         后
局                          ，        断                端的        立
          telnet 客户
                         程序   立 即     开 该    客户          网 络 连接 〈 建   和 断 开 连接 的 过 程 要 在
器          后                                                   返回     务    如下;
服务
        启动     20          ARAB       accept   调用 能 够 正常 ， 服              器输出

connected with     ip: 192.168.1.108     and   port:  38545

## Page 98

    80 > 第 二 简 ”深入解析 高性能 服务 器 编程


                                                查看     accept         返回 的 连接 socket      的         :
         接着    ，   在                器上运行        以                                             状态
             服务                          netstat 命令

         S   netstat     -nt |     grep  54321
         tep       0             0 192.168.1.109:54321                192.168.1.108:38545            ESTABLISHED
                                      命令
                                 输出         ，accept    调用
                                                客户 端                      络 断 开 毫                  知情 下 面 我 们 重新
         netstat     的                说明        对于                    网           不       。                   执行
    上 述 过     ，        不 过 这 次   不   断 开 客户       端   网    络 连接，   而 是 在 建立  连接 后 立即      退出 客户 端 程序        。  这
             程                                                                                                 次
                          正常         ，      务            输出
    accept   调用 同样                 返回    服    器         如下:
         connected       with    ip:  192.168.1.108     and    port:  52070
         再 次 在 服务        器       运行                  命令
                             上       netstat          :
         S$  netstat     -nt |     grep  54321
         tep       1             0 192.168,.1.109:54321               192.168.1.108:52070            CLOSE  WAIT
        〈《 上 面 的
   由 此 可 见 ，accept 只 是 从 监听 队列 中 取出 连接，而 不 论 连 接 处于何 种 状态 如
        和CLOSE_WAIT 状态 )， 更 不 关心 任何网 络 状 况 的 变化 。
ESTABLISHED 状态

             起
                           连接
    5.6                     发

         如 果 说服务 器 通过 listen                调用 来 被    动    接受 连接    ， 那    么客户 端需             要 通过 如 下 系统 调用 来 主
    动 与      服务   器 建立      连接  ，
         #include              <sys/types.h>
         #include     <sys/socket.h>
         int     connect(    int   sockfd,        const struct     sockaddr  *serv_addr,  socklen_t  addrien );
       参数 socket 系统
        返回 socket。serv_ 参数 服务 器 监听 的 socket 地
         sockfd         由                        调用       一个              addr      是
    址 ，addrlen     参数 则 指定这 个 地 址 的长度              。
         connect   成    功 时 返回                  成功建立     连接 ，sockfd       就  唯一 地 标识      了 这 个 连接   ，客 户 端    就
             读写         sockfd     来 与服务
                                                    器通信        失败
    可以 通过                                                             则  返回 -1  并 设置                   其 中 两 种常见
                                                。connect                            errno。                      的
ermo 是 ECONNREFUSED 和 ETIMEDOUT， 它们 的 含义如 下
                                                                                  们在      3.5.1   小     节讨论过 这 种
        我
   口 ECONNREFUSED， 目 标 端口 不 存在 ，连 接 被 拒绝。
        。
     情况
         口 ETIMEDOUT，              连 接          超时。 我 们    在 3.3.3 小 节讨  论   过 这 种 情况   。

    5.7                  关闭 连接

         关闭     一  个    连接   实际      上  就   是 关闭 该    连接   对 应     的 socket， 这 可 以     通过        如 下 关闭 普通 文件 描述
    符 的 系统 调用 来 完成            :

         finclude     <unistd.h>
         int     close({  int    fd  );
                                 参数
         fd  是待           关闭      的  socket.  ALt,     close   系统 调用 并 非 总 是    立即   关闭   一     个 连接 ，而 是    将
            减 1。                                0 ，                       连接。多 进程 程序 ，一
    的 引用 计数                  只 有 当          的 引用 计数 为 时     才 真正 关闭                                  中      次 fork

## Page 99

    第5  编程基础
     章  Linux 网 络 API|  81


系统 调用 默认 将 使 父 进程中打开 的 socket 的 引用 计数 加 1， 因 此 我 们 必须 在 父 进程和子进程
中 都对 该 socket 执行 close 才能
        调用 将 连接 关闭 。
        不是将socket
         如 果 无论如 何 都 要 立即 终止连接                             〈 而          的引用              计数       减  1)，     可以 使 用 如       下 的
                         调用              于              说，          是                    设计
    shutdown   系统                      《相对    close 来 它 专门 为 网 络 编程                      的 ):
                      <sys/socket.h>
         int   shutdown(        int sockfd,         int howto       );
        参数
        关闭 socket. howto 参数 决定 shutdown 的 行为 它
         sockfd    是待                    的                              了                           ，         可 表
                                                                                                                 取      5-3 中 的
    某 个 值   。

                                                                        的可选值
                                                    表 5-3     howto 参数

                      可 选 值                                                             a      x
                   SHUT RD                              关闭      sockfd    上 的 这  一   半。  应 用 程序 不 能    再 针对 socket       文件描述 符
                                                        执行
                                                            操作 并 且 该        socket   接收     区 中 的 数据 都 被        弃
                        -                                  读        ，                      组
                                                                                         们    的 发 送 缓冲
                                                             AA sockfd    上 写 的 这 半     。sock                区           中 的 数据
                                                                        一                                                  会在真正
                   SHUT_WR                              关闭 连接 之前全 部 发 送出                ，应 用    不可                  描述
                                                                                     去             程序 再 对 该 socket       文件   符
                                                           写操作。         这 种情况
                                                        执行                       下，  连 接   处 于 半 关 闭  状态〔    见
                                                                                     读和写
                   SHUT_RDWR                            同时 关闭 sockfd                 上 的

    此              见                                                                    ，      都        。                     连
   由
       可 ，shutdown 能 够分 别 关 闭 socket 上 的 读 或 写 或 者 关闭 而 close 在 关闭
       时只能将            socket 上 的   读    和 写 同 时 关闭        。
    接
                                                           失败
                                                           返回    -1  并        ermo。
         shutdown           成 功 时 返回     0，         则                   设置

                      读写
    5.8                      数据

                                       读写
    5.8.1                          TCP 数据

  的读写操作                             read 和 write        同样 适用 于         socket。    但
                                                                                         是socket 编程
                                                                                                    接口                       提供
         对 文件                                                                                                       了几个专
       用 于            数据 读 写                系统          它 增加 了 对 数据 读 写 的 控制                  。
    门                                                                                           其
            socket                  的               调用  ，   们                                       中用       于 TCP      流 数 据 读
    写 的 系统 调用 是         :

   #include <sys/types.h>
   #include <sys/socket.h>
   ssize_t recv( int sockfd, void *buf, size_t len, int flags );
   ssize_t send( int sockfd, const void *buf, size_t len, int flags );
                                             ，buf len                                                                  flags 参数
         recv         sockfd    上 的 数据                    和
             见后文，        通 常设
    的 含义                                 置   为0 BPAY.   recy     成  功    时 返回 实际 读 取 到 的 数据 的 长               度 ，      它 可 能小 于
    我 们 期 望 的 长度         len。            因 此 我们 可 能 要 多 次     调用        recv， 才 能 读 取 到 完整        的   数据      。recv      可 能 返回
    0， 这 意味 着 通信 对 方 已经关闭 连接了                               。recy   出     错时 返回 -1       并 设置  errmno。
             往                  上 写 人 数据                Allen       参数 分 别 指 定 写         缓冲    区 的 位 置   和   大                成
         send      sockfd                    ，buf                                                               小   。send     功
时 返回实际写人 的 数据 的 长 度 ，失 败 则 返回 -1 并 设置ermo。

## Page 100

                   篇         解析        能
   82 Ss     第 二         深入          高     性  服务 器 编程

                 参数 为                                的                                或
                                                 数据 收发 提供 了 额外
                                                            控制            它 可     取 表     所 示 选 项 中     个
      flags                                              ，                   以        5-4        的     一         几 个 的
      或。
   辑

                                       5-4                                   的
                                       表                 flags                     可选值
          选项 名                                                                                                    recv
                                       SOCK     DORAM                   RAW     HHH        sot          仅
      MSO.CONFIEM                                                指示 数据链路 层 协议 持续 监听 对 方 的 回应 ，直 到 得 到答复。它
                                                                                        MISOCK
                                       路由表， 直 接 将 数据发                送    给      本 地 局       域 网络内 的 主机。这
                                   不查看                                                                  雪
                                     发                      目标
                                                         主机          在
                                去 送 者 确切 地 知道            就              本 地 网        络 上
      MSG_DONTWAIT                 对 socket   的 此 次 操作 将 是非 阻塞的                                                   Y
                                   告诉内 核应用 程序还有          更 多 数据 要 发               送 ，  内 核   将 超时 等 待 新数据
      MSG_MORE                  写
                                      人       发 送 缓冲 后      并                         。这
                                       TCP             区     一    发     送         可防止     TCR     发送过 多小的 |       N
                                报 文     从 而        传输效率
                                       提高
      MSG_WAITALL                  读 操作 仅 在 读 取 到 指定 数量 的 字 节 后 才 返 回                                            |
                                       缓存                ，               会            这     数据     清除     |   N  |
      MSG_PEEK                     罕 探 读     中 的 数据      此 次 读 操 作       不 导致 些       被                           Y
      MSG_OOB                          接收
                                   发 送 或      紧急 数据                                                               Y
      MSG NOSIGNAL                                       socket      连接 连接所     中   写写 数据 时 不 引发                  N

      我     们  举例 来说明        如  何 使用这 些 选              项  。MSG    OOB               选项 给 应   用 程序 提供 了 发      送和 接收 带 外
        的                        清单  清单
   数据
          方法 ，      如 代码           5-6     和 代码          5-7     所 示 。

                                                                     送带外             数据
                                  代码 清单 5-6                                          ”发

      finclude      <sys/socket.h>
      #include      <netinet/in.h>
      finclude      <arpa/inet.h>
      #include      <assert.h>
      Winclude      <stdio.h>
      #include      <unistd.h>
      #include      <string.h>
      #include      <stdlib.h>

      int main(       int  arge,     char*               )
      {
               if   arge   <=   2    )
               {
printf(                              “usage:  %s     ip_address      port_number\n",     basename(     argv[0]    )   );
                      return      1;
               )
               const  char*     ip        =
               int  port   =          atoi( argv[2]  );

                                          in
               struct               sockaddr
               bzero(    &server_address,     sizeof(     server_address              )     );
               server address.sin_family             =    AF_INET;

      ©  fh                       ay. wey “ie”                           i          Ay.

## Page 101

基础
第 5 章 Linux 网 络 编程 API 83



 inet_pton(     AF_INET,  ip,    &server_address.sin_addr );
 server       address.sin_port   = htons(   port );

 int   sockfd =  socket(  PF_INET,     SOCK_STREAM,   0 );
         sockfd     >= 0  );
 if (   connect(  sockfd,    (   struct     sockaddr* )&server_address,
                                 sizeof(        )     )      <  0 )
 {
        printf(   “connection      failed\n"   );
 }
 else
 {
        const   char*  oob data    =  "abc";
        const   char*  normal_data     =   "123";
        send(   sockfd,   normal_data,     strlen( normal_data  , 0    );
        send(   sockfd,   oob_data,     strlen({ oob_data , MSG_OOB    );
        send(   sockfd,   normal_data,     strlen(              , 0    );
 }

 close(  sockfd   );
 return  0;
}



                                代码   清单      带外
                                                      数据
                                        5-7          ”接收

#include  <sys/socket.h>
#include  <netinet/in.h>
#include  <arpa/inet.h>
          <assert.h>
#include  <stdio.h>
#include  <unistd.h>
#include  <stdlib.h>
finclude  <errno.h>
#include  <string.h>

#define BUF  SIZE   1024

int main(   int  argc,  char*    argv[] )
{
    if(   arge   <= 2  )
 {
    printf( "usage: ts ip_address port_number\n", basename( argv[0] ) );
             return   1;
    }
    const    char*  ip  =  argv[1);
    int   port   =  atoi(  argv[2)   );

                            in address;
    struct  sockaddr
    bzero(  Saddress,      sizeof(   address ) );
    address.sin_family        = AF_INET;
    inet_pton(      AF_INET,    ip, éaddress.sin_addr );
                    port   =  htons( port );
    address.sin

## Page 102

«+ 篇  能
84 第 二 深入 解析 高 性 服务 器 编程



         int  sock     =   socket    (  PF_INET,   SOCK_STREAM,     0    );
                    sock      >=  0  );

         int  ret     =  bind(    sock,     struct     sockaddr*    )saddress,              address ) De
         assert( ret        != -1    );

         ret  =    listen(     sock,   5   );
         assert{ ret        != -1    );

         struct                   in client;
         socklen_t             client_addrlength   =     sizeof( client    );
         int  connfd     =  accept(    sock,   (   struct      seckaddr* )éclient,   &client_addrlength });
         if   (   connfd    <  0  )
         {
                   printf(     “errno  is:    %d\n",     errno   );
         }
         else
         {
                   char    buffer[      BUF_SIZE   ];

                               buffer,             BUF_SIZE      );
                   ret   =  recv(     connfd,  buffer,         BUF_SIZE-1,  0 );
                   printf(     “got    td  bytes   of     normal data    '¥s"\n", ret,  buffer  );

                   memset   ( buffer,              BUF_SIZE      );
                   ret   =  recv(     connfd,  buffer,         BUF_SIZE-1,  MSG_OOB  );
                               “got    td  bytes   of     oob  data "ts'\n",   ret,  buffer  );

                               buffer,     '\O',   BUF_SIZE      );
                   ret   =  recv(     connfd,  buffer,         BUF_SIZE-1,  0 );
                   printf(     “got    td  bytes   of     normal data    'ts"\n", ret,  buffer  );

                   close(    ceonnfd   );
         }

         close(     sock    );
         return      0;
      }

                                                                        器程序    (名
      我 们 先在                      上    启动代码 清单                 所 示 的 服务
                  Kongming20                                                            为testoobrecv)，    然
    从 ernest-laptop 上 执行       代码 清单       S-6    所 示 的 客户
                                                                   程序〔
后                                                                       名                                器发
                                                                        为 testoobsend)        来向
                                                                                                         服务
带外           。                                         程中                                               ALR
送                                                                                                        PR
         数据 同 时 用 tcpdump             抓   取 这  一  过            客户 端 和 服务 器 交换 的 TCP
作 如 下   :
      $    ./testoobrecv   192.168.1.109          54321     #  在 Kongming20   LATT RARE, BW     54321  端口
      $    ./testoobsend   192.168.1.109          54321     #  fEernest-laptop    LATTE
        sudo tcpdump     -ntx  -i     ethO  pert   54321

      服务 器 程序 的 输出 如 下          ;

## Page 103

    第 5 章 Linux 网 络 编程基础API fe 85


          got  5        bytes of     normal   data     '123ab'
          got  1        bytes of     oob    data ‘'c'
          got  3        bytes of     normal   data     '123'
          由 此 可         见    ，客 户      端 发             送给服务 器 的 3 字 节 的 带 外     数据   “abc”  中     ， 仅 有 最 后 一     个 字符
    “cs”  被 服务天             成 真正 的 带 外 数据 接收                    正
                                                            〈     如 3.8   节 讨 论 的 那样 )。    并 且   ，    服 务 器 对       数据
        正常
的 接收 将 被 带 外 数据 ，即 前 部 分 正常数据 “123ab” 和 后 续 的 数据
                                                  一                                                     正常 “123” 是 不 能
被 个 调用
  一 recy 全部读出的。
        输出 和 带 外 数据 相关 的 是代码 $-8 所 示 TCP 报 文 。
          tcpdump       的            内容中，                                   清单              的            段

                                             代码 清单              含 带外数据的TCP 报 文 段

          IP  192.168.1.108.60460            >    192.168.1.109.54321:      Flags    [P.U),    seq   4:7, ack  1,  win
    92,   urg 3,    options          [nop,nop,TS      val   102794322   ecr   154703423],    length  3

          这 里 我 们 第         一  次 看 到        tcpdump    输出       标志U，    这 表示    该TCP    报 文 段的头       部 被 设置    了   紧急
                                              移值， 它 指出 带 外 数据 在 字 节 流 中     的 位     置      的下  一 字 节位 置     是  7 (3+4，
    标志 。“urg 3”             是 紧急
        中          该                           序号     相对                    )。          ，
    其                         4 是 TCP 报 文 段的      值        初始 序号 值 的 偏 移     因 此                  带 外 数据 是 字 节 流 中 的 第
          节，即 字符 “c”。
    6 字
              一提的是                   ，flags 参数 只 对     send    和  recy  的 当前 调用 生效      ，  而 后   面 我 们    将 看 到  如 何 通
          值得
    过 setsockopt        系统 调用 永久 性 地 修改 socket                  的某些 属性。

                                     读写
    5.8.2      UDP 数据

                            接口       中用
                                            于 UDP 数据
          socket 编程                                        报读     写 的  系统 调用 是   ;
                        <sys/types.h>
          #include      <sys/socket.h>
          ssize_t       recvfrom(        int  sockfd,      void*  buf,    size_t len, int   flags,  struct   sockaddr*
                                         src_addr,     socklen_t*     addrlen);
          ssize_t       sendto(      int      sockfd,  const    void*   buf,  size_t  len,  int  flags, const   struct
                                     sockaddr*     dest_addr,     socklen_t     addrlen   );
                        读                                                                                            因
          recvfrom           取 sockfd       上 的 数据         ，buf   Allen               参数 分 别 指定 读 缓冲区 的 位置和 大  小 。   为
    UDP   通信 没有 连接 的 概念 ，所 以 我                         们    每  次  读 取 数 据 都   需要 获    发   送 端的              地
                                                                                                 socket    址   ， 即  参数
    src_addr   所 指      的 内     容    ，addrlen    参数 则 指定 该      地    址  的 长  度 。
          sendto 往 sockfd            上 写 人 数据         ，buf 和 len  参数 分 别 指定 写 缓冲区 的 位 置和大小。dest                   addr
    参数 指定 接收 端的                             地
                            socket           址    ，addrlen     参数 则 指定 该地址 的 长 度     。
                            调用       的 flags 参数 以 及
                                                                       返回 值 的 含义
   均与                                                                       send/recv                 系统 调用 的 flags 参数
                               系统                                                                                   及返
          这 两      个
                   。
    回            值相同
                   一提的是     ，recvfrom/sendto               系统调用 也 可以
          值得                                                                   用于 面向  连接    (STREAM)        的 socket 的
                            把最后             两 个        都设置      为NULL     以 忽略
    数据读            ，只 需要                     参数                             发送端           /接收      端的 socket 地
               写                                                                                         址         〈 因
    为 我 们 已经和 对 方 建立了 连接 ，所 以 已经                                其           址
                                                                知道     socket   地 了 )。

## Page 104

 86        第 二    篇    深入 解析 高       性能 服务      器编程

                      数据读写              函数
 5.8.3                                 ”通用
       socket  编程     接口  还   提供 了       一 对 通用 的 数据 读 写 系 统 调用             。      它们 不 仅 能 用 于     TCP 流 数据 ，
                        报;
 也 能 用 于   UDP      数据

       #include     <sys/socket.h>
       Ssize_t    recvmsg(    int    sockfd,    struct   msghdr*     msg,     int  flags );
       ssize_t    sendmsg(    int    sockfd,    struct   msghdr*     msg,     int flags  );
       sockfd                   参数 指定 被 操作 的 目标 socket.  msg         是 msghdr            的
                                                                     参数            结构 体 类 型         指针    ，msghdr
                体的定义如下:
 结构

       Struct    msghdr
       [
           void*      msg_name;                  /*      socket      地址  */
           socklen_t           msg_namelen;      /*      socket   好 址 的 长    度  */
           struct      iovec*      msg_iov;      /*                                */
           int      msg_iovlen;                  /*                  ASE     */
           void*                                                     OY   te         /
           socklen_t                             /*      辅助数据 的 大       小     */
           int      msg_flags;                   /*                     lags    参数 ， 并 在 调用  过 程中   更新  */
       be
                      成员指向一个          socket                 ER.        EAA              socket         地址。对
       msg_name
     面向连接         的   TCP 协议      ，  该 成 员      设有 意义    ，   必 须 被          为                           对数据流
                                                                                      NULL。               这 是 因为
                                                                                         设置
                                                                          员    则 指定   了             所
 socket  而 ，对 方的地 址 已 经 知道                       。msg_namelen        成                   msg name       指 socket
     址的长度。
 地
                                                                                     体的定义如下，
       msg_iov    成员是 iovec       结构 体 类 型 的指针              ，iovec                        结构

       struct     Lovec
       {
           void     *iov_base;           /*     内 存起始 地址     “7
           size_t     iov_len;                                  */
       hi
           上可见，iovec      结构 体        封装    了 一  块     内 存 起 始 位 置          和                     长 。msg_iovlen
       由                                                     的                       度                     指定这样的
 iovec  结构 对 和        有多 少 。         对 于           而言，                    被                                  块分散
                                  个      recvmsg             数     据 将 读 取 并 存放 在        msg             iovlen
            内存中，  这 些 内 存 的 位        置和 长 度 则 由    msg   iov     指向 的       数组 指定        ， 这 称 为   分散 读 〈seatter
 的
             Xf}          而
 read);        sendmsg            言，msg_iovlen   块    分  散   内 存 中 的 数据 将 被              并 发 ，     这 称 为      写
                                                                                         一   送               集中
 gather write).
                      和 msg_controllen 成
       msg_control                              员用     于 辅助数据 的           传送    。 我 们 不详细讨论 它们          ， 仅 在 第
 13  章 介 绍    如   何 使 用          它们 来 实现 在 进程   间 传 递 文件     描述      符 。
       msg_flags    成 员   无 须     设 ，它 会 复制                                 的        参数
                                     定           recvmsg/sendmsg            flags        的   内 容以    影响    数据 读
前，将                                              某     些 更 新 后 的     标志 设置 到         msg_flags 中
 写 过     程 。recvmsg    还会在 调用 结束                                                                   。
 以                                               返回      值 的 含义
                                                                        均与               的         参数
     recvmsg/sendmsg 的 flags             参数     及                                                       及     返回
                                                                            send/recv     flags               值
     。
 相同

## Page 105

                                                                                第 5 章    Linux      网 络   编程基础API         87

    5.9

                                                                                                    应用 中  ，             我 通常
         代码 清单      5-7 演示 了 TCP                      带 外 数据 的 接收 方法            。    但 在 实际             们               无法预期
                                何时到来。好
    带 外 数据                               在  Linux     内 核 检 测 到          TCP     紧急 标志 时 ，                将 通知应 用 程序有 带 外 数据
    需要 接收。内 核通知 应                      用    程序 带 外 数据 到 达 的                 两 种 常见 方式         是  :  IO  复     用 产生 的 异常 事件 和
                    号。    但
    SIGURG 信和                       是 ， 即         使 应 用 程序 得 到 了 有 带 外 数据 需要 接收 的 通知                    ，  还 需要          带外数
        知道
据 在 数据 流 中 的 具体位 置 ，才 能 准确 接收 带 外 数据。这 一点 可 通过 如 下 系统 调用 实现 ，
    finclude <sys/socket.h>
   int sockatmark( int sockfd );
         sockatmark          Bf         sockfd      是 否    处  于 带 外 标记        ， 即  下 一          个 被 读 取 到 的 数据 是 否 是 带 外 数据。
          是         ，sockatmark 返回                  此 时 我 们       就  可以 利用 带_               MSG        的     来            带外
    如 果                                                                                  OOB 标志         reev              调用
                                            1，                                                          接收
    数据 。     如      果 不    是 ， 则     sockatmark       返回        0。

    5.10                        地址 信息      数

         在 某 些      情 况             ，  我 们  想       知道 一         个连接   socket     的 本 端  socket     址     端的
                               下                                                         地 ，以 及 远                   socket 地
    址 。下 面 这 两个函数正是 用 于 解决这 个 问题                                       :

         #include   <sys/socket.h>
         int             getsockname( int         sockfd,       struct  sockaddr*    address,           t*
                                                                                                    socklen address len  );
         int             getpeername( int         sockfd,       struct  sockaddr*    address, socklen_t*     address len );
         getsockname               获取  sockfd             对 应 的 本 端  socket     址，                     于
                                                                                               地 并 将 其 存储 address 参数 指定 的 内
              中，                                                                                  中。    如 果实 际  socket     址
    和 存   该         socket        地 址  的 长 度 则        存储 于      address_len     参数 指向 的 变量                                地
          度大于       address            指                                                                                 功时返
    的 长                            所 内 存 区 的 大小                   ， 那  么 该  socket   地 址 将 被 截断         。getsockname      成
    回 0，  失 败 返回        -1     并    设置ermo。
                                              远                     socket 地         其参数 值 的 含义                 getsockname
         getpeername         获取        sockfd 对应 的                端             址，       及返回            与
    的 参数 及 返回 值              相同     。

    5.11            socket 选项

         如 果 说      fentl    系统 调用 是 控制 文件 描述 符                          属  性     的 通用 POSIX 方法        ，  那么 下 面 两 个 系统   调
    用 则 是 专门 用 来        读      取 和 设置             socket                    HERI TIE:
                    <sys/socket.h>
         int                           int  sockfd,     int       level,    int     option_name,    void* option value,
                                       socklen_t*     restrict          option_len     };
         int        setsockopt(        int  sockfd,     int       level,    int                     const void*
                                       option_value,            socklen_t option_len     );
         sockfd                              参数 指定 被 操作 的 目标 socket。level   人参数 指定      要 操作 哪个 协议 的     选项       〈 即 属性 )，
    比如   IPv4、IPv6、TCP 等                          。option_name           则                          。
                                                                  参数 指定 选项 的 名 字                       我 们 在表  5$-5  中 列举 了

## Page 106

 88 «+   PIB            深入 解析 高    性 能  服务 器    编程

        个                 比较       常用
 socket 通信 中 几                     的 socket 选项     。option_value 和 option_len           参数 分 别 是 被 操作 选项
    值和长度。    不 同 的 选项              具有 不 同类 型的           ，  表            中  “数据
               的                                                                      类型     ”一
                                                   值     如    5-5                       列所示。
                                           表 5-5     socket         选项
           level                           |                  |            打开 调试 信息           说
                               SO_DpEBUG
                                               |   i                 |              地
                               REUSEADDR                                   本 用 本 地  直
                                           |       im         |                     关
                                                                                     socket
                                           | im               |              并           socket
                               so_ERROR                                    获
                                                                               消除                错误 状态
                                               |     oe                                    路由表，直 接  将 数据 发 送  给  本 地
                                                                           不查看
                                   SO_DONTROUTE                                         ESL           send   系统 调用 的
                                                                                                    标志
                                                                        MSG_DONTROUTE               类似
                                                     | in     |            TCP
        sockET          。      SO_RCVBUF                                     接收缓冲区大小
                                               |   im               |      TP
                                                       ， SO_KEEPALIVE
    (通用 socket 选项         与    |               | int          |            发送     周期性 保   话    报 文以 维持 连接
 协议     无关 )                                                               接收 到 的 带 外 数据           将存留在       数据 的 输
                                                                                        存留)，        此 时 我 们 不 能使   用
                                                                        人 队列 中 【在线

                                                                        (而 应 该   像读 取         通 数 据 那样谈  取 带 外 数据 )
                                               | linger                |   若 有 数据
                               SO_LINGER                                       待发送，     则     延迟      关闭
                                               | int          |            TCP 接 收 缓存     区    低 水位 标记
                               SO_RCVLOWAT
                                               |     int      |                     缓存     水
     SO_SNDLOWAT TCP 发送 区 低 位 标记
                                               | timeval               |   接收 数据    超时 《〈     第 11
                               SO_RCYTIMEO                                                       见 章 )
                                               | timeval      |发送                   数据 超时 〈    第
                               SO_SNDTIMEO                                                       见 章 )
                                               |                       |   服务 类  型
    IPPROTO_IP                 tos
    vein)                 |        TI          |     mm

                               IPV6_RECVPKTINFO|                           接收
    IPPROTO_IPV6          |                          int               |     分组    信息
                               |               | int
                               IPV6_DONTFRAG
                                               |     int               |   接收 通信 类 型
                               |_IPV6_RECVTCLASS
                               _TCP_MAXSEG                                 TCP
    IPPROTO     TCP                            | int          |              最大报文庙大小
    (TCP 选项    )               NODELAY
                                               | int                   |     Nagle
                                   函数返回                                            失败
                                                                                   返回  -1             ermo。
             getsockopt 和 setsockopt  这 两个     成功时                           时          并 设置
                                   服务 有 部 分                                只 能 在                           调用 listen 系统
        值得 指出的                 ，对     器而言，            socket  选项                                      调用前             监
                          是                                                                                   针对
        设置才有效。                               这 是 因为
                                   连接          socket
                                                           只能由 accept        调用 返回      ，                      而 accept
 听 sockets                                                                                                 从 listen thi
 听 队 列 中 接受     的         连接 至 少 已 经 完成           了TCP 三   次 握手 的 前 两 个                 listen                  监听 队列 中
    连接                         SYN_RCVD 状态 参 见 3-8            和 代码             S-4)，     这
 的      至少已进入                                  ，     图                     清单           说 明           服 务      器 已 经往 被

S ”确切 地 说，socket在 执行listen调 用 前 是不能 称 为监听 socket 的 ，此 处是指 将执行 listen 调用 的socket。

## Page 107

                                                                                第 5 章  Linux  网 络  编程基础 API  **      89

     连接上发送出了                TCP              报文段               有的                      却应该在                         段中设
 接受                         同步 。 但                                        socket   选项           TCP              同步 报 文
         如      TCP  最 大 报    文 段 选项      〈       回
                                                  忆                      小
 置 ，比                                                              节，       该 选项
                                                   3.2.2                           只能由   同步 报 文 段 来 发 送     )。        这
                                                                                                                      对
                            人员提 供             的 解决 方案                   :     对 监听 socket 设置
 种 情况 ，Linux           给 开发                                        是                            这些 socket HEM,      IBA
         返回 的 连接            将 自动
 accept               socket                  继承     这 些 选              。       些     选项
                                                                   项        这 socket         包括    ，       SO_DEBUG、SO_
 DONTROUTE,.        SO_KEEPALIVE,             SO_LINGER,                    SO_OOBINLINE.       SO_RCVBUF.          SO_
 RCVLOWAT.          SO_SNDBUF.     SO_SNDLOWAT, TCP_MAXSEG 和 TCP                                    NODELAY。        而 对
         而言，        这
                    些 socket     选项 则 应 该 在              调用                                函数
                                                                                   之前 设置     ，因
 客户                                                                                             为                    调用
                                                               connect                              conneet         成功返
 回 之     ，TCP       三 次          已完成。
         后             握手
     下 面 我 们 详细讨论 部 分 重要 的                    socket 选项          。

 5.11.1         SO_REUSEADDR 选项
              们在 3.4.2 TIER     TCP 连接
     我                                                   的TIME_WAIT                状态 ，           并 提 到 服务 器 程序 可以 通过 设
 置 socket       选项   SO_REUSEADDR            来 强制 使 用 被 于
                                                                                        处 TIME_WAIT 状态 的 连接 占用 的 socket
 地 址     。    具 体 实 现 方法    如代码 清单        $-9  所 示       。
                                             代码 清单        5-9      ”重用 本 地 地 址

     int        sock =  socket(   PF_INET,     SOCK_STREAM,                 0 );
     sock >= 0 );
int reuse = 1;
setsockopt( sock, SOL_SOCKET, SO_REUSEADDR, &reuse, sizeof( reuse ) };

     Struct         sockaddr_in   address;
     bzero(      ésaddress,   sizeof(     address         )    };
     address.sin_family     =     AF_INET;
     inet_pton(     AF_INET,     ip,  saddress.sin_addr                     );
     address.sin_port         =   htons({     port   );
int ret = bind( sock, ( struct sockaddr* )é&address, sizeof( address ) );

           过                的 设置
                            之后                ， 即
                                                 使             处
     经                                                             于                  状态      ， 与 之 绑 定 的             地
                setsockopt                           sock                                                   socket    址
                            外，     们也          可以        通过    修改 内 核           参  数 /proc/sys/neUipv4/tep_tw_recycle 来
 也 可以 立即 被 重用。此             我
         回收被关闭的         socket，   从 而 使 得      TCP                 根本就不进入                           ，                 应
 快速                                                       连接                        TIME_WAIT                 状态 进 而 允许
                                            址。
 用        程序立即 重用 本 地 的     socket   地
 5.11.2         SO_RCVBUF 和 SO_SNDBUF 选项
                                                         分别      表示 TCP 接收 缓冲            区       和 发 送 缓冲区  的  小 大   。不
     SO_RCVBUF 和 SO_SNDBUF 选项
 过 ， 当 我 们 用        setsockopt    来 设置    TCP 的          接收 缓冲              区 和 发 送   缓冲 区 的 大 小     时，    系 统 都 会 将其 什
   倍，                                                                   缓冲            是              ，
 加 并 且 不 得          小   于     某 个 最 小     值  。TCP        接收               区 的 最 小 值      256  字 节     而 发 送       区 的 最
                    节   不
 小 值       是2048 字          过，    不 同                可 系统 能    有 不      同 的   默认   最 小 值 )。   系 统 这 样         做 的 目的 ，主
                                          的                                                                           要
 是 确保 一         个TCP        拥有是够的                        缓冲             处          塞            传      望
                                                                                  连接 空闲 区 来 理 拥 〈 比 如 快速 重 算法 就 期 TCP 接

## Page 108

篇    服务 编
90 «f+ 第 二 深入 解析 高 性 能 器 各


                                                 为                                     外，         们可
收 缓冲 区 能 至 少 容纳                   4  个    大小     SMSS         的     TCP  报 文       )。 此        我      以 直接 修改 内   核 参 数
/proc/sys/net/ipv4/tcp_rmem 和                                                wmem     来  强制           接收 缓冲
                                              /proc/sys/net/ipv4/tep_                            TCP       区 和 发 送 组
冲 区              大小   没有                                                     论        内    参
    的                      最小 值 限         制 。    我 们    将 在 第     16  章讨     这     两 个   核       数。
                              一             端                           ，                             ，               们
下 面 我            们     编写 对 客户                   和    服务 器         程序    如   代码    清单 5-10     和 代码 清单     所 示        它
分 别    修改                   区                           缓冲
                 TCP   发 送 缓冲             和 接收           区      的    大小 。
                                          清单                                 缓冲                  程序
                                    代码           5-10           GAL TCP  发 送       区 的 客户 端

            #include   <sys/socket.h>
            #include   <arpa/inet.h>
            finclude   <assert.h>
            #include   <stdio.h>
            #include   <unistd.h>
                       <string.h>
            #include   <stdlib.h>

                          BUFFER_SIZE   512

int main(                 int   argc,     char*       argv[]    )
{
             if(       arge      <= 2   )
             {
                            printf£(    "usage:         ts  ip_address       port_number   send_bufer_size\n",basename
                                        ( argv[0)         )  3
                            return     1;
             }
             const          char*®   ip   =  argv([1);
             int       port     =   atoi(    argv[2)      });

             Struct         sockaddr_in           server_address;
             bzero(             &server   address,        sizeof{         server_address   )   );
             server address.sin_family                        = AF_INET;
             inet_pton(             AF_INET,     ip,                                           );
                                                           =  htons(    port );

             int       sock     =   socket(          PF_INET,    SOCK_STREAM,      0  );
             assert(            sock   >= 0    );

             int       sendbuf       =  atoi(         argv(3)    );
             int       len    =      sizeof(  sendbuf         );
             /*                                                              */
             setsockopt(               sock,           SOL_SOCKET,   SO_SNDBUF,    ésendbuf,      sizeof{ sendbuf ) );
             getsockopt(               sock,           SOL_SOCKET,   SO_SNDBUF,    é&sendbuf,     ( socklen_t* )&len );
             printf(            “the   tcp   send       buffer     size      after setting    is  td\n",  sendbuf });

            if     (   connect(        sock,    (     struct     sockaddr*     )&server_address,      sizeof
                       (    server     address        ) )    !=  -1  )
             {
                            char    buffer[              BUFFER SIZE );
                            memset   (  buffer,         ‘a',    BUFFER_SIZE     );
                            send(      sock,  buffer,         BUFFER_SIZE,     0   );

## Page 109

5章  基础
第   Linux 网 络 编程 API 91



     )

     close(    sock    );
     return    0;
 }
一

                            清单                     缓冲             程序
                      代码          5-11     ”修改 TCP 接收         区 的 服务 器

 #include  <sys/socket.h>
 include   <netinet/in.h>
 #include  <arpa/inet.h>
 finclude  <assert.h>
 #include  <stdio.h>
 Winclude  <unistd.h>
           <stdlib.h>
 finclude  <errno.h>
 #include  <string.h>

 #define BUFFER_SIZE        1024

 int main(   int   argc,    char*     argv[) )
 {
                                                                  .
     if(   arge    <=  2   )
    {
    printf( “usage: ts ipaddress port_number recv_buffer_size\n", basename
    ( argv[O) )
             return   1;
     |
     const      char*  ip   =   argv[1];
     int   port    =  atoi(     argv(2)   );

                        sockaddr
     struct                   in  address;
     bzero(    saddress,       sizeof(     address ) );
     address.sin_family           =   AF_INET;
     inet_pton(       AF_INET,      ip,                        );
                       port
     address.sin               =  htons(   port   );

     int   sock    =     socket(  PF_INET,    SOCK_STREAM,    0
     assert(     sock >=     0   );
     int   recvbuf     =   atoi(     argv[3]  );
     int   len   =     sizeof(   recvbuf   );
     /*         KRETCRRKRERHAD,                       */
    setsockopt( sock, SOL_SOCKET, SO_RCVBUF, &recvbuf, sizeof( recvbuf ) };
     getsockopt(       sock,     SOL   SOCKET,    SO_RCVBUF,  Srecvbuf,  ( socklen_t* )élen  );
     printf(     “the tcp      receive     buffer  size after   settting   is %d\n", recvbuf );

     int   ret   =   bind(     sock,  (    struct sockaddr"   )&address,   sizeof( address ) );
     assert(     ret !=     -1   )

     ret   = listen(       sock,   5  );                          -
     assert({    ret   !=   -1   );

## Page 110

篇  性 编程
92 ++ 第 二 深入 解析 高 能 服务 器



 struct  sockaddr_in      client;
 socklen_t     client_addrlength       = sizeof( client );
 int     connfd = accept({   sock, (   struct sockaddr* )&client,    );
 if  ({  connfd  <    0 )
 |
         printf(      “errno  is:  td\n", errno  );
 }
 else
 {
         char   buffer[    BUFFER_SIZE    ];
                      buffer, '\0',    BUFFER_SIZE );
    while( recv( connfd, buffer, BUFFER_SIZE-1, 0 > 0 ){)}
    close( connfd );
 }

 close(  sock    )                         .
 return  0;
}


   我们 在 ernest-laptop            上 运行    代码 清单                所             的 服务 器 程序〔〈名
                                                              $S-11                                          为 set_recv_ buffer)，然
       在                                                                    端程序                                            向
后  Kongming20 上                 运行 代码 清单 5-10                 所 示 的 客户 (名 为                              set_send_buffer)        来
                                                                                                                                服务
Ae      ACHE            字 节 的 数据   ，          用                                                                           。      作
                 512             然 后    tepdump               抓 取 这     一   过  程        中 双方 交换 的      TCP     报 文    段       具 体操
过 程        如     下 :

   5     ./set_recv_buffer              192.168.1.108         12345     50     #    将       TCP 接收 缓冲区 的 大小 设置为50 字 节
   the tcp                receive    buffer size          after  settting       is  256
   $                                    192.168.1.108             12345  2000      #                                      2000  $9
   the tep                send  buffer  size     after          setting  is 4000
   > tepdump              -nt -i ethO       port         12345
                                        ，                 的                         区最
   从 服务 器                    输出 来           系 统 允许                                             小为          节。 我们
                                                              接收 缓冲                                    字        当
的                                看    TCP                                                       256                         设置 TCP
       缓冲区的大                 小  为               ，         将                                 。              输出            ， 们
接收 字 节 时 系 统 忽略 我                                                       们 的 设置              从 客户 端的             来                我
                                                                                                                      看         设置
                          缓冲            被             增加      。                       情况                                  。    是
的            TCP  发     送    区 的 大小         系统            了 一    倍     这 两 种                和 我 们 前 面 讨    论 的  一     致   下 面    此
次 TCP                   的
                            通信 tcpdump 和输出:
   1，IP                 192.168.1.109.38663           >   192.168.1.108.12345:     Flags           [S$],   seq  1425875256,    win
                  14600,  options      [mss   1460,sackOK,TS     val        7782289 ecr         0,nop,wscale          4), length 0
   2.             IP    192.168.1.108.12345           >   192.168.1.109.38663:     Flags           [S.],   seq  3109725840,
    ack 1425875257, win 192, options [mss 1460,sackOK,TS val 126229160 ecr
                  7782289,nop,wscale            6],   length    0
   3.             IP    192.168.1.109.38663           >   192.168.1.108.12345:     Flags           [.]),   ack  1,    win 913,
                  length  0
   4.             IP    192.168.1.109.38663           >   192.168.1.108.12345:     Flags           [P.],   seq  1:193,     ack l,
                  win   913,    length  192
   5.             IP    192.168.1.108.12345           >   192.168.1.109.38663:     Flags                   ack  193,  win   0,
                  length  0
   6.             IP    192,.168.1.108.12345          >   192.168.1.109.38663:     Flags           [.],    ack  193,  win   3,
                  length  0
   7.             IP    192.168.1.109.38663           >   192.168.1.108.12345:              Flags  [P.],   seq       193:385, ack l,
                  win   913,    length  192

## Page 111

第 5 章 Linux 网 络 编程基础AP| 93


    日 。工三 192.168.1.108.17345             >      197.168.1.109.38663:     Flags   [.],   ack     385,    win  3,
         length  0
    9.   IP  192,.168.1.109.38663         >      197.168.1.108.12345:     Flags   (P.],      seq   385:513,   ack   1,
          win  913, length    128
    10.   IP  192.168.1.108.1234S              >    192.168.1.109.38663:    Flags    [.],   ack     513,  win   3,
          length    0                                                               4
    ll.   IP  192.168.1.109.38663              >    192.168.1.108.12345:    Flags     [F.],     seq  513,   ack   1,
          win  913, length    0
    12.   IP  192.168.1.108.12345              >    192.168.1.109.38663:    Flags     [F.],     seq  1,  ack  514,
          win  3,   length    0
    13.   IP  192.168.1.109.38663              >    192.168.1.108.12345:    Plags    [.],   ack     2,  win  913,
          length    0
    首先 注意第         2     个 TCP 报 文 ，它 指出 服务 器 的 接收 通告                           窗口             为         节。         值小
                                          段                                          大小         192                字 该
                   之中。同
于 256 字 节     ，显 然是在 情理                          时，该 同步报 文    段    还指出       服务 器 采用 的              窗口         扩大 因子 是
                                                                                                                    窗口
                                                                                                                  小
6。 所以 服务 器 后 续 发 送    的    大 部            分TCP 报 文       段    (6、8、10 和 12        的 实际 接收 通告                    大    都
                   节。    因 此 客户 端 每                      次  最 多 给 服务 器 发     送    192  字 节 的 数据           。 客 户   端  一
是             节 ， 即 192 字
            器发送了     5$12     字 节 的 数据 ，这 些数据 必须 至 少       分                 为  3个   TCP    报 文        段      (4、7 和 9
共 给 服务
来 发送    。
               的是     报 文段        和              当 服务 回收 到 客户 端 发送过 来 的 第                        批  数   据            报
    有 意思           TCP                6。                                                    一             〈TCP       文
                                                                                                                的大小为0。
段 4 时，它 立即 用 TCP 报 文段 4                          给予了 确认，但 该确认 报 文 段               的          接收 通告 窗口
这 说 明 TCP      模块 发送该确认 报 文 段                    ，应    用 程序   还没 来    得     及 将 数据 从 TCP 接收        缓冲         中     出
                                                 时                                                               读   。
所以 此 时 客户 端是不 能 发送数 据 给 服务 器                             的    ，直 到 服务 器     发送 一   个                重复 的 确认 报 文段  CTCP
报 文     段6    来扩大 其 接收 通告         窗口    。
5.11.3                                              SO_RCVLOWAT 4 SO_SNDLOWAT 选项
                                                                                            区   和 发 送            区 的 低
    SO_RCVLOWAT 和 SO_SNDLOWAT                          选项 分 别 表示 TCP 接收 缓冲
   位       。它          被                         调用        第  )                                           或        。
水
     标记         们   一    般    VO 复 用 系统                ( 见    9 章 用 来 判断          socket      是   否 可 读        可  写  当
                                                                                  用  系统 调用 将                        通知
TCP  接收 缓冲区 中 可 读数 据 的 总 数 大 于 其 低 水 位标记 时                 ，LO 复                                                  应用程序
可 以 从 对 应 的 socket     上 读 取 数据         :        当 TCP                               发 送 缓冲区 中 的 空闲 空间 〈 可以 写人数据 的空间 )
                                 用系统             调用 将 通知 应 用    程序                可以 往 对 应 的 socke  上 写 人 数据
大 于 其 低 水 位标记 时     ，LO     复                                                                                    。
    默认 情况          下     ，TCP 接收 缓冲            区 的 低   水位标记        和 TCP    发 送     缓冲 区    的    低  水   位     标记 均 为 1
  节。
字

5.11.4      SO_LINGER 选项

                   选项
                               控制     close                                    系统 调用 在 关闭 TCP 连接
                                                                                              行为    默 认 情 况       下
    SO_LINGER      用于                                                             时的            。                  ，
我们 使     用 close       系统 调用 来 关闭     一        个 socket HT, close 将 立即 返回            ，TCP                模块负责 把该 socket
对 应     的TCP   发 送 缓冲区 中 残留 的 数据 发送给 对 方                   。
                                                                       项                    们   需要 给 setsockopt
                                                           的                 值  时   ，
      fn 225-5 Pras,             (获取 ) SOLINGER 选                                     我
                                 个                         结构
                                                                              体，其 定义
(getsockopt)    系统 调用 传递 一            linger         类 型 的                       如下:

## Page 112

 94 第 二篇 深入解析 高性能 服务器编程


      #include   <sys/socket.h>
      struct     linger

                                                                          是关闭 《0)  RAM     “/
            int      l_onoff;               /*                      FG CHO) 还
            int  1l_linger;                 /*         MRS         «/
      }i
      根据    linger  结构 体 中 两 个成员 变 量 的 不同 值,close                             系统 调用可 能 产生 如 下         3    种          之一，
                                                                                                                       行为
                 仔等于       0。    此
                                      时                           SO_LINGER 选项
                                                                          不起作用     ，close 用 默认行为
      回 Lono                                                                                         来关闭      socket.
                   #44                    等
                                                       于0。     此
                                                               时        系统
                           0,                                      close       调用   立即    返回，TCP     模块          将 丢弃被  关
              的  socket    对 应    的   TCP   发 送 缓冲
          闭                                                 区      中残留     的 数据 ， 同 时 给 对 方 发        送 一 个       复位 报 文 段
                           节)。    因 此     ，这 种          情况   给 服务     器 提供 了 异常     终止     一 个连接 的 方法         。
             3.5.2 小
      LJ           不为                     大于0。                                         个  被
          Lono 0，L_linger                                  此 时 close     的    行为 取决 于 两     条 件       :    一         是 关闭
          的 socket  对 应的          TCP    发 送 缓冲区 中 是 否还有 残留 的 数据                       ，    二 是      该 socket     是    阻塞
                                    的。对        阻塞
          的 ，还 是非 阻塞                      于               的  socket，close       将  等     一   段 长为             的时间，
                                                                                       待     1                   linger 直
                                                                                                                        没
          到 TCP 模块 发 送 完所有 残留数据 并得 到 对 方 的 确认。如 果 这 段时                                                         间 内 TCP 模块
          有 发送完                数据 并得 到 对 方 的确认，                          那么 close  系统 调用 将 返回        -1 并 设置      ermo  为
                                          果 socket
          EWOULDBLOCK。                    如               是非       阻塞    的 ，close  将  立即 返回    ，           此 时 我们 需要 根据 其
                 值和     errno    来 判断 残留                数据是 否 已 经 发 送         完毕   。 关 于     和非阻
          返回                                                                           阻塞 ， 我                     们 将 在 第
          8    章 讨论。

 5.12                  网 络 信息    API
      socket          地 址 的 两    个要   素  ，即 了       了   地 址 和      号，         是        表示的。
                                                               端口 都 用 数值 这 不 便于 记忆                                ，     也
                     从          IPv4    转移
 不便于 扩展 【比如                               到         IPv6)。     因 此 在 前 面      的    章节     中，我 们 用 主机 名 来          访问 一  台
 机 器    ， 而 避免 直接                用 其                   。同
                           使          IP 地                样，   我 们 用 服务 名 称 来          代替    端口      。   如，
                                               址                                                  号             比 下 面 两 条
 telnet   命令具有完全 相同               的 作用    :
      telnet        127.0.0.1    80
      telnet     localhost       www
                         中     ，telnet   客户 端 程序          是    通过 调用 某 些 网 络信息 API     来          实现 主机 名 到          IP 地
      上 面 的 例子
 址 的 转换 ，以 及 服务 名 称 到 端口号的转换                                       。    下 面 我 们    将 讨论    网 络信息  API    中            重要的
                                                                   的                                                   比较
 几 个  ，

 5.12.1      gethostbyname 和 gethostbyaddr

     主机 主 机 的 完整信息 ，gethostbyaddr 函数 根据 IP
gethostbyname 根据 名称获取
 地 址 获取 主机 的 完整 信息 。gethostbyname 呆                                通常先在 本 地 的          /etc/hosts               配置 文件 中 查
 找 主机，如 果 没有找到，再 去 访问                                   DNS        器。                  章节                     。      个
                                                            服务 这 些 在 前 面               中 都 讨论              过        这 两 函
 数的          如下:
      定义

## Page 113

    第 5 章 Linux 网 络 编程基础
        API     95


        #include         <netdb.h>
        struct      hostent*        gethostbyname({        const       char*   name  });
        Struct      hostent*        gethostbyaddr(         const       void*   addr, size_t     len, int type     );
                    参数指定 目标 主机 的 主机                                                                      地 ，len 参数 指定 addr
        name                                             名   ，addr             参数 指定 目标 主机    的 IP   址
    指                                                             指                      ，        取值包括           INET
    所 IP       地    址 的 长       度   ，type          参数 指定 addr 所 IP        地 址    的 类    型   其合法                AF_
                                                                  址 )。
    于 IPv4  地 址) #l          AF_INET6                (FA IPv6 地
        这 两个函数 返回 的 都是                           hostent 结构 体 类 型         的    指针 ，hostent      体的     如
                                                                                   结构
                                                                                                              定义 下
        #include         <netdb.h>
        struct      hostent
         {
            char*         h_ name;                          /mw   FBLA     */
            char**           haliases;                      /*                     */
            int         h_addrtype;                         /*    Mab RA                “7
                                                                          ME   +/
            int         h_length;                           /*    SK
            char**           h_ addr     list               /*     按 网    阁 字 节 序列   出 的主机    IE   地 址列表

    5.12.2                                      getservbyname 和 getservbyport
        getservbyname           pA      He Hi 24    FAK KALE 4S HR     95 A  SE      getservbyport 函数 根据            端口 号
        某个服务             的   完整信息       。 它们 实际 上 都是通过 读 取                         文件
    获取                                                                             来    获取          服务                信息
                                                                                                                  的   的。
这 两 个图数 的定义 下
        如 :
    #include <netdb.h>
    Struct servent* getservbyname( const char* name, const char* proto );
        struct      servent*        getservbyport(           int  port,     const   char* proto    );
                    参数 指定 目标 服务 的
        name                                           名字 ，port   参数      指定     目标  服务   对 应 的     端口   号 。proto  参数 指定
    服务 类            ，给 它     传递“tcp”     表 示 获 取 流 服务                  ，给它 传递“udp” 表 示 获 取 数 据 报 服务                  ， 它
               型                                                                                                       给
    传递  NULL 则 表示 获取 所 有 类 型                           的 服务  。
                                                              体类型的指针，              结 构 体     的 定义
                    PRK [Fl ABE         servent        结构                          servent      如下;
        #finclude        <netdb.h>
        struct      servent
        {
                    char*    s_ name;                       /*    RGR          */
                                                                                   能有           多个 */
                    char**   s  aliases;                    /*         务 的别名 列表，可
                    int  3 port;                            /*    MOP      “/
                    char*    s_proto;                       /*    服务 类      型    MEE    cop     或者 udp */

        下 面 我 们 通过 主机 名 和                              名来访问                                                        该机器的系
        服务 目标 服务 器上 的 daytime 服务 ，以 获取
    统 时间，如 代码 清单                 $-12         所 示  。

                                                   代码    清单  5-12     ”访问 daytime 服务
                         <sys/socket.h>
        #include         <netinet/in.h>

## Page 114

96 - 第 二篇 深入解析 高性能 服务 器 编程



        Hinclude      <netdb.h>
        #include      <stdio.h>
        #include      <unistd.h>
        finclude      <assert.h>

        int    main({ int            char                       )
        {
               assert(     arge     == 2       );
               char   *host =              argv[1];
               /*    获取 目标 主机 地 址 信息
               struct    hostent*                 hostinfo   =  gethostbyname(    host
               assert(     hostinfo         });
               /*   RMR  daytime              服务 信息     */
               Struct    servent*                 servinfo   =  getservbyname(               "tep"   );
               assert(     servinfo         );
               printf(     “daytime         port is                  ntohs{ servinfo->s_port     )   );

               struct                           address;
                                                =    AF_INET;
               address.sin_port             =   servinfo->s_port;
               /*       章 下     面的代码 ，          为    hb_addr_list    本 身 是 使用网    络 字 节    序 的 地 址 列表，所 以 使  用其  中 的 IP 地
det.                     地          字节序
                                             址转换 */
                                                                                                       list;
               address.sin_addr             =   * struct         in_addr*                )*hostinfo->h_addr_

               int    sockfd     =  socket(          AF_INET,       SOCK_STREAM,   0     );
               int    result =                       sockfd,     (struct   sockaddr*     )&éaddress, sizeof( address )  );
               assert(     result      !=       -1   );

               char    buffer[(128];
               result    =    read(    sockfd,            buffer,   sizeof(    buffer    ) );
               assert(     result        >  0   );
               buffer[     result      ]    =   '\O';
                           “the        day  tiem         is: ts",   buffer  );
               close(    sockfd         );
               return    0;
        }

                                                                                             的。
        需要 指出 的                ， 上 面 讨论            的 个 函数            都 是不可  重    人       ， 即 非 线 程安全     不
                           是                         4                              的        过    mnetdb.h
                                               本。正
头 文件 给         出                     了 它们 的 可 重 人 版          如 Linux 下 所 有 其 他           数 的 可 重   人版 本 的   命名规则 那样     ，
            数       函数 名 是  在                     名          加上
这 些            的               原 函         数                 尾部  _r  (re-entrant)。

5.12.3         getaddrinfo
                                                         名获得
                                                                     趾地
                                                                         址(内 部             使用  是   gethostbyname eh
        getaddrinfo 函数         既    能  通过 主机                                                的
                                       号    〈 内 部 使 用 的
也 能 通过 服务 名 获得 端口                                                   是   getservbyname        )。    它 是 否 可   重人 取决     于 其
               的    gethostbyname 和 getservbyname                                        TIN A AMA.      EM
内 部 调用
      下 :
如

        Finclude      <netdb.h>

## Page 115

第 5 章 Linux 网 络 编程基础 «+ 97


      int     getaddrinfo(              const     char*      hostname,        const     char*     service,     const         struct
addrinfo*       hints,         struct     addrinfot*         result          ) ;

                                                             也 以 接收 字符 串 表 示 的                                         中 地 (IPv4 采用
      hostname         参数 可     以 接收 主机 名         ，              可                                   址                          点分十
            制字符串               则 采用 十 六 进 制              字符      串                同
进                   ，IPv6                                              )。      样     ，service 人参数 可以 接收    服务 名，              也 可 以
接收 字符 串 表 示 的 十 进 制                           端 口号     。hints    参数 是             应  用 程序 给 getaddrinfo     的     个 提示            以，
                                                                                                                 一                对
getaddrinfo     的 输出 进行更精确的控制                               。hints        参数 可以 被 设置     为NULL，      表          示人允许    getaddrinfo
反馈任何 可 用 的结果。result 参数 指向                                     一                   该 。 个 链表， 用于存储                        的
                                                                              链表     getaddrinfo 反馈
                                                                                                                          结果
      getaddrinfo                                反馈 的 每一条 结果 都是  addrinfo     结构    体 类 型    的 对     象     ，结 构 体 addrinfo     的 定义
       下:
如

      Struct    addrinfo
        {
                int                                              /*       REX */
                int    ai_family;                                fw
                int    ai_socktype;                              /*       服务 类  型      ，SOCK_STREAM    SOCK     DGRAM */
                int    ai                                        /*       见 后文 */
                socklen_t               ai_addrlen;              /*       socket  地址   ai addr 的 长     度    */
                                                                             下 的
                char*      ai_canonname;                         /*       主      别省    */
                struct           sockaddr*    ai_addr;           /*       指向 socket 地址 */
                                                                          指向 sockinfo 结构 的 对               */
                struct           addrinfo*    ai_next;           /*           下一个                          象
      hi
                                                                                                                                  个
      该 结构体中                  ，ai_protocol                                    A     SCA     socket                      系统 调用 的 第 三
                               被设置           为 0。ai_flags         成    员  可以 取 表 5-6     中 的 标志 的 按 位 或    。
参数 相同 ，它 通常

                                                                              员
                                                  表 5-6                   ai_flags 成
          z         项                                                         &      义
                                          在 hints        中           ，        将
                                                                 参数
                                                             设置 表 示 调 用 者 是 否 会     取得 的 socket                                 RFK
                                          通常                                  任何 socket 地 址 的 服务           客 户                 端 程序
AL_ PASSIVE                             器     需要设置它，             表 示 接 受      本地     上                     请求。                   不能
                                             它
                                        设置
                                                                                                       的    别名
AL       CANONNAME                        在 hints        中设置 ，告诉              getaddrinfo             函数 返回 主机
AL NUMERICHOST                          oe          “                  A hostname     UAE AE               IP           Ain
         NUMERICSERV                    “en                                   service  数 使用   十 进 制        的字符
AL                                                                                                     端              形式，   而 不 能 是

                                          在 hints     数        中 设置    。     果        为                               没有
                                                  被           时        如 ai_family     被 设置                  当                 条件 的
Al_VSMAPPED                                                 ，                 为      址
                                        IPv6  地 址 找到 将 TPv4 地 址               射 TPv6 地
                                              和     AL_V4MAPPED 同时
AL       ALL                              必须                                  使用，     理 则 将      。表        示 同 时 返回   符 台 条 件的 IPv6
                                                                                         址
    一                                   地 址以 及 由 IPv4     地址                 得到的    IPv6 地
                                          仅 当 至 少 配置有       一             个 IPv4 地址                     回路 地址) 时，才 返回 IPv4 地 址信息 ，同
                                                                              址《除了 回路
AL       ADDRCONFIG                     样 ，仅 当 圣 少 配置有                     个 IPv6 地
                                                                  一           地址) 时，才 返回                   IPv6          地 址 信息  。它
                                        和 AL V4MAPPED        是 互          斥  的
                           用              参数 的 时            ，    可 以 设置
                      当 我 们 使  hints                      候                   其    ai flags，ai_ family，ai_socktype           Al ai_
                                                                              如，    代 码     清单             $-13 利用 了 hints       参数
protocol               四 个 字段  ，其 他 字段 则 必须 被 设置 为NULL。                       例

## Page 116

 二 深入 解析 高 性 服务 器 编程
 98 第 篇      能


 获取 主机                         上
             ernest-laptop         的   “daytime”       流 服务 信息     。

                                       代码 清单5-13       ”使用getaddrinfo 函数

         struct     addrinfo   hints
         struct     addrinfo*      res;

bzero( Shints, sizeof( hints ) ) ;
hints.ai_socktype = SOCK_STREAM;
geétaddrinfo( "“ernest-laptop", “daytime”, S&hints, 4res );

         SARS 5-13             中 我 们     能 分       析出   ，getaddrinfo 将 隐 式 地分 配 堆 内     存       〈          可以 通过 valgrind
                为                  指针 原本 是 没有指向 一 块合法 内 存                的  ，
 等 工具查看 )，因                                                                             所
                            res                                                         以 ，getaddrinfo 调用 结束 后 ，
 我 们 必须 使用 如 下 配对                  数 来     释放 这 块 内 存

         finclude     <netdb.h>
         void freeaddrinfo         ( struct     addrinfo*    res   );

 5.12.4      getnameinfo

                                                       址同时 获得          以 字符 串 表 示 的 主机 名     《〈 内 部 使    用              的
         getnameinfo      PA       Wt socket 地                                                                          是
                                                                                                                       取决
 gethostbyaddr        函数) 和 服务名 《内              部 使用的是           getservbyport 函数       )。 它 是 否 可重    人                于
             的        gethostbyaddr 和 getservbyport                  是   否    是 它们  的 可 重 人 版     。                  该 函数
 其 内 部调用                                                                                本        的                     定义
      下:
 如

         #include     <netdb.h>
         int      getnameinfo(     const   struct  sockaddr*     sockaddr,     socklen_t   addrlen,    char*  host,
                                   socklen_t      hostlen,   char*   serv,     socklen_t  servlen,  int     flags );
                          将    返回 的 主机
                                              名存储
                                                         在 host 人参数 指向 的 缓存
         getnameinfo                                                                    中， 将 服务      在               serv
                                                                                        名 存储
 参数 指向 的 缓存                            和               参数
 人                        中    ，hostlen     servlen      分别指定这                      块 缓存 的 长     度 。flags           参数 控制
 getnameinfo     的 行为        ， 它 可以 接收 $-7 中 的 选项                。
                                                表 5-7     flags 参数
             选           项                                               a          &
      NIL     NAMEREQD                      如 果 通 过 socket   地 址   不 能 获得 主机 名        则        一个错误
                                                                         ，                       返回
                                            返回数据 报 服务 。大 部 分同时 支持流和 数据 报的服务 使用相同 的 端口号 来
                                                这两种服务 ，但 端口              是 例外。比
      NT     DGRAM                         提供                          512-~514         如TCP     的 514 端口          提供 的 是
                                           shell                  登录 服务 ，而 UDP
                                                                     的 514    谢     口 提供
         一                                                                            的是      syslog MH (4H /ete/services

      NI_NUMERICHOST                        3B    FF RY IP               fi
      NI_NUMERICSERV                        BPS                          WATER
                                            仅 返 回 主机    域名   的 第  一  部     。  比 如 对    名
      NI_NOFQDN                               将        75 A          缓存  分            主机  nebula.testing.com, getnameinfo
                                           只     nebula     host      中

## Page 117

                                            第 5 章     Linux   网 络    编程基础    API «+             99

 getaddrinfo 和 getnameinfo              成功时 返回     0，  失 败 则 返回 错误   ，可 能 的                    码如表
                                            码                                                   错误
 5-8 ras.

                                            的               错误码
                         表 5-8   getaddrinfo     getnameinfo 返回
                选    项                      a      x
         EAL_AGAIN                      调用 临时失败，提 示 应 用   过后 试
                                                          程序 再
                                        的    aifags {fi
         EAI         BADFLAGS           非法
         EAL     FAIL                   名 称解析 失败
                                            的ai_family 套数
         EAI         FAMILY             不 支持
         EAL_ MEMORY                    内 存 分配失败
         EAIL                           非法
的主机
                     NONAME                 名或服务 名
                                                                             中
         EAL         OVERFLOW           用 户 提 供 的 缓冲区  溢出 仅 发生在 getnameinfo 调用
                                        没有 支持的 服务 ，比 如 用 数据 报 服务 类 型 来查找 ssh 服务  。     为
         EAL     SERVICE                    用     服务        因 ssh
                                   服务 只 能 使     流
                                        不 支持的 服务        。如
                                            类            果 hints.ai_socktype 和 hints.ai_protocol 一
                                            型        不
         EAL SOCKTYPE              致 ，比 如 前 者 指定 SOCK_DGRAM，  后
                                            而 者 使 用的 是IPROTO_TCP，
                                   则 会  触发   这 类 错误
                     SYSTEM             系统 错误 ，错 误 值存储
         EAL                                在    errno     中
 Linux 下  strerror       ph      SUA        转换 成易读 的 字符 串 形式 。     样，
                                            ermo        同 下 面 的
 可将表                     其
 数 5-8 中 的 错误 码 转换 成 字符 串 形 式                  ;

 #include    <netdb.h>
 const   char*       gai_strerror( int  error );

## Page 118

    BOR  级
        高 IO wma


                                                                           像    Linux 基础 IO  (LEM  open        fil   read)
        Linux          提供 了很 多 高 级      的    VO     函数。它              们 并 不
                       写  内 核     模 块时  一          般 要 实现 这 些          VO     数 )，但 在特定    的 条 件 下 却 表现 出      优秀   的 人  性
    那么          常用 〈 编
    能 。本 章 将 讨论其            中     和 网 络 编程相关               的 几        个 ，这 些 函  数    大 致 分  为 三 类 ;
        加     用 于 创建 文件 描述 符               数            ，  包
                                             的                  括  pipe.   dup/dup2 MAX.
        O 用 于 读 写          数据       的  函数    ，    括                                                                      。
        包 readwwritev、sendfile、mmap/munmap、splice 和 tee 函数
        和属性的 ，包
     用于 控制 IO 行为 括 fentl

    6.1     pipe 函数

                                                   一个管道    ，以   实现       进程    间 通 信   我
        pipe           可 用   于 创建                                          。             们将   在 13.4 节讨 论如 何 使      用   管道
    来 实现 进程 间 通 信      ，                                 本 章 只 介绍 其 基本 使用方式              的定义    如下   :
        finclude       <unistd.h>
        int     pipe(  int     fd[{2]   );
        pipe     函数    的参数        是    一 个   包含 两     个    int  型      整数  的    数组 指针    该      成功时返回
                                                                           。      函数     0，                        并 将 一 对
         的文件 描述 符 值                  人 其 人        参数 指向    的 数组 。 如 果 失
    打开                                                                     败，    则 返回     -1    并  设置 ermo。
        通过 pipe        函数 创建的           这        两 个   文件       述 符     fa[0]   和 包 1  分 别    构成 管道 的 两  端 ，      往 fal) 写
                    可以从   fa[0]   读
                                                 出。并
    人 的 数据                                         且   ，fa[0]      只    能 用于 从 管道 读 出 数据，人    fa[1]        则 只 能 用 于 往 管 道
             信数据，   而 不 能 反 过 来 使                 。如   果 要 实现
    写                                                                   双向    的 数据 传输   ，   就 应 该 使 用 两 个 管道   ，
                                                                                                                         默
                                             用                                                                          认情
      下，这     一 对 文件 描述 符 都 是 阻塞的                             。 此 时 如 果 我 们 用 read     系统 调用 来 读 取    一    个 室    的 管道   ，
    况
    则 read                                 将 被 阻塞，直到 管道内有 数据 可读    ;   如 果 我 们 用  write 系统 调用 来 往     一 个 满 的     管道 〔   见
        文)         写      ，                                                有        。
    后 中 人 数据 则                         write 亦    将       被 阻塞，        直到 管道 足够 多 的 空闲 空间 可 用                    但 如 果 应 用
         将    fa[0]    和 fa[1]    都    设置 为 非 阻塞
                                                              的，       则 read 和 write  会有 不 同 的行为     。             关 于 阻塞
    程序                                                                                                             和非
    的 ，                第          。                                     描述        )                                 至
    塞
                   讨论 见   8            如 果 管 道的 写 端 文件  符                  fd[1]     的 引用 计数 CL     5.7 节 减少
    即 没有任何 进程          需要         往 管 道中 写 人            该                          管道  的读 端   文件  描述 符 fa[0] 的 read     操作
                                                      数据        ，    则 针对
    将 返回     0，                            即 读 取 到了 文件结束标记 (End     Of File，EOF)     之，
                                                                           ，      反 如 果 管 道     的                读 端 文件 描述
    符       的 引用 计数 减少至0，                         即 没有 任何 进程               需要   从 管道   读 取 数据   ，  则 针对 该 管道   的   写
    件 描述 符                                           操作                    关 SIGPIPE 信号                                  我
            fa[1] 的 write                   将        失败 ，  并 引发 SIGPIPE 信号     。     于        ，                        们将在
    第 10 章讨论          。
                管道 内 部     传输     数据    是    字 节        ，  这
                                                                和          字 节 流 概念
                             的                                      TCP     的     相同 。    但 二 者    又    有 细微的       区  别 。
    应 用 层 程        序     能往 一个    TCP 连接 中            写 人       少字         ，      于                                    大小和
                                                           多 节 的 数据        取 决 对 方 的 接收 通告                            窗口 的
        的大小。而 管道本 身 拥有一 个 容量 限制 ，它 规定 如 果 应 用 程序不 将 数据 从管
本 端的 拥塞 窗口

## Page 119

章 高级 函数 sa 101
第 6


   读走的话，该              管道      最 多 能 被 写  人多 少 字 节 的 数据      。   自 Linux 2.6.11 内 核   起  ，管 道   容  最 的  大
道
小 默认 是    65536               节。     们                       修改       容量
                     字 我         可    以 使用   fentl    函数 来         管道    〈( 见 后 文 )。
  外 ，socket 的 基础API 中 有 一 个 socketpair 函数。它 能 够方便 创建双向 管道 。 定
     此                                                                                                  其
  如下:
义

#include<sys/types.h>
finclude<sys/socket.h>
int socketpair(int domain, int type, int protocol, int fd[2] );
     socketpair      前 三 个 参数 的 含义 与     socket     系统 调用 的 三 个 参数     完全       相同 ，   但 domain     只 能 使
          本            协议
用 UNIX       地 域               族AF_UNIX，   因 为 我 们 仅         能 在 本 地 使用       这 个 双向 管道  。最 后   一    个 参数
                                 一样，    只 不 过 socketpair     创建    的 这 对 文件 描述 符 都 是       既 可 读 又 可    写
WAI  pipe    系统      调用  的 参数
的                    成 时                         失败
     。socketpair        功        时               返回 -1 JFL   errno.

6.2  dup                         dup2 函数

     有 时 我 们 希望              把  标准    输   人 重 定向 到  一 个 文件   ，或    者 把 标准 输出 重 定向 到     一    个  网      连接 络
(比如          编程
     CGI             )。  这 可以 通过 下 面    的    用 于 复制 文件 描述 符 的         dup 或 dup2     来          :
                                                                                             函数
                                                                                             实现

     #include      <unistd.h>
     int     dup(  int   flle descriptor   );
     int  dup2(      int     file_descriptor_one,   int                     );
     dup  PA BCG)            “A                     PRT                                file_descriptor 44
     的文件 、管 道 或                  网     络 连接。并
向 相同                             者        且         dup    返回 的 文件 描述 符 总   是   取 系         统 当前 可 用 的 最小
值         。dup2 和 dup 类
整数                               似，    不 过 它 将 返回 第    一   个 不小  于 file_descriptor_two 的 整数     值  。dup 和
dup2 系统 调用 失败 时 返回               -1     并 设置errno。

注意   通过 dup 和 dup2 创建 的 文件 描述 符                       并    不继承   原文 件 述         属性 ，     如  close-on-exec
                                                                                比
     和    non-blocking          年。

     代码 清单         6-1 利用 dup 图 数     实现了    一     个 基本      的 CGI    器。
                                                                   服务

                                                                   器原理
                                    代码 清单6-1                    CGI 服务

     finclude      <sys/socket.h>
     #include      <netinet/in.h>
     #include      <arpa/inet.h>
     #include      <assert.h>
     finclude      <stdio.h>
     #include      <unistd.h>
     #include      <stdlib.h>
     #include      <errno.h>
     #include      <string.h>

     int main(       int     arge,        )

             if{   arge      <= 2     )

## Page 120

102 第 二篇 深入 解析 高 性 能 服务器编程



       {
                     printf(       "usage;           ip_address port _number\n",   basename(   argv[0] ) );
                     return      1;
       |
       const       char*    ip     =     argv([1];
       int     port    一     atei({      argv[2]      );

       Struct        sockaddr_in            address;
       bzero(        &address,           sizeof(     address ) );
       address.sin_family                   = AF_INET;
       inet_pton(        AF_INET,              ip,                        );
       address.sin_port                  =  htons(    port   );

       int     sock    =           socket(  PF_INET,      SOCK_STREAM,  0  );
       assert(       sock       >=     0   );

       int     ret   =   bind(       sock,       (    struct seckaddr*  )&address, sizeof( address )   );
       assert(       ret    :=     -1      );

       ret     =   listen(       sock,        5  );
       assert(       ret    !=     -1      );

    Struct sockaddr_in client;
    socklen_t client_addrlength = sizeof( client };
    int connfd = accept( sock, ( struct sockaddr* )&client, &client_addriength );
       if   (    connfd     <    0     )
       |
                     printf(       “errno        is:  条 过      errno  };
       }
       else
       {
                     close  {(   STDOUT_FILENO            );
                     dup(   connfd          );
                                          “abed\n"    );
                     close(      connfd        });
       }

       close(        sock   );
       return        0;
  }
一

          在 代码 清单  6-1 中    ，          我 们  先 关闭 标准 输出        文件      描述   符 STDOUT               其 值  是1)，
                            述符             connfd。    因
然后 复制                     socket 文件                  为     dup 总 是 返回 系统 中 最小    的    可 用 文件   描述    ，    所
                                                                                                   符      以
            实际                                                         描述    。                     到
它 的 返回 值           上 是    1， 即       之                前 关闭 的 标准 输出 文件      符 的 值   这   样一  来   ，服 务 器 输出
                                                     会                                         上，  此
标准 输出  的   内 容     〈 这 里 是 “abcd”)                   就  直接 发 送 到    与  客户 连接 对 应    的   socket 因 printf
调用 的 输出 将 被 客户 端 获得                                                   程序      上  是                 器   的基
                                     〈     而 不 是     显示 在 服务 器            的 终端   )。 这 就    CGI    服务
本工作 原理   。

## Page 121

    6章 高级1O 函数 w%* 103
    第


                                   和
    6.3     readv 函数                        writev BA

                                                                                        分散  读                            函数
        ready           将 数据 从     文件           描述 符 读 到 分 散         的内     存 块 中    ， 即         ，writev              则将多块分
    散的   内    存 数 据     一 并   写 人 文件 描述             符     中 ， 即    集中 写。    它们 的        如下:
                                                                              定义

    Finclude <sys/uic.h>
    ssize_t readv( int fd, const struct iovec* vector, int count):
        Ssize_t         writev(      int    fd, const      struct    vector,      int     count    );
                                                                     参数的类型是                      结构 数组    。  我
        fd                        NY                               vector               iovee                  们    在   第 5
讨论过 结构 体 ioveec，                   该 结构 体                  一 块 内 存          。count 是               MALIK BE,
                                                                          区         参数     vector                      HATS
        成功时返回 读出 / 写 人 fd 的 字 节 数 ，
少 块 内 存 数 据 需 要 从 fd 读 出 或 写 到 fd. readv 和 writev 在
             则返回        并 设置                    它 相当           简化
    失败 -1                         erno。          们            于      版的      recvmsg 和 sendmsg      。
                                                                                                 函数
                 第 4                                 Web 服务
                                                             器。当   Web          服务
        考虑                                                                    器解析完一个           HTTP  请 求 之   后 ，如 果 目
      存在且客                    户具  有 读 取 该 文档            的  权限 ， 那      么     它 就需要   发 送    一  个 HTTP    应 答             该文
      标 文档                                                                                                              来传输
    档 。 个                                             行、                               行            的     。    中
        这 HTTP 应 答 包含                       1           个 状态 多 个 头 部 字段       、!     个 空    和  文档 内 容 其               ，前  3
    部 分  的    内 容 可 能     被   Web                   服务 器 放置 在 一块   内 存 中  ，         而 文档 的 内 容 则 通常 被 读  人 到 另外 一       块 单
              的内存中《〈       过          ph                           )。
       独 通                    read      22k     mmap       函数 我           们  并 不 需要 把 这 两 部 分 内 容 拼接 到         一 起    再   发
                          使用  writev 函数
    送 ，而 是 可以                                   将 它们 同时 写            出 ，  如  代码 清单   6-2 Bra.
                                                                              器上的集中写
                                     代码           清单       6-2     Web 服务
        #include        <sys/socket.h>
        finclude        <netinet/in.h>
        #include        <arpa/inet.h>
        finclude        <assert.h>
        #include        <stdic.h>
        finclude        <unistd.h>
        #include        <stdlib.h>
        #include        <errno.h>
        #include        <string.h>
        #include        <sys/stat.h>
        #include        <sys/types.h>
        finelude        <fentl.h>

        #define    BUFFER_SIZE          1024
        /*    定义 两种       HTTP          状态 码 和   信息     */
        Static     const      char*     status_line[2]         =     { "200    OK", "500 Internal  server    error"   1 |

        int main{        int   argc,       char*  argv[]       }
        {
                 if{    arge   <=   3   )}
              {
                          printf(        “usage:  ts    ipaddress      port_number filename\n", basename(    argv[0)  )  );
                          return     1;
              I
              const       char*     ip       =  argqv([l1];
                 int    port =           atei(  argv[2]    );

## Page 122

6 篇  能 编程
104 第 二 深入 解析 高 性 服务 器



/*  和将 目标 文件作为 程序 的                          第三个参数   传  入  “/
const     char*          file  name          =

struct                         in address;
bzero(      Saddress,             sizeof({       address   ) });
address.sin_family                      =    AF_INET;
inet_pton(           AF_INET, ip,                                     );
address.sin_port                 =      htons(     port    );

int   sock        =        socket(      PF_INET,       SOCK_STREAM,   0 );
assert(         sock       >=  0      );

int     ret     =       bind(  sock,          (  struct   sockaddr*   )&address,  sizeof( address ) };
assert(         ret      !=   -1l     );

ret   =   listen(           sock,          5  );
assert(         ret      !=   =1      );

struct                 sockaddr_in      client;
socklen_t           client_addrlength            =     sizeof(  client    );
int   connfd         =  accept{ sock,            {  struct   sockaddr*    )&client,   &client_addrlength ) 7
if (    connfd         <    0  )}
{
          printf(           “errno           is:    td\n", errno
}
else
[
          /*       用  于保存             HTTP   应 答 的状态     行 、 部 字段 和一 个 空 行 的      存 区 “/
          char        header_                   BUFFER_SIZE
          memset        (                    buf,  '\0',   BUFFER_SIZE    );
          /*      用 于 存放 目标 文件 内 容 的 应用程序        存                */
          char*          file  buf;
                                       用 于 获取 目标 文件的 属性 ，比 如 是 否 为 目  ，文
          /*                                     最                        件大小等    */
          struct         stat     file        stat;
          /*      记录 目标 文件 是 否是有 效 文件            */
          bool        valid       =     true;
          /*                header            buf   目前 已 经 使用了 条 少 字 节 的 室 闻     */
          int       len     =  0;
          if(           stat(  file_name,        Sfile_stat  )  <0)       /*     目 标 文 件 存在 */
          {
                      valid       =     false;
          )
          else
          {
                      if(     S_ISDIR(           filestat.st_mode     ) )   /*        */
                       {
                                  valid         =   false;
                       }
                         else  if(      人  ie stac.st_ mode  &  S$ IROTH)   /*     当前用户 有 读 取 目标 文件 的 权限 */
                       {
                                                                            其大小    为 目标 文件的大 小
                                  /*                        file buf， 并 指定
                #1,     REWARXERARAE
file_stat.st_size                                file           buf 中   “/
                                  int        fd   = open(  file_name,   O_RDONLY  );

## Page 123

    6章
    第 高 级  函数 105


                               file_buf     = new    char [     file_stat.st_size   +  1 ];
                               memset(      file   buf, '\Q',   file stat.st_size      + 1 };
                               if (  read(       fd,  file_buf,     file_stat.st_size    )  < 0 )
                              {
                                           valid   =  false;
                              }
                  ]
                 else
                  {
                               valid     =  false;
                  )
            }
            /*  如 果 目     标 文件    有 效  ，   则  发  送 正常   的 BTTP      “/
            if(  valid      }
            {
                            下  面这部分内 容将            HTTP 应 葵   的     行                      和一小
                                                                状态 、“Content-Length”                  头 部 字段
            buf   中 */
行依次加入 header
        ret = snprintf( header_buf, BUFFER_SIZE-1, "ts %s\r\n",
        “HTTP/1.1", status_line[0] );
        len += ret;
        ret = snprintf( header buf + len, BUFFER_SIZE-l-len,
        "Content-Length: td\r\n", file stat.st size );
        len += ret;
                 ret     = snprintf(         header_buf     +  len, BUFFER_SIZE-l-len,     “t%s", "\r\n"  );
                 /* 利用         writev      将  header     buf  # file buf                   */
                 struct      iovec         iv[2];
                 iv[      0    ].iov_base        =    header  buf;
                 iv[      0    ].iov_len      =      strlen(  header  buf   };
                 iv{      1    ].iov_base        =
                 iv[      1    ].iov_len      =    file_stat.st_size;
                 ret      =    writev(     connfd,      iv,   2
            }
            else    /*    Wie                                       “AMBER”                */
            {
                 ret      =    snprintf(                        BUFFER_SIZE-1,  "ts      %s\r\n",
        “HTTP/1.1", status_line[1) );
        len += ret;
        ret = snprintf( header buf + len, BUFFER_SIZE=l-len, "%s", "\r\n" );
                 send(         connfd,                        strlen(    header_buf   ,     0 );
            }
            close(  connfd        );
            delete           file_buf;
     }

     close( sock    );
     return  O;
    I


    代码 清单   6-2  ，             了 HTTP                           ，      现在              是
                 中 我 们 省 略                       请 求    的 接收 及 解析 因 为                   关注 的 重点 HTTP
    应 答 的 发 。我 们 直接 将 目标       文件                               给               ，      户          服务
         送       。  于              作为 第            3 个  参数 传递   服务 器 程序         客         telnet 到 该
        ，
器 上 即 可 获得 该 文件 关 HTTP 请 求 的 解析 我 们 将 在 第 8 章 给 出 相关 代码 。

## Page 124

    106 第 二篇 深入 解析 高性能服务 器编程


    6.4                 sendfile 函数

         sendfile       在 两 个 文件          述  符    之间 直 接 传递       数据    〈 完 全 在 内核 中   操作)，    从 而 避免    了 核
                                                                                                              内
    缓冲区和 用 户 缓冲区 之 间 的 数据 拷贝 ，效 率 很                               ，这 被 称 为 零       。sendfile   数 的 定义
                                                             高        贝                            如下
         #include     <sys/sendfile.h>
         ssize_t       sendfile(int   out_fd,     int in_fd,      off_t*  offset, size_t count );
                      是待读出                      内 容 的 文件 描述
         in_fd 参数                                         符    ，out                                      offset
人参数 指定从 读 人 文件流 的 娜个位 置开始 读 ，如 果 为 空 ， 则 使用读 人 文件流默认 的 起 始 位 置 。
        人纪 间 传 输 的 字 节 数 。sendfile 成
    count 参数 指定 在 文件描述 符                  让   伺       和 out                                    功时返回传输         的
    字 节 数 ，失 败 则 返回            -1  并   设置    erno。     该 函数     的 man     手册 明确 指出     ，in_    必 须 是 一     个 支持
    似     mmap    函数 的 文件描述
    类                                  符     ， 即 它 必 须 指     向 真实    的 文件 ，不 能 是   socket    和     ，  而     out
                                                                                               管道            fd
             是一个                 由
    则 必须              socket。      此   可  见   ，sendfile  几乎     是 专门 为 在 网 络    上传输 文件 而 设计        的  。   下 面 的
    代码 清单           6-3 利用 sendfile   函数 将    服务器    上 的     一  个 文件 传送 给 客户       端 。
                                             清单                     传输    文件
                                       代码         6-3   ”用     sendfile 函数

         finclude     <sys/socket.h>
         f#finclude   <néetinet/in.h>
         finclude     <arpa/inet.h>
         finclude     <assert.h>
         #include     <stdio.h>
                      <unistd.h>
         #include     <stdlib.h>
         #include     <errno.h>
         #include     <string.h>
         #include     <sys/types.h>
         #include     <sys/stat.h>
         #include     <fentl.h>
         Yinclude     <sys/sendfile.h>                              .

         int main(    int    argc,    char*     argv[]  )
         (
               if({   arge   <=  3  )
               {
 printf(                           “usage:   ts              port number  filename\n", basename( argv[0]   } );
 return                            1;
               }
               const  char*     ip   =   argv(l1);
                int   port   = atoi(     argv[2)    );
               const  char*    file    name  =     argv[3];

                int   fllefd   =  open(     file_name,  O_RDONLY     });
               assert({    fllefd   > 0   );
               struct   stat stat_buf;
               fstat(   filefd,     &stat_buf     );

               Struct   sockaddr_in           address;

## Page 125

    6章
    第 高  级IO 函数 «+ 107


             bzero(    &4address,        sizeof({     address  ) });
             address.sin_family              =   AF_INET;
             inet   pton({   AF_INET,          ip,    taddress.sin_addr   };
             address.sin_port            =   htons(   port   );

             int    sock   =       socket(   PF_INET,       SOCK STREAM,  0
             assert(     sock      >=  0 );

             int   ret   =      bind(   sock,      (  struct sockaddr*              sizeof({ address ) };
             assert(     ret   !=     -1 );

             ret    =  listen{    sock,        5   )};
             assert(     ret   :=     -1  3

        Struct sockaddr_in client;
        socklen_t client_addrlength = sizeof( client );
        int connfd = accept( sock, ( struct sockaddr* )éclient, &client_addrlength );
             if  (    connfd   <  0    )
             {
                       printf({   “errno is:          td\n",     errno );
             }
             else
             |
                       sendfile({     connfd,        fllefd, NULL,  stat_buf.st_size );
                                 connfd        );
             }

             close(    sock    );
             return
    }

    代码清单         6-3   中 ，我 们 将 目标 文件 作为 第                              给           ，     户
                                                            3    个  参数 传递   服务 器 程序    客 telnet       到 该 服
                       该文件。       相 比 代码           清单 6-2，   代 码    清单    6-3 没有 为目标   文件分    配 任何 用 户 空间
务 上 即可获得
的 缓存 ，也 没有 执行 读 取 文 件 的 操作，但 同样 实现了 文件 的 发 送，其 效率 显然 要 高 得 多 。
        和
6.5 mmap 函数 munmap 函数

    mmap      函数       用于    申请   一     段 内      存 空  间 。    我 们 可以 将 这 段 内 存 作 为 进程间 通     的 共享 内 存    ，
    也 可以 将 文件直接 映射 到 其                    中    。munmap      A RC FERC     mmap 创建    的 这 段 内 存 空 间   。它 们 的
           如下
    定义      :
     #include <sys/mman.h>
    void* mmap( void “start, size_t length, int prot, int flags, int fd,
                             t   offset        );
                       off
    int     munmap{    void    *start,         size_t   length   );
        参数 允许 用 户 使用某 个 特定                                  地 址 作为 这 段 内    存 的 起 始    地     。 如      果它 设置
    start                                               的                           址        被            成
    NULL， 则 系统 自动分 配 一个地                         址   。length    参数 指定 内 存 段 的 长 度 。prot   参数 用 来 设置 内 存 段

## Page 126

    108          第 二    篇 深入       解析 高 性    能 服务 器    编程

    的 访问 权限 。                    它 可以 取以 下 几 个 值 的  按位 或      ，
          口    PROT_READ，              内 存 段 可 读。
          CY)  PROT_ WRITE，            内 存 段 可 写 ，
          口    PROT_EXEC，          内 存段可 执行。
               PROT_NONE，          内 存 段不能 被 访问      。
                                                                                                   为表 6-1  中 的 某 些    值    (这
          flags   参数 控制内存 段 内 容 被修改                  后     程序        的 行为   。                它 可以 被设置
    里 仅        出         的值)           或             中                                                           ，         能同
               列 了 常用 的 按 位            〈 其 MAP_SHARED 和 MAP PRIVATE                                是 互 斥         的          不
    时 指定       )。

                                        表 6-1  mmap   的 flags        参数 的 常用值及 其 含义
               常用 值                                                          &

               SHARED                 TERY POSIX 方法    存                                                     中
    MAP                            在       进程间 共 享 这  段     内 。对 该     内   存 段的  修改 将 反映 到 被 映射    的   文件    。   它 提供 了 进程

    MAP_PRIVATE                    内 存 段 为调用 进程 所私有              。   对 该 内 存     的修改 不会        反映 到 被 映射 的   文件  中
        这 段 内 存 不是从 文件 射 而 来 的。其 内 容 被 初始 化 为全0。 这 种 下 ，mmap
          ANONYMOUS       |            参数将                                                         情况                 函数
                              的 最 后 两 个             被 忽略
                              wae                  start                        start 必须 是 内 存 页 面 大    小   《4096    节)
    MAP FIXED                                                                                                        字 的

    MAP  HUGETLB              ne       ee 来                    分 配 内 存 空间    .“  大 内 存 页       面”的 大小 可 通过  /proc/meminfo 文
          fd   参数 是 被 映射 文件对 应 的 文件 描述                           。   它   一  般通过   open   系统 调用 获得 。offset                   设
                                                               符                                                           参数
置 从 文件 的 何处开始 映射 (对 于 不 需要 读 和人整个文件 的 情况 )。
          mmap            成功时 返回 指向 目标 内存 区 域                        的 指针   ，                 失 败 则 返回 MAP_FAILED ((void*)-1)
                                      成功时返回                                失败
    并 设置       erno。munmap         函数                                  则   返回    -1 并 设置       ermo。
     们将在第 13 章 进 一 步 讨论如何 利用 mmap 函数 实现 进程 间 共 享 内 存 。
   我

    6.6        splice    函数

          splice     用 于在 两个文件描述 符 之间 移 动 数                            ，    也 是 零 拷 贝 操作           。splice                 函数
                                                                       据                                         的   定义   如下:
          #include     <fentl.h>
          ssize_t   splice(   int     fd_in,   loff_t*  off_in,             int  fd_out,       loff_t* off_out,
                              Size_t  len,       unsigned int           flags    );
          fd_in   参数 是 待 输入数据 的 文件 描述                          。      果                                            么
                                                              符       如    fa_in    是 一 个 管道 文件 描述           符     ， 那 off_in
                                       果     fd_in 不     是           个 管道 文件     述    符        〈 比
    参数 必须 被 设置             为NULL。     如                                                            如 socket)，    那   么   off_
                                                                一                                                          in
    表示 从 输入数据 流 的 何处开始 读 取 数                         。               时，         in                                 从        数
                                                     此 若                    off             被 设置 为 NULL，                 则 表示
                                                   据                                                                  输入
    据 流 当前 但             位 置  读    人       29   off
          的                           ;          in            不 为NULL，     则 它 将     指出  具体 的     偏    移 位    置 。fd_ outoff
        参数
                  含义 与     亿           相同            不     过  用 于 输出        数据     流    。len   参数 指定 移动            的  长     ;
    out          的                     in        ，                                                             数据      度
    flags 参数 则 控制数据                      何 移动  ，它 可以 被设置
                              如                                      为表   6-2    中 的    某 些    值 的 按 位 或   。

## Page 127

                                                                                      第 6 章           级
                                                                                                  高   1O 函数         %* 109

        % 6-2 splice 的 flags 参数 的 常用值及 其 含义
       常用值
        如 果 合 适 的 ，按 整页内 存移动 数据。这 只 是 给 内 核的一个提示 过，
        SPLICE    F     MOVE                的 实现 存在          话   所     2.6.21       它 实际 上 设 有 尾 何 效果         。 不 因 为 它
    一                                                 BUG，       以自内核     后，
        SPLICE    F     NONBLOCK               非阻塞   的 splice   操作，  伍 实际 效果 还会     受 文件   描述 符 本 身 的 阻       状 态 的 影响
                                                                        的  调用
        SPLICE_F        MORE                   给 内 核 的一  个 提示    :，    后续  splice   将读取更      多 数据
        SPLICE    F     GIFT                   对 splice 没有效果
使用 splice                                   fd_in    fd_out      必须 至 少    有     个  是 管道        文件描述          。splice     调
                                                                     一                                符                  函数
    用 成功时 返回 移动 字 节 的 数量                           。 它 可 能 返回       0， 表 示    没    有 数据 需要 移动         ，这 发      生   在 从 管道中
                  据                          文件         )              没有     写                  时                       时返
    读 取 数              〈fd_in    是   管道          描述     符    而 该 管道                       被 人 任何数据    。splice         函数 失败
    le] -1     并 设置       erno。      常 见的       ermo 如 表 6-3     所 示   。

                                            表 6-3    splice         可能产生的    errno  及 其 含义
        ef                                                           ae      义
        EBADF               SMU                    A
        TAFE
        文件系统不支持spliece， 或者目标 文件 以追加 方式打开，或 者两个 文件 描述 符 都 不 是 管道文
        目标
                          PFET, RAR offset 参数 被用于 不 支持 随机 访问 的 设备                       〈   比 如 字符 设备 》
        ENOMEM          |   内 存 不 够
                                                                文件撒述符，    而 off_in 《或 off   out)》 不 为NULL
        ESPIPE              参数 包_in (或         亿out) 是 管道

              下 面 我 们 使用            splice         来实现          个 零 拷 贝 的 回 射服务
                                                         一                            器，    它 将 客户 端 发          送 的 数据 原样
              给客户端，             具 体 实现       如  代码 清单    6-4     所 示 。
    返回

                                               代码 清单6-4     ”使   用splice 函数 实现    的   回 射  服务 器

              #include      <sys/socket.h>
              finclude      <netinet/in.h>
              #include      <arpa/inet.h>
              #include      <assert.h>
              finclude      <stdio.h>
              #include      <unistd.h>
              #include      <stdlib.h>
              #include      <errno.h>
              #include      <string.h>
              #include      <fentl.h>

              int main{         int  argc,    char*  argv[]      )
              {
                       if(  argc     <= 2?   )
                       {
                                   printf(   “usage: %s      ip_address    port_number\n",      basename(     argv[0]  ) );
                                 return     1;
                       }
                       const     char*    ip  =  argv[1];
                       int  port     =    atoi(  argv[2)     );

                       struct                   in      address;

## Page 128

    110 “* 第 二篇 深入 解析 高性能 服务 器 编程



              bzero(                         sizeof(               address    )   );
              address.sin_family                         =     AF_INET;
                                    AF_INET,               ip,                                     );
              address.sin_port               =           htons(    port     );

              int   sock        =            [( socket   PF_ INET,     SOCK_STREAM,              0 };
              assert({        sock    >=  0           );

              int    ret      一   bind(   sock,                (   struct    sockaddr*           )saddress,  sizeof(       address ) );
              assert({        ret !=     -l           );

              ret   =   listen(        sock,               5   );
              assert({        ret '=     -1           );

              Struct                                     client;
              socklen_t           client     addrlength                =  sizeof(          client    );
              int   connfd        = accept( sock,                  (   struct    sockaddr*           )&client,    &client_addrilength  );
              if (    connfd       <   0  }
              [
                        printf(        “errno                  is: %d\n",    errno         };
              }
              else
              |
                        int       pipefd[2];
                                        ret           !=       -1  );
                        ret       -   pipe(           pipefd       );    /*  fait            */
                        /*      # connfd              LRAM                    SA                   */
                        ret       = splice(                connfd,     NULL,     pipefd{1],          NULL,   32768,
                                                                       F
                                                               SPLICE
                                                                            MORE  |
                                        ret           !=       -1  );
                        /*                                     El  connfd                              wy
                        ret       = splice(                pipefd[0],           NULL, connfd,        NULL,   32768,
                                                               SPLICE_F_MORE      |           SPLICE_F_MOVE  });
                        assert(         ret           !=       -1
                        close(      connfd                 j;
              }

              close(     sock       j);
              return        0;
        }

                                                                                                                                        从
        我 们 通过 splice ph                     A           PP        AA FE       A  BI  pipefd[1] 中        ，   然 后 再 使       用 splice    函数
    pipcfd[0] 中 读 出该内 容 到 客户                                 端 ，  从 而 实现     了 简单 高 效 的 回 射 服                   务 。    整个    过 程未执行 recw/
send 操作，因 此 也 未 涉及 用 户 空间 和 内 核 室间 之 间 的 数据 拷贝 。

    6.7     tee   函数

        tee   函数 在 两 个 管道 文件描述 符 之 间 复 制 数据 ，也 是零拷 贝 操                                                            。    它 不 消耗 数据    ，   此
                                                                                                                  作                     因
                                                                                                                     函数
    源 文 件 描述     符   上 的      数据 仍然可以 用 于 后 续 的 读 操                                        。tee      的          如下:
                                                                                      作                原型

        finclude    <fentl.h>
        ssize_t     tee{      int   fd_in,               int      fd_out,    size_t        len,    unsigned  int       flags );

## Page 129

                                                                     第 6     章 级
                                                                               高
                                                                                            IO 函数     ee 111

                            含义                           但           Al                     符
   该          的   参数 的              与 splice 相同        〈 fa in       fd    out   必须 都 是 管道 文件 描述     )。tee
                                                                                            回   0 表示    没有复制 任
上     成功  时 返回 在 两 个 文件描述 符 之                        间 复 制的    数据 数量    〈    字 节 数      )。返
                      时         -1        设置
何 数据   。tee                     失败
                          返回         并          errmno。
                                             和
   代码 清单          6-5 利用    tee     函数          splice PAR,          Linux 下     tee    程序 〈同时 输出 数据 到 终
          的程序，            要和     tee    的 基本 功能                      。
端 和 文件                不

一                            代码 清单6-5 ”同时 输出 数据到 终端和 文件的程序

   //      filename:     tee.cpp
      #include    <assert.h>
      finclude    <stdio.h>
      finclude    <unistd.h>
      #include    <errno.h>
      finclude    <string.h>
      #include    <fcntl.h>

   int main{        int   argc,     char*     argv[]    }
   {
          if   (    arge  !=   2    )
         {
                         printf(    “usage:       %s <file>\n",     argv[0)  );
                      return     1;
         }
          int     filefd =   open(        argv(1],     O_CREAT   |      O_WRONLY |  O_TRUNC,  0666 );
          assert(       filefd   >   0    );

          int     pipefd
          int     ret   =    pipe(   pipefd_stdout         );
          assert(       ret   !=    -1    );

          int     pipefd  file[2]);
          ret     =  pipe(   pipefd file          );
          assert({      ret   !=    -1    );

          /*      将  标准栓  入 内  容    输入       管道 Pipefa   stdout  */
          ret     = splice(     STDIN_FILENO,          NULL,   pipefd_stdout[{1],       NULL,
                                    32768,      SPLICE
                                                         F  MORE   |    SPLICE_F_MOVE   1
          assert(       ret   !=    -1    j);
                                                           到        Pipefd file          */
          /*   4D       pipefd_stdout                   的 给 出 复制
          ret     =  tee(             pipefd_stdout(0],  pipefd_file[1],     32768,     SPLICE_F_NONBLOCK
          assert(       ret   !=    -1l   );
          /* 和将     管道    Pipefd    file                             flefd   上 ， 从 而 将  标准输入    的 内 容写人 文件 */
          ret     =   splice(           pipefd_file[0],  NULL,   filefd, NULL,
                                    32768,      SPLICE_F_MORE      |      F
                                                                        SPLICE
                                                                     MOVE               );
          assert(       ret   !=    -1    );
          /*      将 管道  Pipefd_stdout           的  输出 定向 到 标准 输出     ，    共 内容   和  写 入 文件的 内 容 完全 一  致 “/
          rét     =   splice(    Pipetd              stdout[0]，NULL，    STDOUT_FILENO,   NULL,
                                    32768,                 MORE    |                    );
          assert(       ret   !=    -1    );

          close(    filefd     });
          close(      pipefd_stdout[0)             );
          close(      pipefd_stdout[1]             );

## Page 130

112 + 第 二篇 深入 解析 高 性 能 服务器编程


 close(  pipefd file[0) );
 close(  pipefd_file[1) );
 return  0;
}


6.8     fcntl 函数

                                             的           那样 ，            提 供 了 对 文件 撒 述 符 的 各 种 控制 操
     fentl 函数 ，正     如 其名字 Cfile     control)            描述
作。另 外    一                     个 常见的 控制 文件 描述 符 属性和 行为的  系统 调用 是     ioctl,      AL
                                                                          ii ioctl 比 fentl       能 够
     更多的控制 。     是，    对 于 控制    文件         描述 符     常用  的 属性    和行为     ，fcntl  函数 是由         POSIX
执行           但                                                                                     规
                方法    。所以 本 书 仅 讨论 fentl PHA.     fentl      的        如下
范 指定 的首选                                                 函数
                                                             定义 :

         <fcntl.h>
     int   fentl( int  fd, int emd,      );
     fd             参数 是被操作 的 文件 描述  符   ，cmd 参数 指定 执行 何种类               型 的 操作  。根 据 操 作 类 型 的    不
同 ， 该 函数 可 能还需要 第 三 个 可 选参数                 arg。fentl 函数 支持 的     常用  操作 及 其 参数    如 表     6-4 所 示 。
                           表 6-4     fcntl    支持的     常用操作 及 其 参数
                                                                              第
                                                ，                          三个参数

                                 创建  一  个 新 的 文件 描述   ，    值大于                       文件
        文件描述                   于arg          符     其                               新 创建 的     描述 符
                                                                              |   的
复制                         或 等                                            one     值
符                              与            相似， 不 过 在创建
         F_DUPFD               F_DUPFD                      文件        1             新 创建 的 文件 描述 符
         CLOEXEC           述                                          ong
                               符的同时 ，       设 置 其 close-on-exee 标志                的
                                                                                  值


                           |
                               获取     的               状态标志，这 些 标 志人包括
                            A]   open              系统调用 设置的标志 (O_
      和   设置文|    F_GETFL   |APPEND、O_CREAT 等           》    和 访问
          获取                                                    模           组 的      状态 标志
件 述 符 的   状态                式 (0O_RDONLY、O_WRONLY                和 O_
标志                          RDWR)
                                                             |            |
                               设置 亿 的 状态 标志                   但 部 分标志
                                             ，                     是不       °
                               获得 SIGIO 和 SIGURG        信和 号 的      进       fri
                                                                   宿主   .

                                 定SIGIO     和 SIGURG      {A S978 Hit
                               设

      “7                   |，                                或
             GETSIG.                获取 当 应 用 程序    通 知  ta 可 该    可 写   无   HA,      0  表示
         ~                               fry Se            AY                    SIGIO
                                  当          或          ，           触
                               设置 亿 可 读      可 写 时         系 统 应 该

## Page 131

    6章   高
    第  级IO 函数 *%* 113


        2
        ，        第 三 个 参数

                 由 僻 指 定 的 管道 的 容量 。/proclsyw
                设置

    操作 管道容量    PE  | BR


       函数 返回 6-4 最 后 一 列 所 示 失 败 则返回 -1 FFE errno.
           fentl            成功时的            值如表                ，
                             中，fentl     函数 通常 用 来     将 一 个 文件 描述 符 设 置 为非 阻 塞    的     ，   如 代码 清单6-6
           在 网 络 编程
    STAN.

                                            代码 清单 6-6 ”将 文件描述 符 设 置 为 非        阻塞的
          int     setnonblocking    (    int  fd )
          {
                     int  old_option     =  fentl( fd, F_GETFL };      /sa      获取 文件描述 符旧 的 状态标志
                     int  new_option     =  old_option |   O_NONBLOCK; /*          BARA   +/
                     fentl(  fd,    F_SETFL,  new_option   );
                     return  old_option;                               /*          A
                                                                        *    目后恢复 该状态标志     */
          I

                   外  ，SIGIO
                             Al   SIGURG                这 两 个信号
                                                           与其他 Linux   fA                       SA, EPA
           此                                                                       SER              ICE
    述 符 相关 联 方 可 使               用: 当 被 关联 的    文件     描述 符 可 读 或 可    写 时   ， 系 统 将 触发 SIGIO  (AS;   被
                                                                                                      当
          的文件描述符                                                             可   时，                   信
    关联 〈 而 且 必 须 是                            一 个 socket)    上 有 带 外 数据 读               系 统 将 触发 SIGURG
                。将 信号 和   文件 描述 符 关 联 的         方法  ，  就 是 使 用 fentl   函数 为    目标      文件 描述 符 指定 宿主 进程
或 进程 ， 那么被 指定 的 宿主 进程 或 进程组将 捕获 这 两个信号。
        使
               组                                                                  用    SIGIO 时， 还 需要 利用
    fcntl  设置         其O_ASYNC      标志          VO  标志    ，    过       信号       模型      正       上    异步
                                                           不 SIGIO                 并 非 真     意义 的     1
                                            关
    O 模型 见第               8 音 )。    于                          ，
               ，                           信号 SIGURG 的 更 多 内 容 我 们 将 在 第        10     章讨 论 。

## Page 132

    第 章  服务  堪 序
      7  Linux    程 规范


             除了   网络            外，服 务 器 程序通常还必须 考虑许多 其他                                      细节 问题  。   这 些 细节问题          涉及 面
                      通信
                                                                                                      如:
    广 且 零碎        ，而 且 基 本 上 是 模板 式                    的     ，  所以 我们 称 之为 服务 器              程序 规范  。比
                                     服务
                  Linux             器程序  一        般以 后 台 进 程 形式运                     行 。后 台 进 程 又称  守 护   进程(daemon)。        它
                  没有 控制 终端 ，因 而 也 不 会 意外 接收 到 用户 输入。和守护 进程的父进程 通常是                                               init 进程      .
                  (PID 为 1      的 进程)。
             QO)  Linux      服务   序      通 有             一 套 日  志 系          ，它       至 少 能 输出 日  志 到 文件  ，          有 的 高 级服务
                                                                          统                                                  器
                       能和    日志 到 专门              的UDP 服务
                                                             器。大 部 分后台 进 程 都在                     /varlog 目录 下   拥有         自己
                  还                                                                                                          的
                  日 志 目      录 。
                                                                           非          身份 运行 。
             加 Linux         服务 器 程序              般以 某 个               专门 的                      比
                                         一                                   root                  如mysqld. httpd.     syslogd
                        后                  拥                       账户
    等 台 进 程 ，分 别 有 自己 的 运行 mysql. apache 和 syslog.
        一
             Q) Linux        服务 器 程序通常是可 配置                  的    。    服 务 器          程序   通常 能 处 理 很 多 命令行 选项         ，  如  果
        的选项太多，则 可以 用 配置 文件 来 管理 。绝 大 多 数服务 器 程序 都 有 配置 文件 ，
    次 运行
        下 。 比
                  并 存放 在       fete 目录                   如第    4 章 讨论        的 squid     服务 器 的  配置 文件    是 /etc/squid3/squid.
                  conf,
                                                                                                    并存人 /var/run        目录 中，以
             QD Linux        服务 器 进程 通常 会 在 启动               的时 候 生               成 一   个PID         文件
                                                             如           的            文件
                  记录 该 后 台 进 程           的     PID。          比  syslogd      PID           是/varrun/syslogd.pid。
                                                                                                                             如
             口 Linux         服务 器   程序   通常       需要     考虑 系统             资源     和 限制     ，以 预测 自身 能   承受  多 大 负荷        ，  比
                      可用文件描述 符                    总    数 和 内 存 总 量       等 。
                  进程
             在 开始 系统 地 学 习 网 络 编程 之                          前  ，我 们     将 用      一 章   的  幅 来 探讨服务 器 程序 的     一     些    主要 的
             。
    规范

    7.1           日 志

                                            日志
    7.1.1                             Linux 系统

                  善其事，           先利其              。      服 务 器     调试        和    维护 都 需要           个专业
              工                 必                 器          的                                一     的 日   志 系    统   。Linux HE
    供         个 守护 进程 来          处  理         系统  日    志     一  syslogd，     不 过 现在
        一                                                一                            的    Linux  系统上 使 用   的   都是   它的     升级
版 一一rsyslogd。
   rsyslogd 守护 进程既 能 接收 用 户进程 输出 的 日 ，又 能 接收
                                                                                      志           内 核 日           志。用 户 进 程是通过
                                                  的。         该 函数 将          志和       到一个     UNIX  本 地
    调用 syslog                  生成       系统 志                                      输出                  域   socket 类
                                              日                              日                                       型    CAF_
                                                             监听
                                                                             文件 获取 用 户 进程的            输出   内 核     日   志 在 老 的
    UNIX)         的 文件 /dev/log 中             ，rsyslogd      则         该              以                   。

## Page 133

        第                                         章                 器 规范
                                                  7              Linux 服务
                                                                    程序        “He   115

        上是通过另外     个 守护 进程rklogd 来 管理 ，rsyslogd 利用 额外 的 模块             实现          了 相同
    系统     一        的                                                                的功
    能。内 核日志 由     printk 等 函数 打印 至 内 核 的 环 状 缓存 (ring buffer)    中。 环 状 缓存的   内   容 直 接
    映射到      文件
        /proc/kmsg     中 。rsyslogd 则 通过 读 取 该 文件  获得 内      核日志  。
        rsyslogd 守护 进程在 接收 到 用 户 进程或内 核输入         的日       后，会   把它们 输出 至 某   些   特 定 的
                                                   志
  日 志文   件 。默 认 情    况   ，调试信 息    会 保存 至   /var/log/debug 文件   ，普通信
        下                                                                息保存至 /var/log/
                                               不 上 志 信 息 具 体         如   何 分       发  可
    messages  Xf,        /var/log/kern.log    文件  。    过，                             ，
  在 rsyslogd 的 配置 文件 中 设置 。rsyslogd 的 主 配置 文件是/ete/rsyslog.conf， 其
        中主要
    以                                                                              可  以
        的项包括 : 内 核日志 输入    路 径     ，是 否 接收UDP 日   志 及 其      监听 端口〈 默 认是 514，   见 /ete/
    设置
    services 文件  )，  是 否 接 收 TCP 日 志 及 其 监听 端口 ，日          件的    ，       子
        志文
        权限 包 含 哪 些 配置 文件
        的目标 存储 文件 。
《比如 /ete/rsyslog.d/*.conf). rsyslogd 的 子 配置 文件 则 指定 各 类 日 志
        日志体系。
   图 7-1 总 结 了 Linux 的 系统


    dmesg
    ae


                                         Fl   7-1 Linux 系统日志

    7.1.2 syslog BH
                使用 syslog             rsyslogd    守护 进程    通信    ，syslog  ARE LMF:
    应 用 程序

    #include     <syslog.h>
    void  syslog(  int                   const    char* message,     ... 3
    该            采用 可 变 参 数     二个参数                  和 第 三    个      … )         来 结构
                                               message           参数             化   输出 。priority S
数 是所 谓 的 设施 值 与日志 级 别的按位 或 。设 施 值 的默认 值 是LOG_USER， 我 们 下 面 的讨论也
只 限于 这 一 种 设施 值 。日 志 级 别有如 下 几 个 :
   #include <syslog.h>
    #define  LOG  EMERG               O   /*
    #define  LOG  ALERT               1   /*   报警 ，需 要 立即 采取 动作      wy/
    #define  LOG_CRIT                 2   /*   非常 严重的 情况 */
    #define  LOG_ERR                  3   /*   错误   “/
    #define  LOG  WARNING             4        Be
    #define  LOG  NOTICE              5   /*   通知
    #define  LOG_INFO                 6   /*   信息   */
    #define  LOG  DEBUG               7   /*   Wm wy
                  AAT   VE                的 默认 输出 方式    ，进           步结构
    “FX                                                              化日         志 内
                      syslog                                    一               容;
    #include         <syslog.h>
    void  openlog(        const char*   ident,    int logopt, int    facility )
    ident                 参数 指定 的 字符 串 将 被 添加到 日  志 消 息 的 日期    和 时间 之    后   ， 它 通常 被设置 为程序 的

## Page 134

    116    *:。         第     二    ”深入        解析 高 性能              服务 器 编程

        字                                   参数
    名             。logopt                  对后续 syslog 调用 的 行为 进行 配置                        ，它 可 取 下 列 值      的按 位 或 :
         #define                  LOG    PID             0x01      /*          在日志 消息 中 包含 程序            PID */
         #define                  LOG_CONS               0x02      /*          如 果 消 息不能 记录 到        日   志文 ，则 打印
                                                                                                             件  至终端         */
         #define                  LOG    ODELAY          0x04                      日       志功 能 直到 第      一  次  调用 syslog */
         define                   LOG    NDELAY          0x08)   8
         facility            会数可 用 来 修改           syslog                           HAIL.
                       外，                                重要。            在          阶段                        ，                  发布之后
         此                  日 志 的 过 滤 也 很                   程 序        开发          可 能          需要 输出 很 多 调试 信息                    而
    我 们 又 需要 将 这 些 调试信 息 关                               闭 。    解决 这 个问题 的         方法           并 不  是 在    程序 发 布 之 后    删除  调试  代码
            日后可能还需要用到)，                                                        日                ，    日                        日
    《因为 而 是 简单 地 设置 志 掩                                                                       码   使 志 级 别 大 于 日          志 掩 码 的 志 信
                                             Se              syslog 的 日                     志 掩 码 ;
                                  Fx

         finclude            <syslog.h>
         int         setlogmask{            int    maskpri     );
                                                         值。    该 函数 始终 会 成                 功  ，   它 返回    调用    进程 先前     的 日志 掩 码值。
         maskpri             参数 指定 日 志 掩 码
             后，              忘                              日
    最 不 要 了 使 用                                    如 下 函数 关闭     志        功 能 :

         #include            <syslog.h>
         void          closelog{);

    7.2     APES

                                                                        和EGID
    7.2.1            UID,         EVID,                                   GID

            户信息
         用                   对于服务 器 程序            的       安全 性 来 说 是 很重要                      的 ，   比如  大 部 分 服务 器 就 必须以
    份 司       动     ，但      不 能以    root      身份 运行 。下 面 这             一       组 函数 可以 获取 和 设置               当前    进程 的 真实 用 户    ID
    (CUID)、                 有 效用 户 ID    (EUID)、         真 实组    ID     (GID)      和 有 效 组          ID    (EGID)，

         #include            <sys/types.h>
         #include            <unistd.h>
         uid_t          getuid();                                  /*          获取 真实用户        ID   */
                     t
         uid getevid();                                            /*          RRARAP         ID   */
                     t  getgid();                                  /*          RRERMID          */
         gid
                                                                                       有效组 ID   */
         gid_t          getegid();                                 /*          获取
         int         setuid(       uid_t      uid  );              /*                         ID   */
                                                                               设置
                                                                                        效用    ID   */
         int         seteuid(       uid_t uid     );               /*    有                 户
         int         setgid(       gid_t      gid  );              /*                      TD   */
                                                                                       有效组 ID   */
         int         setegid(             gid     );               /*          设置
         需要 指出 的                      ，  一    个 进程 拥有 两 个 用 户                  ID:            和              存在
   UID
                                    是                                                             EUID。EUID      的         目的 是 方便 资
    源 访 问              它 使得运行 程序的 用 户拥有 该 程序 的有效用 户 的 权限。比
                :                                                                                            如     su   程序 ，任 何用 户 都
    可以 使用它 来修改 自己 的 账户信息 ，但 修改                                                 户时     su      程序 不 得 不 访问 /etc/passwd 文件      ，    而
                             该文件是需要     root  权限     。    那 么以 普通 用 户 身份                            的        如何能访问
    访问                                            的                                             启动     su             程序 /ete/passwd
             呢?               容门 就     在EUID。     用
                                                                               ls 命令
                                                                               可以查看到     程序
    文件                                                                                             的所有者是     root，        并 且 它 被 设置
                                                                                   ，su                                             了
set-user-id 标志。这 个 标志 表示 ， 任 何 普通用 户 运行 程序
                                                                                   su                时，其     有 效用 户   就   是 该 程序 的 所

## Page 135

    器程序规范 + 117
    第7章 Linux 服务


    者              那
    有                     么，     根    据   有 效用 户 的 含义              ，任 何 运行     程序
         root。                                                              su      的 普通用户   都能够 访问 /etc/passwd
文件 。有 效用 户 为 root 的 进程称 为特权 进程〈privileged processes). EGID 的 含义与EUID 类
似: 给 运行目标 程序的组用 户 提供 有 效 组的权限 。
                            清单        7-1    可以 用 来            测试  进程  的     和          的
         下 面      的代码                                                   UID     EUID  区别。
                                                                                       区别
                                          代码 清单     7-1   ”测试            进程 的UID 和 EVID 的
         #include         <unistd.h>
         finclude         <stdio.h>

         int main
         {
                          t uid       =   getuid();
                  uid
                          t
                  uid                   = getevid();
                  printf( "userid            is     td,   effective      userid is:     uid,  euid );
                  return         0;
         )

                  该           ，                           文件                所      为                        该文
         编译
                             文件    将 生成 的 可 执行                     〈 名 为 test_uid)   的 有 者 设置 root，  并 设置
    的    set-user-id             标志 ，   然    后运行 该        程序   以   查看       和     具体操
    件                                                                   UID        作如下，
         $   sudo          chown root:root         test   uid      #   修改 目标 文件 的  所 有 者为roct
         $   sudo          chmod  +s    test  uid   #     设置 目标 文件       的        标志
         $   ,ytest       uid      #    运行   程序
         userid       is      1000,     effective          userid  is:  0
         从 测试 程序的 输出 来                             ，进 程   的UID          启动     的用户      是
                                              看                      是      程序     的 ID，     而 EUID 则 root  账户
             所有者) 的             ID，
    (文件

                          用户
    7.2.2    切换

                                 清单 7-2   展示
                   代码                                       了如何将以 root 身份    启动的   进程 切换 为 以 一个普通  用 户 身份   运行  。
         下 面      的
                                                        代码 清单7-2                 用户
                                                                                ”切换

    Static bool switch_to_user( uid_t user_id, gid_t gp id )
    {
                  /*                      PAE      root   */
                  if    (   (    User     id ==    0 &&     (               0   )
                  {
                                 return      false;
                  I

                                                                       目标 wy/
                  *                                       root     或 者   用户
        gid_t gid = getgid();
        vidt uid = getuid();
        if ( ( (gid != 0 I { uid !=0) ) && ( ( gid != gp id) Il ( uid != user_id) ) )
        {
        return false;
        ]

                  /*    WRFH#         root，   则    已 经 是 目标     用  户 */

## Page 136

118 “* 第 二篇 深入解析 高 性 能 服务 器 编程


 if    (  wid !=s0)
 {
              return  true;
 }

 /*    “7/
 if    (  (   setgid( gp  ia ) < 0 Il ( setuid( user id) < 0 )

              return  false;
 |

 return       true;
}


                  间
                   关
7.3      进程           系

                  组
7.3.1            进程

                      都求属于                    个 进程
                                              组，    因 此 它们
                                                                除了     PID                信息
                   进程                                                                      外，    还    有 进程
     Linux   下  每个                        一                                                组                     ID
        )。我     们 可以 用 如下        上       来 取 指定 进程    的 PGID:
(PGID

     #include   <unistd.h>
     pid_t   getpgid(         pid_t  pid  );
                  成功时返回进程                   所 属 进程
     该 函数                            pid      组的                       PGID， 失 败 则 返回  -1   并 设置 ermo。
     每 个   进程      组 都 有  个 首领 进程              其                。                      组             ，          其中所
                      一                       ， PGID 和                   PID 相同 进 程       将   一 直 存 在           直 到
有 进程 都 退出 ，或 者        加    入 到 其他 进程          组。
     下 面   的     数 用 于 设置        PGID:

                <unistd.h>
     int setpgid(     pid_t      pid,     pid_t pgid    );
                                                                果    pid Al                 pgid       相同 ， 则 由 pid
     1%  PB BOK   PID     为 pid     的 进程     的PGID   设置     pgid。     如
                                                            为                                                     指
定 进程 将被设置 为 进程                              ，如
的                                    组    首领   果    pid   为 则 表示 设置 当前 进程 0，               的    PGID  为   pgid ，  如
         0，则 使用                       pid 作为 目标 PGID setpgid 函数
果 pgid     为                                                    成功时返回                  0，     失 败 则 返回  -1     并 设置

                         只能设置自己         或 者 其子进 程    的    PGID。     并
     一   个 进程                                                   且，    当 子 进                程  调用 exec
我 们 也 不 能 再 在父进 程     中          对 它 设置         PGID。
7.3.2  会话

     一        些有 关联 的 进程组将 形成             一    个 会话〈session)。     下 面    的             用于 创建     一  个 会话 :
     #ineclude  <unistd.h>
     pid_t   setsid(  void       });
         BAS AE                          ED, UPA REPEAL
                                                                                           PT

## Page 137

    第 7 章 Linux 器 规范
        服务
        程序     119


    用 该    数 不 仅 创 建新 会         话  ，  而  且有 如 下 额外 效果       ，;
      调用 进程 成 为 会 话的 首领 ，此 时 该 进程是新 会话的 唯一成员 。
   OC) 新 一个 组 ，
                 建     进程          其  PGID   就 是    调用  进程    的PID， 调 用 进程 成 为 该 组 的 首领     。
                        将甩开终端           〈 如 果 有 的 话 )。
   回 用 进程
   该 数 成 功时 返回 新 的 进程 组的 PGID， 失 败 则返回 -1 并 设置 ermo。
         Linux 进程并 未 提供所谓 会 话                ID   (SID)   的 概念 ，但 Linux 系统 认为 它     等 于     会 话 首 领 所
    在 的          组的                     了如下            来读取
         进程 PGID， 并提供 函数                                    SID:

         #finclude     <unistd.h>
         pid_t   getsid(        t
pid pid );

    7.3.3    用    ps    命令 查看 进程             关系
                        可查看进程、                 组      之
         执行  ps   命令                       进 程    和 会 话 间 的 关系     ;

   ps          -o  pid,ppid,pgid,sid,comm        |  less
   PID            PPID   PGID      SID    COMMAND
           1943   1942   1943      1943   bash
           2298   1943   2298      1943   ps
           2299   1943   2298      1943   less
              们   在                                         的，  以       的父             是          ，
         我
                是     bash shell 下 执行     ps 和 less 命令        所 Al less 命令
                                                              ps        进程                  bash 命令
                                                                                       是         M2
    这 可以从      PPID          PID)         一 列 看 出 。    这  3   条 命令 创建了  1 个 会话 (SID      1943
    个 进程组(PGID          分 别    是   1943 和 2298).    bash      的      和
                                                                   命令 PID、PGID
                                                                           SID   都 相同       ，很 明显 它
         会话的首领 ，         也 是 组     1943   的 首领     命令
    既是                                                      则是组  2298 的 首领  ，因 为   其     也
                                               。ps            PID                            是2298.
    图 7-2  描述    了 此 三 者 的     关系  。


                                                                     “ee
                             bash
                             组     和  话
               ，                    该     ;
                                    会        fk
               Lk:                                                   !
               ，         #1943               SSS             412298
                                                          问关系
                                             图            ”进程

    7.4   系统 资源 限制

   Linux 上 运行 的 程序 都 会受到 资源限制 的 影响 ，比 物理 设备 限制 (CPU 数量、内
                                                                 如                                  存
          等)、系    统 策略   限制  (CPU 时 间        等 )， 以  及 具体 实现     的    限制〈 比 如 文件 名 的     最    大 长度 )。
    数量
    Linux 系统 资源 限制可以通过 如 下                 一 对 国 数 来 读取 和 设置 ;
                  <sys/resource.h>
         int getrlimit(  int       resource, Struct rlimit *rlim     };

## Page 138

    120 第 二篇 深入解析 高 性能服务 器编程


        int      setrlimit( int             resource,     const struct       rlimit *rlim
                        参数
        rlim             是rlimit   结构 体    类        型的    指针    ，rlimit     结构 体 的   定义    如 下  :
        struct     rlimit
        {
                    rlim_t          rlim_cur;
                    rlim_t  rlim   max;
        Ve
                 是        个 整数
        rlim_t         一           类型     ，    它 描述 资源 级 别     。rlim_cur              员
                                                                                      成 指 定 资 源 的 软 限制         ，rlim_max
    成 员 指定 资源 的 硬 限                制 。软 限制    是    一     个 建议 性    的        、  最 好 不 要 超越 的 限制        ， 如  果 超 越 的  话 ，系
    统 可 能 向 进程 发送信 号以 终止其运行 。            如，                                 进程                            ，
                                                      例 当                      CPU        时 间 超 过 其 软 限制   时  系 统    将 向
进程 发 SIGXCPU 信号 当 文件 尺寸超过 其 软 限制 ，系 统 将 向 进程
            送                         ，                                          时                    发送   SIGXFSZ    信息
      见     10   章 )。    硬 限制 一          是 软     限制 的     限     普 通              可以 减 小          限制 而     root        身份
    (   第                                   般        上 。                     程序           硬         ，     只有以
                                         外，    我 们 可 以 使    用                ulimit
    运行 的 程序才能 增加 硬 限制 。                                                               命令修改当前      shell      下的        限
                                                    此                                                                 环境
                                                                                                          资源
                                         将对该     shell                       启动 的      所   有 后 续 程 序有 效    。  我 们 也 可以 通
    制 〈 软 限制           或/           和 硬 限 制)，    这 种 修改
过 修改 配置 文件来改变 系统 软 限制和 硬 限制 ，而 且 这 种 修改 是永久 ， 博 第
        详
                                                                                                 的        见 16 章 。
        resource       参数 指定资源 限制                   类     型 。表 7-1 列举 了 部 分 比较 重要 的              资源限制     类 型 。
                                                                             的部分资源        限制
                                      % 7-1      getrlimit    和 setrlimit 支持                   类型
            资源限制类 型                                                            =
      RLIMIT AS                                                                                           CH] mmap)
                                                        ENOMEM
            一                      产生                       铺误
                                         核心转储文件(core     dump)               的 大小 限制 【单位
      RLIMIT_CORE                    进程                                                        是字节)。  其 值  为0 表示 不 产生
                                   核心 转 全
      RLIMIT_CPU                     进程 CPU     (Pie     Ae)
      RLIMIT_DATA                    进程数据段              〈   初 始化数据  data                  bss         限制 〈 单 位 是 字 节 )
      RLIMIT FSIZE                   文件大             小 限制   〈 单 位是字 节 )，    超 过 该     限制将  使 得 某些   数 《比如 write) 产生
            一                      EFBIG     错误
      RLIMIT     NOFILE              文件
                                         描述符数量限制，    超 过 该                  限制 将  使   得某些  函 数   比
                                                                                          〈         如pipe) 产生 EMFILE 错误
      RLIMIT     NPROC             aiid:     超过 该 限制 将 使 得某些 函数〈                                     如
                                                                                                     比 如rk) 产生 EAGAIN

      RLIMIT_SIGPENDING              用 户 能     挂 起    的     信和     数量 限制
      RLIMIT_STACK                   进程
                                         栈内存限制    〈     单 位    是   字 节 )，    超 过 该 限制
                                                                                          将引起  SIGSEGV 信号
        setrlimit  和     getrlimit   成     功    时        则                            设置
                                                                          失败
                                                                          返回 -1   并       ermo。

    7.5     改变 工作 目录 和 根 目 录

        有 些 服务 器 程序还需要 改变 工作 目录 和 根目
                                         录 ， 比 如 我                                        们 第    章讨 论 的    Web 服务
                                                                                          4               器。           一
    般 来          ，Web       器的           根      系统                                    录                   目            于
                       服务
          说                          逻辑 目 录 并 非 文 件                          的    根   目 “/”，   而 是 站 点 的    根 录        对
    Linux 的    Web     服务 来 ，该 目录
                                说        一般是

## Page 139

                                                             第7
                                                             章     服务
                                                             Linux                    器  程序 规范 **            121

                                                             的  函数 分 别 是 ;
         获取 进程          当前 工作 目录     和 改变 进程工作 目录

         #include     <unistd.h>
         char*    getcwd(      char* buf,   size_t size );
   int       chdir(     const    char* path };
         buf     参数 指向      的  内 存 用 于 存储   进程    当前 工作      目录 的     路径名，            大小由
                                                                          绝对 其                      size  参数 指定 。
       有果当 前 工作     目录      的 绝对 路 径 的 长     度    〈 再加 上     个  空  结束 字 符 “\0?)        超过
    如                                                    一                              了          size，  则 getcwd
    将 返回   NULL，        并 设置     ermo  为  ERANGE。        果                  且         0，
                                如 buf                        为NULL 并 size             非     则     getcwd 可       能
       内部使用         malloc  动态
                                分配内存，    并 将 进程
                                                             的当前 工作 目录 存储
    在                                                        在其中。                     如 果 是 这 种 情况            ，
  我们必须 自己 来释放 getewd 在 内 部 创建的这 块 内 存 。getcwd 函数 成功时 返回 一 个 指向 目
则
标 存 储 区 (buf 指向 的 缓存区 或是getcwd 在 内 部 动态 创建 的 缓存区) 的 指针，失 败 则 返回
    NULL   并 设置        ecrmo。
         chdir             path   参数 指定要 切换 到 的 目标目录。它 成                 功 时                                     设
                                                             返回                   0，               失 败时  返回   -1 并
    置  errno.
                                的函数
         改变 进程 根目录                       是      其 定义
                                chroot，                  如下:
                     <unistd.h>
   int       chroot(       const char*     path   );
                 参数 指定 要 切换 到 的 目标      。                                         时                      设置
         path                   根 目录     它 成                 功  时                     失败
                                                                                      返回 -1  并               ermo。
                                以                                       后， 我    们 仍然   需要    用        使 chdir(“/”)
    chroot 并 不 改变 进程的当前 工作 目录，所     调用                       chroot 之
    来 将工作 目录                至新的     录                    根      ，          序                                     文
       切换 根 目 。 改 变 进 程 的 目录 之 后 程 可 能 无 法 访问 类 似 /dev 的
       )，因 为 这 些 文件(和 目录
    件 〈《和     目录                )     并 非                处    于 新 的  根目录 之     下  。不 过 好 在 调用             chroot 之
                                                                                             文件 描述 符 来的
后 ，进 程原先 打开 的 文件 描述 符 依然 生效 ，所 以 我 们 可以 利用 这 些 早 先 打开
    访问 调用     chroot        之 后 不 能 直接 访问 的 文件(和             )，          是      日     件。                  ，
                                                         目录     其     一     些 志文 此 外                        只 有 特
                            根目录
    权 进 程 才能改变                  。
                          如     序
                                后台化
    7.6    服务

             后，                                          以                         。      进程                  遵循
         最 我 们讨论                如 何在 代码 中 让     一   个 进程 守护 进程的 方式 运行                 守 护                的 编写
                                个                            ，      清单
    一  定 的     步骤       下 面 我 们 通过 一     具体 实现 来 探讨        如 代码            7-3    所 示  。
                                代码 清单    7-3    ”将  服务 器 程序    以守护 进程 的   方式 运行
         bool    daemonize()

                           子进程，   基 闭  父 和   ，  这 样 可以 使 程序 在   后台  运行*/
                    创建
    t
                      Pid pid   =  fork();
                 if (   pid    <  0

                        return      false;
                 |
             else       if  (    pid > 0 )

## Page 140

    122 “¢ 第 二篇 深入 解析 高 性能服务 器编程



          {
                         exit(  © );
          }

        文件权限 码。当 进程创建 新 文件 〔 使用open( const char *pathname, int
              /*  设置
                                                  将是 mode   & 0777 */
    modet mode )     系统 调用)     时，文 件的权限
          umask(          0  ]

                          新的会话，设置本 进程 为              进程 组的 首领 */
              基建
          pid_t          sid  = setsid();
          if      (  sid     <0)
          {
                         return    false;
          }

                  切换 工作目录
          if      (  ¢(   chdir( wa     )   )   <0)
          {
                         return    false;
          }

          /*      关闭 标准输和                       栓 出  设备 和 标准 错误 输出 设备 “7/
          close(          STDIN_FILENO      );
          close(          STDOUT_FILENO        };
          close(          STDERR_FILENO        };

          jw                  RHA.                          Ae wy/ /dev/null 文件 */
                              SR RRR
          open(          "/dev/null",      ORDONLY      );
          open(          "/dev/null",      O_RDWR    );
          open(          "/dev/null",      O_RDWR    );
          return          true;
    }

          上                                       功能
    实际            ，Linux     提供 了 完成 同样                 的 库 函数 :

    #include         <unistd.h>
    int         daemon(   int   nochdir,    int   noclose   );
         中
          ，nochdir                                        工作     ，      0，        设置
    其                        参数 用 于 指定 是 否 改变               目录     如 果 给 它 传 递    则 工作 目录 将 被
        为0时，标 准 输入、标 准 输出
为 “/"《〈 根 目录 )， 否则 继续 使用当前工作 目录 。noclose 参数
和 标准 错误 输出 都 被 重定向 到 /dev/null 文件 ，否 则 依然 使用原来 的 设备。该 数 成功时 返回
0， 失 败 则返回 -1 并 设置 errno。

## Page 141

    性  框架
    高 能服务 器 程序


   这 一 章 是 全 书 的 核心 ，也 是 后 续 节 的 总 览。在 这 一 章 中 ，我 们按照服务器 程序 的 一 般
        解构 下 三 个 主要 模块 :
原理 ，将 器 为如
      服务
   口 处 理 单元 。本 章 介绍 处 理单元
             IO                          VO                      的四种    LO 模型 和 两 种 高          效 事件     处  理 模 式 。
         CD  逻辑 单元 。本 章 将 介绍 逻辑 单元 的 两 种 高效并 发 模式                            ，以 及 高         效  的      还 辑 处 理 方式 一 一
             有               状态
               限             机。
   O 存储 单元 。本 书 不讨论存储 单元 ，因 为 它 只 是 服务 器 程序的可 选 ，而 且 其 内 容 与
     网 络 编程本身 无 关 。
     后，本 章 还 介绍 了 提高 服务器性 能 的 其他 建议 。
   最

                             模型
    8.1      服务

    8.1.1      C/S 模型

         TCPTP                              协议 在 设计 和 实现上并 没有 客户 端和服务
                   ，                     是                       。
    器 的 概念         在 通信 过 程 中所 有 机 器 都                 对   等   的  但 由
    于  资源《视频       、           闻、    件 )     被
                   新 软                   等   都 数据 提供 者 所
                                   所以 几乎 所 有 的 网 络 应 用 程序 都 很 自然地采用 了
       8-1   所示
    图              的           (客户
                   C/S               端!/服务器) 模型 :              所 有 客户
           通过 访问 服务器 来获取             所 需 的   资源   。
                                        器和     TCP            客户 端的工作
         采用 C/S 模型的            TCP 服务
           如图    8-2    所 示  。
    流程
                                                 模型 。 服 务 器 启动 首 先 创建
         C/S                    的逻辑很简单                   后，
    个     〈  或 多 个    )     监听 socket，                    并调用 bind 函数
    一                                                             将其绑                 图   8-1    c/s
    定 到 服务 器 感兴趣 的 端口                上       后                     等待
                                            ，然
                                         调用 listen                  数
    客户 连接 。 服      务         器 稳定 运行 之     后 ，    客 户  端 就 可以    调用  connect    函数    向   服务 器 发    起连接      了   。 由
    于 客户 连接 请 求              是 随机 到 达 的 异步事件           ， 服 务   器 需要 使 用 某   种 UVO 模型 来 监听 这                一 事件 。UVO
           有多种，    图 8-2 中     ，     服 务 器   使    用的   是 VO    复 用     技术 之 一 的 select                系统 调用。当 监听 到 连
    模型
    接 请 求      后   ，    服 务  器 就 调用  accept  函数 接受         它 ， 并 分 配 一   个     逻辑 单元 为      新  的        连接 服务 。逻 辑 单
                                         或者其他。                 图 8-2 中   ，  服 务 器 给 客户         端 分 配    的    逻辑 单元 是
    元 可以 是 新 创建的 子 进程                、    子 线程
    由 fork   系统 调用 创建的 子 进               程 。     辑 单元 读 取 客户 请           求 ，处 理 该     请   求 ，  然 后将     处    理 结果 返回
    给 客户       端   。客 户      端 接收 到服务    器   反馈 的 结果 之           后 ，可以 继续 向 服务 器 发             送请   求   ，     也可 以立即

## Page 142

    124     第 二篇 ”深入 解析 高 性 能服务 器    编程

主动 关闭连接。如 果 客 户 端 主动关闭 连接 ，则 服务 器 执行 被 动关闭 连 。至
        接 此，双 方 的
        的是，    服     务 器       在处    理     一     个 客户 请 求 的同时  还 会  继续   监听 其他 客户 请
    通信 结束。需 要 注意
    求 ，  否则 就 变 成了             服务器效率 低下 的 串 行 了     先处理 完  前   一 个  客户 的 请 求 ，才 能 继
    续 处理下   一 个 客户 请 求 )。图 8-2              中，服 务 器 同时 监听 多 个 客户 请 求是通过 select    调
                                                                                 系统
    用 实现 的  。



                                      |              Cena
                  |
                                      3                                      |
                  |                   3              |
                  3                                  :                       |
                  !                                       子 进程
                  [一             二             一
                      -La0                一          一
                  ES)
                                            一  一          一         |
                                      一        上 和   一    sd        一

                                                                             |

                                            器和 TCP 客户 端的 工作 流程
                     图 8-2     TCP 服务
        C/S 模型 非常适合 资源相对 集中 的 场合 ，             并且 它 的 实现 也 很 简单     ，    但 其       缺点  也 很 明显 ;
                       是通信的中心，当 访问
                      量过大时，                        可 能所 有 客户 都  将   得 到 很   慢 的 响应 。      下 面讨论
    服务                                                                                        的
    P2P 模型 解决了 这 个 问题         。

    8.1.2     模型

        P2P (Peer to    Peer,    模型比      C/S  模型   更 符合     网  络       通信 的 实际 情况 。它     弃 了 以
        器为中心的格局       让 网    络           上所 有 主机 重新 回归 对 等 的 地      模型
    服务                ，                                          位  。P2P     如图  8-3a  所 示 。
        P2P   模型 使 得 每台机 器 在 消耗服务 的 同时 也 给 别人提供                 服务     ，这 样  资源  能 够 充分    、自 由
    地 共享    。 云 计算 机 群可以 看 作 P2P  模型      的 一  个典范  。 但   P2P   模型 的     缺点  也 很 明显 ，   当 用 户 之
    间 传  输的   请 求过多 时   ，            网 络 负载
                      的                将加重。
        所示的           P2P     模型存在 一  个    显著的 问题   ， 即   主机 之 间很 难 互 相 发             。所以     使
        图 8-3a                                                                   现        实际

## Page 143

    8章 高性能 服务 器 程序 框架 125
    第


    的  模型 通常带有 个 专门 的       现 服务   ，如
    用 P2P      一       发              图  所示。    还
                                器      8-3b 这 个 发 现服 务 器 通常 提
   查找      还                )，     个     尽快
供
     服务 〈 至 可以 提供 内 容 服 务 使 每 客户 都 能 地 找到 自己 需要 的 资源 。









                                                                          器
                                                                         服务
                         a)                                        b)
                                                                  现
                                         图 8-3              两种P2P 模
                                        模型               带 有 发 现服务
                             a)  P2P              b)               器的P2P 模型
                                          可以看作           模型
           从 编程角度 来 ，P2P                    模型               的 扩展  : 每 台 主 机 既是客户
               讲                                  CIS              端，                  又 是 服务
           因 我 们 仍然 采用  C/S    模型 来讨论 网 络                    。
    器。     此，                                              编程
               器        框架
                            ”服务
    8.2        编程

           虽然 服务 器 程序种类 繁多，但 其 基本 框架 都 一                       样  ，不 同 之 处在于 逻辑 处 理 。为 了 让 读者
    能 从 设计 的 角度 把 握服务 器 编程 ，本 章 先 讨论 基本 框架 ，                       图
                                                                   如   8-4 所 示 。





           处理            |     x                               Ma
            UO                 |S
            单元






                                                         器基本框架
                                    图 8-4                   服务

           该图既 能 用 来    一 台 服 务  器  ，   也 能 用 来 描述       一     个服务 器 机 群 。两 种 情况 下 各 个 部 件 的
           和功能如表        所
              含义  8-1  示。
                             表 8-1      服务 器 基本 模块         的   功能 描述
                   wR                                                   A   Se  ATL
                                                                    作为
                                                                            服务  实 现 负载
           VO 处理  单元                                                    接人      器，     均衡
                                                                    i  i

## Page 144

    126 第 二篇 深入解析 高性能 服务 器 编程


                                                                                                                                 ( 续 )
                                                                                                                     器机
   模块                                                                                                        服务

                   请求    队列                                                                      各 服务 器 之 间 的         永久     TCP   连接
        VO 处 理           单元 是 服务 器 管理 客户 连接 的 模块                             。    它   通常    要完成 以 下 工作                    等待   并       新的
                                                                                                                 :                     接受
客户 连接，接收 客户 数据，将 服务 器 响应 数据 返回 给 客户 但
        端 。 是，数 据的 收发 不 一 定 在 了
    O 处 理 单元 中 执行                      也 可 能          在  逻辑 单元中 执行 ，具 体 在 何处执行 取决 于 事件 处 理 模式                                    〈      后
                                       ，                                                                                                见
       )。对       于  一                        个服务 器 机 群 来 说      ，LO 处    理 单元 是 一     个 专门 的接    人 服务        器     。它 实现 负载 均衡          ，
    文
    从 所 有 逻辑 服务                                    选取 负荷   最  小的    一    台 来为新 客户 服务      。
           -个 还 辑 单元 通常                        是 一    个 进程 或 线程              。 它分 析 并 处 理 客户     数据 ，然 后将结果 传递给                        IO
    处 理 单 元 或      者                             直接 发送给 客户      〈 具 体 使 用 哪 种     方式    取决 于 事件    处理 模式            )。    对           器机群
                                                                                                                                       服务
    而 言    ，       个 逻辑      单元            本 身 就      是 台          辑 服务      。 服   务  器 通常   拥有    多 个              单元              ，以 实现
               一                                         一                   器                                                         对多
    个 客户任务 的 并 行                  处        理  。
        网 络 和存储 单元 可以 是 数据                                     库、     存和 文件 ，甚        至    是 一 台独 立的服务              器。但      它 不   是   必须
    的 比
        ，      如    ssh,     telnet        等登录 服务 就不需要 这 个 单元                。
        请      求    队列 是 各 单元 之 间 的 通信                            方式     的            处 理    单元  接收 到 客户 请 求                 ，     需 要以 某 时
    种 方式 通知 一个于                             单元 来处        理     该 请    求  。 同样 ，多          个 逻辑 单元同时 访问       一     个 存储 单元      时    ，  也
    需要 采用 某种机 制 来 协调 处 理                                       态                 条 件 。请求队列 通常 被实现为 池 的    一  部      分       ，我 们  将 在 后 面
           池的概念 。对 于服务                                机 群 而        言 ，请求队列 是 各 台 服 务 器 之           间 预    先 建 立       的    、静 态 的      、永
    讨论
    ANY TCP        连接         ，       种               连接 能 提高      服务                     数     效率   ，           它           免
                             这              TCP                              器     之 间 交 换   据 的                  因 为 避        了    动态 建立
    TCP    连接 导              的 额外           的         系统开销    。

    8.3        I/O 模型

                                                                                                                                 第    2 个
        第      5    TYPE,         socket       在 创建的时候 默认是                   的        。我       们 可以 给 socket     系统 调用 的
    会 数 传递 SOCK_ NONBLOCK 标志，或者通过                                      系统 调用的                                                   命令 ，将 其设置
                                                                             fcntl                   F_SETFL                            为
    非阻塞          的  。 阻       蹇和 非 阻塞               的 概念       能  应 用    于所 有 文件 描述       ，    而 不 仅仅
                                                                          符                                是 socket.
    的 文件 描述 符 为                   阻塞       TO，      称 非 阻塞 的 文件 描述 符 为 非 阻塞               VO.
        针对 阻塞VO                                的         调用                               操作                          ，          的
                                  执行
       系统                                                         可 能 因为 无 法 立即 完成 而 被                   系统 挂       起     直 到 等 待       事
       发生为止              。比
如，客 户 端通过 connect                                                        向    服务 器 发 起 连接时                                       ，connect
    件                                                                                                                           将首先发送同步报文
       给                器，        后                                          。            确认 报                      没有           到    客
    段
           服务                 然        等 待 服务         器  返回 确认 报 文           如 果 服 务 器     的         文 段                立即       达      户
                                       将被挂起，直 到 客户 端                         收 到             确认 报 文段并 唤醒 connect               调用 。socket
    端 ，则       connect       调用                                                                                                  的基
    础          中，   可 能 被 阻塞 的                      系统   调用 包括 accept、send、recv
        API                                                                                     和 connect。
                   非阻        VO            执行       的 系统 调用 则 总 是 立即 返回               ，    而 不 管事 件      是否 已     经 发        生   。如 果 事 件
        针对
    没有 立即 发                  ，这 些 系统 调用 就 返回                       -1，       和 出 错    情况 一     。 此 时 我 们 必须 根据                    errno 来
                         生                                                   的            样                                             区
    分 这 两        种      情况    。对                         和 recrv             言，                            被               设置 成EAGAIN ( 意
        accept.send 而 事 件 未 发 生时 ermo 通常

## Page 145

                                                                                                                               SSM                 高    性 能     服务 器 程序     框架 «+     127

    为        来一次”)                                                                                                      为      望              ”                            言            被
       “再 或                                                                                                                 者EWOULDBLOCK ( 意 “期
                                                                                                                                阻塞                     ;对       connect 而     ，ermo     则
                                                                                                                   处理中”)。
    设置成    EINPROGRESS〈         意             为                                                                        “在
         很 显然               ，我     们  只 有 在 事件 已 经 发    生                                   的 情况 下 操作 非                         阻塞        UO《〈        读         、  等)，     程序
                                                                                                                                                                写 才 能 提高                的
                         此  ，     非   阻塞                                                              TO 通常
                                                                                                       要和其他 VO      通知 机 制         起 使             ，比
    效率 。因                                                                                                                                                 如     VO 复
                                                                                                                               一          用                           用和  SIGIO 信和  。
          VO     复 用 是          最 常 使 用    的 VO 通知 机                                        。                       它 指 的      是  ，应 用                通过               用            向内核注册
                                                                                                             制                                                                 程序 VO 复 函数
    一  组 事件 ，内 核通过              IO         复 用          把    其                              中 就 绪                       的   事件 通知 给 应 用 程序                         。Linux 上 常用  的 UO    复
               select.                                            poll 和 epoll_wait，    我 们 将 在 第                           9                      章详细讨论 它们。    需 要 指出     是
        的 BA
   本身是 塞的 ，它们 能 提高 程序效率 的 原因 在 于 它们具有同时 监听 多 个 VO 事件
    函数                                                                                                                                                                     的能力。
               信号
          SIGIO                                     TO 事件 。6.8 节 的 最 后 一 也可以用来报告                                                          段    提 到     ，        我们 可以     为  一    个 目标 文件
                                                        的宿主                                 进程
                                                                                                                        将      捕获 到  SIGIO 信和
                                                                                                                                                       号。       这
    描述 符 指定 宿主 进程               ，    那 么 被指定                                                                                                                        样，         当 目标 文件 描述
        发生时 ，SIGIO 信号 的 信号 处理 数 将 被 触发 ， 我 们 也 就 可以 在 该 信号 处 理 数
符 上 有 事件
                        对目标 文件     述 符 执行非          VO 操作                     阻塞
    中                                                                                       了                           关 于信号
                                                                                                                    。                              的使用，     我 们 将 在   第 10    章讨论  。
          从 理论                  说 ，
                                阻                       复用 信号
                             上                                           塞UVO、LO            和                           驱动     VO    都 是             同步   VO    模型    。 因 为 在 这 三    种 VO
         中     ，LO 的 读 写 操      ，                   在                        都 是    UVO 事件
    模型                                                                                                              发生之后，       由 应 用                     程序    来 完成
                                作                                                                                                                                      的。      而 POSIX 规范
    所        的异步                则不同。                    异步                                                              言，                             对                  读写     ，      些
  定义 VO 模型 对 VO 而 用 户 可以 直接 VO 执行
        操作 这
    操作              告诉    内 核用     户 读 写缓冲                                       区 的 位                    置     ，以   及 LO      操作 完成 之             后  内       核 通 知 应  用 程序 的 方式    。
 阻塞 因 为                                                                                                                                                             的  读 写 操作 已     经 由 内
       VO  的 读 写 操作 总 是         立即                                            返回    ， 而 不                        论  IO   是 理   是          的，                     真正
    核 接    管         。    也 就 是     说 ， 同步 IO    模型 要 求 用 户 代码 自行                                                               执行        VO 操作                          数据 从 内 核 缓 冲 区 读
                            区，或 将 数据 从        用 户                                       区写 人 内 核 缓冲 区 )， 缓冲                               而        异步     IO       机 制 则 由 内 核  来执行    L/
    人 用 户 缓冲
                                                                                                                                                                   台”   完 成   )。        你
   操作          数 据               在  内                                     核 缓冲 区 和 用 户                            区         之间 的 移动 是   由 内        核 “后
    O        〈                                                                                                                                             在               的           可以
                                 步  VO 向 应 用                                      程序通知  的                         是 UVO 就   绪 事
    这 样 认            为    ， 同                                                                                                   件，    而       异步       VO          向 应 用 程序 通知 的    是  VO
    完成 事件                              环境
                            。Linux      下    ，aio.h                                                              头 文件中 定义 的     数 提供 了 对              异步        TO 的 支持    。    不 过 这 部 分
    内 容 不 是 本 书                 的   重点  ，所以 只 做 简单 的 讨论
                               总结，                      的                                                                      的            列于表
作为 我 们 上 面 讨 论 几 种                                                                          IO                                               模型
                                将                                                                                               差异                    8-2 中     。
                                                                                                                                对比
                                                        % 8-2                                                           VO 模型
                                                                                                                        读
                                                                                                                               操作     阶段
                 IO 模型                                                                                                  写       和阻塞
       PAX VO                                                                 wR
                                                                                                                                                   同时 VO 事件 对 UVO 本 身 的 读 写   是非     的
       VO      复 用                         VO 复 用 系统 调用                                                           ， 但 可        监听多个                   。                    操作
  SIGIO 信号 “| ”信号 发 法 写 就 结 事件 ，用 户 程序执行 读 写 操作。 程 序 没有 阻 阶段
       异步 VO                    内 核 执行 该   写  操 作                              并    触发  读 写 完成 事件                              。 程 序 没 有

                            种     效            处
    8.4      两                  高     的   事件            理 模式

          服务 器              程序    通常    需要   处 理 三 类 事件                                     :                       VO   事件     、    信 号 及 定时 事件                   。我   们 将  在 后 续    节 依
    次讨论 这 三                 种    类 型  的 事件   ，这     节 先 从                                   整体 上 介绍                             下    两种商 效             的        事件处   理 模式    :         和
                                               一                                                                                一                                                 Reactor
    Proactor.

## Page 146

128     第 二  篇    深入 解析 高 性能 服务 器     编程

                                             和    Proactor 事件    处  理 模 式  应 运 而     。 同
    随 着 网    络 设计 模式 的兴起              ，Reactor                                   步    VO         模型
                                                                                 生                通
      于实现 Reactor 模式 ，异步   VO         模型则 用 于 实现           Proactor 模式     。 不 过 后 面 我 们 将 看 到  ， 如
常用
      使用同步    VO 方式 模拟出Proactor 模式
何                                     。

8.4.1       Reactor 模式

        是这样一种模式，                                  程              ，           )
    Reactor 它 要 求主线                                             处 理 单元 下 同     只 负责 监听 文件         上
        发生，    有 的 话 就 立即    将 该 事件               通知工作 线程        〈    逻 辑 单元  ，下 同 )。  除 此 之
是   否有 事件                                                                                      外， 主
线程不做 任何 其他 实质 性 的 工作。读 写 数                        据 ，  接 受 新  的连接 ，以 及 处 理 客 户 请 求 均 在         工作 线
    中   。
程
       完成
        用 同步 VO 模型                  以 epollL_wait 为
                                                 例) 实现
                                                           的 Reactor 模式
    使                                                                      的工作 流程    是:
    1  主线 程往 epoll  内 核 事 件 表 中 注册               socket    上 的 读 就  绪  事件
       主线     epoll_wait 等            socket              上 有 数据
    2      程调用                   待    可读。
                           上有数据可读时    ，epoll_wait
                                                                通知主线程。 主 线程
                                                                             则将socket        可 读 事件
    3                     当 socket                                                               放人
     求队列。
请
    4  睡眠 在 请    求队列 上 的 某       工作 线程            被    唤醒 ，    从                  ，             请求，
                             个                                 它    socket 读 取 数据          并 处 理 客户
      往epoll 内 核 事件表 中 注册 该  socket   上 的 写 就             绪  事 件。
然后
        程调用     等                待                    可
    5 主线     epoll_wait               socket         写。
    6 当 socket   可 写时 ，epoll_wait                 通知 主线 程 。主 线程将    socket   可 写 事件    放和 请求 队 列 。
                                                               往socket     上 写 人 服务 器 处 理 客 户   请 求
    7  睡眠 在 请求队列 上 的 某           个    工作 线程被唤醒               ，它
的 结果   。
    图 8-5     结
        总 了 Reactor 模式 的 工作 流程                    。

                                                             写就绪事件
                                                                    注册


      环 等 待监听/连接                         捅 入 就 绪 的      事件 | 请                      工作 线程
                          poll.    WO                        求
    ad                        ]                             um               工作        线程

                                                             写就 事件
                                                                注册

                                         图 8-5      Reactor 模式

    8-5 中    ，   工 作 线程   从请 求     队列 中 取出 事件             后 ，将    根据 事件 的 类 型 来 决定 如 何处      理 它 :
对 于 可 读 事    ，   执行 读 数据和 处        理     请 求 的 操作           于 可 写 事 ，      执 行写 数据 的 操作。        此，
        件                                                        件                               因
        的 Reactor   模式 中 ，    设 必要
                                         区分所谓的    “         读 工 作 线程 ”和 “ 写 工 作 线程
图 8-5 所示                                                                             ”

8.4.2 Proactor 模式

    与 Reactor 模式不         ，Proactor 模式
                     同                   将所有      VO 操作 都 交 给 主线 程           和内 核 来 处 理 ，    工 作 线

## Page 147

                                                                第 8     章 高    性    能 服务 器 程序       框架  <>     129

 程 仅仅 负责 业务 逻辑。                 此                       更      图                  服务           框架
                                因    ，Proactor 模式
                                                           符合     8-4    所 描述 的     器 编程         。
             用异步 VO HARRY  CVA aio_read 和 aio_write 为
                                                                例)     实现
   使                                                            的                 模式
                                                                Proactor                           的工作 流程
                  程调用      函数
                 1 主线  aio_read            向内核注册           socket     上 的 读 完  成 事件   ， 并  告诉  内         核 用 户 读 组
 冲 区 的 位     置 ，以   及 读 操 作 完成时            如     何 通知 应 用   程序     〈     这 里以 信和 号 为  例 ，详 情       请 参考   sigevent
 的 man     手册 )。
   2        主线程继 续 处       理 其 他 有   还   辑  。
   3 当 socket         上 的 数据 被         读    人 用 户    缓冲 区    后 ，内 核 将    向应 用 程序 发     送     一      个信号 ，以      通知
 应 用 程序 数据 已    经    可 用  。
   4             应 用 程序 预先定义 好 的 信号处                 理     数选择  一     个 工作 线程 来 处    理 客户 请       。         作    程
                                                                                                                 工
                                                                                                 求          线
     理完客户       请 求 之    ，  用                                                                  ，               内核用
 处                     后   调 aio_write RAC]                     socket    上 的 写 完 成 事件                        并 告诉
 户 写 缓冲 区 的 位          ，以 及 写     操 作 完成时 如 何 通知               应用 程序       仍      信号
       置                                                        〈             然 以       为例)。
   5 主线 程    继续          处 理  其  他 逻辑       。
                                                            后，  内 核 将 向    应 用 程序 发     送  一             个 信号，以 通知
   6 当 用 户     缓冲      区 的 数据 被 写          人        socket 之
 应 用 程序数据 已 经 发 送完毕        。
   7 应 用 程序 预先               定义  好     的     信和  号  处 理 函 数 选择     个 工作 线程 来 做 善后                 ，           如 决定
                                                                一                 处            理    比            是
 否 关 闭 socket.
                                           的工作          流程。
   图 8-6 总 结 了                                 Proactor 模式

   循环  监听socket                                            aio_write(0，      注 册 写 完成
       等 待                                                      at            事件
          上 的 事件
                 read0，                     册                                                     工作 线程
                                       注
                   完成                      “|           数据
                                       事件                                                         工作 线程
                 T  读                                   读写
              程
             主线
                                                                              事件
                                                           alo_write(j，      注 册 写 完成
                                                   图 8-6   Proactor 模式
       图     8-6 中  ，连
   在                     接 socket      上 的 读 写 事 件 是        通过  aio_read/aio_write 向    内  核      的，            此内
                                                                                                              注册 因
                                                                              以，    主 线程
 核将通过 信号 来    向     应    用   程序    报告 连接            socket 上 的 读 写 事件    。        中的                  epoll_wait 调
                                                                所
 用        能 用 来 检测 监听      上 的 连接
                       socket                      请求事件，    而 不 能 用 来 检测 连接 socket            上 的 读 写   事件    。
 8.4.3 ”模拟 Proactor 模式
                       到   了 使    用                                            的一种         方法 。         是其 原理 ，  主
   参考 文献    3 提            同步 IO 方式 模拟                      出                     Proactor 模式
 线程 执行数据 读 写 操作 ，读 写 完 成 之        ，                     主 线程   向    工作 线程    通知    这  一 “完成 事件 ”。               么从
                                                     后                                                           那
 工作 线程的角度 来            看 ， 它们 就        直接 获得了 数据 读             写 的 结果    ，接 下 来 要 做     的  只          是 对 读 写 的 结果
 进行逻辑       处 理 。
            用同步  IO 模型 〈 仍 然           以 epollL_wait 为
   使                                                       例)  模拟 出     的 Proactor    模式 的 工作     流程    下   如    :
1 主线 程往epoll 内 核 事 件 表 中 注册 socket 上 的 读 就绪事件 。

## Page 148

130         二  篇   深入 解析 高    性   能 服务 器 编程

            程                             待调用 epoll_wait 等socket      上 有 数据
     2 主线                                                                 可读。
    数据 ，epoll_ 主 线程 socket 循环
     3  当 socket   上 有                                 可读时      wait 通知主线程。     从              读取数据，
直到        没有更 多 数据 可 读 ，    然  后  将  读 取 到 的     数据 封装                    成一个 请 求对 象 并 插 人 请 求 队列 。
             请      某 工作 线程                   被    唤醒                     ，它 获得 请 对 象 并 处 客户 请     ，
                                                                                                       然
     4   睡眠 在      求   队 列 上 的       个                                       求      理      求           后
                                                               写就 绪事件    。
往 epoll  内 核 事件 表 中 注册 socket             上 的
            程调用                                          等
              主线                          待                         可
     5       epoll_wait                                   socket   写。
     6   当  socket     可 写时 ，epoll_ wait                        通知 主线 程  。主 线程往 socket 上写人 服务 器 处 理 客户 请
    的结果  。
求
                                                                                   模式
图       8-7       总 结 了 用同步 VO    模型模拟 出      的     Proactor                   的工作流程。


                                                                           注册
               注册 读 就 绪 事件                                               就 事件
     环   等 待监听 /连接
          上的事件
        socket
                                          Y

                  件        符 处
                            描述
                   Be?

                   N


               5 AT                  读 事件





                                                                             THRE

                          Pl 8-7          用 同步 VO 模拟 出 的 Proactor 模式

                  效
8.5       两 种 高       的 并 发 模式

    并                                                              程序“同时 ”执行
         发 编程 的 目的 是让                            多个                       任务 。如 果 程序是 计算 密集 型 的   ，并 发编程
并   没有   优势    ，反 而 由 于 任务 的 切换 使         效率     降低 。但                   如 果 程 序是VO 密集 型 的 ， 比 如 经 常 读 写
                  库等， 则情况
                          就不同了。                由
文件      ，     数据                                 于 IO 操作
         访问                                                                  的速度远设有 CPU 的计算 速度  快     ，所
    浪费 CPU 时 间 如 果 程 多 个 执行 线程 ， 则 当前 UO
以 让 程序 阻塞    于    UVO 操作 将           大量的                                 。      有        被
操作所       阻塞   的 执行 线程 可 主动 放弃            CPU  (或  由操作 系统 来 调度                       )，并 将 执行权转移 到 其 他 线
    这。     一 ，CPU 就 可以用 来                 更  加 有 意义 的                        IO
程        样     来                          做                              事情   除非所有 线程都  同时 被       操作所 阻
                                          此  CPU 的 利用 率 显著 提升                。
塞     )，而 不 是 等 待 IO  操作 完成  ，       因
         实现
     从       上来说，     并 发 编程 主要              有 多 进程和多 线程 两 种 方式              ，     我 们 将 在 后  续 章节 详 细 讨

## Page 149

    8章 高 性 能 服务 器 程序 框架 of 131
    第


论 它 们，这 节 先讨论 并 发 模式 。对 应 于 图 8-4， 并 发模式
               一                           是指     VO     处 理 单元        和  多 个 逻辑 单
    元 之 间 协 调 完     成 任务 的 方法。服 务  器 主要 有 两种并 发 编程     模式 :  半 同 步/半 异步
                        者          〈LeaderFollowers) 模式  。我们 将 依次讨论 之    。
    half-async) 横式和 领导     者 /追随

    8.5.1 半 同 步 /   半 异 步 模式
        首先 ，半 同步/半      步 模式 中     的       “同步 ”和 “异步”与  前 面 的            的      ”
                                           讨论     VO     模型 中                  “同步
                                        中,“        同步”和 “异步  ”区  分 的 是 内  核 向 应用 程
    和 “异步 ”是 完全 不 同 的 概念 。在        LO  模型
    通 知 的 是何种       VO 事件 〔   就  绪事 件 还 是 完成 事件 )，以 及 该    由 谁来完成  VO     写     应用
                                                                             读 〈 是
          还是内核)。    在 并 发 模式
                        中,“     同
    程序                               步” 指 的是程序 完全 按照    代码    序列 的 顺序  执行  ;“ 异步 ”
    指的    是 程序  的   执行需要 由 系统 事件 来 驱动 。常 见的系统 事件     包括 中 断    、  信和   等。    如，
                                                                             号 比 图
                        图    8-8b    则 描述了 异步的     读 操 作 。
    8-8a  描述 了 同步的读 操作，而


                                               信号
                               _             处理函数

                    (start)            SIGIO    |
                               |
          .                    3                3
              被     阻塞，直到
                    读 完成




                    (end)                                (end)
                    a)                                   b)

                                 图 8-8 并 发 模式中 的 同步和 异步
                                        (HE                   RAE
                                       a)                      LE
    按照 同步 方式运行          的    线程 称 为 同步 线程     ，      按   照异    步方式 运行的线程 称 为     异步            线程 。 显
    然 ，异步线 程        的 执行 效率   ，  实 时 性 强    ，这 是 很 多       式    程序 采用 的 模型     。    但          以异步  方
                                                                                                   编写
    式 执 行 的 程序相对 复杂，难         于    和       ，              适合                  。      线程则相反，
                                 调试
                                              扩展 而 且 不         于 大 量的 并   发     而 同步
    它 虽 然效率 相对 较      低 ，实 时 性   较 ，   但 逻辑 简单        。  因 此   ，          器对 于 像 服 务 这 种 既 要 求 较好 的 实
    性，    又 要 求     能        同时 处 理 多 个 客户 请 求 的 应用  程序  ，我 们 就 应 该               同时 使 用 同步 线程 和 异步 线
    时
      来实现    ， 即 采用 半 同 步 /  半 异步 模式 来     实现     ，
    程
    半 同步     /   半     模式    中，  同 步 线程     用        于 处 理 客户 逻辑 ，相 当 于 赂 8-4     中 的 逻辑 单元 ，       异
                                           的         UVO 处    理 单元 。异 步 线程 监听 到 客户 请        求     后 ，
    步 线程用于 处 理        VO 事件，  相当 于 图 8-4 中
                                            中。   请   求队列 将      通知 某 个工作  在     同步         模式 的 工作 线程
    就 将 其 封装 成      请 求 对 象并 插人 请  求   队列

## Page 150

    132     第 二   篇 深入    解析       高 性能服务器编程

    来 读 取 并 处 理 该 请  求对 。具 体 选 择 哪                      个 工作 线程 来为 新     客户 请 求 服   ，         则 取决
                               象                        的                          务          于请求队
          的设计  。比 如 最 简单    的      轮流 选取 工作 线程的         Round Robin     算法 ，也 可以通过 条件 变量
    列
                    量〈     见
    第 14 章)    或信号             第 14   章 ) 来 随机 地 选择     个    工作     线程     。
                                                        一        图 8-9             总 结 了 半 同步/ 半 蜡
    步 模式 的    工作 流程 。

        External  IO source        Asyne  Thread        Request Queue        Sync  Thread

                      notify()

                                   work

                                                    enqueue
notify()
   ead
                                                                                       work
                                                                                   _

                                   图                步 半 同 /半 异步模式     的  工作 流程

               器  程序 中，    如 果 结 合    考虑            种   两 事件处理 模式 和 几 种     MO 模型 ， 则 半同   步/ 半 异步
   在服务
    就存在多种变体。其 中 有 一 种 变 体 称为半 同 步/半 反应 (half-sync/half-reactive) 模
模式
式 ， 图
   如 8-10 Fras.

                                                           程
                                                          主线

                                                       WW Alepoll_ wait()

                                                        插入 连接socket




连接 socket                                           连接        连接socket
  工作 线程                                             工作 线程    工作 线程

                                 图 8-10 半 同 步/半 反应 堆 模 式

                                                        程来充当。它 负责监听 所 有            socket 上 的 事件    。
        几 8-10 中 ，     步        只有一个，由主线
    如 果 监听 socket   上 有 可 读 事  件 发 生 ，即 有 新          的    连接 请 求 到 来 ，主 线程 就       接受  之  以 得 到 新 的 连
    接    socket， 然后 往 epoll 内  核 事件 表 中              该        。                                     读
                                         注册             socket 上 的 读 写 事 件       如 果 连 接  socket   上有
             事件发生，即 有 新 的   客户 请 求 到 来 或有数据 要 发送至 客户     ，主 线程
    写                                                   端                             就将该连接 socket df
                                                        到来时 它                         将 通过
        请求 列 。        工作 线程都         请 求                上     当 有 任务
    人     队      中  所 有          睡眠 在               队列     ，        ，              们      竞争    〈   比
          互斥 ) 获得 任务           的接管
                                 权。  这 种                争 机 制 使 得只有 空闲的 工作 线程          才          有 机
    如申请                                                                                          会来处理
    新 任务 ，这 是 很合理 的     。
                              搬人请求队列    中 的         任务    是  就 绪的  连接 socket。 这 说 明 该    图  所 示 的 半 同
        图 8-10 中    ，主 线程

## Page 151

  第 8 章 高性能 服务 器 程序 框架 «f+ 133


  步!半 反应 堆 模式 采用 的 事件 处                  理      是 模式            Reactor 模式        它 要 求 工作 线程                    从
                                                                            :                    自己               socket Lit
  取 客 户 请 求和往 socket         写    人 服务 器 应      。  答  这 就 是    该    模式 的 名          称  中 “halfreactive”              的 含义。 实
  际 上 ，   半 同步/ 半 反 应          堆 模  式  也 可 以 使  用          模拟的Proactor                       事件 处 理 模式 ，即 由 主线 程        来 完成
  数据 的 读     写。           在 这 种 情况  下， 主 线程     般 会一       将 应 用     程序          数据 、 任 务 类 型    等    信息         封装  为  一个 任
  务 对     象 ，然后将其      〈  或      者指向 该     任务对  象     的    一 个        指针 )        人 请 求  队列          。 工 作     线程   从 请 求 队列
  中 取得 任务对 象 之            后 ，即    可 直接 处 理      之    ， 而 无 须 执行 读 写                 操作了  。       我    们将 在 第     15 章 给  出 一
  个 用 半 同    步/  半 反应 堆 模式 实现 的                 简单 Web       服务器 的 代码            。
      半 同步    /    半 反应 堆 模式 存在如下 缺点                ;
      QC)   主线 程和工作 线程 共享 请 求 队列。主 线程往请求                                         队列 中     添加 任务     ，   或 者      工作线程    从 请
          求 队列 中 取出 任务 ，都 需要 对 请 求 队列 加 锁 保                                 护    ，    从 而 白白     耗费CPU     时     间。
      口 每 个   工作    线程      在同一      时 间 只 能 处      理  一   个 客户 请           求    。 如 果 客 户   数量 较     多    ，       而 工作 线程 较
          少  ，则 请   求   队列 中 将 堆积        很   多 任务 对        象  ，     客 户     端    的 响应     速度 将 越 来 越           慢 。 如 果 通 过 增
   加 工作 线程 来 解决这 一 问题 ，则 工作 线程 的切换 也 将 耗费大 量 CPU 时间。
  图         8-11 描述 了 一 种    相对 高     效  的 半 同      步!/  半 异步 模式                 ，           工作它 的 每 个 线程 都 能     同时   处 理 多
  个 客户 连接     。

                                                              程
                                                             主线
                                           =
对 监听                                                                 调用
                                                           socket

                                                                      一                             派发连接 socket
                                                                                     =
                  epoll_wait()                      epoll_ wait()                        epoll_ wait()
                    工作 线程                                 工作 线程                            工作 线程
                                      图 8-11        高 效 的  半 同     步 /  半   异 步    模式
      图 8-11                      中，主 线程只 管理 监听 socket,    HERE socket           由             工作 线程 来 管理     。当 有 新      连接 的
  到 来     时 ，主 线程就接受        之 并 将      新        返回 的 连接   socket     派发          给 某           个 工作 线程 ，此 后 该     新 socket 上
  的 任何      IO   操作 都 由 被选中 的工作 线程 来 处                     理 ，                  直 到 客户 关闭 连接。 主 线程 向             工作 线程    派发
  socket  的 最    简单 的 方式 ，是 往它和 工作 线程之间 的 管道里写 数                                     据     。工 作 线程    检测 到 管道            上 有
  数据 可 读 时，就分析 是 否 是                一  个                     新 的 客户 连接 请求到  来    。如  果 是   ，则 把 该    新 socket        上 的 读 写
  事件 注册 到 自己 的        epoll  内 核       事件表 中 。
      可     见  ， 图 8-11 中   ，每 个       线程〈         主 线程 和  工作 线程    )            都 维持    自己 的 事件 循环              ， 它 们 各 自 独
  立地 监听 不 同 的 事件            。因 此    ，  在 这 种 高      效  的   半 同     步   /半 异步 模式            中， 每    个    线程 都      工作    在 异步
  模式 ，所以 它 并 非严格 意义上 的 半 同步                    /    半          模式 。我们 将 在 第                15     章 给 出          一个用 这 种 高 效
  的 半 同     步/ 半        模式 实现 的 简单 CGI         服务 器 的 代码       。

## Page 152

    134      ”%*  第 二篇 ”深入 解析 高 性能     服务 器 编程

                 者 / 追随者模式
    8.5.2        ”领导
             领导者/追随者 模式 是 多 个工作 线程 轮流               获得 事件 源 集 ，轮 流 监听       分 发并 处       件
                                                             合           、              理事
    的        种 模式 。在任意
        一        时间点，    程 序 都           仅  有 一  个 领导 者 线程  ，它 负责 监听    VO 事件  。   而 其 他 线
    程 则      都 是  者，      线程                                             领导
                 追随 它们 休眠 在             池 中 等 待 成为 新 的 领导  者     。当 前 的   者  如 果 检 测 到  VO
    事件 ，首 先 要 从 线程 池 中 推选 出 新           的领导   者 线程 ， 然 后 处 理  IO  事件   。 时，             者等
                                                                                   此 新 的领导
             新的VO 事件 ，而 原来 的领导     者 则 处 理  IO 事件  ，二 者      了并发。
                                                         实现
             领导者/追随者 模式包含              下 几 个 组 件  ;句柄
                                   如                  集  (HandleSet)、   线 程 集 (ThreadSet)、
             处理器  (EventHandler) 和      的事件处理器                                           如
    事件           具体                         〈ConcreteEventHandler)。                它们 的 关系
    图 8-12       示 “。
               所



    |                              一
                              TTT  +                     register_handle(): void
    + handle_event():void     —
             f\                    + unregister_handle(): void






    —
                                                             + promote_new_ leader(): void

                                            者/追随
                             图    8-12  领导      者  模式    的组件
               集
      1.  句柄
    句柄       〔〈Handle)   用 于 表示   VO 资源 ，   在 Linux 下 通常 就  是  一 个 文件 描述 符      。  句     集 管
                                                       方法来监听这些句柄上的 LO 事件，并 将    其  中 的 就 绪 事
    理众多 句柄，它 使用          wait_for_event
    件 通知 给 领导者 线程 。领 导 者     则     调用 绑 定 到   Handle 上 的 事件 处  理器 来 处 理事           。   导   将
                                                                                           领
                                                                                件        者
Handle 和 事件 处理器 绑 定 是通过 调用 句柄 集中 的 register_handle 方法 实现 的 。
        集
   2. 线程
    这 个 组件是所 有 工作 线程         〈    包 括 领导 者 线程    和   追随 者 线程   ) 的 管理                   它 负责
                                                                         者 。       各   线程  之
    间 的 同步   ，以  及 新 领导  者 线程 的 推选      。线 程 集中 的 线程 在   任 一 时    间 必 处 于 如 下 三 种  状态  之 一 ，
    C)    Leader: 线程 当前        处于   领导 者 身份 ，负 责 等 待 句 柄 集 上 的   VO     。
                                                                       事件
                               正在处 理事    件。领 导 者 检测
    UL)   Processing    线程                  到              事件 之     ，      转移
                 :                          IO               后     可 以          到 Processing
                         来处理该事件，并 调用        方法推选新的领导者 ，
          状态                           promote_new_leader 也                        可以   指定 其
          他 追随 者 来       处 理事 件   (Event Handoff)， 此 时 领导 者  的    地 位 不 变。 当 处  于 Processing
          状态   的 线程处理完事件 之             后 ，如 果 当 前 线程   集中没有 领导     者  ，   则 它 将 成  为新  的  领导
          者 ，否 则它就直接 转变 为 追随者             。

## Page 153

        第 8                                章 高 性 能 服务 器 程序 框架 «+  135

    Follower:  线程 当前 处 于 追随 者 身份 ，通 过 调用     集的    待
                                          线程     join 方法 等 成 为 新 的 领导
    者 ，也 可 能 被 当前 的领导 者 指定 来   处 理 新 的任务  。
    FA 8-13 显示 了 这 三 种状态 之 问 的 转换 关系 。


    处 理事件                                        领导
                                                 者交
             事件处      理    事件 处           理 — \ FES
             we, EL        完成 ，且
             当前没有          当前 存在
                                          者线程
 领导 者 线程                    领导






        Pl 8-13 领导者/追随 者 模式 的状态 转移
   需要注意 的 是 ，领导 者 线程 推选 新 的 领导者和 追随 者 等 待 成 为 新 领导者 这 两个操作 都 将
    修改 线程 ，因 此 线程集提 供        一     个 成       员         来同步这两个操作，  音态条件。
       集                                         Synchronizer     以
    3. 事件处理器和 具体 的 事件处       理 器
    事件处理 器 通常包含              一     个或    多 个 回调 函数 handle   event。      这 些 回调 函数 用 于 处 理事件 对
    应 的 业务 逻辑 。事 件 处 理 器 在 使用前 需要 被绑定                  到    某 个句柄上 ，  该 句柄 上 有 事件 发生时       ，
    领导 者 就执行 与之绑 定       的     事件   处理 器 中 的 回调 函数     。上   只 体 的 事件  处理 器 是 事件 处 理 器 的    派生
类。它们 必须 重新 实现 基 类 的 handle event 方法 ，以 处 理 特定 的 任务 。
      上面 的讨论，我们 将 领导者 / 追随者模式 的工作 流程 总结于 图 8-14 中 。
   根据

    Leader Thread        Follower Thread         ThreadSet     HandleSct ConcreteEventHandler

                   join()
                                                SS
       wait
                                                       select()

                             joing)

                             Event Arnves
       Promote     new   leader(                       ‘
                                                FE     event0
                             foreven               |
  Wait
                                                   id

                             图 8-14              BS / ay

## Page 154

+ 篇 ”深入解析 性
136 第 二   高 能 服务 器 编程


由 于 领导者 线程 自己 监听 LUO 事件 并 处 理 客户 请 求 ，因 而领导 者 /追随者 模式 不 需要
在 线程之间 传 递任何额外 的 数据 ，              也 无 须      像半     同  步/半 反应 堆 模式 那样  在线程         之   间 同步 对
请求队列 的 访问。但 领导            者 /追随    者      的 一 个  明显 缺点    是 仅 支持      一个 事件 源 集合    ，  因 此 也 无
      8-11 所 示 的  那样   ，让 每 个     工作      线程   独立    地 管理 多 个 客户 连接     。
            状态     机
8.6               有限

     前 面两节 探 讨 的 是服务 器的           IO      处 理 单   元  、请 求 队列 和        单元 之 间 协 调    完成  任务   的
各 种 模式 ，这   一 节 我   们 介绍 逻辑 单元 内 部 的          一      种  高 效编程     方法  :  有 限 状态机         state
      .
machine
     有 的应用 层 协议 头 部 包含 数据包 类 型 字段 ，每 种 类                  型    可  以 映射 为 逻辑单元  的    一   种 执行 状
态 ，服 务器可 以 根据 它 来 编写相应 的 处理                   辑 ，    如 代码 清单      8-1 所 示 。
                            代码 清单 8-1         ”状态 独立 的    有限 状 态 机
     STATE MACHINE( Package   pack    )
     {
      PackageType    type   =  pack.GetType();
      switch(   type  )

           case type  A:
                 process_package_A({              );
                 break;
           case type B:
                 process  package     B{    pack  );
                 break;
      I
     ]

     这 就 是 一个简单     的 有限 状     态 机，   只 不 过 该        状态 机 的 每 个   状态  都   是相互独立  的     ，即状态 之
           间 没有相互 转移 。状 态 之 间 的 转移    需要 是     状态 机 内 部 驱动      的 ，如 代码    清单 8-2 所 示  。
                          代码 清单               带 状态 转移 的有限 状 态 机
                 ()
     STATE_MACHINE
     i
      state  cur_State=    type A;
      while(  cur_State   !=            _C
                               type )

           Package  pack    =  getNewPackage     ();
           Switch(   cur_State   )

                case  type_A:
                      process    package state_A(        _pack  );
                      cur State    =      type B;
                      break;
                case  type_B:
                      process                        B(  _pack  );

## Page 155

    SSR 高性能 服务 器 程序 框架 *%* 137


                           cur_State      =   type _C;
                           break;
                       |
                 )
             }

                                          C，    其
             该状态 机 包含 三 种 状态:             type_A,     type_B 和 type     中  type_A        是            状态 机       的 开始 状态  ，
    type_C       是 状态 机 的 结束 状态 。状 态 机 的 当前 状态     记录 在     cur State                变量               中。    在 一  趟  循环  过 程
                                          包，                                         根据           后       State          变量
    中，状 态 机 先通过            getNewPackage               方法 获得  一 个 新 的 数据                           然 cur              的值
             何                处理该数据 。数 据 包 处 理 完 之     ，   状 态 机     给                                        目标
                               包          后        通过 cur_State                                     变量 传递          状态 值   实 来
             态转移 。
    现状           那       么当 状 态           机 进入下    一    趟 循环时，  它 将 执行 新 的           对 应 的       状态    逻辑   。
             下 面 我们 考虑 有 限 状 态 机 应 用 的     一     个     实例     ，HTTP  请 求  的      读 取 和 分     析            。 很 多 网 络 协议    ，
                        和卫            议，   都  在其   头    部 中 提供 头 部 长 度字   段 。程       根据 该 字段 的 值 就序                   可以  知
    包括       TCP 协议
    道是否 接收 到           一 个           完整  的 协议 头    部 。  但 HTTP  协议                   的   并 未 提供 这 样头 部 长    度    字段  ，并 且 其
    头 部 长度变化 也 很               大    ，     可以  只    有 十 几 字 节， 也 可以 有 上 百 字 节     。 根 据 协议 规定                      ，  我 们 判断
    HTTP                     头 部 结束 的 依据是遇 到  一    个 室  行  ，该 空 行    仅包含 一对 回 车 换行       符                                如
    果         次    读        操作 没有读 和人   HTTP   请 求 的 整个
        一                                 头部     ， 即     没有    遇 到 空               行              ， 那 么 我   们 必须 等    待  客户
继续 写 数 据 并 再 次 读人 因
        。 此，我 们 每 完成一次 读 操作，就 要 分 析 新读和 的 数据中 是 否 有 空
行 。不 过 在 寻找 空 行 的 过 程 ，我 们 可以 同时 完成 对 整个HTTP 请 求头部 的分析 〈 记
                                          中                                                                           住   空 ，
    行 面还有 请 求 行和头 部 域 )，以 提高 解析           HTTP     请 求 的 效率     。                              代 码 清 单8-3   使 用   主  、从 两 个
                 机实现了      最 简单           的HTTP     请 求   的 读 取 和 分   。   了 使  表述                  简洁 ，
    有 限 状态                                为                                                                       我
                                          析                                                               们      约定  ，直 接 称
                                           请 求的一行 包 括 请 求 行和头 部 字段) 为
    HTTP                   〈                                       行。
                                          代码 清单8-3     HTTP 请 求 的    读取 和 分 析
                       <sys/socket.h>
             #include  <netinet/in.h>
             #include  <arpa/inet.h>
             #include  <assert.h>
                       <stdio.h>
             #include  <stdlib.h>
             #include  <unistd.h>
             finclude  <errno.h>
             finclude  <string.h>
             #include  <fentl.h>
             #define        BUFFER_SIZE 4096    /*        +/
             /*                           当前正在分析请求行，当前正在分析类部字段*/
             enum CHECK_STATE         (    CHECK_STATE_REQUESTLINE    = 0, CHECK_STATE_HEADER             };

    完整       */
             enum        LINE_STATUS  {    LINE_OK    = 0, LINE_BAD, LINE_OPEN };
             /*    服务 器 处理     HTTBP  请  求 的结果:                                          PRB:                   GET_REQUEST
                               MR:                                              BAD_REQUESTRESP FORBIDDEN_REQUEST 表示 客户 对 次
                                                                                                                       经 关闭
    Wik A Ay ARAL:             INTERNAL_ERROR     表示 服务    器    内 部  错误 ，  CLOSED_CONNECTION           表示 客户 端 已          连
        了
    接 */

## Page 156

   “深入 性 编程
   138 第 二 解析 高 能 服务 器


    enum HTTP_CODE { NO_REQUEST，GET_REOUEST，BAD REQUEST,
    FORBIDDEN REQUEST, INTERNAL_ERROR, CLOSED CONNECTION };
                                                                                应答报文，       丙 只   是 根据 服务 器 的 处 理    结果   发送    如
      /*                         为了简化 问题 ，我们没有 给 窗户 端   发送  一 个完整    的   HTTP
   下   或  失败 信息       “7/
      static      const char*     szret[]        =  {   "I    get a    correct  result\n",          “Something       wrong\n"  };

      /*                     从 状态 机，用 于 解析 出 一行内容
      LINE_STATUS        parse_line(            char*   buffer,     int&     checked    index，int&             read_index )
      {
           char       temp;
             /“     checked_index             buffer                                 中 当前 正在        分  析    的  字   节            #
buffer                       尾部          一        。buffer      0~checked_index                         已              ，read_index
                                                                                                                     ，   checked
   向      中 客户 数据         的          的   下  字   节           中     第                         字 节 都          分   析 完毕     第
   index~(read_index-1)       字 节 由 下 面的大                小 分  析   */
           for     (  +  checked_index            <    read_index;     ++checked        index     )
           {
                      /* 获得 当前 要 分析的 字 节
                      temp =    buffer[       checked       index ];
                      /* 和如果 当前      的   字 节  是“\r"，        回     符，                                           行
                                                            即
                                                            车     则 说 明         可 能  读  取 到 一     个 完整 的           */
                      af ( temp      ==     '\r'    )
                      {
                                       果   “\r”    字 符 碰巧     是   目前   buffer   中 的 最  后    一 个 已 经    被读   入 的      数据  ，    么这次
                             * 如                                                                                                那
   分 析 没有读 取 到  一  个       完整 的 行 ，      LINE_OPEN       以 表示 还 需要 继续 读         取 客户    数据 才能 进一步          分     析  */
                             if       (     checked_index     +  1      )  ==   read_index        )
                              {
                                            return      LINE_OPEN;                                                   ，
                              ]
                                                         是                             读          个              行
                             /*。     如 果 下  一  个    字符 “\n”，     则 说 明        我 们  成 功      到 一     完整 的
                             else      if (    buffer[      checked     index     +  1      ] ==  '\n'     )
                              {
                                                                                ，
                                     buffer[         checxed     index++     ]  =
                                     buffer[         checked_indext+         ]  =    '\0';
                                       return       LINE OK;
                             }
                             /*      则 的      话   ， 说 明  客 户 发 送  的    HTTB  请  求 存在   语法     问题    */
                             return         LINE_BAD;
                   )
                   * 如 时             的   字  节 是  “\n"，      即 的   行将   ， 则 也 说 明 可 能        取 到 一 个 完整 的          行*/
                   else    if(    temp   ==      '\n'   )
                   {
                           if(    (         checked_index     > 1     4s   buffer[   checked_index         =     1 ]  == "\r' )
                           {
                                     buffer[                 checked_index-1 ]  =    '\0';
                                     buffer[                 checked_index++ ]  =
                                     return         LINE_OK;
                   }
                   return
               }
          )
               如 果 所 有 内      容 都分析 完 毕 也           没   遂 到   “\r”        符，
             分析                                                            字 则 返回  LINE     OPEN,
   据 才 能 进一步
          return        LINE  OPEN;
     )

## Page 157

性  框架
第 8 章 高 能 服务 器 程序 * 139



    人务析请 求行 */
HTTP  CODE parse          requestline(            char*  temp,    CHECK  STATE& checkstate )
{
      char*®         url =     strpbrk(     temp,    "   \t" };
                     请  求 行          没有 空白字符或          字
      /*   如                    中                  “At”       符，则     ARAM */
                          )
      if   (   !     url
      {
                     return          BAD REQUEST;
      }
      *url++         =      '\O';

      char*          method     =      temp;
      if   (        strcasecmp(          method,  "GET"     ) ==  0     /*   支  持GET 方法 */
       {
                     printf{           "The request    method    is   GET\n" };
       }
      else
       {
                     return          BAD REQUEST;
       }

      url      +=    strspn(           url, " \t" );
      char*          version         =  strpbrk(   url,   "  \t"  );
      if   {     !   version           )
       {

                     return          BAD REQUEST;
       }
       *yersion++           =    '"\Q';
      version           +=  strspn(         version,  "   \t"  }?
       /*                HTTP/1.1        */
       if    ( streasecmp(               version,      "HTTP/1.1"  )  !=   0 )
       {
                      return         BAD_REQUEST;
       }
           检查
       /*            URL    是否合法         */
       if    ( strncasecmp({             url,    "http://",   7     ==  0    )
{
                      url   +=       7;
                      url   =    strehr(     url,   '/'   );
}

       if    ¢    !   url   ||]             0 !=    '/'   )
{
                      return         BAD REQUEST;
}
                       "The      request     URL  is:    %s\n",   url  };
                                       理    ，                        分
       /*  HTTP          请 求 行 处         完  毕 状  态 转移 到 头 部   字段 的    析    */
       checkstate           =    CHECK      STATE      HEADER;
       return         NOREQUEST;
 }

 /< spay             Re  */
 HTTP  CODE          parse  headers({        char* temp      )

## Page 158

 篇 解析 性
 140 第 二 深入 高 能服务 器 编程




 {
      /*      到       一 个 空  行   ，说    明 我们   得到 了         一 个   正确 的 HTTBE  请求
      if  (   temp[   0           ==             )
                                        '\or
      {
                return     GET              REQUEST;
 }
else if ( strncasecmp( temp, "Host:", 5 == 0 /* 处理“HOsT” 头 部 字段 */
 {
     temp += 5;
                temp  +=         strspn(             temp,   "   \t" );
                printf(          "the             request  host  is:   %s\n", temp     );
      }
      else      /*    其他头 部 字段            都不       处    理  */

              printf£(     "I     can     not     handle        this   header\n"     );
      }
        return      NO REQUEST;
 }
 /*   Sap HTTPE                      ER    */
   HTTP CODE     parse _content(                     char*   buffer,    inté checked index,       CHECK     STATE&
 {                                            checkstate,       inté&                             start_line     )
        LINE_STATUS                linestatus      =                             /*    记录 当前 行 的   读取   状态    */
        HTTP_CODE     retcode        =    NOREQUEST;                             /*               请 求 的处理结果
        /*   主  状态 ，用 于 从            buffer       中            所有         的行
                                                         取出
                      机                                         完整
        while(   (   linestatus      =         Parse     1]inelf buffer, checked_index,    read   index ) )   == LINE OK )
        {
              char*   temp             buffer                                        start_line
                                  =                +                             /*           是行在buffer       中 的 起始位置  */
              start_line          =    checked_index;                            /*    记录 下 一 行 的起始     位 置   */
              /*    checkstate       记录 主               状态 机 当前 的    状态   */
              switch      (     checkstate              )
              {
                     case       CHECK_STATE_REQUESTLINE:                               第  一 个 状态  ，分 析请 求     行 */
                     {
                                retcode          =      parse    requestline(    temp,    checkstate
                                if (              retecode  ==   BAD REQUEST  )
                                 {
                                                    return  BAD REQUEST;
                                 }
                                break;
                     }
                     case       CHECK_STATE              HEADER:                 *     第  二个 状态   ，分 析 头  部 字段    */
                     {
                                retcode          =      parse    headers(   temp );
                                if (               retcode  ==   BAD REQUEST  )
                                {
                                                    return  BAD     REQUEST;
                                }
                                else    if       (      retcode     ==                 )
                                {
                                                    return  GETREQUEST;
                                }
                                break;
                     }
                     default:
                     {
                                return      INTERNAL            ERROR;

## Page 159

 第 8 章 高 性 能 服务 器 程序 框架 ol} 141



               }
    }
 }
/*  车         没有读 取 到  一   个   完整 的    行    ，   则 表示 还 需要 继续 读 取 客户     数据 才能 进一步 分    析 */
     if(  linestatus       ==          OPFEN        )
 {
             return    NOREQUEST;
 }
     else
 {
             return             BAD REQUEST;
 }
 }

 int main{   int   arge,    char*            argv[]   }
 {
     if(   arge    <=  2   }
     {
               printf(     “usage:        %s    ip_address    port_number\n",     basename(    argv[0] ) );
             return       1;
     }
     const     char*   ip    = argv[l];
     int   port    =    atoi(        argv[2]    };

     struct                        address;
     bzero(         &address,       sizeof({    address }     });
     address.sin_family            =   AF_INET;
     inet_pton(       AF_INET,       ip,                          addr
                                                         ésaddress.sin   );
                       port    =   htons(          port );
     address.sin

     int   listenfd      =  socket(        PF_INET,                     0  );
     assert(          listenfd >=    0    );
     int   ret =     bind(         listenfd,    (     struct sockaddr*     )&address,  sizeof( address   ) );
     assert(      ret  !=   =1
     ret   = listen(       listenfd,       5       );
     assert(      ret  !=   -1   };
     struct                    in  client_address;
     socklen_t       client_addrlength              =      sizeof(  client_address );
     int   fd  =   accept(     listenfd,            (  struct sockaddr*    )&client    address,
                               &client_addrlength             );
     if(   fd  <   0  }
     {
             printf(       “errno        is:    $d\n",       errno  );
     }
     else
     {
             char buffer[ BUFFER_SIZE               ];        /*    RMR +/
             memset(      buffer,      "\0',        BUFFER_SIZE     );
             int   data_read       =   0;
             int   read    index     =    0;                  /*    当前 已经读 取 了 多 少 字 节 的 客户数据  */
             int   checked index                0;            /*        已经分析完了             数据
                               =                                    当前 多 少 字 节 的 客户        */
             int   start_line        =    0;                  /*    4776 buffer 中 的起始 位置 */
             /*   设置 主     状态 机         的 初始    状态    */
             CHECK       STATE checkstate           =   CHECK       STATEREQUESTLINE;
             while(  1         /* 循环读           取 客户 数据 并 分       析 之  */
             {

## Page 160

篇 解析
142 第 二 深入 高 性 能服务 器 编程


                      data_read         =   recv(       fd, buffer     + read_ index, BUFFER   SIZE  - read    index, 0  );
                           if  ({         read      ==      -1    )
    {
    printf( “reading failed\n" );
    break;
    }
    else if ( data read == 0 )
    {
    printf£( "remote client has closed the connection\n" );
    break;
    }
    read_index t=
                                                            HARP           */
                      HTTP_CODE           result        = parse_content(        buffer,      checked index,   checkstate,
                                                                                read  index, start     line );
                      if(          result   ==  NO REQUEST           )          /*    尚未 得 到 一 个 完整的   HTTE    请求     + /
                           {
                                      continue;
                           }
                      else         if(     result                                    得 到     个 完整
                                                    ==      GET            )    /*        一        的、正 确    的       请求 */
                           {
                                      send(       fd,          [DT，     strlen(  szret[0]    ,  0  );
                                      break;
                           }
                      else            /*                                 */
                           {
                                      send(       fd,   szret[{1],      strlen(  szret[1l]   ,  0  );
                                      break;
                           }
             }
             close(          fd    );
           }
           close(  listenfd           );
           return  0;
    }

eee
                   清单         8-3     中         的 两 个  有 限 状
    我   们                将代码
态 机 分 别称 为 主                                 机 和 从 状态   机， 这                                 未 读 取 到
体现                                                      机在内部                                 AN
      了 它们 之 间 的 关系             +     主状态
      从状态机。下 面 先                   分  析 从 状态            机， 即
调用
parse line  AX,    EM           buffer     中             出一 的
                                                          解析              新
                                                                        客户
  行。             描述                                状态    状态             到达
个 图         8-15           了  其 可     能 的       及          数据
                                                                                                    出
                                                                                读 取 到 回 车                           单独
                                          是                                                                         现在
    这 个 状态 机 的 初始 状态                      LINE_OK，                              人                                   Wu
                                                       中新到达的
其原始 驱动 力 来自                  于 buffer
                                                           调
客户 数据。 在         main       函数          中，            我 们 循环
             往   buffer                                 。
用 reev  函数                    中  读 人 客户 数据                 每
  成                  后                    调用
次 功读 取 数据                     ，    我  们 就             parse_
content PA 2 SE  4 Hr       Bris   A  AY BE.          parse_

## Page 161

                                                             SSM     高    性    能   服务 器 程序 框架 +     143

                                                                      函数 现 在 假设 服务 器     经 过    一     次
    content 图 数     首先 要 做 的 就 是调用 parse           line      来获取一个行。
    recv  调用 之        后 ，buffer 的 内 容以 及 部 分 变量 的 值如         图 8-16a     所 示 。
                                                   所         ，它           挨个 检查 图    8-16a 所 的 buffer 中
        parse_line 函数   处    理   后    的 结果    如        8-16b         示             示
    checked_index       (read_index-1)                                         AR. Ff WOR       checked
        值。当
    前                   buffer 中 不 存在
                                          行       结束
                                                         所符，
    index 的                                        以         parse_line 返回     LINE_ OPEN。 接 下  来 ，   程
                                                             后    buffer 中
    序 继续 调用     recv             以 读 取 更 多 客户 数据  ，这 次 读 操作     的    内 容以 及 部 分 变量 的 值    如           网
    后                       函数                 又  开始 处 理 这 部 分 新 到 来 的 数据          ，如
    8-16c   所 示     然   parse_line        就                                        图 8-16d  示。        次
               。                                                                                    所 这
    读                              行                         ，parse line           就可     将
    它
           取 到 了    一   个 完整 的 ， 即                           "”。     此 时                      函数 以 这 行内
    容递交 给   parse_content     中    的 主       状  态 机 来 处      理 了 。

                                                        read_index

    上
        >”    checked_index                              a)
                                                        read_index

    pe                      |                      apt

                                                   checked_index

                                                         b)                       read_index
                                                                                   |

                                                  checked_index
                                                         ©                        read_index
                            HOS]
                                                         d)        checked_index
                            fl 8-16                          的               过程
                                                                             函数
                                                             工作
                    a)  recv Jn. buffer  里 的 初始 内 容 和 部分     变量 的值                 处 理buffer
                    后 的     e)   再 次 调用  reev  后 的 结果    d)    parse_     line 再次处理
                                                              函数 buffer 后 的
                    机使用 checkstate 变量
                                             来  记录 当前
        主状态                                                  的状态。    如 果 当 前   的状态 是  CHECK STATE
                                                             于
    REQUESTLINE，        则 表示 parse line      函数解析 出 的 行 是 请 求     行 ，              是 主 状 态 机 调用  parse_
    requestline     来分析 请 求行     ，      如 果 当 前 的 状态是        CHECK_STATE_HEADER，        则 表示 parse_line
    函数解析出 的是头 部 字段 ，        于 是 主            状态    机 调用 parse_headers     来    分 析 头 部 字段 。checkstate 2
  的初始 值 是 CHECK_STATE_REQUESTLINE，parse_requestline 函数 在 成功 地 分 析 完 请 求
量
    行 之 后   将   其设置     为                    HEADER，         从 而     状态转移。
                                                             实现

## Page 162

    «> 第 二篇 深入 解析 高 性 能 服务器 编程
    144


            ”提高                        其
    8.7           服务 器 性 能         的        他 建议

         性 能 对 服务 器 来 说是      至 关 重 要       的 ， 毕 竟 每      个客户       期 望   其   请 求 能 很    快地   得 到   响应 。   影 响
    服务 器 性 能 的 首要 因素 就 是系统 的 硬件                     资源   ， 比 如  CPU  的 个       数 、 速   度  ，内 存   的   大 小 等。   不
过 由 于 硬件技术 的 飞速 发 展 ，现 代 服 务 器 都 不缺乏 硬件 资源 。因 此，我 们 需要 考虑 的 主要 问
    题 是如何从 “软环境”来 提升服务 器 的 性                        能  。   服 务  器的  “软环境       ”     一           方面 是 指 系统 的 软件
    资源 ，     比 如 操作 系统 允许 用 户 打 开 的         最 大     文件     描述  符 数量   ，    另   一 方面 指 的 就 是      服务      器 程序 本
    号 ，即如何 从 编程的角度 来 确保服务 器 的 性                          能 ，这 是 本 节 要 讨论 的 问题       。
             前 面 我们介绍 了 几 种 高效的 事件处             理   模式     和 并 发 模式 ，      以 及   高 效的     逻辑   处 理 方式    一    有
    限 状 态     机 ，它 们 都 有助   于 提高   服务   器 的     整体 性        能。下 面 我   们    进一步      分  析 高 性   能      服务 器 需要 注
    意 的其他 几 个 方面 :        地、  数据 复制     、   上 下 文   切      换和 锁  。
    8.7.1     xb

       服务器的硬件资源“充裕 "， 那么提高 服务 器 性 能 的 一 个 很 直接 的 方法 就 是以 空间
   既然
    换 时 间，即 “浪费”服务 器 的 硬件 资源 ，                      以 换取 其 运行 效率           。   这 就   是池   〈pool)     的 概念 。   池
    一       组 资源 的 集合 ，这 组 资源 在 服务 器 启动 之 初 就 被完全 创建好                          并 初始        化 ， 这 称 为   静态    资源
    分    配  。当 服务  器 进          人 正式 运行 阶段  ， 即 开始 处 理 客户 请 求 的 时              候 ，  如 果 它 需要 相关 的 资源          ，
    就 可 以 直接 从 池 中 获取     ，   无 须 动     分态    配 。   很 显然     ， 直 接 从 池 中 取得 所 需 资            源 比 动态    分配    资源
    的 速度要 快 得     多 ，  因 为  分 配 系  统 资 源 的      系统  调用 都 是 很        耗 时      的 。 当 服务 器      处 理 完   一   个 客户 连
    接    后  ， 可 以 把相关 的   资源  放 回  池 中 ，    无 须 执行         系统  调用 来   释放       资源  。 从 最      的  效果 来     看 ， 池
相当 于 服务 器 管理系统 资源 的应用 层 设施，它 避免了 服务 器 对 内 核的频繁 访问 。
         不    过 ， 既 然 池 中 的 资源 是   预先  静态 分 配          的   ，我 们 就 无 法    预期 应 该      分 配 多 少 资源 。        这 个 问题
    又 该如何 解决呢?         最 简单 的 解决 方案 就 是 分           配    “ 足 够   多”的 资源        ， 即 针对 每    个        可 能 的 客户 连接
            配 必 要 的 资源 。这 通常    会 导致       资源 的  浪费      ， 因 为    任一时 刻 的 客户        数量 都 可 能 远 远        没有   达到
    服务 器 能 支持 的最大 客户 数量 。好            在 这 种      资源      的        浪费 对 服务 器 来  说 一  般 不 会 构成 问题 。       还   有 一
    种 解决      方案 是预先  分 配 一 定 的 资源    ，      此 后 如 果 发 现 资源 不            用 ，  就 再  动态       分 配 一些 并 加 入 池 中  。
         根据 不 同 的 资源 类      型 ，  池 可 分 为 多        种 ，    常 见 的 有  内 存 池    、   进 程 池 、  线 程     池和 连接 池       它
    们 的 含义 都 很 明确 。
         内 存 池 通常 用    于  socket 的       接收   缓存 和  发 送 缓      存  。对 于 某 些 长 度       有 限 的     客户 请     求 ， 比 如
    HTTP      请 求 ，预先 分 配 一  个 大 小       足够 〈 比  如 5000      字 节 ) 的 接收        缓存  区 是 很     合理    的 。   当 客户 请
    求 的 长度超过 接收 缓冲区 的 大 小             时，    我  们 可 以       选择  丢弃 请 求 或 者      动态           扩大 接收缓冲 区  ，
         进程池和 线程 池 都是并 发 编程 常用的“伎俩”。                               我 们 需要 一 个          工作  进程 或      工作    线程 来
    处 理 新 到 来的客户 请 求        时 ，  我 们 可 以 直接 从       进程      池 或    线程 池 中 取得       一   个 执行 实体       ，  而 无 须 动
态 地 调用 fork 或 pthread_create 等 来 创建 进程和 线程 。
         连接 池 通 常用于 服务 器 或服务 器 机 和                  的 内    部 永久   连接  。    图  8-4  中 ，  每 个    逻辑      单元 可 能 都
    要 频           访问 本 地 的 某个数据        库。   简 单     的 做 法    是 :   逻辑 单元 每 次 需要 访问            数据     库  的 时 候 ，

## Page 163

8章 高性能 服务 器 程序 框架 ss* 145
第


就 问 数 据 库 程序 发 起 连接 ，而 访问 完毕             后    释放 连接 。 很 显然                      ， 这 种 做 法 的            效率   太       低   。 一   解 种
决方案 是 使用连接 池   。             连 接 池 是 服务 器  预先         和 数据 库       程序 建立            的  一 组         连接     的      集合 。  当 革 个  逻
辑 单元需要 访问 数据 库 时，它 可              以 直接 从 连接 池 中 取得                       一   个     连接  的 实体 并 使 用               之        。待完成 数
    之后     ，     辑 单    元   再 将 该 连接 返还 给 连接                   池 。
据 库 的 访问

8.7.2   数据 复制
              高性能 服务器 应 该 避免    不 必要 的 数据 复制           ，       尤 其  是    当   数据 复制 发               生    在 用      户代 码  和 内  核 之
间 的 时 候 。如 果 内   核 可以 直接 处 理 从       socket           或 者    文件    读     人 的 数据       ，            则 应 用 程序 就       设    必要 将 这
些 数 据  从 内  核 缓冲 区 复制 到 应 用 程序 缓冲 区                     。    这 里 说        的           处理”              是
                                                       中                     “直接                       指 的  应 用        程序 不
    的内容，    不 需要 对它们 做 任何 分                            。       比
心这些 数据                                                                如 ftp         服务
                                                        析                        器，      当 客户 请 求      一             个  文件时  ，
服务     只 需要 检测 目标     文件    是   否 存在  ，以   及          客户    是    否           有 读 取 它 的   权限        ，         而 绝对 不 会     关心 文件
的 具体 内 容。这 样的话    ，ftp           服务 器 就 无 须   把 目标 文件                的    内  容 完整     地  读 人 到 应 用     程序                 缓冲区 中
并 调用   send    来发送，        而 是  可以 使    用  零 指贝        ”             函
                                         “                           数  sendfile     来           直接 将    其  发送        给 客户 端 。
    外，                              内核)                            也           该              ，        说               两个
    此 用 户代 码 内 部    〈     不访问 的 数据 复制 是 应 避免                                                       举 例 来                      当
                                                                                         的                      ，            工作
进程 之间 要    传递大  的 数据 时       ，    我 们 就 应 该   考虑       使    用    共享      内   存 来   在 它们 之间 直 接 共享 这 些 数 据                    ，
而 不 是 使 用   管道 或者  消息 队列 来 传递         。  又    比如       代码 清单       8-3         所 示 的          解析   HTTP     请 求 的           中，
                                                                                                                            实例
    们用指针    (start_line) 来 指出
我                               每个行在  buffer           中 的 起 始位              置  ，以 便 随        后    对 行内 容 进         行 访问     ，而
不 是 把  行的   内 容 复 制 到 另外   一 个 缓冲    区中 来 使            用    ，因 为 这 样 既 浪费 空间                  ，    又 效率 低下          。
                          和锁
8.7.3               上 下 文 切换

    并 发 程序必须 考虑上 下 文 切换              〈context switch)              的 问题          ， 即   进程 切换 或 线程                  切换     导致  的
                                型的   服务     器，    也 不 应 该 使              用   过 多 的 工作              线程    〈  或       工作 进程   ， 下
的 系统 开销 。 即 使是    VO 密集
同)，    则 线程 间 的 切换 将 占用 大 量           的    CPU         间    理                                                                 时
                                                   时 ， 服 务 器 真正                  用 于 处                 业务 逻辑          的     CPU
间 的 比重 就 显得 不 足了    。 因 此       ， 为 每 个  客户 连接 都创建                       一   个 工作      线程的             服务          器 模型 是 不 可 取
WY. FA 8-11 所 描述 的     半同    步!/半     模式 是 一           种    比较 合理             的   解决 方案                ， 它               一个线程同
                                                                                                                            允许
                            外，  多 线程  服务     器 的       一     个   优点 是        不 同   的                       线程 可以 同时 运行 在  不 同 的
时 处 理 多 个 客户 连接。此
    。当                      线程
CPU 上                   的数量不大于  CPU       的 数目        时 ，   上 下 文     的  切换 就 不 是 问题                   了。
               并 发 程序需要 考虑 的另外  一 个问题    是                    共享 资源的加       锁 保   护。  锁 通常             被  认为  是 导致        服务 器
    低下的一个因素，    因 为 由           它 引入 的代码 不 仅 不                     处  理  任何    业务 逻辑               ，           而 且 需 要 访问 内 核资
效率
                                                                         使用锁。    显
源。因    此，   服 务 器如 果 有 更 好 的 解决 方案               ， 就 应    该    避免                                然， 图     8-11       所 描述 的 半 同
步/半 异步 模式 就 比 图     8-10     所       的 半 同         步  / 半 反应 扒 模 式 的 效率                            。如 果 服     务    器        使用
                                                                                              商                              必须
锁”，                               ，              用             。                                   都                 块
“ 则 可以 考虑 减 小 锁 的 粒度                 比 如 使         读 写         锁    当 所 有 工作 线程                        只  读 取 一        共 享  内 存
 内容时，读 写 锁 并     不 会    增加 系统        的 额外     开销            。只 有 当 其 中         某   一   个 工作 线程         需要          写 这    块 内 存
的
时，系 统 才     必 须 去 锁 住    这块       。

## Page 164

                                              BIR                                    用
                                                    VO                  复

         VO     复  用 使 得      程序能       同时    监听 多 个 文件 描述     ，        这 对 提高 程序           的性 能    至  关 重     要。   常，
                                                                符                                                    通
                                                               ，
  网 络 程序         在  下 列  情况 下 需要 使            用 IO        复 用 技术
         OU              序  要 同时 处 理 多 个 socket。     比 如 本 章 将 要 讨论 的 非 阻                    塞connect        HA.
         口 客户 端          程序 要 同时 处 理 用 户            输入和 网    络 连接    。  比 如 本 章 将 要讨论          的    聊天 室   程序   。
               TCP     服务 大    同时 处 理 监听            socket  和 连接 socket。             是              用               。
                                                                            这 /O 复 用 使                 最 多 的    场合   后
               续      节 将 展示很 多 这 方面 的 例子           。
                   要同时           处    理 TCP 请
                                                求和     请
         C) 服务                                                 求。    比  如 本 章 将 要讨论 的 回 射服务
                                                    UDP                                                       器。
         QD    服务 器要同时 监听 多 个                       ，或 者   处理    多 种 服务   。  比   如    本 章 将 要讨论 的      xinetd 服务
                                                端口                                                                  器。
 fs Ris ih AE, 复 用 虽然 能 同时 监听 多 个 文件 描述 ， 它 本 身 是 阻塞
                                VO                                            符      但                     的。   并 且 当
  多 个 文件 描述 符 同时 就                    绪 时，    如 果 不 采取 额外 的 措施          ， 程 序 就      只 能 按 顺序 依次       处     理 其 中 的 每
                                                                                                    并发，              能
                                    个 文件描述 ，这 使 得服务 器 程序看 起 来 像是串        行 工作 。      如 果 要 实现
  一                符                                                             的                         只       使用多
  进程或多 线程 等 编程 手段                     。
                                        的  系统 调用 主要
         Linux    下 实现     VO 复 用                          有                和        本 章 将 依次 讨论                   ，
                                                                                                                     然
                                                          select.      poll      epoll，                         之    后
  介绍 使用它们 的 几 个 实例                    。

  9.1          select                系统 调用

         select               系统 调用 的 用 途是 :    在 一 段 指定    时间 内 ，     监听 用 户        感 兴趣 的 文件         述符  上 的 可     、
                                                                                                                     读
  AY ES            SSE.                        select 系统 调用     的    API，     然
                                                                              后      讨论 select                   判断 文件
                                                                                                                  描述符就
的条件，               最 后   给  出  它 在 处 理 带 外 数据 中 的 实际     应 用           。
  绪

  9.1.1        select API

         select    系统 调用 的 原型           如  下  :

         #include     <sys/select.h>
         int   select(     int nfds,      fd_set*       readfds, fd_set*     writefds,     fd_set*  exceptfds,
                           struct     timeval*      timeout  });
         1         参数 指定 被 监听 的 文件 描述 符 的 总         。                   通常
               nfds                                 它                       被     设置    为   seleet     的所有           描
                                                    数                                                            监听 文件
     符中的 最 大 值加             1， 因 为      文件     描述 符 是 从 0     开始计数 的        。
  述
                                                    分别 指向               可 读  、   可 写     和  异常 等 事件
         2 readfds、writefds 和                          参数                                              对应的
                                         exceptfds                                                                  文件
         符集合。应           用 程序
                                调用        select AIRY,        3         个 参数
  描述                                                                              传人    自己  感  兴趣 的 文件 描述 符          。
  select 调用 返回 时，内 核将修改 它们 来通知应用 程序娜                                      些 文件 描述 符 已 经 就              绪。这 3      个 参数

## Page 165

                                                                                                     SOR      IO   复 用 ee  147

 是 fd_set      结构 指针 类                 。fd_set                体的      如下:
                                                    结构
                                      型                           定义
        finclude           <typesizes.h>
        #define          FD SETSIZE 1024

                                                 .h>
        #include           <sys/select
        #define              FD_SETSIZE    __ FDSETSIZE
        typedef          long int      fd      mask;
        #undef           | NFDBITS
        #define      NFDBITS           (      8   *     (int)  sizeof     (    fdmask    )    )
        typedef          struct
        {
                 USE_XOPEN
               _fd_mask           fds _bits[          FD SETSIZE     /                        ];
        #  define        FDS_BITS(set)              ((set)->fds_ bits)
        felse
               _.fd_mask                              FD SETSIZE               /   _NFDBITS     };
        #  define                                                 bits)
        #endif
        }  fd_set;
                             可见，       人纪   _set  结构 体 仅 包 含      一     个 整 型 数组         ，   该 数组 的 每       个  元 素 的 每 一位  〈bit)
        由 以上定义
 标记     一    个 文件              描述  。人    fa_set     能   容纳 的      文件               符 数 量 由 FD   SETSIZE 指定         ， 这 就       了
                                  符                                                                                           限制
 select    能 同时 处 理 的 文件描述 符 的 总 量                             。
    由 于 位 操作 过                于 烦琐             ， 我们 应 该         使   用下 面    的    一 系列 宏 来 访问       fd_set     体中的位;
                                                                                                          结构
    #include               <sys/select.h>
                                                                                                   所 有  位  */
    FD_ZERO(               fd_set  *fdset         );                           /*  WER   fdset 的
    FD_SET(              int  fd,         fd_set  *fdset      );               /*  设置    fdset  的  位 fd   */
    FD_CLR(              int  fd,         fd_set  *fdset      );               /*  清除    fdset 的   位 fd   */
    int      FD_ISSET(         int fd,         fd_set         *fdset           /*  测试           的
                                                                  );                     fdset     位 fd 是   否  设置
             timeout
    3                      参数用来设置                 select          的     时间。                     个             类型的指针，
函数
                                                                  超时                       它 是 一   timeval 结构
                                                                         应用程序  select    等 待   了 多 久 。    不 过 我 们 不 能 完全 信任
 用 指针 参数 是 因为 内 核将修改 它以 告诉
               调用
 select         返回         后  的 timeout 值           ，   比           如 调用 失败 时  timeout   值    是 不         的            体的定
   如                                                                                               确定         。timeval 结构
    下
          :
 义

    struct       timeval
    {
             long         tv_sec;             /*      */
             long         tv_usec;            /*              */

                                       可见，select  给 我 们 提供 了            一    个 微   秒 级   的    定时   方式   。 如 果 给        变量的
    由 以 上 定义                                                                                                       timeout
                    成                                       传递 则   select   将          立即 返回   如 果 给                 NULL， 则
 tv_sec            员和    tv_usec   成     员    都 0，                                           。          timeout    传递
           将一直阻 ，             直 到 某 个 文件 描述 符                     就  绪  。
 select
                成功时返回就绪                        〈  可 读    、           可 写 和 异常  )   文件     描述  符 的 总     数 。 如 果 在 超时 时 间 内 没
    select
 有 任何 文件描述 符 就                         绪   ，select     将      返回0。select         失败 时 返回       -1  并    设置  erno。  如 果 在  select
 竺              程序         接收 到 信号             ，  则     select  立即     返回    -1，   并      设置 ermo  为    EINTR。
 等

## Page 166

148         第 二  篇     深入 解析 高    性  能     服务 器    编程

                   描述符    就
                                          绪条件
9.1.2                                     ”文件
    哪些 情况 下 文件 描述 符     可以    被                   是可读、     者                       ，     于   使用
                                              认为 可 写 或 出 现 异                       对            select 的
                                                                                   常                  非常
                       中，  下 列 情     况     下  socket  可 读 :
关键 。在 网 络 编程
         socket  内  核  接收 缓存区 中 的 字 节 数 大 于 或     等    于    其 低    水           位  标记    SO_RCVLOWAT。 时
                                                                                                       此
            们    可以 无 阻塞
         我              地读该    socket，            并 且 读 操作 返回     的     字 节 数大     于 0。
    Q) socket    通信 的 对 方关闭 连接 。                  此 时 对 该    socket  的 读    将返回
                                                  操作                               0。
    ) 监听     socket    上 有 新    的    连接 请 求       。
    L) socket    上 有 未           处 理 的 错误   。此 时 我们 可以 使          用  getsockopt  来 读 取 和 清除 该错误。
列                下
    下
           情况       socket ATS:
    UL)          内 核发
         socket        送缓存区中                        的 可 用 字 节 数 大 于  或   等于 其    低水 位    标记 SO_SNDLOWAT。
         此 时 我 们 可以 无 阻塞    地              写 socket，该 并 且 写 操作 返回           的   字 节 数   大 于 0。
    Q) socket    的 写 操 作      被                   被关闭
                        关闭    。            对 写
                                             操作    的 socket 执行 写                 操作将    触发 一个 SIGPIPE 。
                                                                                   信和
                                                              成功或者失败 〈 超
                                                             时)                   之
                       使 用 非 阻塞     connect  连接                                  后。
         socket  上 有 未 处   理    的        错误 。 此 时 我 们 可 以 使        用 getsockopt  来  读   取 和 清除 该 错误 。
         络程序中         ，select     能   处 理  的  异常 情况   只   有     一 种  :  socket  上 接收 到 带 外 数据 。   下 面 我们
    网
详细讨论 之     。

9.1.3     ”处  理 带 外 数据
                                                                                                      处于
    上    一 小 节 提 到           ，socket 上 接收 到 普通 数据 和 带 外 数据 都 将                   使 seleet 返回  ， 但 socket
不 同 的 就绪状态          :           前 者处于 可 读    状     ，  后 者处    于 异常 状态    。  代 码 清单     9-1     了
                                                  态                                描述 select     是     如
         处理二者的。
何 同时

                        代码 清单 9-1                 ”同时 接收     普通     数据  和 带 外 数据
    Hinclude     <sys/types.h>
    include <sys/socket.h>
    #include     <netinet/in.h>
    #include     <arpa/inet.h>
    #include     <assert.h>
                 <stdio.h>
                 <unistd.h>
    #include     <errno.h>
    #include     <string.h>
    #include     <fcntl.h>
    #include     <stdlib.h>

    int main(    int   argc,     char*     argv[]     )

            if(  arge  <=    2     )

                       printf(     “usage:   ts     ipaddress                      basename(  argv[0] )
                    return     1;
           }

## Page 167

FIR 用
    IO 复 fe 149


        eonst     char*       ip   =   argv(1);
        int    port       =    atoi(   argv[2]       );

        int    ret      =   0;
        struct       sockaddr_in               address;
        bzero(       éaddress,         sizeof(        address    }    };
        address.sin_family                 =   AF_INET;
        inet_pton(           AF_INET,        ip,    &address.sin_addr            );
        address.sin_port               =  htons(       port    );

        int    listenfd         =   socket(        PF_INET,      SOCK_STREAM,     0    );
                            listenfd   >=   0   );
        ret    =  bind(             listenfd,  (     struct    sockaddr*        )&address, sizeof({ address ) );
        assert(          ret  !=    -1    );
        ret    =  listen(         listenfd,         5 );
        assert(          ret  !=    -1    );

    Struct sockaddr_in client_address;
    socklen_t client_addrlength = sizeof( client_address );
        int    connfd       =  accept(     listenfd,         (   struct     sockaddr*      address,
                                           éclient_addrlength               );
        if  (         connfd  <   0  )
        {
                  printf(             “errno   is:               errno      );
                  close(        listenfd        );
        }

        char     buf[1024);
        fd_set
        fd_set       exception fds;
        FD_ZERO(          &read_fds        );
        FD_ZERO(                                    );

        while(       1    )
        {
                 memset      (  buf,     '\0',     sizeof(       buf    )   );
                                                          要        在            fds
    /* 每 次 调用 select 前 都 重新 read fds exception PREXA HRA
                  发生之后，                描述               被          修改
connfd， 因 为 事件 文 件                             集 合 将     内 核         */
                 FD_SET(             connfd,   éread_fds
                 FD_SET(             connfd,           Gexception_fds   );
                 ret      =  select(      connfd      +  1,          Sread_fds, NULL,  éexception_fds, NULL   );
                 if {       ret   <  0   )

                              printf(            “selection          failure\n" );
                              break;
                 }

                 /*     对 于 可 读     事 ，采 用 普通的                                该取数据
                                         件               recv    函数             “/
                 if     {   FD_ISSET(      connfd,       éread_fds          ) )
                 {
                              ret   =  recv(       connfd,       buf,      sizeof( buf   -1, 0 );
                              if(   ret     <=   0    )
                              {
                                            break;
                              }

## Page 168

150 %* 第 二篇 深入 解析 高 性 能 服务器 编程


                printf(    "get    %d bytes of normal data:     和        };

        /* 对  于  异常 事件 ，采 用      带 Msc  OOB HAH recy                 +/
        else  if(    FD_ISSET(   connfd,       &exception_
                                                       fds  )   )
        {
                ret  =  recv(    connfd, buf,  sizeof(  buf    -1,   MSG_OOB );
                if({ ret <=   0    )
                {
                         break;
                }
                printf(    "get       bytes of oob data:             ret, buf };
        }
 )
 close(  connfd    };
 close(  listenfd    );
 return  0;
}


9.2   poll         系统 调用

                                      似，    也 是 在           指定 时 间 内 轮 询       一 定数 量 的  文件    描述 符 ，以   测试    其
    poll  系统 调用和 select 类
中 是  否有 就 绪                         的 原型
                   者  。poll          如下:
    #include         <poll.h>
    int   poll(    struct    pollfd*    fds,    nfds_t       nfds,    int     timeout  );
                          是一个                               结构
    1   fds 参数               pollfd         类型的             数组 ，它 指定          所有 我 们   感 兴趣 的  文件描述 符    上 发 生
                          等事件     。pollfd       结构    体 的     定义    如 下 :
的 可  读 、可写和 异常

    Struct  pollftd
    {
          int     fd;
          short       events;         /*            册的 事件     */
          short       revents;        /*                                         */
    hi
        中，  人弓     成 员 指定 文件描述              符   ，events  成    员 告     诉 poll  监听     上 的 哪些 事件    ， 它 是  一    系列
    其
           的按位或                  员                       ，        应用程序                                事件
事件                 ，revents      成 则 由 内 核 修改       以 通知 他 上 实际 发 生 了 哪些                                 。  poll
               的事件类型如表 9-1     所 示     。
支持

                                                                        类型
                                            表 9-1     poll              事件               办
                                                                                               和 |
和                                                                                  是     可作为      是 天 可 作为
POLLIN                            数据 优先             )
                      数据         普通 和             数据 可 读                         是                     是
POLLRDNORM         | ”普通     数据 可 读                                              是                     是
                      ”优先        〈Linux         不     )                                              Se:
POLLRDBAND         |         级带数据可读                     支持
POLLPRI               高优先级       数据 可 读 ，       如 TCP
                                    比               带 外数据                                              是
POLLOUT                        人                         )                       是
                      数据 〈 包括 普通 数据 和 优先 数据                    可 写                             |       是

## Page 169

第 9 章 ID 复用 “+ 151


                                                                                               〔【 续 )
    事件                                                                    是    否    可                输出
                                                                                                  作为
POLLWRNORM         |                                                      |     是                 是
POLLWRBAND         |    优先                                  |             是     |                 着
                                                                                                  |
POLLERR            | 铺误                                                   |      |                是
                       挂    起。  比 如 管道  的 写 端        闭 后，读 端  描述符     上 将 收
POLLNVAL           ”|   文件 述符没            开                               |                       是
    # 9-1    中         ，POLLRDNORM、POLLRDBAND、POLLWRNORM、POLLWRBAND                                     由
                                     上是将       POLLIN 事件
XOPEN 规范 定义            。它   们 实际                              和           事件
                                                                   POLLOUT               分得更细致    以 别
                                                                                                    ， 区 对
待 普通 数据      和 优先 数据    。
                       但           Linux  并 不 完全 支持 它们          。
                       需要根据          reev   调用 的 返回 值         来    区分 socket   上 接收 到 的 是   有  效 数 据还 是 对
    通，应 用 程序
                                     的处理。        不
方 关闭 连接 的 请            ，    并 做相应                                  内      2.6.17     JFK,   GNU 为 poll 系统
                       求                             过， 自   Linux        核                              调
        了一个                            事件 ， 它
用 增加               POLLRDHUP                     在socket    上 接收 到 对 方 关 闭            连接 的 请 求 之 后 触发 。 这
为 我 们 区分上 述 两种情况 提供                  了  一 种    更 简单 的 方式      。    但 使   用    POLLRDHUP     时，      们
                                                                                                     事件 我
                                                                                                       需要
在代码 最开始 处 定义                               _GNU_
                                        SOURCE。
    2 nfds         参数 指定 被 监听 事件集合               fds  的 大 小 。 其 类 型   nfds    t  的       如下:
                                                                                          定义
    typedef        unsigned   long int   nfds t;
        timeout
    3              人参数指定    poll     的          值，                 。      为 -1!                     将
                                        超时 单位 是               毫秒      当 timeonut 时     ，poll           调用
                                                                                                       永远
阻塞 ，直 到 某 个            发生，                       为0时                  将 返回。
                   事件 当 timeout                      ，poll    调用
                                                                          立即
                                                           。
    poll    系统 调用 的 返回 值           的   含义        与 select 相同

9.3     epoll                    系列 系统 调用

                        核事件表
9.3.1                      内

    特有 IO 复
    函数 它 在 实现 与 select、poll 有 很 大 差异 首
    epoll    是 Linux      的              用       。              和使用上                                 。
先   ，epoll   使用        组 函数   来   完成 任务     ，     而 不 是  单个 函数     。      次
                   一                                                  其     ，epoll   把    用 户 关 心 的 文件 描述
符 上 的 事件 放 在 内         核 里    的  一 个 事件 表 中         ， 从 而 无 须  像   select     和 poll   那样 每 次 调用 都 要 重复 传
人 文件 描述 符 集 或 事件                 集。但 epoll     需要 使     用 一 个  额外  的  文件 描述     符    ，    来 唯一 标识 内 核 中 的
        表。         这 个    文件      描述 符 使 用  如  下 epoll_create 函数
这 个 事件                                                                来      创建
    #include          <sys/epoll.h>
    int     epoll_create(     int size   )
    size    参数 现在 并     不 起     作用  ，   只 是 给 内      核 一 个  提示     ，  告 诉 它       事件 表 需 要 多 大 。  该 函数 返回
的 文件描述 符 将 用 作 其 他所有               epoll     系统 调用 的 第      一  个      参数，以 指定 要 访问 的 内 核 事 件 表      。
             PR        epoll           的 内    核  事件 表
    FAY

## Page 170

    152 第 二篇 深入 解析 高 性 能服务器编程


   #include <sys/epoll.h>
   int epoll_ctl( int epfd, int op, int fd, struct epoll_event *event )
    fd     BRE BRE                           RY,  op  参数 则 指定 操作 类                       。操 作      型     下
                                                                                     型      类 有 如         3 种  :
                                                              表中注册 fd 上 的 事件    。
    J                        ADD，                             往 事件
                                                     的    注册 事件         。
    CJ     EPOLL_CTL_MOD,                                 fd 上
                             DEL，  删除 包 上 的 注册 事件                       。
    口 EPOLL_CTL
                                       是epoll_event       结构 指针 类                                  的 定义
    event      参数 指定 事件 ，它                                                      型  。epoll_event            如下，
    Struct       epoll event
         (
               __uint32_t     events;      /*    epoll     B#     */
               epoll             data;     /*             */

          中        员               事件类型                   的                   事件类
                                                                               型
                                                                                     和               。      示
    其          events 成 描述                          。epoll     支持                        poll  基本 相同 表        epoll  事件
    KAMAL         poll    对 应 的   宏 前   加  上 “E”，         如                    可读        是
                                                          比     epoll    的 数据
                                                                                         事件        EPOLLIN。  但  epoll 有
    两 个  额外    的 事件 类 型       一                  和 EPOLLONESHOT。               它们 对 于     epoll      的   高效 运 作 非常    关
                                                  员用                         于存储
                                                                       用户     数据 ，   其 类  型 epoll_data_t  的 定义
    键 ，我 们 将 在 后 面 讨论 它们           。data   成                                                                  如下，
    typedef      union     epoll   data
         {
            void*     ptr;
            int  id;
            wint32   t    让了
                     ¢ u64;
         } epoll data t;
                                       体，
    cpoll_data_t     是   一    个 联合         其      4 个 成    员    中     使 用最 多 的       是    fd.                  CREAT Bh
    文件     描述 符  。ptr    员                 与              的用户              。                                         体，
                                     成 可 用 来 指定  fd 相关
                                                                         数据 但 由 于                      是    一      个 联合
                                       AN    fd 成     ，      员    因 此 ， 如 果 要 将            文件描述    符和 用 户     数据 关联 起 来
    我 们 不 能       同时 使  用  其 per WA
    《正如 8.5.2    小 节讨 论 的 将        句柄   和 事件          理 器 绑 定处          一 样 )，以 实现 快速              的数据 访问     ，只 能  使 用
    他手段 ，比        如            放弃
                              使用  epoll_data        t        的          ，而
    其                                                                    在           指向 的 用 户 数据
                                                          人      员            ptr                      中包含
                  成 时返回            失败
    epoll_ctl        功                     则     返回   -1 并     设置        errmno。
    9.3.2   epoll_wait 函数
    epoll      系列 系统 调用 的 主要 接口是                    epollL_wait     函数   。 它 在 一 段       超时        时 间 内 等 待  一  组 文件 描
    述 上 的 事件 ，其               如下:
    符              原型

    finclude      <sys/epoll.h>
    int     epoll     wait(   int  epfd,   struct         epoll_event*     events,       int   maxevents,
                              int  timeout       });
    FZ, PB  SIM DS                 A OC          A                                   [el  -1   并 设置 errno.
   参数
                                                          timeout                    的    含义   与poll HOA) timeout    参数
相同 。maxevents 参数 指定最多 监听 多 少 个 事件 ，它 大于
        必须 0。

## Page 171

                                                                                      第 9 章    IO 复用 ee 153

                        加       如果检测到事件，                  就 将    所有 就     绪的 事件  从    内 核 事件表   〈 由 epfd BAGS
    epoll_wait
定) 中 复制 到 它                的  第 二       个 参数 events         的     中。            数组                          到
                                        指向
                                                                 数组        这 个        只 用 于 输出  epoll_wait 检测
             就绪事件，  而 不       像            select
的                                       和    poll 的 数组 参数 那样              既用 于  传人用 户 注册 的 事件     ，  又 用 于 输出
内 核 检测 到 的 就绪事                    件     。 这 就 极大   地 提高了     应 用  程序          索引 就 绪 文件描述 符    的 效率 。  代 码 清单
          了这个                 。
9-2 体现

                                        代码 清单 9-2 poll       和    epoll   在 使 用 上 的 差别
    /*                  poll    返回 的 就    绪  文件描      特  */
    int   ret    =      poll(     fds,    MAX_EVENT     NUMBER,   -1     };
    /*         遍历所有 已注册 文件描述                   并    损 到  其 中 的 就 绪    者  【当然 ，可以 利用   ret 来硝做 优化  ) */
    for(     int    i   =    0;   i     < MAX EVENT  NUMBER;     ++i     )
    {
              if(       fds[i].revents     &     POLLIN    )     /*                       */
              {
                        int   sockfd     =    fds[i).fd;
                        /*      处理  sockfd    */
              )
    }

    /*       和如何 索  引   epoll   返回 的    就  绪 文    件描 述     */
    int   ret    =      epoll   wait(     epollfd,     events,     MAX_EVENT_NUMBER,   -1 );
    /*                        ret 个     文件 描述     */
    for   (   int       i= O;       i     < ret;  i++ )

              int       sockfd                events[i].data.
                                     =                    fd;
              /*    sockfd              */
    I


9.3.3    LT 和 ET 模式

   epoll  对 文件 描述 符 的 操作有 两种模式:                                (Level                  电 平 触 发   ) 模式
                                                       LT                 Trigger，        和            ET (Edge
Trigger，  边 沿 触发) Hh.             LT     模式 是   默认 的 工作 模式                ，这 种            下 epoll
                                                                             模式           相当 于     一       个 效率 较
               往        epoll 内 核 事件 表        注册     一 个   文件       描述    符    上 的    EPOLLET     时         将以
FAY poll。      当                                                                          事件 ，epoll
ET 模式 来 操作 该            文件              。ET 模式
                                   符            是    epoll            的 高 效 工 作 模式    。
            于采用     LT 工作 模式 的          文件 描述          符   当，   epoll_wait 检测          到 其   上有 事件 发    生   并 将 此
              对
    通知应用程序后，                        应 用  程序 可以 不 立即 处 理 该 事件                    。这
事件                                                                                     样，   当 应 用  程序下  一  次 调用
              时，epoll_wait           还 会 再  次    向 应 用    程序    通告           此 事件     ，直 到 该          被处理。
epoll_wait                                                                                               事件 而 对 于
采用  ET    工作 模式 的             文件    描述   符   ，当 epoll_wait                检测 到 其上 有 事件 发    生    并 将 此 事件 通知应
用 程序 ，应 用 程序必须 立即 处                     理 该 事件         ，                  为后续的
          后                                                  因               epoll_wait     调用 将 不 再 向  应 用    程序
    这一事件 。              可
通知                            见  ，ET 模式 在 很 大 程度             上 降低         了同一   个     epoll 事件 被 重复 触发    的 次 数 ，
                                 高   。 代 码 清单 9-3 体现           了       LT 和 ET 在 工作    方式    上 的   差异 。
因此 效率 要 比       LT 模式

## Page 172

  + 篇 ”深入 析 能
  154 第 二 解 高 性 服务 器 编程


                                                   代码   清单    9-3 LT  和 ET 模式
  See
  #include          <sys/types.h>
  #include          <sys/socket.h>
  finclude          <netinet/in.h>
  #include          <arpa/inet.h>
  #include          <assert.h>
  finclude          <stdio.h>
  #include          <unistd.h>
  finclude          <errno.h>
  finclude          <string.h>
  #inmclude         <fentl.h>
  finclude          <stdlib.h>
  finclude          <sys/epoll.h>
  finclude          <pthread.h>

  #define        MAX_EVENT_NUMBER                1024
  Wdefine        BUFFER      SIZE   10
  /*   SCRE
  int   setnonblocking({               int      fd  }
  {
           int old           option      =  fentl(      fd,  F_GETFL  );
           int             new_option    =  old    option    |   O_NONBLOCK;
           fentl(           fd,  F_SETFL,          new_option     };
           return
  }
                                    的                   到             的                              et
  *                          fd 上                   注册 epollfd 指示     epoll         FH           enable 指定
RGA fd        启用 ET    神  式  =*/
                                                                      et        )
  void    addfd(          int       epollfd,      int   fd,  bool enable
  {
           epoll          event    event;
           event.data.fd               =   fd;
           event.events             =    EPOLLIN;
                                 et    )
           if(         enable
           {
                          event.events            |=  EPOLLET;
           }                                                                                         1
           epoll_ctl(            epollfd,          EPOLL_CTL_ADD,     fd,   s&event );
           setnonblocking(                fd      );
  }
                    的工作流程
      LT  模式                       */
  void    ]t(    epoll_event*            events,         int  number, int   epollfd,   int listenfd  )
  {
          char          buf[  BUFFER_SIZE
          for       (    int  i  =  O;    i     <    mumber;  i++ )
          {
                          int    sockfd    =                        fd;
                                                  events(i).data.
                          if  (      sockfd     ==   listenfd )
                          {
                                                              in  client_address;
                                   struct              sockaddr
      socklen_t client_addrlength = sizeof( client_address );
                                   int   connfd       = accept(   listenfd, (   struct sockaddr* )éclient_address,
                                                                  &éclient_addrlength  };

## Page 173

用
WOR llO 复 *%* 155



                                      addfd(   epollfd,        connfd,   false   );  /*                    ETMR *+/
                      }
                      else    if     (  events[i].events           &    EPOLLIN    )
                      {
                              /*     K¥     socket                                                         */
                              printf(          “event       trigger  once\n"     );
                                               buf,      ‘\0',  BUFFER_SIZE      );
                              int      ret     = recv(       sockfd,    buf,  BUFFER  SIZE-1,    0     );
                              if(      ret               )
                              {
                                        close(           sockfd    );
                                        continue;
                              I
                              printf(          “get      %d  bytes   of     content: ts\n",      ret,  buf );
                      }
                      else
                      {
                                               “something       else    happened     \n"   );
                      }
         }
     }
     /*     模式
        ET      的工作流程             “/
     void   et(    epoll   event*       events,          int   number,  int   epollfd,       int listenfd   )
     {
            char    buf[   BUFFER SIZE           ];
            for  (   int   i  =    O; i      <   number;       i++ )
            {
                     int   sockfd     =      events[i].data.fd;
                     if (    sockfd     ==       listenfd      }
                     {
                              struct         sockaddr_in        client_address;
                              socklen_t          client_addrlength          =  sizeof(         client_address );
                              int     connfd     =     accept(  listenfd, (    struct     sockaddr*   )&client_address,
                                                                 &client_addrlength          );
                              addfd(         epollfd,        connfd,    true  );     /*                  ETMA   */
                     }
                     else    if   (    events[i].events            &  EPOLLIN    )
                     {
                             /*                                       FURNMRERAR,                CARE  socket
所 有     读出   */                                                            ，
                             printf(         “event       trigger    once\n"   );
                             while(        1   }
                              {
                                       memset(         buf,    "\0',  BUFFER_SIZE
                                       int     ret     =  recv(    sockfd,   buf,    BUFFER_SIZE~1,      0 };
                                       if(     ret     <  0  )
                                       {
                                                 /*                                                        LG,
epoll                sockfd EM EPOLLIN Bf,                                        */
                                                 if(      (          == EAGAIN    )  ||    ( errno ==  EWOULDBLOCK )   )
                                                 {
                                                               printf(   “read   later\n™    );
                                                               break;

## Page 174

    156 者  篇  性 编程
        °° 第 一二 深入 解析 高 能服务 器




                                             }
                                          close(     sockfd );
                                          break;
                                   I
                                   else   if(   ret  == 0 )
                                   {
                                          close(     sockfd );
        }
        else
        {
        printf( "get td bytes of content: %s\n", ret, buf );
        }
        )
        }
        else
        {
        printf( "something else happened \n" );
        }
      }
)

    int main(   int      arge,   char*      argv[]  )
    {
         if(  arge    <=   2   )
      {
        printf( “usage: ts ip_address port_number\n", basename( argv[0] ) );
        return 1;
      }
        const     char*     ip   =   argvy[1];
        int   port    =  atoi(                  );

        int   ret   =   0;
        struct                     in     address;
        bzero(                       sizeof(    address  ) );
        address.sin_family             =  AF_INET;
        inet_pton(       AF_INET,      ip,    s&address.sin_addr   );
        address.sin_port             =  htons(  port   );

        int   listenfd       ~   socket(      PF_INET,   SOCK_STREAM, 0 );
                    listenfd         >= 0  );

        ret   =   bind(   listenfd,        (  struct   sockaddr*          sizeof( address )   );
        assert({     ret    !=  =-1    );

        ret   =   listen(        listenfd,    5 );
        assert(      ret    !=   -1    );

        epoll_    event   events[      MAX_EVENT NUMBER
        int   epollfd     =          epoll create( 5 );
        assert(     epollfd        !=  -1  );
        addfd(    epollfd,       listenfd,      true   );

        while(    1   )
        {
                  int   ret    =       epoll_wait( epollfd,  events,  MAX EVENT NUMBER, -1 );

## Page 175

    第 9 章 VORMR fe 157


                         if     (    ret < 0  )

                                      printf(  “epoll     failure\n"   );
                                      break;
                           }

        lt( events, ret, epollfd, listenfd ); /* @ALT RR */
                                events,     ret,     epollfd,     listenfd     );   /*
              }

             close(         listenfd      );
             return         O;
       }

       读者 不 妨 运行 一下这 段 代                       ，
        然
        码 后 telnet 到 这 个 服务 器 程序上并 一 次传输超过 10 字 节
                                                    后          和                            模式
                                                然     比较       模式
    (BUFFER_SIZE           的 大小 )         的 数据 ，             LT      ET                的    异同  。 你 会 发     现 ，正 如 我
    们      的                    下        被触发        数要         下少很多。
       预期      ，ET  模式
                                事件         的 次 比 LT 模式

                                         的    描述          都      非 阻塞
    注意             每 个 使用       模式       文件
                         ET                                  应 该   是      的。    如 果 文 件         述 符 是 阻塞       ，   么
        的 那
读 或 写操作 将 会 因为没有 后 续 的 事件 而 一 直 处 于 阻塞 态〔〈饥 渴 态 )。

    9.3.4    EPOLLONESHOT 事件

              我们使用              模式 ，一
       即使                       ET         个 socket        上 的 某 个     事件 还    是   可 能 被 触发 多     次。这 在 并 发       程序
    中 就 会 引起    一   个 问题 。比 如              一  个 线程    〈   或 进程       ， 同)                                         后开
        下 在 读 取 完 某 个 socket 上 的 数据
        可 〈EPOLLIN 再 次被
始 处 理 这 些 数据 ，而 在 数据 的处理 过 程 中该socket 上 又 有 新 数据
    发)，此 时 另外       一       个 线程被唤醒 来读取 这 些新的 数据。于 是 就 出现了 两 个 线程同时 操作                                           一 个
    socket 的 局  面   。    这 当然    不    是 我 们 期 望     的    。 我 们 期 望的    是     一 个 socket  连接 在 任    一时 刻     都 只  被 一
    个 线程 处     理。 这     一       点 可 以 使用epoll       的 EPOLLONESHOT     事件 实现            。
           于        了                               的文件        符，                                  上
       对
             注册 EPOLLONESHOT 事件
                                                               描述              操 作 系 统  最 多 和 触发 其  注册 的      一  个 可
                                                一次，        除 非 我 们 使用 epoll        ctl  函数 重 置 该    描述  文件     符 上 注
    读、可 写 或者异常 事件 ，且 只 触发
       的EPOLLONESHOT            事件 。这
    册                                      样，       当        个 线程 在 处 理基    个     socket 时，                   其 他 线程
                                                    一                                                          是不可能有
       会操作该                     的    。 但 反 过  来 思考    ，       注
                                                             册了                         事件
    机                                                                                       的                 socket
                socket                                         EPOLLONESHOT                         — A          RAE
        处理完毕，              该           就应该     重置这个            的                                         ，        这个
    线程                      线程 立即                            socket 上 EPOLLONESHOT 事件                           以 确保
    socket 下   次 可 读            ，其 EPOLLIN      事件
           一                    时                         能被和     ，    进 而 让 其 他       工作 线程      有 继续处机 会       理 这
    个 socket。
       代码    清单     9-4     展示 了                                       事件
                                         EPOLLONESHOT                 的使用 。
                                          代码  清单9-4     ”使   用EPOLLONESHOT     事件
       #finclude    <sys/types.h>
       #include               <sys/socket.h>
                              <netinet/in.h>

## Page 176

 篇 解析 性 服务 编程
 158 * 二 深入 高 能 器
     °° 第 =



         #include         <arpa/inet.h>
         finclude         <assert.h>
         #include         <stdio.h>
         #include         <unistd.h>
                          <errno.h>
         #finclude        <string.h>
         #inelude         <fentl.h>
         #include         <stdlib.h>
         #include         <sys/epoll.h>
                          <pthread.h>

                       MAX_EVENT_NUMBER            1024
        #define        BUFFER_SIZE     1024
        struct         fds
         [
                     int     epollfd;
                     int     sockfd;
         ] ;

        int     setnonblocking(      int           fd    )
         {
                     int     old    option  =     fcntl(           fd, F_GETFL     );
                     int     new_option     =                      |   O_NONBLOCK;
                     fentl(      fd,        F_SETFL,    new_option         );
                     return            old_option;
        }
                                     #                                 到             指
        /*      将                               上 的 EPOLLIN                          示的                表     ， 数
                    fd                              EPOLLET    车                          件注册 epollfd
        是否往册         fd   |                EPOLLONESHOT 事件     */                               epoll 内      件 中 参 oneshot
指定
        void addfd(          int          epollfd,     int     fd,     bool oneshot  )
        {
                                            event;
                     event.data.fd     =         fd;
                     event.events     =     EPOLLIN            |     EPOLLET;
                     if(     oneshot  )
                     {
                                      event.events     |=  EPOLLONESHOT;
                     }
                     epoll_ctl(             epollfd,             EPOLL_CTL_ADD,    fd, sevent      );
                     setnonblocking(            fd     );
        )
                    于                。            操作           ，              的                       被      操作    会
        /*    重        fd 上 的    事件     这样           之        尽 上                                  事件
 发 fda       的                       ，            一次     后    fd                   EPOLLONESHOT        注册      是 系统 仍然
        上     EPOLLIN 事件             且 只 触发          */
        void    reset_oneshot(                int  epollfd,   int          fd      )
        {
                epoll        event         event;
                event.data.fd        =      fd;
                event.éevents        =    EPOLLIN     |          EPOLLET    |      EPOLLONESHOT;
                epoll_ctl(           epollfd,        EPOLL_CTL_MOD,                fd, sevent    );
        }
        /*                   */
        void*       worker(      void*        arg  )
        {
                int       sockfd     =  (  (fds*)arg             )->sockfd;

## Page 177

第 9 章 VORA “we 159


    int    epollfd       =   (         (fds*)arg  )=->epollfd;
    printf(       “start       new    thread      to receive  data  on  fd:  %d\n", sockfd );
    char    buf[ BUFFER_SIZE             ];
    memset({      buf,     '\0',      BUFFER_SIZE     );
    /*           MARR sockfd              WPF)           AGAIN HR   */
    while(     1    )
    {
            int     ret    =       recv( sockfd,    buf,     BUFFER_SIZE-l,  0 );
            if(     ret    ==   0   )
            {
                       close(       sockfd     );
                       printf(        “foreiner      closed  the  connection\n"  );
                       break;
            }
            else      if(     ret   <    0 )
            {
                       if(         errno   == EAGAIN  )
                       {
                                  reset_oneshot(        epollfd,    sockfd  );
                                                                      _
                                  printf£(     "read  later\n”    );
                                  break;
                       I
            }
            else
            {
                       printf(        "get    content:            buf  );
                       /*           Ss,
                       sleep(       5    );
            }
    }
    printf(         "end   thread        receiving    data  on fd:      sockfd     );
}

int main({  int     argc,          char*   argv[]    )
{
    if(     arge    <=   2   )
  {
    printf( “usage: %s ip_address port_number\n", basename( argv(0] ) );
                  return   1;
    }
    const    char*       ip    =  argv[l];
    int     port    =  atoi(      argv[2]      );

    int    ret    =   0;
    Struct     sockaddr_in            address;
    bzero(     taddress,          sizeof(     address    )  });
    address.sin_family                =    AF_INET;
    inet_pton(         AF_INET,          ip,                        };
    address.sin_port              =   htons(     port );

    int         listenfd   =   socket(     PF_INET,     SOCK_STREAM,   0   );
    assert(       listenfd        >=     0 );

## Page 178

篇  性 编程
160 第 二 深入 解析 高 能 服务 器


      ret      =       bind(            listenfd, (   struct       sockaddr*        )5address，    Sizeof( address  ) };
                         ret      !=   =]    );

      ret      = listen(          listenfd,           5  );
      assert({           ret    !=     -1    );

      epoll_ event            events[{           MAX_EVENT         NUMBER ];
      int      epollfd        =     epoll_create(            5     );
      assert(          epollfd           !=    -1 );
                       ，     听                                        上是不能注册                 的
           /*  注意        监 socket                listenfd                           事件       ，     否则              只
                  后          连接                              触发                   EPOLLONESHOT        应     用   程序 能 处 理
客户                                                                         的
一 个   连接  !                 因为 续 的 客户        请 求 将 不 再       listenfa 上 BEPOLLIN 事件 。/
      addfd(                 epollfd,    listenfd,       false       );

      while(        i    )
      {
                 int     ret     =     epoll      wait{      epollfd,     events,     MAX_EVENT_NUMBER,   ~-1   );
                 if    (    ret <        0   )
                 {
                             printf(           "“epoll   failure\n"       );
                             break;
                 }

                 for     (   int       i   =   O; i  <   ret;          )
                 {
                             int       sockfd     =
                             if     (    sockfd      ==  listenfd      )
                             {
                                           Struct    sockaddr_in          client_address;
                                           socklen_t               client_addrlength   = sizeof(  client_address     };
                                           int   connfd  =             listenfd,   (   struct sockaddr*
                                                             监         描述 &client_addrlength         );
                                           /*    对 每 个 非     听     文 件    符 都    注册 EPOLLONESHOT    事件    */
                                           addfd(    epollfd,        connfd,     true
                             }
                             else        if    (  events[i].events        &     EPOLLIN  )
                             {
                                                       t thread;
                                                 pthread
                                           fds               fds for_new_worker;
                                                             worker.epollfd     =      epollfd;
                                           fds_for_new_worker.sockfd               =   sockfd;
                                           /*    新 启动 一个工作 者 程为                          */
                                         pthread _create(            &thread,    NULL,   worker,    ( void*    )
                                                                     &fds_for_new_worker        );
                             }
                             else
                             {
                                         printf({       “something          else happened  \n"    );
                             }
                }
      }

          eclose(            listenfd    );
           return  0;
    }

## Page 179

    WIR VORA 161


        从 工作 线程函数                  worker      看，     果                            工作             处               某                                    求
                                   来 如                                一     个                     线程 理 完               个  socket         上 的   一 次 请     〈 我
        用                      这个过程)      后，                                       到该                                               求                    将继续
们
   休眠 Ss 来模拟 之 又 接收 socket 上 新 的 客户 请 ， 则 该 线程
                                   该    socket                        上  注册            了 EPOLLONESHOT 事件                                 ，             其他 线程
    为 这 个     socket   服务     。    并     且 因为                                                                                                            没有机
                                   FF     5                               后        仍然                没收   到 该 socket      上 的 下      一    批    客户 数据   ，   则
    会 接触 这 个         socket,   WIR     TREY
                     为该 socket     服务 。     时，                                       它 调用 reset_oneshot      来重置该                         上的注册事件，
    它 将 放弃                         同                                                              函数 socket
    这 将使     epoll       有 机 会 再  次 检测 到 该     socket                         的                   ，                               得          有
                                                                      上 EPOLLIN 事件                                 进 而         使 其 他 线程              机 会 为 该
                 。
    socket      服务
                                                                                                                                          时刻肯定           只 有
        由 此 看 ，尽                   一个     socket     在    不    同时间 可 能 被 不                        同    的 线程         处          ， 但同一
    来                          管                                                                                          理                                一
                               这 就 保证 了 连接 的 完整在为它服务。                     性            ，    从 而 避免 了 很 多 可 能 的                       态 条件     。
    个 线程

        组                          用     的                             比较
    94     三              MO                                         复 函数

                                                    了    select. poll 和 epoll 三
                                            前 面 我 们讨论                              VO    组        复  用 系统 调用           ，  这     3  组 系统 调用 都              同时能
    监听 多 个 文件    描述                符     。    它们 将 等    待    由     timeout         参数 指定 的        超时              时 间     ，     直 到      一 个    或 者   多 个 文件
    描述 符上有 事件 发生时 返回 ，返 回          值 是                                    就   绪    的 文件           描述     符    的 数量        。        回         没有            发
                                                                                                                                                      返 0 表示
                                                                                                                                                       事件
                                   集、最 大     支持 文件 描述 符                                数      、    工 作 模     式 和 具体 实现             等     四 个 方面       进一步 比
    生。现 在 我 们 从 事件
    较 它们 的 异同 ，以 明确 在 实际应用 中应 该 选择 使用 哪个                                                              〈      或    哪些 )。
        这     3 组 函数 都        通过     某 种    结构     体 变量 来                 告诉           内      核    监 听 哪            些文件描述         符      的 哪些 事件           并 ，
                                                                                                                                    上                      使
    用 该     结构体          类 型的 参数 来    获取     内 核    处                 的                 结果        参数
                                   理                                                          。select 的           类型          人aset      没有        将 文件 描述 符
      事件
    和     绑定，              它仅仅     是     一     个 文件 描述 符 集合                              ，    此                                              的
                                                                                   因 select       需要 提供                   3             个 这 种 类 型     参数 来 分
        人和输出可读、                    可 写 及 异常
    别传                             等事件。                                   这        方面
                                                                          一                            使得 select  不    能      处  理        更 多 类 型的  事件 ，另
                                                                    的在线修改 ，                               应 用 程序
    一     方面由于 内 核对 fd_set 集合                                                                               下次调用  select                前 不得 不 重 置 这3 个
                                   类型     则 多
        KR.     的 参数               少                                     聪                    一
    set        poll                pollfd                             “                明 ”        些。    它 把 文件 描述                  符和 事件         都 定义  其中 ，
    任何 事件 都 被 统一 处                 ，    从 而 使 得 编程                    接口简洁                    得         。 并 且 内 核 每            次  修改 的     是   pollfd      体
                                   理                                                              多                                                   结构
        员，而                   events  成     员 保持
                                   不变，                                                          因 此 下 次 调用  poll  时 应 用 程序
    的     revents     成                                                                                                                  无须重置              类pollfd
                         集参数。由     于 每 次 select 和 poll                   调用 都                 返回 整个          用 户  注 册 的 事件               集合    〈      其 中 包括
    型 的 事件                                                                                                                                                 就
    绪的和 未 就 绪              的)，所以    应 用     程序                                             索引 就 绪 文件 描述 符  的 时 间  复 杂 度            为O    (Cn). epoll    则 采用
    与 select 和 poll        完全 不 同 的     方式 来 管理用 户注册 的 事件 。它 在 内 核中维护 一个事件 表，                                                                              提
                                                                                                                                                           并
    供 了     一 个 独立 的 系统 调用         epoll_ctl     来     控制 往               其            中      添加    、     除、                        。      样，         次
                                                                                                  删 修 改 事件 这 每                                        epoll_
                                                                                              的        事件 ，  而 无 须 反 复 从 用 户                 空间    读 人 这 些 事
    wait                                                                  调用 都 直接 从 该 内 核 事件 表 中 取得 用 户注册
        系统调用的 events 参数 仅用 来 返回 就绪的 事件 ，这 使 得应用 程序 索引 就 绪 文
件 。epoll_wait
件 描述 符的时间 复 杂 度 达到 O (1).
        poll 和 epoll_wait          分 别用 nfds 和     maxevents                                                                参数 指定 最 多 监听 多 少 个 文件 描述符和 事件 。
    这 两    个数值        都    能达到 系统 允许 打开 的    最                           大  文件 描述 符 数             ，
                                                                                                               目   即 65 535           (cat/proc/sys/fs/file-
    max)。      而 select    允许 监听 的 最 大 文件                             述 符 数 量 通                   有    限制     。    虽 然 用 户                   可以 修改 这 个 限制  ，
    但 这 可 能 导致不可 预期 的后果            。

## Page 180

    162      第 二    篇   深入 解析 高          性       能      服务 器    编程

        在 ET 高 效 模式。
    select 和 poll 都只能 工作在 相对 低 效的LT 模式 ，而 epoll 则 可以 工作
        且 epoll 还 支持  EPOLLONESHOT 事件                            。    该               事件   能               减少可读、                                   等事件被
    并                                                                                           进一步 可 写                                            和 异常
             的次数。
    触发
             原理上来说               ，select
  从 实现                                                和 poll  采用 的 都                      是轮 询 的       方式 ， 即 每 次 调用 都 要 扫描         整个                注
 描述符集合，                     并 将    其     中 就          绪   的 文件 描述 符 返 回                       给        用 户 程 序    ，                  因 此 它们 检测 就 绪 事件 的
       文件
    算法 的 时 间 复杂 度            是0 Cn)。epollL_wait                 则     不                   ， 它 采用 的 是 回调 的 方式                    。                内 核 检测
                                                                                      同                                                             到就绪
    的 文件描述 符     时   ，  将 触发     回调                    函数 ，                       回 调 函 数 就 将    该 文件描述    符    上 对 应        的  事件 人 内 核    就         绪
    事件 队列。内 核最后 在适当 的                    时 机 将               该  就    绪                       事 件 队列 中  的 内 容 拷贝 到 用 户               。     此
                                                                                                                                   空间          因 epoll_
    wait   无 须轮询 整个 文件 描述 符 集合来检测 哪些事件                                                          已      经 就 绪 ，其   算法       时 间  复 杂 度    是    O    (1).
        是，当     活动 连接   比较 多 的 时                        ，epoll_wait     的 效率
                                                                                                未必比                 select
    但    和                                                                                                            poll 高    ，              因 此 时 回调
                                                 候                                                                                  为                 函
                                 以                             适用于连接数量多，                                   连接
    数 被 触发 得 过     于 频繁      。      所    epoll_wait 但 活动                                                                  较 少    的 情况    。
             后，为了便于 阅读 ，                 我
          最                              们            将   这  3 组 IO              复 用 系统 调用 的 区 别 总                   结     于   表 9-2   中  。
                                          表 9-2                 select.                   和
                                                                                                 poll
                                                                                                epoll  的 区 别
           系统   调用          |            select,                 |                              pl                                 epoll
                              用 户        通过 3         个   参数 分       别传                                                   内 核 通过    一     个 事件  表 直
                                         的
                                            可
                                                        读、                    |           此     二   二            ”|  理              感
                             人 感兴趣 可 写 及 异                                                                        接管 用 户                   兴趣 的 所 有
                                等事件 ，                 内 核 通     过 对 这 些
                             常                                                                             传人        事件 。因 此 每次调用 epoll_
                                                                                                           通过
                                                                                                                 “|           时，     传人
       事件 集合                 参数     的    在 线  修改 来           反馈     其 中          用 户 通过                                             用户
                                                                                                       内 核           wait          无 须反复
                                就绪事件 。                这 使    得  用 户   每          修改             revents
                             的                                                                                       感兴趣
                                   调用                           这             |                 aca        反馈 其                       的事件   。epoll_wait
                             次           select       都 要 重      3               中                                   系统调用          的 参数     events 仅 用
                             个 参数                                                                                    来                 就绪的事件
                                                                                                                          反馈
       应 用 程序索引 就绪文件

       最 大 支持   文件 描述 符 数                                                                                                 65 535
                                                                                                                                ET   高效
       工作 模式                                                                                                                        模式
                                                                                                                          支持
                                                         采用 轮 询方式来检测 就绪               采用 轮 询 方式来检测 就                      采用 回调 方式来检测 就     绪
                                                             时间复杂度为        绪 事件 ，算 法 时 间 复 杂 度 ”|事件 ，算 法 时 间 复 杂                                度 为
       内 核实现和 工作 效率| 事件， 算法
                             O(n)                                                43      O(n)                        OU)

                                         应                    :                             塞
    9.5      复 用 的            高 级                     用      一             非 阻              connect

         conneet  系统 调用 的       man      手册 中 有 如 下                       一 段         内 容   :

         EINPROGRESS
         The socket  is nmonblocking                    and     the    connection               cannot     be     completed         immediately.
    It  is  possible  to     selecti(2)               or            poll(2)      for        completion     by     selecting         the     socket
    for  writing.  After     select(2)                            indicates                                use                      to     read
    the  SO_ERROR  option     at     level            SOL_SOCKET                 to       determine        whether          connect()      completed
    successfully   (SO_ERROR     is       zero)           or     unsuccessfully                                      is      one   of     the usual
érror codes listed here, explaining the reason for the failure).
             了                       出 错时
         这 段 话描述     connect                            的一种 errno {fi                     ，EINPROGRESS。              这 种 错误         发    生 在 对 非

## Page 181

                                                                                         第 9 章 IO  复用 *%s*   163

    的                                                                   。    据           的     ，              下，
塞        socket 调用    connect，     而 连接 又 没有 立即     建立                  根     man               文档
                                                                        时                       解释      在 这 种 情况
    们                                      函数     监听                      socket         。
我               可以 调用 select.    poll   等     来                    这 个 连接 失败 的           上 的 可 写   事件 当  select.
                            ，                      来读
                                            取错误码并清除该                                     上   的
poll     等 返回                                再 利用 getsockopt                             错误。
                      后                                                                socket 如 果 错          误 码
 0，表示 连接              成 功 建立     ，和     否则 连接 失败     。
是
     通过                        塞            ，                                          多        。
            上 面 描述 的 非 蛆                  conneet 方式 我们     就 能 同时 发 起                   个  连接 并 一 起 等 待   下 面 看
看非 阻塞                 的 种 实现                如 代码     清单
            connect        一                         9-5              所 示    。

                                            代码 清单      非 阻              塞    connect
     #include     <sys/types.h>
     #include     <sys/socket.h>
     finclude     <netinet/in.h>
     #inelude     <arpa/inet.h>
     #ineclude    <stdlib.h>
     #ineclude    <assert.h>
     #include     <stdio.h>
     finclude     <time.h>
     #include     <errno.h>
     Hinclude     <fentl.h>
     #include     <sys/ioctl.h>
     finclude     <unistd.h>
     #include     <string.h>

            BUFFER_SIZE               1023

     int            setnonblocking(    int  fd  )
     {
           int old         option       =      fentl(    fd, F_GETFL    );
           int      new_option          =  old  option     |    ONONBLOCK;
           fentl(          fd,      F_SETFL,      new    option };
           return          old      option;
     }
     /*     超时 连接 函数 ，参 数              分 别 是        器        址，     口号      时间         秒
                                            服务     地 端                  和 超时 《 毫
  KAW    socket,                 -1    */
     int    unblock_connect(            const   char*     ip,   int   port,    int   time   }
     {
           int      ret    =     0;
                                         in     address;
           struct          sockaddr
           bzero(          égaddress,      sizeof(     address    )   );
           address.sin_family               =   AF_INET;
           inet_pton(               AF_INET,    ip,     &address.sin_addr       );
           address.sin_port               =     htons(   port   );

           int      sockfd     =   socket(     PF_INET,     SOCK_STREAM,         0   );
           int      fdopt      =   setnonblocking(     sockfd         );
           ret      = connect(          sockfd,     (    struct sockaddr*                sizeof(   address ) });
           if     (   ret    == 0    }
           {
                      /*         如 果 连  接 成     功 ，则     sockfd   的 属性       ，并 立即 返回    之   */
                      printf(           "connect     with     server  immediately\n"     );
                      fentl({     sockfd,     F_SETFL,     fdopt        );

## Page 182

> 第 二
  篇  解析 性 编程
164 *> — 深入 高 能 服务 器




                 return     sockfd;
}
else         if    (    errno   != EINPROGRESS     )
{
                 /*    如 果  连                       接 没有立 印 建立 ，那 么只有 当 errno
出错返回                                                              EINPROGRESS 时 才 表 示和连接 还 在进行，
                 printf(     “unblock     connect     not support\n" );
                 return    -1;
]

fd_set           readfds;
fd          set  writefds;
struct           timeval     timeout;

FD_ZERO(                        );
FD_SET(            sockfd,      é&writefds   );

timeout.tv_sec                = time;
timeout.tv_usec            =     Q;

ret        =    select(     sockfd +  1,     NULL,                 NULL, &timeout   };
if      (    ret     <= Q   )
{
                /*   select                               «/
                Printf£(    “connection      time     out\n"   );
                close(    sockfd   );
                return    -1;
}

if      (    !   FD_ISSET(      sockfd,                    )   )

                printf(     "no    events  on  sockfd     found\n" );
                close(    sockfd   );
                return    -1;
}

int           error  =  Q;
socklen_t            length     =  sizeof(    error   );
/*           WA  getsockopt              职并清除 sockfa  上 的     错误 */
if(        getsockopt(            sockfd，S0OL  SOCKET，    SO_ERROR,      S&length     ) < 0 )
{
                 printf( “get     socket     option      failed\n" );
                 close(     sockfd );
                 return     -1;

        锚     误  号 不   为0 表示 连接 出    错     */
if({          error  !=   9  )
{
                 printf(    "connection    failed     after select with  the error: td \n", error };
                 close(     sockfd );
                 return     -1;
}
/*      和连接 成功 */

## Page 183

    SOR 1O 复用 we 165


                      printf(  “connection            ready    after select  with the socket: td  \n",  sockfd );
                      fentl(       sockfd,    F_SETFL, fdopt   );
                      return       sockfd;
         }

         int main(        int  arge,       char*        argv[) )
         {
               if({     arge   <=    2   )
               {
                        printf(          “usage:      ts        port_number\n",           basename( argv[0] )  );
                        return          1;
               }
               const    char*       ip      = argv[1];
               int      port   =        atoi( argv[2]      });

               int      sockfd    =     unblock_connect(     ip,     port,   10 );
               if     ( sockfd       <   0    )
               {
                        return          1;
               }
               close(           sockfd   );
               return        0;
         }

         但 遗憾 的         是  ，  这 种 方法              存在几     处 移植 性 问题               非 阻 塞  的socket  可 能  导致 connect
                         次    ，select         对 处 于EINPROGRESS     下                           起    。    后，     于
    始终 失败 。其                                                                             状态 的 socket 可 能 不 作用 最 对
    出 错           的 socket, getsockopt  在       有 些 系统     Linux)          上 返回           代码 清单         所 期 望的 )，
               在有些系统             源自伯克利的               UNIX)     上 则       返回       这些 问题  没有  一 个 统一 的  解决方法    ，
    而
                        可自行      参考相关 文献。
    感 兴 趣 的 读者

                                        用             室
                                                                              程序
    9.6     IO                          复 用 的 高 级  应     二  :                 聊天

         像  ssh 这 样 的         登录 服务 通常                要 同时 处 理 网     络 连 接 和 用 户 输入      ，这     以使用
                                                                                                     岂可 ITO 复 用 来
                                                                                         如 何  使用  IO       复 用 技术
    实现 。本 节 我 们以 poll                 为    例   实  现  一 个 简单的聊天 室 程序             ，以 阐述                          来同
               络 连接                  户       。该聊天     程序能     有                   同时
    时 处 理 网             和 用             输入            室      让 所     用 户                   在线群聊， 它 分 为        端和服
                                                                                                               客户
    务 器 两个部 分 。其 中 客户 端 程序有两个功能                       :一     是     从 标准 输入 终端             读 人 用 户   数据  ， 并 将 用 户
发送至服务器 ，                         二 是 往 标 准            输出 终端打印 服务 器 发送给 它            的    数据 。 服 务 器 的     功能 是 接收
    数据
    客户 数据 ，并 把 客户 数据 发送给 每                            一                        个登录 到 该 服务器上 的 客户端〈 数 据 发送者 除外 )。下
    面 我 们 依次 给 出 客户 端                   程序    和 服务 器 程序    的    代码   。
                          客户
    96.1                   端
               端          程序 使用  poll      同时 监听 用 户     输入    和 网 络 连接         ， 并 利用 splice 函数 将 用 户   输入   内 容
         客户
    直接 定向 到 网 络           连接     上以 发 送             之 ，从 而 实现 数据    零    拷   贝  ，提 高  了  程序执行 效率。客       户    端程序

## Page 184

篇  能 器
166 *+ 第 二 深入 解析 高 性 服务 编程


如 代码 清单
         9-6    所 示 。

                                                   9-6      室
                                     代码 清单                         客户端程序
                                                                     ”聊天
一
                                                           一
¥deline         _GNU_SOURCE   1
             <sys/types.h>
#include     <sys/socket.h>
#include     <netinet/in.h>
fiinclude    <arpa/inet.h>
#include     <assert.h>
finclude     <stdio.h>
#include     <unistd.h>
#include     <string.h>
#include     <stdlib.h>
#include     <poll.h>
#include     <fentl.h>

#define    BUFFER   SIZE    64

int main(    int    argc,      char*  argv[]       )
{
     if(     arge   <=  2   )
     {
                                                                                   ( argv[0) )
               print£(      “usage:     ts      ip_address                  basename
               return      1;
     }
     const      char*    ip    =  argv(1);
     int     port =   atoi(       argv[2]       );

     struct      sockaddr_in         server      address;
     bzero(      &server_address,            sizeof(    server_address  )   );
     server     _address.sin_family              = AF_INET;
     inet_pton(       AF_INET,        ip,    &server_address.sin_addr       );
                                             =   htons( port  );

     int     sockfd   =               PF_INET,         SOCK_STREAM, 0 };
                  sockfd      >=  0   );
     if    ( connect(         sockfd,   (    struct     sockaddr* )&server_address, sizeof
             (   server_address         )    )   < 0   )
     {
                printf(     “connection          failed\n"    );
                close(   sockfd       );
                return   1;
     }

     pollfd         fds[2);
     /*                         HERA) 和 文件描述特              socktfd  上 的 可 读 事件 “/
                =        Q;
     fds[(0].events         =  POLLIN;
     fds(0].revents           = 0;
     fds[1l].fd =          sockfd;
     fds[1).events          =   POLLIN     |    POLLRDHUP;
     fds[1].revents           = 0;

## Page 185

用
第 9 章 IO 复 e+ 167



  char    read    buf (BUFFER_SIZE];
  int  pipefd[2];
  int  ret     =  pipe(   pipefd   );
  assert(      ret   !=  -1  );

  while(     1   )
  {
       ret     =  poll(   fds, ?,   -1l  );
       if(     ret   < 0  )
       {
                  printf({   “poll    failure\n"  );
                  break;
       }

       if(                       &     POLLRDKUP  )
       {
                  printf(    “server    close the  connection\n" );
                  break;
       }
       else      if(      fds[1).revents  & POLLIN   )
       {
                  memset(           buf,  '\0',  BUFFER_SIZE );
                  recv(   fds(1).fd,     read_buf,   BUFFER_SIZE-1,    0 );
                  printf(    "ts\n",    read buf  );
       }

       if(     fds[0).revents     & POLLIN    }
       {
                  /*   2   splice   将用 户    输入 的 数据  直接 写 到 sockfa  k  (FHM)  */
                  ret  =  splice(   0，NULL，Pipetd[1]，NULL，32766，
                                         SPLICE_F_MORE  |      _F
                                                                     SPLICE
                                                                       MOVE );
                  ret  =  splice(   pipefd[0),   NULL,  sockfd,  NULL,    32768,
                                         SPLICE_F_MORE  |      _F
                                                                     SPLICE
                                                                       MOVE );
       }
 }

  close(              );
  return  Q;
}



           器
9.6.2    ”服务

     器     使
               用                                                       用
 服务
    程序 poll 同时 管理 监听 socket 和 连接 socket， 并 且 使 牺牲 空间 换取 时 间 的
策略来    提高服务          ，       清单
           器 性      能  如 代码         9-7    所 示 。

                                  代码 清单           室服务器程序
                                              ee
一
#define  GNU_SOURCE    1
#include <sys/types.h>
finclude  <sys/socket.h>
#include  <netinet/in.h>

## Page 186

篇 ”深入
  解析 性
168 第 二  高 能服务 器 编程



#include  <arpa/inet.h>
#include  <assert.h>
finclude  <stdio.h>
finclude  <unistd.h>
#include  <errno.h>
#include  <string.h>
#inelude  <fcntl.h>
#include  <stdlib.h>
finclude  <poll.h>

#define  USER_LIMIT   5      /*    最 太 用 户 数量 */
#define  BUFFER_SIZE   64    /*        */
#define  FD_LIMIT   65535          XP    RR
* 客户 数据 ;客户     socket     Hat,        MME     */
struct   client_data
(
                  in  address;
            sockaddr
        char*  write  buf;
        char  buf[  BUFFER_SIZE    ];


int     setnonblocking(   int   fd   )
{
    int    old_option      =  fentl(      fd,  F_GETFL );
    int    new_option      =  old_option       |
    fentl(     fd,   F_SETFL,      new_option        };
    return     old_option;
}

int main(  int    arge,   char*    argv[]     )
{
    if(    arge   <=  2   )
  [
    printf( “usage: ts ip_address port_number\n", basename( argv[0] ) };
    return 1;
  |
    const     char*    ip  =  argv[l);
    int    port   =   atoi(   argv[2)       );

    int    ret  =   0;
    struct                       address;
    bzero(        saddress,   sizeof(       address    ) );
    address.sin_family           =   AF_INET;
    inet_pton(     AF_INET,        ip,                   );
    address.sin_port          =  htons(     port     );

    int    listenfd     =  socket(       PF_INET,        0     );
    assert(        listenfd   >=   0  );

    ret    =  bind({  listenfd,      (    struct     sockaddr* )4&address, sizeof( address ) );
    assert(     ret    !=  -1   );

    ret    =  listen(        listenfd,    5 );

## Page 187

第 9 章    + 169



             assert( ret             !=   -1

                  创建                   RH,    HH                                           对
             /*     users                              FED_LIMIT                           象。    可 以  预期  : SWIM socket   连接
                                     且  socket 的 值           可以 直接
                                                                  用         索引 《 人必  为数组
都可   以获得     个     这 样 的 对       并 ，                                                          的下标)        连接
    一                 象                                                                               socket   对点   的 client
data MR.         socket        和 客户数据 关联 的 简单 而高效 的 方式                           */
             client_data*                users   =     new                            LIMIT]
             /*                                                                         poll                              “/
             pollfd
             int user              counter    =  QO;
             for(  int      i    =   1;     i  <=      USER_LIMIT;     ++i   )
             {
                                         =    -1;
                                                 =     0;
             }
             fds[0).fd         =     listenfd;
             fds[(0].events             =   BOLLIN       |
             fds[0).revents              =    0;

             while(    1    )
             {
                   ret      =    poll(        fds,     user_counter+l,          -1l   );
                   if     (    ret <        0
                   {
                                 printf(         "poll        failure\n"     );
                                 break;
                   }

                   for(        int      i   =   0; i     <    user_counter+1;         ++i  )
                   {
                                    if(  (    fds({i).fd      ==     listenfd     )  4&6  (     fds{i).revents & POLLIN  ) )
                                    {
                                              struct         sockaddr_in     client_address;
                                              socklen_t       client_addrlength                =  sizeof{ client_address  );
                                              int      connfd      =        listenfd,          (  struct  sockaddr* )
                                                                  &client_address,                    &client_addrlength );
                                              if    (         connfd   <  0 )
                                              {
                                                         printf(     “errno      is:    td\n",    errno   );
                                                         continue;
                                              }
                                                                     请求太多   则 关闭
                                              /*    和如果                        新到的连接            */
                                              if(      user_counter     >=     USER_LIMIT     )
                                              {
                                                         const     char*     info    =  "too     many users\n";
                                                         printf(        “ts",  info     )?7
                                                         send(        connfd,  info,      strlen(     info , 0   )?
                                                         close(     connfd     );7
                                                         continue;
                                              }
                                                       对 于 新 的 连接     ，    同 时修改 fds
                                                 /*                                    和  users 数组    。前  文 已 经提 到
对 应 于 新 连接 文件    描述     connfd       的  客户 数据 */
                                              user     countert+;
                                              users(connfd].address                 =  client_address;

## Page 188

第 篇 ”深入    编程
170  二     解析 高 性 能 服务 器



       setnonblocking(            conntd  );
                                       =  connfd;
       fds[user_counter].events            =  POLLIN    |  POLLRDHUP |  POLLERR;
       fds[user_counter].revents              =    0;
       printf(   "comes    a new   user,  now  have   td   users\n", user counter };
}
else  if(   fds[i].revents         &  POLLERR   )
{
       printf(     "get    an   error    from  %d\n",      fds[i].fd );
       char errors(         100  J];
                   errors,     '\O"',    100  );
       socklen_t         length  =   sizeof(   errors      );
       if(  getsockopt(       fds[i].fd,  SOL_SOCKET,      SOERROR,  éerrors,
            &length) <0)
       {
               printf(       “get     socket  option     failed\n"   );
       }
       continue;
)
else  if(   fds[i].revents         &  POLLRDHUP     )
{
           DARE  PMA AEE,        UREA te ae,                   Aah
                                 =   users(fds[user_counter).fd];
     close(           fds[i].fd  );
       fds[{i]   =  fds[user_counter];
       i--;
     user_counter--;
                   "a    client   left\n"     );
)
else  if(  fds[i].revents         &   POLLIN   )
[
     int    connfd    =       fds[i].fd;
   memset( users(connfd].buf, '\0', BUFFER_SIZE });
   ret = recv( connfd, users(connfd].buf, BUFFER_SIZE-1, 0 );
   printf( "get td bytes of client data ts from %d\n", ret,
    .buf, connfd };
    users(connfd]
     if({   ret   <   0   )
     {
                                  读操作出错， 则 关闭      连接 */
               /*   如果
               if(    errno    !=   EAGAIN    )
               {
                          close(    connfd    );
                          users[fds[i].fd]      =              .fd);
                                                   users(fds[(user_counter)
                          fds(i)    = fds[user_counter];
                          i--;
                          user   counter--;
               )
     }
     else if(       ret ==     0  )
     {
     )
     else
      {

## Page 189

                                                                                             第 9 章     复用 ee    171

                                                                                             其他  socket 连接 准备 写 据 */
                                                             如 果 接 收 到 客户       数据   ，则 通知
                                                      for(   int         j = 1;     j <= user_counter;  ++    )
1                                                     {
a                                                                  if({      fds(4).fd   ==  connfd   )
‘                                                                  {
                                                                   continue;
                                                                   }

                                                             fds[j]).events         |=   ~POLLIN;
                                                             fds[j).events          |=   POLLOUT;
                                                             users[fds[j].fd].write_buf          =            .buf;
                                                                                                      users[connfd)
                                                          }
                                                }
                                      }
                                      else  if(  fds[i]).revents                &    POLLOUT  )
                                      {
                                            int  connfd            =    fds[i].fd;
                                                                                          buf  )
                                            if(  !           users(connfd).write
                                            {
                                                             continue;
                                            }
                                            ret              send{     connfd,            users[connfd].write
                                                 =                                                       buf,
                                                                                         users[connfd].write   , 0 );
                                                                        strlen({                         buf
                                            users(connfd) .write_buf             =       NULL;
                                                写         完  数据 后      需要 重新注册     fds(i]     上 的可 读 事  件 */
                                            fds[i).events                |=  ~POLLOUT;
                                            fds[i].events                |=  POLLIN;
                                      }
                          }
                ]

                delete  []    users;
                close(  listenfd     );
                return  0;
           }

        97 INMO 复 用 的 高 级 应 用 三: 同时 处 理TCP 和 UDP 服务
        此，我 们 讨论 过 的 服务 器 程序 都 只 监听 个 端口 。在 实际
           至                                                                                      应用中，         有 不 少服务
                                                                   一                                               器程序
     能同时监听 多               端口 ，比 如 超级 服务 inetd 和 android                    的 调试 服务adbd。
                                                     个    socket             只 能 与 一 个socket  地 址 绑
           从   bind 系统 调用 的 参数 来         看 ，         一                                       定     ， 即     一个   socket
     只 能 用 来 监听     一  个     端口 。因 此   ，         服 务 器     如              果 要 同时 监听 多 个  端口   ， 就 必须 创建    多个 socket，
     并 将 它们 分 别 绑 定 到 各 个         端口   上 。 这 样 一             来   ，服     务        器 程序 就需要 同时 管理多 个 监听          socket,
           复   用 技术 就  有了 用 武之      。       另
     VO                            地       外     ， 即         使 是 同一 个             端口 ，如 果 服 务 器 要 同时处理 该             上
     的     TCP  和 UDP   求，                                                   :                                       报
                                               请 则 也 需要 创建 两 个 不 同 的    socket     一  个  是 流   socket，  另 一     个 是 数据
                                            上。       比          如 代码    清单  9-8     所 示 的 回 射 服      务  就 能 同时 处 理   一
     socket，    并 将 它们 都     定 到  该        端口
     个 端口 上     的 TCP  和 UDP      求。
                                   请

## Page 190

篇”深入 解析 性 编程
172 第 二  高 能 服务 器


               代码     清单          ”同时     理         服务
                             9-8        处 TCP HRM UDP 请 求 的 回 射 器

#include   <sys/types.h>
#include   <sys/socket.h>
#include   <netinet/in.h>
#include   <arpa/inet.h>
#include   <assert.n>
#include   <stdio.h>
#include   <unistd.h>
#include   <errno.h>
#include   <string.h>
#include   <fcntl.h>
#include   <stdlib.h>
#include   <sys/epoll.h>
finclude   <pthread.h>

#define  MAX_EVENT    NUMBER      1024
#define       TCP_BUFFER_SIZE   512
#define  UDP BUFFER      SIZE   1024

int      setnonblocking(   int    fd )
{
      int  old_option      =  fcntl(     fd,    F_GETFL );
      int  new_option      =  old_option        |  O_NONBLOCK;
      fentl(  fd,    F_SETFL,       new_option     };
      return  old    option;
}

void  addfd(  int    epollfd,       int    fd  )
{
      epoll  event    event;
      event.data.fd      =   fd;
      event.events     =   EPOLLIN      |    EPOLLET;
      epoll  ctl(    epollfd,                       fd,   s&event );
      setnonblocking|        fd   );
}

int  main(   int           char*        argv[]  )
{
      if( argc    <= 2 )
 {
    printf£( “usage: ts ip_address port_number\n", basename({ argv(0] ) );
    return 1;
 }
      const  char*    ip   =  argv[1];
      int  port   =  atoi(    argv[2?)      );

      int  ret =    Q;
      struct   sockaddr_in        address;
      bzero(   Saddreéss,     sizeof(     address    )  );
      address.sin_family          = AF_INET;
      inet_pton(     AF_INET,       ip,             };
      address.sin_port        =   htons(   port    );

## Page 191

    第 9 章 VORA f+ 173



                                              创建TCP socket. #HHWEAMO
    /*                                                     port 上 */
    int   listenfd       =    socket(         PF_INET,     SOCK_STREAM,  0 );
    assert(      listenfd        >=   0   );

    ret   =      bind(  listenfd,         (    struct    sockaddr*   )saddress, sizeof( address ) );
    assert({       ret  !=    -1   );

    ret   = listen(         listenfd,          5 );
    assert(        ret  !=    -1   );

              UDP      socket,                             port 上 */
    bzero(    Saddress,               sizeof(    address    )  );
    address.sin         family       =   AF_INET;
    inet_pton(             AF_INET,    ip,         saddress.sin_addr   );
    address.sin_port             =   htons{      port    );
    int   udpfd      =  socket(        PF_INET,      SOCK_DGRAM,     0 );
    assert( udpfd >=             0   };

    ret   =      bind(  udpfd,       (   struct    sockaddr*     )éaddress,  sizeof( address )  };
    assert(        ret  !=    -1

    epoll        event  events[{       MAX_EVENT_NUMBER        ];
    int        epollfd  =    epoll_create(             5 );
    assert(           epollfd  !=    -1   );
                                   socket
    /*   324  TCP                 和  UDP     socket     LH      */
    addfd({   epollfd,         listenfd          );
    addfd(    epollfd,         udpfd      );

while( 1 }
{
     int number = epoll wait( epollfd, events, MAX_EVENT NUMBER, -1 );
          if  (        number  <   0  )
                                                                         ，
          {
                     printf(       “epoll        failure\n"     );
                     break;
          }

          for    (   int  i= 0;         i    <   number; i++     )
          {
                     int sockfd         =    events[i].data.fd;
                     if   (   seckfd      ==     listenfd )
                     {
                               struct        sockaddr_in       client_address;
                               socklen_t          client_addrlength      = sizeof( client_address );
                               int     connfd      =           listenfd,   ( struct sockaddr* )
                                                               &éclient_address,&client_addrlength  });
                               addfd(        epollifd,     connfd );
                     I
                     else     if   ( sockfd        ==    udpfd   )
                     {
                               char   buf[       UDP_BUFFER_SIZE       ];

## Page 192

篇 解析 能
174 第 二 深入 高 性 服务 器 编程



                                   buf,              UDP_BUFFER_SIZE           );
                  struct    sockaddr_in              client_address;
                  socklen_t                                        =            (
                                                                           sizeof client_address   );

                  ret   =  recvfrom(          udpfd,     buf,  UDP_BUFFER_SIZE-1,   0,
                                                                                                      );
                                              ( struct sockaddr*                      &client_addrlength
                  if(   ret    >   0       )
                  i
                          sendto(           udpfd,  buf,       UDP_BUFFER_SIZE-1,     0,
                                    {      struct sockaddr*      )4aclient_address, client_addrlength };

           }
           else   if (  events[i).events                 &  EPOLLIN     )
           {
                   char            TCP_BUFFER_SIZE
                   while(    1   )
                  {
                                               buf,  "\O',    TCP_BUFFER SIZE       );
                          ret    =  recv(           sockfd,    buf,     TCP_BUFFER_SIZE-1, 0 );
                          if(       ret     <   0   )
                          {
                                    if(         {   errno = EAGAIN )     ||  ( errno = EWOULDBLOCK ) )
                                      {
                                                    break;
                                      }
                                    close(           sockfd    );
                                    break;
                          }
                          else     if(          ret  ==   0 】
                          {
                                    close(           sockfd    );
                          }
                          else
                          {
                                    send(           sockfd,    buf,     ret,   0  );
                          }
                   }
           }
           else
           {
                   printf(     "something            else     happened     \n"    );
           }
 )
 }

 close(  listenfd  );
 return  0;
}
eee

## Page 193

 9章 VORA ee 175
 第


 9.8            超级服务          xinetd

     Linux           因特网服务            inetd 是 超级 服务   。   它 同时     管理   着 多 个    子 服务     ， 即      监听 多 个      。  现
                                                                                                  端口              在
                     上使   用   的 inetd       服务 程序  通常 是 其     版本
    Linux    系统                                                                  升级 xinetd。xinetd 程序 的 原理 与 inetd 相
 同 ，但 增加了                 些 控制 选项            ， 并 提高了 安全
                     一                                    性。    下 面 我    们     从 配置     文件        和 工作 流程 两个    方面对
 xinetd 进行 介绍         。

    9.8.1        xinetd   配置文件
                                                           和        目录      下 的 子                                文件
     xinetd 采用 /ete/xinetd.conf         主              配置 文件    /etc/xinetd.d        配置                 来  管理   所有服
 务。主 配置 文件 包含的是通用                       选项     ，       这 些 选    项将 被  所 有 子 配置 文件 继承    。            不 过 子 配 置 文件 可
             这些选项 。           每     一 个 子   配置 文件  用  于 设置 一 个 子 服务     参数     。     如                         子 服务
 以 覆盖                                                     的        比                          ，telnet            的配
                                                    内容如下:
 置  文件                                               的 典型
     1       4   default:   on
     2       #   description:         The   telnet server serves     telnet     sessions; it  uses \
3 # unencrypted username/password pairs for authentication.
4 service telnet
5 {
     6                    flags                       -   REUSE
     7                    socket      type            =   stream
     8                    wait                        =   no
     9                    user                        =   root
     10                   server                      =   /usr/sbin/in.telnetd
     11                   log     on_failure          +=  USERID
     12                   disable                     =   no
     13         }

    /etc/xinetd.d/telnet              文件 中 的 每     一 项 的 含义    如 表  9-3 所 示     。

                                      # 9-3    /etc/xinetd.d/telnet 文件  的     项 目 及 其   含义
   项             目                                        =      x
 service                  服务
                                     多
                                                                            的      该标志
                          设置连接 的标志      。REUSE     表示 复 用     telnet       连接  socket。    已经过时，    每    个     都默认
     fags             启用 REUSE 标志                                                                 连接
 socket_type              ik HY
                              采用单线程方式    〔wait=yes)       还     是   多 线程
                          服务                              方式      单 线程                        方式   表示    xinetd 只
 wait                        accept 第 一 次 连接   ， 此 后 将由 子 服务    进程  来 accept MEH.      SPAT xinetd     一   直 负 责
                                                          数据
                             accept 连接 ，而 子服务 进程 仅处理连接     socket     上 的       读
 User                     子 服务 进程 将以 user     指定 的    用   户 身份   运行
 server                   子服务 程序 的 完整路 径
 log on_failure           定义                         时输出日  志 的 参数
                                      服务不能 启动
 disable                  是     启 动 该子服务
                                                          其 man                               文档
    xinetd           配置 文件的内 容 相 当 丰 富 ，远 不               止     上   面 这 些    。   读 者 可 参考         来    获得    更多

## Page 194

176           第 二      篇         深入 解析 高                 性能 服务 器     编程

    。
信息

9.8.2              xinetd  工作 流程

                                                                            如时间          日     期 服务   daytime、   回 射服务 echo  和
     xinetd            管理  的 子 服务              中       有 的 是 标准 服务       ，  比
丢弃 服务 discard.                   xinetd        服务 器 在 内 部 直接处理 这 些 服务                            。   还有 的 子 服务     则 需要 调用 外 部
的 服务 器 程序 来 处                              。xinetd 通过 调用 fork 和             困                  来加           载运 行这些 服 务 器 程序 。比
理                                                                           exec                                             如
               服务 都是                       种 类 型的子服               。        们仍以           服务
telnet、fp                         这                             务            我  telnet           为例来探讨      xinetd 的工作 流程   。
                       看   xinetd          守护 进程             的PID 〈    下 面   的操作 都       在 测试
     首先 ，查                                                                                        机器    Kongming20 上 执行 ):
     3  cat    /var/run/xinetd.pid
     9543

                                                                            机:
     然后开局 两个终端并 分 别 使用如 下 命令                                            telnet  到 本

     5  telnet         192.168.1.109
        下来使用                      命令 查看 与 进程                   9543               相关
     接                     ps                                                    的进程
     $  ps    -eo                          pid,ppid,pgid,sid,comm |    grep  9543
        PID        PPID          PGID           SESS         COMMAND
        9543            1        9543           9543         xinetd
        9810       9543          9810           9810         in.telnetd
     10355         9543          10355          10355        in.telnetd
                       ，                             用                                    ，                   子
     由 此 可 见                               我 们 每 次 使     telnet 登录 到 xinetd 服务                 它 都 创建 一 个     进程 来 为   该telnet
              。                  运行    ，                                                                  文件
                                                                                                              中     的。 个     子
客户 服务             子 进程                     in.telnetd              程序 这 是 在  /etc/xinetd.ditelnet           配置
                                                                                                              定义             每
进程 都 处        于    自己 独立 的 进程                      组        和 会 话     。 我 们 可以 使    用 lsof        命令    〈 见
                                                                  中                                       第 17   章 ) 进一步    查看
                                                         哪些
子 进程 都 打开 了                                          文件描述符:
                                                                          为
     §  sudo       lsof        -p     9810 # UF##               9910      例
     in.telnet               9810          root   Ou     IPv4   46189       TCP            Kongming20:telnet->Kongming20:38763
(ESTABLISHED)
     in.telnet               9810          root lu     IPw4    48189    0t0  TCP          Kongming20:telnet->Kongming20.:38763
(ESTABLISHED)
     in.telnet               9810          root   2u     IPv4   48189    OtO  TCP          Kongming20:telnet->Kongming20:38763
(ESTABLISHED)
                   略                       无       输出    。              输出 我              知道      ，     进程                   输
     这 里 省             了   一     些 关 的                        通 过 lsof    的      们                   子    9810 关闭 了        其标准
                                          、标 准 输出和 标准 错误 ，而
人                                                         将socket     文件  符          dup       EM]        Auk,   telnet 服务 器 程
     序 将 网 络 连     接 上 的         输入当 作 标 准                   输入 ，并 把 标准 输出 定向 到 同一 个                    网 络 连接上    。
                                                          使用 lsof    命令  :
     再 进一步 ， 对 xinetd 进程

     $  sudo       lsof        -p     9543
     xinetd  9543                 root          5u       IPv6     47265   OtO    TCP      *:telnet      (LISTEN)
                           说明              xinetd  将       一直    监 听 telnet         连接 请       ， 因
     这  一                          条 输出                                                   求      此    in.telnetd 子 进 程只处 理 连 接
socket，  而 不 处 理 监听 socket。                                 这 是 子  配置 文件 中        的 wait       参数 所 定义 的 行为    。
                                                                          也  可以  用  上 述
                                                                                               方法 来
     对 于 内 部           标准 服务                 ，xinetd     的   处 理 流程                                  分析，这     里 不   再   。

## Page 195

        第 9       章  IO 复用 se 177

 上面讨论的， 们将    的值是    情况  为图
   综合 我 xinetd OTHER (wait 选项 no 的 ) 绘制 9-1
所 示 的 。
    形式



               对 于 /ete/xinetd.d 目 录 下
               的 每 个 被 使 的 服务    ， 分
               个     并名
               别 创 建一
               socket
      en0
《如 果 是TCP 服 务 )




accept()
                  是     服务)
                        【如果

                        进程
                        子

                        -人
    socket (ty          0、1     和 2
    RF)
                    外 部 服务        标准 服务

                                 到文    运行
                  将 stocket     dup    肉部
                  描述符0、1
                  件 和 2 上     ，        服务
                  然后关闭 socket

setgid()
setuid()
setsid()

 exec()
                    子服务程序
   调用

        图     9-1 xinetd   的 工作 流程

## Page 196

第 10 量 信  号


           是由用户、 系 统 或 者 进程 发              送    给                     的 目标 进程                 ，    个
信号                                                                        信息 以 通知 目标 进程 某                                状态 的 改变
或系统 异常    。Linux 信和     号  可 由 如 下 条件 产生              :
CO     对 于 前 台 进       ， 用 户 可     以   通过 输 人     特殊         的                     终端 字符 来     给   它 发 送信 和 号。      输
           程                                                                                      比如                      Ctrl+C
    通常 会给进程 发送               一    个 中        断 信 号。
       系统 异常 。比  如        浮 点 异常和 非法内 存 段 访问                      。
                            如     alarm     定时 器 到 期 将 引起                            SIGALRM     信和
                   系统 状态 变化 。比                                                                 号。
                           或调用              函数
口 运行       kill             命令    kill       。
           器程序必须处理          〈     或至少          忽略 )  一   些 常 见        的              信和 号，    以 免      异常终止 。
服务
           如何在 程序                中 发   送信    和    号  和 处 理 信 号        ，然 讨论                    支持
本 章 先讨论                                                                                    后      Linux      的信和号种类，           详
                                                                                                                               并
细 探 讨 其    中  和 网 络 编程  密切       相关 的 几 个。

10.1      Linux  SHR

10.1.1     发 送信 号

Linux 下    ，     个 进程 给其他 进程 发送信 号 的                         API                    是 kill    函数   。        其 定义
           一                                                                                                 如下:
include <sys/types.h>
#include     <signal.h>
int       kill({ pid_t  pid,   int     sig    );
该 函数 把     信号           发
           sig             送给    目标     进程     ，     目                由 标 进 程        pid   参数 指定    ，    其 可 能 的 取  值  及 含义  如
# 10-1 所 示    。

                                                           的 pid  参数
                                 表     10-1    kill      函数                                    及其含义
pid 参数                                                                      =
pid>0                  {i              PID 为 pid     的   进程
pid=0                  信     发 送       本 进程组内 的其他 进程
                                                                                               目标     进程
                                                                                                                发送信         号
pids -1                信     发 送   给   除 init 进程    外                 的所 有 进程       ， 但 发  送  者 需要 拥有 对                  的 权限
pid   < -1             信和    发着给 组      ID 为 -pid      的 进程    组                  中 的 所有 成 员
                                                  果      取 值              0，          则        函数 不 发 送    任何   信和  号。 但
Linux      定义 的 信号 值 都 大于                  如
                                 0，        sig                                  为        kill                              将 sig
设置     为0 可以 用 来 检测 目标 进程 或 进程组是                         否   存    在   ，              因 为 检查    工作   总    是 在信号 发    送  之 前 就   执
                                                         由于进程                        PID   的 回     ，可 能 导致
行 。不 过 这 种 检测 方式 是不可 靠                     。                               方面                                 被 检测     的     PID
                                        的     一                                                绕                               不
是 我 们 期 望的      的                          ，                                          是
           进程 PID;，           另   一    方面 这 种 检测 方法 不                                    原子 操作     。

## Page 197

                                                                                  第10 章    信 号  +     179

                成功时返回                        失败
       该 函数                                                                       几  errno 如 10-2 所 示 。
                    0，                      则返回 -1   并    设置    errno。         种可能的      表
                                  表 10-2                  kill  出 错的情况
                ermo                                                会
                                                                效的信和号
                EINVAL                                          无
                EPERM                                     该 进程      权限 发    送信和号   给 任何 一 个目标进程
                ESRCH                                     目标    进程 或 进程组不 存在

                           处理方式
10.1.2                      ”信号
                在                ，                                  。        的                    如
       目标 进程    收 到     信号    时  需 要 定义     一        个 接收 函数 来 处 理     之     信 号 处     理     数 原型 下   :

       #include <signal.h>
       typedef  void                         t) (   int       );
       信号 处 理 函 数 只带有     一      个 整 型 参数            ，    该   参数 用 来指示 信号 类     型     。 信 和号 处理 函 数 应 该 是
   重人的，         理 则很 容易 引发 一 些     态条                件    。   所以 在    信和 号 处 理 函   数 中 严禁 调用 一  些 不  安全 的
可
PA RK  .
       除了 用 户 自     定            义 信号 处 理 函 数 外    ，bits/signum.h     头 文件 中 还     定义 了 信和 号 的 两 种其 他 处 理
        一一SIG_ IGN
方式 和 SIG_DEL:
       #include        <bits/signum.h>
       #define  SIG_DFL                              0
       #define  SIG_IGN    ((__sighandler_t)         1
       SIG_IGN  表示 忽略 目标 信号       ，SIG_DFL                                   表示 使用信号 的默认 处 理 方式 。信和号 的 默认
           有                     进程  略                              程                             转
处 理方式 如 下 几 种       结束            《Term)、                 忽
                    :                                              信号        结 束 进     并 生成 核心       储 文件
(Core)、         停 进程〔〈Stop)，         以 及 继续         进程    〈Cont)。
                    号
10.1.3     Linux 信和

    都定义在 bits/signum.h 头 文 件 ，其 中 包括 标准 信号 和 POSIX 实时
       Linux                  的 可 用 信号                              中                                   信
                                         表  10-3 Arar.
号  。本 书 仅 讨     论 标准信和     号， 如
                                         #®    10-3       Linux   标准 信号
           8                                                        =x
                                                          ET
   SIGINT                                                 RRB
                |POSIX                                    键盘
   SIGQUIT                    |        Core          |            输入使进程过出
   SIGILL                                                 mo
   SIGTRAP                                                          TM
                                                                                      函数
                                                                    生成
   SIGABRT  42BSD                                         进程      调用 abort   时       该信号
                                                                SIGABRT
   SIGIOT     |                        Core        |      和              相同
                                                          总
                                                                   错误    错 误 内 存 访问
   SIGBUS                                                       线     ，

## Page 198

    篇  性 编程
    180 “+ 第 二 深入 解析 高 能 服务 器



                                                                                                                           (  续 )
             as

              =|      posix     |  Term                           一个                      。                      者
    SIGKILL                                              终止
                                                                                    进程            该 信号 不 可 被 捕获 或 忽略
    SIGUSRIL  “|      POSIX    =|    Tem        |        用                  定义    信号 之        一
    SIGSEGV   “|      ANSI      |       core    |                 内存段引用
              “|      POSIX    “|    Tem                                     义                之二
    SIGUSR2                                              用 户 自 定 信号
               |               “|    Tem        |                  被                                 者         连接
                 POSIX                                                                                           中写 数据
    SIGPIPE                                              往 读 端     关闭 的 管道 或                         socket
               |                |    Tem        |          alamm        setiimer                          实时
    SIGALRM      POSIX                                   由      或                                设 置 的      间 钟  超时 引起
                                                                                                 默认               是
    SIGTERM                                              终止 进程 。kill                          命令     发  送 的信号 就 SIGTERM
                                                            的                              该              报告 数学     器
    SIGSTKFLT                                            早期     Linux                  使 用       信号 来            协 处 理     栈 错误
    siGcLD       |           =|      ign        |               状态          NF
    SIGCHLD      POSIX                                                               发        化         或
                                                         子 进程                          生变 〈 退 出           者     )
    SIGCONT   “|      posix  ”|    com              _                                           (Ctrl+Q)。 如 果 目 标 进程 未     处  于 暂停 状
    SIGSTOP                         Stop        |        暂停       进程                                      不  被     者
    siorst?      |    rosx      |     sp        |               (Cut+S)。                             该 信号   可 捕获 或 忽略
               |              |     Stop      _—|                                   试图     终端
    SIGTTIN      POSIX                                        后 台 进程                   从              取 输入
    SIGTTOU   =|      POSIX   |     Stop        |             后 台 进程                试图 往 终端 输出 内 容
              ”|              |                 |                           连接
  SIGURG 42BSD socket 上 接收 到 紧急 数据
                                                            的                              用              软
    SIGXCPU                                              进程 CPU 使 时 间 超过 其                                  限制
                                                                                            尺寸超过其软限制
    SIGXFSZ                                              文件
    SIGVTALRM                                              SIGALRM                            似
                                                                                              类， 不 过 它 只 统计 本    进程        空间
                                                                                                                   用 户        代码 的

    做
                                                                                               ，    同时
                                                                                                        统计   户代码              运行
    SIGPROF                                              与 SIGALRM 类                             它          用     和 内    核 的     时 间
    SIGWINCH                                                                           发         化
                                                         终端 窗口          小 生变 大
    SIGPOLL                                              与 SIGIO        似
                                                                                 类
                                                                               绪，比
                                                         IO 就               如          socket      上   发生 可 读、    可 写 事  。 因
                                                                                                          ，    而     件        为TCF
                                                                                                                              在
    sicI0                                       WI                      SIGIO                 的     条 件 很 多     故 SIGIO  无 法  TCP
                                                                          器中使用                     。SIGIO 信号
                                                                                                     可用在    UDP 服务
                                                                                                                   器中，             不
                                                                            服务                                                 过 也 非
                                                党 少见
                                                ust                                        SrGpWR
    SIGPWR                                                                             PS CUnint            Power Suppl          » a 5

                                                         非法
    SIGSYS                                                  系统 调用
                 |                cere      |               ,                    和                      效果相同
    SIGUNUSED    ”|                                      保留                                        通常 SIGSYS
   我 们 并 不 需要 在代码 中 处 理所有 这 些 信号 。本 章 后 面将重点 介绍 与 网 络 编程关系 紧密
        :
的 几 个 信号 SIGHUP、SIGPIPE 和 SIGURG。 后 续 章 节 还 将 介绍 SIGALRM、SIGCHLD 等
    的使用。
信号

## Page 199

 第10 章 信 号 181


 10.1.4        中 断 系统 调用
 如 果 程序在 执行处                  于  阻塞 状态      的       系统 调用 时 接收 到 信号     ，并 且 我 们 为 该 信号 设置  了       信
 处理 数，                                               调用将被中断， 且                 为                 们可以使
 号 则 默认 情况                       下 系统 并              errno              被 设置        EINTR。          我
                                                                        以自动 重启 被         该       中 的系
 用 sigaction 函数     〈 见 后 文      ) 为 信号     设置      SA_RESTART 标志                                  信号
 统 调用  。
 对 于 默认 行为 是 暂停 进程 的信号                              比
                                                 〈    如 SIGSTOP、SIGTTIN)，    如 果 我 们        没有 为 它们 设
 号处理               数， 则      也可以              某      调用     如
 置信                      它们 中 断 些 系统 比               connect.                 epoll_wait).   POSIX 没有
 这种               ，   是              。
 规定
              行为 这       Linux     独 有 的

 10.2     ”信号      函数

 10.2.1    signal   系统 调用
 要 为一个        信号 设置 处     理        函 数         ，可 以 使用 下 面的signal  系统 调用    ，
#include <signal.h>
_Sighandler_t signal ( int sig, _sighandler_t _handler )
 sig       参数 指出 要 捕获 的 信号 类                。        参数
                                              handler     是   _sighandlert   类 型  的         指针，
                                  型                                              函数 用 于 指
 定 信和号    sig  的 处 理 函 数 。
 signal               成功时 返回  个 图 数 指针              ，  该 函数 指针 的 类 型    也是 _sighandler
                                   一                                                        t。 这 个 返回
 值 是前      一    次 调用 signal  函数 时     传人    的 函数 指针    ，  或 者    是   信号 sig 对 应 的 默认      处 理 函  数 指针
 SIG_DEF《〈     如 果 是 第     次 调用                     的
                          一          signal      话 )。
 signal        系统调用 出 错时 返回          SIG_ERR，        并  设置 errmo。
 10.2.2       Sigaction     系统 调用

 设置 信号 处 理 函 数 的 更 健壮 的 接口 是                        如  下 的 系统 调用

 #include      <signal.h>
 int       sigaction(    int sig,     const   struct    sigaction* act, struct sigaction*     oact );
 sig       参数 指出 要 捕获 的 信号           类        ，act    参数 指定 新 的信号 处 理 方式                           参数
                                            型                                    ,oact        则 输出  信
                          果     不    为           的
 导 先前 的处理 方式【如                                      话      和      都
     NULL )。act oact 是sigaction 体类型的指针，
     结构
 sigaction   结构 体     述  了   信和号 处 理 的 细节                  如下:
                                                 ，         其定义

 struct        sigaction
 fitdef


            _Sighandler_t        sa_handler;
            void (*sa_sigaction) (          int,    te
                                                    siginfo void*       );
           )

## Page 200

  182 %* 第 二篇 深入解析 高性能 服务 器编程


       _sigaction_handler;
  #  define  sa_handler       __sigaction_handler.sa_handler
  ¥  define  sasigaction      __Sigaction_handler.sa_ sigaction
  felse
                     t   sa_handler;
       _Sighandler
  fendif

       _sigset_t   sa_mask;
       int   sa_flags;
       void   (*sa_restorer)  (void);
     be
  该结构 体        中 的 sa_hander  AM  O48 a {aS  pa. sa_mask     成 员 设置进程的 信号 掩 码
  (确切 地 说 是 在   进程 原有 信号      掩       码的基础 上 增加 信和号 掩 码 )，以 指定 哪些信号 不 能发送 给 本
                                  (_sigset_t
                                  同义词 类 该 类 型     一 组 信号                    关
  进程 。sa_mask   是信号 集 sigset_t        的     ) 型，     指定     。              于信
  集，我  们   将 在  后 面 介    绍 。sa_flags 成员用 于 设置 程序收 到 信号 时 的 行为 ，其 可 选 值如表 10-4
  号


                                             表 10-4 sa_flags 选项
      选      项                                   a      x
                         如 果   sigaction 的  sig 参数是 SIGCHLD，则 设置 该标志 表示    子 进 程    停时不 上
    SA_NOCLDSTOP，      SIGCHLD    信号
    SA_NOCLDWAIT         如
                            果  sigaction 的 sig      SIGCHLD， 则 设置 该标志 表示 子 进程结束时 不 产生 价 尸进程
    SA_SIGINFO            wen  sa_sigaction  Ey fa       CA     sa_handler),        &     HK
                         fi
                               由             PARCUE MAY 8
    SA_ONSTACK           调用 sigaltstack          Bei      Ao fei
    SA_RESTART                 Fad aR           9 HE
                                                 该                   信号。   默 认 情 况 下，我 们 期 望进程
    SA NODEFER           当 接 收 到   信和  号 并   进入 其 信号 处 理 函数时 ，      不屏蔽                      在
                       处 理  一  个信号 时 不 再 接收 到 同 种信号 ，理 则 将 引起   一 些 竟      条 件
SA_RESETHAND | 信号处理 数 执行 完 以 后 ， 复信 叶 的 网 认 处 理 方式
SA_INTERRUPT 中 断 系 统调用
    SA_NOMASK            同 SA_NODEFER
    SA_ONESHOT           la]SA_LRESETHAND
    SA_STACK             ln} SA_ONSTACK

                    成  员    已经    过时   ， 最 好 不 要 使   。sigaction 成 功时 返回      0，        失败
    sa_restorer                                  用                             则       返回 -1 并 设
  置 errno。

                     集
  10.3           ”信和 号

                           集函数
  10.3.1                    信号
                文提到 ，Linux   使    用数据 结构 sigset_t     来 表示
                                                             一组             。其 定义
    前                                                                         如下:
    #include     <bits/sigset.h>

## Page 201

    第10 章 信 号 183


        # define  _SIGSET_NWORD3S         1024        /   8   *  sizeof       (unsigned      long    int)))
        typedef   struct
        {
             unsigned   long       int   _
        |
                   可见           ，sigset  实际    上 是        一 个 长 整 型 数组            ，数 组 的 每    个    元 素    的 每 个 位 表示  一  个
        由 该 定义
    信和 这 种 定义 方式                   文件 描述 符 集                     类                                                       修
                          和                              fd_set  似          。Linux  提供 了 如 下     一   组     数来设置    、    改、
                 和查询信号集:
    删除

        #include   <signal.h>
        int  sigemptyset  (sigset_t*             set)                               /*
        int  sigfillset  (sigset_t*             _set)                               /*    在 信号集中 设置所 有人入 号
        int  sigaddset   (sigset_t*              set,     int        _signo)        1/*   将 信和  号      添加至信号全       */
        int  sigdelset   (sigset_t*              set,     int         signo)              将    信号                   */
        int sigismember         (_const  sigset_t*        set,      int  _signo)    /*    Bi     PAGER}     +/

             ”进程
    10.3.2                       信号 掩 码
            文提到，     我 们 可以 利用 sigaction                    结构 体         的    sa_mask    成 员     来 设置进程的信和   号 掩     码 。 此
        前
    外，如 下                       数 也 可以 用 于 设置 或 查看       进程   的  信号 掩 码       :
        #include   <signal.h>
        int  sigprocmask(      int         how, const         sigset_t*          set,    sigset_t*   oset );
        _set 参数 指定 新     的     信号            ，_oset        参数 则 输出 原来 的信号 掩码                  〈 如 果 不      为NULL       话)。
                                          码                                                                              的
    MA  _set 参数 不     为 NULL，      则 how 参数 指定 设置                             进程信和       掩 码 的 方式     ， 其 可 选 值如 表    10-5
    示。
    所

                                                      表   10-5      _how      参数
_how 着 数                                                                      &     x
    SIG_BLOCK         新
                   |  ”新  的进程信号              掩 码    是 其 当前    值  和  set          指定信号 集 的 并 集
                               进程              码                                  集 的       ，                    被
    SIG_UNBLOCK                            信号 掩    是  其 当前 值                信号
                          的                                      和  ~-_set            交集 因 此 _set     指定 的 信号 集 将 不  屏
    SIG_SETMASK       直接 将 进程信号 掩码设置                        为 set
           果 _set  为      NULL，    则    进程    信号 掩 码 不                   ，此 时 我 们 仍然 可以 利用     _oset     会          数 来 获得
        如                                                           变                                                   进
                     掩码。
    程 当前 的 信号
        sigprocmask       成 功 时                          则返回     -1      并 设置     ermo。
                                  挂起的信号
    10.3.3                           ”被

             进程信号掩码后，                   被         屏蔽 的信号 将 不 能 钙                  程 接收 。    如 果 给 进程 发    送 一    个 被屏蔽   的
        设置
    信号 ，则 操作 系统 将该信号                    设置         为 进程     的    一  个 被 挂         起的信号 。    如 果 我 们 取消 对 被 挂       起信号 的
    屏蔽，则 它 能 立即 被 进程接收 到。如 下                                     可以 获得            进程 当前 被     挂    起 的信号 集 :
        #include   <signal.h>
        int  sigpending(  sigset_t*          set         );

## Page 202

    184 > 第 二篇 深入 解析 高性能 服务 器 编程


                                             显
        set   参数用于 保存 被 挂 起的信号               集    。 然，     进 程 即使    多 次 接收 到 同一 个 被 挂    起   的        ，
    sigpending   数 也 只     能 反映 一        。  并
                                       次     且，   当 我    们    再 次 使用 sigprocmask   使 能 该 挂起的 信号 时      ，
    该信号 的 处 理          数 也 只 被   触发 一    次 。
        sigpending     成功时 返回                时             设置
                                           失败
                                             返回     -1 并     ermo。
        关于 信号 和            集                 提供     多                                一介绍            要
                       信号       ，Linux   还     了    很 有 用 的    API，     这 里 就          了               需
                                                                              不    一     。            提醒
    读者 的     是 ，要 始终 清楚 地 知道 进程             在    每 个 运行  时    刻 的 信号      ， 以        及 如 何 适 当地 处 理 捕获 到
    的信号。在 多 进程 、多 线程               环境      中，    我 们 要以 进程    、线 程 为    单位    来 处   理信号 和 信和号 掩     码。 我
们 不 能 设想 新 创建 的 进程 、线 程 具有 和 父 进程 、主 线程完全 相同 的信号 特征 。 如
        比 ，fork 调
用 产生 的 子 进程将继承 父 进 程的信号 掩 码，但具有 一 个 空 的 挂起信号 集 。
10.4 ”统一 事件 源
                       是一种异步事件:    信号 处 理 函 数 和 程序         的   主 循环 是     两 条 不 同  的 执行 路   线 。很    显然 ，
   信号
信号 处 理 函 数需要 尽 可 能 快地执行 完毕 ，以 确保该信号 不被屏蔽 〈 前 面 提 到 过 ， 了
        为
        避免
                       fa                                               太
                                                                          久。  一  种   典型的  解决 方案      是 :
    把 信号    的  主要 处 理      逻 辑 放 到 程序      的     主 循环  ，   当 信和号 处 理 函 数 被 触发        ， 它 只 是          地通
                                             中                                       时                简单
    知 主循环 程序接收 到 信号 ，并 把 信号                  值 传递      给    主 循环 ，   主  循环  再 根据   接收 到 的 信和  号  值    执行
    目标 信和号 对 应       的 逻辑 代码    。  信 号 处 理 函 数 通常 使           用管道 来     将信号 “传递        ”给 主 循环   :    信号
    处 理     数 往 管 道的写 端 写人信号                 ，    主 循环 则 从 管道    的   读 端  读 出 该信号      。 那  么        主循环
                                             值                                         值               怎
                                                                         使用 IO          复 用 系统 调用 来 监听 管
    么知道 管道上何时有 数据               可 读        ?     这很 简单 ，           我 们 只 需要
    道的  读   端 文件 描述符上 的 可 读 事件 。             如 此 一         ，  信 号 事件 就    能和 其 他 IO     一样被处理，
                                                         来                             事件
    即 统一 事件 源    。
            多优秀的       VO    框架 库和 后 台 服 务 器     程序 都      统一  处 理   信号 和   IO 事件 ，比
        很                                                                              如     Libevent IO
        库和    xinetd   超级 服务    。代 码 清单      10-1 给    出    了 统一 事件 源   的 一 个 简单     实现
    框架

                                         代码     清单 10-1 ”统一    事件 源
        #include  <sys/types.h>
        #include  <sys/socket.h>
        finclude  <netinet/in.h>
        #include  <arpa/inet.h>
        #include  <assert.h>
        finclude  <stdio.h>
        #include  <signal.h>
        #include  <unistd.h>
        #include  <errno.h>
        #include  <string.h>
                  <fentl.h>
        #include  <stdlib.h>
        #include  <sys/epoll.h>
        Yinclude  <pthread.h>

## Page 203

第10 章 信 号 ee 185


#define  MAX_EVENT_NUMBER            1024
Static   int  pipefd(2];

int          setnonblocking(   int   fd }
{
      int  old_option          =  fentl(  fd,     F_GETFL   );
      int  new_option          =  old_option     | O_NONBLOCK;
      fentl(    fd,     F_SETFL,      new_option    );
      return    old_option;
}

void  addfd({   int     epollfd,      int fd  )
{
      epoll_      event  event;
      event.data.fd          =   fd;
      event.events         =   EPOLLIN    |  EPOLLET;
      epoll_ctl(        epollfd,          EPOLL_CTL_ADD,    fd, éevent );
      setnonblocking(            fd  );
}
         处理函数        */
/*  信号
void  sig_handler(          int   sig   )
{
                原        errno，            后     ，              的
      /*  保留     订 的                在 函数 最    恢复     以    保证 函数 可     性  */
      int  save_errno          =  errno;
      int  msg   =     sig;
      send(   pipefd[1],          ( char*    )émsg,   1,    0); /*
      errno   =          save_errno;
I
         信号的处理函
                         数
/*  设置                       +/
void  addsig(       int  sig     )
{
      Struct    sigaction         sa;
                 &sa,               sizeof(   sa)     );
      sa.sa_handler          =   sig_handler;
      $a.sa_flags       |=   SA_RESTART;
      Sigfillset(       &sa.sa_mask       );
      assert(    sigaction(         sig,     &sa,  NULL   ) !=  -1 );
}

int main(    int     argc,     char*   argv[]     )
{
      if(  arge      <=  2   )
      {
             printf(         “usage:   %s      ipaddress    port_number\n", basename( argy[0] )
                 return  1;
      )
      const       char*  ip    =  argv[1];
      int  port      =  atoi(     argv[2)   );

      int  ret   =     0;
      Struct    sockaddr_in           address;
      bzero(    s&address,        sizeof(    address   )    );
      address.sin_family           =   AF_INET;

## Page 204

 篇”深入 解析 高 性 能服务
 186 +> 第 二     器 编程



      inet_pton(        AF_INET,        ip,        éaddress.sin_addr      });
      address.sin_port            =   htons(         port    );

      int   listenfd       =           socket(   PF_INET,    SOCK_STREAM,    0    );
      assert(     listenfd        >=     0     };

      ret   =     bind(  listenfd,             (   struct    sockaddr*    )taddress, sizeof( address ) });
      if(   ret   ==    -1   )
       {
              printf(      “errno           is      d\n",    errno );
                 return  1;
 .     )
      ret   =      listen(   listenfd,             5  );
      assert(     ret          <1    j);

      epoll_event        events[        MAX_EVENT_NUMBER              );
      int   epollfd      =              epoll _create(  5    );
      assert(     epollfd       !=    -1       );
      addfd(    epollfd,        listenfd            );

      /*               socketpair   创建 管道 ，             册
                                                        往                           sy/
      ret   =         socketpair(   PF_UNIX,         SOCK_STREAM,         0, pipefd );
                  ret    !=    -1     );
      setnonblocking(           pipefd[1])           );
      addfd( epollfd,           pipefd[0]            );

      /*                            BR         +/
      addsig(     SIGHUP       );
      addsig(     SIGCHLD       );
      addsig(     SIGTERM       );
      addsig(     SIGINT       };
      bool    stop_server        =           false;

      while(                            )
      {
            int   number      =             epoll_wait(      epollfd, events,    MAX_EVENT_NUMBER, -1 };
            if  (    (     number   <    0     )   && (      errno != EINTR  )   )
            {
                           printf(                   failure\n"       );
                           break;
            }

            for   (    int  i   =   0;     i     <   number;   i++    )
            [
                       int        sockfd   =     events[i].data.fd;
/*                                                           listentd,                 «/
                       if(        sockfd   ==      listenfd    }
     {
     struct sockaddr_ in client_address;
     socklen_t client_addrlength = sizeof( client_address );
     int connfd = accept( listenfd, ( struct sockaddr* )
     &éclient_address,&client_addrlength );

## Page 205

章 w%*
第 10 fH 号 187



                        addfd(     epollfd,       connfd );
               )
                            如 果 就绪的 文件描                   则
               /*                               是Pipefd[0]，      处理信   +/
               else     if( (    sockfd    ==  pipefd[0) ) && (   events[il.events & EPOLLIN )  )
               {
                   int      sig;
                        char          signals[1024];
                        ret    = recv(       pipefd[(0), signals, sizeof{  signals   , 0    );
                        if(    ret ==      -1 )
                        {
                                 continue;
                        )
                        else     if( ret     ==  0 )
                        {
                                 continue;
                        )
                        else
                        {
                                                   值占 LER,    ACES        ANCL SIGTERM
                                /*   国  为 每个信号
例， 说明如 何安全   地 终止    服务 器 主 循环  “/
为
                                for(    int i    = O; i  <  ret;      )
                                {
                                         switch( signals[i)     )
                                           {
                                              case    SIGCHLD:
                                              case    SIGHUP:
                                              {
                                                      continue;
                                              }
                                              case    SIGTERM:
                                              case    SIGINT:
                                              {
                                                      Stop server =   true;
                                              }
                                    }
                            }
                       )
               }
               else
               {
               }
          }
   }

    printf£(   "close fds\n"     );
    close(   listenfd   );
    close(   pipefd[1]  );
    close(   pipefd[0)  );
    return   0;
                                                                          .
 }

## Page 206

 188           第 二  篇       深入        解析      高    性  能 服务 器  编程

                                      相关   信和
 10.5          网络 编程                                   号

     本 节 中 我 们详细 探讨 三个和 网 络 编程密切 相关 的 信和号                                          。

 10.5.1        SIGHUP

     当 挂起进程 的控制 终端                                     ，SIGHUP            将被       没有      网    后
                                                                 信号
                                                  时                            触发 。对 于      控制 终端 的              络 台 程 序
      言  ，它们 通常利用                 SIGHUP          信和 号 来     强制      服务   器 重读 配置 文件    。   一 个典型
 而                                                                                 的    例子    是                 xinetd 超
 级 服务 程序        。
                        在接收 到                 SIGHUP        信和号  之     后将调用    hard_reconfig 函数         ( 见 xinetd 源码 )，
     xinetd 程序
 它循环 读 取           /ete/xinetd,d/             目录 下 的        每 个子配 置 文件 ，并              其
                                                                                      检测
                                                                                   变化 。     如 果              某 个 正在 运行 的
 子 服务 的 配置                  文件  被     修改以 停止 服务 ，则                   xinetd   主 进程   将给 该   子服务 进程 发送            SIGTERM
              以结束  它 。 如 果 某          个       子     服务 的 配置          文件 被        修改 以开启     服务 ，则   将                 新的
 信和                                                                                                               xinetd
                                                                                   创建
                                                  对应的端口 。            下 面 我 们 简单       地分 析 xinetd 处
 socket     并 将 其 绑 定 到           该 服务                                             理    SIGHUP                        信和
                                                                  上                                                    号
 的 流程    。
                 机器 Kongming20                上 具有
     测试                                                 如下环境:
     $   ps    -ef  |    grep         xinetd
     root     7438  1    0  11:32     ? 00:00:00            /usr/sbin/xinetd     -stayalive -pidfile /var/run/xinetd.pid
     reot      7442       7438    0      11:32    ?    00:00:00      (xinetd  service) echo-stream   Kongming20
     $     sudo    lsof    -p     7438
     xinetd        7438    root           3r  FIFO     0,8          37639  pipe
                   7438    root           4w  FIFO     0,8  OtO     37639  pipe
     xinetd        7438    root           5u  IPv6     37652  OtO     TCP  *:echo (LISTEN)
                           来看         ，xinetd     创建
     从      ps                   的 输出                       了子进程        7442，  它 运行    echo-stream   内 部 服务。从    lsof  的
 输出 来       看       ，xinetd 打开        了     一 个 管道 。该 管道               的  读 端 文件描述 符 的 值     是     3，       写 端 文件描述 符 的
 全       4。          后 面 我 们 将    看 到，它们 的 作用 就是统一 事件                              。   现 在 我 们 修改                     目录
                                                                                  源                                    下
                   文件，并
                                  给               发
 的 部 分 配置                                               送一个                    信和
                                      xinetd                  SIGHUP           号。具体 操作     如 下 ;
     $     sudo    sed   -i     's/disable.*=.*no/disable                   =  yes/'  /etc/xinetd.d/echo-stream   #
 ik echo    +
     §      sudo     sed   -i                     ‘'s/disable.*=.*yes/disable  =  no/' /etc/xinetd.d/telnet   #       开启
 telnet RF
     5     sudo    strace        -p     7438     &>    a.txt
     5     sudo    kill    -HUP       xinetd

                             命令 〈 见
     strace                         第    17   章 )                           能 跟踪 程序 执行 时 调用 的 系统 调用 和 接收 到 的 信和 号。这 里 我们
 利用              命令 跟踪 进程                                                      服务
     strace                                   7438，    即 xinetd           器    程序 ，以   观察xinetd 是     如 何 处理SIGHUP     信
     的。       此
              次  strace     命令 的 部 分 和输出如代码 清单                           10-2 所
 号                                                                               示。
                        代码 清单10-2                      用 strace  命令 查看 xinetd      理     的 流程
                                                                                  处 SIGHUP

-=~~ {si_Signo=SIGHUP, si_code=SI_USER, si_pid=7697, si_uid=0,
    si value={int=1154706400, ptr-0x44d36be0)} (Hangup) ---

## Page 207

 第10 音 信 号 189


 write(4, "\1", 1 = 工
 sigreturn() = ? (mask now [])
 poll([{fd-5, events=POLLIN}, {fd=3, events=POLLIN}], 2, -1 = 1 (({fd=3,
     revents=POLLIN}])
 ioctl(3, FIONREAD, 1 = 0
 read(3, "\1", 1 = 1

 state4 ("/Jetc/xinetd.d/echo-stream", {st_mode=S_IFREG|0644, st_size=#1149, ... = 0
 open ("/etc/xinetd.d/echo-stream", O_RDONLY) = 8
 time (NULL)       =     1337053896
 send(?, "<31>May 15 11:51:36 xinetd[7438]"..., 139, MSGNOSIGNAL) = 139
 fstaté4(8,        {st_mode=S_IFREG|0644,         st_size=1149,                   = 0
 lseek(8,    0,                   =  0
 Fent164(8,        F_GETFL)   =   0  (flags O_RDONLY)
 read(8, "# This is the configuration for ",.., 8192 = 1149
 read(8, "", 6192 = 0
 close(8) = 0

 kil1l(7442,       SIGTERM)   =   0
 waitpid(7442,       NULL,    WNOHANG)     = 0

 socket (PF_INET6,       SOCK_STREAM,     IPPROTO     TCP)        =  5
 fentl64(5,        F_SETFD,   FD CLOEXEC)     =   0
 setsockopt 5,       SOL_IPV6,       IPV6   V6ONLY,   [0],     4     = 0
                     SOL_SOCKET,      SO_REUSEADDR,           [1],   4  =  0
 bind(S, {sa_family=AF_INET6, sin6_port=htons(23), inet_pton(AF_INET6, "::",
     sin6_flowinfo=0, sin6_scope_id=0}, 28 = 0
 listen(S, 64        =  9

 该 输出 分为    4        个 部    分  ，  我 们用 空 行    将   每 个 部 分 隔 开         。
 第     一 部 分 描述 程序接收 到               SIGHUP         时， 和                            用     通知     该           的
                                      信号 信 号 处 理 函 数 使                               管道                   主 程序
                                                                                     信号
 来。信和    号 处 理 函 数 往     文件 描述        符   4  〈 管 道  的 写 端      )    写         人信号   1(SIGHUP 信号
 到                                                                                值    )，                而 主 程
 序 使用     检测
     poll        到文件描述符              3  〈 管 道  的 读 端  ) 上 有 可 读 事                ，就    将 管道上的     读入。
                                                                            件        数据
                    了  xinetd     重新 读 取     一 个  子 配置 文件          的   过 程 。
 第 二           部 分 描述
                    了  xinetd     给 子 进   程  echo-stream   (PID 为 7442               送     77-2             tt
 第 三 部 分描述                                                                              发 SIGTERM  KK
子进程 ，并  调用 waitpid            来 等 待 该    子    进程   结训
 该
                    了  xinetd 启动 telnet                 的过程                         一个流服务
 第 四 部 分描述                            服务                       :                       创建 socket 并 将 其 绑 定 到 端
                     该端口       。
 口 23 上 ，然 后 监听
 10.5.2   SIGPIPE

 默认 情况       下    ， 往  一 个 读      端 关闭 的 管道       或 socket     连接 中 写 数据 将 引发        SIGPIPE 信号。            我们
       需要 在 代码 中 捕获 并  处 理     该  信号    ， 或 者 至   少 忽 略        ，    因 为    程序    接收 到     信和            号 的 默认
                                                          它                          SIGPIPE                 行
 为 是 结束 进程 ，而 我         们 绝对 不 希望 因为 错误 的             写 操作 而 导致 程序                   。     起                 的
                                                                                     退出           引 SIGPIPE 信号
       将设置   errno    为 EPIPE。
 写 操作
 第    5  章 提 到     ，      们   可以 使    用   send 函数     的MSG_NOSIGNAL                  标志 来 禁止    写        操作 触发
                     我

## Page 208

    190   **   第 二      篇     深入 解析 高       性 能   服务 器  编程

SIGPIPE 信号。在 这 种 情况
        下，我 们 应 该 使用 send 函数 反馈 的 errno 值 来 判断管道或者
    socket 连接 的 读 端是否 已经关闭                    。
        此 外，我 们 也 可以 利用 IO                        复  用  系统 调用 来 检测 管道和socket     连接     的 读 端是知已经关
                        例，                        关闭    ，      描述
                                                        符        将
                                                        被     ，
    闭 。以  poll    为 当 管道               的   读 端          写 端 文件     上     的 POLLHUP     事件
                                                        时                                     触发 当
                                                     的                             事件
    socket 连接     定     方     关闭 时       ，socket  上     POLLRDHUP     将被            。

    10.5.3      SIGURG

                              下，内 核通知应               用 程序 带 外 数据 到 达 主 要 有 两
        在              环境                               种方法 : 一
             Linux                                      种是第     9                              章介绍
    的 IO     复 用 技术     ，seleect      等系统调用 在 接收 到 带 外 数据时 将 返回 ，并 向 应 用 程序报告                socket
                                                出        例子    用                                  信
    上 的 异常 事件 ， 代 码 清单 9-1                   给 了      一     个 这 方面 的 ，      另 外 一 种 方法 就 是 使 SIGURG
        ， 如 代码 清单       10-3      所 示 。
                                   代码 清单10-3           用 SIGURG 检测 带 外 数据       是 否 到 达
          #include      <sys/socket.h>
          #include      <netinet/in.h>
          #include      <arpa/inet.h>
              #include  <assert.h>
          include       <stdio.h>
          #include      <unistd.h>
                        <stdlib.h>
          finclude      <errno.h>
                        <string.h>
          #include      <signal.h>
                        <fentl.h>

          #define  BUF_SIZE        1024

          static   int    connfd;
                        信号的处理函数             */
          /* SIGURG
        void    sig_urg(          int sig   )
          {
                int     save_errno       =  errno;
                char    buffer[          BUF_SIZE   ]:
                              buffer,      '\O',    BUF_SIZE   );
                int     ret       recv(    connfd,     buffer,  BUF_SIZE-1,  MSG_OOB ); /* 接收
                              =                         带外                                 数据 */
                printf(       “got    td   bytes    of cob   data           ret, buffer );
                errno     =   save_errno;
          }

          void  addsig(       int     sig,  void (     “*sig_handler )( int  ) )
          {
                struct      sigaction       sa;
                              &sa,    '\O',   sizeof(     sa )
                sa.sa_handler          =   sig handler;
                sa.sa_ flags       |=  SA_RESTART;
                sigfillset(        &sa.sa_mask       );
                assert(       sigaction(      sig,     &sa,  NULL  ) != -1 );

## Page 209

   10章
   第  fe 号 we 191




   }

   int main{     int       arge,            char*   argv[]       )
   {
       if({    arge          <=  2  )

                 printf(                  “usage:   %s         ip_address  port_number\n",    basename( argy[0] )  );
                 return          1;
       )
       const              char*  ip    =    argv[1]:
       int     port        =       atoi(    argv[2]          );

       struct                      sockaddr_in  address;
       bzero(                               sizeof(          address    )  );
       address.sin_family                       =   AF_INET;
       inet_pton(                AF_INET,         ip,       Saddress.sin_addr       );
       address.sin_port                     =   htons(           port  );

       int     sock        =                (
                                        socket  PF_INET,           SOCK_STREAM,   O     );
       assert(           sock       >=   0     );

       int          ret  =    bind(      sock,         (    struct    sockaddr*   )Saddress,   sizeof(  address )  };
       assert(              ret  !=     -1     );

       ret     =            listen{  sock,        5     );
       assert(              ret         -1l    );

       Struct                      sockaddr_in  client;
       socklen_t              client_addrlength                   =   sizeof(  client     );
       connfd        =     accept(          sock,        (   struct    sockaddr*    )&client,  &éclient_addrlength );
       if   {    connfd          <   0   )
       {
                                                    is:      td\n",    errno   );
       }
       else
       {
                 addsig(                  SIGURG,   sig_urg         );
                         使用                    信号 之              ， 我 们必须
                 /*              SITGURG                     前        设置       socket              */
                 fentl{          connfd,                  F_SETOWN,   getpid() );

                 char         buffer[          BUF_SIZE          ];
                 while(          1          /*         环    接收 普通 数据
                 {
buffer,                                                          BUF_SIZE         });
                              ret    =   recv(              connfd,   buffer,  BUF_SIZE-1,  0  );
                              if({   ret       <=   0    })
                              {
                                            break;
                              }
                              printf(             “got   td      bytes of  normal   data    ‘4s'\n", ret, buffer );
                 }

                                 connfd         );

## Page 210

  192 «+ 第 二篇”深入 解析 高 性能服务 器 编程


        }

        cleose(  sock );
        return   0;
     }

     读者 不 妨编译 并 运行 该服务 器 程序 ，然 后            使用代码 清单       所     的               来往该
                                                             描述
                                                                            客户 端 程序
       需程序发送数据       ，以 观察服务 器 是如 何     同时  处 理 普 通  数据 和 带 外 数据 的 。
  服务
       此，我    们讨论
     至               完了 TCP  带 外 数据 相关 的    所 有 知识   。下 面 帮 助 读 者 重新 梳理 一    下 。3.8
      我们介绍 了    TCP   带 外 数据 基本 知识    ， 其 中 探讨了 模块
  节 中                                                        是如   何 发送         和 接收
                     的                          TCP                       带外数
的 。5.8.1 小 节 描述了 如 何在 应 用 程序中 使用带 MSG OOB 的
  据                                                    标志     send/reev   系统 调用 来 发
       带外数据 ，并 给     出  了 相关 代码   。9.1.3  节和
  TK /接收             小 10.5.3                         小 节 分 别  介绍 了 检测 带 外 数据     是
  到  达 的 两 种  方法     VO 复 用 系统 调用 报告 的    异常  事件和    SIGURG    号。       程序
                   :                                         信和 但 应 用          检测 到
            到达后， 我    们 还 需要 进一步 判断 带 外 数据 在 数据      流中  的 具体 位    ，        才 能 够准确
  带 外 数据                                                          置              无误
                     的     sockatmark 系统 调用 就     是  专门  用于  解决 这 个问题   的 。    它 判断
  地 读 取 带 外 数据   。5.9   节 介绍
  个  socket 是 否处 于     带 外 标记，即
  一                  该    socket     上 下    一 个 将 被 读 取 到 的  数据 是 否 是 带 外 数据 。

## Page 211

                     HR                                      定              时

         网 络        程序  需要 处 理 的 第   三 类 事件 是 定时 事件                   ，     比 如     定期        检测 一 个 客户 连接 的 活动     状态  。
    服务 器 程序 通常 管理着 众多 定时 事件 ，因 此 有 效                         地 组               织 这 些 定时 事件           ，  使之能 在 预期的   时   间
    点            发            且 不 影响服务 器 的 主要 逻辑    ， 对 于 服务 器 的 性 能                  有  着 至 关 重 要 的     影响   。 此，      们
                                                                                                       为                我
    要 将 每个定时 事件 分 别 封装 成 定时                        ，并 使     用某种       容器 类 数据 结构                   ， 比  如链表 、 排 序       和
                                               器                                                                       链表
    时间 轮 ，将所有 定时 器 串联 起                  来 ，以      实现 对 定时 事件                  的 统一 管理           。 本 章 主要讨论 的     就 是两  种
       效  的 管理 定时 器 的 容器:     时 间 轮 和 时 间 堆             。
                 过，在 讨论如   何 组     织 定 时 器     之    前  ，我 们  先      要 介绍 定时 的 方法                   。 定 时 是 指 在  一 段 时 间 之
         不
    后     发某 段 代码     机 制 ，
                     我
                     的               们     可以  在 这     段代码 中 依次 处 理                     所有 到 期的 定时     器 。    换 言 之 定，  时
    机 制 是定时 器 得以 被处理 的 原动力                     。Linux   提供 了 三 种 定时 方法                                它们
                                                                                                 ，    是:
         口 socket 选项     SO_RCVTIMEO 和 SO_SNDTIMEO。
         口 SIGALRM 信和 号。
         O       VO 复   用 系统 调用 的 超时 参数        。

    11.1           socket 选项 SO_RCVTIMEO 和 SO_SNDTIMEO

                              过 socket     选项      SO_RCVTIMEO 和 SO_SNDTIMEO，                          它们 分 别 用         设
         第       $ 章 中    我们 介绍                                                                                         来
    置  socket         接收 数据 超时时间 和 发 送 数据 超时 时 间                  。   因 此        ，   这 两 个        选项 仅 对 与    数据 接收 和 发 送
              的  socket 专用 系统 调用 〈socket           专用 的 系统 调用 指 的                       是5.2  一    5.11 节 介 绍 的 那些 socket
    相关
                                有 这 些系统                                         send、sendmsg、recv、recvmsg、accept
    API)           效，              调用包括                                                                和               们将
        connect。 我
选项 SO_RCVTIMEO 和 SO_SNDTIMEO 对 这 些 系统 调用 的 影响 总 结 于 表 11-1 中 。
        的作用
        表 11-1 SO_RCVTIMEO 和 SO_SNDTIMEO 选项
       系统 调用                                                                     系统 调用 超时 后 的 行为
       send                                                 -1，          置
                                                        返回 设                   ermo   为EAGAIN 或EWOULDBLOCK
       sendmsg                                          返回 -1，           置 ermo                    或
                                                                  设                   为 EAGAIN EWOULDBLOCK
       recv                                             返回 -1，    设      置     ermo   为  EAGAIN     或EWOULDBLOCK
       recvmsg                                          返回 -1，           置
                                                                  设            ermo   为 EAGAIN     或 EWOULDBLOCK
       accept                                           返回  -1，   设      置     ermo   为  EAGAIN    i EWOULDBLOCK
       connect                                          返回 -1，    设      置    crrno   为      EINPROGRESS

                   章 的 标题叫 定时     这 是  行业内 常用      的 叫 法    。实      际 上  ，     其 确切 的 叫法是 定时 器 容器。二 者 党 混           ，   书
                     过，    从 本 章 的 第 一             段    话 还 是能    看 出       二  者  的  区   ，    定 时 器 容器 是 容器 类 数据 结构 ，   比
                 也 没有 刻意 区分。不                                                            别                              如
     时 间 轮 ，定 时 器 则 是 容器 内 容纳 的 一 个 个对 象 ，它 是 对 定时 事件 的 封装 。

## Page 212

194        第 二     篇”深入     解析 高 性         能 服务 器    编程

      表            可
     由     11-1                   见，在 程序   中，我    们 可 以    根据 系统 调用 (send、sendmsg、recv、recvmsg、
      和                              值以
accept                                      及              判断    ，                                否
         connect)        的 返回                  errno      来       超时时 间 是     否 已   到 进 而 决定      是     开 始 处 理
     任务    。                                   例            ，       序           用                             来
定时          代      码  清单     11-1    以   connect          为     说 明 程     中 如    何             使 SO_SNDTIMEO 选项
    。
定时

                                                                                时间
                                      代码     清单    11-1              设置 connect 超时

 #include <sys/types.h>
 #include <sys/socket.h>
 #include <netinet/in.h>
 include <arpa/inet.h>
 #include <stdlib.h>
 #include <assert.h>
 #include <stdio.h>
 #include <errno.h>
 #include <fcntl.h>
 #include <unistd.h>
 #include <string.h>
 /* 超时 连接 函数 */
 int timeout_connect( const char* ip, int port, int time )
 {
    int ret = 0;
                                      in   address;
           struct               sockaddr
           bzero(      &address,             sizeof(    address ) );
           address.sin_family              = AF_INET;
           inet_pton(      AF_INET,          ip,                          );
                                       =   htons(     port   );

           int     sockfd    =  socket(      PF_INET,      SOCK_STREAM,     0  );
           assert(       sockfd     >=   0   );
                           SO_RCVTIMEO
                                               SO_SNDTIMEO          所 设置 的      时 间 的 类 型   timeval，    这 select
           /*                                      和                        超时        是                 和
             系统调用 的超时 参数 类 型 相同   “/
           struct          timeval   timeout;
           timeout.tv_sec          =    time;
           timeout.tv_usec            =  0;
           socklen_t        len    =  sizeof(        timeout    );
    ret = setsockopt( sockfd, SOL_SOCKET, SO_SNDTIMEO, &timeout, len });
                         ret   !'=   -1  );

           ret     =  connect(     sockfd,         (  struct    sockaddr*  )Gaddress, sizeof(     address  ) );
           if    (  ret   ==    -1   )
           {
                      /*  起 时 对 应     的 错误        号是EINPRDGRESS。  下 面这个 条 件如果 成     立 ， 我 们 就可以 处 理  定时任务了   */
                      if(      errno  ==   EINPROGRESS       )
                      {
                               printf£(          “connecting    timeout,  process   timeout logic    \n" );
                                  return   -l;
                      }
                      printf£t     “error    occur      when    connecting  to    server\n" );
                      return    -1;
           }

## Page 213

    Sue 定 时 器 fe 195



        return sockfd;
    }

    int main(   int  argc,     char* argv[] )

        if(   arge   <=  2   }

        printf( “usage: ts ipaddress port_number\n", basename( argv[0] ) );
               return       1;

               char*      ip  = argv[1);
        int   port   =  atoi(   argv[2)  );

        int   sockfd    =   timeout_connect( ip, port, 10 );
        if  ( sockfd     <   0  )

               return       1;
        }
        return 0;
    }


                                号
    11.2               SIGALRM 信和

    第     10  章 提 到，由        和 setitimer                                     ARR      SIGALRM
信号。 因此， 我 们 可以 利用 该 信号 的 信号处理 数 来处理定时 任务。
        但
                                                  函                          是， 如 果 要 处 理 多 个
                                                       号，  并 在 其 信号 处   理 函  数 中 执行 到 期 的   任
    定时任务 ，我们 就需要 不 断 地 触发        SIGALRM          信和
    务 。一   般 而        ，SIGALRM 信号 按照    固定 的 频率 生成           ， 由     或
                                                                           即 alarm
              言                                                          setitimer  函数 设置 的 定
                  不变。如     果 某 个 定时 任务      的    超时 时 间 不  是 了 的  整数 倍   ，那 么    它实 际被执行的   时
    时 周期   了 保持
间 和 预期 的 时间 将 略有偏差。因此 定时 周期 了 反映 了 定时 的 精度 。
        如何使用SIGALRM 信和
       本 节 中 我 们 通过    一   个 实例  一一   处  理非 活 动       连接 ， 来 介绍              号    定时        。
    过，我     们 需要 先 给 出 一种简单      的    定时    器 实现  一    一  基于 升序   链表 的   定时  器 ，并 把 它 应 用 到 处
    不
    理 非 活 动连接 这 个 实例         。  样，                                信和          函数是     处
                            中   这 我 们 才能 观察 到             SIGALRM     号  处 理     如 何     理 定时
                   的。   此
    器 并 执行 定时任务               外， 我 们 介绍 这 种   定时       器 也 是 为 了 和 后 面 要讨论 的 高 效   定  时 器 一 一
    时 间 轮 和 时 间 堆 做对 比  。

    11.2.1   基于    升序            定时
                        链表 的  器

       定时 器 通常至少要包含 两 个 成员            个 超时
                              :             一          时间  〈 相 对 时 间或 者 绝对 时间 )    和 一   个 任务
    回调 函数    。有 的 时 候 还 可 能 包含 回调 函数 被 执行              时 需要    传人 的 参数   ，以 及 是 否 重启  定时  器 等
    信息 。如 果 使用链表 作为 容器 来 串联所有 的 定时                     器  ，则 每 个  定时  器  还要 包含 指向 下   一  个 定时
    的 成员。                  ，                 的，
    指针 进 一              步       如 果 链 表 是 双向               则 每 个 定时 器 还 需要 包含 指向 前 一 个 定时 器 的
          成员。
    指针

## Page 214

篇 ”深入解析 性 编程
196 第 二  高 能 服务 器


代码       清单            实现                       升序               。  定时    定时
                11-2          了     一 个 简单 的     定时 器 链表         升 序     器 链表 将 其 中 的 器 按照
     时间
      做升序排序
                      。
                     超时
                                          代码  清单     11-2 ”升序    定时 器 链表
#ifndef               TIMER
#defineé     LST_TIMER

#include        <time.h>
#define      BUFFER_SIZE       64
class                          /*     前 向 声明     */

/*                     FP          socket Hit.     socket        */
struct
{
         sockaddr_in       address;
         int    sockfd;
         char     buf[  BUFFER SIZE           |];
         util     timer*   timer;
i

               器类 */
/*    定时
class       人   1 七
i
public:
      util      timer()    :       prev(    NULL ,   next(   NULL ){)

public:
      time_t      expire;      /*     任务 的    超时 时  间，    这 里 使用 地 对 时 间 =*/
      void     (*cb  func)(     client        data*  );   /* 性务回 调 函数 */
      /*    回调 函数处理的客户 数据 ，由 定时 器 的 执行 者 传递给 回调 函数               +
      client_data*          user data;
                                            指向
                                                定时           */
      util_timer*          prev;      /*    前一个     器
                                            指向
      util_timer*          next;      /*        下一个定时器       */
be

/*                                                               “/
class             sort_timer_lst
{
public:
      sort_timer_lst()             :  head(     NULL ,    tail({ NULL )  {}
      /*                        MRRP
      ~sort_timer_lst({)
      {
                util _timer*         tmp    = head;
                while(     tmp     )
                {
                     head    =     tmp->next;
                     delete         tmp;
                     tmp   =       head;
                }
      }

## Page 215

 第 11 章 定 时 器 ee 197


        /*   将  目标定时器       Limer        到      中
                                             链表    +/
        void    add_timer(       util_timer*    timer    )
        {
                if(     !timer   )
                {
                         return;
                }
                if(   !'head   )}
                {
                         head    = tail    =  timer;
                         return;
                )
/*                  如 有果 目  标定时 器 的超时 时       间小 于 当前 链表 中 所 有 定时 器 的 起时 时间，则 把 该定时       器 插入 链表 头      ，
                A.                            add_timer(  util_timer* timer, util_timer*  lst head   )   ，
 把 它插   入 链表 中 合     的 位 置 ，以  保证 链表 的 升序       特性  */
                if{   timer->expire        <  head->expire )
                {
                         timer->next       =  head;
                         head->prev      =   timer;
                         head    = timer;
                         return;
                }
                add_timer(       timer,    head );
        }
        /*
 AMR                                              Mee     */
        void    adjust_timer(      util_timer*      timer  )
        {
                if(     !timer   )
                {
                         return;
                )
                util _timer*      tmp  = timer->next;
/*                  如 果 被 调整 的 目标定时 器 处 在链表 尾部，或 者 该定时                器 新 的起 时 值 仍然 小于其 下 一   个定时    器 的
 BHR.               */
     if( !tmp || ( timer->expire < tmp->expire ) }
     {
     return;
     }
     果目 标定时 器 是 链表 的头节点，则 将 该定时 器 从 链表 中 取出 井 重新 和 链表 “/
     * 如
               if(    timer ==    head )
               [
                      head   =    head->next;
                      head->prev       =   NULL;
                      timer->next      =     NULL;
                      add_timer(       timer,   head  );
               }
               /*     如 果 目 标定时 器 不是链表 的 头 节点 ，则 将 该定时 器 从 链表 中 取出 ，然 后插入 其 原来 所 在位置 之
            中  +/
 后 的 部 分 链表
               else
               {
                                                                                                          -
                      timer->prev->next         =  timer->next;
                      timer->next->prev         =  timer->prev;

## Page 216

198 第 二篇 深入解析 高性能服务 器 编程



                         add_timer(    timer,      timer->next );
            }
        )
        /*                 timer 从链表中副除 */
        void                    util_timer*        timer )
        {
                if(   !timer    }
                {
                          return;
                }
                /*
                if(   (   timer   ==      head  ) && (   timer == tail   )    )
                {
                             delete  timer;
                         head     =      NULL;
                         tail     =      NULL;
                         return;
                I
                /* 如 果 链 表 中 至       少 有 两 个    定时   ，   且 目标定时   器 是链表  的    头 结 点 ，则 将链表 的 头 结 点重杆 为原头
    的下一个节点，           后   删除目标定时       器 “/
节点
                if(   timer   ==    head     )
                {
                          head =     head->next;
                          head->prev       =  NULL;
                          delete        timer;
                          return;
                I
                /*
                   REMR                  */
                if({  timer   ==    tail     )
                {
                          tail =     tail->prev;
                          tail->next       =  NULL;
                          delete        timer;
                          return;
                ]
                /*   如 果 目 标定     时 器 位 于 链表 的 中 间    。  则 把 它 前  后 的定时     器 串联 起   人  咕    目标定时器 */
                timer->prev->next            =  timer->next;
                timer->next->prev            =  timer->prev;
                delete       timer;
        }
        /*    SIGALRM     信号 每 次 被 触发 就 在其信号 处 理 函数     如 果 使    用          统一 事件    ， 则 是 主 函 数 ) 中 执行
tick 函数 ，以 处 理     链表 上 到 期 的     任务   */                                         源                  一次
        void    tick()
        |
                if(   'head    )
                {
                          return;
                }
                printf(    “timer      tick\n"     );
                time_t   cur    =   time(     NULL   );  /*  就 得 系统当前 的时间         */
                util timer*       tmp   =    head;
                /* 从 头 结点开始 依次处理 紫 个定时 器，直 到        到     一     个 尚未到 期 的定时 器，这 就 是定时 器 的     核心     */

## Page 217

    第 11 章 定 时 器 + 199



                 while(        tmp  )
            {
                           /*   因为 每 个定时 器 都 使 用绝对 时 间作 为起时         值 ，所 以 我们可 以 把 定时 器 的 超时 值 和系统 当
前 时间，比 较 以 判断定时 器 是 否 到期                “7
                          if(   cur  <  tmp->expire )
                          {
                                   break;
                          }
                               调用 定时 器 的回调 函数 ，以 执行定时 任务
                          tmp->cb_func(     tmp->user_data  );
                          /*    执行 完定时 器 中 的 定时 任务 之     后 ，就 将 它从  链表 中 删除  ， 并 重 置链表 头 结点  。/
                          head  =   tmp->next;
                          if(   head   )
                          {
                                   head->prev  =  NULL;
                          }
                         delete     tmp;
                          tmp   =  head;
                 }
            }

    private:
   /*                                DK,   EMAAH      HHH             adjust timer 函数 调用。该   函数表示 将
    目标定时 器  timezr                          之后的部分链表中     +/
  void                add_timer(      util_timer*   timer, util timer* lst_head }
             {
                      util_timer*      prev =  lst_head;
                      util    timer*   tmp  = prev->next;
                      /*  HF                                            BP
    节点，并    目标 定时 器 插入该节点 之前               */
                      while(    tmp  )
                      {
                           if(     timer->expire  < tmp->expire )
                           {
                                    prev->next  =   timer;
                                    timer->next   = tmp;
                                    tmp->prev  =  timer;
                                    timer->prev   = prev;
                                    break;
                          }
                         prev =     tmp;
                         tmp    = tmp->next;
                      }
                                    1st_head 节点 之 后 的 部 分 链表，仍  未找到 超时 时 间 大 于 目标  定时  的 超时  时 间 的 节
    点，则 将目标 定时 器 插入 链表 尾部 ，并 把它设置 为链表                  新的  尾 节点 */
              if(     !tmp   )
              {
                       prev->next      =  timer;
                       timer->prev       = prev;
                       timer->next       = NULL;
                       tail =     timer;

## Page 218

200 “%* 第 二篇 深入 解析 高性能服务 器 编程


|

private:
 util_timer*   head;
 util  timer*  tail;


    fendif

        了            ，        文件      lst        。
    为 便于 阅读            我 们 将 实现 包含 在 头      中   。sort_timer  是   一 个 升序 链表       其 核心 函
数 tick  相当 于      一       个 心 搏      数 ，它 每隔一段  固定 的 时 间 就执行 一 次 ， 以 检测 并 处 理 到 期   的 任
务   。判               断定 时任务 到 期的    依据 是 定时 器 的 expire 值小于当前 的 系统 时 间。从 执行 效率     来 看 ，
添加 定时 器 的 时 间 复 杂 度是On)，      删 除定时 器    的     时 间 复 杂 度  是     执 行   定时 任务 的 时 间 复
Fe ETE O(1)-
                                  非活动连接
11.2.2                               处理
    现在 我 考虑 上 述               升序        。                                            通常
              们               定时 器 链表 的 实际 应 用     一     处 理非 活动 连接   服 务 器          程序
                              该连接，
要 定期    处 理 非 活 动    连接       给 客户 端     一    个 重连     请 ，或 者 关闭           或
                                :        发        求                      者其他     。Linux
在 内 核中提供 了 对 连接 是 否处于 活动状态 的 定期检查 机     制     ，     我们     可     以    通过  socket     选项
KEEPALIVE                       来激活 。不 过 使用这 种 方式 将 使 得应用 程序对 连接 的 管理 变 得复杂   因
                          它        。                                                 此，
  们    可以 考虑 在 应 用 层 实现 类 似 于        的 机     制     ，以     管理所有 长 时间 处 于 非 活   动   状   态
我
              如，代    码 清单 11-3 利用 alarm      周期
                              性地触发    SIGALRM 信和
                              号，                                                    该信号
的 连接。比                                                                        的      信号
处 理       利用 管道通知 主 循环执行 定时 器 链表 上 的 定时 任务一一关闭 非 活动的连接     。

                                                     非活动连接
                              代码 清单 11-3               ”关闭

    #include       <sys/types.h>
    #include       <sys/socket.h>
    #include       <netinet/in.h>
    #include       <arpa/inet.h>
    #include       <assert.h>
    #include       <stdio.h>
    #include       <signal.h>
    #include       <unistd.h>
    f#finclude     <errno.h>
    #include       <string.h>
    finclude
    #include       <stdlib.h>
    #include       <sys/epoll.h>
    #ineclude      <pthread.h>
    finclude       "“lst_timer.h"

              FD_LIMIT    65535
    #define   MAX             1024
    f#define  TIMESLOT    5

## Page 219

    第 11章 定 时 B + 201



    static   int       pipefd[2];
    /*                 11-2  中 的     升序 链表 素 管 理     定时 器   。/
    Static         sort_timer_lst   timer_lst;
    static   int  epollfd     =    0;

    int   setnonblocking(       int    fd )
    {
           int  old_option      =   fentl(   fd,  F_GETFL       );
           int  new_option      =   old   option  |  ONONBLOCK;
          fentl(    fd,    F_SETFL,     new_option     );
          return    old_option;
    )

    void addfd(     int    epollfd,     int  fd  )
    {
          epoll   event     event;
          event.data.fd       =    fd;
          event.events     =    EPOLLIN     |  EPOLLET;
          epoll _ctl(      epollfd,         EPOLL_CTL_ADD,    fd, &event    );
          setnonblocking(          fd  );
    }

    void  sig_handler(        int   sig   )
    {
          int   save_errno      =   errno;
          int   msg =   sig;
          send(       pipefd[1]),   {  char*   )&msq,   1,    0 );
          errno   =    save_errno;
    }

    void  addsig(      int  sig    )
    {
          Struct        sigaction   sa;
          memset(      &sa,  '\O",     sizeof(   sa )   );
          $a.sa_handler       =    sig   handler;
          sa.sa  flags    |=  SA_RESTART;
          sigfillset(      &sa.sa_mask      );
          assert(      sigaction(      sig,  &sa,    NULL   )   !=  -1   );
    }

    void
    {
              定时 实 上 就 是                    tick BK           */
          /*        处理任务，           际       调用
          timer_lst.tick();
          /*   因为 一次       alarm    调用 只   会 引起 一 次 SIGRALRM        信号 ， 所 以 我们要 重新定时，以 不 断 触发SIGALRM
    */
          alarm(    TIMESLOT
    }

   定时 它 删除 非 活动 socket 上 的 事件 并 关闭 +s
    /*         回调函数，                       连接                   注册       ，    之
    void  cb  func(                        user  data   )
{
     epoll_ctl( epollfd, EPOLL_CTL_DEL, user_data->sockfd, 0 );

## Page 220

+  篇 解析 能 编程
202 ** 第 一二 深入 高 性 服务 器



    assert(     user_data       );
    close(    user_data->sockfd               );
    printf(     “close      fd      td\n",    user data->sockfd );
}

int main(   int     arge,   char*         argv[]  )
{
    if({  arge    <=  2   )
    {
             printf(      "usage:       %s              port_number\n", basename( argv[0) ) };
             return      1;
    }
    const    char*    ip    =       argv[1]:
    int   port    =    atoi(       argv[2]    );

    int   ret   =   0;
    struct    sockaddr_in         address;
    bzero(    Saddress,            sizeof(    address ) );
    address.sin_family            =   AF_INET;
    inet_pton(       AF_INET,       ip,    éaddress.sin_addr   );
    address.sin_port           =  htons(      port  );

    int   listenfd      =   socket(        PF_INET,  SOCK STREAM,   0 );
    assert(     listenfd       >=   0   };

    ret   =  bind(    listenfd,             struct  sockaddr* ( )Saddress, sizeof( address ) });
    assert(     ret   !=    -1   );

    ret   =  listen(      listenfd,        5  );
    assert(     ret   !=        );

    epoll    event    events[       MAX_EVENT_NUMBER      ];
    int   epollfd     =            epoll create( 5 );
    assert(     epollfd       '=  -1    );
    addfd(         epollfd,   listenfd        );

    ret   =  socketpair(        PF_UNIX,        SOCK_STREAM,  0, pipefd );
    assert(     ret   !=    -1   );
    setnonblocking(                pipefd[1]  );
    addfd(         epollfd,        pipefd[0]  );

    /* REBSREBR */
    addsig(     SIGALRM      );
    addsig(     SIGTERM      );
    bool         stop_server  =   false;

    client_data*        users     =   new     client_data[FD_LIMIT];
    bool    timeout     =   false;
    alarm(         TIMESLOT  );     /*   定时      */

    while(    !stop     server      )
    {
          int   number      =     epoll  wait(     epollfd, events,   MAX_EVENT_NUMBER, -1 };

## Page 221

Sue 定 时 BM fe 203



if  ( (    number   <   0       )   &&  ( errno          !=    EINTR     ) )
{
           printf(             “epoll  failure\n"           );
           break;
}

for   (   int  i= 0;        i     <   number;     i++       )
{
          int  sockfd       =     events[i].data.fd;
          /*   处理新 到 的 客户连接               +/
          if(  sockfd       ==      listenfd      )                     {
    struct sockaddr_in client_address;
    socklen_t client_addrlength = sizeof( client_address });
                int            connfd = accept(        listenfd,      (    struct sockaddr* )éclient_address,
                                                               &client_addrlength  );
                addfd(            epollfd,              connfd    );
                                                               =    client_address;
                users(connfd).sockfd                        =     connfd;
                /*        创建 定时       器   ，设 置         其 回调    函数 与 超时 时 间  ， 后 定  定时 县 与      用户  数    据 ，最
    timer      lst      */
                util        timer*        timer        =      new   util   timer;
                                          data    =      éusers[connfd);
                timer->user
                timer->cb_func               =    cb_ func;
                time_t            cur   =  time(             NULL   };
                timer->expire                =       cur  +    3    * TIMESLOT;
                users(connfd].timer                       =    timer;
                timer_lst.add                timer(         timer       );
          )
              处
          /*   理信号          wy/
          else  if(   ( sockfd        ==   pipefd[0]              )   6& ( events[i).events &  EPOLLIN  ) )
          {
                int   sig;
                char           signals[1024);
                ret   =     recv(     pipefd[0],               signals,     sizeof( signals    , 0 );
                if(   ret         == -l   )
               {
                            fi    handle     the       error
                            continue;
               }
                else    if({        ret   == 0    )
               {
                            continue;
               }
                else
               {
                        '   for(      int    i  =      O;   i     <   ret;     )
                            {
                                  switch(      signals[{i]            )
                                  {
                                          SIGALRM:
                                  [

## Page 222

篇 解析 性 服务 编程
204 第 二 深入 高 能 器


                                                /*   Mtimeout    变量 标记有 定时 任 者    要  处  ，但不立即
时 任务。这 是 因为定时 任务 的  优先 级 不 是             高 ，  我 们优先   处 理其他   更  重要 的 任            理      处理定
                                                timeout  = true;
                                                break;
                                        }
                                        case    SIGTERM:
                                        {
                                                stop   server   = true;
                                        }
                              }
                        }
              }
}
/*     处           理 客  户 连接上 接收 到 的 数据             “7
else               if({    events[i).events         &    EPOLLIN     )
{
                                 users[sockfd].buf,           "\0',   BUFFER_SIZE )  ;
                   ret   =        recv(  sockfd,                          BUFFER_SIZE-1,  0 );
                   printf(         "get    td bytes    of client  data   %s from %d\n", ret,
                                 users[sockfd).buf,         sockfd       );

                   util_timer*           timer     =  users[sockfd] .timer;
                   if(    ret    <    0  )
                   {
                                 /*                                                */
                                 if(     errno     !=  EAGAIN   )
                                 {
                                           cbfunc(     &users[sockfd]     );
                                           if(    timer   )
                                           {
                                                                          timer     );
                                           }
                                 )
                   ]
                   else    if(     ret     ==   0  )
                   {
                            1/*    WRHAEBAMBER,
                            cb_func( éusers[sockfd)               );
                            if(        timer    )
                            {
                                         timer_lst.del_timer(         timer );
                            }
                   }
                   else
                   {
                            /*
延迟 该连接 被关闭 的时间 */
                           IEr         timer  )
                           {
                                                t  cur  =  time(    NULL );
                                              time
                                        timer->expire     =  cur   +  3  *  TIMESLOT;
                                        printf(     “adjust    timer  once\n"  );
                                                                       timer   );

## Page 223

第11 章 定 时 器 f+ 205



                        }
                     }
              |
              else
              {
                      others
              }
        }

    /* 最 后 处 理定时 事件 ，因为TVO 事件有 更 高 的 优先 级。当 ，这 样 做将导致定时 任务不能 精确
地 按照预基 时 间 执  行     /
        if(   timeout     )
        {
                                 ();
                    timer_handler
                    timecut = false;
        }
       |

       close(  listenfd   });
       close(  pipefd[1]   );
       close(  pipefd[0]   );
       delete  []   users;
       return  0;
}



11.3      IO  复 用      系统       调用 的 超时   参数

          下
    Linux     的  3 组 IO    复 用 系统 调用 都带有 超时 参数 ，因 此 它们 不仅能 统一处理 信    号    和           VO
事件 ，也 能 统一 处 理定时 事件 。                    是由于                   之                      返回
    但 IO 复 用 系统 调用 可 能 在 超时 时 间 到 期 前 就
(有     事件
    LO            发生)，所 以 如 果 我 们 要 利用 它们 来 定时          ， 就需要 不 断 更 新 定时 参数以 反映       剩余
    时  ，                     Aras.
的
      间   如 代码 清单      11-4

                             代码 清单 11-4 ”利用 UVO       复 用 系统 调用 定时

    #define  TIMEOUT   5000

    int timeout    =   TIMEOUT;
    time_t   start   = time(   NULL    );
    time_t   end =   time(   NULL
while( 1 )
 [
    printf( "the timeout is now td mil-seconds\n", timeout );
          start  =   time(   NULL   );
          int number  =     epoll   wait( epollfd,  events, MAX_EVENT NUMBER, timeout );
          if( (  number    < 0  )   && (  errno !=  EINTR ) )
          {
                   printf(      “epoll  failure\n"  );
                   break;
          }

## Page 224

    206 “%* 第 二篇 深入解析 高性能服务 器 编程


                                                       定时
               /*     fRepoll           成 功 返回        则 说 明 起 时 时间 到， 时 便 可 处 理 定时 任务 ，并 重置
                                wait        0，         时间     +/
               if{    number   == 0 )
                  {
                         timeout  = TIMEGUT;
                         continue;
                  }

               end    =  time( NULL     };
                                            值大于      0， 则 本 次 epoll_wait 调用 持续
               /*     wRepoll  wait     的 返回           的时间是 (     end     -     start     *
    1000       AA]                                     ARATE
        ms,                         timeout            K     epoll_wait        */
               timeout -=     (   end   - start    )  * 1000;
                                                       wait
               /*   重新计算 之 后的       timeout    值 有  可 能 等      说明 本 次 epoll
           而 且 共 超时 时 间  也 刚好 到 达，此 时 我们 也 要 处         理 定时     ，并 重 置定  时 时间 +/
               if(    timeout   <=  @   )
                  {
                         timeout  =    TIMEOUT;
                  }

               //  handle    connections
           }


                                   器
    11.4                     高 性 能定时

                        时间
    11.4.1               轮
      文提到，基 于 排序链表 的定时 器 存在 一 个问题 : 添加 定时 器 的 效率 偏 低。下 面 我们 要
   前
讨论 的时间 轮 解决了这 个 问题。一 种 简单 的 时间 轮 如 图 11-1 所 示 。

                                        slot



                                                   owe

                                                   PA          的时间轮
                                                                 简单

                                           线)      指针 指向 轮子    上 的 个 (slot)。    它     以 恒定
    图     11-1 所 示 的 时 间 轮              内  ，( 实        一                               的速度
   时针          动，                          指向          指针      )，          次      为
    顺
             转        每          转动 一 步 就      下 一 个     虚 线 指向 的 槽     每 转动 称      一 个 滴答
    (tick)。 一  个 滴答      的时间 称 为 时间 轮 的 槽 间隔 si        (slot interval)， 它 实际上 就 是 心  时间。 该
时 间 轮共有 个 因 此 它 运 转 一 周的时间 是 Ne*si。 每 个 槽指向一条 定时 器 链表， 条
        每
                         模                                                              链表

## Page 225

                                                                                      第 11   章     定 时 器    ee   207

    上 的 定时 器 具有 相同 的 特征                    : 它们 的     定时      时间 相 差            的 整数    倍。        时间 轮 正 是 利用 这 个 关系
    将 定时 器         列  到 不 同 的       链表      中。              假 如 现在 指针 指向            我 们 要 添加       个 定时 时间 为       的
                                                                            槽cs，               一             人     定
    时，则 该定时 器 将 被搬人 档                        ts  Ctimer slot)      对 应 的        中:
                                                                          链表
                                                 ts=   (cs+   (ti/si))    %N
   基于 排序 链表 的 定时 器 使用唯一 条 链表 来 管理所有 定时
                                                       的  一                             器，          所以 插 人 操作 的 效率 随
者 定时 器 数目 的增多 而 降低。而时间 使用哈 希 表 的 思想，将定时 器 散 列 到 不 同 的 链表
    这 样         每条链表  上 的 定时 器 数目 都              将 明显     少    于 原来 的     排序    链表 上 的 定时 器        数目  ，          操作
                                                                                                   人              的效
    率 基 本 不 受     定   时 器 数目 的影响            。
                                                                                        小          提高 执行 效率 ，      则
       很 显然 ，对 时 间 轮 而              言       ，要 提高     定时      精度 ，就 要       使 si    值 足 够                          要
                 大。
    求  值 足够
       图 11-1           的       是 一 种 简单 的时 间            轮 ，   因 为 它  只   有 一     个 轮子  。      而 复杂 的时 间 轮 可 能 有 多 个
    轮子 ，不 同 的 轮子 拥有不 同 的 粒度 。相 邻的 两 个 轮子 ，精 度 高的转                                            一     图，精 度 低 的 仅 往 前 移
    动 一 ，就 像 水表 一                   。下 面 将 按照
                                    样                 图  11-1  来编写        一 个 较为 简单      的   时 间 轮   实现 代码  ，   如 代码
    清单  11-5    所 示   。

                                                   代码     清单   11-5   Hee
       Vifndef     TIME     WHEEL     TIMER
       tdefine               TIME_WHEEL_TIMER

       #include       <time.h>
       finclude       <netinet/in.h>
       #include       <stdio.h>

       ‘define                      64
       class     socket
                  tw   timer;
       /*       BR        和 定时 器     */
       struct     client_data

                                  address;
             int     sockfd;
             char     buf[                       ];
             tw_timer*          timer;

                 器类    */
          定时
       class     tw_timer
       {
       public:
             tw_timer(          int     rot, int ts   )
             :  next(   NULL        ,    prev{   NULL    ,     rotation(    rot     , time_slot(     ts )()}

       public:
             int         rotation;      /*   记录 定时 器 在 时 间 轮   转     多 少    后生效       +/
             int          slot:     /*       记录 定时 器属于 时 间 办 上 哪个                   对 应的链表，下       ) */
                                                                                    调函数 */
             void         (*cb      client_data*              ); /*  定时
             client_data*           user_data;      /*     客户 数据 */

## Page 226

    篇    服务
    208 + 第 二 深入 解析 高 性 能 器 编程



                                                     指向
tw_timer*                      next;           /*          下一个定时          */
tw_timer*                      prev;           /*                         */
        }

    class              time_wheel
    {
    public:
                                :        cur_slot(           0    )
              {
                  For(      int i        =   0;      i <    N;    ++i )
    {
                            slots[i]           =     NULL;        /*  初始化 每 个     的头 结点 */
    }
              }
              ~time     wheel ()
              {
                  /*                                                    +/
                  for(      int i        =   O;      i <   Ny;    +4i )
                  {
                            tw_timer*              tmp   =   slots[i];
                            while(         tmp       )
                            {
                                    slots[i]             =   tmp->next;
                                    delete             tmp;
                                    tmp        =     slots[i];
                            }
                  }
              }
   创建
   定时                                                                 并 把 它   入    的    中 */
              /* AGH timeout                             一个          器，     播
tw_timer*                      add _timer(             int   timeout     )
              {
                  if({           timeout   <   0     )
                  {
                               return        NULL;
                  }
                  int       ticks      =   0;
                  /*
    情 于 变量  ticks ?.                                                          SI,     We  ticks 向上折  为  FURS
    ticks 向 下  折人各为    上                     */
                  if(            timeout   <   SI     )
                  {
                               ticks       =   l;
                  }
                  else
                  {
                               ticks       =   timeout         /   SI;
                  }
                  /*                            计算 待插入 的定时 器在时 间      多  少  后     被 触发 */
                  int       rotation         =     ticks     /
                                                           RR         Pt    /
                            tA
                  int       ts =  (      cur_slot          +   (   ticks    )     ) % N;
                  /*                                              PHM  rotation        PAE */
                  tw_timer*         timer          =   new       tw_timer(  rotation,   ts );

## Page 227

                                                                     第 11   章 =H 器 e+         209

头 结点 */       /*   如 果   第 ts 个   槽中 尚 无 任何   定时     器，则 把 新 建 的定时 器 插入   其 中 ，并 将 该定时 器 设置 为 该槽的
              if({ !slots[ts]       )
              {
                     printf£(       “add timer, rotation   is %d,  ts     is   cur_slot is
                                    rotation, ts,    cur_slot );
                     slots[ts]       =   timer;
              }
                                  器播入第    ts    槽    中
              /*   和 否则，   将定时
              else
              {

                   timer->next      =  slots[ts];
                   slots[ts]->prev      =  timer;
                   slots[ts]      = timer;
              }
              return     timer;
         }
         /* 删除 目标定时 器        上  imer   */
         void  del_timer(       tw_timer*    timer    )
         {
               if(  'timer      }
               {
                           return;
               }
               int  ts     =    timer->time_slot;
              /*   slots(ts])     是    目标定时 器 所 在      的 头 结 。如 果 目 标定 时 器 就 是 该 头 结 ，则 需要
槽的头结点                                   点                                点        重置第    ts
个
               if(  timer     ==    slots[ts]  )
               {
                         slots([ts]    = slots[ts]->next;
                         if{  slots[ts]   )
                         {
                                slots[ts]->prev     =  NULL;
                         }
                         delete     timer;
               }
               else
               {
                    timer->prev->next     =     timer->next;
                    if({     timer->next  }
                    {
                             timer->next->prev     =     timer->prev;
                    }
                    delete      timer;
               }
         }
                   间到后，      调  用 该 函数   ，时间 办     前 滚动 一 个 槽 的 间隔  */        ，
    /* SI 时
    void
    {
    tw_timer* tmp = slots[cur_slot]; /* 取得 时间 上 当前 机 的 头 结点
    printf( “current slot is td\n", cur slot );
    while( tmp )

## Page 228

ss* 第 二篇 深入 解析 高 性 能 编程
210        服务 器




     {
             printf( "tick                the  timer     once\n"  );
             /*    如 果 定     时  器 的                0,                 */
             if( tmp->rotation                 > 0   )
             {
                      tmp->rotation--;
                      tmp    =         tmp->next;
             }
                   否则 ，说 明定时                        到 ，   执行
             /*                          器    已 经    期    于 是     定时任务，然 后删除 该定时器 */
             else
             {
                      tmp->cb_func(              tmp->user data     );
                      if(    tmp     ==    slots[cur_slot]        )
                      {
                                printf(        "delete     header  in cur_slot\n" };
                                slots[cur_slot]     =     tmp->next;
                                delete       tmp;
                                if(    slots[cur_slot]     )
                                {
                                         slots[cur_slot]->prev        = NULL;
                                }
                                tmp    =   slots[cur_slot];
                      }
                      else
                      {
                                tmp->prev-Snext     =     tmp->next;
                                if(    tmp->next     )
                                {
     a                                             =               tmp->prev;
                                }
                                tw_timer*           tmp2  = tmp->next;
                                delete       tmp;
                                tmp    =    tmp2;
                      }
             }
     }
     cur_slot      =  ++cur_slot           %  N;      /*  更新时间 辊 的 当前   ，以   反映 时间 轮 的 转动 */
 }

private:
 /*    Ble                *V
 static const       int   N  =    60;
 /*    每1                                          s     */
 static     const   int   SI    = 1;
 /*                                                BRE */
 tw_timer*   slots(N];
 int    cur_slot;     /*                 Saw */
]

#fendif


见，                    ，                                           O    定时
可   对 时 间 轮 而         言   添     加 一    个 定时    器 的     时 间 复 杂 度 是     〈1)， 删 除 一 个 器 的 时

## Page 229

    定 时 器 of 211


    间 复 杂 度 也是                O  〈1)，     执 行     一 个        定时     器 的    时间 复 杂 度         是 O (n)。        但 实际 上 执行 一 个                   定时   器 任
    务 的              要比 O                           ，                            所
                             效率  Cn)     好得       多     因 为 时间 轮 将                 有 的 定时 器 散列 到 了 不 同                        的     链表 上  。    时 间 轮
    的 槽 越            ，等     价    于散 列表 的          人          Centry)       越
                                                    口                          多，    从 而 每 条 链表 上 的 定时 器 数量                               越   。 外，
                多                                                                                                                           少 此
我们 的 代码 仅 使用了 一 个时间 轮 。当 使用多 个 轮子 来实现 时间 轮时，执 行 一 个 定时 器任务 的
    时间 复 杂 度 将 接近                  O〈1)。     读 者 不 妨                 把代码       清单 11-3        做             ，                            排序
                                                                                                          稍
                                                                                                         修改  用 时间 轮 来 代替                        链表 ，
    以 查看 时 间 轮 的 工作                 方式    和 效率           。
                                  堆
    11.4.2                       时间

                      前 面 讨 论 的  定时 方案 都 是以 固定 的 频率 调用 心 搏函数                                   tick，        并 在          其 中  依次 检测 到 期         的  定
     器，       然      后 执行 到 期        定时     器 上 的 回调 函数                    。   设计 定时 器 的 另外 一                    思路        是     将      所有        器中
    时                                                                                                            种            ，                   定时
                                                                                              样，                   旦 心 搏 函 数            被调用 ，      超
    超时 时 间 最 小 的 一 个 定时                           的 超时 值 作为 心 搏 间隔                 。       这     一                            tick                 时
    时间 最小 的 定时 器 必然 到                             ，我         们     就 可以 在 tick                   函数 中 处 理 该         定时     。     后，                的
                                     期                                                                               器                    然 再 次 从 剩余
    定时               找 出超时 时间 最                   的 一              ， 并 将 这 段 最     时 间 设 置     为    下                次 心 搏 间隔           。          如
                                     小                         个                   小                         一                              此 反复  ，
    就 实现了 较为精确的定时                    。                                                                                         21
        最       小    堆 很适合 处 理 这 种定时 方案 。最小 堆 是 指 每个节点                                                的
    值 都    小  于      或 等于其 子 节点 的 值的完全                             二  叉        。 图     11-2           给
    个具有       6   个 元 素          的 最     小 堆 。                                 树                    出了一                  24                       31
        树 的 基本 操作 是 插 人 节点 和 删除节点 。                                                对 最小 堆 而     言 ，   它      65          )     26        32
    们 都 很 简单           。 为    了    将     一 个 元 素             插 人 最 小         堆 ，    我    们   可以 在 树 的 下
    8 A                EE FC                                       BT   ESS                          HE                        图 11-2
    序 ，则             人完成。          否则 就 执行          上    虑 操作         ，    即 交换 空    穴     和 它  的   父 节
    点 上 的 元            。不 断 执 行      上    述       过          ， 直 到         可以 被    放    人 空     ，                       则 搬 人 操作 完成   。  如，       们要
                       素                                 程                                     穴                                        比 我
  图 11-2 所 示 的 最 堆 中 插入 值为14 的 元 ， 则 可 以 按照
    往                               小                                          素               图     11-3        所 示 的           步骤   来 操作   。
                         ©                          |                              CD                                         ©                     |
        4                            CGC)                                                                        全                       ©
    SE               Oe              Of                            &                          O®                                              &

                         a)                                                    b)                                                c)
                                                             图 11-3   ”最小 堆        的    插  人 操作
                                                  a)     创建 室穴        b)       上 虑 一    次  c) 上     二次
        最       小    堆     删除 操作 指 的 是 删除其根节 点 上 的 元                                       素 ，并 且 不 破坏 堆 序 性                      质 。 执 行    删除   操作
    时，我       们 需要 先 在 根 节 点 处 创建 一 个 空                                    。   由 于 堆 现 在      少    了 一 个 元           素   ， 因 此 我 们 可 以        把    的
    最 后    一  个 元素       移动 到 该 堆 的 某                        个 地        。      果              放                      ，   删除                 。
                                                                   方              如 天     以 被       人 空              则         操作 完成           否 则 就

## Page 230

  212     第 二      篇       深入 解析 高    性    能     服务     器 编程

           下虑操作    ， 即     交换 空 和 它 的 两 个 儿子 节点 中 的 较 小                           者。不 断 进 行    上    述 过 程 ， 直 到  可
  执行
                                                     如，        我们 要 对 图       11-2 所 示 的 最小 堆 执行 删除 操作    ，      可
  以 被放人 空 ，则 删除操作 完成 。比
              穴                                                                                                  则
  以 按照图  11-4      所 示 的         步骤    来 执行  。
                                                                         ©        ©
             OY                     OO               O                            @®                     ©
                                                 ©                                ©
                     a)                                        b)                 c)
                                                 图     11-4 ”最       小 堆 的    删除 操作
                                    a)   在 根 节点 处 创建 空认                 下虑一       ec) 下 虑 二 次
    由 于 最小 堆 是                  种    完全 二            ，所 以 我    们 可 以 用 数组 来       组   织 其  中 的 元     。比
                           一                                                                            如，  图    所
                                                 树                                素                           11-2
的 最 小堆可以 用 图 11-5 所 的 数组来表示。对 于 数组中 的 任意 一个位 置?上 的 元 ，其
      素 左儿子
 在位置 上，其 儿 子 贡 点 在 位置 2
  节点     2  1                                                  2     上，其         父 节 点 则 在 位 置[(F1)  2 0 上    。  与
  用 链表 来表示 堆 相 比                ，   用  数组表示      堆    不        仅 节省 空间  ，        而 且 更 容易 实现堆 的 插入   、删 除   等操作
                                21     | 24  |   31     |   65 | 26     |  32     |     |
                                    0   1            2      3    4         5     6    7      8
                                                 图     11-5    ”最小 堆 的 数组表示

    假设 我 们 已经有                       个 包含      个 元 素        的 数组     ，         现 在 要 把 它 初始 化 一 个   最 小        。 那
                                一                                                 为                     堆      么最简
    的     是 :              化一个空堆，                    后  个                         堆 。
  单
       方法                                                               初始 然 将 数组 中 的 每 元 素 人 该 中 不 过 这 样 做 的 效率 偏
  低 。实 际 ，我 们 只 需要 对 数组中 的第 [CN-1) /2                                         ~0     个 元 素 执行 下虑 操作   ，     即 可 确保
        上                                                                                                       该数
       一个最小堆。                       因为
  组 构成                          这 是                 对包      个 元 素 的       完全  二 叉   树 而     ， 它具有    [CN-1)  /2  非
                                                                                  言                              个
                                                                                                                 这
  叶子 节点 ，这 些 非 叶 子 节点 正 是 该完全                               二 叉 树 的     第  0~ [CN-1)    /2   个 节点   。   我 们 只 要 确保
  些 非 叶子 节点 构成 的子树 都具有                               序 性       质 ，整   个 树 就   具有   堆 序 性 质 。
    我  们  称 用 最 小 堆 实 现                 的    定时  器 为    时   间 堆   。   代 码 清单      11-6 给   出 了 一种   时 间   的 实现  ，其
  中，最小 堆 使用数组 来 表示                  。

                                                     代码 清单 11-6

    f#ifndef MIN_HERP
    #define  MIN_HEAP

    #include       <iostream>
    #include               <netinet/in.h>
    #include       <time.h>
    using   std::exception;

    #define  BUFFER_SIZE            64

## Page 231

第11 章 定 时 器 fe 213


class     heap_timer;         /*        前 向声明    */
/*         socket  PEAR             */
struct     client_data
{
      sockaddr_in          address;
      int         sockfd;
      char    buf[      BUFFER_SIZE     );
      heap timer*           timer;
   he

/*   定时器类
class          heap_timer
{                                                                     :
public:
     heap timer(          int   delay     )
     {
            expire      =  time(        NULL   ) +    delay;
     }

public:
     time_t   expire;         /*                      Mx    ath]  */   器
的                                                                     回调函数
     void   (*cb                client_data*             ); /*    定时     +/
     client_data*         user_data;           /*     用  户  数据   */
be

/*   BRR
class     time_heap
{
public:
     /*                                                              */
     time_heap(       int    cap     )    throw  (    std::exception    )    : capacity( cap ,   cur_size( 0 )
     {
                                            timer*                    创建
           array      = new     heap                      [capacity];  /*     堆数组 */
           if  (   !   array        )
           {
                       throw        std::exception();
           }
           for(    int     i  =     0;    i <  capacity;    ++i     }
           {
                                        =   NULL;
           }
     }
     /*    构造函数 之          二，      用 已  有数组   素 初 始 化    推  */
     time_heap(                                init_array,        int  size,   int capacity  )   throw
    ( std::exception ): cur_size( size , capacity( capacity )
{
           if  (   capacity          <    size   )
           {
                   throw      std::exception();
           )
           array      =  new    heap     timer*           (capacity);  /*        */

## Page 232

ws*  篇 解析 性 服务 编程
214  第 二 深入 高 能 器




       if   (     !          array  )
       {
                             throw  std::exception();
       )
       for(       int     i      =  O; i    <   capacity;  +4i )
       {
                              array[i] =    NULL;
       J
       if   (     size          !=  0  )
       {
                                    化       组
                     /*     韧 始       堆 数       */
                     for     (     int i    =   0; i   < size; ++i   )
                     {
                                   array{    i  ]  =   init_array[   i ];
                     }
                     for     (     int   i  =   (cur_size-1)/2;    i   >=0; --i )
                     {          /*  SHB            ((cur_size-1)/2)~0      TERE    */
                            percolate down({             i );
                     }
       )
]
/* FAR     ie               /
                     ()
~time_heap
{
       for     (     int     i=       0; i   <   cur_size;   ++i )
       {
                     delete         array[i);
       }
       delete           []   array;
}

public:
/*                              timer     */
 void add_timer( heap_timer* timer ) throw ( std::exception )
 {
                      !timer    )
       {
                     return;
       }
       if(     cur_size             >=   capacity      )   /*            AI     */
       {
                  resize();
       }
                  插入                    ，                  加             置
       /*    新            了  一     个 元 素    当 前    堆   小 1，hele 是 新 建 空 穴 的 位    “7
       int           hole =     cur_sizet++;
       int            parent    =   0;
       /*                                                                */
       for(       ;     hole      >   0;   hole=parent     )
       {
                        parent       =   (hole-1) /2;
                        if   (     array[parent]->expire     <=      timer->expire )
                        {
                                       break;                            .
                        }

## Page 233

 11
 第 章 定 时 器 ee 215



                          array(hole)    = array(parent];
             }
             array([hole]         =  timer;
     }
     /*    删除目标定时         器   上 imer    */
     void    del      timer (   heap timer* timer       }
     {
             if(      '!timer  )
     {
     return;
     }
     /* 仅仅 将 目标定时 器 的 回调 函数 设置 为 空 ， 所 调 的 延迟 。这 节省 真正 除 该定时 造
 的开销 ，但 这  样 做容易      使        组      “/
 成
             timer->cb_func           = NULL;
     上
     /*      得 堆 顶    部 的     定时  是  +/
                           top()    const
     {
          if           empty()    )
          (
                      return      NULL;
          }
           return     array[0];
     }
     /=    删除堆顶 部 的定时 器              +/
     void    pop_timer()
     {
             if(       empty()    )
             {
                      return;
             )
             if(        array[0]    )
             {
                      delete      array(0);
                      *                                 */
                      array[0]       =
                      percolate _down(     0);      /*    */
             }
     }
     /*  OBR          */
     void    tick()
     {
           heap timer*     tmp        = array([0);
           timet      cur     =   time(   NULL );   /*    +/
           while(           !empty()  )
           {
                 if(      !tmp    )
                 {
                              break;
，                }
                                     Ot ie BM, A
                 /*   SORT
                 if(      tmp->expire     > cur    )
                 |                                      .

## Page 234

    篇 解析 性 编程
    216 “s* 第 二 深入 高 能 服务 器




                            break;
                   )
                   /*     理 则  就 热 行   堆   顶  定时     器 中  的 任务 */
                   if(                              func
                            array[0]l->cb                 )
                   {
                            array(0)->cb_func(              array[0)->user_data );
                   )
                   /*     将 堆 顶    元 素 副      除 ，同 时        新的 堆   顶定时 (array[0])  */
                     pop_timer();
                     tmp    =  array[0);
        }
    )
    bool    empty()         const    { return     cur_size       == 0; }

private:
    /* 最小            的  下虑    作   ，它 确保 堆       数组    中  以  第 hole 个 节点 作为 根 的 子 树 拥有 最 小 堆 性质 */
    void                               int      hole     )
    {
            heap _timer*          temp  =     array([hole);
            int    child      =   Q;
            for    (;       ((hole*2+1)         <=    (cur_size-1));   hole=child  )
            {
                   child      =   hole*2+1;
                   if     ( (child     <    (cur_size-1))        56 (array[child+1l]->expire <
                                         array(child)->expire  )   )
                {
                          ++child;
                }
                if     (    array(child]->expire            <  temp->expire )
                {
                          array(hole)         =  array[child);
                }
                else
                {
                          break;
                }
        }
                                =  temp;
    }
                                  A 1H   */
    /*                            SMBH
    vold    resize()        throw      ( std::exception          )
    {
            heap_timer**           temp     =   new   heap  timer*    (2*capacity];
            for(     int    i  =   0;    i  <   2"capacity;      ++i  )
            {
                     temp[i]       =   NULL;
            }
            if  (    !    temp    )
            (
                     throw
            }
            capacity        =   2*capacity;
            for    (    int   i= 0;     i     <  cur_size;         )
            {

## Page 235

第 11 章 定 时 器 we 217


                  temp[i]   =
           }
           delete   [] array;
           array  = temp;

   private:
   heap _timer**    array;     /*  M&M */
   int     capacity;  /*        */
   int     cur_size;  /*    推  数组  当前 也 含 元来的 个    数 “/

   fendif

                       见，   对 时 间 堆 而  ， 添  加     个  定时 器 的 时 间 复 杂 度是O  删
   由 代码  清单 11-6  可        言                  一        除
                           因 时 间
一  个定时 器 的 时 间 复 杂 度是      O 〈1)，  执 行 一个  定时 器 的 时 间 复 杂 度是O (1)。     此，
的      是很高的。
          效率

## Page 236

              第     12     章      高              性      能 LO                       库
                                                                     框架            Libevent

   前 面 我 们利用 三 章 的 篇幅较 为 细致 地 讨论 了 Linux 服务 器 程序必须 处 理的三 类 事件
事件 、信 号 和 定时 事件 。在 处 理 这 三 类 事件 时我们 通常需要 考虑如 下 三 个 问题 ;
             统一 事件     。很 明显     ，    统          处      理 这 三 类 事件 既 能 使 代码 简单            易 ，    又 能             一些潜在
               源                  一                                                                                避免
           的        错误    。前 面 我 们     已 经 讨论           了 实现 统一 事件 源 的             一 般   方法    一 利用  VO        用
                                                                                                                    复
                                                                                         一                         系统
               调用 来 管理所有 事件      。
                    性。    不同 的 操作 系统             具有     不 同  的       复 用 方式        ，
        CD   可 移植                                                                  比
                                                                 VO                如     Solaris 的        dewpoll 文件，
                              机     ，Linux 的 epoll               系列 系统 调用          。
           FreeBSD 的 kqueue       制
             对 并 发 编程的 支持 。在 多 进程和多 线程                           环境     下 ，                我 们 需要 考虑 各 执行 实体 如 何 协同 处
           理 客户 连接    、信     号 和     器                              竟态条件。
                                                                 定时 ，以 避免
        所     的 是  ，开 源社 区 提供 了 诸多 优秀 的                  VO             框架 库 。 它们 不 仅 解决    了         上 述问 题   ，让 开发
    者 可以 将 精力 完全 放 在 程序的 逻辑上                            而 且 稳 定性         、 性 能     等 各 方面 都 相当   出       。    如
                                                                                                                   比
                                                   ，                                                 色          ACE.
    ASIO 和 Libevent。     本 章 将 介绍     其   中           相对 轻 量 级 的 Libevent      HEAR FE.
               库
                              概
                                     述
    12.1                            框架

        VO   框架   库以 库 函 数 的 形式           ，      封 装    了 较为  底层        的  系统  调用    ，给  应用     程序提供 了     一   组 更 便
    于 使 用 的接口     。   这 些 库   数 往往 比             程序     员 自己 实现 的     同样              功能 的 函数 更 合理  、和更 高      ， 更
                                                                                                                   且效
    健壮 。因 为 它们 经 受 住了真实                          网 络 环境 下 的 高  压 测试 ，以 及 时 间 的 考验         。
           种     框架    库  的   实现 原理 基本 相似                   ，      要 么以            模式 实现    ，  要 人          Proactor
        各                                                                                            以             模
    陈 实现 ，要 么同时    以 这        两  种    模式 实现               。举 例 来     ，         于                                 库包含
                                                                      说    基 Reactor         的                 IO 框架
    如 下 几 个   组    件:     句柄  (Handle)、               事 件 多 路  分 发    器    (EventDemultiplexer)、     事件        处 理 器
    CEventHandler)  和 具体 的 事件 处 理器                      (ConcreteEventHandler)、Reactor。      这 些 组件    的           如
                                                                                                                  关系
    图 12-1
        1. 句柄
               RA            ARTS,     BY IO            事件 、 信 和 号           和 定时 事件 ，统     称为 事件
        VO                    HERE                                                 一                 源。     一   个 事件
    源 通常   和  个 句柄     定 在    一     。                句 柄 的 作用
             一                    起                           是， 当 内 核 检 测 到 就 绪 事件          时，        它 将 通过      来
    通知 应 用 程序这     事件    。    在 Linux REF,                 VO 事件 对应 的 句柄 是 文件 描述                     ，    信和       对
                     一                                                                              符             事件
    应 的 句柄     就  是信号 值   。
                   多路分发器
        2.               事件
              的到来是随机的、           异 步        。           我 们 无法 预知     程序      何    时 收 到 个 客户 连接 请             ，   又
        事件                                  的                                      一                               亦  求
    或 收 到  一 个 暂停信号 。所 以         程序     需要         循环     地 等 待  并    处   理事 件     ， 这 就 是     循环。                循环
                                                                                         事件 在 事件

## Page 237

    章 高
    性能     UO 框架
    第 12        库                                 Libevent “*:* 219

    一般使用       IO       复 用 技术
中，等 待 事件        来           实现 。LO 框架 库 般 将 系统 支持    各 种          复
    一                                             的      IO 用    系统
调用 封装 成 统一 的 接口 ，称 为 事件 多 路 分 发  。 事 件 多 路分发器的 demultiplex     是等待事
    器                                                            方法
    的是        等函数。
件 的 核心 ，其 内 部 调用  select、poll、epoll_wait


ee
+handle_events () :void     +get_handle () -Handle

+remove_handler () :void    ee                    上
<<uSeS>> |        notifies  *



+register_event () :void                                     ee

+demultiplex () :void

                                图                        框架
                                12-1         VO         库组件
        外，事   件 多 路     分   发  器 还 需要 实现                 和                 方法
     此                                       register_event
                                                             remove_event     ，以 供 调 用 者 往
事件 多 路分 发器中 添加 事件 和 从 事件 多 路 分 发器中 删除事件                      。
     3. 事件处理器和 具体 事件处理 器
     事件 处 理     执行 事件对 应的业务 逻辑。它 通常包含                        一 个或 多 个   handle_event 回调  ，
这 些 回 调 函 数 在 事件 循环中 被 执行                    框架 库 提供 的 事件 处 理 器 通常         是 一 个 接口 ，用 户 需
    继承
要       它来实现自己     的            处理器            处           。    ，
                            事件 ，     即      具体 事件 理      器    因 此 事件 处     理 器 中 的 回调 函数 一
般 被 声明 为虚     数           ，以支持   用 户 的 扩展  。
   外，事 件处    理     器        一   般 还 提供 一   个 get_handle 方法    ，它 返回 与 该    事件处  理 器 关联 的 名
     此
柄。 么，事 件处    理             器和 句柄 有 什么 关系 ?   当 事 件 多 路 分 发    器检测 到 有 事件 发    生  时  ，它 是 通
     那
过 句柄 来通知 应 用 程序             的  。 因 此 ，我  们 必须 将 事件 处 理     器   和 句柄 绑     ，才 能 在      发生时获
                                                             定                          事件
取 到 正确  的 事件 处 理 器         。
     4.Reactor
                            库的核心 。    它 提供 的 几 个 主要     方法    是 :
     Reactor 是 /O 框架
        handle_events。     该   方法  执行事件      。  它     如下过程 :     待           ，   后     处理所
                                                                         重复 等
                                                                          事件          然 依次
        有 就绪事件 对 应 的 事件处理 器           。
                                             多路分  发     器
     四 register_handler。         该 方法 调用 事件     的        register_event  方法 来 往事 件 多 路 分 发
        恬 中 注册     一     个  事件 。
                                                                             来 删除  事件  多 路
     Oremove_ handler。该 方法 调用 事件 多 路 分 发 器     的         remove event   方法               分
        发 器中 的     一     个 事件  。

## Page 238

220 “* 第 二篇 深入 解析 高性能 服务 器编程


图 总
  12-2     结   了 VO 框架 库 的 工作 时 序 。
Application        Reactor        EventDemultiplexer    ConcreteEventHandler

                  ()
    register_handler

    一
                                 ()
                        demultiplex
    Wait

                       handle ready


                                          图 12-2 VO      框架库    的 工作 时 序 图

                                             源码
12.2        Libevent                  分析
                                                                    库，    其  学 习 者 和 使用者 众多。
     Libevent     是开源 社区 的                   款 高性能 的TUO 框架                                         使
                                      一                                                            用
                                                                    软件                            浏览
                                                         内 存对 象 缓存
Libevent 的 著名           有    :     高  性      能 的 分 布     式          memcached,     Google          器
                                      本。作 为    个 框架
            的           版                                           具有
Chromium       Linux                         一   LO      库   ，Libevent       如下  特点
     口 FFA              Libevent      支持       Linux.     UNIX 和     Windows.
 统一 事件 源 。Libevent 对 LO 事件 、信 号 和 定时 事件 提供 统一 的 处 理 。
     口 线程 安全                              使
                        。Libevent         用  libeventpthreads   库    来提供 线程    安全  支持。
     口 基于      Reactor      的              。
                                          模式
                                         实现
     这   一  节 中   我 们 将               简单地研究  一 下 Libevent     源 代码 的 主要 部     分 。分 析 它 除了 可以 更 好 地 学
                外 ， 还
                        有如        下 好
习 网 络 编程                           处:
     CD  学 习 编写     一   个 产品 级 的 函数 库 要 考虑               细节         。
                                                         中使用  了 大 量 的 函数
                                                                    指针，    用     C                语言
        提高     C  语言 功底 。Libevent 源码                                实现了多态机
        制 ， 并 提供 了          些 基础 数据 结构 的 高               效    实     ，比 如 双向 链表   、      最
                        一                                       现        小    堆        等。
                        网站是                                          其
     Libevent    的 官方       http:Wlibevent.org/，                中   提供    Libevent HATA FR,     UREA
            框架库的第一手文档，                       并 且 源   码    和 文档 的 更   新也 较为 频繁    。 笔 者写作 此 书 时 使 用 的
Libevent
                                                 月3  日     发 布的2.0.19。
Libevent    版  本 是 该 网站 于     2012    年 5

12.2.1

     分 析    一    软件 的源代     码     ，   最 简单           效 的 方式有   是   从 使  用人手，   这 样 才能 从   整体上 把  该 软
件 逻辑结构           。代 码 清单                  是 使    用       库     实 现      一      World”      程
的                           12-1                 Libevent            的      个“Hello       序。
                                             代码  清单      12-1 Libevent 实例
     #include     <sys/signal.h>
     finclude    <event.h>

## Page 239

库Libevent f+ 221
第 12 章 高 性 能 框架


void      signal_cb( int     fd,      short  event,         void*     arge )
         {
          Struct   event_base*        base   =  (   event_base*           )argc;
          struct   timeval   delay     =  {    2,   0    };
    printf£( "Caught an interrupt signal; exiting cleanly in two seconds...\n" );
    event_base_ loopexit( base, &delay );
 }
void      timeout_cb( int      fd,    short          event,  void*        arge     )
         {
          print£( “timeout\n"         );
         }

int       main()
         {
          struct   event_base*     base     =   event

          struct   event*    signal_event         =  evsignal_new({       base,            SIGINT, signal_cb,    base  );
          event_add(                             NULL    );

          timeval  tv    = { 1,   0   };
          struct   event*           timeout_event   =  evtimer_new(       base,            timeout_cb,    NULL );
          event_add(     timeout_event,           &tv    );

          event_base_dispatch(         base      );

                           timeout_event         );
          event _free(     signal _event       );
          event_base     free(  base     );
         I
一 一

代码        清单                     ，                               了                         主要
    12-1 虽然 简单 但 却 基 本 上 描述 Libevent 库 的 逻辑 ，
    象。一
    个event_base 相当
    于一个Reactor 实例
1 调用 event_init              数 创建     event_base 对                                                                     。
                       事件 并 设置 它们 的                                                  Reactor     实例            new     和
2         创建具体 的               处理器，                                   所从属                      。evsignal_
                             信号       事件    处   理    器 和 定时 事件            处        理     ， 它 们 是        在
evtimer_new   分 别 用 于 创建                                                             器         定义         include/event2/
               中的宏:
eventh 文件

    evsignal_new(b, x, cb, arg) \
    event_new((b), (x}, EV_SIGNAL|EV_PERSIST, (cb), {arg))
          #define  evtimer   new(b,    cb,     arg)          event        new((b),     -1,     0, (cb),   (arg))
          见，               和信口是                                       用                通用             理    图            的
可 它们 的 统一                        event_new           函数 ， 即               于 创建                 事件 处     器       〈 12-1 中
EventHandler)    的           。其 定义    是:
          Struct  event* event_new(struct          event_base*        base,    evutil_socket_t        fd,  short events,
    void (*cb) (evutil_socket_t, short, void* , void* arg)
                                          事件              理      从                                             Ab
JEP,      base     参数 指定 新 创建 的                       处      器        属 的  Reactor.        fd                       SHAE
的句柄。创
              建  VO                      事件
关联                                     处理器时    ，应 该       给  fd       参数 传递        文件     描述 符    值 ，   创 建信号 事件 处      理
ark},         fd 参数     传递              ，               清单                    的                              处
                             信号       值    比   如   代码                 12-1 中       SIGINT;     创建 定时 事件        理 器 时   ，

## Page 240

 222          第 二  篇    深入 解析 高         性    能服务 器             编程

 则 应 该给    fd 参数 传递           -1。events                    指定                                       定义
                                         参数                              事件 类    型   ，其 可 选 值 都          在 include/event2/event.h
     中，如 代码 清单                     12-2 Aras.
 文件

                                           代码 清单           12-2      Libevent          的        类型
                                                                                 支持
                                                                                       事件

           idefine     EVTIMEOUT                       OxOl    /*            定时 事件   +/
           #define     EV_READ                         0x02    /*   可 读 事件           */
           idefine     EV_WRITE                        0x04    /*   可 写         事件   */
           #define     EV SIGNAL                       0x0     /*   信号 事件            */
           #define     EV_PERSIST                      0x10    /*   永久 事件            */
           /* 按   沿       球 ， 要  用                                          ，       如
                                   件    需 TAO 复 系统 调用支持 比                            epoll      */
           #define     EV ET                           0x20

     代码 清单     12-2 42,            EV_PERSIST 的            作用 是         :                           8        event          调用
 event_add    下    〈   见 后         文 )。
                                                                                                  事件                         方
     cb                          参数 指定 目标 事件对 应    的  回调 函数     ，   相 当 于 图            12-1     中        处理器的handle event
 法   。arg 参数 则 是       Reactor
     event_new         BX               HY                 4 event 类 型 的 对               ，  也       是                      理 。
                                                      —                                象                   就 Libevent 的 事件 处 器
                                                           处理器，           而 不       是 事件    ，   会 使 读者     觉得 有 些  混 乱，  故 而 我
 Libevent  用 单词“event” 来 描述 事件

  事件 指 的 是 个 句柄 上 绑 定 的 事件 ， 比如 文件 描述 符0 上 的
                          一                                                                              可读 事件 。
              处理器，                       是             结构
     L)   事件                  也 就               event      体类           的            象     除                               个要素
                                                                    型
                                                                                 对                         ， 了 包含 事件 必须 具备 的 两
                                      和事件类型) 外，    还   有 很     多 其他 成            ，  比 如 回调
          《句柄                                                               员                       数。
     由事件 多 路 分 发 器 管理 ，事件处理 器 则 由 事件 队列 管理 。事 件 队 列 包 括 多 种 ，比
OC) 事件
     及 evmap 中
          如 event_base              中 的 注册 事件 队列                、活 动 事件 队列 和 通用 定时 器 队列                   ，以
          的 IO 事件         队列          、 信 号 事件 队列          。    关 于 这 些 事件 队列                   ， 我们 将 在 后 文   依次    讨论。
     OC)        事件 循环 对   一         个 被 激活 事件              〈   就 绪 事件)     的 处         理 ，  指   的   是 执行 该    事件对 应  的 事件处   理
                          函数。
                               中 的 回调
     3 调用 event_add 曙                    ，将 事件                 处理器 添加 到 注册 事件 队列                      中  ，并 将 该 事件   处 理 器 对 应
 的 事件     添加到 事件 多 路 分 发 器                            。event_add    函数 相当于                          中
                                              中                                          Reactor      的  register_handler 77}.
     4 调用        base                                      执行           循环
                                          event
                                       dispatch            来        事件               ，
     5 事件 循环           结束            后  ，   使用*free        系列           来        释放    系统 资源        。
          此可见，代           码 清单 12-1      给 我 们 提供 了                     条 分 析          Libevent     源      的     。
     由                                                                                                代码
                                                                    一                                           主线   不 过 在 此 之
 前 ，我们先 简单 介绍                 一    下    Libevent       源 代 码    的  组织               。
 12.2.2       源 代 码       组        织 结构

               代码 的 目录 和 文件 按照 功能 可 划分 为 下 部 分                                                      :
     Libevent 源               中                                                        如
                                                                    A     Libevent       主      版   本 升级
                                                                                                          到2.0 之 后 引入
     加 头 文件 目录 include/event2.                                 HAA JE                                     的，                 在
              及                                                                                                             提供
          1.4 更老 的 版                   本中 并  无         此 目录     。该  月    录 中 的         头 文件       是  Libevent           给应用程序使

## Page 241

                                                      第 12 章            高性能            框架
                                                                                               VO  库Libevent      223

        的           如                                                                          文件
      用 ，比     ，event.h     头 文件 提供 核心 函数                    ，httph      头         提供     HTTP     协议 相关 服务         ，
      rpc.h 头   文件 提供 远程过程 调用 支持                  。
QC)   源码 根 目 录 下 的 头 文件     。             这 些 头 文件 分 为 两     类           类 是 对         include/event2             下的部
                                                                 :      一                                          目录
          的包装 ，    另 外                    类 是供 Libevent                            性 内 部 使 用 的 辅助  头 文件 ，      它 的 文件
      分 头 文件      一                                                                    们                           名都
      具有 *-internal.h     的 形式     。
QD    通用 数据 结构 目录     compatsys。              该     目录    下 仅 有  一    个     文件 一   一    queue.h。它 封装 了             平台
      的 基础 数据 结构 ，包 括 单 向链表                       、双 向    链表   、      列                循环
                                                                队 、尾 队列 和              队列     。
      sample 目录。它 提供     一    些示例程序               。
Otest 目录。它 提供      一     些 测试 代码             。
口     WIN32-Code 目录。    它 提供 Windows                      平台 上 的 一    些 专用 代码          。
口         文件 该 文件 实现              Libevent                            =                和                            两
      eventc              ，                                               event        event_base               个  结构
      体 的               操作。
   -                     相关
加 devpoll.c、kqueue.c、evport.c、selectc、win32selectc、pollc 和                                         epoll.c 文件 。 它 们 分
      别 了 下                               制，
 封装
   如 VO 复 用 机 /dewpoll、kqueue、event ports. POSIX select, Windows
      select.   poll     和 epoll。这 些 文件的 主要 内 容 相                似  ，都是    针对 结构 体     eventop〈               见 后 文 )
      所 定义 的 接口     的具体                   实现。
O minheap-internal.h                      文件 。该 文件 实现了  一  个 时 间 堆        以 提供 对 定时 事件 的 支持     。
Q) signal.c 文件     。                 它 提供 对 信和号 的 支持 。    内                            体
                                                     其 容 也 是 针对 结构                     eventop           所 定义 的  接口 函
      数的具体 实现    。
O     evmap.c   文件 。它 维护 句柄                       描述符或信号)               与 事件 处 理 器     的     映射    关系     。
口 event_tagging.c 文件 。它 提供 往                      缓  冲区 中  添加 标记          数据     〈 比 如     个 整数            )，       以
                                                                                       一                          及从组
      MEDS fe     TE AY
      event_iocp.c     文件 。它 提供对            Windows IOCP        (Input/Output Completion Port，                  输 人 输
      出 完成 端口) 的 支持     。
Q) buffer*.c 文件 。    它 提供
          对网络                                              VO 缓冲
                                                      的       控制 ，  包
                                                                        括     输入       输出 数据 过 滤                   ，传
                                                                          ，                                       输速率
      限制 ， 使用SSL (Secure                  Sockets Layer)   协议对 应 用 数据              进行    保护 ，以 及 零                贝文件
      传输 等。
LJ evthread*.c     文件 。它 提供 对 多 线程的支持                 。
Wilistenerec 文件 。它 封装 了 对 监听                  socket        的 操作 ，        包 括 监听   连接    和 接受    连接    。
          是     Libevent 的 日 志 系统                     。
Ch logs.c  文件 。它
(J    evutil.c. evutil_rand.c.     strlepy.c                和 arc4random.c 文件  。它们 提供     些 基 本 操作                ， 如
                                                                                       一                            比
                              取    socket 地 址 信息     、读   取 文    、  设
      生成 随机                  、获                                           置  socket     属性
                  数                                              件                     等。
J         和                               文件 。        们
      evdns.c, http.c     evrpe.c             它      分 别提 供了 对            DNS          协议     、HTTP     Hi Al     RPC
                                                                 的    支持    。
      (Remote Procedure Call，             远 程 过 程 调 用 )                    协议
                                             未见使用。
Cd    epoll_sub.c                          文件。该 文件
在 整个源码中                                                                                和     evmap.c            等 4 个

## Page 242

 224   «+    第 二   篇”深入解析     高 性 能   服务 器 编程

       最为重要。             它们 定义
                           了    event
 文件                                   和        结构
                                      event_base          体，   并 实现了 这 两 个结构 体 的相关     操作    。
 下 面 的 讨论 也 主要 是围绕 这 几 个 文件展开         的     。

                             体
 12.2.3               event 结构

               文提到 ，Libevent
                 前         中的事件处理器是     event            结构 类    型 。event 结构 体 封装了 句柄、      事件
型、      回
 调                 数   ，以 及 其 他 必要 的    标志 和 数据    。     该结构 体 在 include/event2/event_struct.h
 类                                                                                           文
RE
 {FP

      Struct event
      {
             TAILQ ENTRY (event)  ev_active_next;
                                  ev_next;
             union  {
                   TAILQ_ENTRY (event) ev_next_with_common_timeout;
                   int min_heap_  idx;
          }     ev_timeout  pos;
          evutil    socket_t  ev_fd;

          struct    event_base*   ev_base;

          union    {
               struct    {
                       TAILQ ENTRY (event) ev_io next;
                       struct timeval     ev timeout;
               }   ev_io;

               struct    {
                       TAILQ_ENTRY (event) ev_signal_ next;
                       short  ev_ncalls;
                       short  *ev_pncalls;
               }   ev_signal;
          }    _ev;

          short    ev_events;
          short    ev_res;
          short    ev_flags;
          ev_uint8七 ev_pri;
                      t   ev_closure;
          ev_uint8
          struct    timeval  ev_timeout;

          void     {*ev_callback)   (evutil_socket_t,   short, void *arg);
          void     *ev_arg;
      }e

              下 面 我 们 详细 介绍 event 结构 体 中 的 每 个      成员  :
      口 ev_events。     它 代表 事件 类      型    。其 取 值 可以     是代码 清单    12-2 所 示 的 标志 的 按 位或〈     互
  斥 的 事件 类 型 除外，比如 读 写 事件和 信号 事件 就不能 同时 被 设置)。
     有已经注册 的 事件处理 器 〈 包
        ev_next。    所                                   括VO    事件 处 理器和 信号 事件处    理 器)      通过

## Page 243

                                                                                                   章 高
                                                                                                    性能 VO 框架
                                                                          第 12                             库    Libevent          “+   225

            该 成员串 联 成 一 个尾队列 ，我                         们  称 之 为                   注册     事件 队列。
                                                                                                       宏   TAILQ_ENTRY              是 尾 队列
                      类 ，    它 定义 在                                                   文件
            中 的 节点       型                   compat/sysyqueue.h                                中，
         #define  TAILQ_ENTRY(type)              \
         Struct {     \
                 Struct  type*  tqe          next;   \     /*                下 一 个 元  素    */
                 struct  type**   tqe    prev;       \     /*     前 一 个元素 的 地址                 “7
         }
            ev_active_next.                             SEF               a8 i        TARA                                        PR, FRAT
            活动 事件 队列。活 动 事件 队列 不                             止     一   个  ，  不 同 优先 级            的     事件 处   理   器 被  激活       后 将 被  捅 人
            不 同 的 活动     事件    队列        中   。 在 事件 循环                    中，Reactor            将 按    优先  级   从 高 到 低             所   有 活动
            事件 队列 ，并 依次      处     理         其   中 的     事件 处 理 器            。
                                                           体，     它 仅        用      于             定时 事件 处 理   。   为    讨论
            ev_timeout_pos。       这 是    一 个联合                                                             器        了        的    方便   ， 后
            面 我 们 称 定时 事件           处 理 器 为    定时 。                老 版             本  的                中，
                                                             器                            Libevent         定 时 器 都 是       由    时间 堆 来 管
                 的。但    开发    者 认为    有  时 候 使          用  简单 链表 来 管理 定时 器 将                               具有     更 高的 效率           。  因
            理                                                        的                                                                此，
                 版本的                         了所谓                                          器”的           。
            新           Libevent     就 引入 “ 通 用 定时                                                                  概念 这 些 定时 器 不 是 存储 在 时
            间 堆 中，而 是 存储 在 尾 队列                         中， 我 们 称 之 为                      通用          定时 器 队列   。 对 于 通用 定时 器 而 言      ，
                                         体    ev_next_with                                              成 指 了 该 定时 器 在                通用
            ev_timeout_pos 联合            的                                common timeout                  员     出
            定时 器 队列 中 的 位                。对 于 其 他 定时 器 而                                          ，ev_timeout
                                    置                                                 言                   pos   联合 体   的  min_heap     idx
            成员指 出 了 该定时 器                在时 间 堆 中 的 位                  置  。一                              个 定时 器 是 否 是 通用 定时 器 取决 于 其 超时
                大小，           具 体 判 断          原则请读者 自己 参考                                文件
            值                                                                                    中的    is common             timeout 函数
                                                                       event.c                                                        。
                   这 是   个 联合                                                                              事件                        evev_
            _ev。         一                  体。 所 有 具有 相同                     文件 描述 符 值                 的VO     处理器通过
                             员串 联 成          个尾队列 ，我们 称 之 为          VO 事件 队列                              所                     有 具有 相同
            io.cv_io_next 成                  一                                                                                         信和号
            值 的 信号 事件处理 器 通过                 ev.ev_signal.ev_signal_next 员                                 一           尾          ，     们称
        成 串 联 成 个 队 列 我
        队列 。ev.ev_signal.ev_ncalls 成 员 指定 信号 事件 发生时 ，Reactor
            之 为 信 叶 事件                                                                                                              fy eth
            行 多 少 次     该 事件对 应 的 事件处                    理 器 中                   的 回调              。ev.ev_signal.ev_ pncalls         成员要
                                                                                               数                                      指针
                是
            么     NULL，                                要 么 指向 ev.ev_signal.ev_ncalls.
         在 程序        中，              我 们 可 能 针对 同一 个    socket     30  4                           BY i /                             EA
               器 《它们拥有             不 同 的 回调 函数    )。当 该           文件   描述             符         上 有 可 读 / 可 写 事件 发     生    时 ，    所 有 这
    理                                                                                                                                    些
    事件处理 器 都应该 被             处              。所
                             理               以     ，Libevent      使       用       VO      事件 队列 将          具有 相同 文件               符值 的 事
    件 处 理 器 组 织 在 一         。这
                            起    样，      当    一     个 文件 描述               符  上 有 事件 发              生   时，  事 件 多 路 分 发        器   就 能 很  快
        也是由于 相同
地 把所有 相关 的 事件处理 器 添加 到 活动 事件 队列 。信和号 事件 队列 的存在
                                                                               中                                                        的原
    A.   WL,     VO   事件 队列 和 信号 事件队列 并 不 是 注册 事件 队列 的 细致 分                                                类    ，   而 是 另 有 用 处       。
         Chev fd.        VO    事件
        处理器，它 是 文件 描述 符 对 于信和号 事件处理 ，它 是 信号
                                                                                               值                       器                值。
                      该      理             从                                    属 event_base 实例
            ev_base。        事件处              的                                                。
         口  evy_res。   它记录 当前 激活 事件                  的    类 型     。
                             一 事 件 标志。其                           选 值定义
                                                                                          在                                           文件
         Jev_fags。      它 是     些                          可                                   include/event2/event_struct.h          中:
   fdefine EVLIST_TIMEOUT 0x01 /* 车 件 处 理器从 属于通用定时 器 队列 或 时间 */

## Page 244

 226 **       第 二    篇   深入 解析 高 性   能   服务 器 编程

     #define    EVLIST   INSERTED    0x02      /*     事件 处 理   器  从 属于注册 事件 队列
     4define    EVLIST_SIGNAL     0x04         /*    没有 使 用    */
     #define    EVLIST_ACTIVE     0x08         /*    事件 处 理    器  从 属于 活动事件 队列 *7
     #define    EVLIST     INTERNAL     0x10       /*     内 部 使用 */
     #define    EVLIST_INIT     0x80      /*   事件 处      理   器 已 经 被 初始化*/
     #define    EVLIST_ALL      (Oxf000        Ox9f)                   定义
                          |                              /*      所有    标志  “/
     Jev pri。它 指          件处理 器 优先                    级   ，值   越小 则 优先 级 越   高。
     口 ev_closure。                  它 指定 event_base  执行   事件处    理  器的  回调 函数 时 的 行为         。    其 可 选 值
文件
         于 event-internal.h     中:
     /*      默认   行为  “/
     #define    EV_CLOSURE_ NONE     0
     /*  执行 信号 事件处理 器 的 回调 函数                时 ，调    用        signal.ev   necalls  次  回调函  数    */
     #define    EV_CLOSURE   SIGNRAL  1
     /*       行   完  回调函数  后 ，再 次    将 事件    处 理器    加 入 注册    事件队列 中  */
     #define    EV_CLOSURE_PERSIST     2
                          器有效，                           指 定 定时 器 的 超时 值    。
         ev_timeout。      它仅对定时
     gev_callback，        它 是 事件 处理器 的 回调 函数 ，由          event_base 调用                     。    回 调 函 数   被 调
              时     ，它
         用              的3 个 参数 分 别 被 传人事件 处 理 器 的       如下     3     个 成    员     :           ev_fd、ev_res 和
         ev_arg.
     Oev arg，         回  调   数 的 参数    。
 12.2.4       往 注 册 事 件 队列 中                   添加
                                                      事件 处 理 器
         面提到，创 建          一  个  event
                          对                        的 函数
                                                         是    event                (及
                                                                            其变体)，    它
     前                                                                                     在               文件
                                                         new                                   event.c      中
 实现。 该          数   的 实现   相当 简单     ，  主 要 是 给          event 对 象  分 配 内 存 并初始     化 它 的 部 分 成           ， 此
                                                                                                       员    因
 我们 不           它。event                            ，      需要调用
         讨论               对 象 创建好 之                后  应 用 程序     event_add                   函数 将 其 添加 到 注册 事
 件 队列 ，并 将 对 应 的 事件 注册 到 事件 多 路 分                        发     上        在                                  文件
         中                                                      器   。event  add        event.c         中  实现，
 主要 是 调用 另外           一  个 内 部 函数     event    add_internal，    如 代码    清单   12-3 所 示 。
     代码 清单12-3 event_add_internal 函数
 Static inline int event_add_internal(struct event *ev, const struct timeval
     int tv_is_absolute)
 {
              Struct     event_base    “base   -     ev->ev_base;
              int   res  = 0;
              int   notify = 0;

              EVENT BASE     ASSERT    LOCKED (base) ;
                                                setup (ev);
              _@vent_debug_

              event_debug((
                      "“event_add:  event: tp         (fd td),   %tsts%scall
                      ev,
                      (int)ev->ev_fd,
                      ev->ev_events    & EV_READ         ? “EV_READ "  :  9

## Page 245

 第 章 能 库
   12 高 性 VO 框架 Libevent f+ 227


                                                     & EV_WRITE   ? "EV  WRITE " :   " ",
                         tv     ?      "EV_TIMEOUT        "  :  " ",
                         ev->ev_callback));

              EVUTIL_ASSERT(! (ev->ev_flags            &     ~EVLIST_ALL));

              /*                                                                            RAY,      HB
 在 时 间 堆 上    预     留 一  个  位    置     */
              if     (tv    !=         NULL          !(ev->ev_flags & EVLIST_TIMEOUT))    {
                                    if (min_heap_reserve     (&base->timeheap,
                                       1     +     min_heap   size(sbase->timeheap))   == -1
                                       return         (-1);
              }

              /*

 4%  event     结构 体         ev_ncalls的               # ev_pncalls RA) */
     #ifndef      _EVENT_DISABLE_THREAD_SUPPORT
           if     (base->current_event                 ==     ev  &&  (ev->ev_events & EV_SIGNAL)
                        &&      !EVBASE_IN_THREAD(base))     {
                        ++base->current_event_waiters;
           )            EVTHREAD_COND_WAIT (base->current_event_cond,                     base->th_base_ lock);
     #endif

     if ((ev->ev_events & (EV_READ|EV_WRITE|EV_SIGNAL)) &&
     ‘(ev->ev_ flags & (EVLIST_INSERTED|EVLIST_ACTIVE))) {
                        if      (ev->ev_events         &        )
                                                       SEH     1/0                     “/
res                                            =  evmap_ioc_add(base, ev->ev_fd,     ev);
                         else          if      (ev->ev_events     &   EV_SIGNAL)
                                       *                                             “/
                                       res     =   evmap_Signal_add(base, (int)ev->ev_fd,   ev);
                         if              (res  !=  -1
                                       /*              */
                                                       (base,                EVLIST_INSERTED)
                                                       ev,                                  ;
                         if              (res ==   1 [
                                       /*              PHT RAKE,                           */
                                       notify        = 1;
                                       res     =   0;
                         )
         )

           /*     下   面将事 伟处理器                         至 通用 定时 颖 队列 或 时间  中    。对 于 信和 号  事件处理 和              B
 据 evmap_*_add        函数 的 结果 决定                  是  否   添加〔 这 是 为    了给 事件 设置 超时)， 而 对 于  器，      添加之
                                                                                          定时 则 始终 应 该     */
         if     (res !=         -1         &&     tv   '= NULL)   {
                      Struct           timeval         now;
                      int                   common_timeout;

                                                       ALS,     eR IM                                  ag
 在 变量  ev->ev_io_timeout                          ev_io      是    定义在    event-internal.h X#PME:     #define
 io_timeout       —_ev.ev_io.ev_timeout                */                                                   ev_
     if (ev->ev_closure == EV_CLOSURE_PERSIST && !tv_is_ absolute)

## Page 246

    篇  性 服务
    228 第 二 深入 解析 高 能 器 编程



              ev->ev_io_ timeout    = *tv;

        /* 如 果 该 事件 处理 已 经 被插入 通用 定时 器 队列 成时间 堆 中，则 先 剧 除它 */
        if (ev->ev_flaqgs & EVLIST_TIMEOUT) {
        if (min_heap elt_is top(ev))
        notify = 1;
        (base, ev, EWLIST_TIMEOUT);
        event_queue_remove
        )

              /*                                                  DBR
    共 调 函 数 被 执行 。对 于 信号事件 处 理   ，必 要 时 还 需 其ncalls    O CAR,     ev_pncalls 如
                           器                                      果不为
NULL， 它 指向 ncalls)。 前 面 提 到，信 号车件 被 触发 时 ，ncalls 指定其回调函数 被 执行 的 次 数。将 ncalls 设
   为 0，可 以干净 地 终止 信号 事件 的 处理 “/
              if  ((ev->ev_flags &    EVLIST_ACTIVE) &&
                    (ev->ev_res  &  EV_TIMEOUT)) {
                    if (ev->ev_events   & EV_SIGNAL)  [
                       if (ev->ev_ncalls  &&     ev->ev_pncalls) {
                           *ev->ev_pncalls     =     0;
         ，             }
                    )

                                        (base, ev, EVLIST_ACTIVE);
              }




                                     = is_common_timeout(tv,      base);
             if       (tv_is_absolute)     {
                        ev->ev_timeout     =<  *tv;
                                                                  时间    “7
             /*         断 应 该 将定时 器插入通用 定时     队列 ，还 是 插入
             }     else     if (common_timeout)    [
                        struct   timeval   tmp   = *tv;
                        tmp.tv_usec    &=  MICROSECONDS_MASK;
                        evutil   timeradd(é&now,   &tmp, sev->ev_timeout);
                        ev->ev_timeout.tv_usec     |=
                                  (tv->tv_usec &   ~MICROSECONDS  MASK);
             }     else     {
                                        加 上 当前
                        /*                 系统时间，以 取得定时   器 超时 的   绝对 时  间
                        evutil_timeradd(snow,     tv,  sev->ev_timeout);
             )

                              ( (
             event_debug
                                      timeout     in td  seconds, call   tp",
                         (int)tv->tv_sec,      ev->ev_callback));

                                                                                  最
                                               ev,   EVLIST_TIMEOUT);      /*     后 搬入 定时 加 */
             /*                                                                    common_timeout_
    schedule 函数 将 其转移 到  时间 准    。这
                             中       样，通 用 定时 器 链表 和 时 间 堆 中 的    定时   器 就得到   了  统一 的 处理 “/
             if       (common_timeout)     {
                         struct   common_timeout_list     *ctl   =

                         if  (ev  == TAILQ_FIRST(éctl->events))        {
                             common_timeout_schedule(ctl,         Snow,  ev);

## Page 247

    章 高 性能IO 框架
    第 12    库  Libevent 229


                     }
          )       else   {
                      if     (min_heap_elt_is   top(ev))
                           notify =      1;

     }

      /*     WRLE,                */
      if     (res  !=  -1     &&  notify &&@                    (base) )
                                               EVBASE_NEED_NOTIFY
                               notify base (base) ;
                evthread

      _event_debug_note_addi(ev);

      return      (res);
    }

                          见                    函数
    从 代码     清单 12-3  可         ，event_add_internal    内部     调用 了 几 个  的     :
        重要 函数
       io add。 该 函数将VO 事件 添加到 事件 多 路 分 发 器 中，并 将对 应 的 事件 处 理 器
  evmap
         到        事件 队列 ，同 时 建立    事件
     添加     VO                                 VO      和 IO     事件    处理 器 之 间 的 映射 关系  。 们
                        中                                                                 我
     将 在 下   一  节 详 细讨 论该         函数 。
     evmap_signal_add。 该 函数 将 信号 事件 添加 到 事件 多 路 分                 发 器     中    ，并 将 对 应 的 事
     件 处理器 添加 到 信和号 事件队列                       中，同 时  建立   信号   事件     和 信号 事件 处 理 器 之间 的 映
          关系。
     射
                                          该 函数 将 事件  处理 器     添加到 各 种  事件队列 中 :     将 IO 事件
     处 理    器和 信号 事件          处   理 器插   人 注册 事件 队列        ;  将  定时    器 揪 人 通用 定时 器 队列 或 时
     间 堆     将 被                    激活 的 事件 处 理 器 添加 到 活动事件      队列    。 其 如     清单
                                                                             实现
        ，                                                         中        代码          12-4
       示。
     所

        代码 清单12-4 event_queue_insert 函数
static void event_queve_insert(struct event_base *base struct event
        int queue)

                                 ASSERT_LOCKED (base) ;
                /*  避免重复 插入        */
        if (ev->ev_flags & queue) {
        /* Double insertion is possible for active events */
        if (queue & EVLIST_ACTIVE)
        return;

                                           "%s:  tp(fd %d)    already on queve @x", —func_,
                                           ev,  ev->ev_fd,    queue);
                       return;
                )

        if (~ev->ev_flags & EVLIST_INTERNAL)
        base->event_count++; /* 将 event_base 拥有 的 事件 处 理 器总数 加 1 */

                         人                     /*    标记 此 事件 已 被 添加过
                ev->ev
                              ags |= queve;                       */

## Page 248

    230 *s* 第 二篇 深入解析 高 性能服务 器编程


                            switch    (queue)      {
                            /*    将 I/O 事 件 处理器 或 信号 事件处理 器 播入注册 事件 队列        “7
                            case    EVLIST_ INSERTED:
                                                                                  ev,  ev_next);
                                             break;
                            /*    将 就绪事 件 处 理 器 人 活动     事件  队列 */
        case EVLIST_ACTIVE:
        base->event_count_activet++;
        TALLQ_INSERT_TAIL (Sbase->activequeues[(ev->ev_pri],
        ev,@v_active_next);
        break;
        /* 将 定时 器 插入通用 定时 器 队列 或时间 “7
        case EVLIST_TIMEOUT: {
        if (is_common_timeout (4ev->ev_timeout, base)) {
        “struct common_timeout_list *ctl =
        list (base, 4&ev->ev_ timeout);
        insert_common_timeout_inorder(ctl, ev);
        i else
        min_heap_push(&base->timeheap, ev);
        break;
        }
        default:
        event_errx(1, "%s: unknown queue @x", —func__, queue);
        }
    }

             “往事                             分     器
    12.2.5                  件 多 路              发 中 注册 事件
         event_queue_insert                  所做 的 仅仅   是 将   个  事件 处 理 器加入    event_base 的 某         个 事件
                                                        一                                              队列
                            的  IO 事件处理器和 信号 事件处理        器    ，我们  还 需要 让 事件 多 路 分 发                器 来 监听
    中。对 于 新 添加
    其 对 应    的 事件 ，同 时 建立 文件 描述                       、信    和号 值 与 事件处理 器 之 间 的 映射 关系    。       这 就 要 通过
                                         符                                                              调
                                                   add 两
                                                        个函数
    用 evmap_io_add 和 evmap_signal_                      来完成 。    这 两  个                   相当 于 事件 多 路 分发器
    的                                 ，                 。                              ，               一下
    中    register     event    方法 它们 由             evmap.c   文件 实现 不 过 在讨论 它们 之        前 我 们         先 介绍
它们 将 用 到 的 一 些 重要 数据 结构，如 代码 清单 12-5 所 示 。
       代码 清单12-5 evmap_io. event_io_map 和 evmap_signal, evmap_signal_map
    #ifdef BEVMAP USE_HT
    #include "ht-internal.h"
    struct event_map_entry;
                                                        map                              event_map_ entry
         /*                 EVMAP_USE_HT,          WH event_io
    对 象和      事件 队列 〈                 前      ，具 有 同样 文件描述    值的    事件                          之 的
   I/O                                  文               处理 构成                 I/O    事件队列 )     间      关系，
      上也就是存              了  文件描述特和             事件处理 器 之间 的 映射     */
    实际
                                      map,   event_map entry);
         HT_HEAD(event_io_
         felse        /*   @i)                 map # FHM        map—#
                                event_io_               event signal           */
         #define                             event_signal_map
         #endif

                                                                                                     信号 数
         /*        下面 这 个   结构   体  中 的 entries 数组 成   员    存  信 号 值和信号 车 件 处理器 之 问  的 卫       〔 用  值索引
    Mlentries         得 到 对 应    的  信号 事件 处 理 )     */

## Page 249

章 高 性 能 1O 框架
第 12    库    Libevent f+ 231


|   struct   event_signal_map             {
    void **entries; /* 用 于 存放 evmap_ io 或evmap signal 的 数组 */
                  int   nentries;         /*   entries         的大小
                                                           数组     */
     be

             定        EVMAP_USE_NT,                event 中 的 成 员 有 如 下 类 型                   «/
    /*  如 果       义了                                       ic     map         具
    Struct   event_map          entry    |
                                (event                      map_nede;
                  HIT_ENTRY
                  evutil_socket_t         fd;
                  union       {
                                struct     evmap_io         evmap_    io;
                  }     ent;
    Ve

    /*  event_list              £tievent 组 成 的 尾 队列  ，    前 面   讨论    的所 有  事件队列 都 是这 种 类型   “/
    TAILQ  HEAD (event_list,              event);

    /*         BFA (MWR, evmap                   io.events      t#    1/0   事件 队列) wy/
    struct   evmap_io         |
                  struct        event_list     events;
                         ev_uintl6_t  nread;
                         ev_uintl6_t  nwrite;
    ] ;

                                地说，evmap_signal            ,events    才 是   事件队列)
    /*  信号事件 队列 【确切                                              信号             */
    struct   evmap_signal          {
                  Struct        event_list     events;
    bi
   一

    由 于                                                          个                           ，      们
    evmap_io_add 和 evmap_signal_add 两 函数 的 逻辑 基本 相同 因 此 我 仅讨 论
           add                        清单
   evmap_io 函数                ， 如 代码      12-6     所 示    。

                                             代码 清单12-6           io         函数
                                                   evmap
   一 ”一                                                          _add
                                                                 一
    int evmap_io add(struct                 event_base     “base,     evutil_socket_t fd,    struct event wev)
    {
                  /*                      HEM1/O MAMMA           */
                  const       struct  eventop *evsel         =    base->evsel;
                  /*    获得                中 文件  描述     符     与     事件    队列
                              event_base                   1/0               的映射   表  哈 希 表  或 数组)  */
                  struct      event_io_map       *io = &base->io;
                  /*                     1/0 BEAR     */
                  Struct      evmap_io     *ctx  =   NULL;
                  int   nread,  nwrite,        retval     =  0;
                  short     res =     0,  old  = 0;
                  struct      event   *old_ev;

                  EVUTIL                  ==     ev->ev_fd);

                  if     (fd  < 0
                                  return     0;

   #ifndef   EVMAP_USE_HT

## Page 250

篇 解析 性
232  二 深入 高 能 服务 器 编程


          /*     1/0 事件队列数组               io.entries    中，     小 文件描述 特 占 用 一   。     如
                                                                                项     果  fd 大 于 当前    的 大
小 ，则 增加 数组 的 大小《扩大 后 的 数组 的 容量要 太 于                     */
    if (fd >= io->nentries) [
    if (evmap_make_space(io, fd, sizeof(struct evmap_io * == -1
    return (-1);
    )
 fendif
    是否被定义而有不同的实现，但 目的
           /*                           EVMRP_USE_HT                                          都是创建ctx，  在
             和ecx  未 加 映射
射表 ic        中为fd            关系      w/
          GET_IO_SLOT_AND            CITOR (ctx， io,    fd, evmap io, evmap io  init, evsel->fdinfo len);

=                       ctx->nread;
          nwrite     =  ctx->nwrite;

          if     (nread)
                               old   |=   EV_READ;
          if     (nwrite)
                               old   |=   EV_WRITE;

          if     (ev->ev_events           &  EV_READ)     {
                          if       (++nread      ==  1
                                          res    |=   EV_READ;
          }
          if     (ev->ev_events           &  EV WRITE) {
                          if        (++nwrite     ==  1
                                          res     |=  EV_WRITE;
          )
          if     (EVUTIL_UNLIKELY(nread               > Oxffff    || nwrite  >  Oxffff)) {
                        event_warnx("Too          many events       reading  or writing   on  fd  $d",
                            (int) fd);
                        return -1l;
          }
          if     (EVENT_DEBUG_MODE_IS_ON()              &&
                     (old_ev       = TAILQFIRST(&ctx->events))           &&
    (old_ev->ev_events4éEV_ET) != (ev->ev_events&EV_ET)) {
                         event_warnx (“Tried            to mix edge-triggered  and    non-edge-triggered"
                                   " events      on  fd td",   (int) fd);
                         return         -1;
          }

          if     (res)   {
                         void        *extra  =     ((char*)ctx)  +   sizeof(struct    evmap_io);
                         /*        往事件    多 路 分 发   器 中   注册 事件。add
的 后端I/D 复用机  制   ， 这         接 口 函 数 有 不  同 的 实现    。 我   们将 在后 面 讨  论车件 多 路 分 发 器 的  接口    函数 “/
                         if        (evsel->add(base,       ev->ev_fd,
                                             old,    (ev->ev_events   &  EV_ET) |  res,    extra) ==   -1
                                             return     (-1);
                         retval         = 1;
          }

          ctx->nread         =                          nread;
          ctx->nwrite            =   (ev_uintlé_t)      nwrite;

## Page 251

  章 性能 库
  第 12 ”高 VO 框架 Libevent «3+ 233



      将 插
      /* ev 到I/D 事件队列 ctx 的 尾部 。ev_io_mext 是定义在event-internal.h 中
      文件
  fdefine ev_io_next     next
      _ev.ev_io.ev_io        */
      TAILQ                  ev, ev_lo next);

      return   (retval);
  }


                                               体
  12.2.6      eventop                         结构
                 结构体封装了                      LUO                           一          ，                       、         等。
        eventop 复                                      用 机 制 必要 的               些 操作     比 如 注册 事件                         等 待事件 它
                                    ATA JA          VO   复 用 机 人 制       提 供 了        个 统一
  为 event_base     FFM                                                                的               接口 。该 结构
                                                                                一                                体定义在       event-
                              中，        如 代码 清单     12-7 所 示 。
  internal.h 文件

                                                     代码     清单12-7         eventop     体
                                                                                      结构

        struct     eventop           {
                          /*    后     端
                          const         char  *name;
                                          化函数  */
                          /*               初始
                          void       *(*init)       (struct  event_base         *);
                          /*                 */
                          int         (*add) (struct     event_base        *,   evutil_socket_t          fd,  short old,
short                                                 events,  void        *fdinfo);
                          /*   删除事件          */
                          int         (*del) (struct     event_base        *,   evutil_socket_t          fd,  short old,
short                                                 events,  void        *fdinfo);
                          /*  等 待 事件         */
                          int             (*dispatch) (struct  event_base             *,   struct  timeval    *);
                          /*                                             */
                          vold       (*dealloc) (struct        event_base             *);
                          /*            程序调用 fork                                      event_base    */
                          int   need_reinit;
                          /*             复 用 技术     支持 的 一 些 特  性  ，可 选    如   下  3                   EV_PEATURE_ET     边        发
  事件 EV_ET)、EV_FEATURE_O1【〈                                           事件 测算 法 的复杂 度是D(1))                 FDS    仅能
                                                                                                           MEV_FEATURE_ (不
                                                                                                                        监听  socket
  上 的 事件   ， 还 能     听 共 他 类           型   的 文件        符 上 的 事件 )      */
                          enum       event_method feature                features;
                          /*    AH                                 1/0                                        MAA,
                                                                   io      add(Revmap
                                                                                      io           del)     函数
                                                                                                           在调用   eventop    的 add(
                          1/0       复  用机   制 的 事件表 中 。evmap                                                                     或
             时，将     这    段     内 存  的 起   始地 址 作为 第     5   个参数 传递给add〔〈
  del)  方法                                                                            或  del) 方法      。下 面    这个 成员则 指定了 这 上 内 看 的
  长度 */
                          size       t fdinfo       len;
        Ve

                                                                                                         poll.c
        i  3c #2                  devpoll.c. kqueue.c.       evport.c.          select.c,   win32select.c.  和           epoll.c 文件
           对不同的                                     实现                     定义
                                                                                       的这    套     。     么
                                                                                                          ，           多种         用
  分 别针 IO 复 用 技术                                        了 eventop                              接口     那 在 支持 VO 复
        的            上                                            呢                   于这    些                        级
  技术
           系统             ，Libevent      将 选择 使 用             个        ?   这取决                 VO          复 用 技术 的 优先    。Libevent
        的  后
                 端                          及 它们              级   在                   中            ，         清单
  支持             IO       复                               用 技术 的 优先    event.c                 文件
                                                                                      定义 如 代码                    12-8 所 示    。

## Page 252

    篇
    ”深入 性 编程
    234 第 二 解析 高 能 服务 器


                                       的后端    VO 复 用 技术 及 它们 的 优先 级
                 代码 清单12-8  Libevent 支持      ee
    Ee
                         EVENT PORTS
    extern  const struct  eventop  evportops;
    fendif
    #ifdef  _EVENT_HAVE_SELECT
    extern  const struct  eventop  selectops;
    #endif
                   HAVE POLL
    #ifdef  EVENT
    extern  const struct  eventop pollops;
    fendif
    fifdef  EVENT_HAVE_EPOLL
    extern  const struct  eventop  epollops;
    fendif
    #ifdef  EVENT             KQUEUE
    extern  const struct  eventop  kqops;
    #endif
    #ifdef  _EVENT_HAVE_DEVPOLL
    extern  const struct  eventop  devpollops;
    fendif
    #ifdef  WIN32
    extern  const struct  eventop  win32ops;
    fendif

    Static  const struct  eventop  *eventops[] = [
    fifdef
           &evportops,
    fendif
    #ifdef  _EVENT HAVE_WORKING KQUEUE
           &kqops,
    fendif
    ¥ifdef  EVENT_HAVE_EPOLL
           &epollops,
    fendif
                     HAVE DEVPOLL
    #ifdef  EVENT
           &devpollops,
    fendif
    #ifdef
           &pollops,
    #endif
    #ifdef  EVENT_HAVE    SELECT
           &selectops,
    fendif
    #ifdef  WIN32
           &win32ops,
    fendif
              NULL



. Libevent 通过 过 爵 eventops 数组 来 选择 其 后端 IO 复 用 技术。 历 的 顺序是从 数组 的 第
        个 元 素 开 ，到 最 后 个 素 结 。 以， 复
      一 始 一 元 束 所 在 Linux 下 ，Libevent 默认 选择 的 后 VO

## Page 253

    第 12 章 ”高 性能 库
        1O     框架 Libevent 235


    很                                    ，             修改              清单                              用
    用 技术 是 epoll。                       但
                                       显然 用 户   可以         代码              12-8 中 定义 的 一 系列     来 选择 使        不 同
      端                        。
    的 后  VO   复    用 技术

                                                体
    12.2.7                   event_base        结构
             体                      是                                                                中
            结构    event_base             Libevent 的 Reactor。           它 定义 在   event-internal.h 文件  ，          清
                                                                                                             如 代码
    单 12-9 所 示    。

                                         代码      清单    12-9     event_base     体
                                                                             结构

      struct      event_base         [{
                             36%         的         时       候 逃    一 种  后     复
                       /*            Reactor    择                        I7O    用机制，并 记录 在 如 下 字段  中 */
                   const          struct    eventop    *evsel;
                                                                             来                       初始化 */
                       /*    指向 I/O      用  机 人  制 真正 存储 的 数据 ，它 通过                   evsel RAM init 函数
                   void          *evbase;
                               事件变化 队列 。其
                       /*                        用途是:如果     一     个      文件     符上 注册 的 事件被多 次 修改  ，  则 可 以 使 用缓冲
    来 避免重复 的系统 调用 【比如                                    它 仅 人  能 用 于 时间 复 末 度  为O(1)   HI/OMMRA   */
                   struct                   event_changelist    changelist;
                       /*      指向 信号 的 后     处   理机    制     ，  目 前 仅在singal.h   文件   中 定义了 一 移 处 理 方法 */
                   const          struct eventop       *evsigsel;
                       /*    信号 事件 处     理器 使    用的   数据 结构     ，    共 中 封堵   了 一 个由socketpair     的    。  信和
                                                                                                     创建
                                                                                                     管道  它 用 于  号
    处 理 函数  和 事件 多 路 分       发 器 之 问     的通信，        这 和 我 们在   10.4  节讨 论 的 统一 事件 源 的 思路 是 一 样   的*/
                   struct         evsig   info   sig;
                   /*        逐 加 到  该   event_base                           */
                   int       virtual            event_count;
                   int       event_count;
                   int       event_count_active;
                   /*        是否执 行完适 动 事            队列 上        的    任务  之 后 就退出事件 循环   */
                   int       event_gotterm;
                             是 否  立 即    退 出事件   循环   ，而    不 管 是 否    还 有 任务   要 处 理   */
                   int
                   /*        是 否  应 该     一 个  新 的        事件    循环  */
                   int       event_continue;
                   /*        目前 正在 处 理 的 活动 事件队列 的 优先级                   */
                   int       event_running_priority;
                   /*        事件 循环 是 否 已经启动           “/
        int running_loop;
        /*
BMRA
        struct event_list *activequeues;
        /* 活动 事件队列 数 的 大 小 ，即该event_base 一 共有 nactivequeves 个 不 同优先 级 的 活
动 事件队列 “7
        int nactivequeues;
        /* 下面3 个 成员用 于 管理通用 定时 县 队列 */
        struct common_timeout_list **common_timeout_queues;
        int n_common_timeouts;
        int n_common_timecuts allocated;
                   /*                                                                                  BR
                             */
                                                     cb queue   defer   queue;
                   struct         deferred
                   /*

## Page 254

 篇 解析 性
 236 “*** 第 二 深入 高 能 服务 器 编程



                  event_io
        struct             map   io;
        /*                   il         RM KR    “/
        struct    event                   sigmap;
        /*    注册 事件 队列 ，存
        struct    event_list      eventqueue;
        /*    时间堆 */
        struct    min_heap timeheap;
        /*                          “/
        struct    timeval  event_tv;
        struct    timeval  tv_cache;
 #if defined(_EVENT_HAVE_CLOCK_GETTIME)        && defined (CLOCK MONOTONIC)
        Struct    timeval      tv_clock_diff;
        time_t                              diff;
 #fendif

“/
 #ifndef
        unsigned    long   th_owner_id;    /*     当前 运行该event_base        */
        void  *th_base_lock;    /*    Mevent_base     */
        /*    当前 事件   循环   正在执行 哪个 事件       处 理 器 的 回调函数 */
        struct    event    “current_event;
        /*                       */
        void  *current_event_cond;
        int      current_event_waiters;    /*     $f current_event_cond    */
 fendif

 #ifdef  WIN32
        struct    event_iocp port      *iocp;
 fendif

        /*    该event_base  的 一   些 配 置     数 */
                            baseconfig
        enum event                flag     flaqs;
                                 创建的管道)     */
                  这 组成员 变量 给 工作 线程唉醒 主 线程提供 了 方法【使用socketpair
        int
        evutil    socket_t th_notify     fd[2];
        struct    event    th_notify-;
                           fn)  (struct    event_base “base);
        int                 (*th_notify
 b?


 12.28 ”事件    循环
       后 ，          一
  最                 下          的      ”，        循环
                我 们讨论    Libevent  “动力       即  事件 循环 。Libevent 中 实现 事件 的 函
 是 event base loop。 该 函数 首先调用     VO 事件 多 路 分 发 器 的事件监听 函数 ，以 等 待事件;
 数
         发生时，         处理
                          之                    的      清单      示
                                               。
 当 有 事件 就依次                    。event_base_loop 函数 实现 如代码     12-10 所
                      代码   清单  12-10 event_base_loop 函数

  int  event_base  loop(struct event_base  “base, int flags)
  {
             const  struct eventop *evsel  = base->evsel;

## Page 255

库
第 12 章 高 性 能 1O 框架 Libevent ++ 237



           struct      timeval  tv;
           struct      timeval  *tv_p;
           int   res,    done,  retval   =  0;

           EVBASEACQUIRE th_base_lock);

           /*    一小event_base                        +/
           if                        loop)
                           (base->running_  {
                                             reentrant  invocation. Only one   event_base_loop"
                                "can run     on  each event_base    at once.", —func_);
                                             LOCK(base,    th_base_lock);
                         return     -1;
           }

           base->running_loop       =   1;   /*  标记该event_base     已 经 开始 运行   */

           clear_time_cache(base);           /*  清除 event_base                 */

           /*          设置信号 事件的 event_base 实例       */
                                             added    base->sig.ev_n_signals_added)
           if                (base->sig.ev_signal_
                             evsig_set_base (base)  ;

           done  =     0;

   #ifndef EVENT DISABLE THREAD         SUPPORT
         base->th_owner_id    = EVTHREAD_GET_ID();
   #endif

         base->event_gotterm    =     base->event_break = 0;
         while ('done)   {|
                 base->event_continue         =  0;

                 if    (base->event_gotterm)        {
                                break;
                 }

                 if         (base->event_break)  {
                                break;
                 )

                 timeout_correct(base,               /*        */

                 tv_p    =  &tv;
                 if                              (base)
                       (!N_ACTIVE_CALLBACKS
                         &&    !(flags  & EVLOOP_NONBLOCK)) [
                                /*
*/
                                                (base,  &tv_p);
                 )     else {
                                /*                                                 “HO”.
这样   用   系统
 I/O 复     调用直接 返回，程序           也 就   可 以 立即处   理  就绪 事 件了 +/
                                evutil_timerclear(é&tv);

## Page 256

+ 篇 解析 性 编程
238 第 二 深入 高 能 服务 器





                  /*    和如果                       中 没有注册 任何 事件         ， 则 直接
                      event_base                                           事件            */
                  if                                   (base)    46 'N_ACTIVE  CALLBACKS (base)) {
                                    event _debug(("%s:           no    events  registered.",
                                    retval        = 1;
                                    gote done;
                  }

                  /*                                  ay */
                  gettime(base,           &base->event_tv);
                                               (base) ;
                  clear_time_cache

                  /*    调用 事件 多 路 分 发 器             的dispatch          等竺     ， 绪      插入   事件
                                                                 方法
                                                                            事件  将 就 事  件   活动    队列   。/
                  res   =    evsel->dispatch(base,               tv_p);

                  if    (res   ==   -1        {
                                    event        _debug(("%s:    dispatch    returned  unsuccessfully.",
                                                    __func__));
                                    retval        = -1;
                                    goto done;
                  }

                  update_time_cache(base);                 /*              ARE             +/
                  /*    检查 时间 堆 上 的 到 期             事件并        依次 执行之  +/
                                               (base) ;
                  timeout_process
                  if    (N_ACTIVE_CALLBACKS (base))                {
                               /*   调用    event_process_active         函数 依次    处理 就绪  信号 事件和 IT/O 事件 +/
                               int     n  =                      active (base);
                                              event_Pproces3
                               if   ((flags       &  EVLOOP_ONCE)
                                          &&    N ACTIVE  CALLBACKS (base)     ==
                                          &&    n   != 0
                                                done  = 1;
                  })    else   if  (flags     &   EVLOOP_NONBLOCK)
    done = 1;
    )
    event_debug(("ts: asked to terminate loop.", _func_)};

done:
          /*                                        FM MRE               +/
          clear_time_cache (base);
                               loop    =  0;
          base->running

          EVBASE RELEASE _LOCK(base,                th_base_lock);

          return  (retval);
)
eee

 此，                                       库的核心        代码，                 还     。         理解
至 我 们 简要 介绍 了 Libevent                                           但 这 些     远     远 不 够 要    Libevent 的
          实现                      ，                   深入                          代码
设计 理念 以 及     上 的 细节 考虑                读 者 最      好 自己           分 析 其 每 一 行     。

## Page 257

    第 13 章    编程
        多 进程


       进程是 Linux          操作 系统 环境 的 基础        它 控制 着             系统上 几乎 所   有 的 活动        。        本 章 从 系统
                                               ，                                                          程序
    员 的 角度 来 讨论          Linux       多 进程 编程 ，包 括   如 下 内 容  :
       CD       复制 进程映像的         fork  系统 调用 和 符 换 进程 映像           的exec 系列 系统 调用          。
       CD       僵尸 进程 以 及     如  何 避 免 僵尸进程     。
                       间通信     (Inter-Process Communication,  IPC)     最 简单
       QC)      进程                                                的                    方式 ;管道   。
                                       间通     方式 信号
                3 ff System V 进程 :             量、             消 息 队列 和 共享 内              存。它们 都 是    由 AT&AT
        的，所 统称
    System V2 版本的 UNIX 引信 以 为System V IPC.
       QC)      在 进程间 传 递 文件 描述 符          的    通用 方法 : 通过    UNIX     本 地 域 socket       传递特殊      的 辅助  数据
                                       考5.8.3   小
                关于 辅助 数据 ，参                   节)。

    13.1          fork   系统 调用

       Linux       下 创建 新 进程 的         系统  调用 是 fork. Jive      LaF:
       #include       <sys/types.h>
       #include       <unistd.h>
       pid_t       fork( void   );
       该 函数 的 每          次      调用 都返回     两  次 ，在 父 进程 中    返回 的 是 子 进程     的       PID， 在 子 进 程    中 则 返回
    0。 该    返回     值  是  后续 代 码     判断  当前 进程 是     父 进 程还    是 子 进     程的依据 。fork        调用 失败 时 返回    -1，
并 设置errmno。
   fork 函数 复制当前 进程 ，在 内 核 进程表 中 创建一个 新 的 进程表 。新 的 进程
                                                                                 项                  表项 有很 多
       和原进程相同，比                  如  堆 指 针、栈 指针 和 标志 寄存 器 的        值        。但也 有 许多 属性 被 戏子          了  新 的
    属性
    值 ，比        如  该 进程的   PPID     被      成原进程                             清除    进程                的
        设置 的 PID， 信 号 位 图 被 〈 原 设置 信号 处 理
           不    再  对 新 进程起作用 )。
                                                                                                     和
       子 进程        的  代码 与 父 进程完全 相同 ，同 时 它 还 会 复制 父进程 的 数据                            〈   数据    、       栈数据
    态 数据 )。数 据 的 复制及                   的是所 谓 的写时 复制 (copy         writte)，           即 只  有 在 任     进程   〈 父
                                                                  on                             一         进
       或子进程) 对 数据 执行了写操作 时，复制才 会               发                生《〈 先 是 缺 页 中     断    ，  然后操作 系统 给        进
    程
    程 分         内 存 并 复制 父进程           数据  )。   即
           配                   的               便如此，    如 果 我 们 在     程序    中         分  配 了 大 量 内   存 ，那 么 使
                                  十
                                 谨慎    尽 量 避 免 没 必要 的 内 存 分       和 数据 复制    。
    用 fork      时 也 应 当      分 ，                                  配
        后，父 进程中打开 的 文件 描述 符 默认 在 子 进程中也 是 打开
       此 外         ，创    建子                                                                         的，  且 文件
                                 进程
       符的引用计数加            1。       不
    描述                                 仅如此，    父 进程   的 用 户   根 目录    、当 前 工作                 目录 等 变量 的 引用计数
    均 会加        1。

## Page 258

240           第 二 篇     深入 解析 高      性      能服务    器 编程

13.2      exec          系列 系统 调用

    有 时 我 们 需要 在 子 进程中执行其                          他 程序      ，     即      换 当前 进程 映像             ，     这 就      使用如下
                                                                                                                       需要 exec
系列 函数 之        一 :

    #include      <unistd.h>
    extern     char**   environ;

     int  execl(     const       char*    path,    const       char*       arg,     ...  };
     int  execlp(       const    char*    file,    const       char*       arg,     ...  )}
     int  execle(       const    char*    path, const        char*     arg,       ...,     char*     const    envp[] );
    int   execv(     const       char*    path,    char*       const       argv[]     );
    int   execvp(       const    char*    file,    char*       const       argv[] );
     int  execve(       const    char*    path, char*        const     argv[],       char*           const envp[) );
    path      参数 指定 可 执行 文件 的完整                   路     径    ，file            参数 可以 接受 文件           名  ，  该 文件  的     具体 位 置 则
在 环境变量         PATH    中 搜寻 。arg                   可变参数                                                ，            传递
                                          接受                          ，argv                则 接受 参数 数组     它 们 都 会 被        给 新
程序 〈path       或  file  指定 的 程序           )  的 main AM.                                   参数 用 于 设置 新  程序   的 环境 变量     。    果
                                                                      env~p    如
未 设置 ，则                            将使用                                的                  变量。
                  新程序 由 全 局 变量                                                    environ 指定
          它                                                                      环境
        般情况下                     函数
                                     是不返回的，            除
                                                             非出错。                它 出 错时
    一                                                                                        返回 -1，        并  设置             如
                        ，exec                                                                                     erno。      果
                        中            调用 之 后 的 代码
没 出  错            ，则 原 程序  exec                        都不会执行，                     因 为    此   时 原 程序 已 经    被       exec   的 参数
    的程序完全                   〈    包 括代码
指定                                          和数据)。
    exec       数 不会关闭 原 程序打开 的 文件描述                                   ，    除 非 该         文件  描述 符 被 设置     了          似
                                                                      符                                           类       SOCK
CLOEXEC 的         属性       见
                        〈 5.2 节 )。

13.3     ”处                    理 僵 尸进程
    对 于 多 进程 程序 而                  言 ，       父 进程 一般 需 要 跟踪 子 进程 的 退出 状态                         。     因 此  ， 当 子 进 程        运
                                                                                                                            结束
  时，                         内 核 不 会 立即 释放 该 进程 的  进程表 表     项     ，  以 满足            父  进程 后   续 对 该      子进程  退出      信息 的 查
行
询   如 果 父 进程         还  在 运行         )。   在 子 进程   结束     运行 之        ，        父 进程       读      取 其 退出 状态 之        ，    们   该
                                                                                                                             我
                                                                          后                                       前      称
子       处于        尸态。                                进                态           是             进程         者               ，
    进程 另 外                         一 种 使 子 进程              人 从        的              情况 ，  父           结束 或            异常 终止 而
子 进程 继续 运行 。此 时 子 进 程的                      PPID   将 被    操作        系统 设置             为 1， 即    init 进程 。init   进程接管       了 该
子 进程 ，并 等 待它
                        结束    。  在 父 进程      退出      之     后 ，    子 进程         退出 之     前  ， 该 子 进程 处     于 尸       态 。
     此可见，无 论                     种 情况 ，如 果                父 进程    没有             正确 地 处  理子 进程    的    返回 信息，              子进程
    由                                                                                                                       都将
    在僵尸态，并 占据
                                 着内    核资    源。    这 是      绝对    不 能 容许          ，    毕 竟 内 核资
停留                                                                                                         源有限。           面这对函
                                                                                  的                                          下
数 在 父 进程中 调用，以 等 待子进程 的 结束 ，并 获取 子 进程                                                的    返回    信息     ，  从 而       了从      进程
                                                                                                                            避免
的 产生，或 者 使子进程的 僵尸                           态立即    结束 ;
    #include      <sys/types.h>
    #include      <sys/wait.h>
    pid_t      wait(    int*     stat_loc    );
    pid_t      waitpid(    pid_t     pid,    int* stat_loc,           int        options    );
                  将阻寨进程，             直 到 该 进程 的       某      个    子   进程         结束    运行 为     止    。 它 返回 结束 运行 的        子 进
    wait 函数

## Page 259

                                                                                              第 13 章             ”多     进程     编程 *%* 241

    程 的PID，     并 将 该子进 程的退出 状态信息 存储 于                                                 参数 指向 的 内 存                                      头
                                                                        stat_loc                                 中     。sys/waith       文
    件 中 定义了 几 个 宏 来 帮助 解释 子 进 程 的退出 状态 信息 ，                                            表                                示。
                                                                               如                         13-1            所

                                                     13-1             进   程状态 信息

                                                                                   会          x
                             )                    如 果子 进程 正常 结束，它 就返回
                                                                                                          一个非       0
    WIFEXITED( stat_val                                                                                             值
    WEXITSTATUS( stat_val                         如
                                      )             果    WIFEXITED      非   0， 它 返回                           子 进 程 的 退出 码
    WIFSIGNALED( stat_val             )           WRF                                                RE — PAR OAS nk.            OK
                                                    果    WIFSIGNALED     非 0，    它
    WTERMSIQG( stat_val      )                    如                                返回      一  个               信和号 值
    WIFSTOPPED( stat_val         )                MRF                          Eee                            0
                                                  如 WIFSTOPPED 非 0，                                               它返回
    WSTOPSIG( stat_val )                            果                                     一个信和                      值
                                                                                       而 waitpid                 解决              了 这 个 问题。
        wait  函数 的 阻塞特性 显然 不是服务 器 程序 期 望                                           的   ，
                                                                    果 pid      取 值   为 -1，                   那 么 它 就    和 wait   函数 相同 ，即
    waitpid 只  等 待     由 pid     参数 指定 的 子 进程                。   如
    等 待 任意         个 子    进程 结束 。stat_loc 参数
               一                                                的含义和           wait                          stat_loc    参数 相同 。options 参
    可以控制                      函数
    数              waitpid                  的行为 。该 参数 最 常用的 取 值是                       WNOHANG。                  当 options        的 取 值 是
    WNOHANG 时 ，waitpid                      调用 将 是 非 阻                  果                  的
                                                                    ，                                       如 pid 指定 目标 子 进程 还 没有 结束 或 意外
                                                        果目   标 子 进                            了    程 确实 正常 退出       则    waitpid       返回
    终止 ， 则    waitpid     立即          返回    0  ，  如                                                           ，                     该子进程的
    PID. waitpid     调用 失败时 返回 -1                   并 设置     ermo。
        8.3  节 曾 提 到     ，    要 在 事件 已 经 发                  生的                                调 情况 下 执行 非 阻 塞    用 才能 提高 程序      的效率   。对
    waitpid 函数 而        言 ，我             们  最 好 在 某  个   子  进程      退出之后再 调用               它                        。那 么 父 进程从何 得 知 某 个子进
                      了呢?  这 正是             SIGCHLD       信和 号   的  用  途。当         个 进程
    程 已 经 退出                                                                     一                                     结束时，它 将 给 其 父 进程发送
    个    SIGCHLD       信和
                                      因此号。  ，           我
                                                    们     可以 在 父 进程 中 捕获 SIGCHLD 信号                              ，                  并 在 信号
    一                                                                                                                                 处理函数
                                                             个子       进程  ， 如 代码     清单    13-1                  所 示     。
    中 调用 waitpid 函数         以“彻底                                   结束 ”一
                                               清单                                                                  信号
                                            代码    13-1             SIGCHLD                                    的典型处理函数
        static     void              handle child( int      sig  )

               pid   t   pid;
               int   stat;
               while     (  (           pid =    waitpid(   -1,     &stat,  WNOHANG       ) )                >     0     )
                                               RB
                           MBF
                                                         HERE */
               }
        }


13.4 Fe

   第 6 章 中 我 们 介绍 过 创建 管道的系统 调用 pipe， 我 们 也 多 次 在 代码 中 利用 它 来实现 进程
内 部 的 通信。实 际上， 管 道也是 父 进 程和子 进程间 通 信 的 常用手段 。

## Page 260

242     第 二       篇    深入 解析 高       性能 服务 器                编程

        能在父、           子 进程 间 传 递 数            据          ，    利 用     的     是 fork      调用 之 后 两 个 管道       文件   描述 符     (fd[0]
    管道
和 fa[l])    都 保持打开       。    一  对 这 样       的文件               描述 符 只 能 保证                  父 、  子 进程 间    一 个 方向 的 数据 传输    ，
                 父 进程和子 进程必须有 一  个 关闭        fa[0]，            另    一   个 关闭         fa[1]。   比    如，我 们 要   使用   管道     实现从 父
       癌子进程写数据，            就 应 该 按照               图              所 示 来 操作
进程                                                      13-1                         。
                         close(fd[0])
                                                                                            读
                   父进程                                                     管道    |                       子进程

                                                                                              close
                                                                     He
                                                               BAB
                                    图 13-1            父 进 程通过 管道 向 子 进程写数据

                           父、                     之子 进程 间 的 双向 数据 传输                        ，    就 必须
    显然 ，     如    果 要实现                                                                             使    用 两 个 管道 。  第   6  中 我
         还介绍过  ，socket   编程                     接口 提供 了 一     个 创建全         双 工      管道     的   系统 调用   :socketpair。squid 服务
们
        第         4 章  )             就
                                 利用是  socketpair                 创建 管道                                ，以 实现
个 程序〈 见                                                                                       在父    进程   和 日 志 服 务   子   进 程之间
       日志信息 ，      下 面 我 们 简单 地 分 析                         。    在             机器                            下   ;
传递                                                                  测试               Kongming20              上 有 如
                                                          之                                                  环境
    $   ps -ef |  grep squid
    root       12489       1    O  20:37     ?                   00:00:00        squid
    squid      12491   12489    0  20:37     ?                   00:00:02        (squid-1)
    squid      12492   12491    0  20:37     ?                   00:00:00        (logfile-daemon)   /var/log/squid/access.log
    squid      12493   12491    0  20:37     ?                   00:00:00        (unlinkd)
    $ sudo   lsof  -p  12491
    squid     12491   squid               unix          Oxeaf?b440                   Oc0     40603   socket
    $ sudo   lsof  -p  12492
    log_file   12492   squid     Ou       unix          Oxeaf2b680                           40604   socket
    log_file   12492   squid     lu       unix                                       OtO     40604   socket
    leg file   12492   squid     Zu   CHR                        1,3                 ord      4449  /dev/null
    log fle    12492   squid     3w   REG                        8,3                 202    271412  /var/log/squid/access.log
                   输出
    这 些           说明   Kongming20     上                     了                        。      服务      了                ，     中子进
                                             开启                  squid 服务                该      创建 几 个 子 进        程          其
                  用
程 12492    专门      于 输出 日 志 到                                                            文件   。 父        进程12491  用 socketpair
                                                                                                             使
创建 了   一   对 UNIX 域 socket,                 然后                     关闭 了 其 中 的  一  个  ，   剩 下 的 那个   socket 的值是       9.   子 进程
    了这一对UNIX 域 socket， 并 关闭 了 其 中 的 另外 一 ， 下的
12492  则 从 父 进程     12491 继承                                                                                          个      剩
那个      则
    socket        被dup           到 标准 输入 和 标准 输出                        。      下 面 我 们       telnet 到 squid     上，
                                                                        上                                    服务 并 向        它 发
  部分数据。           同 时 开启 另外 两          个 终端 ，               分 别 运行                                       命令
送                                                                                             以 查看  进程       和               在
                                                                               strace                      12491     12492   这
个 过 程中交换 的 数据。具 体操 作 如 代码 清单                                             13-2  所 示   。
                                         代码 清单13-2          ”用      strace     命令 查看 管道         通信

    S$ telnet     192.168.1.109          squid
     Trying   192.168.1.109...
    Connected     to  192.168.1.109.

## Page 261

    第13 章 ”多 进程 编程 <> 243


          Escape      character   is   '*)}',
          a
          $     sudo  strace  -p  12491
   write(9, "L1338385956.213 40 192.168.1"..., 104 = 104
          $     sudo  strace  -p  12492
          read(0,       "L13383865956.213      40   192.168.1"...,                         4096  =   104
          write(3,        "1338385956.213      40   192.168.1."...,                         101  =   101

                  此可见，进
          由               程    12491                           接收 到 客户 数据 后 将 日  志 信息 输出 至           管道 〈  写 文件 描述    符 9)。 日
    志 服 务         子 进 程 使 用  阻塞   读 操作 等 待 管道       上 有 数据 可 读                       〈 读 文件 描述        符 0)，   然 后将  读 取 到 的 日
                  写人 /var/log/squid/access.log 文件   〈  写 文件 描述                         符    3)。
    志 信息
                  过，管道 只     能                          〈 比 如 用于有 关联 的 两 个 进程        、     子 进程  )  间 的   通信          而 下 面 要讨论
          不                                                                         父                    。                    的
    3 种   System V IPC   能     用 于                      之       无 关联 的 多 个 进程间 的        通信 ，    因 为 它们 都 使 用 一 个 全 局 唯一    的  键
    值 来标识          一 条 信道 。       过，      特殊                为                                              Out，      )，
                          不 有         一     种     的                      管道 称    FIFOS (First In First     先 进 先 出            也
    叫 命名管道 。它 也 能 用 于 无 关联 进程 之间 的 通信                                               因
                                                            。                           为  FIFO        管道 在网络 编程 中 使 用 不 多   ，
    所 以 本 书 不讨论它        。

    13.5          ”信和 号 量
                                量原语
    13.5.1                       信号
          当 多 个 进程 同时 访问 系统上 的 某 个 资源                       的时                   候  ，    比 如 同时 写   一 个 数据 库    的 某 条 记录 ，    或
    者 同时 修改 某 个 文件 ，就需要 考虑 进程 的 同步 问题 ，                                                 以 确保任 一时 刻      只   有  个           可以拥有
                                                                                                         一                   进程
                                      常， 程    序  对 共享 资源 的     访问 的代码 只 是 很 短 的 一                        段 ，但 就    是    这   一 段
    对 资源          的 独占 式 访问。通
    代码 引发 了 进程之间 的 竟                          条 。我 们 称 这 段代码 为 关键代码 有                                   或 者 区。
                                      件                                                                  临界 对 进程 同
    步   ， 也 就       是确保任     一 时 刻   只 有 一   个 进程 能 进   人    关键 代码 段                       。
                                      具有通用 目的
          要 编写                            的代码    ，以 确保 关键 代码 段                          的  独占 式 访问 是非常困难     的     。有 两    个  名
                                      的解决方案                 ，              它们    试图     从 语言 本      身 〈 不 需要 内 核 支持  )       解决
    为   Dekker      算法和 Peterson     算法
    并 发 问题 。但 它们 依赖 于 忙 等             ，即     进程要持续 不断                                   地 等 待                个 内 存 位 置状态 的 改变。这
                                            待                                                                                 种
                        率太低，         显 然 是    不 可 取 的  。
    方式下      CPU      利用
          Dijkstra    提出 的 信号量 (Semaphore)             概念                   是 并 发 编程 领域 迈 出 的 重要 一       步     。    信和号 量 是   一
                  的变量 ，它 只 能 取 自      然     数 值并  且 只                      支持两 种    操作     :  等 待   (wait)  和 信号 〈signal)。    不
    种 特殊
      在   Linux/UNIX 中    ,“ 等        待 ”和 “信和
    过                                 号”都                   已 经    具有                   特殊 的 含义      ，所以 对    信和号 量 的 这    两种 操
    作 更 常用的 称呼           是     P、V   操作。这 两个字母 来自于 荷兰 语 单词 passeren (传递                                  ， 就    好像进           人
             区                        放 ，        退          区)。                                     量
    临界 和 vrijgeven〈 释                 就 好 像         出 临界                            假 设 有 信号 SV，         则 对 它 的 P、V         操作


SO ”这 里 要 注意 一 下，虽 然 这 种 特 屿 的 管道 被 专门 命名为FIFO，但 并 不 是只有 这 种 管道才 循先进 先出的
  则 ， 其 实所有 的 管道 都遵 先 进 先出的原则 。

## Page 262

    244 ++       第 二    篇  深入 解析 高 性 能服务 器           编程

             义如下:
    会
        口 P(SV)，           果      于                  将   减 1，                  果                      0，     进程
                 如 SYV 的 值 大     0，     就
                                                         它                     如    SV  的 值           为      则 挂 起             的 执行 。
                             因为                      等待SV 而 挂                  ，则 唤醒          ，       如 果 没     ，则
        口                如 果 有 其 他进程                                       起                  之              有      将    SV   加 1，
   信号            量的     取值 可以 是任何 自然 。但 最 常用                            的  、    最 简单 的 信号 量                  是   二 进 制   信号       ，  只
                                                 数                                                                            量      它
  取0 和           1 这 两 个     值 。本 书 仅讨论 二        进    制信号           量   。 使     用   二   进 制 信号 量 同步 两 个 进程                  ， 以 确保
    能
    关键 代码 段      的     独占 式 访问 的 一 个典型 例子            如   图      13-2      所 示       。
               图   13-2 中  ，      当 关键 代码 段 可
        在                                                                                                    een
        时，     二 进  制    信号 量 SV 的 值 为  1，
    用
    进程A和 B 都有机 会             进   人      关键 代码
    段 。        果 此 时     进程    A执 行了                 X              非                         Pa
          如                             P(SV)        进程 的 关 健                                                进程           8 的非 关

                 就会被挂起。直                 到 进程
    P(SV) 操作                                A
    离开 关键 代码             段 ，并 执行 V(SV)     操作
             加 1，              关键 代码段才 重新 变 得
    将 SV
        。
    可 用        如   此 时 进程     B  因为 等 待SV                                                用    ”使             保护
                                                                     图 13-2                   信号 量           关键代码 自
    而处于 挂 起 状态 ，则 它 将 被 唤醒，并
    进入 关键代码 。                样，             A                                            ，
                         段 同 这 时进程 如 果 再 执行                                             P(SV) 操作  则 也 只 能               被 操作 系统 挂 起以 等
    待 进程       B 退出 关键代码 段     。

    注意 ”使用       一     个   普通 变量 来 模拟   二 进      制 信号    量      是 行       不     通     的 ， 因 为         所有 高    级  语言 都         一 没有 个 原
                 操作可        同时    如              操作                             是否为                                        将
             子 以 完成 下 两 步                           :    检测 变量 true/false，                                   如 果 是 则           再 它 设 置
             % false/true.

        Linux     信号 量的      API 都定义    在 sys/semh              头 文件                中，  主 要 包含 3             个 系统    调用        semget,
                                                                                         量集，          而 不 是     单个    信和 号        ，  因
    semop 和 semctl。        它 们 都 被 设计 为操作            一 组 信号          量         ， 即 信号                                         量      此
这 些 接 口看上 去 多 少 比 我 们 期 望的要 复杂 一 。 们将分 3 小 节 依 次 之。
                                                              点  我                                      讨论

    13.5.2       Semget      系统 调用
        semget     系统 调用 创建     一  个 新 的  信和         号 量 集    ，  或 者           获取    一    个 已               经 存在 的 信号 量  集       。其 定义
     下 :
    如

   Winclude <sys/sem.h>
   int semget( key_t key, int num_sems, int sem_flags );
               参数 用 来 标识     一                       个 全 局                               唯一 信号 量  集   就 像         名 全 局           地 标识
        key      是一个键值，                                              的                        ，              文件                 唯一
                 一样。     要 通过 信号 量 通信 的 进程           需要          使  用    相同 的 键 值 来                   创建      /  获取
    一           个 文件                                                                                         该      量。
                         会 数 指定                                                                                                   信号 则
        num_sems             要     创建   /获取          的 信号 量 集中 信号 量 的 数目                                 如 果 是。      创建           量，
                 被指定:      如 果 是 获取 已 经 存在 的 信号                  量  ，    则 可 以               把 它              为0。
    该 值 必须
                                                                                                   个 比特 是 该信号 量  权限               其 格式
        sem_flags        参数 指定   一 组   标志 。它                低 端的                                         的     ，              和     含义
                                                               9

## Page 263

                                                                                   第     13 章     ”多   进程 编程       245

与系统调用      open
都                          的    mode        参数 相同 。    此 外，            还可以和                     标志做按位                ”
                                                                                                     它 IPC_CREAT    “或
运算 以 创建 新 的信号 量                         此 时    即使信号 量 已 经 存在                           也 不 会         产生  错误    。    们还
                           集                                                   ，semget                               我
可以 联合 使用       IPC_CREAT               Ail IPC_EXCL     标志 来 确保 创建                     一  组  新    的 、唯    的        量集。
一                                                                                                                   信号
在 这 种           下          和                                                返回错误并设置
           情况 ，        如 果 信 号 量 集 已 经     存在     ，       则 semget                                  errno    为 EEXIST。
                           与用           O_CREAT 和 O_EXCL 标志 调用                         open  来 排他       式 地  打开 一  个 文
这 种 创建信号               的 行为
件相似 。
semget     成 功时返回          个 正 整 数值        ，    它     是 信号 量 集 的            标识                                失败     并
                           一                                                       符                    时 返回   -1，   设
置 errno。
        果  semget     用 于 创建
如                                      信号量集，         则 与之 关联 的 内 核 数 据             结构     体          ds   将       被 创建
                                                                                                  semid             并初
化              ds      结构 体            定义
始          。semid          的             如

#include <sys/sem.h>
/*   访 结 构 体 用 于         描述     TPC    对象〔〈信 号 量 、共    享内 存 和 消息 队列         )  WM */
struct           ipc_  perm

                t      key;                                     /*     Si   */
           key
           uid_t       uid;                                     A*     所 有 者 的有效用户     ID       */
           gid_t       gid;                                     /*     所 有 者的有 效组       TD       */
                                                                                    者的有效用户ID     */
           uid_t       cuid;                                    /*     创建
                t
           gid cgid;                                            /*                      ID       */
           mode_t     mode;                                     /*     访问 权限  */
                                                                       省  略 其 他 填充
                                                                /*                      字段  +/
}

struct           semid_ds
{
           struct     ipe_     perm                             /sa                         */
           unsigned      long   int      sem_nsems;             /*     该 信号 量集中 的 信号 量 数目       “/
           time_t     sem_otime;                                /*     最  后 一  次调用     semop     的  时间 */
           time_t     sem_ctime;                                /*     最  后 一  次 调用     semctLl        */
                                                                /1*    省 略其他 填 克 字段         */
he

                                                      初始
                                                      包括 :
semget     对 semid_ds 结构 体                的       化
    将 sem_perm.cuid
口                          和           sem_perm.uid    设置 为          调用  进程 的 有 效用 户         ID.
口       将  sem_perm.cgid 和 sem_perm.gid     设置 为                     调用  进程 的   有  效    组       ID。
                                                                               的最低     9          位 。
口       将  sem_perm.mode     的 最          低 9      位 设置 为sem_flags 参数
C)      将  sem_nsems       设置          为num_sems。
口 将 sem_otime 设置           为           0。
口       将  sem_ctime       设置    为            当前 的 系统时间 。
13.5.3     semop           系统 调用
                                                                                                之前，          我    们 需要
semop      系统 调用 改变        信和          号 量 的  值  ，即 执行      P、V 操作            。在讨论 semop                             先

## Page 264

246  «*       第 二 篇       深入         解析    高   性 能    服务 器    编程

介绍 与 每 个                 量关 联        的   一    些 重要 的 内        核 变 最:
     unsigned          short     semval;                                    信号 量 的值 “7
         unsigned      short     semzcnt;                               /*       等 信 号 量 值 变为          0  的 透 程      数量  */
     unsigned          short     semncnt;                                        等 待信号 量 值增加 的 进程 数量     “7
         t                                                                       最 后       次 执行                      操作
     pid sempid;                                                        /*           一             semop      的      进程  TD */
              对 信号               的操作 实际上 就 是对 这 些 内 核 变 量 的 操作                                         的 定义
     semop                                                                                         。scmop        如下:
     finclude     <sys/sem.h>
     int semop(          int     sem_id,       struct  sembuf*     sem_ops,              size_t    num_sem_ops     );
                              是由       semget  调用 返回 的 信号 量 集 标 识                        符 ，用以     指定 被 操作 的 目标信号 量 集                 。
     sem_id 参数
                                                                                                            ，sembuf
sem_ops                  参数 指向 一 个 sembuf        结构 体 类 型      的  数组                                      结构体的定义如下:
     struct        sembuf
    {
              unsigned        short        int   sem_num;
              short      int     sem op;
              short      int     sem_flg;
    }
         中    ，sem_num           fit bi          Ss   BR         aS Be               0     表示 信和号 量    集中              的 第 一    个信号   。
     其
sem_op   成员 指定 操作 类                  型     ，         其 可 选 值为   正 整数    、0     和 负 整         数。                        每种类 型 的操作 的行为又受到
sem  fig 成 员 的 影响                。sem      fg 的 可          选    是 IPC_  NOWAIT Al SEM_UNDO.                                IPC_NOWAIT 的
     是，                                                                                            ，                      阻塞          。
含义 无 论 信 号 量 操 作 是                                   成功  ，semop     调用 都 将 立即 返回                   这 类 似 于 非                 IO      操作
SEM_UNDO 的 含义                        ， 当      进程 退出 时 取消
                                   是                           正在进行的                     semop    操作    。    具 体 来      说   ，sem_op   和
sem_fig            将 按照如  下 方式 来 影响              semop     的 行为   ;
     口   如    果       sem_op    大 于     0，     则     semop    将 被操作 的 信和号                    的值semval 增加     sem_op。               该 操作
                                           操作 信和           集拥有      。                    时   设置了                                   标志，则
        要 求 调 用 进程对 被                                号 量       写 权 限 此                                 SEM_UNDO                       系
 统 将 更 新 进程的semadj 变量 〈 用以跟踪 进程对 信号 量 的修改 情况)。
                                                     这是一个“等                                                                    操作 。该 操作
                              等于               则 表示
     口 如 果            sem_op         0，                                          待     0”(Cwait-for-zero)                           要求调
                    对         操作信和         号   量 集    拥有 读     权 限 。如 果     此 时 信号 量 的 值               是 0，                 则 调用 立即 成功返
        用 进程
        回 。 如 果 信          和   号   量 的 值 不 是 0，      则        semop                              失败 返回 或 者 阻塞 进程以 等    待信号 基 变    为  0。
                  情况 ， 当                   IPC_NOWAIT                                 标志
        在 这 种                 下                                被        指定             时   ，semop  立即 返回     一     个 错误        ，   并 设置
                  为                        如 果 未 指定                                        标志，则    信和     量                    值
        erro          EAGAIN。                                 IPC_NOWAIT                               的     semzcnt         加     1， 进
                                                                                     量的值       semval 变
        程 被       投入      睡眠 直到下 列               3    个 条 件 之 一 发    生      信号                         为 0，                      此 时 系统
                                                                        :                                                           和将该
              号量的                    值
        信和                                     减     ;被 操作 信号 量 所 在 的信号 量 集 被 进程 移                                        ，此
                          semzent                1                                                                除        时    semop 调
                                                               调用
                                                                                   信号                       此 semop     调用 失败 返回      ，
        用 失败 返回                  ，errno 被 设置         为  EIDRM    ;               被           中断，            时
                                                               将该信叶 其              的           值
        ermo                被 设置 为 EINTR，            同 时 系统                              semzcnt  减     1。
                                     于         则 表示 对          信和 号 量 值 进行               减 操    作  ， 即 期                      望 获得信号 。该
     口   如     果 sem_op          小 0，                                                                                          量      操
        作 要 求         调   用 进程对 被 操作 信号 量 集 拥 有 写 权                                  限   。 如 果 信   和    号   量 的值semval       大 于 或    等
                                     绝对              则         操作 成         ，    调用 进程 立即 获得信号                           ，并        且 系统
        于     sem_op      的                  值 ，     semop                  功                          量                              将
        该 信号 量            的 semval             值 减    去        的 绝对                  。  此 时     如  果 设 置    了了              SEM_UNDO 标志
                                                     sem_op                        值                                                  ，

## Page 265

 第 章”多 编程
   13  进程 < 247


   则 系统 将 更新进程                semadj     变量          。       如 果 信和号  量     的 值           小
                              的                                                  semval      于               的 绝对
   ffi.      St semop              失败 返回 或 者 阻                     进程 以 等 待信号 量 可 用。  在 这 种          下，
                                                                                          情况 当              IPC
                                  标志被指定时，semop   立即 返回         一   个 错误         ， 并 设置   errno  为      EAGAIN   i
   NOWAIT
   采 未 指定 IPC_NOWAIT 标志，则                            号量的                         加        投入
                                                              信和 semncnt    值     1，   进程 被          睡     眠 直到 下
                              量的值                semval
   列 3       个 条件 之 一 发生      信号                                 变得大于或等于                  的 绝对              ，   此
                              :                                                       sem_op            值      时
             将该信号量的           值
                              减 1，                   并
   系统                                                将     semval                                             同 如
                              semncnt                                          减去 sem_op  的 绝对 值       ，     时，
   ALSEM_UNDO 标志
                              被    设置 ， 则 系统
                                                              更新 semadj 变量       ;   被 操作          信号 量 所 在 的信和 号
                             移除，此
   量 集 被 进程                   时     semop     调用 失败 返回              ，errno   被    设置     为 EIDRM   ;        被
                                                                                                               调用
                                                                                                               信号
         断，          时                                     被           为
   中 此                    semop  调用 失败 返回 ，errno
设置                                                                        EINTR，                 同 时 系统 将 该信号 量 的
                   值
   semnent                减   1。
 要                                                                    执行 的 操作
 semop       系统 调用 的 第         3 个    参数    num_sem_ops   指定                          个数    ， 即 sem_ops      数组 中
         素的个数  。semop       对 数组 sem_ops     中 的 每 个  成   员     按照 数组 顺序 依次 执行            操作，           并 且 该 过 程
 元
 是 原子操作        ，以             别的进程在                                                   集中      量执行
                                       避免    同一时 刻 按照 不 同        的 顺序 对 该           信号     的信号              semop
         的竟态条件。
 操作 导致
                                           失败
                                           返回 -1 并 设置     ermno。      失 败     的   时 候                          数组
 semop       成  功  时 返回                 则                                                                中      的
                                                                                         ，sem_ops        指定
 所 有 操作 都 不 被 执行          。

 13.5.4  semctl               系统 调用
                                                                                   如下;
 semetl      系统 调用 允许 调用 者 对 信和号 量 进行 直接 控制 。其                                      定义

 #include       <sys/sem.h>
 int     semctl{     int      sem_id, int    sem_num, int  command,          ...  )}
 sem_id      参数 是 由 semget     调用 返回的信号                    集标识            符  ，用以 指定 被     操作           的 信号 量 集 。
 sem_num 人参数    指定 被 操作 的 信号 量 在 信号 量 集 中 的                        编号 。command        参数 指定 要 执行 的           命令 。
 有 的 命令需要 调用 者 传递第            4     个  参数    。第  4    个 参数       的  类 型     由 用 户 自己      定义    ，但   sys/sem.h  头
 文件 给 出 了 它 的推荐 格式 ，          体如下:
                              具

 人 semun
         int  val;                                        /*        于
                                                                 用                    */
                                                                                 #    Ipc_SET #4     */
         struct    semid_ds*     buf;                     /*     JAF IPC_STAT
                                                                 用                 于 GETALL
         unsigned             array:                      /*                     和 SETALL @4    */
         Struct    seminfo*                                      AF IPC_LINFO@?          */
 be

 struct      seminfo

         int       semmap;                                /*     Linux         内 核 没有 使用 */
         int       semmni;                                /*     系统最多可 以拥有 的 信号 量 集 数目               */
         int       semmns;                                            最 和   可以    拥有 的信叶 量     数目  */
         int       semmnu;                                /*     Linux  AREA             */
         int       semmsl;                                /*    EERE

## Page 266

248 第 二篇 ”深入 解析 高 性 能 服务 器编程


                  int    semopm;                                              /*     semop                  sem  RRA   */
                  int    semume;                                              /*     Linux  内 核 没 有 使用  */
                  int    semusz;                                                                结构 体的  /* sem_undo 大小 */
                  int                                                         /*    最 大 允许
                         semymx;                                                                 的信号量值 “7
                                                                    的             操作
   /* 最 多 允许的UNDO AK SEM UNDO 标志 semop 的次数) */
   int semaem;
hi

              semetl     支持 的所有 命令如 表                     13-2  所 示 。

                                                        表 13-2     semctl 的 command 参数
                    |                              全                                                        值
  命          今                                                                                      semet! 成     时 的 返回
                   |       信号
      IPC_STAT                   量 关联的        内   核 数     据 缚   构 复 制 到 semun.buf 中             0
                         将  semun.buf    中 的 部 分 成     员  复制 到 信 生      集   关联    的 内  核 数
IPC-   SET          据                                                                           0
                         结构 ，     同 时 内 核数据 中
                                                       的                              被
                                 中                            semid_ds.sem_ctime    更新
                               移除信号量 ，           唤 本     有 等       待该信号 量   焦 的    进程
 PC一   RMID              立即                                                                     0
                    (semop       返回 错 ，并 设置 errmno              为 EIDRM)
                               系统 信和                量 源 配 置信 息  ，  将 结果 存储 在  semun.    buf | ”内 核        量集
                                                                                                    信息
IPC       INFO                                                                部分                   项 的最大 索引 数组 中 已 既 被 使 用 的
   一                中。这 些信 息 的 含义             见 结 构    体    seminfo         的 注释                          值
                     与             类             不 semun.__buf.semusz         被       为  系
                            IPC_INFO          似，       过                          设置
SEM_INFO          | 统 目        拥有 的 信号        量集  数目     ，  而   semnu， bufsemaem      被  设置 |   同 IPC_
                                                                                                       INFO
                    为系统 目前拥有 的                信号 量 数目
                                              似， 不 过 此 时 sem_id     参数 不 昆 用      来        示 信
                   |     与 IJPC_STAT 类
                                   ，                                                        的所 |
SEM_STAT               号 量 集 标     识     符 而 是 内  核 中 信号           集 数组 的 索引  《系统                           sem_
                    A fri        A EK            PA)                                         ，
                            由  sem_id              SRM}         OY PTA AS       semval   值
                   |   将
GETALL                      到  semun     amray
                    导出                        中                                                 °
GETNCNT           |                semnent         值                                            信号量的    semnent 值
GETPID                 PE                  sempid 值                                             信和
                                                                                                   号量的  sempid 值
GETVAL                 获得
                               信号     量  的 semval 值                                             信和号 量的  semval 值
GETZCNT           | ”非得        信号 量 的     semzent  (if                                          信号 量  的 semzent 值
                       用   semun.array     中 的 数据 填充 由          sem_id      的信号
                                                                    标识 量 集
SETALL     “|中           的  所  有     量的                ，            据
                                 信号     semval         值 同 时 内 核 数     中    的 semid_            0
                    ds.sem_ctime     更新
serv                   将 信号 量的     semval         值 设置    为 semun.val，    同 时 内     核    中
                                                                                        数据


                                                                                                                   eee
注意     ”这些操作                中                                        ，GETNCNT、GETPID、GETVAL、GETZCNT 和 SETVAL 操作 的 是 单个
                                                                的             量
                                                                                  集                     量          其他操
     SFE, CA HAIR                                                     sem_id 指定
                                                                   信号          中 的 第     sem_num        个 信号 ;       而
     作 针 对 的 是 整个信号 量                              ，  此
                                                   集  时 semctl     的 参数             sem  num    被 忽略 。
                                              值取决于                 参数         ，       如
       semctl       成       功时 的      返回              command                         表  13-2   所 示 。semetl 失败时 返回 -1，
并 设置   errno。

## Page 267

                                                                         第 章”多     编程
                                                                           13  进程    <<    249

13.5.5              键       值 IPC_PRIVATE
semget               的调用 者 可  以 给    其 key 参数 传递      一个    特殊 的 键     值 IPC_PRIVATE 值 为 0)， 这
样无论该信号 量 是 否 已 经 存在                   ，semget   都 将 创建    一 个        新 的信号 量。使用该 键 值 创建的  信号 量
   并 非 像 它的名       声                               。                       ，   有
                字 称 的 那样 是 进程 私有                   的    其 他 进程   ， 尤   其 是 子 进程 也 方法 来 访问  这 个
         量。所
           以        的             手册
                                         的 BUGS 部
信号                                                    分上说，
                                                                         使
                semget        man    用名字 IPC                               PRIVATE 有 些      CG
史 原 因 )，应 该 称          为     IPC_NEW。    比 如 下 面     的  代码 清单    13-3  就 在   、子     间使用一个
                                                                           父    进程         IPC
PRIVATE 信号 量 来同步            。
                                代码 清单13-3         ”使    用IPC_PRIVATE       量
                                                                       信号

#include        <sys/sem.h>
#finclude       <stdio.h>
finclude        <stdlib.h>
#include        <unistd.h>
finclude        <sys/wait.h>

union   semun
{
         int    val;
        struct      semid      ds* buf;
        unsigned          short  int* array;
        struct      seminfo*         buf;
he
                                                  V担作    */
/*     op                         PH, op为1 时 执行
void     py(    int    sem_id,    int  op  )
{
       struct      sembuf      sem_b;
       sem_b.sem_num          = 0;
       sem_b.sem_op         =   op;
                       flg  =   SEM_UNDO;
       sem_b.sem
       semop(        sem_id,    &sem_b,    1 );
)

int    main(       int   arge,  char*    argv[]   )
{
       int      sem_id   =  semget(   IPC_PRIVATE,     1,   0666 );

       union       semun   sem_un;
       sem_un.val        =  1;
       semctl({        sem_id,  0,   SETVAL,   sem_un });

       pid_t id     =     fork();
       if(      id <0)
       {
                return    1;
       I
       @lse     if(    id  ==   0  )
       {
                printf({   “child    try   to get  binary   sem\n" );

## Page 268

    篇  能 编程
    250 第 二 深入 解析 高 性 服务 器


                                                父、子 进程
                                /* 在                          间共享 TPC_PRIVATE 信号       量的    关 刍 就 在于二 都可                标识
        者 以 操作 该 信号 量 的
sem_id */
        sem_id, -1 );
        printf( "child get the sem and would release it after 5 seconds\n" );
                                sleep( 5        );          _
                            pv(            sem_id,      1   );
                            exit(         0  );
                    i
                    else
                    {
                            printf(          "parent          try to    get    binary  sem\n"  );
                            pyv(           sem_id,      -1   );
        printf( “parent get the sem and would release it after 5 seconds\n" );
                                sleep( 5        );
                            py{            sem_id,      1   );
                    }

                    waitpid(       id,       NULL,           0 };
                    semctl(                sem_id,      0,   IPC_RMID,   sem    un  ); /*              */
                    return         0;

                                                                                   .

             ee
                       一
                        个            是:                 在                                                使用
     另外
        例子 工作 prefork 模式 下 的 httpd 网 页 服务 器 程序 1 个
             来              各                                            调用     。       分                         。    测试
    信号 量                                  同步 子 进程对  epoll_wait     的            权 下 面 我 们 简单        析  一 下 这 个 例子 在        机
                                          ，  用                                                      如
      Kongming20 上                        使      strace        命令 依次 查看         httpd  的 各 子 进 程 是     何 协调 工作 的  ;
         $ ps          -ef     |       grep  httpd
            root                  1701              1      O 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1703       1701          0 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1704       1701          O 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1705       1701          O 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1706       1701          O 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1707       1701          0 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1708       1701          O 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1709       41701         O 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
         apache                   1710       1701          O 09:17 ?                 00:00:00 /usr/sbin/httpd -k  start
                   sudo     strace        -p         1703
         semop 393222,                    0,        -1,                        1
                   sudo strace            -p         1704
         semop         393222,            0,        -1,      SEM  UNDO}},      1

         $         sudo strace            -p         1709
         epoll                wait(14,               , 2,  10000   =    0
         $         sudo strace            -p         1710
         semop(393222,                    0,        -1,      SEM_UNDO}},       1
             此             见,                       进程
         由
                    可            httpd      的 子              1703~1708 和 1710       都 在 等 待信 号 量    393222   (这 是 一  个 标识 符 》
                       有                                拥有该              量，         进程
    可 用                          只
                                进程 1709 暂时
           ，                                                    信号             因 为     1709    调用 cpoll_wait  以 等 待 新  的 客户 连
             有                     到来时，                 程                      之，      号
    接 。 当           新 连接                         进  量                                                             ，
                                                             1709 将 接受          并 对信和     393222 执行 V 操作          此 时 将 有
         个          子                该
    外 一                          进程 获得      信号          并 调用     epoll_wait     来 等 待 新    的   客户 连接 。 那 么 我 们    如    何 知道信号

## Page 269

                                                                                  第       章
                                                                                     13                            «+ 251

    Hit  393222  是 使 用 键值 IPC_PRIVATE              的呢?                                   晓。
                              创建 答案 将 在                                      13.8       节 揭
          下 面    讨论 另外 两种     了   PC   一   一共享 内   存    和 消息 队列      。       这 两    种IPC  在 创建       资源          的时 候 也 支
    持    IPC_PRIVATE      值，     与信号               的
                        键 其 含义     量 IPC_PRIVATE BSH,

                        内
                         存
    13.6               ”共享

          其 享内存 是 最高效         机
                             的 IPC     制，   因 为 它            不涉及 进程          之 间 的 任何 数据 传输            。          这 种 高效率
                 是，我      们 必须 用 其 他 辅助    手段       来        同步 进程对 共享            内 存 的 访问     ，            否则 会 产生 竞 态 条
市 来的问题
件 。因 此，共 享 内 存 通常和 其 他 进程间 通 方式一 起 使用 。
          Linux  共享 内 存   的 API           都 定义在               头              中，                      调用
sys/shm.h 文件                                                                      包 括    4 个 系统              :    shmget、
    shmat、shmdt 和 shmctl。     我 们 将 依次讨论 之             。

    13.6.1       shmget   系统 调用
          shmget 系统 调用 创建 一 段 新    的                共享 内  存  ，或    者 获取      一  段 已 经    存在    的 共享 内           存  。 其 定义
       下;
    如

          #include <sys/shm.h>
                          t key,     size_t size,      int    shmflg );
          int   shmget(  key
          和        系统调用一样，key                       一                ，          标识        段
              semget          参数 是                           个 键     值      用 来                 一 全 局 唯一 的   共享 内 存     。
    size  参数 指定 共享 内 存 的      大    小 ，    单 位是     字 节 。     如 果 是 创建         新      的 共享 内 存  ，  则 size        值 必 须 被 指
    定 。如 果 是 获取 已经存在          的    共享 内     ，      则 可以 把     size            为
                              存                                             设置  0。
                        的使用和 含义
                                     与             系统    调用   的 sem_flags         参数 相同    。      不
          shmfig 参数                                                                                过            shmget 支持
                              semget                                                                                    两
个 额外 的 标志 一 一SHM_HUGETLB 和 SHMNORESERVE。 它 们 的 如下
        含义 :
        似
          JU  SHM_HUGETLB，     类     于             的     MAP_ HUGETLB             标志    ，  系 统 将
                              mmap                                                                   使用          “大 页 面 ”
              来 为 共享 内 存 分 配 空间     。
                              类 似 于                               NORESERVE 标志            ，                        不 为 共享
                              mmap                                                                                   内存保留
                              样，                   当 物理 内 存 不 足              的时  候   ， 对 该 共享      内存 执行 写          操作  将
              交换 分区 〈swap         空间 )。    这
              触发 SIGSEGV        号。
                                信和
          shmget 成功时 返回          个    正                          整数 ，它 是 共享内存 的 标识                               失败
                            一                 值                                     符  。shmget         时   返回       -1， 并
    设置    ermno。
              果  shmget  用 于创建
          如                   共享内存，则               这 段 共 享 内 存的              所 有 字 节 都 被     初始      化                  与
                                                                                                       为 0，         之  关联
    的 内 核 数 据结构    shmid_ds            将 被 创建 并        化               ds             定义
                              初始                             。shmid          结构 体 的       如 下     :

                 shmid ds

                                                                     共享
                 struct  ipc_perm shm_perm;                   /*             肉存的操作权限      */
                 size_t  shm_segsz;                           /mw                                  “7
                 __time_t    shm_atime;                       /*     对 这 和   内  存 最 后 一 次   调用  shmat  的   时    间   */
                 __time_t    shm_dtime;                       /*     对 这 上   内  存量 后  一  次  调用  shmdt  的   时    间   +/
                 __time_t    shm_ctime;                       /sa      对 这 上段内  存       最后一次调用  shmct]   的   时间     */

## Page 270

252 第 二篇”深入 解析 高性能服务 器编程


            __pid_t       shm_cpid;                           /*   @ MAA   PID  */
            __pid_t       shm_lpid;                           /*   最 后 一  次 执行shmat 或 shmdt   的 的
                                                                                             操作
                                                                                             进程 PID */
            shmatt_t     shm_nattach;                         /*   目前 关联 到 此 共享 内      存 的    数量
省               一 些           填充 字段  “/                                                      进程 “/
  );
shmget Xf shmid_ds                  结构体    的 初始 化包括      :
         将  shm_perm.cuid 和 shm_perm.uid             设置 为       调用 进程 的      有 效用 户    ID.
口        将  shm_perm.cgid 和 shm_perm.gid             设置 为       调用  进程的      有 效 组   ID。
C)       将  shm_perm.mode      的     最    低 9  位 设置    为  shmflg       的最低
                                                                            参数  9  位 。
                               为size。
器 将 shm_segsz 设置
                                                                                为 0。
口        将  shm_lpid、shm_nattach、shm_atime、shm_          dtime         设置
                             为
口 将                      设置     当前
            shm_ctime                       的时间 。
13.6.2      shmat 和 shmdt        系统 调用
共          内 存 被 创建             之 后          ， 我 们 不 能   立即 访问         它 ，而 是 需要 先     将     它 关 联 到 进程的  地 址
空间 。使用完 共 享 内 存              之    ，    我 们 也 需要 将 它 从     进程           地   址  空间  中  分       。这 两项        分别由
    中                            后                                                      离                  任务
如 下 两 个系统 调用 实现          :

#ineclude         <sys/shm.h>
void*       shmat(    int    shm_id,     const     void*  shm_addr,     int shmflg   );
int      shmdt(     const    void*     shm_addr    );
        中                     参数
其           ，shm_id          是由         shmget 调用 返回 的 共享 内                    符
                                                                       存标识          。shm_addr      参数 指定 将 共享
内 存 关 联 到 进程 的 哪 块 地 址                      ，  最 终 的   效果 还   受到 shmflg             的可选标志
                                 空间                                    参数                           SHM_RND 的
影响:
口 如 果 shm_addr为                                                                              的做法            以
                             NULL，          则 被 关联       的 地  址 由 操作 系统 选择          。 这 是 推荐     ，          确
                       的可移植性。
    保 代码
口 如 果       shm_addr         空，     且                    未被            ，              内存被关联到
                         非 并                                       SHM_RND 标志
                                                                           设置 则 共享                    addr 指定
            地址处。
    的
                             空，    并 且 设置       了    SHM_RND       标志    ，    则 被关联
C)      如   果 shm_addr 非                                                                的地址是       [shm_addr-
    (shm_addr % SHMLBA)].                      SHMLBA 的 含义         是    “  段 低 端     边界 地 址     倍数  ”(Segment
    Low Boundary Address Multiple)，                    它     是                       小 (PAGE
                                                         必须 内 存 页 面 大                        SIZE)        的 整
            倍。          的                      ，         于                                                是圆整
    数 现 在                Linux     内 核         中   它 等     一  个   内 存 页 大    小 。SHM_RND                  的 含义
    Cround)，     即 将 共享 内 存 被 关联的地 址向下 圆                               整 到 离 shm_addr    最 近  的      SHMLBA 的
                    倍地址处。
    整数
除了         SHM_RND 标志 外           ，shmflg       参数 还 支持    如     下 标志    :
    SHM_RDONLY。              进 程    仅能        读 取 共 享    内 存 中 的 内       容 。  若 没有       该     ，           可同
                                                                                    指定
                                                                                                      标志 则 进程
    时 对 共享 内 存 进 行 读 写 操作 〈                     然，
                                                      当 这 需要 在 创建 共享 内 存             的 时 候    指定 其 读 写 权 限 )。
    SHM_REMAP。               如 果 地 址     shmaddr     已 经    被关联 到      一   段  共 享 内 存   上，    则 重新 关联。

## Page 271

    第 13 音 ”多 进程 编程 <> 253


             SHM_EXEC。            它 指定 对 共享 内 存 段     的          执行权限。  共享
                                                                     对           内存而言，     执 行权限 实际上
             和 读 权 限 是      一     样 的 。
           shmat             成功时 返回 共享 内 存 被关联 到         的 地    址 ，失 败 则返回  (void")-1 并    设置erno。shmat
    成 功      ，将 修改        内     核数据   结构          ds       分          下
             时                        shmid 的 部 字段 ， 如
           口 将        shm_nattach 加  1.
                                      为调用    进程   的PID.
           口 将        shm_lpid 设置
                                                   的时间。
                  将         设置        为              当前
                      函数
                            关联        shm_addr    处 的 共享 内 存 从 进程       分   离  它 成    时 返回   失 败 则 返
           shmdt          将  到                                       中         。     功       0，
    回 -1   并 设置       ermo.     shmdt     在 成 功调用 时 将 修改 内 核    数据   结构        ds             ， 下
                                                                        shmid 的 部 分字段          如
           口      将     shm_nattach 减 1.
                                    为调用    进程     的PID。
           口 将        shm_Ipid 设置
                                                为当前的时间。
                                       加 将 shm_dtime 设置

    13.6.3            shmctl     系统 调用

   shmetl 系统 调用 控制 共享 内 存的某些 属性。其 如下;
        定义
   #include <sys/shm,h>
   int shmctl( int shm_id, int command, struct shmid_ds* buf );
      中 ，shm_id 参数
           其                        是由    shmget  调用 返回 的 共享 内 存 标识            符  。command 参数 指定 要 执行
    的 命令          。shmetl 支持 的    所   有    命令如 表  13-3 所 示 。
                                              ®   13-3 shmetl     的
                                                                     支持
                                                                    命令
    IPC_STAT          |                                                               0        人

                                  中的    shmid_ds.shm_ctime 被
      一                 内 核数据                         更新

      -                 shmdt                     POR      ABR

IPC_INFO 需要
        将 buf 转换成 shminfo 结构 体 类 型来读 取 这 些 系统 信息 。shminfo| 4 ES BEE
        缚 构 体 与 seminfo 似，
                                      类 这 里 不 再                                             最
        大索引值
SHM_INFO 信息。应 用 程序需要 将 buf 转换成shm_info 结构 体 类 型 来读取 这 些信| 同 TPC INFO
        似，这 里 不 再 述
                        息 。shn_info   结构   体 与shminfo 类

                        识
    SHM_STAT              符，    而 是 内 核    中 共享 内 存 信息 数组 的 索引    【每 个共享内 存 的信息   都|  组 中    值为shm
                                                                                           索引     id 的
                        是 该 数组 中 的     项 )                                                     共享
    SHM_LOCK                          一                                                0   内存的标识符
    SHM_UNLOCK                                                                         0
        返回
        取决 command 参数 如 13-3 所 示 。shmetl 失败时 返回 -1,
                 shmetl 成功时 的         值      于        ，             表

## Page 272

    254 «+ 第 二篇 深入 解析 高性能服务 器编程


    并 设置errno。

                      内存的POSIX 方法
    13.64  ”共享
                            过    mmap BRM.                标志 我 们 可 以                    实现 父  、
     6.5  节 中    我们 介绍
    子 进 程 之间 的      匿名内     存    共享 。通 过 打开 同一 个 文件       ，mmap         也可以 实现 无 关 进 程 之间 的 内 存
    共享 。Linux 提供 了 另外       种利用              在 无 关 进      程 之     共 享 内 存 的          方式 。这 种 方式
                            一            mmap                                               无须任
         的     支持  ，但 它 需 要 先 使 用        下    函数 来 创建 或打开 一个                         共享
    何 文件                    如                                         POSIX       内存对象:
     #include      <sys/mman.h>
     #ineclude     <sys/stat.h>
     #include      <fcntl.h>
     int  shm_open({    const    char*  name,     int oflag, mode_t mode );
                            与                              系统
                            方法
     shm_open      的 使  用     open                    调用完全相同。
                                                                          共享 从 可 移植 性 角度 考虑 该参数
     name  参数 指定 要 创建 /打开                的      内存对象。      的     ，                         应该使用
    “/somename”  的  格式      以 “/”          开
                            :        始，      后 接 多 个 字符    ，  且 这 些     符 都 不是“/:       \ 0” Fi
    尾，长   度不 超过     NAME_MAX     (ili       dé 255).
                            方式。它 可 以         是    下 列  标志 中 的 一   个 或者多 个 的 按 位 或 ;
     oflag 参数 指定 创建
                            读 方 式                          共享
     口    O0 RDONLY。        以 只          打开             内存对象。
                                             打开共享内存对 象     。
     口 O_RDWR。          以 可 读    、              可 写 方式
                                                          时     参数
     口     CREAT。       如 果        共 享 内 存 对    象 不 存在 ，     则 创建   之 此  mode     的最低     位 将 指 9
     定 该 共享内存对 象的 访问 权限。共 享 内 存对 象 被 创建 的 时 ，其 初始
                                                          候      长度为0。
     00 EXCL。         和     O_CREAT    一    起 使   用 ， 如 果 由  name 指定 的 共享 内 存 对 象 已    经      则存在 ，
             shm_open 调用 返回 错误    ，    否 则 就创建        一个 新的共享内存 对象    。
     口 O_TRUNC。         如 果 共 享         内存 对  象 已 经 存在 ，则 把 它 截断 ，使 其 长 度为0。
     shm_open      调用 成 功 时 返回             个  文件描述 。该 文件     符可用     的                        ，
                            一                          符        描述 于 后 续     mmap            调用
                                                          时 返回    -1， 并 设置     errno。
    从 而 将 共享 内 存 关联 到 调用 进程     。shm_open                   失败
     和 打开 的 文件 最 后          需要   关闭 一        样 ，  由 shm_open            创建的共享 内 存 对象 使 用完之后也 需要
    被 删除  。这 个 过 程 是        通过如  下    函数 实现 的     :
        #include   <sys/mman.h>
        #include
        finclude   <fentl.h>
     int   shm_unlink(      const  char     *name  );
            该 函数将  name 参数 指定     的    共享 内 存 对 象      标记 为 等 待 删除 。当 所 有 使 用该共          内存 对 象
              都使用           将    它从 进程   中   分 高 之    ，      系 统 将 销毁 这 个 共享 内 存 对 象所      占据
            的 进程   ummap                     后                                           的资源。
                                            共享
                                                      函数 则 编译           时 候 需 要 指定 链接   -lrt。
     如 果 代 码 中 使        用      了 上 述 POSIX      内存        ，     的        选项
        内存实例
13.6.5 ”共享
   在 9.6.2 小 节 中，我们 介绍 过 一 个 聊天 室 服 务 器 程序。下 面 我 们 将 它 修 改 为 一个多 进程

## Page 273

                                                        第    13 章      ”多 进程 编程      255

      器 :           个 子 进 程   处 理      个 客户 连接   。同
    服务       一                     一              时，我 们 将    所有 客户 socket 连接 的 读 缓冲 设计 为
             内
                    存，            清单 13-4
    一 块 共享                如 代码      所 示         。
                                       清单 13-4  ”使
                                                 用      聊天
                        代码                          共享 内 存 的 室 服务 器 程序

      #include     <sys/socket.h>
      #include     <netinet/in.h>
      #include     <arpa/inet.h>
      Winclude     <assert.h>
                   <stdio.h>
      #include     <unistd.h>
      finclude     <errno.h>
      #include     <string.h>
                   <fentl.h>
      #include     <stdlib.h>
      #include     <sys/epoll.h>
      #include     <signal.h>
      #include     <sys/wait.h>
      #include     <sys/mman.h>
      #include     <sys/stat.h>
      Winclude

      #define       USER_LIMIT  5
      #define       BUFFER_SIZE    1024
      #define       FD_LIMIT   65535
      #define       MAX_EVENT_NUMBER    1024
           #define  PROCESS   LIMIT     65536

      /*   处       个 理 一 户连接    必要 的    数据 “7
      struct        client_data
      {
       in                                                            WH */
sockaddr address;                                   /*        socket
           int     connfd;                          /*    socket 文件描述 */
                   t  pid;                          /*    处  理 个 连接 的  程  PID */
           pid                                          这      子          的
           int     pipetd[2];                       /*                    */


    static          const char*  shm_name -
    int   sig_pipefd[2];
    int   epollfd;
    int   listenfd;
    int
    char*  share       mem  =   0;
    /rw   客户 连接 数组       。   进 程 用 客户 连   的 编号 来索引 这 个 数 ，印 可 取得 相关 的 客户 连接 数据 “/
    client_data*        users    = 0;
    /*                                                                     */
    int*        sub_process   =  0;
    1/*   当前 客户     数量   “/
    int   User      Count =   0;
    bool        stop_child  =   false;

    int          setnonblocking(   int fd )

## Page 274

** 篇  性 编程
256 第 二 深入 解析 高 能 服务 器




 {
        int  old     option   =  fentl(   fd,     F_GETFL   };
        int      new_option   =  old_option       |     _NONBLOCK;
        fentl(   fd,   F_SETFL,     new_option          });
        return   old_option;
 }

void     addfd(  int     epollfd,     int fd      )
 {
        epoll  event     event;
        event.data.fd       =   fd;
        event.events      =   EPOLLIN     |        EPOLLET;
        epoll_ctl(     epollfd,     EPOLL_CTL_ADD,          fd,   sevent );
        setnonblocking(         fd );
)

void     sig_handler(       int    sig )
(
        int  save_errno       =  errno;
        int  msg =     sig;
        send(        sig_pipefd[1],    (  char*      )&msg,  1,   0  );
        errno  =
I

void     addsig(    int   sig,            void(*handler) (int),   bool restart = true )
{
        struct  sigaction       sa;
                 &sa,     "\O',    sizeof(    sa      ) );
        Sa.sa_handler      =    handler;
        if(  restart     )
        {
                                                                     .
             $a.sa_flags      |=   SA_RESTART;
        }
        Sigfillset(               );
        assert(  sigaction(          sig,    48a,    NULL  ) !=   -1  );
}

void                      ()
{
        close(  sigpipefd[0]           );
        close(  sig_pipefd[1]          );
        close(  listenfd        );
        close(  epollfd       );
        shm_unlink(        shm_name    );
        delete  []     users;
        delete  []     sub_process;
}

            一个子进程
  停止                   */
           void child_term_handler(  int     sig     )
{
        stop_child     =  true;
)

## Page 275

                                                                                            第 13     章   ”多 进程   编程 +    257

                 运行的函数。       数                                         理                                 保存             数据
  /* 于 进程 参 idx 指出 该 子 进程 处 的 客户 连接 的 编号 ，users 是 所 有 客户 连接 的
 hl,   €&  share_mem       指出 内                存   的     起   地址     */
      int  run_child(       int              idx,  client_data*         users, char*        share_mem    )
      {
           epoll     event                events[  MAX                  );
                            用                                                                                Pat           x
/*              子 进程 使           IT/7O                                                             socket.
      符
 件 描述
           int  child_epollfd                   =  epoll_create(        5    );
           assert(     child_epollfd                     !=  -1 );
           int  connfd      =                      users[idx) .connfd;
           addfd(                                        connfd     );
           int  pipefd      =      users[idx]              .pipefd(1];
           addfd(    child_epollfd,                      pipefd     );
           int  ret;
                                                   信号               */
           /*  子 进 程需要 设置 自己的                              处理函数
           addsig(     SIGTERM,              child_term_handler,        false     );

           while(    !stop_child                 )
           {
                int    number            =   epoll_wait(            child_epollfd,        events, MAX_EVENT_NUMBER, ~-1    );
                if   (  (   number             <   0 &&        (    errno  !=  EINTR      ) )
                {
 .                     printf(             "epoll          failure\n"     );
                       break;
                )

                for    (      int  i       =    0;   i     < number;    i++   )
                {
                       int    sockfd             =   events([i).data.fd;
                                                                        +/
                       if(      (           sockfd   ==      connfd     ) &@& (     events[i).events   & EPOLLIN ) )
                       {
                              memset( share_mem                      +    idx*BUFFER_SIZE,    '\0"',   BUFFER_SIZE );
                              /*
                                                                     节
                                                                        。 此
 idx*BUFFER_SIZEX,                                                   字     ，                  是             的
                                                                        因     各 小 客户      连接 的 读 缓 存     共享     */
                              ret          =       recv(   connfd,  share_mem   + idx*BUFFER_SIZE,        BUFFER_SIZE-1, 0 );
                              if(          ret     <     0   )
                                {
                                           if({    errno      !=    EAGAIN    )
                                           (
                                                                        = true;
                                           }
                              }
                              else             if(   ret      ==  0  )
                              {
                                           stop      child     =    true;
                              }
                              else
                              {
                                                                                RORRSP
                                           /*                             RBG Miho sue        来 处         理 “/
                                           send(         pipefd,     (    char* )&idx,      sizeof( idx   , 0   );
                              )
                       I

## Page 276

篇 解析 性 编程
258 第 二 深入 高 能 服务 器


                /*    主 进程通知 本 进程〔 通 过 管道)                  将第client 个 客户 的 数据发送副本 进程负责的        窗户  wy7
                else     if(   ( sockfd       ==    pipefd   )  && ( events[i].events &  EPOLLIN )  )
                {
                       int   client       =    0;
                       /*  接收 主 进 程 发 送 来           的数据     ，即  有 客户 数据 到 达 的连接 的 蚁   “/
                       ret   =   recv(        sockfd,   (   char*  )&client, sizeof( client ,  0  );
                       if(   ret    <     0                     ‘
                       {
                                    errno     !=    EAGAIN   》
                             {
                                    stop_child        =    true;
                             )
                       |
                       else   if(      ret     == 0 }
                       [
                             stop      child     =  true;
                       }
                       else
                       {
                             send(        connfd,    share_mem     + client  *
                                          BUFFER_SIZE,      0   );
                       }
                }
                else
                 {
                       continue;
                 }
           i}
     }

     close(    connfd     );
     close(    pipefd     );
     close(    child_epollfd           );
     return    QO;
}

int  main(     int arge,     char*        argv[)    )
{
     if({  arge    <=  2  )
     {
           printf(     “usage:      ¢s                      port_number\n",   basename(       )   );
           return     1;
     }
     const   char*     ip  =
     int   port    =   atoi(   argv[2)        );

     int   ret =     0;
                             in   address;
     struct    sockaddr
     bzero(                    sizeof(        address     ) );
     address.sin_family           =    AF_INET;
     inet_pton(       AF_INET,      ip,     sSaddress.sin_addr     );
     address.sin_port          =  htons(      port    );

     listenfd      =  socket(     PF_INET,     SOCK_STREAM,     0    );

## Page 277

    第 13 ”多 编程
      章     进程 + 259



    assert(     listenfd           >=     0

    ret   = bind(     listenfd,               (   struct       sockaddr* )Saddress, sizeof( address ) ode
    assert(   ret       【=     -1      );

    ret   = listen(                   listenfd,   5   );
    assert(     ret     !=     -1      );

    user_count      =    0;
    users =     new   client_data                 [   USER_LIMIT+1     ];
                      =    new       int      [   PROCESS_LIMIT     ];
    for( int      i=     0;     i    <     PROCESS        LIMIT;  ++i  )
    {
          sub_process[i]               =   -1;
    I

|
    epoll_event       events[            MAX_EVENT_NUMBER           ];
    epollfd     =                 epoll_create(   5   );
                epollfd         !=    -1     );
    addfd(         epollfd,        listenfd          );

    ret  =            socketpair(    PF_UNIX,             SOCK_STREAM,   0,        );
    assert(     ret   !=       -l    );
    setnonblocking(             sig pipefd[1]               );
    addfd(         epollfd,     sig_pipefd[0]               );

    addsig(            SIGCHLD,    sig_handler            );
    addsig(     SIGTERM,           sig           handler  );
    addsig{     SIGINT,                                   );
    addsig(     SIGPIPE,           SIG_IGN           );
    bool            stop_server    =       false;
    bool    terminate         =    false;

        创建                ，            所       客户    socket        缓存
    /*        共享 内      存   作 为          有                     连接 的 读     */
    shmfd  =    shm_open(            shm     name，        O_CREAT |  D_RDWR，0666
    assert(     shmfd        !=    -1     );
    ret  =           ftruncate(    shmfd,     USER_LIMIT        *    BUFFER SIZE );
    assert(     ret   !=       -1    );

        share_mem = (char*)mmap( NULL, USER_LIMIT * BUFFER_SIZE, PROT READ |
        PROT_WRITE, MAP_SHARED, shmfd, 0 );
                share_mem            !=                        );
    close({   shmfd      );

    while(    !stop   server           )
    {
         int    number        =    epoll           wait(  epollfd,   events,        -1     );
         if  (  (          number    < 0     )   &&    (  errno   != EINTR     ) )
         {
                printf(                 ”epoll   failure\n"     );
                break;
         )

## Page 278

篇”深入 性 服务 编程
260 第 二 解析 高 能 器



for (     int L  = 0;   i     <  number;   i++     )
{
    int   sockfd    =             events([i].data.fd;
    /*    “1/
    Lf(   sockfd     ==   Listenfd       )
    i
          struct                     in  client_address;
    socklen_t client_addrlength = sizeof( client_address );
    int connfd = accept( listenfd, ( struct sockaddr* )
    &Client_address, éclient_addrlength );
    if ( connfd < 0 )
    {
    printf( “errno is: %d\n", errno );
    continue;
    }
          if(    user_count         >=  USER_LIMIT      )
          {
                 const    char*     info   -     "too many   users\n";
                 printf(        "ts",   info  );
                 send(    connfd,       info,  strlen(      info , 0 );
                 close(       connfd    );
                 continue;
          ]
          /*    RB     user_count        沾 客户 连接        的  相关 数据 *7
                                                    =

          users(user_count].connfd                    connfd; =
          /*    在 主       和 程   子 进 程 问 建 立 管       。 以  传递      的数据
                                                道        必要 “/
          ret   =  socketpair(      PF_UNIX, SOCK       STREAM,  0,            );
          assert(         ret   !=  -1  );
          pid_t     pid   =     fork();
          if(    pid <    0     )
                                                                               。
          |
                 close(       connfd
                 continue;
          }
          else     if(  pid      ==  0  )
          {
                 close(       epollfd    );
                 close(       listenfd    );
                 close(       users[user_count].pipefd[0])           );
                 close(       sig_pipefd[0)        );
                 close(       sig_pipefd[1)        );
                 run_child( user_count,               users,    share_mem  );
                 munmap( (void*)share_mem,                   USER_LIMIT *  BUFFER_SIZE );
                 exit(    0     );
          }
          else
          {
                 close(       connfd    );
                 close(       users[user_count).pipefd(1]            );

## Page 279

第 13章 多 进程 编程 <> 261


                  addfd(             epollfd,   users[user                     );
                  users(user_count).pid                   = pid;
                                         连接                                 值  程      值之
                                                                                                        加
*/                /*   记录 新 的                     在       users 中 的 索引 ， 建 立 进 pid    和 该 索引
                  sub_process[pid)                =    user_count;

           }
      ]
      /*   处   入 号     事件 “/
      else    if(  (   sockfd   ==        sig   pipefd[0]   )  && ( events[i].events & EPOLLIN         ) )
      {
           int    sig;
           char                signals[1024];
    ret = recv( sig_pipefd[0], signals, sizeof( signals , 0 );
           if(    ret  ==   -1   }
           {
                  continue;
           }
           else    if( ret      ==     0    )
           {
                  continue;
           }
           else
           {
                  for( int     i     =   O07;   i   <  ret; 441i  )
                  i
                          switech(                         )
                       {
                            /*                                                 TR */
                            ease         SIGCHLD:
                            {
                                 pid_t   pid;
                          int        stat;
                                 while (     (    pid  =  waitpid(    -l,   &stat, WNOHANG ) ) >     Oo  )
                                       {
                                                       用 子 进 程的  Pid  取得 被关闭 的客户 连接 的 编号         */
                                                int    del_user   =   subprocess[pid];
                                                sub                    =    -1;
                                                if(           <0)        || ( del_user > USER_LIMIT  )   }
                                                {
                                                       continue;
                                                }
                                                /*     WEB  del   user 个    客户 连接  使 用 的四基数 据    */
                                                              epollfd,      EPOLL_CTL_ DEL,
                                                            users(del_user].pipefd[0),     0     );
                                                close(                          );
                                                users[del_user]       =  users[(--user_count];
                                                                                =     del_user;
                                       }
                                       if({     terminate     £&  user_count   ==  0   )
                                       {
                                                stop   server    =  true;
                                       }

## Page 280

篇  性
262 第 二 深入 解析 高 能 服务 器 编程




                                    break;
                               I
                               case   SIGTERM:
                               case   SIGINT:
                               {
                                                            “/
                                    /*                 BRBSBEM
                                                 "kill     all  the clild   now\n"
                                    if(   user_count     ==     0 )
                                    {
=                                         stop         =        true;
                                          break;
                                    }
                                    for{  int    i    = O; i    < user_count; ++i )
                                    {
                                          int    pid    = users[i].pid;
                                          kill(    pid,    SIGTERM    };
                                    }
                                    terminate       =   true;
                                    break;
                               }
                               default:
                               {
                                    break;
                               }
                       }
                 )
          }
     }
      /*           子        进 程 向 父 进程 写  了   数据   */
     else    if(   events[i]).events       &     EPOLLIN   )
     {
          int    child   =     0;
          /*                        child 变量     记录 了     是 哪个  客户  连接 有    到 达 *7
          ret    =   recv(     sockfd,      (   char*   )&child,    sizeof( child , 0 );
          printf(      “read       data  from    child    accross   pipe\n" );
          if(    ret   <=     -1   )
          {
                 continue;
          }
          else     if(  ret      == 0  )
          {
                 continue;
          ]
          else
          [
                       向  除 负责
                   /*               处理第     child 沾 客户     连接 的 子 进 程    之外 的 其他子 进 程 发 送 消息 ，通
      */
                 for(   int      j  = 0;    j <  user_count;       ++  )
                 {
                       if({                            !=       sockfd   )
                       {

## Page 281

 第 13 章 ”多 进程 编程 « 263


                                         printf(     "send data  to child     accross  pipe\n" );
                                         send(                            (   char*  )&child,
                                             sizeof(     child        0  );
                                 }
                            }
                         |
                   ]
             }
         }

                         ();
         return   0;
     }

     上 面    的  代码有      两 点 需要 注意 :
     CO  虽然 我 们 使   用      了 共享 内       ，但 每    个  子 进程    都只 会  往 自己     所 处 理 的               客户 连接
                                   存                                                            所对应的那
 只                                                                             是    为了      “共享
            部 分 读 缓存 中      人     数据    ，所以 我 们       用 共享 内 存      的 目的                            因
        一                   写                        使                                      读"。    此，
        每 个 子 进程 在 使用共享 内 存的时 候 都 无 须                  加    锁 。     样做                 室    器      应用
                                                       这
                                                                                         符合 “聊天
                                                                                         服务        ”的
                           了          性能。
        场景 ，同 时 提高                     程序
     配了足够
CD 我 们 的 服务 器 程序在 启动的时 候 给 数组 users 分 多的 空间，使 得它可以 存储
        所 有 可 能 的 客户 连接      的    相关 数据 。 同样 ，我们       一         次    性 给 数组 sub_process    分 配   的空间
        也 是以 存储所 有 可 能 的 子 进 程 的             相关      数据   。这 是 牺牲        空间 换取 时 间     的 又 一 例子 。
        ”消息
 13.7        队列

                                                                                                   数据
     消息 队列 是 在 两 个 进程        之   间 传       二    进  制 块    数据 的   种  简单有    效 的 方式      。 每 个
                                           递           一                                          块都有
     特定 接 收 可 以 根据 类 型 来                               地 接收 数据       而 不       一 定      像 管 和 命名 管道 那
 一 个        的类型，           方                       有选择                ，                  道
        以先进先出    的 方式       接收 数据       。
 样 必须
                         的 API   都定义       在 sys/msgh 头 文件 中         ，包
     Linux     消息 队列                                                        括 4  个            msgget、系统调用 ，
 msgsnd、msgrcv 和 msgctl。     我 们 将 依次讨论 之            。

 13.7.1     msgget     系统 调用

                       系统调用 创建一个 消息 队列，或者获取                                     个 已有的 消息 队列。其 定义
     msgget                                            一                                     如下:
     #include <sys/msg.h>
     int msgget( key_t     key, int     msqgilg   );
                                             是一个键      值    ，       标识      个            的 消息
和 semget 系统 调用 一 样 ，key 参数 用 来 一 全 局 唯一 队列 。
     msgfig    参数 的 使    用 和 含义  与 semget     系统     调用    的 sem_flags     参数 相同    ，
                           一个正 整数            ，    它 是 消息 队列 的 标识              。msgget        失败
     msgget    成功   时 返回                     值                        符                     时返回  -1， 并
 设置  errno。
         果      用     创建
     如                       消息队列，           则 与     之关联 的 内 核 数 据   结构                         将 被 创建
            msgget  于                                                         msqid_ds             并初始
 化 。msqid_ds   结构 体      的 定义 如 下  :

## Page 282

    264 %* 第 二篇 深入 解析 高 性 能服务 器编程


    struct      msqid ds
    [
                struct   ipc    perm       msg_perm;                             消息 队列 的 操作 权限        */
                time_t   msg_stime;                                     /*       最    后  一  次    调用  msgsnd  的  时 间
                time   七 msg_rtime;                                              最    后  一  次 调用     msgrev  的  时间  +/
                time_t   msg_ctime;                                     /*       最    后         一 次被修改 的 时间  */
                unsigned long          msg_                             /*       MERA                           «/
                msgqnum_t    msg_qnum;                          .       /*                                      */
                msglen_t                                                /*       消息 队列 允许 的 最大字 节 数               */
                pid_t          msg_lspid;                               /*       最后 执行         msgsnd 的 进程      的EID   */
                pid_t          msq_lrpid;                               /*       最 后 执行        msgrev 的 进程      的 PID  */

    13.7.2      msgsnd          系统 调用
                系统 调用 把一条 消息 添加到 消息 队列                                      。      其定义
    msgsnd                                                                  中                    如下:
    #include
    int         msgsnd(     int msqid,        const     void*       msg ptr, size_t              msg_sz, int      msgflg );
    msqid       参数 是由        msgget          调用 返回     的       消息 队列标识 符                。
    msg_ptr         参数 指向   一           个    准备 发 送    的 消息         ， 消 息 必 须 被            定义     为 如下 类 型     ;
    struct      msgbuf
    |
                long   mtype;      /*        消息 类型  +/
                char   mtext(512);            /*  消息 数据
    hi
           中                    员                 的            ，                 一                                 消息      。msg
    其           ，mtype      成 指定 消息 类型                          它 必须 是                个 正 整       数 。mtext     是      数据       sz
    参数 是 消息         的  数据 部 分        Cmtext)      的 长          。   这 个 长  度             可以 为 0，       没有           数据。
                                                                                                 表示
                                                          度                                                  消息
                                                                                        仅支持IPC_  NOWAIT
    msgfig      参数 控制           msgsnd            的 行为    。    它 通常                                   标志，           即以非    阻  塞 的
     发送消息。默               认 情 况              下，   发 送 消息 时          如 果 消    息   队 列 满           了， 则 msgsnd       将 阻塞    。若 IPC
    方式
                       被指定 ， 则 msgsnd             将 立即 返回 并             设置       ermo         为     EAGAIN。
NOWAIT 标志
   处 于 阻塞状态 的 msgsnd 调用 可 能 被如 下 两种异常情况 所 中 断 :
   OQ 消息 队列 被 移 。此
                            除                时    msgsnd  调用 将 立即 返回 并 设置                        ermo    为  EIDRM.
    口      程序   接收 到          信和叶            时                                             设置
                            。          此          msgsnd  调用 将 立即 返回 并                           ermo    为 EINTR.
        则返回 -1 并 设置ermo. msgsnd 成功时 将 修改 内 核数据 结构
                            返回       0，                失败
    msgsnd      成功时
                的 部    分 字段       ，    下         所
    msqid_ds                         如          示:
    加 将         msg    qnum     加    1，
           将                 设置
    口           msg_lspid              为调用        进程    的 PID。
                                                  的时间。
           将    msg_stime 设置 为当前

    13.7.3      msgrcv          系统 调用

                                                                                         定义
    msgrcy                                       系统 调用 从 消息 队列 中 获取消息 。其                如下:

## Page 283

第 13 章 ”多 进程 编程 265


     finclude
     int    msgrev(          int        msqid,         void*             ptr,   size
                                                                  msg                t  msg    sz,    long int  msgtype,  int
msgilg    );
                           是由       msgget         调用 返 阿 的 消息 队列 标识 符              。
     msqid        参数
     msg_ptr      参数 用              于            存储接收    的 消息       ，msg_sz   参数 指  的   是 消息 数据 部 分 的 长 度      。
                  人参数 指定 接收 何种                            类 型 的   消息     。 我
     msgtype                                                                  们可以 使     用    如 下 几 种   方式 来 指定 消息 类 型
     口    msgtype          等于       0。           读 取消 息 队 列 中 的 第          一  个 消息 。
                             于      0。           读 取消 息 队 列 中 第            一  个 类 型 为   msgtype    的    消息 《除非 指定    了     标志
     CO   msgtype          大
          MSG     _                              见 后文
                                    EXCEPT，          )。
                                    0。于          读 取消 息 队 列 中 第 一个类 型 值比                msgtype    的 绝对 值     小 的 消息 。
     口    msgtype          小
     参数                              控制    msgrev          的行为         。 它 可 以 是  如  下  一     些标志  的 按  位  或 ;
     UIPC_ NOWAIT。                      如 果 消               队 列   中没有 消息 ， 则         msgrcv            调用 立即 返回 并 设置  errno 为
          ENOMSG.
                                                                       于 0，   则 接收 消息 队列 中 第          一    个非 msgtype   类 型 的
     加                                           MR msgtype       大
          消息。
     口      MSGNOERROR。 如 果 消 息 数据 部 分 的 长 度 超                                      过   了     msg_sz， 就    将   它 截断。
     处 于 阻塞状态 的                         msgrcv     调用 还 可 能 被如 下 两 种 异常情况 所 中 断                      :
                                                   时     msgrev  调用
     QD                                    消息队列被移 。此
                                            除                            将立即返回 并 设置         errno     为 EIDRM。
                                                   时     msgrev    调用 将      立即返回    并  设置    errno   为 EINTR。
     口 程序 接收 到 信号                               。  此
     msgrcy 成              功 时 返回              0，        失 败 则 返回   -1   并 设置   ermo。msgrcy          成 功时       将 修改 内核数 据 结构
                           字段           ，如         下 所 示 :
msqid_ds      的 部    分
                          将msg qnum          减 1。
     口
              msg_lrpid 设置将
     口                                             为调用进程的          PID.。
           将                        设置
     口        msg_rtime                                    为当前的时间  。
13.7.4        msgctl                系统 调用
                                               调用控制消息队列
                  系统                                                的某些属性 。其定义
     msgctl                                                                                如下:
     #include <sys/msq.h>
     int    msgctl(                 int    msqid, int      command,   struct      msqid_ds*    buf    };
                  参数                msgget                 调用 返回  的 共享 内 存 标 识       符  。command           指定 要        。
     msqid                           是由                                                                参数      执行 的命令
msgctl    支持 的所有 命令如 表 13-4                                所 示 。

                                                                                        支持
                                                                                        命令                     ，
                                                           % 13-4      msgetl     的

IPC_STAT                                                                                                0

                  内 核 数      据      中 的                                 被
一                                       msqid_ds.msg_ctime             更新

## Page 284

    266 第 二篇 深入 解析 高性能服务 器 编程


    (SE)



                 用立即
        一                      返回并设置   erno    为 EIDRM)
                        获取 系统 消息 队列           源 配 置信息，将结果 存储 在            中。
                 |          将      成 msginfo        来               buf 应 用 程序           有 有            ，
    IPC_INFO     需要 buf 转换                       结构 体 类 型     读 取 这 些 系统 信息 。msginfo
                                             似，这 里 不 再
                 结构 体与seminfo 类

    =                                                                                =

                        与 JPC_STAT     似，      过此时                               |
                                类 不              msqid                               内核 消息 队列信息 数组 中
   符，                          而 是 内 核消息     队列 信息 数组 的 索引     《每 个 消息   队列的信息 都 是|  索引 值  为msqid 的 消息 队列
    MSG_STAT     |识
该 数组 中 的                        一     项 )                                           的 标识 符

                                                                    如 13-4   所 示 。msgctl 函数 失败时 返回      -1
             msgctl     成功时的 返回值取决于              command   参数    ，     表
    并 设置       ermo。

    13.8                  IPC 命令

                 述                               间                  用      唯一            值
             上     3  种   System V IPC 进程        通      方式 都 使         一 个 全 局      的 键    (key)  来 描述   一
    个 共享 资源             。 当   程序  调用     semget.   shmget 或者msgget     时 ，  就 创建    了这 些 共 享  资源    的 一 个 实
    Bi].     Linux        了 ipes      ，                    系统 拥有 哪些                      。               机器
                 提供             命令            以 观察 当前      上               共享 资源 实例      比           如 在 测试
                                             :
    Kongming20                   上 执行 ipcs 命令

             $ sudo     ipcs
             ------     Shared  Memory     Segments    --------
             key            shmid            owner     perms        bytes     nattch     status

                        Semaphore      Arrays --------
             key            semid            owner     perms        nsems
             Ox00000000     196608           apache     600            1
             Ox00000000     229377           apache     600            1
             0x00000000     262146           apache     600            1
             Ox00000000     294915           apache     600            1
             0x00000000     327684           apache     600            1
             0x00000000     360453           apache     600            1
             0x00000000     393222           apache     600            1

             一 一 一 一 一一 Message Queues       --=------
             key            msqid            owner     perms        used-bytes     messages
                                                                                         见，   该   系统    目前
             输出 结果 分                显示了 系统    拥有   的 共享 内     存、信 号 量  和 消息 队列 资源    可 。                 尚
        量。这
未 使用任何 共享 内存和 消息 队列 ，却 分配了 一 组 键 值 为0 (IPC_PRIVATE) 的信号 些信和号
  的所有者是 apache， 因 此它们
        是由 httpd 服务 器 程序 创建
        的。其 中 标识
                                                                                     符为    393222     的 信号
    量                                                                                                    量
    在                          小 节讨 论 的 那个            用于 在     各个子  进 程  之 间 同 步         使    用     权 的 信号
    正 是 我 们 13.5.5                               httpd                     epoll_wait                   量。
   外，                           用                系统                        资源
   此 我 们 可以 使 ipcrm 命令 来 删除 遗留 在 中 的 共享 。

## Page 285

                                                                                      第 13     章   ”多 进程 编程 %* 267

                   进程                        文件
      13.9     在         间 传 递                         描述 符

            于  fork 调用 之        后    ，父 进程        中    打开 的 文件 描述 符 在          子  进程 中 仍然          保持 打开 ，   所 以 文件
        由
      述 符 可 以  很 方便      地 从 父 进程       传递                      。                 ，                文件            传递
                                                  到    子     进程    需    要 注意 的 是      传   递  一  个      描述 符 并 不 是
      一 个 文件描述 符 的          值   ，而 是 要 在 接收                  进程 中  创建 一个     新的   文件 描述         符  ，并 且 该 文件   描述符和
      发 送 进程 中 被 传递的 文件 描述 符 指向 内 核中相同 的 文件 表                                         项 。
                                                                                                    地说，        如 何在
        那么     如 何 把 子 进程 中 打开 的 文件描述 符 传递给                                       呢父 进程  ? 或 者 更      两个
     通俗
不 相干的 进程 之间 传 递 文件 描述 符 呢 ? 在 Linux 下， 们
     我 可以 利用 UNIX 域 socket 在 进程间
                         特殊的辅助数据，                                                     清单            出      ，      在
      传递 以 实现 文件 描述 符 的 传递                                             代 码                13-5    给 了    一 个 实例   它
      子 进程中打开            个 文件描述
                    一                      符，     然    后     将 它 传递 给 父 进程 ，    父 进程 则 通过 读 取 该 文件    描述        符 来
      获得 文件的 内 容        。

                                       代码 清单 13-5 ”在 进程 间 传 递 文件描述 符

        #include    <sys/socket.h>
                    <fentl.h>
        #include    <stdio.h>
        #include    <unistd.h>
        #include    <stdlib.h>
        #Hinclude   <assert.h>
                    <string.h>

        static  const    int    CONTROL_LEN              = CMSG_ LEN( sizeof(int)     );
        /*   发 送 文件描 述   符  ，fd     参数是      用 订       递 信息  的 UNIX   socket, fd_to     参数 是 待 发    送    的 文件描述特 */
        void   send_fd(     int     fd,     int     fd   co send     )
        |                                                             :
              Struct     iovec  iov[1];
              struct     msghdr     msq;
              char  buf[0);

              iov[0].iov_base          =    buf;
              iov(0).iov_len         =     1;
              msg.msg_name                  NULL;
              msg.msg_namelen          =    0;
              msg.msg_iov              -    iov;
              msg.msg_iovlen         =     1;

              emsghdr    cm;
              cm.cmsg_len     =              CONTROL_LEN;
              cm.cmsg     level     =
              cm.cmsg_ type     =             SCM RIGHTS;
              *(int     *)CMSG DATA(    &cm         )   =
 _            msg.msg_control          =               /*              */
              msg.msg_controllen             =  CONTROL_LEN;

              sendmsg(   fd,    smsg,        0  );
        }

## Page 286

篇  性 编程
二
268 +s 第 一 深入 解析 高 能服务 器


7*   接收 目标     文件        特    “/
int  recv_fd(      int   fd     )
{
     struct    iovec iov[1];
     struct    msghdr        msg;
     char  buf[0];

     iov[0].iov_base            =  buf;
     iov(0].iov_len           =   1;
     msg.msg_name               =  NOLL;
     msg.msg_namelen            =  Q;
     msg.msg_iov                =  iov;
     msg.msg_iovlen           =   1;

     emsghdr    cm;
     msg.msg    control         =  &¢m;
     msg.msg_controllen              =  CONTROL_LEN;

     recvmsg(     fd,    &msg,       0 );

     int   fd_to_read         =   *(int    *)CMSG_DATA(     écm );
     return    fd_to_ read;
}

int
{
     int   pipefd[2];
     int   fd_to_pass         =   0;
                   QR, FH            Pi.   KABA                       是
                                                    pipefd(0)     # pipefd[1] 都 UNIX M socket */
     int   ret  =  socketpair(          PF_UNIX,        SOCK_DGRAM,  0, pipefd );
     assert(    ret    !=     -1   );
                                                    ;

     pid_t  pid    = fork();
                pid    >=     0   );

     if  ( pid    ==   0     )
     {
           close(   pipefd[0)          );
           fd_to     pass     =   open(     “test.txt",     O_RDWR, 0666   );
                       通过               文件            送           。     件        ，     将
           /*  子 进 程                 管道 将       描述   发     到  父 进   如 果 文      打开 失败           标
                                                         程            test ,txt      则      子 进程
                RRA RHE         */
                               pipefd[1],   (   fd_to_pass    > 0 )  ?  fd_to_pass : 0 );
           close(            fd_to_pass  };
           exit(   0   );
     ]

               pipefd[1]          );
                                                                     进程     接收     描述符
     fd_to_pass     =          recv_fd(  pipefd[0]   );      /*   父     从 管道   目标 文件     */
     char  but
     memset({   buf,     '\O"',      1024 );
     read(  fd_to_pass,           buf,   1024   );   /*               */
     printf(    "I    got     fd  td    and    data ts\n",            buf     );
     close(    fd_to_pass          });
}

## Page 287

 编程
 第 14  多 线程


     到 1996 年 ，Xavier Leroy 等人才 开 发出第 一 个 基本 符合
      早期     Linux 不 支持 线程                         ，直
     低而且问
 标准 的 线程库LinuxThreads。 但 LinuxThreads 效率 题 很 。自 内
 POSIX                                                                                                 多      核2.6 FF
    才 真 提 供 内 核 级 线程 支持 ，并 有 两个 织 致力 编写
 始 ，Linux    正                                  的                    组                  于      新的线程库 ，           NGPT
                                                                                                          )。不 过 前 者 在
 (Next Generation POSIX Threads)                 和                 (Native POSIX Thread Library
                                                                                                       高，       且 更 符
 2003     年 就 放奔了    ，    因 此 新           的     线程库就 称为            NPTL。NPTL     比 LinuxThreads        效率
 合 POSIX 规范                    ，所以 它已    经   成为glibe     的                               部 。本 书 所 有 线程相关 的 例 程 使用的 线程
 一                                                                 分                                   库都
 是 NPTL。
                                                                                 简 pthread)                      标准 而
                                                                                                                   限于
      本 章 要讨论 的 线程                       相关  的 内 容     都         属于POSIX 线程 〈           称              ，     不局
                       体包括:
 NPTL     实现             ，具
      口 创建     线程    和 结束 线程              。
      CD  读 取 和 设置 线程属性                   。
      口 POSIX                      线程 同步 方式        ，POSIX    信和  号 量、互 斥锁    和 条件 变量     。
      在 本 章 的 最           ，我  们     还        将          在            下，             、     程、           程序
                       后                               介绍    Linux 环境 库                 进 信 号 与 多 线程              之 间
 的 相互 影响    。

 14.1         Linux    线程 概述

 14.1.1               线程 模型

                                                                                                                   运行
         线程 是    程序           一中 完成 个 独立任务 的     完整执行 序列 ，           即           一  个 可 调度 的 实体 。根 据               环境
           者的    身份    ，  线   可 分 程          为  内    核 线程        和 用 户 线程     。内 核  线程     ，在有 的 系统上 也 称为         LWP
 和 调度
                                                 )，运行在内 核 空间，由 内 核 来 调度                            用
 (Light Weight Process，       轻              级 进程                                       ，                    户线程 运行在用
          ，                     由 线程
                         库来       调度。      当 进程     一            个 内 核 线程 获得 的 使        用
 户 室 间                                           的                                CPU        权时    ，   它 就  加 载  并 运行
   个用 户 线程        。       见，                        相                        ”。    可以拥有
 一        可 内 核 线 程                              当 于 用 户             线程 运行   的   “容器     一                   个 进程 MTA
                                             中  M < N。             并 且 在 一 个 系统  的 所 有 进程 ，M           和 六         比值
                                  其
 核 线程 和                  N 个 用 户 线程 ，                                                   中                          都是
          的。 照 MON               的 取
 国定       按                       值，         线 程 的    实现 方式          可 分 为 三 种  模式  完全 在 用 户 空间 实现                 、完
                                                                                    :                              全由
 内 核 调弃    和 双 屁 调度        〈two level            scheduler).
完全 在 用 户 空间 实现 的 线程 无须内 核 的 支持 ，内 核 甚 至根本 不 知道这 些 线程 的 存在 。线
    库负责           所                          ，      如            的优先
                                                           线程                       等                  longjmp
 程 管理 有 执行 线程 比                                                      级、      时 间 片          。线 程 库 利用           来切换 线
    的                                        是                       的。      实际             核 是 把  整个进程        作为 小 单
 程
       执行 ， 使 它们 看 起 来 像                        “并 发 ”执行             但         上 内      仍 然            最
    来  调度    的。换
 位        句               话说，              个 进程 的    所    有 执行       线程    共享    该进程    的               时间 ，它们 对 外 表现
                                  一                                                     片                          出相

## Page 288

    270      第 二      篇  深入   解析    高 性    能服务 器      编程

       的优先级。因
    同                    此，对 这 种 实现 方式 而                 言   ，N=1， 即   M    个 用 户 空间 线程对 应                    1     个 内  核 线程 ，
    面 该内核 线程 实际上 就是进程 本                    。      完 全 在 用 户       空间  实现     的                线程 的 优点是                和      线程
                                                                                                                             创建
                                           身                                                              ，        调度
    都 无须内 核的 干预 ，因 此 速度相当                             。并 且 由 于 它 不 占用 额外 的 内 核资                               ，              即使
                                                  快                                                     源     所以            一个进
    程 创建 了很 多 线程 ，也 不 会对系统 性 能 造成明显 的                              影响    。  其 缺点  是        ，     对 于 多 处 理 器 系统     ，         一
    个 进程    的   多 个 线程 无法运行 在 不同           的 CPU 上           ，因 为 内 核       是  按照 其 最       小     调度 单位 来 分    配            CPU
    的 。     外，  线 程      的优先  级   只 对 同一 个 进程              中 的 线程有     效    ，比 较 不 同               进程中  的 线程    的        优先 级 没
         此
    有        早期的伯克利 UNIX 线程 就是采用 这 种 方式 实现                                  的  。
         完全 由 内 核 调       的   模式 将 创建             、    调 度 线程的 任务        都  交   给 了 内        核  ， 运 行在        用       户  空间 的 线
    程库无 须 执 行 管理 任务 ，这 与完全 在 用 户 空间 实现 的 线程恰恰                                           。    二    者 的 优 缺             点  也 正好 互
    换。较 早    的 Linux     内 核对 内 核        线程     的     控制 能   力有     限， 线 程     库   通 常 还       要 提供 额外 的 控制    能    力         ，
             是线程同步机制，                                          经                                        。                    调度
    尤其 不 过 现代                              Linux      内 核 已              大 大 增强 了 对 线程     的 支持               完 全 由 内     核
    的 这 种 线程实现 方式 满足      M:N=s1:1，        即 1           个 用 户   空间 线程 被           映射为  1      个 内 核 线程
                   双 层 调度 模式 是 前 两种       实现 模式 的 混合         体        调     度   M 个 内   核 线程        ，         线 程 库
                                                                                                              调度
    用 户 线程     。     这 种 线程实现  方式          结合   了 前 两 种 方式         的 优点  :   不 但 不               会 消耗过  多 的 内     核资     源 ，  而
    且 线程    切换 速度       也 较   快 ，同 时      它可 以 充分 利用 多 处 理 器 的 优势               。
    14.1.2     Linux             库
                                线程

                                           库是     LinuxThreads 和 NPTL，          它们 都              采用是         1:1 的      方式  实现
         Linux  上 两 个最有 名 的 线程
    的 。    于                              的时候                                   支持                有           ，  可
         由     LinuxThreads     在 开发                  ，Linux 内 核对 线程 的          还非常 限                           所以 其     用 性  、
         性以及    POSIX 兼容
                          性都远远不及                      NPTL。    现
    稳定                                                             代 Linux   上 默认 使         用    的 线程 库           是NPTL。      用
       可 以 使 用  如     下 命令 来 查看 当前         系统       上 所  使   用 的 线程   库 :
         S$ getconf   GNU_LIBPTHREAD_VERSION
         NPTL   2.14.90

         LinuxThreads    线程 库     的    内 核         线程是用  clone   系统 调用 创建的      进程    模拟                      的 。clone    系统 调用
    和  fork 系统 调用        的   作用 类 似    :  创建    调用     进程 的 子 进程        。   不 过 我们 可 以              为clone             系统 调用 指定
    CLONE_THREAD 标志       ，    这 种        情况    下 它 创建       的 子    进 程 与   调用  进程 共享 相同 的 虚拟                 地 址    空间       、
文件描述符和 信和号 处 理 数 ，这 些 都 是 线程的特点。不
                                                                            过，  用 进程 来模拟                内     核 线程    会  导致   很
    多 语义 问题           ， 比
         口 每     个       拥有不同的    PID，     因 此 不 符合                规范
                                 线程                            POSIX        。
         DO Linux     信号 处理本 来是基于 进程                  的  ，   但 现在 一 个       进程  内 部 的      所    有 线程 都 能      而         且 必 须 处
                信和  号。
    理
                 户ID、    组 ID   对 一             个 进程中的 不 同 线程来说 可 能 是 不 一 样的                。
         加 用
         口      程序    产生 的 核心   转 储    文件 不    会      包含   所有 线程    的  信息    ，  而 只 包含      产生                    该 核心 转 储 文件 的
                      的信息。
            线程
         口 由 于 每个线程 都是            一    个  进程    ，因 此 系统 允许 的           最 大 进程    数              也 就 是最  大 线程 数     。
         LinuxThreads     线程库一个有 名 的 特性            是 所 谓 的         管理    线程     。              它 是 进程中  专门    用   于 管理    其   他

## Page 289

   第 14 章 ”多 线程 编程 “* 271


   工作 线程的
             线程 。其 作用 包括           ;
   口 系统 发 送         给        进程 的 终止   信号 先 由 管理 线程 接收           ，                 管 理 线程 再 给 其 他 工作 线程 发         送     同样
                以终止它们，
                             的 信号
   口 当 终                  工作 线程 或  者   工作 线程 主动 退出 ，管理线程 必须 等 竺                                         结束            以 避免
             止                                               时                                                ，          己
       进程
   口 如 果 主 线程     先    于 其 他 工作 线程 退出     ，则 管理线程                                  将 阻塞    它           ，直 到 所 有 其他 工作 线程 都
                       之后才唤 它
                           结束   。
   CD    回收 每个线程 堆栈 使用的 内 存            。
   管理               ，                                        。并                        能 运行             一               ，所
             线程 的 引入            增加 了 额外     的     系统 开销          且 由               于 它 只             在  个CPU 上           以
                    库也不         能 充分 利用 多 处 理 器 系统 的             优势                。
   LinuxThreads 线程
   要     解决     LinuxThreads     线程 库    的    一     系列 问题    ，    不 仅 需 要 改进 线程              库 ，           最 主要 的 是 需要 内 核
                                                                      开始 ，提供 了 真正                       的 内  核 线程 。      的
   提供 更 完善    的 线程 支持           。 因 此        ，Linux   内 核从 2.6    版本                                                     新
库也应运而生。                           相
   NPTL  线程                            比      LinuxThreads，NPTL   的 主要 优势                    在于 ;
   CO    内   核  线程     不 再 是    一个 进程        ，因 此 避免 了 很 多 用 进程 模拟 内 核                                线程导致的语义问题        。
                                              止线程 、回 收 线程    堆栈   工作 都 可        等      以     由 内 核 来完成。
                                             终
   OC)   据   弃  了 管理 线程         ，
   口 由 于 不 存在             管理    线程 ，所以              个 进程   的 线程   可以               运行在    不  同        的 CPU 上           ，从
                                              一                                                                       而充分利
              用 了 多 处 理    系统 的 优势        。
   口 线程的 同步         由              内 核 来 完成  。隶    属于  不 同   进程   线程 之间 也 能 共享 互的                            锁，   因 此 可 实现
                              同步。
                               线程
       跨 进 程      的
                    线程和结束                        线程
   14.2                                         ”创建

                                                                                    上，它      们 都 定义
   下 面 我 们讨论 创建 和 结束 线程                的    基础 API。Linux 系统                                              在   pthread.h 头 文
   中。
   件
   1.    pthread_create
  一个            线程的             是                                              其 定义
   创建               函数               pthread_create。                            如下;
   #include     <pthread.h>
                                                                                                 t*  attr,
   int       pthread   create(    pthread_t*     thread, const                        pthread_attr_
                                  void*     ( *start_routine                   )(  void* ,   void"   arg   );
   thread     参数 是    新    线程        的 标识     ，        后
                                       符             续                             函数   通过   它 来 引用 新 线程          。其 类 型
                       定义
   pthread_t 的        如下:
   #include     <bits/     pthreadtypes.h>
   typedef     unsigned    long        int    pthread_t;
   AY               t             个 整 型     类     。
                                                     实
             pthread     是  一          型     际上                            ，Linux  上 几乎   所  有 的 资源 标识 符 都        是一    个 整
       数，    比
             如  socket、  各
                    种           System V IPC     标识
   型                                   符等。
       用 设置新线程的 属性。给 它 传递 表示 使用默认 线程 属性。线
   attr      参数   于                                          NULL                                                 程拥有

## Page 290

    272    *:*     第 二  篇 深入 解析 高 性    能  服务 器 编程

多 属性 ，我们 将 在 后 面详 细讨论 。start_routine 和 参数 分 别 指定 新 线程将运行的 及
                                           之                         arg                              数
    其 参数。
        pthread_create    成功时 返回   0，      失 败 时 返回                  码。                       数量        能
                                                             错误          一   个 用 户 可以打开 的 线程               不 超
                                               表        7-1)。 此
    过 RLIMIT_NPROC 软 资      源                                外，      系     统 上 所 有 用 户    能 创建 的 线程 总 数 也 不
                                   制 限  〈 见
得 超过 /proc/sys/kernel/threads-max 内 核 参数 所 定义 的 值 。
   2. pthread_exit
        好，内 核
   线程 旦 被 创建
                 一                     就可以          调度 内 核 线程 来      执行      start_routine 函数 指针所 指向    的    函
      了。                 在                                               、                ;
数 线 程 数 结束 时 最好 调用 如 下 函数 ，以 确保 安全 干 净 地 退出
   #include <pthread.h>
        void pthread        void*    retval              );
        pthread_exit      函数 通过 retval 参数           向    线程 的 回收  者 传递 其       退出   信息    。它 执行完 之 后 不  会    返
                 者，     而 且永  远不 会  失败 。
    回 到 调用
        3.   pthread_join
        一        个 进程 中 的所有 线程       都  可以     调用                    函数 来      回收   其  他 线程〈       前 提 是 目标 线程
      可          的，                     其                    ，                        的      和               调
是
    回收 见 后 文 )， 即 等 待 他 线程 结束 这 类 似 于 回收 进程 wait
        waitpid 系统
        如下:
用。pthread join 的 定义

        #include        <pthread.h>
        int      pthread  join(                thread,       void**  retval    );
        thread     参数 是 目标 线程的标识                    ，retval   参数 则 是 目标 线程          返回 的  退出 信息   。该        会一
                                               符                                                            函数
        成功时返回 失败
直 阻 ，直 到 回收 的 线程 结束 为 。该 函数
             蹇                                 止                                      则返回  错误    码。    可 能 的 错
    误 码   如  表     14-1 所 示 。

                            表 14-1        pthread_join       函数 可 能 引发 的       错误    码
              错误 码                                           mm
                            死锁，    比 如 两 个 线程       豆    相 针对 对 方 调用 pthread_join，   或 者 线程对白 身 调用 pthreadjoin
 EDEADLK | 可 能 引起
 EINVAL 目标 线程 是 不可回收 的，或 者 已 经有其 他 线程 在回收该目标 线程
 ESRCH 目标 线程不存在

   4. pthread_cancel
   有 时 候 我 们 希望异常 终止一个 线程 ， 即 取消 线程 ，它 是 通过 如 下 函数 实现 的 ;
        <pthread.h>
   int pthread cancel({ pthread_t thread );
                                                        。      成功    时
                                                                         返回           则返回     码
   thread 参数 是 目标 线程 的 标识 该函数 0， 失败。 过，
                                                    符                                         错误     不
       接收 到      请 求 的目标 线程 可以 决定是                       否 允 许 被 取消以 及         如  何 取消   ， 这 分 别 由 如 下 两个    函
         数 完成

   #finclude <pthread.h>
   int pthread setcancelstate( int state, int *oldstate };
        int                               int           type, int              );

## Page 291

                                                                                   第 14  章 多 线程         编程 *%*  273

    这 两个                         第     个 参数 分 别 用     于 设置 线程     取消               状态    是 否 允 许 取      )》     和 取消
                            的       一                          的                   〈                消            类型
    何取消)，第                  二    个 参数 则 分 别 记录 线程 原来的取消 状态和取消 类                    。state                      有两个可
                                                                                   型                             参数
  值:
选
   口 PTHREAD_ CANCEL ENABLE， 人允许线程 被取消。它 是 线程 被 创建时的 默认取消 状态 。
    口 PTHREAD_CANCEL_DISABLE，                                  禁 止 线 程被            下，    如 果                    个线程
                                                                      取消。这 种 情况                         一        收到
          取消 请        求     ， 则     它 会将请 求 挂     起 ，直 到 该 线程允许被取消        。
    type                      参数 也 有 两个可 选值 :
    口      PTHREAD                                             线 程             随    时 都  以被取消      。它 将 使      得 接收
                        CANCELASYNCHRONOUS，                                        可                              到
                  请求的    目标          线程立即 采取 行动                。
          取消
                                                                                   推迟行动，直 到        它 调   用了 下 面 几 个
    J PTHREAD_CANCEL_ DEFERRED， 人允许 目标线程
          所 谓       的 取消 点 函数        中     的 一 个    :             pthreadjoin、pthread_testcancel、pthread_cond_wait、
          pthread_cond                          timedwait、sem_wait
                                                                 和        根
                                                                                   据                 标准 ，其 他 可 能 阻塞
                                                               sigwait。 POSIX                                    的系
                                  如 read、wait，   也 可以 成为
          统 调 ，比                                               取消
                      用                                             点。不 过为了 安全 起     见             ，我    们 最 好 在 可 能
    被             取消 的 代码
          会                             中调用  pthread_testcancel     函数以 设置 取消 点     。
   pthread_setcancelstate 和 pthread_setcanceltype 成功时 返回 0， 失败 则 返回 错误 码 。

    14.3  ”线程属性

                        结构
    pthread_attr_t                   体定义    了  一 套 完整的线程 属性 ，                  如    下 所  :
    #include                 <bits/ pthreadtypes.h>
    ¥define         SIZEOF_PTHREAD ATTR_T                 36
    typedef         union
    {
                                                       ATTR_T);
          char      size[_SIZEOF_PTHREAD
          long    int     _ align;
    }     pthread       attr  t;
           见，     各 种 线程            属性   全 部 包含     在          个     字符 数组     。    线 程 库 定义了      系列            来操作
    可                                                     一                    中        一
    pthread attr  t   类 型    的 变量        ，以 方便 我 们 获取 和 设置 线程 属性                   。    这 些 数 包 括  :
    #include            <pthread.h>
                              EMR       “7
    int     pthread_attr_init        (                 pthread_attr_t* attr );
    /*                                                                             */
    int     pthread_attr_destroy     (     pthread_attr_t*                 attr     };
    /*     下 面    这   些    函 数   用  于 获取 和 设置       属  性   对 象 的 其     个 属性 *V/
    int pthread attr_getdetachstate ( const pthread_attr_t* attr, int* detachstate );
    int pthread_attr_setdetachstate ( pthread_attr_t* attr, int detachstate );
    int pthread_attr_getstackaddr (const pthread_attr_t* attr, void ** stackaddr );
    int pthread_attr_setstackaddr ( pthread_attr_t* attr, void* stackaddr );
    int                                                (   const     pthread_attr_t*     attr,          stacksize );
    int                              setstacksize      (   pthread_attr_t*         attr,  size_t stacksize);
       int  pthread     attr_getstack     ( const              pthread_attr_t*     attr,    void** stackaddr,       _
                                                 size          t*     stacksize);

## Page 292

    274 > SI ”深入 解析 高性能 服务器编程


    int        (                         pthread attr                  attr,  void*   stackaddr,
                                         size_t           stacksize    );
    int    pthread   attr_getquardsize   (    const       pthread       attr_t      * attr,   size_t*    guardsize  );
    int    pthread   attr_setguardsize      (pthread          attr_t*      attr,       size
                                                                                          t   guardsize     );
int pthread_attr_getschedparam ( const pthread _attr_t* attr, struct
        sched_param* param );
int pthread_attr_setschedparam ( pthread_attr_t* attr, const struct
        sched param* param );
    int    pthread_attr_getschedpolicy             (      const                          attr,   int*    policy );
int pthread ( pthread_attr_t* attr, int policy );
    int    pthread_attr_getinheritsched                 { const    pthread_attr_t*         attr,    int*  inherit);
int pthread_attr_setinheritsched (pthread_attr_t* attr, int inherit );
    int    pthread_attr_getscope (       const            pthread_attr_t*      attr,     int*  scope     );
                                                              t*       attr,   int   scope    );
    int    pthread_attr_setscope (       pthread_attr
    下 面 我 们详细讨论 每 个 线程 属性                   的  含义         ，
                                        状态     。它
    四     detachstate，             线 程 的脱离                有 PTHREAD        CREATE     JOINABLE     和           PTHREAD
          CREATE_DETACH     两个可 选                  。      前 者  指定 线程         是可以 被 回收            ，后 者 使            线程
                                               值                                                 的                  调用
          脱离 与 进程中其 他 线程 的 同步 。
                                                      脱    离   了 与 其     他 线程 同步 的 线程 称                  为“脱离   线程 ”。
               线程在退出时将    自行             释放        其      占用 的 系统 资源          。线 程 创建 时 该          属性      的    默认 值 是
          脱离
          PTHREAD_CREATE_JOINABLE.                        此外，     我们 也 可以 使          用 pthrcad_detach         函数 直接 将
                       设置为脱离线程。
          线程
    C)    stackaddr 和 stacksize，   线 程 堆栈             的   起 始          和 大 小  。一   般 来     说，          我 们 不需要 自己 来 管
               为    Linux     默认 为 每 个 线程 分                            配了 足够
          理 线程 堆栈，因                                                            的   堆栈 空间       〈     一 般   是& MB)。  我
          们  以
            可          使用      命令 来 查看 或 修改                          这 个 默认 值 。
                  护           区 域 大      。如
    C)    guardsize，    保                    果        guardsize 大
               小                                                        于  0，  则 系统        创建 线程         的   时 候 会 在其
               的尾部额外分配     guardsize                      节            ，         保护                错误
          堆栈                                    字 的 空间 作 为                           堆栈 不 被                  地 覆盖 的 区
                   果 guardsize   等于0，    则 系统 不 为 新 创建                    的  线程  设置      堆栈 保护           。        如 果
          域 。如                                                                                     区              使用者
          通过 pthread_attr_setstackaddr    或        pthread_attr_setstack                                           IY
          guardsize 属性 将   被   忽略 。
    L)         线 程 调度 参数                 。  其 类 型          是           param   结构
          schedparam，                                         sched                  体。    该 结构 体 目前 还          只 有 一
          个 整型类 型 的成员         一  一       sched_priority，      该    成员  表 示 线   程   的 运行    优先      级   。
               程
          schedpolicy，     线   调度        策略    。      该 属性 有                              SCHED_FIFO、SCHED_RR 和 SCHED
        选 ， 其 SCHED_OTHER 是 默认 值 。SCHED 表示 采用 轮转 算
          OTHER 三 个 可         值     中                                                          RR
          法 (round-robin)  调度 ，SCHED_FIFO 表示 使 用                           先进  先    出 的 方法 调度      ，     这 两  种     方
                                                                                                                   调度
          法都具备 实时调度 功能 ，但                只 能 用 于以 超级 用 户 身 份                        运行 的   进程 。
    QJ inheritsched，     是 否 继 了            线程 调度 属性                         。
                                                             的                该 属性 有        PTHREAD _INHERIT
          SCHED 和 PTHREAD_EXPLICIT_SCHED                      两        个  可  选 值 。    前 者 表示 新 线程          沿用    其 创建
        调度参数，这 种 情况下再 设置 新 线程的 调度 参数属性 将 没有 任何 效果。后
          者 的 线程                                                                                                    者
          表示 调用 者 要 明确 地 指定 新 线程的调度 参数                        。
    CI    scope,        CPU 的 范围                      ， 即  线程    优先       级 的 有    效 范   围 。POSIX     标准      定义了 该 属

## Page 293

                                                                                             第    14 章 多 线程 编程 fe    275

          性的                 SCOPE                              和                        SCOPE        两     可选
                       PTHREAD                                                              值，                         前
                                             SYSTEM                 PTHREAD
                                                                                            PROCESS     个
          者 表示 目标 线程 与 系统中 所 有 线程 一 起 竞争                                 CPU     的 使 ，      者      仅
                                                                                            后
                                                                                            用      表示 目标 线程          与 其
          他 隶属 于 同一 进程             的    线程     竞争    CPU            的 使      用。      目 前 Linux     只 支持   PTHREAD_SCOPE_
          SYSTEM 这 一         种   取 值 。

                                      信号
  14.4       POSIX                     量
                                               程序一样，多 线程 程序也 必须 考虑 同步问题                     可
       和 多 进程                                                                               以看 作     一              种 简单
  的 线程同步 方式 ，不 过 很 显然 ，它 无 法 高效地 实现 复杂 的 同步 需求                                              ，    比    如       控制对共享 资源 的
 式访问，又 抑或是在 某 个 条件 满足之 后 唤醒 个 线程 。接 下 来 我 们讨论 3 种 专门
  独占                                                                     一                                           用于线
                                      量、
  程 同 步 的 机 制            ，POSIX  信和 号                              互 斥量和 条件变量。
       在    Linux 上        ，信和   量                   。                   论                  量，                         外
                           号        API 有    两   组  一           组 是 第 13            章 讨   过的 System V IPC           信号 另
                                                   量。           这 两 组接口
      组 是 我 们 现在 要讨论的 POSIX 信号                                                               很相似，但 不 保证
  一                                                                                         能互换。               由 于 这 两 种
  信号 量 的 语义 完全 相同            ，    因 此 我 们 不      再                信号 量 的 原理           。
                       信号 量 函 数   的 名 字 都    以                  开
       POSIX                                     sem_                头，           并 不 像大       多 数 线程      那样以pthread_ 开
  头    。常 用 的 POSIX        信和号   量      数是下 面 5              个  :
       #include          < semaphore.h>
       int sem_init(       sem_t*    sem,    int pshared,                unsigned     int value     };
       int         sem_destroy(            sem       );
       int sem_wait(       sem_t*    sem    };
       int         sem_trywait({ sem_t*    sem       );
       int sem_post(       sem_t*    sem    );
       这 些 函 数 的 第         一   个 参数  sem 指向 被                操作 的 信号     量    。
       sem_init            用 于 初始 化              命名 个 未        的     信号 (POSIX            信和号     量 API 支持
                                        一                                                   命名信号量，                     不
本书 不讨论 它 )。pshared 参数 指定 信和号 量 类 。 果 其 值 为
  过                                                                 的     型     如           0，         就 表示 这 个 信号 量 是 当
                           量，和
                             否则
  前 进程        的    局 部信号                               该信号量就   可以          在 多 个 进程          之间 共享 。value 参数 指定 信号    的 初
         值。        ，         一       经               初始                  不                  结果
  始 此 外 初 始                化     个 已    被        化           的            信号 量 将 导致     可 预期 的     。
                                                 量           ，以   释放 其 占用 的 内 核资
       sem_destroy         函数 用 于 销毁 信号                                                     源。如 果 销       一   个   正    其他
        等待的            信号  量，                  则 将 导致 不 可 预期 的结果     。
  线程
                           以                   原子 操作 的 方式 将     信号
       sem_wait                                                      量的值减               如 果 信和    号 量的 值 0，  则
                                                                         1，                 为                    sem_wait
  将 被 阻塞              ，直 到 这 个 信号   量具有 非      0          值。
       sem_trywait        与 sem_wait    函数 相似        ，         不 过 它 始 终 立即               返回     ， 而 不 论  被操作 的 信号  量  是否
          非    0       ， 相 当 于 sem_wait 的 非 阻 塞 版                        。当   信和号  量    的 值 非     0时 ，sem_trywait    对 信和
  具有               值                                                本                                                  号量
          减     操作     。             当 信号
  执行                         量的值             为0
              1                                  ，    时    它 将 返回 -1     并 设置           errno     为  EAGAIN.
                                                                    量的值加                当 信号
                         函数 原子 操作 的 方式               将          信号                          量的值大于     时                ，其
       sem_post            以                                                     1。         0      他正在
                         等 号 量     的    线程 将       唤醒           。
  调用 sem_wait              待信                             被
                                                    则          返回 -1           并 设置errmo。
       上 面         这    些 函 数 成  功时 返回  0，    失败

## Page 294

276                第 二 篇 深入 解析 高  性   能     服务 器   编程

14.5

                                          BT DUAR     RUBE, LR
个 二 进     制信号量 见                      节      进入         代码             ，                  斥              其
                     〈 13.5.1 小 )。              当     关键          段  时   我们 需要 获得 互       锁 并 将           加 锁 ，
这 等价于            二 进   制信号 量的 P 操作       ，  当 离开 关键     代码      段   时   ，我 们 需要 对 互       斥锁 解锁      ，以  唤醒 其
他 等 待 该 互            锁 的 线程 ， 这 等     价  于 二 进    制 信号 量  的  V  操作     。
                     斥锁基础    API
14.5.1             互

                                                        :
    POSIX            互斥锁 的相关 函数 主要 有 如 下            5   个

                     <pthread.h>
   int                                pthread_mutex_t*    mutex,       const
                                      pthread_mutexattr_t*        mutexattr         );
   int       pthread_mutex_destroy(                     t*
                                            pthread_mutex       mutex         );
   int       pthread _mutex_lock(     pthread_mutex_t*    mutex        );
   int       pthread _mutex_trylock(        pthread_mutex_t*    mutex         );
   int       pthread_mutex_unlock(          pthread_mutex_t*   mutex     );
    1K           pA        4 B              指向               目标 互 锁 的 类                               型 pthread
                     —     mutex                    要 操作  的            互斥锁               斥     是              _
        结构
mutex_t              体。
                     用                               于 初始
                                            化互斥锁                       参数
    pthread_mutex_init 函数                                                         指定     互    锁 的 属性 。 如 果 将 它
设置为
        NULL，        则 表示 使用默认 属性。我                 们 将 在 下  一  小 节讨 论 互            斥    锁 的 属性 。  除  了 这 个 函数
外，我     们        还     可以 使用如 下 方式 来         初始化  一   个 互斥锁
                     上     mutex =    PTHREAD_MUTEX_INITIALIZER;
    Pthread_mutex
    宏 PTHREAD_MUTEX_INITIALIZER                       实际  上只 是 把 互             斥锁 的 各 个 字段 都 初始       化 为0。
                                         用  于 销毁
                                                    互斥锁，     以 释放 其 占用
    pthread_mutex_destroy 函数                                                        的 内 核资 源    。销    一  个 已 经
加 锁 的 互斥锁 将          导致    不可 预期 的       后  果 。
    pthread_mutex_lock     函数    以      原子 操作 的 方式 给         一  个 互    斥      锁 加     。    果     斥        经
                                                                                      锁      如 目 标 互  锁    已 被
  上     则                                        调用
锁            pthread_mutex_lock               将  阻塞 ， 直 到 该 互   斥             占有          解锁
    ，                                                           锁 的                者 将 其     。
    pthread_mutex_trylock     与 pthread_mutex_lock           数 类              ， 不 过 它 始 终 立 即 返          ，   而
                                                                         似                            回      不
                                                                                          版本。             当 目标
论 被     操作       的 互   斥锁 是 否 已 经 被 加       锁 ，  相 当 于 pthread mutex          lock  的 非 阻塞                   互
斥 锁 未     被      加     锁 时，pthread mutex_trylock    对 互  斥锁 执 行  加           锁 操  作 。 当 互  斥 锁 已 经 被加    镇 时 ，
                                              码EBUSY。    需 要 注意 的        是        ，这 里讨论 的     pthread   mutex
pthread_mutex_trylock 将 返回 错误
lock 和 pthread_mutex_trylock                的行为是   针对 普通 锁 而 言         的 。    后 面 我 们 将 看 到    ，   对 于 其 他 类 型
的 锁 而        言   ，    这 两 个 加 锁 函  数 会有 不 同 的 行为      。
    pthread_mutex_unlock                以原子 操作 的 方式给            一  个 互        斥  锁 解     锁。   如 果 此 时 有 其 他 线程
        等待这个互斥锁，     则 这 些 线程                 中的 某  一  个  将 获得 它  。
正在
    上 面这些  成 功时返回 0，失 败 则 返回 错误 码                              。

## Page 295

 第 14 章 多 线程 编程 f+ 277


 14.5.2

     pthread mutexattr_t            结构 体 定义了       一     套 完 整 的 互斥锁 属     性            。线 程库提 供 了 一 系列 函数 来
 操作  pthread  mutexattr             类 型的
                               t        变量       ， 以 方便 我们 获取 和 设置 互                            斥   锁 属    性   。这 里 我 们 列 出   其
 中 一 些        主 要 的   函数:

#include <pthread.h>
/* 初始化 互 斤 锁 属性对 得 /
int pthread_mutexattr_init( pthread_mutexattr_t* attr
     /*                   MR */
int pthread_mutexattr_destroy( pthread_mutexattr_t* attr );
   获取 和 设置
     /*                 互斥锁         的Pshared 属性   */
int pthread_mutexattr_getpshared( const pthread_mutexattr_t* attr, int* pshared )j;
int pthread mutexattr setpshared ( pthread_mutexattr_t* attr, int pshared };
   获取 和 设置
     /*                 互 锁的        tyYPe 属性 */
     int                                            const    pthread_mutexattr_t*                   attr,     int* type);
int pthread mutexattr_settype( pthread _mutexattr_t* attr, int type };
                          斥                      属性                                     斥     锁   属性         是
                                                                                                                          否人
                   讨论 互      锁 的 两 种 常用                                 和 type。                                    互 pshared 指定
     本 书 只                                             :   pshared                                                           允许
                   互斥锁，      其 可      选 值有两 个    :
                  跨 进程 共享
                                                           互
                                                                  锁 可以     跨 进程 共享
     口         PTHREAD                   PROCESS_SHARED。    斥                   被
     口 PTHREAD                                  PRIVATE。   互斥     锁         只 能 被 和  锁 的        初始   化             线程隶属于同一 个 进程
               的     线程共享 。
               斥锁                     互斥锁的类型                                   如下          类             互 锁
     互
                    属性 type                           指定   。Linux              支持 4  种          型     的       ，
                          MUTEX            NORMAL，      普 通            。这 是 互     斥  锁     默    认 的 类     。当                个线程
     口 PTHREAD                                                    锁                                        型    一           对一个
                   锁加锁以后，                        该    线程          将     形成 一 个 等    竺                       并 在 该 锁 解 锁 后 按 优 先
               普通 其 余 请 求 锁 的
               级 获得 它  。  这 种 锁 类 型          保证  了 资源 分 配 的 公平             性     。但  这 种 锁 也很 容易 引发          问题        :    一 个
                                               普通 锁 再 次                 锁 ，    将 引发
               线程   如 果 对  一      个 已 经 加 锁 的                     加        死锁 ，                      对   一    个 已 经 被其他 线程    加
                       锁解锁，           或 者对  一  个  已     经         解锁 的 普通 锁 再 次 解锁              ，   将 导致     不  可  预期 的后果   。
               锁 的 普通
               PTHREAD_MUTEX              ERRORCHECK，        检 错          锁    。 一   个 线程如 果 对             一  个   已 经 加 锁 的 检 错
                 再次加锁，                             回                                                  其
               锁 则 加 锁 操 作 返 EDEADLK。 对                                         一    个 已 经 被             他 线程   加   锁 的 检 错 锁 解
               锁 ，   或者 对  一     个 已 经解锁    的    检 错 锁 再 次 解锁                               ，则 解锁 操作 返回 EPERM。
     口                  MUTEX                                             。                                  在            前
               PTHREAD
                                                RECURSIVE， 骨            锁     这 种 锁 允许            一  个 线程     释放 锁 之        多 次
               对 它 加 锁 而 不发         生死   锁。                           不 过 其 他 线程如 果 要 获得 这 个    锁 ，  则 当前 锁 的         拥有 者 必须 执
               行 相应       次 数 的  解锁     操作 。 对 一  个 已 经 被              其他 线程    加    锁 的          套 锁 解    锁   ，或  者对   一 个 已 经
               解锁 的 嵌 套 锁 再 次 解锁           ， 则 解锁    操作 返回             EPERM。
          口             MUTEX                                          。      线程                对        个     经   锁 的  黑 认 锁 再
               PTHREAD
                                            DEFAULT， 默认 锁                一     个     如    果         一      已     加
               次 加   锁，或      者  对  一 个 已  经 被 其他 线程加 锁 的                  默认     锁 解     锁     ， 或 者对       一 个 已 经     解锁 的默认
                    再次        ，         不    预期 的       后  果 。这 种 锁 在      实现 时 候 可 能 被 映射 为                 上          面 三 种锁
               锁
                         解锁     将 导致 可                                                     的
                 一。
               之

## Page 296

篇  性 服务 编程
278 «+ 第 二 深入 解析 高 能 器


14.5.3

             BN   —  TP                     HEM     —    OR     RE  RET,
这 种 情况 还 不 容易 被 发         。    文提到，                                                ，
                          现 前 在               一 个 线程 中 对    一   个 已 经加 锁 的 普通 锁  再 次 加 锁 将
 死    锁 。                                          仔细        。     外   ，         个     按照
导致      这 种 情况 可 能 出 现在 设计得 不 够                          的 递归 函数 中  另  如 果 两       线程    不
            申请            ，                         ，    清单
同 的 顺序 来        两  个 互     斥 锁 也 容易 产生 死         锁     如 代码     14-1 所 示 。
                          代码 清单            按 不 同 顺 序 访问 互 斥    锁 导   致死锁
                                                     -一-一
一
#finclude    <pthread.h>
#include     <unistd.h>
#include     <stdio.h>

int   a =    0;
int   b =    0;
pthread      mutex_t  mutex_a;
pthread      mutex_t  mutex_b;

void*   another(     void*     arq )
 {
    pthread_mutex_lock( Smutex_b };
    printf( "in child thread, got mutex b, waiting for mutex a\n" );
    Sleep( 5 );
    ++b;
    pthread mutex_lock( &mutex_a );
    b += att;
       pthread         mutex_unlock(  émutex_a  );
       pthread_mutex_unlock(                    );
       pthread    exit(    NULL   );
}

int
{
                  t
       pthread       id;

       pthread    mutex init (     &mutex_a,    NULL     );
       pthread    mutex_init(      &mutex_b,    NULL     );
       pthread    create(     éid,   NULL,  another,     NULL };

    pthread mutex_lock( émutex_a );
    printf( “in parent thread, got mutex a, waiting for mutex b\n"™ );
       sleep(   5 );
       tha;
       pthread    mutex_lock(      amutex_b  );
             b+t;
                                      émutex_b  );
       pthread    mutex_unlock(       &mutex_a  );

       pthread    join(    id,   NULL  );
       pthread mutex_destroy(          &mutex_a  );
       pthread mutex_destroy(                    )};

## Page 297

    第 14 章 ”多 线程 编程 “+ 279


                    return 0;
         I

                        14-1,     EA RRA          AE BE  mutex_a，       然 后   操作 被    该  锁 保 护   的     a，
                          并    没有     释放
    但 操作 完毕 之             ，  主 线程     立即         互斥锁    mutex_a，        而 是   又申请 互     锁          b，
                          后                                                                   mutex     并
    在 两        个互 锁 的 保护 下 ，操 作 变量     和     b， 最 后 才     起              释放 这 两 个 互 斥锁     与  此 同时，     子
                          a                           一                             ，                   线
    程 则 按照相反 的 顺序 来申请 互     锁     mutex_a         和  mutex_b，     并 在 两       个锁 的 保护    下 操作 变   最 a   和
              们用    sleep     来    模拟连续 两 次 调用 pthread   mutex    lock  之
                                                                             间的
    b。         我                                                                    时间   差   ，以    确保代码 中
                      各自 先占有     个 互     锁 (主线 程 占                      子 线程
    的 两个                                                                       占有                       然
          线程              一                          有   mutex_a，                 mutex_b)，       后 等   待
          一个互太            〈    主线程
                          等待    mutex_b，          子 线程
                                                      等待    mutex_a)。         这
    另外                                                                         样，    两   个           就个持住
                                                                                                       线程
                                                 死锁。 如 果 代 码 中 不        加    入 sleep 函数    ，   则 这 段代 码 或
    了，谁 都 不能继续往下 执行 ，从 而 形成
    许 总 能 成        功  地 运行 ，从 而 为 程序 留下了   一     个 潜在    的 BUG.

               ”条
    146                       件 变 量

         如 果 说 互        斥       锁 是用于 同步 线程对 共享 数据 的访问 的             话  ， 那  么条 件   变量 则 是 用 于 在   线程   之
    间 同        共享 数据 的     。条件 变量 提供         了    种 线程间 的 通知
          步               值        一                                 机制   :   当 某 个 共享    数据     达到 某 个 值
    的时       候    ，唤  醒 等 待      这个 共享 数据 的 线程
         条 件变量          的     相关 函数 主要有 如 下 5  个 :
         #finclude      <pthread.h>
         int    pthread _cond_init(  pthread_cond_t* cond, const        pthread_condattr_t*   cond_attr);
         int              pthread_cond_t*                   cond);
         int              pthread_cond_t*                     cond      };
         int    pthread     cond_signal( pthread_cond_t*   cond );
         int    pthread_cond_wait(   pthread_cond_t* cond,                        mutex       };
             AE pH        AS —-    条件                                    ，              的       是
                      LAY     TSB cond   指  癌  要 操作 的 目标
                                                                        变量 条 件 变 量           类 型 pthread_
                    t 结构
    cond              体。
         pthread_cond_init     函数 用 于 初始化 条件 变量       。cond_attr                  参数 指定 条 件 变 量 的 属性 。如 果
                 它设置  为NULL，   则 表示 使 用默认 属性。条 件 变量 的 属性不                    多，而且    和 互     锁 的   属性 类 型
    将
    相似 ，所 以 我 们           再 效    。 了                  外，                 们     可                       初始
                          除 pthread_cond_init 函数                        我
                          不                                                   还 以 使 用 如 下 方式 来
    化  一  个 条 件 变量        :

   pthread cond_t cond = PTHREAD_COND INITIALIZER;
   %# PTHREAD_COND_INITIALIZER 实际 上 只 是 把 条件 变 量的 各 个 字段 都 初始化为 0。
        用于 销毁
        条件变量 ，以 释放 其 占用
         pthread_cond_destroy                                                 的内核资源。销            一   个 正在
被 等 待的条件 变量 将 失败 并 返回EBUSY。
   pthread_cond_broadcast 以 广播 的方式唤醒所 有 等竺目标 条 件变量 的 线程 。pthread_
    cond_signal       函数 用 于 唤醒     一 个 等 待目标  条件 变量 的 线程         。     至 于  哪个线程     将  被 唤醒   ，  则 取决 于
    线程    的优先           级和 调度 策略    。 有 时 候 我 们 可 能 想 唤 醒  一    个 指定 的 线程         ，但 pthread     没有 对 该 壳

## Page 298

  280   +     第 二  篇  深入    解析  高性能服务 器    编程

                方法。   不 过 我 们 可以 间接 地 实现 该           需求 ，   定义  一 个 能 够   唯一  表示 目标 线程   的  全 局
  求 提 供解决
  变量 ，在 唤醒 等待条件 变量 的 线程前先 设置 该变量                     为      目标  线程  ，然后    采用 广播 方式      唤醒 所 有
  等待条 件 变量 的 线程 ，这 些 线程被唤醒 后 都 检查 该变量以 判断 被 唤醒                      的     是 否 是 自己      ，   如 果
  就开始 执行        后  续代 码     ， 如 果 不 是 则 返回    继续 等    待。
      函数 于 等 目 标 条件 变量 ，mutex 参数 于 保护 条件 变量 的 斥
       pthread_cond_wait      用          待                      人       是用                 互
                                      操作的     原子     。在 调用 pthread
  锁 ，以 确保 pthread_cond_wait                      性                 cond_ wait 前，  必 须 确 保 互   斥
                经加锁，和否则 将     导致  不可 预期 的        结果。pthread     cond_wait     函数 执行 时，首 先  把  调
  锁   mutex  已
                                                                    见，                从 pthread
  用 线程放人 条件变量 的 等待队列 ，然 后 将 互斥锁                                解锁  。可
                          中                          mutex          cond                   wait
  开始 执行 到 其 调用 线程 被放人 条件 变 量 的 等待                    之间
                                                     队列 的 这 段 时间 内         ，pthread_cond_signal
                                                                 之，pthread    cond wait      函数
  和 pthread_cond_broadcast     等       不会修改 条        件 变量  。换言                              不会错
  过 目标     条 件 变量 的任何     变化 。    当 pthread_cond_wait                 成功返回    ，    锁       再
                                                            函数 时 互 斥     mutex                将
         被锁上 。
  次
       上 面 这 些 函 数 成功时 返回    0，   失 败 则              错误码。
                          返回

                                   同步机制包装类
  14.7       ”线程

      复用代码， 时由于 后 文 的需要，我 们 将 前 面 讨 论 的 3 种 线程 同步机制 分 别 封
 为 了 充分 同
成 个
              类，实 现在      文件
        3               lockerh   中，    如 代码 清单 14-2        所 示 。
                          代码     清单              14-2 lockerh 文件
       #ifndef    LOCKER_H
       §define  LOCKER_H

       #include    <exception>
       finclude    <pthread.h>
       #include    <semaphore.h>
       /*                 “/
       class    sem
       {
       public:
             /* 创建 并 初始化信号量
             sem()
             i
                  if( sem_init(  &m_sem,     0,  0 != 0 )
                  {
                      /*                                            +/
                      throw    std::exception();
                  }

                   迟号量     “/
                销毁
             ~sem()
             {
                   sem_destroy(  &m_sem );
             }

## Page 299

第 14 章 多 线程 编程 > 281


Am    等待信 号量  */
bool
{
      return  sem_wait( km sem  )   ==  0;
}
/*            */
bool  post ()
{
      return  sem_post( &m_sem  )   ==  0; *


private:
         sem_t   m_sem;

          互斥锁的类     */
   封装
class    locker
{
public:
              并 初始 化 互 斥镇
             ()
   Locker
       {
          if(        &m_mutex,          NULL           ) != 0 )

              throw
          }
   }
   /*             +/
               ()
   ~locker
   {
             pthread mutex_destroy( é&m_mutex );
   )
   /*             */
   bool   lock()
   {
          return                     sm_mutex   】    ==  0;
   }
              互斥锁    */
                  释放
   bool   uniock()
   {                                                     .
          return                                     )   == 0;
   }

private:
        pthread_mutex_t m_mutex;
M7
             变量
/= 封装     条 件    的类
class    cond
{
public:
        * 创建 并 初始化 条 件 变量 */
        cond ()
        {
          if(    pthread_mutex_init( &m_mutex,  NULL   )  != 0 )

## Page 300

 282 “s* 第 二篇 深入 解析 高性能 服务 器 编程


         {
                    throw  std::exception();
         )
         if  (    &m_cond,                      NULL  ) != 0 )
         {
                  /*  构造 函数中  一  旦 出 现 问 题 ，就 应 该立即   最 放 已 经 成 功 分配了 的 资源 */
                  pthread &m_mutex );
                  throw   std::exception();
         )
 I
        销毁              */
   /*     ()       条件变量
   ~cond
 {
                            &m_mutex           );
                                     &m_cond  );
 }
   /*                   */
   bool   wait    ()
   {
          int     ret =   0;
                                    &m_mutex  );
          ret     =  pthread  cond_wait( &m_cond, &m_mutex   );
          pthread_mutex_unlock({    &m_mutex   );
          return ret      ==  0;
   】
   /*    唤醒 等 待       件 变量 的     +
                    ()
   bool   signal
   {
          return      pthread _cond_signal(  4&m_cond ) == 0;
   }

 private:
    pthread _mutex_t        m_mutex;
    pthread               m_cond;
 hi

 #fendif


 14.8   ”多     线程 环境

 14.8.1  BARR

     如 果 一     个     能  被多 个 线程 同时 调用 且 不     发生 竞态条     件 ，则 我们称 它 是 线程安全    的
 (thread safe)》，  或者说 它是可    人函数。Linux 库 函 数   只有 一小 部 分 是 不 可 重人 ，   如       小
     的 比 5.1.4
讨论 的 inet_ntoa MAX, VAR 5.12.2 小 节讨 论 的 getservbyname
     和 getservbyport 函数。关
 节                                                                            于
 Linux 上 不 可     重人 的 库     的 完整 列表，请 读者 参考    相关 书籍     ， 这 里 不 再 述。 这 些 库   数
 之 所以 不 可 重人                主 要 是 因为其内 部 使 用了 静态 变量。不
                 ，                                  过  Linux 对很 多 不 可重人 的 库
 提供 了 对 应 的 可     重   人 版 ，这 些 可重人 版 本 的 函数 名  是 在 原   函 数 名 尾部 上     比
                          本                                加     _r。       如， 数

## Page 301

                                                                                第14     章  多 线程  编程 +>  283

                                            r。在 多 线程 程序中                      调用 库     ，   一   定 要 使 用 其 可 重
    localtime      AY A] HA                localtime
    和人 版本，     否则 可 能 导致 预想 不 到 的 结果        。
                      和
    14.8.2                       ”线程
                                  进程

          这样一个                     : 如 果     个 多 线程 程序           某  个 线程 调用 了 fork 函数     ， 那          么新 创建
      思考                      问题           一               的                                              的子
    进程     是 否 将 自动 创建        和     父 进程    相同   数量 的 线程    呢    ?  答案  是       否”，              望      。
                                                                              “和 正 如 我 们 期            的 那样 子
进程 只拥有 一 个 执行 线程 ，它 是 调用 fork 的 那个线程的完整 复制 。并 且 子 进程将自动继承 父
       中互 锁                             之                  。            ，       进程                   斥
    进程 〈 条件 变 量 与 类 似 )                               的 状态   也 就 是      说 父       中    已   经 被加 锁 的 互  锁 在 子
    中也是被锁住的。这 就 引起 了 一 个 问题 : 子 进程可 能 不 清楚 从 父 进程 继承 而 来 的 互斥锁
进程
    的 具体     状态       加     锁 状    态 还 是解锁 状态 )。这 个 互斥锁 可 能                       锁    了 ， 但 并 不 是 由 调用 fork
    函数 的那个线程 锁 住               的    ，而 是 由     其 他 线程 锁 住     的    。如 果 是 这 种 情况        ，则 子 进 程 若 再 次 对 该 互
    斥 锁 执 行    加 锁 操 作 就       会    导致 死     锁  ，如 代码 清单     14-3   所 示 。
                                                                 程序中调用        fork 函数
                                    代码 清单14-3     在 多 线程

      #include       <pthread.h>
                     <unistd.h>
      finclude       <stdio.h>
      finclude       <stdlib.h>
      finclude       <wait.h>

      pthread      mutex_t     mutex;
      /*              运行的函数。它 首先 获得 互       销                           s.        */
      void*      another(     void*     arg     )

               printf(     “in   child     thread,  lock   the   mutex\n"     );
               pthread_mutex_lock(          S&mutex   );
               sleep(    5 );
               pthread     mutex_unlock(     é&mutex    );
      }

      int     main ()

               pthread mutex         init(  &mutex,    NULL   );
               pthread     t  id;
               pthread_create(          Sid, NULL,      another,   NULL );
                                                                 fork
               /*                           1s,                         作之前，    子      已 经 开始 运行并    得了 互斥
    量  mutex   */
               sleep( 1      );
               int   pid   =  fork{);
               if(   pid   <  0

                         pthread    joinf   id,  NULL  });
                         pthread    mutex_destroy(     &mutex    );
                      return      1;

                      if(    pid    ==  0

                         printf(    "I  am  in   the child,  want   to  get   the  lock\n" );

## Page 302

   284       第 二   篇 ”深入    解析 高 性    能      服务 器 编程

                   /*            子 进 程从父 进 程 继承了互   锁 mutex 的 状态，该 互  处    于锁 住的状态 ，这 是 由 父 进     程中 的
         */  pthread      mutex_lock           Ak,                                                 RE
                                             &mutex   );
                   printf(  "I     can not   run to   here, oop...\n" );
                   pthread_mutex_unlock(                };
                   exit(    0 7
             }
             else
             {
                   wait(      NULL );
             }
                          join(  id,   NULL  );
             pthread      mutex_destroy(     &mutex   );
             return     0;
        }

                 过 ，pthread 提供
                              了一个            专门
        不                                      的
                                                 函数   pthread_atfork， 以 确保 fork 调用 后 父 进 程        和    子 进
   程 都拥有           个 清楚 的 锁 状态。该 函数 的 定义
             一                                          如下
        #finclude  <pthread.h>
        int      pthread_atfork( void  (*prepare) (void),   void (*parent) (void), void (*child) (void) );

        该   函数 将 建立         3  个 fork  句柄 来 帮助 我        们  清理 互  斥锁 的 状态    。prepare    句柄 将 在          调
                                                                                                      fork
   用 创建出子 进程之 前 被 执行 。它 可以 用 来 锁 住所有 父 进程中 的 互 斥               锁     。parent     句柄 则 是               fork
   调用 创建 出         子        进程 之 后 ， 而 fork  返回 之     前 ，  在 父 进 程中 被 执行    。    它 的         作用 是 释放 所 有 在
   prepare  句柄 中 被 锁 住 的 互 斥     锁    。child     句柄 是 fork    返回 之    前 ，  在 子 进 程   中  被 执行     。和 parent
   句柄 一            ，child 句柄 也 是 用     于 释放 所 有 在     prepare 句柄 中 被 锁 住    的 互 斥     。 该             成功时返
             样                                                 锁                                        函数
  0，失 败 则 返回 错误 码 。
回
       清单14-3 正常 工作，就 应 该 在其中 的 fork 调用 前 加 入 代码 清单14-4
   因此，如 果 要 让代码
       。
所 示 的代码

                              代码 清单14-4               ”使用  pthread_atfork 函数
                          ()
        void       prepare
        {
                   pthread_mutex_lock( &mutex     );
        }
        void       infork()
        {
                   pthread mutex_unlock(    émutex    );
        }
        pthread atfork(       prepare, infork,     infork   };


                   和信号
   14.8.3          ”线程
        每个线程 都 可以 独立 地 设置信号 掩                         。 们在    进程
                                             码        我       10.3.2 小 节讨 论 过 设置 信号 掩 码           的

## Page 303

                                                                                   第 14   章   ”多 线程      编程 f+ 285

    数 sigprocmask，        在         环境                         用                                本的
                        但 多 线程      下 我 们应 该 使                    如 下 所 示 的        pthread  版       sigprocmask 函
    数来设置线程信号 掩 码                ;

#include              <pthread.h>
#include              <signal.h>
        int                         ( int how, const     sigset_t*       newmask,               oldmask     );
        该函数 的参数 的含义 与               sigprocmask                                  ALA              pthread_sigmask
                                   则返回错误码。
    成 功时 返回 失败
        由 于 进程 中 的所有 线程 共享 该 进程的信号 ，                              所以   线程        库 将 根据 线程      掩  码 决 定   把 信号 发
                                    此，  如 果 我 们 在 每    个     子    线程 中   都       单独 设置 信号 掩     码 ，    就 很  容易 导致
    送 给   哪个具体        的 线程    。 因
                      外   ，所 有 线程 共享 信号    处    理 函     数    。 也 就 是       说  ，  当 我 们  在   一 个 线程     中 设置 了 某 个
    逻辑 错误。此
    信号 的 信号 处 理         函  数        ，它 将 覆盖 其 他 线程 为 同一 个 信号 设置 的                    信和 号   处 理  函 数   。        这
                                后                                                                            两点都说
    明，我 们 应 该定义           一 个 专门    的   线程 来 处 理 所 有 的 信号           。  这 可 以 通过 如 下 两 个           步骤     来 实现 ，
                      程  创建 出 其     他 子 线程    之前     就  调用          pthread_sigmask  来  设置    好   信号
        1   在 主线                                                                                       掩码，  所 有 新
    创建的子线程 都 将 自动继承 这 个 信和                          掩 码 。这 样 做 之       后 ，    实   际 上 所 有 线程 都 不 会         响应被 屏蔽
    的 信号 了     。
        2 在 某 个 线程        中调用       如 下         数 来 等 待 信号 并 处 理 之     :
                      <signal.h>
        int     sigwait(   const     sigset_t*   set,   int* sig  );
                                                                                                               创建
        set     参数 指定 需要 等 待 的信号 的 集合 。我    们                 可以 简单        地     将 其 指定 为     在  第 1   步 中     的信
    导       ，表   示 在 该 线程           中 等 待所有 被 屏蔽 的 信号          。参
           码                                                         数    sig    指向 的     整数       用 于 存储 该 函数 返回
    的 信号值                 成 功       时     失败
            。sigwait                则返回    错误                  码。           旦                   ，
                                                                     一 sigwait          正确 返回          我 们 就 可以 对
    接收 到 的 信号 做 处 理了。 很 显然 ，如 果 我 们     使                    用 了  sigwait，       就  不应  该  再 为 信号     设置   信号 处 理
      数 了  。    这 是 因为 当 程 序 接收 到 信号     时     ，    二      者 中 只 能 有   一 个       起 作用  。
        代码 清单         14-5 HA       pthread sigmask      的               。          展示          通过           步
                                    函数                       man               手册 它     了 如 何     上 述 两 个       邓
        在一个线程中统一                处 理   所 有 信和号  。
    实现

                                      代码 清单 14-5     ”用  一   个 线程 处 理       所 有  信和 号
        #include      <pthread.h>
        finclude      <stdio.h>
        finclude      <stdlib.h>
        finclude      <unistd.h>
                      <signal.h>
                      <errno.h>

        #define   handle_error_en(en,     msg)      \
              do  {   errno =   en;      perror(msg);   exit (EXIT_FAILURE);        }  while  0

        static    void   *sig   thread(   void  *arg    }

                  Sigset_t    “set    = (sigset七 *     arg;
                  int   s,  sig;

## Page 304

篇 解析 服务 编程
286 第 二 深入 高 性 能 器




                   for(       77     )
                   {
                              /*                  WA  sigwait                  */
                              S$   =   sigwait(   set, &sig       );
                              if(     s   != 0
                                                                  s,                );
                                             "Signal handling         thread   got  signal %d\n",   sig );
                   |
        }

        int         main(  int     argc,     char*   argv[]  )
        {
                                     t   thread;
                   pthread
                   Sigset_t       set;
                   int     s;

                   /*                                                   */
                   Sigemptyset(              &set );
                   sigaddset(             &set,   SIGQUIT );
                   Sigaddset(             &set,   SIGUSR1 );
                   s    =  pthread         sigmask(   SIG_BLOCK,        &éset, NULL   );
                   if(     s        !=0   )
                                  handle     error_en{ s,                             );

                   S$   =  pthread _create(         &thread,      NULL,   &sig     thread, (void   * &set };
                   if(     s      !=  0   )
                                  handle     error_en{ s,                             );

                    pause ();
        )

              后                                              ，                          送
最 ,pthread 还 提供 了 下 面 的 方法 使 得 我 们 可以 明确 地 将 一 个 信号 发 给 指定 的 线程 :

        finclude       <signal.h>
        int                                pthread_t thread,          int sig  );
                                                                                      的信号。       果      为 0，
                   thread         参数 指定 目标 线程         ，sig            参数 指定 待 发 送                如  sig 则 pthread_
kill                       ，                      执行                  。    们       利用     来      是
        不 发 送信 号                  但 它 任 然 会           错误 检查           我    可 以        这 种 方式     检测 目标 线程      否 存
在                             成     功  时 返回  0，   失 败 则 返回 错误 码           。

## Page 305

                 BAST                                               池和       线程

         在 前 面的章节 中，我们是 通过 动态 创建                               子   进程   〈 或 子线程   )   来 实现   并 发服务    器 的 。 这 样
    做 如 下 缺点
      有           :
         口   动态创建 进程    〈    或 线程        ) 是 比较  耗费             时 间  的  ， 这 将 导致  较   慢 的 客户 响应   。
         口   动态 创建 的子进程          〈      或 子 线程 )     通常        只   用 来    为 一个 客户     服务   〈 除 非 我  们  做 特殊 的 处
               )，这 将 导致      系统  上 产生 大 量 的 细微                   进程  〈    或 线程)。进
                                                                                      程 〈  或 线程 )        间 的 切换
            理                                                                                                将消
            耗 大量 CPU   时间。
         CD  动态创建的子     进程       是当前     进程 的    完整              映像  。  当 前 进程        必须 谨慎 地 管理其   分 配 的  文件 描
            述符和 堆     存 等系统 资源 ，         否 则     子 进 程            可 能   复制  这 些 资源    ，从 而 使 系统     的 可 用  资源 急
            剧 下 降 ，进 而 影响服务 器 的 性 能 。
         第  8                                    介 绍 过 的 进程池和 线程 池 可 以 解决上述 问题 。本 章 将 分 析 这 两  种  “ 池 ”  的 细节 ，
给 出 它们 的 通用 实现 ，并 分别用 进程池和 线程 池 来实现 简单的并 发服务 器 。

                 池和线程池概述
    15.1       进程

         进程地和 线程 池 相           似，所               们 以 这 里 我 只 以 进程    池 为 例 进 行 介      绍 。  如 没有特殊 声明      ， 下 面
    对 进程池 的 讨论 完全 适用 于 线程 池              ，
         进程    池 是 由   服务 器    预先创建的     一     组 子 进             程 ，这 些     子进程   的  数目    在 3~10个 之 间    当 然 ，
    这 只 是   典型 情况)。比如 13.5.5             小 节 所 描述     的        ，httpd     守护 进程 就 是 使      用 包含 7   个  子 进程 的 进
    程 池 来 实现 并 发     的 。线    程 池 中 的     线程             数量 应 该 和 CPU      数量差不多     。
         进程    池 中 的 所有   子 进 程          都  运行 着 相同    的    代码      ，     并 具有 相同 的 属性       ，比 如 优先     级、PGID
    等 。因 为 进程池 在 服务 器 启动之初 就 创建好了                              ，   所 以 每    个子  进    程 都  相对 “和干净 ”，     即 它们 没
    有 打开 不 必要 的 文件描述符                〈           继承 从 父 进程 而     来)，      也 不 会错误    地 使   用 大 块的   堆 内 存 〈 从 父
    进程 复制得到 )。
         当 有 新 的   任务 到 来       时    主 进程 将，   通过     某 种 方式              选择进程 池       中 的 某 一 个子 进 程 来 为  之  服
    务   。相 比 于   动态创建子       进   程   ，    选择 一 个 已    经            存在 的 子 进 程的 代价     显然   要 小 得多 。    至 于 主 进程
    选择   哪个子     进 程 来为   新  任务 服务         ，         则 有 两 种 方式  :
         口 主 进程 使    用某   种  算法 来 主动             子 进程   选择       。  最 简单    、最 常用     的 算法   是随机    算法  和 Round
            Robin〈 轮 流 选     取 ) 算法      ， 但 更 优秀    、         更   智能     的 算法 将 使    任务   在 各 个工作    进程  中 更 均
            义 地 分    配 ，从 而 减轻 服务 器 的     整体压力                 。

## Page 306

 288                             第 二    篇 深入 解析 高    性        能     服务 器 编程

     CQ)                     主 进程和所 有 子 进 程通过                     一      个 共享 的     工作          队列 来同步         ， 子 进程 都 睡眠 在 该       工作     队列
                                 。当 有 新 的 任务 到                来 时   ，主 进程 将任务 添加 到 工作                 队列              中。      这 将 唤醒 正在 等 待 任务
                         上
                                 子     程，      过                         进                                           权”，
  的
     进 不 只 有 一 个 子 程 将 获得 新 任务 的“接管 它 可以 从 工作 队列 中 取
                                                     并执行之，    而 其 他 子 进程       继续 睡眠 在将           工作        队列 上 。
                         出任务
                                                   择好子进程后，       还             用                      告诉
        当选 主 进程                                                                     要 使     种 通知 机 制 来            目标 子 进 程      有     新任务 需要 处
                                                                                      的方法是，在 父 进 程和  子 进程 之间 预 先 建立好              一 条 管道 ，   然
 理    ， 并 传递 必要 的 数据                           。    最 简单
                                                                                   然，         要 预先定义    好      一  套 协议 来   规范      管道 的 使 用 )。
 后 通过 该 管道 来                         实现所有        的     进程         间 通 信     【 当
 在 父 线程                          和     子 线程 之间传 递 数 据就要 简单 得                       多    ，       因 为 我 们 可以     把  这 些   数 据定义为全 局        的 ， 那
 么 它们 本 身 就 是锌                                 有 线程 共               。
                                 上面的论述，我         们 将               进程 池 的     般 模 型           描绘  为  图         所 示 的 形式
        综合                                                                一                           1$-1                。

                                                                                                                      进程
                                                                                                  通知 机 制                池
                                                                                                      全                村
                                                                                                 池模型
                                                                         FA        15-1           进程

理多                                           客户
 15.2                            ”处

        在 使用进程池 处                              理 多 客户         任务         时，   首 先 要 考虑 的          一    个 问题    是  ，   监听 socket    和 连接 socket
 是 否 都                       由    主       来统一管理。                  第                                                      ，           步
                                     进程 回 忆                               8   章 中     我 们 介绍 过 的 几 种 并 发 模式               其    中 半 同     / 半 反
 应 堆 模 式 是 由 主 进 程                             统     管理       这 两     种          socket    的; 而
                                                     一                                          图 8-11  所 示 的 高        效的 半 同  步  /  半 异步 模式 ，
     及领导 者 /追随
 以                                   者模式，则                    是 由                       主 进程 管理所有 监听 socket，     而 各 个   子        进程 分 别管 理属 于
                                的连接    socket
                                 自己          的   。 对     于 前      种 情况 ，主 进程 接受 新 的 连接以 得 到 连接                            socket，            然
                                                              一                                                                            后它需
                                             给子进                对     线程
 要 将                     socket 该     传递         程            《    于      池而言，                  父    线程将 socket      传递给    线程 是     很  简单 的 ，
                                                                                                池而     ，     我 们 必须
 因为 它们 可以 很 容易 地 共享该                                          socket。     但 对      于    进程 言                            使用 13.9     节 介 绍 的 方法
 来 传递 该                          socket)。    后 一     种 情况 的 灵活 性更大 一                          些 ， 因 为  子 进 程可 以 自己 调用     accept        来 接受 新
 的 连接 ，                          这 样    父 进程 就 无    须    向    子    进 程 传    递  socket，          而 只 需要 简单 地 通知 一          :“      我        到新的
                                                                                                                          声                 检测
                                          它.”
 连接                      你 来 接受 ，
        在                            小 节 中 我 曾讨论过 常 连接 ，即                                     个 客户 的 多 次 请求可以 复 用 一个                        连接
                             4.6.1               们                                 一                                                TCP      。
 那么                      在 设计 进程，            池 时 还 需要         考虑      :   一   个 客户 连接 上 的所有任务是否 始 终 由                          一  个    子 进 程来处
 理      如 果 说 客户 任务 是 无 。                        状态               的 ， 那 么 我 们 可以 考虑 使 用 不 同 的                        子 进程 来为 该    客户    的不 同 请
 求 服务 ，                               几
                                      如 15-2 Pras.

## Page 307

                                                        15    章  ”进程
                                                    第                   地和线程   池     289

                                                    器
                                                           服务
                                                              池
                    请求1，   客     请求2        .进程
                 客户          户
                                 |                      Fe              回应2
                     1，       回
                   回应        应2  [UL

                   图 15-2 ”多 个 子 进 程 处理同一 个 客户 连接   上 的    不 同 任务
   但    如 果 客 户任务 是 存在 上 下 文 关 系     的  ，  则 最 好 一 直 用同一 个   子   进程 来 为 之 服务     ，    实
                                                                                     否则
现 起 来   将比较      麻烦   ，因 为 我 们 不 得 不 在 各 子 进程 之  间 传 递    上  下 文 数据。在    9.3.4     中，
                                                                                   小节 我
们讨论了      epoll  的 EPOLLONESHOT 事件     ，这  一 事件 能  够确保     一 个   客户    连接 在    整个生命  周期
中 仅 被   一 个     线程处 理 ，

               半 同 /             进程
15.3               步     半 异步     池 实 现

          前面的讨论，     本 节 我 们 实现 一    个  基于 图 8-11  所 示 的 半 同    步/半 异步    并发 模式    的 进程
   综合
                     示。          为 了 避免
池，如 代码 清单         15-1 所         在父、    子 进程 之间传 递      文件   描述        ，       我 们 将 接受
                                                                  符                 新连接
的 操作 放 到 子 进 程       。很 显然       ，    对 于 这 种 模式
              来处理的。   中          而言，    一               个 客户 连接 上 的    所 有 任务 始终 是 由  一
个 子 进程

                                 15-1      步
~                            代码 清单     半 同   /   半 异步进程
   “    filename: processpool.h
   #ifndef  PROCESSPOOL_H
                     H
            PROCESSPOOL

   finclude      <sys/types.h>
   #include      <sys/socket.h>
                 <netinet/in.h>
   #include      <arpa/inet.h>
   #include      <assert.h>
                                                                                   ，
   finclude      <stdio.h>
   #include      <unistd.h>
   finclude      <errno.h>
   #include      <string.h>
   #include      <fcntl.h>
   #include      <stdlib.h>
   finclude      <sys/epoll.h>
   #include      <signal.h>
                 <sys/wait.h>
   #include      <sys/stat.h>

                                 是 目标 子 进程 的 PID，m pipefd                          */
   class  process
   {

## Page 308

“*** 第 简 ”深入解析 性 编程
290    二       高 能 服务 器



public:
   process()          :   m_pid(  -1 ){}

public:
   pid_t        m_pid;
   int     m_pipefd[2];
MG

/* RRBR,                                                              “/
template<       typename     T   >
class          processpool
{
private:
      /*                                               HH create      processpool 实例    */
      processpool(           int  listenfd,  int  process_number    = 8 );
public:
                        单
      /*        体       式  ，以保证 程序         创建 一个processpool 实例 ，这 是 程序正确处理 信号 的 必要 条 侍  */
 static processpool< T >* create( int listenfd, int process_number = 8 )
  {
           -      if(      !m_instance     )
    {
    m_instance = new processpool< T >( listenfd, process number );
    }
    return m_instance;
  }
    ()
 ~processpool
 {
                  delete   []
   }
                           */
        void  run();

private:
      void      setup_sig pipe();
      void      run_parent();
      void      run_child();

    private:
      /*     进程   池        的 最 大子     数量    “7
      static      const      int  MAX_PROCESS  NUMBER  = 16;
      /*     每 个 子         最雪 能   处理的 客户 数量   */
      static      const      int  USER_PER_PROCESS  =  65536;
      /*     epoll                    */
      static      const      int  MAX_EVENT_ NUMBER =  10000;
                  油中的进竹总数          “/
      /*     进程
      int     m_process_number;
      /*      子 进 程在池 中 的 序号 ，从 0 开始          “/
      int     m_idx;
      /*                          epoll                    em         */
      int     m_epollfd;
      /*          socket     */
      int     m_listenfd;
      /*      子 进 程   通过 m_ stop   来 决定    是否 停 止 运行 “/

## Page 309

15  池和 池
第   章 ”进程
    线程   291



       int m_stop;
           保存所有 子 进 程的描述 信息                “/
       process*     m_ sub process;
                  池 态实
                           例
       /*  进程                “/
       static     processpool<         T >*   m_instance;

template<     typename     T >
processpool<      T >*    processpool<          T  >::m_instance    = NULL;

/*                 HTH.                                             HM   “/
static    int

static    int  setnonblocking(            int   fd   )
{
          int  old_option        =  fcntl(      fd,   F_GETFL  );
          int  new_option        =  old_option        | O_NONBLOCK;
          fentl(    fd,   F_SETFL,       new_option       );
          return
}

static    void addfd(      int        epollfd,    int   fd )
|
          epoll     event  event;
          event.data.fd      =      fd;
          event.events       =   EPOLLIN     |    EPOLLET;
          epoll_ctl(      epollfd,             EPOLL_CTL_ADD,  fd,  sevent );
          setnonblocking(           fd   );
}

    从             标识                内    事件
/*       epollfd          的epoll                  中删除   fd  上 的 所 有 注册   事件 */
static    void    removefd(        int   epollfd,     int  fd  )
{
          epoll _ctl(     epollfd,             EPOLL_CTL_DEL,  fd,  0 );
          close(    fd   );
)

static    void sig_handler(            int  sig    )
{
          int  save_errno        =  errno;
          int  msg   =   sig;
          send(   sig_pipefd(1l),          (  char*     )émsg,  1,  0 );
          errno   =
)

Static    void    addsig(      int  sig,     void(    handler   ) (int), bool restart = true )
{
          struct         sigaction  sa;
          memset(   &sa,     '\O0',      sizeof(     sa )  );
          sa.sa_handler        =   handler;
          if(  restart     )
          {
                       sa.sa_flags  |=   SA_RESTART;
          }

## Page 310

+ 二 解析 高 能 服务 器 编程
292 “** 第 一 篇 ”深入 性



&sa.sa_mask );
sigaction( sig, &sa, NULL ) != -1 );
    .


/*                                    EF socket,
直接 引用 它。     process     number     指定 进程 池 中 子 进 程 的 数量  “7
template<     typename     T   >
 processpool< T >::processpool{ int listenfd, int process_number )
    listenfd , m_process_number( process_number , m_idx({ -1 ,
    m_stop( false )
 {
    assert(        ( process_number   >  0 ) && (                   <= MAX PROCESS NUMBER )    );

       m_sub  process      =   new   process[ process   number  ];
       assert(     m_sub_ process    };

                                                             process_number
       /*                                            个子进程，并建立它们和父进程之问的管道 */
       for(   int    i =  0;   i <   process_number;    ++i )
       {
             int   ret =  socketpair(  PF_UNIX,    SOCK_STREAM,  0, m_sub_process[i).m_pipefd  );
             assert(     ret   ==   0 );

             msub_process[i].m_pid     =
                               m_sub_process(i].m_pid  >= 0 );
             if(   m_sub_process[i].m_pid     >     0  )

         .
                      close(   m_subprocess[i}).m_pipefd[1)      );
                     continue;
             b
             else
             {
                      close(   m_subprocess[i).m_pipefd[0)       );
                     midx    =   ij;
                      break;

       }
)

(=               */
template<     typename     T   >
vold   processpool<       T       >::setup_sig_pipe()
{
                                监听     管道
       /*    创建   epoll   事件        表 和 信号 “/
       m_epollfd       =   epoll_create( 5
       assert(     m_epollfd      !=  -1 });

       int    ret  =   socketpair(    PF_UNIX,  SOCK_STREAM,    0,  sig_pipefd );
       assert(     ret   '=  -1   );3

       setnonblocking(                );
       addfd(        m_epollfd,   sig_pipefd[0]    );

           设置信号 处 理 函数 */
       addsig(     SIGCHLD,      sig_handler  );

## Page 311

池和 池 *%*
第 15 章 ”进程
    线程    293



       addsig(        SIGTERM,   sig_handler
       addsig{ SIGINT,         sig_handler  );
       addsig(        SIGPIPE, SIG_IGN  );
}

              midx      MA -1,    FRBP  midx  MATSEF 0,
还是子 进 代码    */
template<      typename   T  >
void processpool<       T   >::run()
{
      if(   m_idx   !=  -1   )
      {
            run_child();
            return;   .
      }
                    ();
      run_parent
}

template<      typename   T  >
void  processpool<      T   >::run_child()
(                                           .
              sig
      Setup
                       pipe();

      /*                                    找到 与 父 进 程通信 的 管道 */
      int   pipefd   =  m_sub_process(m_idx]).m_pipefd[ 1 ];
      /*                                pipefd,        accept   HAH */
      addfd(    m_epollfd,     pipefd  );

      epoll_event events[        MAX_EVENT_NUMBER  ];
      T*    users   =  new T   [ USER_PER_PROCESS  ];
      assert(     users   );
      int   number   =  0;
      int   ret   = -l;


    while( ! m_stop )
    {
    number = epoll wait ( m_epollfd, events, MAX_EVENT_NUMBER, ~1 );
       if((number            <    0  )   &&   ( errno   != EINTR ) )
       {
        printf(                 “epoll        failure\n"   );7
        break;
       )

       for(int  i            =  0;   i   <  number;    i++ )
       {
        int     sockfd               =   events[i).data.fd;
        if((sockfd                   ==    pipefd )   6&   (        & EPOLLIN )            )
        {
                  int                 client  = 0;
                  /*            MR.               client                                      MRE
RA,                          */
                ret             =      recv(  sockfd,  (   char* )aclient, sizeof( client  ,  0 );
                                    ret    <  0 ) &&  (   errno  != EAGAIN ) ) [| ret == 0 )

## Page 312

    Se ”深入 解析 性 服务
    294 第 二 简    高 能 器 编程




          {
               continue;
          }
          else
          {
               struct   sockaddr_in     client_address;
               socklen_t       client_addrlength    =  sizeof(        );
               int connfd      =  accept (m_listenfd,  ( struct   sockaddr* )
                                       &client_address, &client_addrlength          di
               if  (connfd      < 0  )
               [
                        printf(   "errno is:    ¢d\n",    errno   );
                        continue;
               |
               addfd(   m_epollfd,     connfd   );
               /*  模板类 了    T  必须 实现   init 方法  ，以 初始     一   个客户 连接。我们     使用
    处  象          )，             效率               化        直接                       connfd
来 索引 还 辑 理 对 〈T 类 型 的 对 象 以 提高 程序 */
        users(connfd).init( m_epollfd, connfd, client_address );
        }
        }
        /* 下 面 处 理子进程接收 到 的 信号 */
        else if( ( sockfd = sigpipefd(0) ) 65 ( events[il .events & EPOLLIN ) )
        {
        int sig;
          char        signals[1024);
          ret  =  recv(        sigpipefd[0),  signals,   sizeof(    signals , 0 );
          if(  ret  <=  0     )
          {
                      continue;
          )
          else
          {
               for(     int  i =  0;   i < ret; ++i )
               {
                    switch(      signals[i])  )
                    {
                             case  SIGCHLD:
                             {
                                   pid_t   pid;
                                   int   stat;
                                   while  ((pid = waitpid( -1,    &stat, WNOHANG) ) > 0 )
                                   {
                                          continue;
                                   }
                                   break;
                             }
                             case  SIGTERM:
                             case  SIGINT:
                             {
                                   mstop =    true;
                                   break;
                             )

## Page 313

15  地
第   和 池 + 295
    进程
    线程



                                         default:
                                                 {
                                                      break;
                                                 }
                                        }
                                    J
                          )
                    }
                                         据，         那 么 迷     客户    请 求 到 。  调 用 逻辑 处理对 象的process  处理之
                 /*       如 果 是     其  他 可 读数                        来            方法     */
                 else      if(                                &  EPOLLIN  )
                    {
                                  users[sockfd] .process         () ;
                    )
                 else
                    {
                                  continue;
                    }
          }
     }

     delete    [])     users;
     users   =   NULL;
     close(   pipefd         );
     # close(        m_listenfd          );   /*                          URRRH:     HR   mlistenta 的 创建
                       CLIX),           PRA OM Cw                                 由     哪个
建 ，就 应该由 哪个函数 销毁           ”*/                                                    函数 创
     close(   m_epollfd              );
}

template<    typename             T  >
void  processpool<           T                        ()
                                    >::run_parent
{
              sig
      setup
                               pipe();

      /*                   m_listentd     */
      addfd(                m_epollfd, m_listenfd       };

      epoll_event               events(   MAX_EVENT_NUMBER          };
      int                                       =   0;
      int    new_conn        =      1;
      int    number     =    0;
      int    ret     = -1;

      while(     !     m_stop        )
      {
             number        =      epoll     wait(   m_epollfd,      events,   MAX_EVENT_NUMBER, -1 );
             if      ( (   number       < 0   )   &&  ( errno       != EINTR  )  )
             {
                       printf(         “epoll    failure\n"
                       break;
             ]

## Page 314

   篇  能 编程
   296 第 二 深入 解析 高 性 服务 器




         for    (  int  i  =   O;     i  <   number;       i++  }
         {
                int sockfd       =    events[i] .data.fd;
                if( sockfd       ==      m_listenfd        )
                {
如 果 有 新                                         到     来  ，  就采用 Round      Robin 方式将共     分  一 个子进 程 处理     */
                        int    i   =     Sub
                        do
                        {
                                 if(     m_subprocess[i].m_pid                    != -1 )
                                 {
                                           break;
                                 }
                                 i    =   (i+1)%m_process_number;
                        }
                        while(        i   != sub      process counter             );

                        if(    m_sub_process[(i].m_pid                   ==    -1  )
                        {
                                 m_   stop   =    true;
                                 break;
                        }
                        sub_process_counter                  =
                        send(
                                   (     char*    )&new_conn,                        mew_conn ,    0
                        printf(          “send    request     to   child       %d\n",    i );
                }
                /*  下       处理 父   进程接收 到         的    信号   */
                else   if(  (  sockfd        ==  sigpipefd[0)          ) 664   (   events[i]  events & EPOLLIN   ) )

                       int      sig;
       char signals[1024];
       ret = recv( sigpipefd[0], signals, sizeof( signals , 9 );
       if( ret <= 0 )
       {
       continue;
       }
       else
       {
                                  for( int        i    =  0;  i  <      ret;  ++i    )
                                  {
                                            switch(                     )
                                         {
                                                 case     SIGCHLD:
                                                 {
                                                          pid_t      pid;
                                                          int   stat;
                                                          while  (  (  pid   = waitpid(  -1, &stat, WNOHANG ) )  > 0
                                                      {
                                                                  for(     int   i =  0; i <  m_process_number;  ++i  )
                                                                  {
                                                                           进程         子     进程     了     则 主 进        基
                                                                  /*       果         池中第     退出 ，     程
                                              为 -1，以    标记    该 子   进  程   已经过   出   */
   闭相应 的 通信 管道 ，并 设置   相应的m_   pid

## Page 315

章 池和 池 s%*
第 15 ”进程
    线程    297



                                   if(   m_sub_process[i].m_pid.== pid     )
                                   {
                                           printf(   "child td join\n",     i
                                           close(    m_subprocess[i).m_pipefd[0] );
                                           m_subprocess[i].m_pid = -1;
                                    }
                            }
                       }
                       /*  如 果 所 有 子       进程 都 已 经  退出 了，则 父 进 程也退出       */
                       m_stop  =     true;
                       for(   int    i   = 0;      i < m_process_number;  ++i   )
                       {
                            if(                      :=     -1     )
                            {
                                    m_stop       =   false;
                            }
                       }
                       break;
                  )
                  case  SIGTERM:
                  case  SIGINT:
                  {
                       /*
SR,
之
实现 */
                        printf( “kill            all   the clild now\n" );
                        for(  int      i   =   0;    i < m_process_number;  ++i  )
                        {
                              int      pid     =   m_sub_process[i]) .m_pid;
                              if(      pid     !=    -1  )
                              (
                                           kill(     pid,  SIGTERM });
                              )
                       }
                         break;
                   }
                  default:
                  {
                       break;
                  }
                 }
          }
         )
     }
     else
     {
         continue;
     }
    }
    }

                           者               文件        符     )
    close( m_listenfd ); /* 由 创建 关闭 这 个 描述 《 见 后 文 */
    close( m_epollfd );

## Page 316

篇    编程
298 *+ 第 二 深入 解析 高 性 能 服务 器



}

fendif
eee





，                                               非常    的  器。  们
回忆    6.2 节       我 们 曾   实现 过      一 个            简单   CGI 服务 下 面 我 将 利用 前 面 介绍 的 进
程 池 来 重新 实现       个 并   发   的  CGI  服务
一                                               器， 如 代码  清单 15-2 所 示 。
                                                       器
                        代码 清单       15-2  ”用    进程 池 实 现 的  并 发 CGI 服务
#include      <sys/types.h>
              <sys/socket.h>
              <netinet/in.h>
finclude      <arpa/inet.h>
finclude      <assert.h>
#include      <stdio.h>
finclude      <unistd.h>
finclude      <errno.h>
finclude      <string.h>
finclude      <fentl.h>
#include      <stdlib.h>
#include      <sys/epoll.h>
#include      <signal.h>
finclude      <sys/wait.h>
#include      <sys/stat.h>

#include      "processpool.h"      /*     引用    上     一 节 介 绍 的 进程池 */

用     于     处 客户               请 求
/*        理       CGI           的类 它 可  以 作为           */
class  cgi_conn
{

      egi_conn(){}
                      {}
      ~cgi_conn()
      /*                                    +/
      void    init(   int   epollfd,    int     sockfd,   const       client_addr )
      {
              m_epollfd     =  epollfd;
              m_sockfid   =    sockfd;
              m_  address   =    client_addr;
              memset(   m_buf,      '\O', BUFFER_SIZE     );
              m_read_ idx      =  0;
      }

                      ()
      void    process
      {
              int   idx =   Q;
              int   ret =   -1;
              /*                    PR      */
              while(      true   )

## Page 317

    第 章  地和 池
        线程   299
      15 进程




                                          .

idx = m_read_idx;
ret = recv( m_sockfd, m_buf + idx, BUFFER_SIZE-l-idx, 0 );
    /*                                    «/
    if(   ret   <  0  )
    {
             if(   errno    !=     EAGAIN  )
             (
                   removefd(          m_epollfd, m_sockfd     );
             }
             break;
    }
    /*                                    */
    else   if(    ret   ==  0    )
    {
             removefd(      m_epollfd,     m_sockfd       );
             break;
    }
    else
    {
             m_read    idx    +=      ret;
             printf(         "user    content is:     ts\n",  m_buf );
             /*         如 果 通 到 字符        则 开始 处  理 客 户 请    求  */
             fort     ; idx   <     m_read_idx;   ++idx    )
             {
                              >=      1    && ( mbuf[ick-1] = '"\r" ) 6& (  mbuf[idk]) = '\n" ) )
                  if(   (          idx
                  {
                                break;
                  )
          }
                       有              特                                 数据
          /*  如 果     设 遇 到 字         “\rvn"，   则 需要     取 更 多 客户     “/
          if( idx      ==  m_read_idx     )
          {
                   continue;              ，
          }
          m_buf{      idx-1   ]      =


    char*      file   name           = m_buf;
                 客户要      的
                                    运行
    /*                              判断 CGI           */
    if(      access( file_name,           F_OK     ) == -1  }
    {
               removefd(           m_epollfd,      m_sockfd  );
               break;
    )
                   进程
                            来执行
    /*                              创建 CGI    程序 */
    ret      = fork();
    if(ret         == -1      }
    {
              removefd(        m_epollfd,     m_sockfd      );
               break;
    }
    else       if(    ret   >        0 )

## Page 318

篇  能
300 “¢ 第 二 深入 解析 高 性 服务 器 编程




                       {
                               /As                        */
                               removefd(           m_epollfd,  m_sockfd );
                               break;
                       }
                       else
                       {
                                              将
                                                标准                 执行
                               /*        子 进程      输出 定向 到     m_ sockfd， 并     CGI 程序 */
                                             STDOUT_FILENO     );
                               dup(       m_sockfd    );
                               execl(        m_buf,    m_buf,  0  );
                               exit(      0  );
                       }
                 )
          }
     ]

private:
      /*                       */
      static       const  int   BUFFER_SIZE        =    1024;
      static     int   m_epollfd;
      int   m_sockfd;
      sockaddr_in        m_address;
      char      m_buf[   BUFFER_SIZE         )];
      /*                                      REARS                */
      int   m_read_ idx;
he
int  ecgi_conn::m_epollfd            =    -1;

/*          */
int main(     int   argc,        char*    argv[) )
{
      if(   argc    <=  2   )
      {
                printf(     “usage:       ts   ip_address     port_number\n",       argv[0] ) );
                return    1;
      }
      const      char*   ip  =  argv([1];
      int   port    =  atoi(                   );

      int   listenfd      =  socket(         PF_INET,   SOCK_STREAM,    0 };
      assert(         listenfd  >=      0 );

      int    ret  =   0;
      struct     sockaddr_in          address;
      bzero(                    sizeof(       address     ) );
      address.sin_family              =   AF_INET;
      inet_pton(       AF_INET,         ip,  éaddress.sin_addr    );
      address.sin_port          =     htons(      port  );

      ret   =    bind(   Listenfd,        ({    struct  sockaddr*   )&address,   sizeof( address ) );
      assert(      ret   !=  -1     );

## Page 319

池和线程池 <> 301
第 15 章 进程


 ret =   listen( listenfd, 5
 assert(   ret  != -1 );

processpool< cgi_conmn >* pool = processpool< cgi_conn >::create( listenfd );
 if( pool  )
 {
         pool->run();
         delete  pcol;
 }
             前文提到的     ，main 函数 创建了文件描述符1istenfd， 那 么就由
 close(  listenfd  );   /* 正如
  */
 return  0;
}


15.5     半 同         步 /   半 反 应 堆 线程 池 实 现
           本 节 我们实现    一   个 基于 图 8-10  所 示 的 半 同 步    /半 反应 堆 并 发模式    的 线程 池，        如 代码 清
单 15-3       所 示。 相 比 代码      清单 15-1 所 示 的 进程       池 实  现 ， 该 线程池 的   通用 性 要 高 得   多， 因 为 它
使 用 一  个  工作     队列 完全     解除   了 主线 程     和工作      线程 的  耦合  关系: 主线 程 往 工作队 列 中 插入 任务      ，
工作 线程     通过 竞争来           取得 任务 并 执行   它    。 不 过    ，如  果 要 将 该 线程    池 应 用 到 实际 服务 器    程序
中 ， 那 么 我    们   必须 保证     所   有 客户 请 求 都 是 无 状态   的     ，  因 为 同一 个   连接上 的 不 同 请 求 可 能  会 由
                  处理。
不 同 的 线程

                              代码 清单     半 同步     /  半 反 应 堆 线程池 实 现
    /f filename:     threadpool.h
    #ifndef                   H
                     THREADPOOL
                            HA
    #define         THREADPOOL

    finclude      <list>
    finclude      <estdio>
    #include      <exception>
    #include      <pthread.h>
    /*   引用第      14 章 介绍 的 线程 同步机 制的包装 类        */
    #include      "locker.h"

    /*   REBAR,                                                   “/
    template<        typename T >
    class   threadpool
    {
    public:
             /*  #&    thread number  是 线程池 中  线程 的 数量    ，max_requests 是 请求队列 中 最 多 允许 的 、等 竺
处 理 的  请 求的数量
         threadpool(       int  thread_number  = 8, int   max_requests  = 10000 );
                      ();
         ~threadpool
                                  “/
            bool  append(   T*  request );

    private:

## Page 320

    “ww* 第 二篇”深入 性 服务
    302        解析 高 能 器 编程


    /* 工作 并   运行的      函数 ，  它  不 断 从   工作  队列 中 取出 任务 并 执行 之 */
    static    void*    worker(  void*   arg );
       void run();

   private:
                                            池中的线程数 */
    int                 /* 线程
    int     m_max_requestsi  /*          请求队列 中 允许 的最大 请 求数 */
                t*  m_threads;    /*                   HASH   m_thread number */
    pthread
    std::list<                                 请
                    T* >    m_workqueue;    /* 求队列     */
    locker              /*                               */
    sem     m_quevestat;  /*                   «/
    bool               /* 是 否 结束
            m_stop;                   线程  “/


    template<   typename     T >
    threadpool<    T  >::threadpool(      int      thread_number,  int           ) :
        m_thread_ number( thread_number , m_max_requests( max_requests ,
        m_stop( false , m_threads( NULL )
{
      if((      thread_number     <=    0 )    ||   ( max_requests  <=    0  ) )}
    {
                throw    std::exception();
    }

     m_threads                 pthread
                      =  new              上[   m_thread_ number   );
     if('m_threads         }
     {
         throw        std::exception();
     }

     /*           thread   number     个 线程   ， 并 将 它们 都 设置 为 脱离     线程    */
     for (      int   i  =  0; i <    thread_number;   ++i  }
     {
         printf(         "create   the    tdth  thread\n",  i   );
        if( pthread_create( m_threads + i, NULL, worker, this ) != 0 )
        {
        delete [] m_threads;
        throw std::exception();
        }
         if(       pthread     detach(    m_threads[i] )   )
         {
                      delete   []  m_threads;
                      throw    std::exception();

     }
    }

    template<   typename     T >
                                          ()
    threadpool<    T  >::~threadpool
    {
          delete   []   m_threads;                    .
    他        七  P  =  true;
    )

## Page 321

    15  池和线程池 + 303
    第




       template<    typename      T    >
       bool  threadpool<       T  >::append(    T* request  )
       {
             /* 操作 工作队列 时            一 定 要 加 ，  因 为 它 被 所有 线程 共享 */
             m_queuelocker.lock();
             if (   m_workqueue.size()     >    m_max_requests   )
             {
                m_quevelocker.unlock();
                return       false;
             }
                                          request     );
             m_queuvelocker.unlock();
                                       ();
             m_quevestat.post
             return true;
       }

       template<    typename      T   >
       void*  threadpool<      了     >::worker( void"   arg )
       {
            threadpool*         pool  = ( threadpool*   )arg;
            pool->run();
            return        pool;
       }

       template<    typename      T   >
       void  threadpool<       T  >::run{)
       {
             while    (  !   m_stop     )
             {
    .               m_queuestat.wait();
                    m_queuvelocker.lock();
                    if   (m_workqueue.empty()      )
                    {
                             m_queuelocker.unlock();
                             continue;

                    T*   request      =  m_workqueue.front();
                                          front ();
                    m_workqueuve.pop
                    m_queuelocker.unlock();
                    if   (   ! request    )
                    {
                             continue;
                    I
                    request->process();
            )
       }
       #endif

             一提的是，                              用 pthread create   时， 的第
    值得 在                       C++ 程序 中 使                   函数 该 函数     3 个 参数 必须 指
                   数。                                   用      员     成
                                                            员     数     员
回 一 个 静态 而 要 在 一 个 静态 函数 中 使 类 的 动态 成 (包括 函 和 成 变 量 )，

## Page 322

304          第 二    篇    深入  解析 高 性 能    服务器编程

则 只 能 通过     如 下 两      种  方式 来 实现  :
    CD 通过 类 的 静态对 象 来 调用。比 如 单体                        中，     函数 可 以 通过 类 的 全 局 唯一      实例 来
                               模式
             HAS ALA       PRK
                                  BUR                  PUR TERA PACE S|
             动态 方法    。
                             用的 是 第 2    种 方式 将 线程
    代码 清单           15-3 使        :                       参数设置为this 指针  ，然 后  在    worker 函数
中 获取 该 指针 并 调用 其动态 方法run。

             ”用                                            器
15.6                                    线程 池 实 现 的 简单 Web 服务

             中，我
         在 8.6 节           们曾    使 用 有 限状态 机 实现 过      个  非常  简单 的   解析 HTTP         请 求 的服务
                                                       一                                  器。
              我   将 利用 前 面
下 面          们             介绍  的       线程 池 来 重新 实现  一 个 并 发  的Web   器。
                                                                     服务

15.6.1               http_conn 类

         首先 ，我 们 需要 准备 线程池 的 模板 参数     类             ， 用以   封装 对 逻辑 任务 的 处  理 。这 个 类 是 http_
                                                         实现                 (http_conn.cpp)。
conn,               15-4   是  其 头 文件 (http_conn.h)，  代 码    清单15-5 是 其  文件
                               代码 清单    15-4     ”http_conn.h 文件
                                    H
         #ifndef       HTTPCONNECTION
         #define     HTTPCONNECTION_H

         #include   <unistd.h>
         #finclude  <signal.h>
         #include   <sys/types.h>
         #include   <sys/epoll.h>
                    <fentl.h>
         #include   <sys/socket.h>
         #include   <netinet/in.h>
         #include   <arpa/inet.h>
         finclude   <assert.h>
         #include   <sys/stat.h>
         finclude   <string.h>
         #include   <pthread.h>
         #include   <stdio.h>
         Winclude   <stdlib.h>
         #include   <sys/mman.h>
                    <stdarg.h>
         #include   <errno.h>
         #include   “locker.h"

         class    http_conn
         {
         public:
         /*                   */
                                        LEN = 200;
         static    const int       FILENAME

## Page 323

    15  地和 池
    第   线程  305



    /* 读     区     的 大小     *7
    Static     const   int   READ BUFFER_SIZE      =     2048;
    /* 写 冲区 的 大小 */
    static     const   int   WRITE _
                                        BUFFER   SI2B  =  1024;
    /* HITP    请 求 方法 ，但 我们 仅 支持           GET */
    enum METHOD     {    GET  = 0,  POST,      HEAD,   PUT,   DELETE,
                         TRACE,   OPTIONS,     CONNECT,      PATCH   };
    * MHSPRMRH, EKRAUMARMRKA (HIB SR)                             */
    enum CHECK STATE         {   CHECK STATEREQUESTLINE           =  Q,
                                 CHECK_STATE_HEADER,
                                 CHECK_STATE_CONTENT        };
       服务
    /*       器处理      HTTP   请 求  的可 能 结果
    enum HTTP_CODE       {   NO_REQUEST,       GET_REQUEST,       BADREQUEST，
                             NO_RESOURCE,                              FILE_REQUEST,
/* 行的读 取 状态 “/ INTERNAL ERROR, CLOSED CONNECTION };
    enum LINE_STATUS         {   LINE_OK =     0,    LINE_BAD,     LINE_OPEN };

    public:
         http_conn(){)
                            {}
         ~http_conn({)

    public:
             初始化新接受 的 连接 */
         void  init(      int    sockfd, const     sockaddr_iné        addr  );
         /*    KARR */
         void  close_conn(        bool   real_close       =  true   );
                         请求     “/
             处 理客户
         void
         /*
         bool  read();
         /*                     */
         bool  write();

    private:
                       初始
                       连接     “7
         /*           化
         void  init();
         /* 解析     HTTB       AR */
         HTTP_CODE       process_read();
         /* 填充     HTTP       BE */
         bool  process_write(            HTTP_CODE    ret   );

 这一组函数被    调用
         /*    下面                     Process_read           以分析       RR   */
         HTTP  CODE      parse       request_line(     char*  text  );
         HTTP_CODE       parse_headers{        char*    text  );
         HTTP_CODE       parse_content({       char*    text  );
         HTTP  CODE      do
         char* get_line()          (    return m_read_buf        + m_start_line; }
         LINE  STATUS            parse_line();

         /*    下 面 这   一 组 函数    被   process   write 调用    以  填充        “/
         void  unmap();
         bool  add_response(            const  char*       format, ... )j

## Page 324

   篇  性
   306 ++ 第 二 深入 解析 高 能 服务 器 编程


 bool                  (     const  char*     content         );
 bool          add_status_line(     int   status,      const    char* title );
 bool          add_headers(    int  content_length           );
 bool          add_content_length(      int     content_length     );
 bool          add_linger();
 bool          add_blank_line();

   public:
               FR      上  的 车 件 都 被             到 同一   个                    MUM     文件
socket                                                 epoll                    epoll   描 荐设置为
   pas */
         static    int  m_epollfd;
             统计        */
         /*        用户数量
         static    int m_user_count;

   private:
         /*     HTTP   连接 的socket   和 对   方    的  socket    HH  */
         int   m_sockfd;
         sockaddr_in     m_address;

         /*    读 缓 冲区
         char   m_read_buf[   READ_BUFFER_SIZE           ];
         /*                                                                 */
         int   m_read_idx;
         /*    当前 正在 分 析 的 字符 在 读 暖冲区 中 的 位置             “7
         int   m_checked_idx;
         /*    当前 正在 解新的 行 的起始 位置              “/
         int   m_start_line;
         /*    写 缓冲区 “7
         char    m_write_buf[ WRITE_BUFFER_SIZE              ];
         /*            EHR              */
         int   m_write_ idx;

         /*    主     机 当前 所 处 的 状态 */
                            m_check_state;
         /*    请 求 方法 */
         METHOD m_method;

         /*    赛 户 请 求 的目标 文件的          路     ，   其 内  等于                          网
                                              径        容     doc_root + murl,  doc_root 是 站 根 目 录 */
                                              LEN    ];
         char   m_real file[  FILENAME
         /*                                   */
         char*     m_url;
                       版本号 ， 我    们   仅 支持                  */
         /*    HTTP  协议
         char*     m_version;
         /*    ERA     */
         char*     m_host;
         /*    HTTP  请 求的消息 体 的 长度  */
         int   m_content_length;
         /*    HTTP  请 求 是否要 求 保持 连接      “/
         bool    m_linger;

         /*    客户 请 求 的目     文件被 mmap     到 内     存  中 的 起  始 位置 “7
         char*     m_file_address;
                                                                HE.            RETR, HRMKHANS

## Page 325

15  池和 池
 第  线程  “:* 307



 信息 */
         struct stat   m_file stat; 写 ，  定义    ， 中
               我 将 采用              来执行
 的 数量 */ /*     们      writev       操作   所 以   下 面 两 个 成 员 其 m_iv_ count
         struct lovec  m_iv[2];
         int m_iv_count;
   he

   fendif



                                        代码   清单
                                                     15-5 http_conn.cpp 文件

 #include       “http_conn.h"

                             状态
 /*  定义        HTTP  响   的 一 些       信息    “/                                                 ，
 const      char*    ok_200 title      =   "OK";
                                 400 title   =    "Bad   Request";
 const      char*    error
 const      char*       error_400    form       =    "Your  request  has  bad syntax or   is  inherently
 impossible to  satisfy.\n";
                                 403 title   =    "Forbidden";
 const      char*    error
 const      char*                    error  =     “You   do  not have  permission to get  file from this

 const      char*    error_404       title   =    "Not   Found";
                                  404    form     =    "The     requested file was not   found on   this
 const         char*                    error

                                 500 title   =    "Internal     Error";
 const      char*    error
                                  500    form     =   "There     was an   unusual problem serving    the
 const         char*                    error
 requested  file.\n";
 /*  网 站        的 根目录
 const      char*    doc_root     =  "/var/www/html1";

 int  setnonblocking(           int    fd  })
 {
            int      old_option   =  fentl(       fd,  F_GETFL   });
            int      new_option   =  old_option       |  O_NONBLOCK;
            fentl{      fd,   F_SETFL,    new_option        );
            return      old   option;
 )

 void      addfd({   int     epollfd,  int       fd,   bool  one shot   )
 {
           epoll    event     event;
           event.data.fd      =   fd;
           event.events      =  EPOLLIN      |      EPOLLET  |   EPOLLRDHUP;
           if(  one_shot     )
           {
                event.events        |= EPOLLONESHOT;
           )
           epoll_ctl(        epollfd,           EPOLL_CTL_ADD,   fd, éevent );
           setnonblocking(        fd   );
 I

## Page 326

二篇 解析 性 编程
308 * 第 = 深入 高 能服务 器
    *%*



     void  removefd(     int   epollfd,      int fd     )
     {
           epoll_ctl(      epollfd,      EPOLL_CTL_DEL,        fd, 0 );
           close(   fd   );
     )

     void modfd(    int    epollfd,      int  fd,    int ev    )
     {
.          epoll  event event;
           event.data.fd       =   fd;
           event.events      =   ev  |   EPOLLET     |  EPOLLONESHOT   |  EPOLLRDHUP;
           epoll_ctl(      epollfd,      EPOLL_CTL_MOD,        fd, sevent );
     }

     int  http_conn::m_user_count           = 0;
     int  http_conn::m_epollfd           = -1;

     void  http_conn::close_conn(            bool                  }
     {
           if(                   && (    m_sockfd       '=        )
           {
                  removefd(        m_epollfid,     m_sockfd     );
                  m_sockfd       =  -1;
                                                              /* 关闭一个连接时，将客户总量减 1 “7
                  m_user_count--;
           )
     }

     void  http_conn::init(         int    sockfd,      const  sockaddr_ins&  addr }
     {
           m_sockfd    =   sockfd;
           m_address     =   addr;
               如 下 两 行 是 为     了避免 TIME_WRAIT            ，仅    用 于 调试 ，实际他    用 时 应 该去掉 */
    int reuse = 1;
    setsockopt( m_sockfd, SOL_SOCKET, SOREUSEADDR, Greuse, sizeof( reuse ) });
    addfd( m_epollfd, sockfd, true );
    m_user count+t+;

           init ();
     }

     void
     {
           m  check   state   =    CHECKSTATE_REQUESTLINE;
           m linger   =    false;

           m method   =    GET;
           m_url  =  0;
           m_ version    =   0;
           m_content_length         =   0;
           m_host   =  0;
           m_start_line      =   0;
           m_ checked_idx     =    0;
           m_read   idx    = 0;

## Page 327

15 章 池和 池
第 ”进程
    线程   *%* 309


      m_write         idx =   0;
      memset(             m_read_ buf,   '\0',                        );
                  ( m_write_buf,          '\O0",    WRITE_BUFFER_SIZE   );
      memset(             m_real file,   '\O0*,   FILENAME_LEN        );
)

    从 其 析 请 参考                                              */
/*  状太机，              分
http_conn::LINE_ STATUS                  http_conn::parse_line()
{
      char     temp;
      for    (  +     m_checked_idx          < m_read_idx;        ++m_checked_idx      )
      {
             temp     =   m_read_ buf[        m_checked_idx         ];
             if     ( temp    ==          )
             {
                            if  (     (   m_checked_idx     +     1 ) == m_read_idx      )
                            {
                                         return   LINE_OPEN;
                            )
                            else     if   (    m_read_buf[     m_checked_idx       +   1 ] == '\n'" )
                            {
                                         m_read_buf[{    m_checked_idx++     ]     =  '\0';
                                                  buf[   m_checked_idx++     ]     =  '\0';
                                         return   LINE_OK;
                            )

                            return        LINE_BAD;

                else      if(     temp    ==   '\n' )
                {
                            if(  (                     > 1  )  &&   ( m_read buf[ m_checked_idk - 1 == '\r' ) )
                            {
                                          m_read    buf[    m_checked_idx-1     ]    =   '\O';
                                          m_read    buf[    m_checked_idx++     ]    =   '\O';
                                          return    LINE_OK;
                            }
                            return

      }

      return     LINE OPEN;
)

/*     读取客户数据，              直 到      无   数据 可  读 或  者对 方 闭     连  接   */
bool  http_conn::read()
{
      if(    m_read_idx        >=     READ   _BUFFER_SIZE      )
      {
               return     false;
      )

      int                      =    0;
      while(     true     }

## Page 328

 篇 解析    编程
 310 第 二 深入 高 性 能 服务 器




     {
        bytes      read  = recv(  m_sockfd,         m_read_buf    + m_read_idx, BUFFER SIZE -
                                  m_read idx,          0 );
        if    (   bytes_read     ==   -1 )
         (
                    if({ errno   ==      EAGAIN  ||   errno   ==  EWOULDBLOCK   )
                    i
                             break;
                    )
                    return    false;
         )
        else      if  (  bytes_read      ==   0  )
         {
                    return    false;
         )

        m     read   idx   +=       bytes read;
  }
  return      true;
 }

            请求行，                    、    标                 版本号
  SMU HTTP 著 得 请 求 方法                            目
                                              URL，    以 及 HTTP */
 http_conn::HTTP_CODE      二  PP_CCnn                 request 1inef char* text   )
 {
  m_url     =  strpbrk(    text,    "    \t"   );
if   (   !    m_url   )
 {
              return     BAD  REQUEST;
 }
  *m_url++       =  "\O';

  char*     method    =  text;
if   (  streasecmp(       method,        "GET"   )    == 0 )
 {
              m_method     =  GET;
 )
  else
 {
              return          BADREQUEST;
 )

  m_url     +=   strspn( m_url,       "    \t"  );
 m      version   =  strpbrk(                "  \t"    );
if   (   !    m_version     )
 {
              return         BAD_REQUEST;
 )
  *mversiont+         =  "\O';
  m_version       +=  strspn(    m_version,         "  \t"
  if (  strcasecmp(       m_version,      “HTTP/1.1"       )  !=    0 )
 {
              return         BAD REQUEST;
 }

## Page 329

    15 章 池和 池
    第    ”进程
        线程   311




       if  (   stUrncasecmp(         m_url,         “http://",         了 ) ==   0 )
    {
                                 +=  7;
                     mourl      =   strchr(  m_url,             '/'  );
    }

       if  (   !     murl       |]   murl(   0 !=                    1
    {
                         return   BAD REQUEST;
    }

       m_check_state              =  CHECK_STATE_HEADER;
       return        NOREQUEST;
    上

    /* 解析            请 的 一 个 头       部
                                      信息
                         :HTTP CODE  http_conn::parse_headers{           char*           text   )
    http_conn:
    {
       * RAST,                                      EE       */
       if(    text{ 0       ]     == "\a'   )
        [
        /* 如果 HTTP 请求有 消息 体 ，则 还 需要 谈 取 m_content_length 字 节 的 消息 体 ，状 机 转移 到
CHECK_STATE_CONTENT 状态 */
           if     {  m_content_length               !=   0    )
           {
                       m_check_state         =      CHECK       _STATE_CONTENT;
                             return  NO_REQUEST;
           )

                                                                的   HTTP
           /*     否则 说 明 我们 已 经       到      了     一  个  光 整             AR */
           return           GET   REQUEST;
       }
       /*  处理                                    头
Connection                                     部字段  +/
       else      if  (     strncasecmp(      text,            "Connection:",     11   )   ==  0  )
       {
                  text       +=   11;
                  text       +=   strspn(   text,        "    \t"
                  if   ({   streasecmp(      text,            "keep-alive"   )    ==  0   )
                  {
                            m_linger     =   true;
                  )
       }
       /*  XH              Content-Length    部字段 头            */
        else if ( strncasecmp( text, "Content-Length:", 15 ) == 0 )
        (
                  text       +=   15;
                  text       +=   strspn(   text,        "        J);
                                             =      atol(       text   );
       }
       /*  处理        Host 头     部   字段 */
       else      if  (     strncasecmp{      text,            "Host:",   5 )  ==  0   )
       {

## Page 330

+ 篇  性
  二  深入 解析 高 能 服务 器 编程
312 *° 第 _



                  text    +=  5;
                  text    +=  strsepn(          text,       " \t"
               m_host     =    text;
      }
      else
      {
               printf(        “oop!         unknow       header           text  );
      )

      return   NO_REQUEST;

}

      我  没有        解析                       的            ，      是           完整
/*      们    真正          HTTP   请 求             消息     体    只 是 判断 它  否 被      地 读  入 了  */
                                                                             char*  text )
{
      if  (         m_read_idx  >=        (         m_content_length  + m_checked_idx     ) )
      {
               text[     m_content_length                    ] =
               return
      }

      return   NO_REQUEST;
}

/*                              8.68,                        “/
http_conn::HTTP_CODE            http_conn::process_read()
{
      LINE_STATUS        line_status            =     LINE_OK;
      HTTP_CODE          ret  =
      char*       text    = 0;

    while    ( (   (          m_check_ state     ==                        )   &&  (        == LINE_OK  ) )
                ¢    (                          =                     ) == LINE_OK      ) }
      {
            text     =           get_line();
            m_start      line     =       m_checked_idx;
                         "got     1       http     line:     %s\n",  text );

                switch    (  m_check_state               )
            {
                     case     CHECK STATEREQUESTLINE:
                     {
                             ret     =      parse     request_line(     text   );
                             if   (       ret ==     BAD_REQUEST      )
                             {
                                            return          BAD_REQUEST;
                             I
                              break;
                     |
                     case     CHECK         STATE HEADER:
                     {
                             ret     =                       text       );

## Page 331

第 章 进程
  15  池和线程池 fe 313



                              if (    ret ==   BAD REQUEST         )
                              {
                                        return     BAD REQUEST;
                              )
                              élse    if  ( ret    ==     GET_REQUEST    )
                              {
                                         return                    ();
                              }
                              break;
                        }
                       case   CHECK    STATE_CONTENT:
                        {
                              ret   =  parse_content(           text  );
                              if (    ret ==   GET_REQUEST         }
                              1
                                                                 request
                                        return     do              ();
                              }
                              line_status     =    LINE_OPEN;
                              break;
                        )
                       default:
                        {
                                  return             INTERNAL ERROR;
                         )
                }
         }

         return    NOREQUEST;
)

                                          请求时，          我们就 分 析  目       标 文件
/*     当 得 到 一 个 完整       、正 确   的HTTP                                 的   属性 。如 果 目 标 文件 存在 、对 所 有 用 户 可
读 ，且 不 是  目录，  则 使    用 mmapP 将 其 陵 射 到   内 存 地    址   m_ file_ address 处 ， 并 告诉调用 者 文件 成    功 */
                                                                  ()
                                               http_conn::do_request
{
         strcpy(      m_real file,     doc_root        );
         int   len    =  strlen(      doc_root     });
         strncpy{      m_real file     + len,      m_url,     FILENAME_LEN - len - 1 );
         if (   stat(     m_real  file,     &m_file_     stat }) <    0  }
         {
                   return     NO_RESOURCE;
         }

         if (   !  (   m_file_stat.st_mode         &     S_IROTH   )    )
         {
                   return     FORBIDDEN REQUEST;
         }

         if (   S_ISDIRI      m_file_stat.st_mode         )   )
         (
                   return     BAD_REQUEST;
         }

         int   fd  =   open(      m_real file,     O_RDONLY     );

## Page 332

   篇 解析  编程
   314 “%* 第 二 深入 高 性 能服务 器


      m_file_ address = ( char* )mmap( 0, m_file_stat.st_size, PROT_READ,
       MAP_PRIVATE, fd, 0 );
      close( fd );
      return FILE REQUEST;
}

     /*                                    munmap  操作 “/
                                               ()
     void     http _conn::unmap
     {
              if(       m_file_address             )
              {
                        munmap{            m_file    address, m_file  stat.st_size   );
                                  m_file_ address    = 0;
              )
     }

     /*    写  HTTP      响应      */
     bool                                :write()
     {
              int       temp    =    0;
              int       bytes_have_send                = 0;
              int                                  =   m_write_idx;
              if     {               to
                                            bytes    ==     )
              {
                         modfd(         m_epollfd,     m_sockfd,      EPOLLIN     );
                         init();
                         return             true;
              }

              while(       1    )
              (
                         temp        =  writev(        m_sockfd,    m_iv, m_iv_count );
                         if      (   temp      <=    -1  )
                         {
                                       /*                                 USER        事件。               虽   在 此 期 间，  服
   无                法立即 接收 到 同一 客户   的 下     一 个  请    求 ， 但这 可以 保证 连接    的 完整    性 “/ EPOLLOUT                       务
                                       if(     errno     ==  EAGAIN )
                                       {
                                                  modfd(     m_epollfd,   m_sockfd,    EPOLLOUT    );
                                                  return     true;
                                       }
                                    unmap ();
                                           return     false;
                        }

                                    _to_
                                            bytes
                                             send    -=  temp;
                        bytes _have_send               +=   temp;
                        if    (     bytes_to_send      <=          bytes_have_send  )
                        {
                                    /*      发送 HTTP              HEE RTT    请   求中 的 Connection 字段   决定 是 否 立 即 半 连接 */
                                    unmap ();
                                       if(     m_linger      }
                                       (

## Page 333

第 15  池和线程池 fe 315



                                  init();
                                  modfd(      m_epollfd,     m_sockfd,  EPOLLIN  };
                                  return  true;
                             }
                             else
                             {
                                  modfd(      m_epollfd,     m_sockfd,  EPOLLIN  );
                                  return  false;
                             }
             }
      I
)

            缓冲                        数据
/* 往 写     中     写 入 竺 发 送       的     “/
bool  httpconn::add_response(              const       char*   format,  ...  )
{
      if({  m_write_idx          >=                     SIZE
                                      WRITE_BUFFER )
      (
                  return     false;
      I
      va_list
          arg                   list,  format   );
      int   len    = vsnprintf(    m_write buf    + m_write_idx, WRITE_BUFFER_SIZE     - 1 ~ mm write idx,
                                   format,      arg_list  );
      if({     len   >= (                            - 1   - m_write_idx     ) )
      {
                  return     false;
      )
      m_write_idx            += len;
      va_end(        arg     list  );
      return        true;
}

bool  http_conn::add_status_line(                int   status, const    char*     title )
{
      return addresponse(             "ts    td  ts\r\n",   “HTTP/1.1",     status,     title   );
}

bool  http                                  int  content_len   )
{
      add_content_length({            content_len
      add_linger();

)

bool  http_conn::add_content_length(                 int  content_len   )
{
      return     addresponse(         "Content-Length:                  content_len     );
}

bool  http_conn::add_ linger          ()
{
      return     addresponse{     "Connection:  ts\r\n",  (  m_linger = true   ) ? "keep-alive" : "close" );

## Page 334

篇  性 服务器编程
二  深入 解析 高 能
316 第 —




)

bool    http_conn::add_blank_line()
{
        return  add_response( "%s", "\r\n"  );
}

bool                            const     char* content   )
{
        return  addresponse(     content     );
}

/* 根据服务 器 处理      BTTB 请 求 的结果，决 定返回 给 客户 端的内容     wy7
bool    http_conn::process_write(   HTTP_CODE   ret )
{
        switch  ( ret )
        {
         case     INTERNAL_ERROR:
             {
                  add_status_line(  500,                 title   );
                  add_headers(    strlen(   error_500_form   )   );
                  if  ( !   add_content(  error_500 form   )    )
                  {
                            return false;
                  }
                  break;
             I
         case     BAD REQUEST:
             {
                  add_status_line(  400,      error_400_ title   );
                  add_headers(    strlen(   error_400_form   )   );
                                                400 form   )    )
                  if  ( !   add_content(           error
                  {
                            return false;
                  )
                  break;
             }
         case NO_RESOURCE:
             {
                  add_status_line(  404,    error_404    title   );
                                  strlen(   error_404 form   )   );
                  if  ( !   add_content(  error_404 form   )    )
                  {
                            return false;
                  }
                  break;
             }
         case     FORBIDDEN   REQUEST:
             {
                  add_status_line(  403,    error_403_title      );
                  add_headers(    strlen(   error_403_form   )   );
                                                 403 form  )    )
                  if  ( !   add_content(            error

## Page 335

15 音 池和 池
第 ”进程
    线程   + 317



                    {
                                      return false;
                    }
                    break;
              }
              case  FILE     REQUEST:
              {
                    add_status_line(             200,   ok_200_title   );
                    if   (   m_file_stat.st_size           != 0 )
                    (
                             add_headers(        m_file_stat.st_size     );
                                       0     ].iov_base    =  m_write buf;
                             m_iv{     0     ).iov_len   =   m_write_idx;
                             m_iv{     1                   =  m_file_address;
                             m_iv{     1     ]).iov_len  =   m_file stat.st_size;
                             m_iv_count         =  2;
                             return         true;
    }
    else
    {
    const char* ok_string = "<html><body></body></html>";
                             add_headers(            strlen(  ok_string ) );
                             if  (  !       add_content(     ok_string  ) }
                             {
                                            return    false;
                             }
                    ]
              )
              default:
              {
                    return      false;
              }
      }

      m_iv[      0  ]).iov_base   =    m_write     buf;
      m_iv[      0  ]-iov_len    =  m_write_idx;
      m_iv_count       =  1;
      return       true;
}

                                                 请求
/*                                                           的入口    函数 */
         void http_conn: :process()
{
      HTTP_CODE     read_ret      =    process_read();
      if  (   read_ret      ==  NO_REQUEST       )
      {
                   modfd(   m_epollfd,       m_sockfd,       EPOLLIN   );
                   return;
      I

      bool    write_ret      =  process_write(     read_ret      );
      if  (   !    write  ret   )
      {

## Page 336

篇  性 服务 编程
318 %* 第 二 深入 解析 高 能 器






 }

 modfd( m_epollfd, m_sockfd, EPOLLOUT );
I
—


15.6.2  main    BAY
       好        类   之 ，main后                                         负责 UO  写  清
定义
         任务                      函数 就 变 得 很 简单               了   ，它 只 需要 读     ， 如 代码
    所  示   。
        15-6

                           代码 清单     15-6 ”用                 的
                                  线程 池 实 现                       Web 服务 器

#finclude   <sys/socket.h>
#include    <netinet/in.h>
#include    <arpa/inet.h>
#include    <stdio.h>
#include    <unistd.h>
#include    <errno.h>
#include    <string.h>
#include    <fentl.h>
#include    <stdlib.h>
#include    <cassert>
finclude    <sys/epoll.h>

finclude    “locker.h"

finclude    “http_conn.h"

#define  MAX_FD        65536
#define MAX   EVENT  NUMBER      10000

extern  int   addfd(     int  epollfd, int     fd,   bool            );
extern  int   removefd(     int  epollfd,  int     fd  );

void   addsig({ int  sig,     void(  handler    )            bool restart = true }
{
       Struct      sigaction  sa;
       memset(  &sa,    "\0',    sizeof(  sa    )    );
       sa.sa_handler  =     handler;
       if(  restart  )
       {
              sa.sa_flags     |~ SA_RESTART;
       }
       sigfillset(  &sa.sa_mask     });
       assert(     Sigaction(    sig,     NULL     )   !=    =1   );
}

void   show_error(   int     connfd,  const     char*  info     )
{
       printt(           info    );

## Page 337

15 章 池和 池
第    ”进程
    线程   319




 send(  connfd, info, strlen( info , 0 );
 close{ connfd  );
)



int main(   int  argc,     char*     argv[]   )
{
    if(    arge  <=   2   )
    i
             printf(      "usage:     $s    ipaddress port_number\n",    basename( argv[0] )
             return     1;
    }
    const    char*     ip  =  argv[1];
    int    port   =  atoi(    argv[2]       );

    /*    0  SIGPIPE      信号 */
    addsig(     SIGPIPE,      SIG_IGN       );

    /*                 */
    threadpool<                       >*    pool  = NULL;
    try
    {
             pool    =  new   threadpool<       http_conn  >;
    }
    eatch(     ...   }
    (
             return     1;
    |

    /*                                          conn       HR */
    http_conn*       users    =   new   http_conn[     ED     ];
    assert(       users   );
    int        user_count  =  0;

    int   listenfd      =  socket(      PF_INET,    SOCK_STREAM,  0 );
    assert   (  listenfd      >=  0   );
    struct     linger     tmp   = {   1,    0 };
    setsockopt(        listenfd,                      SO_LINGER,  &tmp,  sizeof( tmp ) };

    int    ret  =   @;
    struct     sockaddr_in        address;
    bzero(     Saddress,      sizeof(       address ) };
    address.sin_family            =  AF_INET;
    inet_pton(       AF_INET,       ip,   saddress.sin_addr   );
    address.sin_port          =   htons(     port

    ret   =  bind(     listenfd,           struct   sockaddr* )éaddress, sizeof{  address ) );
    assert(     ret    >=  0  };

    ret   =  listen(      Listenfd,       5   );
    assert(     ret    >=  0  );

    epoll    event     events[      MAX_EVENT_NUMBER     ];

## Page 338

 *  第 二
 320    篇  性
**   一     深入 解析 高 能服务 器 编程



 int epollfd  = epoll_create( 5
         epollfd  !=  -1 );
 addfd( epollfd,  listenfd, false );
 http_conn::m_epollfd    = epollfd;

 while( true  )
 {
     int  number  =  epoll_wait( epollfd,   events,   MAX EVENT NUMBER, -1 );
     if  {    mumber  <  0 ) G& ( errno !=  EINTR )   )
     {
              printf(   “epoll  failure\n" );
              break;
                                                          。


     for (   int  1   =     O;  4    < number;     i++ )
     {
             int  sockfd        =                       fd;
                                     events[i).data.
             Lf(  sockfid       ==    listenfd    )
             {
                      struct         sockaddr_in       client_address;
     socklen_t client_addriength = sizeof( client_address };
                      int   connfd     =   accept(  listenfd, ( struct          )&client_address,
                                                     &client_addrlength       );
                      if    (   connfd      <  0 )
                      {
                                     printf(   “errno  is:    td\n", errno    );
                                     continue;
                      }
                      if{      http_conn::m_user_count     >=     MAX_FD      )
                      {
                                     show_error(    connfd,   “Internal     server   busy" );

                      }
                                     化 客户连接      */
                      /*    初始
                      users(connfd].init(              connfd,      client_address   );
             )
            else     if(          events[i]).events &  ( EPOLLRDHUP     |   EPOLLHUP |  EPOLLERR ) )
             {
                      /*                                PRR     */
                      users[sockfd].close_conn();
             和
            else     if(    events[i].events           & EPOLLIN  )
             {
                     /*
                     if(    users[sockfd].read()        )
                     {
                                     pool->append(     users  + sockfd      );
                     上
                     else
                     {
                                     users(sockfd].close conn();
                     }
         ]

## Page 339

15 音 池
    和 池
第 ”进程
    线程 321



             else if(  events[i].events  & EPOLLOUT )
             {
                       写的结果，决 定     是    否 关 闭 连 接 */
                  /*   根据
                  if(  !users[sockfd].write()    )
                  |

                  }
             }
             else
             {}
     }
 )

 close(  epollfd  );
 close(  listenfd  );
 delete  []  users;
 delete  pool;
 return  0;
)

## Page 340

_(no text content on this page)_

## Page 341

.

## Page 342

服 务  调制 、 试
    调     和 测试


在 前 面 的          章节   中 ，    我 们 已经 细致 地     探讨   了 服务 器 编程    的   诸多 方面 。现 在 我        们要 从 系统 的
角度 来 优化 、改 进服务            器  ，这   人包括   3 个 方面的内 容，系 统 调制 、服 务 器 调试和压力 测试                。
Linux     平台 的      一     个  优秀 特性是内 核 微        调 ，即 我们可       以        通过 修改 文件的 方式 来 调整内  核  参
数 。16.2 节 将 讨论 与          服务 器 性  能 相关 的 部   分内 核 参       数  。这 些 内 核 参 数 中，系 统 或 进程    能     打开
的 最 大 文件           符 数 尤 其 重要     ，    所 以我们 在  16.1 节 单     独 讨论 之 。
在服务 器 的 开发 过 程                    中    ，我们可 能  磁 到 各 种    意想不 到 的 错误。    一  种 调试        方法   是 用
tcpdump 抓   包    ，正 如 本 书 前 面                       章节 介绍 的 那样 。不 过 这 种 方法主要 用 于 分 析 程 序 的 输入和 输
出。对 于 服务 器 的 逻辑 错误 ，更 方便 的 调试 方法是 使用 gdb                          调试 器     。我们将 在 16.3 节 讨   论 如
何 用 gdb 调试 多 进程和多 线程 程序             。
编写压力 测试工具 通常被认为是服务 器 开发的                                  一  个 部  分。 压 力 测试工具 模拟 现实         世界 中
高 并 发 的 客户 请        求 ，    以 测试 服务      在 高压状 态 下 的 稳定         性。我 们 将 在 16.4 节 给 出     一 个 简单 的
压力 测试 程序    。

                   文件
16.1    最 大               描述 符 数

文件描述 符                是服务 器 程序 的        宝贵 资源 ，几 乎所有 的 系统 调用 都是和 文件描述符打 交道                     。
系统 分 配给 应 用 程序 的 文件             描述 符 数      是有限制        的 ，所 以我们 必须 总    是 关闭 那些 已      经 不  再 使
用 的    文件描述 符      ，  以 释放 它们 占用 的 资源       。     比                 如 作为 守护 进程运行 的服务 器 程序就 应 该总是
关闭 标准 输入   、标         准    输出和 标准 错误 这    3
Linux                                        对 应 用 程序能 打开 的最大 文件描述 符 数量有两 个层次 的 限制 ， 用户 级 限制和 系统
级 限制   。用 户 级 限制          是指 目标    用 户  运行 的所有 进程总共 能 打开 的 文件描述 符 数                 ，  系 统 级 的 限
制 是 指所有 用 户 总 共 能 打开 的 文件描述 符 数                 。
下 面 这 个          命令是 最     常用   的 查看 用 户 级      文件  描述 符 数 限制      的 方法:
$     ulimit     -n
    们   可 以 通过
                      如    下 方式
                                将用户     级 文件 描述 符 数 限 制 设 定
我                                                                  为max-file-number:
$     ulimit     -SHn   max-file-number
                 设置       临时      ，         的  session          。    永久                 描述
不 过 这 种                 是       的  只 在 当前               中 有    效  为     修改 用 户 级    文件       符 数
限制 ，可       在                             文件 中 加     如         项
    以 /etc/security/limits.conf                     人   下 两

    *  hard  nofile   max-file-number
    *  soft  nofile   max-file-number

## Page 343

                                                       第 16                    章        器     、     试和
                                                                                        ”服务
                                                                                                                       调制 调
                                                                                                                         测试  +    325

   第    一 行是指系统 的 硬 限制 ，第             二        行是软 限制 。                       们在        种
                                                                               我    7.4 节讨 论 过 这 两            资源 限制           。
   如 果 要 修改 系统 级 文件 描述 符 数 限制                          ，     则 可 以 使 用         如    下

   sysctl     -w                     fs.file-max=max-file-number
   不 过    该 命令           也 是 临时 更 改 系 统 限              制       。要 永久 更 改 系统 级 文件 描述 符 数 限                                制   ， 则 需要 在
/ete/sysctl.conf     文件中 添加 如     下 一 项 :

   然后     通过 执行 sysctl        -p                            使更改生效。
                                                                命令

                       参
16.2      调整 内 核               数

          所有的内核模块，         包 括 内      核                心        模块 和 驱动 程序              ， 在                                        文件
   几乎                                                  核                                都   /proc/sys                 系统下     提供    了
某 些 配 置 文件以 供用 户 调整 模块 的                   属性                 和 行为 。    通 常   一              个 配置 文件 对应 一         个内  核 参     ，     件
                                                                                                                              数     文
名 就是     参数   的  名 字  ，文 件  的     内 容 是 参数 的           。
                                                       值     我 们 可以 通过                  命令  sysctl     -a               查 看 所 有 这 些 内
人     参数 。本 节 将讨论 其        中和 网 络 编程               关系     较为 紧密 的 部分 内 核                参   数 。
核

16.2.1    /proc/sys/fs     目录 下 的 部 分 文件
   /proc/sys/fs     目录 下 的 内      核参数 都 与 文件 系统相关。对 于 服务 器 程序 来                                                             ，其 中 最 重要
                                                                                                          说                         的
是 如 下 两       个参数    :
   口 /proc/sys/fs/file-max，       系 统 级    文件          描述 符 数 限制              。直 接 修改     这 个             和 16.1 参数            节讨 论 的
    方法有相同的效果 〈 不 过 这 是 临时 修改 )。一 般 修 改 /proc/sys/fs/file-max 后，应
 修改
        用 程序 需要 把 /proc/sys/fs/inode-max               设置为新 /proc/sys/fs/file-max                                     3   ~~   44%, 7
        则 可 能 导致 i    节 点数 不够用        。
   口    /proc/sys/fs/epolymax_user_watches，            个 用 户                  能   够往    epoll 内 核事件
一                                                                                                         表中                  注册 的 事件
        的 总     。      它 是 指 该  用 户 打 开 的                  所    有 epoll   实例 总 共 能 监听 的 事件     数目             ，           而
                量                                                                                                             不  是 单个
        epoll 实例 能 监听 的 事件                      数目 。往 epoll           内   核 事 件 表 中 注册     一    个 事件 ，在                  32     位 系统上
                                                                          上则消耗          160 字 节 的 内                          核 空间。所
        大 概 消耗 90 字 节 的 内                  ，在 核 空间    64     位 系统                                                             以，    这
        个 内 核 参 数 限制       了 epoll             使 用 的 内 核     内 存 总 量      。
16.2.2    /proc/sys/net                       目录 下 的 部 分    文件
          中     络 模块        相关    参数 都             于                          目录
                                                                                            下，其
   内 核        网        的                   位           /proc/sys/net                         中和 TCPAP                      协议 相关 的 参数
                       目                                    和             在 前           的
                                                                                        章节
主要 位 于 如 下 三 个     子       录 中      core.  ipv4                                         中，我                               经介绍 过 这 些 子
                                                                   ipv6。      面         们     已
目录 下 的 很 多 参数 的 含义             ，                 现 在 再 总 结 一下    和服务      器 性   能       相关 的 部 分 参数     ，
                                                           定    listen    监听 队列         ，能 够    建立                           完整连接 从 而
   口 /proc/sys/net/core/somaxconn，                         指                            里                                          进人
                                  的 socket             的
        ESTABLISHED    状态                              最    大 数目         。    读 者 不     妨                          修改 该 参数并 重新 运行代码 清
        单 S-3，          看 看 其影响。
                                                                          定             监听 队列
                                                                         指              里，能够
                                                                                                              转移
   口 /proc/sys/neVipv4/tcp_max_syn_backlog，                                   listen                                         至 ESTAB-

## Page 344

    篇        性 服务器 与
    326 第 三 高 能 优化 监测


                                                                                   的 socket        的最  大     数目   。
            LISHED     或者       SYN_RCVD           状态
                                                                                            3
                                                                                                                     个 ，分 别 指定    socket 的 TCP 写    区
            /proc/sys/neUipv4/tcp_wmem，                                            它 包含            值                        一个                  缓冲
                         最小值、 默  认 值     和   最 大 值 。
            的
    加 /proc/sys/net/ipv4/tcp_rmem,                                                          3      值，                   一个                       冲
                                                                                                                   个 分 别指定    socket 的 TCP   读 缓  区 的
            最 小   值    、默 认 值和       最 大          值。  在代码                              清单        3-6 中  ，我 们 正 是         通过           修改 这 个 参数来 改变 接
                窗口大小的。
                                   收 通告
                                                                                                                                                  步标
    加 /proc/sys/netUipv4/tcp_syncookiecs，                                             指 定        是 否 打 开 TCP        同步        标签   〈syncookie)。     同
                            动 cookie 来 防止
                                                                                  一个监听     socket   因 不  停      地                        重复 接收 来 自 同一
            签 通过 局                                                                                                                             个地址的连接
            请求〈同        步                               listen  报 文 段 )，而 导致              监听 队列         溢出    〈 所 谓   的  SYN          )。
                                                                                                                                      风暴
    除了 通过 直接 修改 文件 的 方式来修改这 些 系 统 参数外 ，我                                                                            们 也 可 以 使     用                命令
                                                                                                                                  sysctl        来  修改
    它们。这 两种修改 方式 都 是 临时                           的                             。        永 久 的 修改  方法 是  在                                  文件中加入相应网络
                                                                                                                                      /etc/sysctLconf
        及其数值 ，         并 执行      sysctl                                            使
    参数                                       -p    之    生效                           ，    就     像 修 改系统     最     大 允许   打开       的 文件 描述 符 数   那样。

    16.3    gdb 调试

                                                        调试
                                                                                                 调试      我 读者                                   gdb 调
    Linux       程序     员 必 然 都 使             用 过 gdb                                  器来               程序。          们也假设          得基本的
                           断点，                                             查 看 变量
    试 方法     ，比 如 设置                                                              等。这 一     节 要讨论 的 是             如 何 使用 gdb      来 调试   多 进程    和多 线
程 程序 ，因 为 这 是 后 台 程 序 调试不可 避免 而又比较困难 的 部 分 。
        多 程序
        进程
    16.3.1                                                                  用 gdb 调试

            果 一 个 进程通过           fork       系统 调用 创建                                      了     子 进  程  ，gdb    会   继续   调试       原来的 进程   ，   子 进程 则
    如
    正常运行 。 那 么该如 何调试 子 进程呢                                                      ?   常用 的 方法          有 如 下 两        种。
    1.      单独 调试子进 程
                                                                                                                                  方法来    调试     。   举
    子 进 程       从本质        上 说 也 是             一  个 进程                             ，                      因 此 我们 可以 用 通用 的 gdb 调试            它      例
    说，如 果 要 调试 代码 清单  描述
    来                                        15-2                                  的  CGI        进程 池 服务 器          的 某  一 个 子 进程     ，    则 我 们 可以 先
          器，然后找到 目标                          子 进程                           PID，的         再      将  其 附加 (attach)        到    gdb 调试
    运行 服务                                                                                                                         器上，          具 体操 作
    如 代码 清单      16-1   所 示     。

                                                                                      附加子        进程    的PID                   来 调试
                                 代码 清单16-1                                  ”通过                                              子  进程
    S       .fegisrv          127.0.0.1     12345
    S$  ps    -ef   |   grep     cgisrv
    shuang             4182      3601       O     12:25                            pts/4         00:00:00       .fegisrvy     127.0.0.1  12345
    shuang             4183      4182       O     12:25                            pts/4         00:00:00       ./cgisrv      127.0.0.1  12345
    shuang             4184      4182       0O    12:25                            pts/4         00:00:00       ./cegisrv     127.0.0.1  12345
    shuang             4185      4182       0     12:25                            pts/4         00:00:00       ./egisry      127.0.0.1  12345
    shuang             4186      4182       0     12:25                            pts/4         00:00:00       .fegisrv      127.0.0.1  12345
    shuang             4187      4182       0     12:25                            pts/4         00:00:00       ./fegisrv     127.0.0.1  12345
    shuang             4188      4182       O     12:25                            pts/4         00:00:00       .fegisryv     127.0.0.1  12345
    Shuang             4189      4182             12:25                            pts/4         00:00:00       ./egisrv      127.0.0.1  12345
    shuang             4190      4182       0O    12:25                            pts/4         00:00:00       ./egisrv      127.0.0.1  12345

## Page 345

    第 章 器 、 试和   <>
      16 ”服务
           调制 调
             测试    327



     $     qdb
                                                         到  gdb 调试   器  */
     (gdb)      attach  4183  /*    4-224 4183 附加
     Attaching      to   process    4183
     Reading       symbols  from    /home/shuang/codes/pool_process/cgisrv...done.
     Reading        symbols    from  /usr/lib/libstdct++.so0.6...Reading     symbols from    /usr/lib/
             debug/usr/lib/libstdc++.s0.6.0.16.debug...done.
     done.
     Loaded     symbols     for     /usr/lib/libstde++.so0.6
     Reading       symbols  from    /lib/libm.so.6...(no           debugging symbols found)...done.
     Loaded      symbols    for /lib/libm.so.6
     Reading        symbols    from  /lib/libgcc_s.s0.1...Reading     symbols     from /usr/lib/debug/
    lib/libgcc_s-4.6.2-20111027.s0.1.debug...done.
     done.
     Loaded      symbols    for /lib/libgec_s.so.1
     Reading        symbols from    /lib/libe.so.6...(no           debugging symbols found)...done.
     Loaded      symbols    for /lib/libc.so.6
     Reading        symbols from    /lib/ld-linux.so.2...(no     debugging   symbols   found)...done.
     Loaded      symbols    for /lib/ld-linux.so.2
     0x0047c416     in   __kernel_vsyscall             {) 子进程中的断点
     (gdb)     b   processpool.h:264         /*   设置
     Breakpoint     1    at 0x8049787:       file processpool.h,     line    264.
         (gdb) c
     Continuing.

     /*                              telnet     127.0.0.1   12345
         预期 的 ，在 断 点 处 暂停 “/
     Breakpoint 1, processpool<cgi_conn>::run_child (this-0x9a47008) at processpool.h:264
     264
     (gdb) bt
     #0 processpool<cgi_conn>::run_child (this=0x9a47008) at processpool.h:264
     #1       0x080491fe    in processpool<cgi_conn>::run     (this=0x9a47008)     at processpool.h:169
     #2 Ox08048ef9 in main (arge=3, argv=Oxbfbc0b74) at main.cpp:138
     (gdb)
ee

                调试 follow-fork-mode
     2.     使用      器选项
                    器
                    的选项                                  在
     gdb      调试 follow-fork-mode 允许 我 们 选择 程序           执行 fork                         系统 调用 后 是 继续 调
    试父 进程       还是   调试 子 进程     。其  用 法 如 下         :

  (qdb)         set     follow-fork-mode     mode
                                值    是          和        和      。                              用
     JEP,       mode                            的 可 选 parent
                                                     child， 分 别 表示 调试 父 进程     子 进     程   还 是 使    前 面
                         使用           follow-fork-mode    选项 来 调试 子 进     程  ， 如 代码 清单16-2
    的 例子     ， 这 次 考虑
                            代码   清单     16-2        ”使用follow-fork-mode 选项 调试 子  进程

       $    gdb ./egisrv
  (gdb)         set     follow-fork-mode         child
  (qdb)         b  processpeool .h:264
       Breakpoint     1 at    0x8049787:     flle     processpool.h,   line  264.
  (qdb)         ry 127.0.0.1    12345
     Starting program: /home/shuang/cedes/pool_process/cgisrv 127.0.0.1 12345
          [New  process  4148
          send  request  to    child 0

## Page 346

    328 + 第 三篇“高性能 服务 器 优化与监测


    [Switching           to  process  4148

    Breakpoint           1, processpool<cgi_conn>::run_child         (this=-0x804c008) at processpool .h:264
    264                                                  .process();
    Missing         separate               debuginfos,  use:   debuginfo-install           glibe-2.14,90-24,
 fol6.6.i686
    (gdb) bt
    #0 processpool<cgi_conn>::run_child (this=0x804c008) at processpool.h:264
    #1 Ox08049lfe in processpool<cgi_ conn>::run (this=0x804c008) at processpool .h:169
    #2 Ox08048ef9 in main (argc=3, argv-Oxbffff4e4) at main.cpp:138
    (gdb)
一

                                     多              程序
    16.3.2                                    用 gdb 调试
                                      线程
    gdb     有 一 组命令 可 辅助多 线程 程序                       的 调试    。    下 面 我 们 仅 列 举其   中常用 的  一  些 :
    Ql   info  threads，      示                          所                      个     分                   们可以
                             显 当 前 可 调试 的 有 线程                  。gdb  会 为 每           线程 配 一  个ID，         我
            用 个              操作                        。      面有            线程      被            的
         使
               这         ID  来       对应 的 线程               ID 前 “*” 号 的       是 当前               调试 线程 。
         thread ID，         调 试 目    标 ID         的         。
                                                           指定
                                                           线程
                                                                      程序时，    默    认除  了 被 调试 的     线程  在 执行
    C) set scheduler-locking [offlonlstep]。 调 试 多线程
         外，其 他 线程也 在 继续 执行 ，                        但   有    的 时 候 我们 希望   只 让 被 调试 的 线程 运行         。 可    通
                                                                                                         这 以
                                                                           值:  o      不 锁定任何 线程         ，  即
         过 这 个 命令 来 实现 。该命令 设置                         scheduler-locking 的
         所 有 线程 都 可以 继续 执行 ，                      这 是   值                     调试                 会
                                                            默认 on   表示 只 有 当前 被 的 线程                继续 执行
         step  表示 在 单步执              行的 时      候  ，只 有 当前 线程 会      执行    。
    举例 来 ，如 果 要 依次 调试代码 清单                               15-6  所 描述   的 Web     器     为                   父线
                说                                                                           服务 〈 名 websrv) 的
    和子线程 ，      则 可以 采用 代码               清单    16-3 所 示 的 方法
    程                                                           ，
                                           代码 清单16-3 ”独立 调试 父 线程和子 线程
                                                         eer
                                                                                                          OC
       gdb    ./websrv
    (gdb)   b   main.cpp:130           /*  设置父线程中的 断            */
    Breakpoint           1  at DOx80498d3:     file  main.cpp,  line  130，
    (gdb)   b   threadpool.h:105           /*                         */
    Breakpoint 2 at Ox804a10b: file threadpocl.h, line 105.
    (gdb)  vr   127.0.0.1            12345
    Starting             program:    /home/webtop/codes/pool    thread/websrv     127.0.0.1  12345
    [Thread     debugging            using libthread_db     enabled]
    Using        host    libthread_db     library        "/lib/libthread  db.so.1".
    create        the    Oth   thread
    [New    Thread         Oxb7felb40     (LWP           ]
                                                  5756
            从 另一    个          使用                                             HAR,
        终端 telnet 127.0.0.1 12345
A,
    Breakpoint 1, main (arge=3, argv-Oxbff{ff4e4) at main.cpp:130
    130                                           if{     users[sockfd].read() )
    (gdb)        info    threads     /*                                       H1     */
    Id         Target        Id            Frame
    2          Thread                      (LWP     5756              0x00111416    in __kernel_vsyscall ()

## Page 347

    16 章 ”服务
    第    器调制 、 试和   “ie*
                 调
                测试      329


    *  1       Thread     Oxb7fe3700                 (LWP     5753         “websrv" main        argv=Oxbffff4e4)       at
    main.cpp:130
    (gdb)  set       scheduler-locking                  on       /*                          «/
    (gdb)    nm    /*                                                      «/
    132                                                 pool->append(        users    +     sockfd  );
    (qdb)     n
    103                             for   ( int      i  =     0; i     <   number;  i++  )
    (gdb)
    594                                   true )
    (qdb)
    96                            int     number     =  epoll        wait(   epollfd,    events,                   -1  );
    (gdb)
    “Cc
    Program     received              signal   SIGINT,           Interrupt.
    Ox00111416         in                                        ()
    (gdb) thread            2     /*                                            为2 */
    [Switching         to     thread        2  (Thread      Oxb7felb40       (LWP    5756
    #0             0x00111416    in     _    kernel    vsyscall          ()
    (gdb)  bt        /*                 显示 子 线程 的调用       */
    #0            DOx00111416    in                    vsyscall          ()
                                                                     2.1   () from   /lib/libpthread.soa.0
    #1             Ox44d91c05    in     sem_wait@@GLIBC
    #2             Ox08049aff    in     sem::wait       (this=0x804e034)     at     locker.h:24
    #3             Ox0804a0db    in     threadpool<http_conn>::run           (this=0x804e008)     at      threadpool.h:98
    #4                           in     threadpool<http_conn>::worker        (arg=0x804e008)     at       threadpool.h:89
    #S             Ox44d8bed3    in     start_thread        ()       from   /lib/libpthread.so.0
    #6             Ox44ccBa2e    in     clone  ()      from      /lib/libc.so.6
    (gdb)  nm      /*                                                      +/
     Single stepping until exit from function __kernel_vsyscall,
  which    has       no         line    number  information.
     Ox44d91c05 in sem_wait@@GLIBC_2.1 () from /Lib/libpthread.so.0
     (gdb)
eee
   后，                                     池    线程         程序                                ，      将        个          线程
    最      关     于 调试 进程 和                             池              的  一 个  不 错 的  方法      是 先     池 中 的 进程  数  或
           至     1，                   程序                             ，       如               是              的     逐步
    个 数减少            以            观察      的   逻辑 是        否   正       确    比 代码 清单      16-3     就 这 样 做      ;然后       增
加 进程或线程 的 数量 ，以 调试 进程 或 线程 的 同步是否 正确 。

    16.4   压力 测试

                                                                     如   IO   复 用  方式   ，   多 线程    、 多 进程  并 发  编程方式  ，
    压力 测试 程序 有                   很 多 种      实现 方式           ，    比
    以 及 这 些 方式     的     结合 使             。    过，                的                           程度             ，          和进
                                        用                              不 单 纯 VO 复 用 方式 的 施 压      是   最 高 的        因 为 线程
    程 的调度 本 身 也 是 要 占用 一定                   CPU 时间 的                 。      此，               用            一个通用
                                                                         因 我 们 将 使           epoll    来实现 的 服务
    器 压力 测试 程序 ，如 代码 清单                     16-4       所
                                                            16-4                       器压力测试程序
                                            代码 清单                                          ”服务
    Ol
                   <stdlib.h>
                   <stdio.h>
    #include       <assert.h>
    #include       <unistd.h>

## Page 348

    +  篇 高 性能 与
    330 > 第 一三 服务 器 优化 监测



    Winclude     <sys/types.h>
                 <sys/epoll.h>
    #include     <fcntl.h>
    #include     <sys/socket.h>
    #include     <netinet/in.h>
    #include     <arpa/inet.h>
    #include     <string.h>

    /*                                              */
    static     const    char*         request   =   "GET   http://localhost/index.html HTTP/1.1\r\
nConnection:

    int         setnonblocking(   int    fd )
    {
            int  old_option       =    fentl(   fd, F_GETFL    );
            int      new_option   =                 |  O_NONBLOCK;
            fentl{   fd,  F_SETFL,         new_option   });
            return
    }

    void    addfd(   int  epoll_ fd,        int   fd )
    (
            epoll  event          event;
            event.data.fd       =     fd;
            event.events     =    EPOLLOUT      |  EPOLLET |   EPOLLERR;
            epoll_ctl{    epoll_fd,         EPOLL_CTL_ADD,     fd,          );
            setnonblocking(           fd );
    }

    /*    疝  服 务 器 写 入  len 字 节 的 数据        */
    bool    write_nbytes(       int      sockfd,    const char*  buffer,   int  len )
    {
             int  bytes  write        =  0;
             printf(    "write    out    %d bytes    to  socket  d\n",    len,  sockfd );
             while(  1 )
             {
                  bytes_write         =  send(  sockfd,    buffer,  len,  0
                  if  (  bytes_write        ==  -1   )
                  {
                         return         false;
                  ]
                  else   if  (  bytes_write         == 0  )
                  {
                         return         false;
                 )

                  len  -=
                  buffer   =   buffer      +
                 if      len   <=     0  )
                 {
                         return       true;
                 }
              }
     }

## Page 349

16 章 器 、 试
第 ”服务    和
      调制 调
        测试 > 331


/*  MBS                      */
bool   read_once({          int  sockfd,  char*     buffer,  int   len  )
{
       int           bytes_read  =  0;
                     buffer,       '\0O', len   );
       bytes_read          =    recv(  sockfd,    buffer,  len,  0 );
       if (     bytes_read         ==  -1 )
      [
                  return       false;
      I
       else     if   (   bytes     read   == 0  )
      {
                  return       false;
      |
       printf{       “read      in  %d   bytes  from  socket  $d   with  content:  bytes_read,
                     sockfd,       buffer    );

       return         true;
]

               器    起                     ，    们      改变               压力
/*  向  服务         发   num    个  TCP   连接     我 可 以 通过       num  来 调整 测试      */
void   start_conn(           int   epoll  fd,   int num,   const   char* ip,  int  port )
{
       int      ret  =   0;
                                   in address;
       struct                sockaddr
       bzero(                       sizeof(    address )   );
       address.sin_family              =  AF_INET;
       inet_pton(          AF_INET,      ip, saddress.sin_addr     );
       address.sin_port             =  htons(   port );

       for   (    int    i   = 0;   i  <  num;  ++i  )
       {
                  sleep({ 1      );
                  int    sockfd     =  socket(    PF_INET,  SOCK_STREAM,  0   );
                  printf( "create         1  sock\n"  );
                  if(    sockfd     <  0  )
                  {
                             continue;
                  }

    if ( connect( sockfd, ( struct sockaddr* )Saddress, sizeof( address ) ) = 0 )
    {
    printf£( "build connection td\n", i );
                               addfd(  epoll_fd, sockfd     );
                  )
       }
I

void   close_conn(           int   epoll_fd,   int   sockfd )
{
       epoll      ctl({     epoll   fd,   EPOLL_CTL DEL,    sockfd,    0 );
       close(       sockfd     );
}

## Page 350

篇 能 优化 监测
332 *%* 第 三 高 性 服务 器 与



  int  main(     int     argc,      char*   argv[]          }
  {
           assert(      arqe   ==     4);
           int           fd epoll                             100
                epoll          =                  _create(            );
                             epoll_fd,      atoi(             argv[    3   ]   , argv[1],       atoi(     argv[2]     )
           epoll      event  events[        10000       );
           char      buffer[     2048  ];
           while   (  1   )
           {
                int     fds  = epoll        wait(              epoll_fd,   events,       10000,      2000 );
                for   (   int       i= 0;   i   <     fds;      i++    )
                {
                          int sockfd        =   events[i).data.fd;
                          if   (    events[i).events              &    EPOLLIN        )
                          {
                                    if    ( !   read_once(             sockfd,        buffer,   2048      )     }
                                    {
                                                close         conn(        epoll     fd, sockfd      )};
                                    }
                                    Struct      epoll         event        event;
                                    event.events              =   EPOLLOUT         |  EPOLLET   |    EPOLLERR;
                                    event.data.fd               = sockfd;
                                    epoll      ctl( epoll             fd,      EPOLL_CTL_MOD,        sockfd,      s&event );
                          h
                          else if(      events[({i]).events                &   EPOLLOUT    )
    {
    if ! write_nbytes( sockfd, request, strlen( request } ) )
    {
    close_conn( epoll_fd, sockfd };
    }
    struct epoll_event event;
    event.events = EPOLLIN | EPOLLET | EPOLLERR;
    event.data.fd = sockfd;
    epoll_ctl{ epoll_fd, EPOLL_CTL_MOD, sockfd, Sevent };
    }
                          else      if(   events[i].events                  &  EPOLLERR    )
                          {
                                    close_conn({                   epoll_ fd,            );
                          }
                  }
             )
   }

                            压力               程序         为                              测试
   下         考虑 使 用 该                  测试           名         stress_test)     来           代码        清单   15-6    所     的
      面                                            〈                                                                  Web  服
务 器 的          性。                            机器                                运行                    然 后   从            上 执行
                稳定 我 们 先 在 测试 ernest-laptop 上                                         websrv，                   Kongming20
                              器         发起                            。               作
                                                                                         如下:
stress_test，  向 websrv 服务                      1000     个 连接                             具体操
   §     ./websrv           192.168.1.108   12345           # 在   ernest-Laptop          上 执行        ，  听       端口12345
                                                                                                在 Kongming20
   $     ./stress  test      192.168.1.108          12345         1000         #                         上执行
      果                      器                          ，         么                                                 将     行
                      服务                                                       和                     这 两 个 程序         直 运
   如       websrv                   程序 足够 稳定                那         websrv          stress_test                     一    下
去 ，
   并 不 断交 换 数 据          。

## Page 351

                     第                         17 章      系统 监测 工具

 Linux           提供 了 很多有 用 的 工具，                 以  方便  开发   人员 调 试     和 测评 服务 器 程序     。娴 熟    的网   络 程
 序 员 在 开发 服务 器 程序    的 整个 过 程               中 ，      都  将不 断 地   使    用这 些    工具中 的   一 个 或者   多 个 来 监测 服
 器行为 。            其 中 的 某     些 工具 更是           黑客们  常用  的 利器  。
 务
     本                        将讨论几 个 最常用 的 工具 :                                                          和
                                                             tcpdump、nc、strace、1lsof、netstat、vmstat、ifstat
 mpstat。 这 些工具 都 支持 很 多 种 选项 ，不 过 我们                     的讨论 仅 限 于 其 中 最 常用          、    最     的   。
                                                                                                   实用
                                                                                     那些

 17.1        tcpdump

tcpdump 是 款经典 网 络抓包 工具 。 即 使 在 今天，我 们拥有
                     一      的                                                        像Wireshark   这 样 更 易于
 使用和 掌握的 抓 包工        具 ，tcpdump     仍然          是     网  络 程序  员 的 必 备 利器 。
 tcpdump         给 使用者 提供 了 大 量 的 选项 ，                   用 以 过 滤 数据 包 或   者    定制 输出 格式    。    前 面      中
                                                                                                        音节
 我 们 介绍 过 其中 的 一     些     ，    现 在 我 们 把 常见             的 选项 总 结   如 下  ，
                                                         名:          使用  数字 表示 端口
 口               使用     地 址 表示 主机    ，    而 不 是 主机                                   号， 而 不 是服务
         -mn，    名称。
     的网 卡 接口 。“-i any” 表 示 抓取所 有 网 卡 接口上 的 数据 包 。
口 -i， 指 定 要 监听
    输 出 一个稍微 详细 的 信息 ，例 显 IP 数据 TTL 和 TOS 信息
         -v，                                           如，    示            包中的              。
             AFT     DRE.

         -<，仅抓       取 指定 数量 的 数据 包         。
         -x，     以 十 六 进 制 数    显示       数据 包     的  内 容 ， 但  不 显示 包 中以 太 网 帧 的      头     部 信息 。
 中       -X，      与 -x  选项 类    似， 不 过 还 打印 每 个          十   六 进     制字 节 对 应  的 ASCII     。
                                                                                     字符
 C)-XX,           45-X     不 过 还 打印 以 太 网 帧              的    头  部 信息     。
         -s，      设 置 抓包时 的抓取 长     度       。 当 数据 包 的 长         度    超过 抓 取 长 度     时 ，tcpdump  抓 取 到 的 将
         是        截 断  的 数据    包 。 在 4.0  以   及 之 前 的 版 本        中    ，  默 认 的 抓 包    长度  是 68 字 节 。 这 对 于
 但
         IP,     TCP 和 UDP     等   协议 就 已 经 足够 ，                 对                   的
                                                         了        于       像DNS、NFS 这 样 协议     ，68      字 节
         通常不能 容纳     个 完整 的     数据              包    。 比 如 我 们 在  1.6.3    小 节 抓   取 DNS  包时，           使用
                     项    一                                                                           数据 就
                     试机    器                                               是         过
         了     -s 选     〈 测 emest-laptop 上                    ，tcpdump 的 版 本    4.0.0)。       不 40 之 后 的 版
         本，默 认 的抓包 长 度 被修改为                   65     535   字 节， 因 此 我 们 不 用      再 担心   抓 包 长 度 的 问题 了 。
                     值来显示    TCP 报 文 段 的 序号                  ，     而 不是      值。
 加 -S，            以 绝对                                                 相对
                                                的    格式    定向 到 某        文件
 口                将     的 输出 特殊
         -w，        tcpdump      以                       个                。
 口       -r，     从 文件 读 取 数据 包 信 息 并 显示 之               。
             使用      外，tcpdump            支持                              过      据                     式的操
 除了
                    选项      还                        用 表达 式 来 进一步          滤 数               包 。tcpdump 表达

## Page 352

    334        第 三     篇”高 性 能 服务器优化 与监测

作 数 分 3 ，类
     为 种 型 (type)、 方向 (dir) 和 协议 (proto)。 下 面 依次 介绍 之 。
              类 解 其 后 面 紧                      的 参数 的 含义 。tcpdump 支持 host.             net.             port 和
         口    型，         释                                                          的类型包括
              portranpge。   它 们 分 别 指定 主机 名      IP     地 址     )，          用 CIDR            方法 表示 的 网络地 址 ，闪
              以及                 。           如， 抓取                                     络      包，             使
               导                               整个 1.2.3.0/255.255.255.0                           网 上 的 数据 可 以
                                                  端口 范围 比 要
               如下命令:
              用

         $    tepdump  net       1.2.3.0/24
                     sre  tia     A        LAY  AK       dst   指定 数据包 的 目的             端。比  如       要 抓 取进人 端口
              13579  的 数据        包 ，   可 以 使 用 如 下 命令     :
         S    tepdump  dst       port  13579
                             定    目标 协议      。 比 如 要 抓取    所 有 ICMP 数据
         O 协议        指，                                                     包，     可以 使 用   如 下 命令 :
         $    tcpdump     icmp
         “EPR,  HOTTY            Fe            RE     SLE RE RL                    a                   tepdump
                                                                            包 and (a &&).             or
    支持      的逻辑    操作        符 和   编程 语言       中的  逻辑    操作    符           完全 相同 ，  括                      (MH
                                                              所    有           非 Kongming20 的 主机
    JD.  not  (或 者             比   如   要 抓 取 主机 ernest-laptop 和                        之间             交 换 的 IP
            包，
    数据 可以 使 用                如     下 命令   :

         5               ip  host     ernest-laptop   and     not Kongming20
                                                                                                             我
         如 果  表 达      式 比较 复杂             ， 那 么 我们 可以 使    用  括号 将 它们 分           组。    不 过 在 使用插 号  时，    们要
                                             ，  么     号     其               住          它被                   。比
    么 使用 反 斜 “\”              对 它 转            要 人 用 单 引     “'”  将                                          如
                                           义                                   插   ，以 避免 shell   所 解释
                      机          目标 端口         是      3389 或 22    的 数据
    要 抓 取     来 自主       10.0.2.4，                                          包，    可以 使   用如 下 命令 :
        $     tepdump    ‘sre     10.0.2.4     and (dst port   3389 or  22)'
     外 ，tcpdump 还 允许 直接 使用数据 包 中 的 部 分 协议字段 的 内 容来过 滤 数据包。比
         此                                                                                                  如，
        抓取               报文段，
    仅 TCP 同步 可 使 用 如 下 命令                      :

        §                'tcp[13]     &    2    != 0"
                             部     第           个 字 节 的
        这 是 因为 TCP               头     的       14      第     2 个 位 正    同步 标志  是    。    该命令 也 可以 表示 为 :
        $     tepdump 'tcp[tcpflags]           &   tcp-syn !=  Q'.
              后                  的 具体 输出 格式    除了     与     选项          ，   有 关外还 与 协议
        最                                                                                              有关。前 文中
                      ，tcpdump                                                                           我们讨论过
                                                                                       协议     输出 格式          ，
    IP.  TCP.   ICMP.     DNS 等            协议    的tcpdump      输出 格式    关 于 其他 。       的tcpdump
    请 读者 自己 参考 tepdump 的                   man     FA,

    17.2      lsof

        lsof  (list   open       file)  是  一    个 列  出当前 系统 打开 的 文件         描述      符 的 工具  。    通 过 它 我们 可以 了
    解   感 的       进程     打开      了 哪些 文件描述     符     ，    或 者 我 们   感兴趣 的 文件 描述 符 被 哪些 进程 打开 了            。
        兴趣
        lsof   命令 常用 的 选项 包括                   :

## Page 353

    第 17 音 ”系统 监测 工具 of 335


             -i， 显 示    socket      文件        述       符。  该  选项     的    使用         方法 是
      lsof -i 46 [protocol] (@hostname|ipaddr) (:service|port]
                         中 ，4 表示        IPv4    协议     ，6    表示     IPv6 协议               protocol
                 其                                                                          指定传输屋协议，                       可以 是
        TCP      或者        UDP ;    hostname                     指定 主机 名  ;     ipaddr   指定 主机 的    IP HOHE:  service         服
                                                                                                                             指定
        BH:         port      指定 端口                。      如，                                                                  的
                                                号     比 要 显示                    所 有 连接 到 主机              192.168.1.108 的 ssh 服务
        socket      文件描述 ，可 以 使 用命令
                                      符                          :
    $   lsof     -1@192.168.1.108:22
                      果 -i    选项    后                      不 指定任何 参数 ，  则 lsof      命令 将 显示 所 有     socket 文件 描述 符    。
                 如
                                                       进程        的 有 文件 描述 符                。
          -u，    显示 指 定 用 户             启 动 的      所   有              打开        所
    Ch    -<，    显 示 指 定      的       命令     打开 的      所            有 文件 描述         符 。 比 如 要 查看 websrv    程序 打开         了 哪些 文
        件               符  ，可以 使        用    如 下 命令     :
    »   lsof     -c    websrv
          -p，    显 示 指 定 进 程打              开 的     所   有 文件 描述 符     。
                        打开 目标 文件                                 符 的    进程          PID。
        -6，      仅 显示           了                      描述                       的
                                                                 的                 参数 ， 以 查看 哪些 进程   打开    了 该 文件。
    我 们还可以 直接 将 文件名作为 lsof 命令
    下 面          一个实例                      看                     器打开                哪些      描述 。      清单
         介绍                      ，  查                          websrv 服务     了      文件        符       具 体操 作 如 代码          17-1
    示。
    所

        代码 清单17-1 用 lsof 命令查看websrv 服务 器 打开 的 文件描述 符
一
    S$      -ef     |           websrv          #                               websrv 程序
   Ps                   grep                                                        的 进程号
    shuang             6346     5439         0  23:41          pts/3            00:00:00  ./websrv  127.0.0.1 13579
    $      sudo  lsof      -p   6346       #   A
    COMMAND PID USER FD TYPE DEVICE SIZE/OFF NODE NAME,
    websrv       6346 shuang        cwd DIR        8,3       4096               1199520
    websrv       6346 shuang        rtd    DIR     8,3       4096               2         /
    websrv       6346 shuang        txt    REG     8,3       64817              1199765   /home/shuang/codes/pool_thread/websrv
    websrv       6346 shuang        mem REG        &,3                                    /lib/1ld-2.14.90.s0
    websrv 6346 shuang mem REG 4,3 2000316 1319678 /lib/libe-2.14.90.so0
    websrv 6346 shuang mem REG 8,3 135556 1319682 /lib/libpthread-2.14.90.so0
    websrv       6346 shuang        mem REG        8,3       208320         =   1319681   /lib/libm-2.14.90.s0
    websrv 6346 shuang mem REG 8,3 115376 ”1319685 /lib/libpgce_s-4.6,.2-20111027.s0.1
    websrv 6346 shuang mem REG 8,3 948524 814873 /usr/lib/libstdct+.s0.6.0.16
    websry       6346     shuang    0u     CHR     136,3     0t0                6         fdev/pts/3
                 6346     shuang    lu     CHR     136,3     Ota                6         /dev/pts/3
    websrv       6346     shuang    2u     CHR     136,3                        6         /dev/pts/3
    websrv       6346     shuang    3u     IPv4    43816     0t0                TCP
    websrv       6346     shuang    4u                       0                  4447      anon_inode

    lsof     命令 的 输出 内 容 相 当                 丰 富      ，   其 中 每            行内  容 都 包含     如 下 字段 ，

## Page 354

 336          第 三 篇         高   性    能 服务 器 优化 与 监测

                                                 序所使用           的 终端               命令      〈   默 认 仅 显示
                                                                                           前     9                     个 字符
     CY COMMAND，                    执行程                                                                                  )。
     OPID，     文 件 描述 符所 属 进程的                                  PID.
                            有   该 文件 描述 符的 用 户的 用户 名                                       。
          USER，   拥
              文                  符         描述         。  其
          FD，       件    描述             的                          中    cwd 表示 进程 的        工作    目录 ，rtd              PAR       A ok.  txt
          表示 进程 运行 的 程序 代码                                  ，mem     表示 直接 映射 到 内存 中 的 文件 〈 本 例                       中     都 是 动态库     )。
              的                                                     是 以 “数字
                                            +                          访问权限 ”表示
          有                                                                        的，      其 中 数字 是 文件      述 符                   具体 数值 ，访
              FD                                                                                                    的                    问
          权限 包括                 (可 读                             (可写    )  Alu             读可写)。
                         r                  )、w                                    (可 在 本 例     中     ，0u、lu、2u                     分 别 表示
                                                                                                                         的监听    socket ;4u
          标准 输入          、      标 准 输出 和 标准 错误 输出                                      ，3u                  表示 处 于 LISTEN 状态
          表示    epoll       内 核 事 件 表 对 应 的 文件 描述 符                                    。
                                                                中                DIR
                                                                其
     口    TYPE，     文件 撒 述 符 的 类                            型  。                       是    目录 ，REG 是 普通 文件         ，CHR            是字符 设备
          文件                    是                                                                                        知类 。更 多 文件 描述 符 的
                                    ，IPv4
                                     IPv4           类 型  的  socket               文件 描述     符     ，0000 是 未     型
型请            参考 sof                    命令       的   man      Ft,                ix
          类
                     DEVICE，                             。      字符                 设备          块 设备 ，  其 表示 方法        主 设备              ，次
     口                                       文 件 所 属 设备     对 于                            和                        是 “           号      设
              号?”。  由 代码            清单      17-1              可   见       ，            器测 试 机  上 的 程序 文件 和     动态库 都存放     在    设备   “8,3”
          备
          中    。    中                                              个                       硬盘    表 示   是 该    硬盘    的 第         个     分 ，即
                                               其 ,“8” 表 示 这 是 一     SCSI                   ;“3”      这      上              3        区
                                           的     标准                     输入、 标 准    输出         和 标准 错误 输出对 应    的  设备          是“136.3"。  其
          sda3.  websrv         程序
          中 ,“136”                       表 示 这 是    一 个       伪           终       :“3”      表 示 它 是 第   3 个 伪 终   端     ， 即  /devpts/3。  关
     更多 细节，请 参考 文档 http: //www.kernel.org/pub/linux/docs/lanana/
  于 设备 编号 的
                                                                于     FIFO         类 型 的 文件     ，   比     如   管道
                                                                   对                                           和 socket，               该字段
          device-lisUydevices-2.6.txt。                                                                                                 将显示
              个 内   核 引用 目标 文件                        的       地     ，或 者 是其                1     节点
          一                                                        址                             号。
                                文 件            小    或 者     但                    。如 果      该 字段显示     “0t* ”或    者  “0x*
          SIZE/OFF，                        大                                值              为                          "，就 表示             这
          是       个 偏    移          ，   否 则      就    表示 这 是                     个 文件 大     小    。 对 字符 设备 或    者FIFO     类 型     的      定
         一                      值                                           一                                                           文件
          义 文件        大  小      没有      意义       ，   所 以       该     字段将显示                 一    个 偏移值  。
     口    NODE，       文 件           的 i         节 点       。     于                      Wika
                                                      号       对 socket,                    be “TCP”.
       NAME，             文 件        的   名 字    。
              们          用 命令
     如 果 我            使         telnet                向             websrv  服务 器 发 起 一     个    连接        ，则           再 次 执行 代码 清单 17-1 中
 的 lsof 命令时         ， 其 输出                        将多出 如 下      一  行      :
     websrv     6346            shuang           5u         IPv4      44288        Ot0     TCP
 (ESTABLISHED)
     该    输出                            表示 服务 器打开 了   一     个 IPv4          类 型    的       socket,     AYES,                   ESTABLISHED
     。 socket                                               端 socket                                                            远 端 socket
 状态 该                    对 应        的   连接        的   本                          地 址 是        (127.0.0.1，13579)，                        地址
 则   是 (127.0.0.1，48215)。

 17.3         ne

ne (netcat) 命令 短小 精干 、功 能 强大，有 瑞士军刀 ”的 美誉 。它 主要 被 用 来 快速构

## Page 355

                                                                                第17 章     系统 监测     工具 <>         337

    网络           。                                                              端口                  ，
                建
        连接 我 们 可以 让 它     以 服务 器 方式 运行                   ，     监听    某  个        并 接收 客户 连接                 因 此 它 可 用
        客户端程序。                   我 们 也 可以 使 之以 客户 端 方式 运行               ，    向  服务 器 发起       连接 并 收发 数据    ，       因
来 调试
        。
此 它 可以 用 来调试 服务器 程序 ，此时它有点 像 telnet 程序
   nc 命令 常用 的 选项包括 :
   口 -i， 设 置 数据 包 传送 的时 间 间 隔 。
     -1，以 服务 器 方式
    四                     运行，    监听 指 定 的端口              。nec     命令         默认 以 客户 端          方式 运行  。
              -k， 重 复 接受 并 处 理     某    个 端口 上 的 所   有 连接，    必 须 与 -1              一起使用。
    口                                                                           选项
            -n，   使
                        用 IP     地 址 表示 主机 ，而 不 是 主机
                                                                   名:使用    数字 表示 端口             ，      而 不 是 服务
                                                                                           号                名称。
                 “ine     节令以               端方式 运行 时        ， 强制 其 使   用    指定 的 端口        号    。3.4.2 小 节 中    我 们 就
                 使用   过 该    选项    。
                 设置本 地 主机 发 送出 数据
       -s$，               的                       包的 IP     地 址 。
    口 -C,，       将    CR     和     LF 两个字符 作为 行  结束      符    。
    Q)-U,                 UNIX     本 地 域 协 议 通信  。
                          UDP     Pi.     命令默认 使用 的 传输层 协议是                     TCP 协议
                          ne                                                               。
                      果                 客户
    中  -w，              如    nc       端在指定 的 时 间 内 未 检测 到 任何 输入                 ，    则 退出 。
                  “ne 客户端和 代理 服务     器 通信 时          ，    该     选项         指定 它们 之间 使           用  的   通信 协议  。     目
  nc支持                    的代理 协议 包 插 “4”(SOCKS v.4), “5”                            (SOCKS v.5)           和 “conneet”
       前
       CHTTPS proxy).     ne     默认 使 用 的代理 协议是          SOCKS v.5.
                                                         号。    比
                                                                           如，   要
                 指 定 目标代理服务 器     IP            地 址 和端口                             从                      连接
                          的                                                         Kongming20              到 ernest-
                   上      squid     代理 服务   器   并 通过     www.baidu.com 的 Web 服务                                 可 以 使
       laptop             的        ，                     它来访问                                               ，
                          如下命令:
       用

    9  nc      -x  ernest-laptop:1080     -X          connect www.baidu.com     80
                                                                     Pa                         tein,
                  MA      OLA                                  EARP RMR
       ernest-laptop        20     ~     50       之间 的 服务     ，    可 以 使        用    如 下 命令    :
    $  nc      -z  ernest-laptop     20-50
    举例 来           说  ，   我 们 可以 使     用 如 下 方式 来          连接 websrv 服务 器 并 向 它 发              送数据 ，
    $  nc      -C  127.0.0.1     13579    服务器 监听     13579 端口
    GET   http://localhost/a.html     HTTP/1.1       (B#)
    Host:     localhost
    (B=)
    HTTP/1.1          404     Not  Found
    Content-Length:       49
    Connection:           close

    The   requested       was         not  found on this     server.

    这 里 我 们 使用了           -C     选项     ， 这 样 每 次 我 们 按     下 回     车      键 向  服务 器 发 送      一 行 数 据     时    ，nc 客户
        都会给服务             器 额外
                          发送一个                                               是      服务
    端程序                                                                                       器期望的                  行
                          <CR><LF>，                      而 这 正 websrv                               HTTP            结

## Page 356

 338           第 三篇”高性           能 服务 器优化   与      监测

 东 符。发送完 第         三 行       数   据 之 后 ， 我们 得 到 了 服务 器 的 响应     ，    内 容 正 是 我 们     期 望       的 ，     服 务 器 没
 有 找到 被请求 资源 文件                            可                                                                 它
                 的               ahtml。            见 ，nc  命令 是   一 个 很 方便 的      快速  测试 工具     ， 通 过        我们
 能 很快找 出服务器 的 逻辑 错误                  。

 17.4           strace

     strace    是 测试 服务 器 性 能         的   重要 工具。它 跟踪 程序运行 过                     程 中             执行 的系统 调用和 接收 到
 的信号 ，并 将 系统 调用                  名、 参数、    返回        值及  信和号 名   输出 到 标准 输出 或      者    指定 的 文件     。
     strace    命令 常用的 选项包括       :
     口 统计 每 个系统 调用 执行 时间、执 行 次数和出 错 次 数              。
                    踪由           调用生成
     口   -f，   跟     fork                的子进程。
口 -t， 在 输出 的 每 一行信息 前加 上 时间 信 息 。
  - <，指定
     一个表达式，用 来 控制 何跟踪 系统 调用 〈 或 接收 到 的 信号 ，下 同 )。 其
                                                     如                                                       格
         ae:

       [qualifier=)
                                                                       read
     qualifier  可以 是     trace.    abbrev.     verbose.   raw.   signal, 和     write     中 之 一 ，  默 认 是 trace.
           用   于 进一步 限制          被跟踪 的 系统调用 的 符号 或 数值 。                它 的 两       个特殊 取 值是      all  和  none，
 value 是
 分 别 表 示 跟 踪所 有 由 qualifier      指定 类 型 的系统 调用 和 不 跟踪 任何该类 型 的系统 调用。                                         于
                                                                                                             关
 value   的 其 他 取    值     ，  我   们 简单  地 列举      一 些 ;
         -e                 跟踪 指定                系统 调用。
                           例
               trace=set，   只              的         如，-e     trace=open，close，read，write                   表示
         只 距 足              close.   read 和 write        这 四
                 open.    种系统                        调用     。
       @ -etrace=file，      只 跟踪 与 文件            操作 相关 的 系统 调用     。
       ®   -etrace=process， 只 中 中                进程 控制相关 的 系统调用     。
       @ -etrace=network，        只 跟踪 与 网 络          相关   的 系统 调用  。
           -etrace=signal，       只 中   与信号 相关 的 系统调用     。
       @ -etrace=ipc，        只 跟踪 与 进程 间 通 信 相 关 的 系统 调用     。
       @ -c    signal=set，       只 跟踪 指定 的 信和             比如   ，-e signal=!SIGIO           表示 跟踪 除 SIGIO 之 外 的
               有信和号。
         所
                                                                        如 ，-e  read=3，5                  表示 输出
       @ -e    read=set，     输 出 从 指定 文件中读 入 的 数据。例                                                       所有从文
                   和                    读 人 的 数据
                   3        5                  。
                                    输出
                                         指定 文件
     口   -o，   将 strace      的      写人  的                。
    命令 的 一 行 输出都包含 这 些 字段 系统 调用 名 、参数和 返回
     strace        每                                 ，        称                    值。              比 如 下 面 的
     :
     示例

       $ strace  cat     /dev/null
     open("/dev/null",           ©      RDONLY|O LARGEFILE)    = 3
     这行 输出 表示      :        程序 “cat /dewnull”        在 运行 过 程中执行了              open  系统 调用       。open    调用 以

## Page 357

                                                                                                  第 17  章  ”系统            监测     工具                339

  只 读 的 方式 打开 了 大 文件 /devnull，                               然 后 返回          了     个 值 为   3   的           描述 。 文件                             需 要 注意
                                                                              一                                      符                            的是，
  该 示例命令 将 输出 很多内 容 ，这 里 我                                   们 省 略 了很 多 次 要 的 信息                     ，         在 后 面 的       中，                 们    仅
                                                                                                                                                 实例 我
                                                                                                                                                  也
  显示 主题相关 的 内 容                 。
        当 系统 调用 发 生 错误 时        ，strace                      命令 将 输出 错误                标识  和 描述     ，   比 如 下 面           的 示 倒    ;
     $     strace      cat  /foo/bar
                                O_RDONLY|O LARGEFILE)               =         -1             ENOENT (No such     file     or     directory)
                                                                                                  比
     strace     命令 对 不 同 的      参数 类 型将有                     不 同 的 输出 方式                ，        如;
                                的字符串     ，strace             将 输出 字符 串 的      内     容      。   默 认 的      最     大 输出 长 度              32 是      字 节 ，
     中        于C       风格
                                                                                  如  Is    -!      命 令  在  运行 过 程            中     将  读 取        /etc/
          过 长 的 部 分             strace     会 使用                           。       比
          passwd 文件         :
     $     strace      ls      -1l
        read(4,                                                                                 4096 =  2342
                                件     名    并 不   被                   当
     需要 注意 的 ，                                               作                                  C 风格
  是                             文     strace                                  的    字符               串，其 内 容 总 是                       地被完整 和      输出。
     QO)   对 于               体                               ”输出          结构     个             ，          用                      个                  开。
                            结构     ，strace HA “{}”           该                      体 的 每                                             字段 并 “,” 将 每 字段
                                                             用    “…” 省 略 部分 输出                。    比
          对 于 字段较多 的 结构                       体  ，strace     将                                             如
        $  strace ls            -1    /dev/null
        istaté4d("/dev/null",     (st_mode=S_IFCHR|0666,                      st_rdev-makedev(1l,                   3),                ... =    0
                                                                                        第  1  个 参数        是字符                入参数 串 输            “/dew/
               上 面     的    strace     输出 显示                 ，lstat64             系统 调用 的
                                           则是            结构 体 类 型            的 输出 人    参数     〈 指
          null"，   第                   二 个 参数    stat                                                针    )，strace           示   仅 显  了 该       结构   体
                           的两个        ，                  和                    的是，                              调用            时，
          参数
                                字段                             st_mode                                                            失败            输 出 参数
                                                              st_rdev。                     需 要 注意 当 系统
          将 显示 为 传人前 的 值        ，
     口 对 于 位                    参数      信和号集类型                                                       用    ]”                     集合
                       集合                                    sigset_          t)，strace                                   将 “[ 输 出 该      中      所 有 被
                                位，并 用 空格将 每一             项 隔     。假          设 某   个 程序                         有 如
          置 1 的                                              开                                中                下代码:
        sigset_t       set;
        sigemptyset (sset);
        Sigaddset (éset,                SIGQUIT);
        Ssigaddset (4set,               SIGUSR1);
        sigprocmask(        SIG     BLOCK, &set,             NULL   };
               该            的                将
        则 针对
                       程序       命令
                                strace      输出 如 下 内 容

        rt_sigprocmask(SIG_BLOCK,     [QUIT                  USR1],          NULL,   8     =     0
        针对 其 他 参数 类 型           的     输出 方式                  ，请 读者 参考         strace HY    man                                                      KT
  序       到的                    该                            及                。            如，                        终端
      信号 ，strace 将 输出 信号 的 值 其 描述 比 我 们 在 一 个 上 运行 “sleep
接收
                   然令，    后 在     另 一 个 终端 上 使               用 strace  tr                                      “Ctrl+C”                      ik “sleep
  100”    命
                         进
                        观察 strace     的 输出     具体 操作         如         下  :
  100”              程以          。有
        5  sleep      100
           ps      -ef |  grep     sleep
        shuang         29127     29064     O     03:45       pts/7           00:00:00     sleep     100

## Page 358

+      第 三
340 “%* 一 篇 高性能 服务 器优化 与 监测



         Strace      -p  29127
     Process     29127       attached
     restart_syscall(<...        resuming         interrupted           call   ...>)   = ?      ERESTART_RESTARTBLOCK
(Interrupted      by     signal)                “ctrl+c”     Pi “sleep    100”
     ---   SIGINT         {si_signo=SIGINT,                                   ---
     +++   killed        by  SIGINT   +++
                     一   个使   用                   的        、              :              处
     下 面 考虑                    strace  命令
                                                    完整 具 体 的 例子               查看      websrv 服务 器 在           理 客户 连接
                用                。                          作如下;
和 数据 时 使             系统 调用 的 情况              具体操

     $  ./websrv         127.0.0.1        13579
     > ps  -ef       |   grep   websrv
     shuang     30526 29064       0       05:19 pts/7      00:00:00     ./websrv     127.0.0.1 13579
     $    sudo  strace       -p 30526
     epoll _wait(4,

         见                                                                    客户         。      的是
     可 ， 服 务 器 当前 正在 执行                      epollL_wait     系统 调用以 等 待                请   求          值 得 注意  ，epoll_
                                 识   epoll   内    核 事件     表     文件  符
wait   的 第   一  个 参数 〈 标                                                描述     )     的 值   是 4， 这 和 前 面 lsof  命令 的
              。              使
                                用                                    起                 连接      送        求
输出 一            接     来                                                                                       ，
           致         下           17.3 节 描述        的    方式对服务 器 发              一      个     并 发   HTTP           请 此 时
strace   命令 的 输出 如 代码            清单        17-2   所 示 。

                                                  代码 清单                            命令
                                                       17-2     strace        的    输出

     epoll     wait(4,       {{EPOLLIN,         {u32=3,                                  10000,     -1  =     1
                         (sa_family=AF_INET,                   sin_port=htons(41408),  sin_addr=
                inet_addr("127.0.0.1")},                   16    =  5
                     5,
     getsockopt              SOL_SOCKET,        SO ERROR,     [0],    4   =    0
                     5,      SOL_SOCKET,        SO_REUSEADDR,       [1], 4     =    0
     setsockopt
     epoll_ctl(4,          EPOLL_CTL_ADD,         5,         {EPOLLIN|EPOLLRDHUP|EPOLLONESHOT|EPOLLET,  {u32=5,
                     u64-4818361493978349573)})                =  0
     fentl64(5,          F_GETFL)    = 0x2      (flags     O_RDWR)
     fentl64(S,          F_SETFL,             O_RDWR|O NONBLOCK)  =   0

     epoll _wait(4,          ({({EPOLLIN,    {u32=5,       u64=-4818361493978349573)}},     10000,  -1  =     1
     recv(5,     "GET       http://localhost/a.html        HTTP"...,            2048,  0   = 38
     recv(S,     0xa601739e,     2010,          0 =    -1    EAGAIN   (Resource      temporarily unavailable)
     futex(OxBace034,            FUTEX_WAKE_ PRIVATE,      1        =   1

     epoll                   ((EPOLLIN,      {u32=5,             u64-85869934597})),  10000,    -1 = 1
     recv(S,     “Host:       localhest\r\n",          2010,    0   =   17
     recv(S,     Oxa60173af,     1993,          0 =    -1    EAGAIN   (Resource      temporarily unavailable)
     futex(Ox8ace034,            FUTEX_WAKE       PRIVATE,     1    =   1

     epoll                   ({EPOLLIN,      (u32=5,              u64=8589934597}}),  10000,    -1 = 1
     recv(5,     "\r\n",        1993, 0      =  2
                 0xa60173b1,     1991,          0 =    -1    EAGAIN   (Resource      temporarily unavailable)
     futex (0x8ace034,           FUTEX_WAKE PRIVATE,       1        =   1

epoll wait(4, ({EPOLLOUT, {u32=5, u64=5})}}, 10000, -1 = 1
writev(5, [{"HTTP/1.1 404 Not Found\r\nContent-"..., 114}), 1 = 114
     epoll    ctl(4,       EPOLL_CTL_MOD,         5,  {EPOLLIN|EPOLLRDHUP|EPOLLONESHOT|EPOLLET,         [(u32=5,
                                                           =        9

## Page 359

    第 17 章 ”系统监测 工具 341


           epoll_ ctl(4,     EPOLL_CTL_DEL,          5,     NULL) =  0
           close(5)  =    0
           epoll     wait(4,

           上 面    的输出分 为 五    个    部     分     ，  我 们 用 空   行将 每         个 部 分 隔 开    。
           第      部  分 从 第     次                       系统 调用 开始          。 此
              一                 一    epoll_wait                          次       epoll_wait           调用 检测 到 了 文件 描述
    符 3       的          。                                                          来看，          符                  的
           上 EPOLLIN 事件     从 代码 清单                     17-1     中 lsof    的 输出 文 件描述            3     正       是 服务 器
                                                                                    是     服务
    监听     socket。   因 此，这 个 事件 表示 有 新 客户 连接 到                           来 ，于       websrv       器对监听    socket    执行
    了 accept      调用 ，accept     返回     个         新  的  连接 socket，       其 值     为  5。接着   ， 服 务 器     这个新
                         一                                                                       清除            socket
    上 的 错误        ，设 置     其SO_REUSEADDR 属性                        ，    然 后往 epoll  AK SEE   HEP EME     socket   上 的
                                                                   两
                                                                  事件     最 后 设置 socket     为非 阻塞
    EPOLLRDHUP 和 EPOLLONESHOT                               个        ，              新            的。
                                                                         次  epoll_wait    调用 检测
           第  二     部分从 第       二  次 epollL_wait    系统 调用 开始。此                                   到了文件描述符            5
      的    EPOLLIN 事件     ，    这 表示 客户              端的第     一     行 数据 到 达 了     ，    于 是 服务 器 执行 了 两 次   recv     系统
    上
                          来接收数据。第   次      调用
    调用                                                 读取到      38   字 节 的 客户 数据        ，
                         一             recv                                             即    “GET http:   /Aocalhost/
    a.html         HTTP/1.1\r\n”.  43 —%K     recv  调用 则      失败     了     ，ermo    是EAGAIN， 这 表示 目前     没有       更 多
                     客户 数据 可
                     读。    此
                                   后，  服 务 器 调用
    的                                                   了   futex  函数 对 互        斥  锁 解  锁，以 唤醒 等 待 互    斥锁 的   线程  。
    可 见              线程
 ，POSIX                  库中的 pthread               mutex_unlock          在内部            调用了   futex 函数。
           第 三 、四 部 分    的     内 容 和 第 二 部分 类似              ，    我 们 不 再         。
                        五部分中        调用 检测 到 了 文件 描述 符                            上
           第             ，epollL_wait                                                 的  EPOLLOUT 事件   ，       这 表示工作
                                                                                        程开始执行    writev            系统
    线程正确 地 处理了 客户 请      求                ，    并 准备     好   了 待 发    送    的 数据   ，  因 此 主线                          调
FTES PS A HTTP 应
        符。最
        后 ，服 务器从 epoll 内 核 事件 表 中 移 除 文件 描述符5 上 的所
    有 注册 事件        ，                并 关闭 该 文件 描述 符  。
           由 此 可 见       ，strace 命令 使 我          们  能   够   清楚    地 查看 每 次 系统 调用 发      生    的   时 机   ，以     及 相关 参数
        值，               更                。
    的 这 比 用 gdb 调试
                                   方便

    17.5          netstat

           netstat 是 一     个 功能 很强大 的 网 络信息 统计 工具                        。               它 可以 打印 本 地 网 卡 接口 上 的 全 部 连
                                                   等    。 对 本 书 而        ， 我 们 主要
    接 、路 由 表 信 息 、网 卡 接     口     信 息  言                                                利用  的是 上 述 功   能 中 的 第  一 个 ，
                                                       竟，             要 获得
                                      路由                                 表 信 息 和 网卡 接口信息         ，我
                                                                                                 们                 可以
    即 显示      TCP                         连接 及 其 状态信息 。毕                                                       使用输
    出 内 容 更          的                                 。
                  丰富     route     和   ifconfig       命令
           netstat                 命令 常用的 选项 包括  :
                       用                          ，                     名， 使用数字表示
                     使                                                                  端口 ，                      服务
              -mn，       IP     地  址 表示 主机             而 不 是 主机                                  号 而 不 是       名称。
           加  -a，    显 示 绪 果 中 也 包含 监听 socket.
                     仅 显示     连接
              -t，        TCP     。
           加         显 示 路 由 信息
              -r，                   。
                     显 示 网     卡                接口 数据 流量
              -i，        的                             。

## Page 360

342           第 三  篇      高 性 能    服务 器 优化 与    监测

                  隔                     输出
    口     -c，     每      1s            一次，
                  示      socket 定时
    口             显                   器 〈比如 保 活               定时  器 )     的 信息
          -o，                                                                。
    口     -p，     示                   的    的                       字。
                 显 socket     所 属 进程      PID                     和 名
                                          器，    并 执行          telnet   命令 对 它 发起    一  个 连接 请 求    :
    下 面 我        们运行      websrv    服务

    §     ./websrv       127.0.0.1  13579     上
    $     telnet         127.0.0.1  13579
    然后执行命令                          netstat -nat|grep 127.0.0.1:13579   查看 连接 状态       ，   结 果 如 下 :
    Proto     Recv-Q      Send-Q    Local     Address                     Foreign     Address        State
    top              0           0  127.0.0.1:13579                       0.0.0.0:*                  LISTEN
    tep              0           0  127.0.0.1:13579                       127.0.0.1:48220             ESTABLISHED
    top              0           0  127.0.0.1:48220                       127.0.0.1:13579             ESTABLISHED
                          可见              的 每 行 输出               都 包含如        个 字段     默 认 情况
    由         以         上 结果  ，netstat                                 下 6        〈             ):
    口 Proto，           协 议 名  。
    口     Recv-Q，socket        内 核 接收     缓冲                   区 中 尚未 被应用     程序读 取 的      数据    量
    口     Send-Q，        未 害               方确认 的 数据 量  。
    口     Local Address，       本 端的        IP 地    址和          号。
    (J                                   对 方的                  地址和 端口
          Foreign Address，                IP                         号。
                                                                    协议， 如     UDP 协议   ，这     字段
    D State,      socket      的 状态 。对 于 无状态                                               一               将显示 为。而
          对 面向 连接 的 协议 而 盲                        ，netstat     支持 的               State 包 括 ESTABLISHED、SYN_SENT、
          SYN_RCVD、FIN_WAIT1、FIN_WAIT2、TIME_ WAIT,                                       CLOSE,     CLOSE   WAIT.
          LAST_ACK,                 LISTEN,       CLOSING,    UNKNOWN.                     3-8 中      的     同名 状态
          一致     9。
       上 面 的 输出 中           ，  第    1    行  表 示 本 地     socket       址
                                                                 地        127.0.0.1:13579     处 于 LISTEN KAS,  Ff
              待任何远端            FA         表示            )     对 它    发  起
等                                                                         连接    。
                               (      0.0.0.0:*                               第     2  行  表 示     服 务器和 远 端   地 址
                            了一个连接。第       3              行    只是 从 客户 端的 角度 重复 输出 第 2                   行 信息 表示 的
127.0.0.1:48220    建立                                                                                           这
          ，                                                   服务     程序                           客户
                                                                                           端程序                 的
个 连接                             因 为 我 们 是 在 同一 台 机 器 上 运行                                                     。    器 (websrv) 和    (telnet)
    在      服务                   器 程序 开发 过 程 中，我 们    一   定    要 确保 每 个        连接 在  任  一 时 刻 都     处于 我 们 期 望 的 状
                                   我们应该 习惯
                                        于使用 netstat     命令
态 。因 此                                                   。

17.6          vmstat

 vmstat          是 virtual  memory        statistics 的 缩写                                                       ， ，它 能 实时 输出 系统 的 各 种 资源 的 使 用 情况
                              使用      、CPU    使 用    率以 及      UVO           使
比如 进程         信息   、     内 存                                         用    情况 。



          SYN_RCVD        CLOSE 分别对 应 图3-8     中 的      SYN_RECV               CLOSED，UNKNOWN
©                    和                                            和                    示未知状态。

## Page 361

17 章 ”系统 工具
 第     监测  ** 343


                                                           参数
 vmstat     命令 常用的 选项                      和              包括:
                  示系 统 自     启        动以 来 执行
 口 -f，     显                                            的    fork KR.
 口   -<s，  显 示 内         存 相 关 的          统计 信息以 及 多 种 系统活动                             的              〈    如                                次数)。
                                                                                                       比        CPU 上 下 文 切换
      -4，   显 示          盘 相关 的 统计 信息                   ，
 口
 加   -5p，   显 示 指        定    磁 盘分 区 的 统计 信息              。
      -$S，         用                       显示。            数                                                                             000     和
 口 使 指定                      的    单位 来                  参        k、K、m、M                      分 别代表 1000、1024、1                               000
     1    048  576    字 节     。
 D delay，                          采 样间隔 〈 单 位    是 s)， 即 每隔              delay               的 时间 输 出   一  次 统计       信息    。
      count，          样 次         数    ，即   共和输出    count        次 统计 信息                。
 默认 情况 下，vmastat                                                      输出 的 内 容 相 当 丰 富。请 看 下 面 的        示例 ;
                                                                      输出
 $         vmstat  5 3   ¢     每       5      输出 一 次 结果，           共      3           次
 procs                         -一 一        一一 一 一    -Sap--
 xr   ob      swpd       free  buff              cache                si            so  bi         bo       in  cs           us      sy   id  wa
 0    0       0          714864          48088  1486188               0             0   12         3        149        280    0     1     99   0
 1    0       0          66548           48088  1494640               0             0   Oo         0        454        619    0     0     99   0
 0    4       0          74608           48096  1486188               90            0    0         10       289        339    O     0     99   O

 TE, 381                 行输出 是 月系统 司 动以 来                          的                            平均结果，而 后 面的                   输出 则 是 采样 间隔 内 的 平均
结果     。vmstat     的 每条和                   都包含
                         输出                6     个 字段              ， 它们 的 含义 分 别 是                   ;
                                程信息    .“r”                     表 示 等 待 运行 的 进程 数目                     表 示 处 于 不 可 中                      睡眠 状态 的
     procs，        进
     进程 数目           。
 QO  memory,                                                                 AAA, A ETS
                                                                                    YW  (KB)。“swpd”                               表示 虚拟 内 存 的 使用数
     .“free” 表 示空闲内 存 的 数量 .“buff” 表 示 作 为“buffer cache” 的 内 存 数 量。从
     磁盘 读 人 的 数据 可 能 被 保持                                        在 “buffer cache”       中     ，以 便 下        一    次     快速 访问     .  “cache”     #
     示 作 为“page cache”                     的 内存 数                量       。 待  写     人 磁盘 的 数据               将 首先           被     放 到 “page cache”
  中，然后由 磁盘 中 断 程 序 写人磁盘 。
     都是KB/s。“si” 表 示 数 据 由
口 swap，交 换分区《〈 虚 拟 内 存) 的 使用信息 ，各 项 的 单位
     磁盘 交换 至 内 存 的 速率                               ;“so”    表 示 数 据 由 内 存 交 换                                               磁盘 的 速率 。如 果 这 两 个值经
          发生变化             ， 则    说 明 内 存 不 足           。
     常
      io，          设备             用        ，                是                                          设备 人                          速率
 口 块         的 使 信息                                   单 位                               表 示 从 块                 读      块的                :“bo”
     示 疝 块 设备 写人块                         的速率    。
                     系
                                     信息 .。“in”                    表 示 每 秒 发 生    的  中 断 次          :“cs”    表 示 每 秒 发 生 的                   下 文 切
 口    system，            统                                                                    数                                          上
                                       数。
     换 《 进 程 切 换 )           次
                       使
                                           信息 .“us” 表 示 系            统 有 进程 运行                         用               的            CPU
 Qicpu,        CPU           用                                              所                                在 户 空间       时 间 占              总 运行
                                                                                                                                         时间的比
     时 间 的 比例                ;,“sy”           表 示 系 统       所有 进程 运行                   在内                核 空间的 时间 占    CPU                     总运行
                         示                处     空闲      状态 时间 占                               总 运行
     例                 表                                                                                               时间的比例      :“wa”      表
          :“id”              CPU             于                     的                   CPU                                                   示 CPU
                             的时间          占   CPU       总 运行
     等    待   VO 事件                                                  时间的比例              ，

## Page 362

    344           第 三       篇           高    性   能     服务 器 优化 与        监测

     过 ， 我 们可 以 使 用 iostat 命令 获得 磁盘 使用 的更多 信息，也 可以 使用mpstat 获得
           不                                                                        情况
    CPU      使 用 情 癌           的      更 多 信息         。vmstat         命令    主要 用 于 查看 系统 内 存 的 使 用 情况               。

    17.7      ifstat

                    是 interface             statistics         的 缩写 ，它 是                                       个 简单 的网络 流量 监测 工具。其常用
           ifstat                                                                  一                               的    选项         和
    参数 包括     :
           口 -a，监 测 系统上 的所有 网 卡 接口                                      。
           QO -i，   指 定 要 监测                       的   网  卡 接口      。
           口        在 每 行输出信息 前                                加 上  时 间
                                                                                    的               KB/s。
           L)-b,    LI      Kbit/s                                            为 单位显示 数据 ，而 不 是默认
                                                                                 每 delay       的     时 间 输 出一  次 统计 信息   。
                delay，      采 样间隔 〈 单 位                            是 s)，     即   隔
           口    count，      采 样 次                    数 ， 即 共 输 出     count               次 统计 信息    。
           举例 来 ，我 们 在                                     机器                                       如下命令:
                       说                                                              测试 ernest-laptop 上 执行
           $    ifstat      -a        25          #    每   局 2   秒      一     次 结果 ，   共 输出         次
                                                                        出                            5
                          lo                                     etho
  KB/s                    in            KB/s         out     KB/s in           KB/s     out
              8.62                             B.62            124.71  515.74
              7.46                             7.46            125.50  510.30
              1.79                      1.79                   126.87  497.57
              8.10                             8.10            127.82  526.13
              9.53                             9.53            130.10  516.78

   从 输出 来 ，emmest-laptop 拥有 两个网 卡 接口 虚拟
                                 看                                                        :              的回路接口   lo 以 及以 太 网 网 卡 接口
                                                     都以              为 单位显示
    eth0。ifstat       的每条 输出                                                        各网卡接口上 接收                  和 发  送    数据 的 速率 。因
                                                           KB/As                                                                 此，
      用                                                                          服务                 输入     出     。
使 ifstat 命令 就 可以 大 概估计 各 个 时 段 器 的 总 输
                                                                                                        、    流量

    17.8          mpstat

                                                                                                                                  的
           mpstat     是 multi-processor statistics                      的 缩写 ，它 能实时 监测 多 处 理 器 系统上 每个              CPU             使
                                            和    iostat                               命令 通常
                                                                        都集成在包       sysstat              中 ， 安
                                                                                                               装 sysstat      即 可 获得
    用 情况      。mpstat                      命令                                                                      这两个
                                                 用法是                                                命令
    令      。mpstat              命令    的 典型                     (mpstat    的         选项    不多，            这 里 不 再 专门 介绍   ):
           mpstat     [-P                  ({|ALL}]    [interval          } [count]
                                      要监控的           CPU 号 0            CPU 个
           选项 P 指定                                                      一           数    -1)，           其 值“ALL” 表 示 监 听 所 有 的 CPU.
     参数
        采样 〈 单 位 是 s)， 即 每 interval 的 时间 输 出 一 次 统计 信息 。count 参数
    interval           是                间隔                                隔
              次数             ， 即      共和 输出                    次 统计 信息 ，但                            最  后     还 会输出
    是 采样                                 count                                            mpstat                  这 count 次 采样结果 的 平
    均值 。与                 vmstat      命令 一 样         ，mpstat         命令 输出 的 第      一          次     结果 是 自        系统 启动以来 的 平均 结果，而
    后 面               〈《count-1)      次 输出结果               则 是 采样 间隔 内 的            平均              结果 。
                                                           机器                                          上 执行
           举例 来             ，    我 在 测试
                       说                们                        Kongming20  如下命令:

## Page 363

 第 17 章 系统监测 工具 345


   $    mpstat      -P ALL     5    2  #                                                           2 次
   Linux    3.3.0-4.f¢16.i1686            (Kongming20)                                06/25/2012         _i686_  2 CPU)

   CPU             tnice              tsys   tiowait                              tirq  t%tsoft  tsteal  tquest                tidle
   all      6.69   0.00               16.16   0.00                                0,00   7.65     0.00    0.600                69.60
   0        §.00   0.00               13.20   0.00                                0.00   7.20     0.00    0.00                 74.60
   1        8.09   0.00               18.75   0.00                                0.00   6.09     0.00    0.00                 65.07

   CPU      tusr                             tiowait                                     tsoft   tsteal  %quest                tidle
   all      8.05   0.00               19.08   0.00                                0,00   8.05     0,00    0.00                 64.81
   0        5.81   0.00               16.83   0.00                                0.00   B.42     0.00    0.00                 68.94
   1        10.24  0.00               17.02   0.900                               0,00   7,86     0.900   0.90                 60.88

   Average:
   CPU      %usr   tnice  sys                tiowait                              tirgq  tsoft   %steal                        tidle
   all      7,32   0.00               17.62   0.00                                0.00   7.85     0.00    0.00                 67,17
   0        5.41   0.00               15.02   0.00                                0.00   7.81     0.00    0.00                 71.77
   1        9.17   0.00               19.989  0.00                                0.00   7,97     0.00    0.00                 62.97
   WY BARAT.                                                   TET                   PAT eR.                AUK         EA    AB    3   FRA
 息  ， 每  条 信息 都 包含               如  下 几 个 字段                   :
                                                                    CPU是哪个        的 数据 .。“0”   表 示 是        第     1  个  CPU 的     数据 ,“1” 表
   口 CPU，           指 示 该 条 信息
           第                                 数据                         则 表示
                                                                                     是这两个             数据
         是      2   个  CPU     的                                    ,“all”                   CPU                    的平均值。
                      了 nice          值       为  负     的 进程 ,系统 上 其他 进程 运行 在 用 户 室间 的
                                                                                                                            时间占     CPU   总
   口     %usr，        除                                                                                                                   运
         行 时 间 的                 。
                                比例
                                     为负的 进程 运行                          在         用 户空间             的 时间 占 CPU                  总 运行
   口 %nice，nice 值                                                                                                             时间的比例。
                                                                                            时间 占 总 运行
                    系          所 有 进程 运行                                在       内         核 空间 的                                   时间的比例 ，但
         %sys，             统 上                                                                         CPU                              不包括
                和 软件 中 断 消            耗 的CPU 时
                                                                                                      时间的
   口                           等 待        磁盘           操作 的 时间 占                            总 运行                       比例
         %iowait，CPU                                                                 CPU                                。
                                                                                                                       运行
   口 %irq，CPU                用 于 处 理 硬件 中 断 的 时间 占 CPU                                       总                     时间的比例。
     时间的比例
口 %soft, CPU 用于 处 理 软件 中 断 的 时 间 占 CPU 总 运行 。
     一
                               物理 CPU              可以 包含 CPU， 这                                          一    对 虚拟 CPU 由 超级 管理            管
   口 %steal，               个                                                      一对虚拟                                               程序
         理  。当 超级 管理           程序         在        处           理        某      个  虚拟 CPU 时   ，     另 外                        个 虚拟 CPU 则 必须
                                                                                                         一                             等待它处理
                               。                               待                                              。                  时 间
         完成 才能 运行              这 部 分 等                                       时 间  就 是 所谓 的 steal     时间     该 字段 表示 steal           占   CPU
                                时间的   。
         总运行                   比例
                                                   的 占 CPU 总 运行 的                                           。
     口   %guest，       运 行 虚    拟     CPU                      时间                                  时间        比例
                                                   占 CPU的时间             总运行
                                                                                     时间          的 比例
   口 %idle，         系 统 空闲                                                                          。
       所               输出 字段              ，        我 最 关心
                                                                                  的是                      和              它们   基本
     在 有 这 些                              中                    们                      “user.   “sys         %idle。                   上反映 了 我
 们 的 代码 中 业务 逻辑代码 和 系统 调用 所 占                                                     的  比例 ，以 及 系统             还能 承受 多 大 的 负载          。很 显然 ，
                    中，   执 行                           系统调用                 占用       时
                                                                                      间  比 执行    用      户     业 务      辑占 用   的 CPU 时
          输出                                                                                                                             间要
 在 上  面的                                                                的 CPU                                                            多。
 这 是 因为 我 们 在                   该 机器                   上 运行 了  16.4     节 介绍 的 压力 测试 工具                     ，    它 在 不   停地   执行 recv/send 系
          统 调用 来 收发 数据 。

## Related pages

_To be filled by downstream LLM agent during entity/synthesis ingest._

## Source

- Local path: `[[/private/tmp/lfs_pdfs/books/Linux高性能服务器编程.pdf]]`
